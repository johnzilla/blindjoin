---
phase: 08-public-endpoint-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - coordinator/Cargo.toml
  - coordinator/src/config.rs
  - tests/integration/round_bootstrap.rs
  - tests/integration/full_round.rs
autonomous: true
requirements: []
tags: [config, foundation, tower, rate-limit]

must_haves:
  truths:
    - "D-04: `CoordinatorSection` exposes four operator-tunable knobs — `rate_limit_info_per_min`, `rate_limit_writes_per_min`, `request_timeout_secs`, `max_concurrent_connections`."
    - "D-04: Each new field has a `#[serde(default = \"default_xxx\")]` annotation pointing at a default-fn returning the literal from CONTEXT D-02/D-04 (60/30/30/256)."
    - "D-04: All four new fields are reachable as `BLINDJOIN__COORDINATOR__*` env-var overrides via the existing `Environment::with_prefix(\"BLINDJOIN\").separator(\"__\")` source — no `load()` changes required."
    - "D-05: `coordinator/Cargo.toml` declares `tower_governor = \"0.8\"` and enables `tower-http` feature `\"timeout\"`."
    - "Every existing `CoordinatorSection { ... }` literal in tests is updated — `tests/integration/round_bootstrap.rs:117` AND `tests/integration/full_round.rs:58,448,814,1279` — with the 4 new fields, so `cargo build --all-targets` succeeds."
    - "`CoordinatorConfig::with_defaults()` literal in `coordinator/src/config.rs` includes the 4 new fields with D-02/D-04 defaults."
  artifacts:
    - path: "coordinator/src/config.rs"
      provides: "CoordinatorSection with 4 new fields + 4 default-fns + with_defaults() literal"
      contains: "rate_limit_info_per_min"
    - path: "coordinator/Cargo.toml"
      provides: "tower_governor dependency + tower-http timeout feature"
      contains: "tower_governor"
    - path: "tests/integration/round_bootstrap.rs"
      provides: "Updated CoordinatorSection literal with new fields (1 site, ~line 117)"
      contains: "rate_limit_info_per_min"
    - path: "tests/integration/full_round.rs"
      provides: "Updated CoordinatorSection literals (4 sites at lines 58, 448, 814, 1279)"
      contains: "rate_limit_info_per_min"
  key_links:
    - from: "coordinator/src/config.rs"
      to: "BLINDJOIN__COORDINATOR__* env overlay"
      via: "existing Environment::with_prefix source at lines 84-95 — unchanged"
      pattern: "Environment::with_prefix"
---

<objective>
Foundation wave for Phase 8. Adds the four operator-tunable knobs from CONTEXT D-04 to `CoordinatorSection`, bumps `coordinator/Cargo.toml` to depend on `tower_governor = "0.8"` (D-05), and enables the `"timeout"` feature on the existing `tower-http` dependency. No runtime behavior change — this plan exists exclusively to enable Plans 02 and 03 to wire up middleware without compile breakage.

Purpose: Plans 02 and 03 read from these config fields; without them they cannot compile. This is the smallest possible foundation that unblocks parallel execution of the two downstream waves.
Output: Cargo dependency surface ready, config schema extended, all existing test literals updated to include the new fields so `cargo build --all-targets` still passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-public-endpoint-hardening/08-CONTEXT.md
@.planning/phases/08-public-endpoint-hardening/08-RESEARCH.md
@.planning/phases/08-public-endpoint-hardening/08-PATTERNS.md
@coordinator/src/config.rs
@coordinator/Cargo.toml
@tests/integration/round_bootstrap.rs
@tests/integration/full_round.rs

<interfaces>
<!-- Existing patterns the executor must mirror. Extracted from coordinator/src/config.rs:24-36 -->
<!-- Each new field gets a serde-default annotation + a default-fn below the struct. -->

From coordinator/src/config.rs:24-36 — analog field + default-fn pattern:
- `#[serde(default = "default_ban_file_path")] pub ban_file_path: String,`
- `fn default_ban_file_path() -> String { "ban_list.jsonl".to_string() }`

From coordinator/src/config.rs:101-124 — `with_defaults()` literal contains every field by name.
Every existing `CoordinatorSection { ... }` literal in tests must be similarly extended or compilation breaks.

Known `CoordinatorSection { ... }` sites in the workspace (verified via grep):
- `tests/integration/round_bootstrap.rs:117`
- `tests/integration/full_round.rs:58`
- `tests/integration/full_round.rs:448`
- `tests/integration/full_round.rs:814`
- `tests/integration/full_round.rs:1279`
All five sites must be extended in Task 3 or `cargo build --all-targets` will fail with E0063.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add tower_governor dependency and tower-http timeout feature</name>
  <files>coordinator/Cargo.toml</files>
  <read_first>
    - coordinator/Cargo.toml (read in full — line 36 is the `tower-http` line to mutate)
    - .planning/phases/08-public-endpoint-hardening/08-RESEARCH.md §"Standard Stack" and §"Installation (diff against current `coordinator/Cargo.toml`)" — confirms exact version and feature names
    - .planning/phases/08-public-endpoint-hardening/08-PATTERNS.md §"Tower-http feature flag bump" — shows the before/after diff
  </read_first>
  <behavior>
    - tower-http line: features list changes from `["limit"]` to `["limit", "timeout"]`
    - New line added: `tower_governor = "0.8"` immediately after the `tower-http` line in the `[dependencies]` block
  </behavior>
  <action>
    Edit `coordinator/Cargo.toml` at the `[dependencies]` block. On the existing `tower-http = { version = "0.6", features = ["limit"] }` line (line 36 per PATTERNS analog), add `"timeout"` to the features array so it reads `features = ["limit", "timeout"]`. Immediately below that line, add `tower_governor = "0.8"`. Do not modify `[workspace.dependencies]` — `tower`, `tokio`, and `axum` are already declared at workspace scope. Do not pin minor versions tighter than what RESEARCH §"Standard Stack" specifies. Run `cargo build -p coordinator` after the edit to confirm crate resolution succeeds (RESEARCH §"Package Legitimacy Audit" approved `tower_governor` as a benwis-authored, well-established crate — the human-verify checkpoint is folded into Plan 02 Task 1 before the first runtime use, not here, because the dep-add alone is no-op until middleware.rs imports it).
  </action>
  <verify>
    <automated>cargo build -p coordinator --all-targets</automated>
  </verify>
  <acceptance_criteria>
    - Source: `coordinator/Cargo.toml` contains `tower_governor = "0.8"` (single line in `[dependencies]`)
    - Source: `coordinator/Cargo.toml` `tower-http` line features list contains both `"limit"` AND `"timeout"`
    - Behavior: `cargo build -p coordinator --all-targets` exits 0
    - Behavior: `cargo tree -p coordinator | grep tower_governor` shows a `tower_governor v0.8.x` entry
  </acceptance_criteria>
  <done>tower_governor 0.8 + tower-http timeout feature are resolved by cargo; downstream code can `use tower_governor::*` and `use tower_http::timeout::TimeoutLayer` without missing-import errors.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add four operator-tunable knobs to CoordinatorSection with serde defaults</name>
  <files>coordinator/src/config.rs</files>
  <read_first>
    - coordinator/src/config.rs (read in full, 125 lines) — see existing `ban_file_path` field at lines 24-26 and its `default_ban_file_path` at lines 34-36 for the exact pattern to copy
    - coordinator/src/config.rs lines 84-95 — confirms env-var overlay is fully automatic; no changes needed in `load()`
    - coordinator/src/config.rs lines 101-124 — `with_defaults()` literal must be extended for the 4 new fields
    - .planning/phases/08-public-endpoint-hardening/08-CONTEXT.md §"Operator-tunable knobs (D-04)" — the four field names + types + defaults are locked
    - .planning/phases/08-public-endpoint-hardening/08-PATTERNS.md §"`coordinator/src/config.rs` (4 new fields in CoordinatorSection)" — shows the exact insertion form
  </read_first>
  <behavior>
    - `CoordinatorSection` struct gains four new fields: `rate_limit_info_per_min: u32`, `rate_limit_writes_per_min: u32`, `request_timeout_secs: u64`, `max_concurrent_connections: u32`
    - Each new field carries `#[serde(default = "default_xxx")]` pointing at a matching default-fn below the struct
    - Four new default-fns return the literals from D-02/D-04: 60, 30, 30, 256 respectively
    - `with_defaults()` literal at line 109-121 lists all four new fields with the same default values
    - `cargo build -p coordinator` still passes (struct-only change; no consumers yet)
  </behavior>
  <action>
    Insert the four new fields into `CoordinatorSection` immediately before the existing `tor_mode` field (which keeps `tor_mode` last). Mirror the `ban_file_path` analog: each field gets a `#[serde(default = "default_xxx")]` attribute and a corresponding `fn default_xxx() -> T { LITERAL }` immediately below the struct definition (just after the existing `default_ban_file_path` fn). Field names, types, and defaults are fixed per D-02/D-04 — do NOT rename: `rate_limit_info_per_min: u32` (default 60), `rate_limit_writes_per_min: u32` (default 30), `request_timeout_secs: u64` (default 30), `max_concurrent_connections: u32` (default 256). Then update the `CoordinatorConfig::with_defaults()` literal at lines 109-121 by adding the four new fields with the same literal values (60u32, 30u32, 30u64, 256u32) — keep the existing `ban_file_path` and `tor_mode` lines unchanged. Do NOT touch `load()` — the env-var overlay (`BLINDJOIN__COORDINATOR__RATE_LIMIT_INFO_PER_MIN` etc.) is automatic via the existing `Environment::with_prefix(\"BLINDJOIN\").separator(\"__\")` source at lines 92-94.
  </action>
  <verify>
    <automated>cargo build -p coordinator --all-targets 2>&1 | grep -E "error\[" ; cargo test -p coordinator --lib config:: 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Source: `coordinator/src/config.rs` contains the literal string `rate_limit_info_per_min: u32` inside `pub struct CoordinatorSection`
    - Source: `coordinator/src/config.rs` contains all four default-fns: `default_rate_limit_info_per_min`, `default_rate_limit_writes_per_min`, `default_request_timeout_secs`, `default_max_concurrent_connections`
    - Source: each default-fn returns the literal from D-02/D-04 — `60`, `30`, `30`, `256` respectively (grep verifiable)
    - Source: `with_defaults()` literal at the bottom of the file mentions all four new field names
    - Behavior: `cargo build -p coordinator` exits 0 (struct extension only; no downstream consumers yet — those land in Plans 02/03)
    - Behavior: `BLINDJOIN__COORDINATOR__RATE_LIMIT_INFO_PER_MIN=99 cargo run -p coordinator -- --help` (or equivalent unit test deserializing from env) yields `rate_limit_info_per_min == 99` — env overlay reachable without code changes (deferred to Plan 04 integration test if not unit-tested here)
  </acceptance_criteria>
  <done>The four new config fields exist on `CoordinatorSection`, have serde defaults matching D-02/D-04, are present in `with_defaults()`, and inherit the env-var overlay automatically. The coordinator still compiles and runs identically (no behavior change yet — fields are read by Plans 02 and 03).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update existing CoordinatorSection literals in tests so they keep compiling</name>
  <files>tests/integration/round_bootstrap.rs, tests/integration/full_round.rs</files>
  <read_first>
    - tests/integration/round_bootstrap.rs (read in full, 225 lines) — the literal at lines 117-129 is the one that breaks once Task 2 lands
    - tests/integration/full_round.rs — FOUR `CoordinatorSection { ... }` struct-literal sites verified via grep at lines 58, 448, 814, 1279. Read each site (and ~30 lines of surrounding context per site) to confirm the exact existing field shape before extending.
    - .planning/phases/08-public-endpoint-hardening/08-PATTERNS.md §"Pattern to copy" subsection on `with_defaults()` — confirms every literal must be extended
    - Run `grep -rn "CoordinatorSection {" tests coordinator/src 2>/dev/null` BEFORE editing to RE-CONFIRM the full set of sites (planner-verified list above is authoritative as of 2026-05-25; re-grep guards against drift if other plans landed concurrently).
  </read_first>
  <behavior>
    - Every `CoordinatorSection { ... }` struct literal across the workspace lists the four new fields with reasonable test values (typically matches default literals 60u32 / 30u32 / 30u64 / 256u32)
    - Specifically all five known sites are updated: `tests/integration/round_bootstrap.rs:117` AND `tests/integration/full_round.rs:58,448,814,1279`
    - `cargo build --all-targets` and `cargo test --no-run` both exit 0
  </behavior>
  <action>
    Run `grep -rn "CoordinatorSection {" tests coordinator/src` and confirm the five known sites (round_bootstrap.rs:117; full_round.rs:58, 448, 814, 1279). If grep returns additional sites beyond these five, update each with identical four field-additions. For each match, add the four new fields (`rate_limit_info_per_min: 60`, `rate_limit_writes_per_min: 30`, `request_timeout_secs: 30`, `max_concurrent_connections: 256`) to the struct literal, placing them immediately before the existing `tor_mode: false,` line. The canonical site to learn the shape from is `tests/integration/round_bootstrap.rs:117-129` (the `CoordinatorSection { ... }` block inside the `let cfg = CoordinatorConfig { ... }` literal). Repeat the identical four-field insertion at each of the four `full_round.rs` sites (lines 58, 448, 814, 1279) — note that line numbers may shift slightly as you edit earlier sites first; use the surrounding `coordinator: CoordinatorSection {` marker rather than absolute line numbers to locate each block after the first edit. Do NOT change `with_defaults()` again — that was done in Task 2. Do NOT introduce a helper or refactor existing tests beyond field-addition — the goal is "preserve compilation," not improve test ergonomics. After all edits, run `cargo build --all-targets` to confirm zero E0063 (missing-field) errors.
  </action>
  <verify>
    <automated>cargo build --all-targets 2>&1 | grep -E "E0063|missing field|error\[" | tee /tmp/p08-01-task3-errors.txt && test ! -s /tmp/p08-01-task3-errors.txt</automated>
  </verify>
  <acceptance_criteria>
    - Source: `tests/integration/round_bootstrap.rs` `CoordinatorSection { ... }` block contains literal lines for all four new fields
    - Source: `tests/integration/full_round.rs` contains all four new fields in EACH of the four `CoordinatorSection { ... }` blocks (was: lines 58, 448, 814, 1279)
    - Source check: `grep -c "rate_limit_info_per_min" tests/integration/round_bootstrap.rs` returns ≥ 1
    - Source check: `grep -c "rate_limit_info_per_min" tests/integration/full_round.rs` returns ≥ 4
    - Behavior: `cargo build --all-targets` exits 0 with zero "missing field" or `E0063` errors
    - Behavior: `cargo test --no-run` exits 0 (compiles all tests, including the existing `round_bootstrap` and `full_round` integration tests)
  </acceptance_criteria>
  <done>All existing `CoordinatorSection { ... }` struct literals across the workspace include the four new fields with sensible test values — specifically the 1 site in `round_bootstrap.rs` and the 4 sites in `full_round.rs`. `cargo build --all-targets` exits 0. No semantic behavior changed — the literals just satisfy the extended schema.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config file → CoordinatorConfig | Untrusted operator-supplied TOML / env-var values cross into runtime config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Tampering | `coordinator.toml` / `BLINDJOIN__COORDINATOR__*` env vars (operator-supplied) | accept | Operator owns the config surface; tampering inside it is out of model (CLAUDE.md trust model: operator is trusted). The new fields are bounded u32/u64 ranges and serde-validated. |
| T-08-01-02 | Denial of Service | Operator sets all four new knobs to `0` or `u32::MAX`, breaking the layer construction | mitigate | Plan 02 Task 1 (`build_rate_limit_layers`) `.expect("non-zero period and burst")` per RESEARCH Pattern 1 — fail-fast at startup rather than silently mis-configuring. Documented in Plan 02. |
| T-08-01-SC | Tampering | npm/pip/cargo installs — specifically `cargo add tower_governor@0.8` | mitigate | tower_governor approved in RESEARCH §"Package Legitimacy Audit" (benwis author, MIT/Apache, established crate). slopcheck unavailable in env — Plan 02 Task 1 inserts a `checkpoint:human-verify` (blocking-human gate) immediately before the first `use tower_governor::*` import, so the operator eyeballs the crate page once. **This plan adds the Cargo.toml line; no runtime use yet. The checkpoint lives in Plan 02 where the actual import lands.** |

**What this plan does NOT mitigate (called out per security_threat_model):**
- Per-peer throttling on Tor — impossible by design (CONTEXT D-01); only Plan 02's `GlobalKeyExtractor` is correct
- Sybil attacks — BIP-322 ownership proofs already address this (CONTEXT D-01), unchanged here
- Volume DoS on `/round/input`, `/round/sign`, `/round/output`, `/round/tx`, `/info` — landed in Plan 02, not here
- Slow-loris / request-timeout — landed in Plan 02, not here
- Connection exhaustion — landed in Plan 03, not here
</threat_model>

<verification>
- `cargo build --all-targets` exits 0
- `cargo test --no-run` exits 0
- `cargo tree -p coordinator | grep tower_governor` shows a `0.8.x` entry
- `grep -c "rate_limit_info_per_min" coordinator/src/config.rs tests/integration/round_bootstrap.rs | grep -v ':0'` confirms presence in both files
- `grep -c "rate_limit_info_per_min" tests/integration/full_round.rs` returns ≥ 4 (one per site at the four CoordinatorSection literals)
- `grep -c "rate_limit_info_per_min\|rate_limit_writes_per_min\|request_timeout_secs\|max_concurrent_connections" coordinator/src/config.rs` returns at least 12 (4 fields × 3 sites: struct decl, default-fn name, with_defaults literal)
</verification>

<success_criteria>
The four new operator-tunable knobs from CONTEXT D-04 exist on `CoordinatorSection`, have serde defaults matching D-02/D-04 (60/30/30/256), and inherit `BLINDJOIN__COORDINATOR__*` env-var overrides automatically. `tower_governor = "0.8"` is declared in `coordinator/Cargo.toml` and `tower-http` has the `"timeout"` feature enabled. All existing `CoordinatorSection { ... }` struct literals across the workspace compile cleanly. No runtime behavior has changed — Plans 02 and 03 can now wire layers using these fields without compile errors.
</success_criteria>

<output>
Create `.planning/phases/08-public-endpoint-hardening/08-01-SUMMARY.md` documenting: (a) the four field names + types + defaults landed, (b) the env-var overlay names operators can use, (c) the Cargo.toml diff applied, (d) confirmation that no behavior changed and all existing tests still compile + pass.
</output>
</output>
