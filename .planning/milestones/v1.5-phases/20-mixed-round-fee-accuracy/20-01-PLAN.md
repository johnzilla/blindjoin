---
phase: 20-mixed-round-fee-accuracy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - coordinator/src/bitcoin/tx.rs
  - coordinator/src/bitcoin/fee.rs
  - coordinator/src/bitcoin/utxo.rs
  - coordinator/src/round/state.rs
  - coordinator/src/round/input_reg.rs
  - coordinator/src/round/signing.rs
  - coordinator/src/api/handlers.rs
  - coordinator/src/config.rs
autonomous: true
requirements: [FEE-01, FEE-02, FEE-03]
requirements_addressed: [FEE-01, FEE-02, FEE-03]
tags: [fee-math, bip-141, script-type, coordinator, refactor]

must_haves:
  truths:
    - "D-124: coordinator/src/bitcoin/tx.rs exposes pub const fn script_input_vbytes(ScriptType) -> u64 and pub const fn script_output_vbytes(ScriptType) -> u64 returning conservative-rounded-UP BIP-141 weights (P2WPKH 68/31, P2TR 58/43, P2SH-P2WPKH 91/32); the hardcoded INPUT_WEIGHT_VBYTES and OUTPUT_WEIGHT_VBYTES consts at tx.rs:11,13 no longer exist"
    - "D-123: ParticipantInput carries a script_type: shared::bip322::ScriptType field; build_coinjoin_psbt sums per-input weights via script_input_vbytes(inp.script_type) and uses script_output_vbytes(output_script_type) for the denomination + change outputs"
    - "D-123: The script_type value propagates UtxoDetails -> RegisteredInput -> ParticipantInput, set exactly once by the coordinator inside dispatch_ownership_proof at utxo.rs:99 (CRIT-01 invariant preserved into the fee path; never client-declared)"
    - "D-122: estimate_fee_share in coordinator/src/bitcoin/fee.rs takes (&BipConfig, n, fee_rate) and computes the worst-case-across-allowed-set upper bound = max(script_input_vbytes(t) for t in bip_config.allowed_set()) * n + script_output_vbytes(bip_config.output_script_type) * 2 * n + TX_OVERHEAD_VBYTES; both call sites (handlers.rs:165 + handlers.rs:505) pass &state.config.bip"
    - "D-125: Regression test fee_share_p2wpkh_only_matches_v14_baseline in coordinator/src/bitcoin/tx.rs::tests constructs a 3-participant uniform-P2WPKH round (fee_rate=2 sat/vB) and asserts fee_share == 266 byte-exact"
    - "D-126: Regression test fee_share_mixed_script_differs_from_uniform_baseline constructs a 3-participant 1xP2WPKH + 1xP2TR + 1xP2SH-P2WPKH round (output_script_type=P2WPKH, fee_rate=2 sat/vB) and asserts fee_share - 266 >= 1 (computed delta is 9 sats/participant; headroom comfortable)"
    - "D-127: Phase 20 ships as ONE plan (20-01) with all 7 file changes landing in a single atomic series of task-scoped commits inside Wave 1; no separate FEE-01/02/03 sub-plans (type coupling between UtxoDetails/RegisteredInput/ParticipantInput.script_type forbids staged plumbing)"
    - "v1.3 full_round::* 8/8 green AND v1.4 mixed_script_e2e_three_clients_broadcast still passes with the new fee math"
    - "grep -rn 'detect_script_type' coordinator/src/ shared/src/ shows zero new call sites versus pre-phase HEAD (exactly the existing utxo.rs sites inside dispatch_ownership_proof)"
  artifacts:
    - path: "coordinator/src/bitcoin/tx.rs"
      provides: "script_input_vbytes + script_output_vbytes const fns; ParticipantInput.script_type field; build_coinjoin_psbt(output_script_type) signature; 6 vbyte-pin unit tests + 2 FEE-03 regression tests"
      contains: "pub const fn script_input_vbytes"
    - path: "coordinator/src/bitcoin/fee.rs"
      provides: "estimate_fee_share(&BipConfig, n, fee_rate) using worst-case-across-allowed-set formula"
      contains: "pub fn estimate_fee_share"
    - path: "coordinator/src/bitcoin/utxo.rs"
      provides: "UtxoDetails.script_type field populated from existing dispatch_ownership_proof derivation"
      contains: "pub struct UtxoDetails"
    - path: "coordinator/src/round/state.rs"
      provides: "RegisteredInput.script_type field with #[zeroize(skip)]"
      contains: "pub script_type: shared::bip322::ScriptType"
    - path: "coordinator/src/config.rs"
      provides: "BipConfig::allowed_set() iterator helper"
      contains: "pub fn allowed_set"
  key_links:
    - from: "coordinator/src/bitcoin/utxo.rs"
      to: "coordinator/src/round/state.rs::RegisteredInput.script_type"
      via: "handlers.rs::post_input passes utxo_details.script_type to register_input"
      pattern: "utxo_details\\.script_type"
    - from: "coordinator/src/round/state.rs::RegisteredInput.script_type"
      to: "coordinator/src/bitcoin/tx.rs::ParticipantInput.script_type"
      via: "handlers.rs::get_tx and signing.rs::assemble_and_broadcast both read reg.script_type"
      pattern: "script_type: reg\\.script_type"
    - from: "coordinator/src/bitcoin/tx.rs::build_coinjoin_psbt"
      to: "coordinator/src/bitcoin/fee.rs::script_input_vbytes / script_output_vbytes"
      via: "per-input weight sum + per-output multiply (replaces n*INPUT_WEIGHT_VBYTES formula)"
      pattern: "script_input_vbytes\\(inp\\.script_type\\)"
    - from: "coordinator/src/api/handlers.rs (both call sites)"
      to: "coordinator/src/round/signing.rs::assemble_and_broadcast"
      via: "both build_coinjoin_psbt callers MUST pass state.config.bip.output_script_type (WR-04 byte-identical PSBTs)"
      pattern: "config\\.bip\\.output_script_type"
---

<objective>
Phase 20 replaces the v1.4 P2WPKH-only fee approximation in coordinator/src/bitcoin/{tx.rs,fee.rs} with a per-script weight table and threads shared::bip322::ScriptType through three coordinator-internal structs (UtxoDetails → RegisteredInput → ParticipantInput) so a mixed-script CoinJoin round charges a fee_share that reflects actual per-input witness weights. Two regression tests pin the v1.4 P2WPKH-only baseline byte-equal (266 sats at fee_rate=2) and prove the per-script branch fires for mixed rounds (≥1 sat/participant delta; computed delta is 9 sats).

Purpose: Close FEE-01 + FEE-02 + FEE-03 verbatim. Preserve CRIT-01 (coordinator-derived ScriptType, never client-declared) into the fee path. Preserve WR-04 (single canonical fee helper consumed by both get_tx display and assemble_and_broadcast broadcast — both PSBTs byte-identical). Preserve the v1.3 + v1.4 cross-phase invariants.

Output: Updated tx.rs (vbyte table + struct field + signature + body + 8 tests), fee.rs (signature + body), utxo.rs (struct extension + return), state.rs (struct field), input_reg.rs (signature + write site), signing.rs (struct construction + call site), handlers.rs (3 call sites), config.rs (allowed_set helper). One single atomic commit per task. Phase summary at .planning/phases/20-mixed-round-fee-accuracy/20-01-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-mixed-round-fee-accuracy/20-CONTEXT.md
@.planning/phases/20-mixed-round-fee-accuracy/20-RESEARCH.md
@.planning/phases/20-mixed-round-fee-accuracy/20-PATTERNS.md

# Files modified by this plan
@coordinator/src/bitcoin/tx.rs
@coordinator/src/bitcoin/fee.rs
@coordinator/src/bitcoin/utxo.rs
@coordinator/src/round/state.rs
@coordinator/src/round/input_reg.rs
@coordinator/src/round/signing.rs
@coordinator/src/api/handlers.rs
@coordinator/src/config.rs

# Cross-phase invariant gates (verified at plan end)
@tests/integration/full_round.rs
@tests/integration/mixed_script_e2e.rs

# Canonical contract surface (consumed; do NOT modify)
@shared/src/bip322/mod.rs

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->
<!-- Use these directly — no codebase exploration needed. -->

From shared/src/bip322/mod.rs:150-157:
```
#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
#[serde(rename_all = "snake_case")]
pub enum ScriptType {
    P2wpkh,
    P2tr,
    #[serde(rename = "p2sh-p2wpkh")]
    P2shP2wpkh,
}
```

From shared/src/bip322/mod.rs:238-248 (DO NOT add new call sites — exists, used at utxo.rs:163,184):
```
pub fn detect_script_type(spk: &Script) -> Result<ScriptType, Bip322Error>
```

From coordinator/src/bitcoin/utxo.rs:37-40 (CURRENT — extend with script_type):
```
pub struct UtxoDetails {
    pub value_sats: u64,
    pub script_pubkey: ScriptBuf,
}
```

From coordinator/src/bitcoin/utxo.rs:99-106 (existing derivation site — CRIT-01 single source):
```
let derived = dispatch_ownership_proof(
    &script_pubkey, ownership_proof, network, bip_config, message.as_bytes(),
).map_err(|e| UtxoError::InvalidProof { reason: e.to_string() })?;
```

From coordinator/src/round/state.rs:53-68 (CURRENT — extend with script_type + #[zeroize(skip)]):
```
#[derive(Debug, Clone, Zeroize)]
pub struct RegisteredInput {
    pub utxo_str: String,
    pub change_address: String,
    pub blind_sig_hash: [u8; 32],
    #[zeroize(skip)]
    pub script_pubkey: ScriptBuf,
    pub value_sats: u64,
}
```

From coordinator/src/bitcoin/tx.rs:17-23 (CURRENT — extend with script_type field):
```
#[derive(Debug, Clone)]
pub struct ParticipantInput {
    pub outpoint: OutPoint,
    pub value_sats: u64,
    pub script_pubkey: ScriptBuf,
    pub change_address: ScriptBuf,
}
```

From coordinator/src/bitcoin/tx.rs:53-58 (CURRENT — add output_script_type 5th param):
```
pub fn build_coinjoin_psbt(
    inputs: &[ParticipantInput],
    outputs: &[ParticipantOutput],
    denomination_sats: u64,
    fee_rate_sat_per_vbyte: u64,
) -> Result<Psbt, TxError>
```

From coordinator/src/bitcoin/fee.rs:11-18 (CURRENT — replace with worst-case-across-allowed-set formula):
```
pub fn estimate_fee_share(n: u32, fee_rate: u64) -> u64 {
    let n = n as u64;
    if n == 0 { return 0; }
    let estimated_vsize = 10 + n * 68 + n * 2 * 31;
    (estimated_vsize * fee_rate) / n
}
```

From coordinator/src/config.rs:191-217 (sibling helpers — model allowed_set after these):
```
pub fn allows(&self, st: ScriptType) -> bool { match st { ... } }
pub fn supported(&self) -> Vec<ScriptType>  // alphabetical; load-bearing for PKARR
```

BipConfig fields touched (config.rs:158-187, do NOT modify struct):
- allow_p2wpkh: bool
- allow_p2tr: bool
- allow_p2sh_p2wpkh: bool
- output_script_type: ScriptType
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FEE-01 — per-script vbyte table + 6 vbyte-pin unit tests + delete legacy consts</name>
  <files>coordinator/src/bitcoin/tx.rs, coordinator/src/bitcoin/fee.rs</files>
  <read_first>
    - coordinator/src/bitcoin/tx.rs (entire file, 224 LOC; especially lines 10-15 consts, lines 53-70 build_coinjoin_psbt body, lines 131-224 inline tests module)
    - coordinator/src/bitcoin/fee.rs (entire file, 18 LOC)
    - shared/src/bip322/mod.rs lines 150-157 (ScriptType enum)
    - .planning/phases/20-mixed-round-fee-accuracy/20-PATTERNS.md §"File: coordinator/src/bitcoin/tx.rs" (Add 1 + Add 5 sections — replacement code with derivation comments + 6 test bodies)
    - .planning/phases/20-mixed-round-fee-accuracy/20-RESEARCH.md §"BIP-141 vbyte references" + §"Code Examples" Example 4
    - .planning/phases/20-mixed-round-fee-accuracy/20-CONTEXT.md §D-124 (the table + P2TR=58 divergence rationale)
  </read_first>
  <behavior>
    - Test script_input_vbytes_p2wpkh_is_68 asserts script_input_vbytes(ScriptType::P2wpkh) == 68
    - Test script_input_vbytes_p2tr_is_58_up_rounded asserts script_input_vbytes(ScriptType::P2tr) == 58 (NOT 57 — STATE.md round-UP policy)
    - Test script_input_vbytes_p2sh_p2wpkh_is_91 asserts script_input_vbytes(ScriptType::P2shP2wpkh) == 91
    - Test script_output_vbytes_p2wpkh_is_31 asserts script_output_vbytes(ScriptType::P2wpkh) == 31
    - Test script_output_vbytes_p2tr_is_43 asserts script_output_vbytes(ScriptType::P2tr) == 43
    - Test script_output_vbytes_p2sh_p2wpkh_is_32 asserts script_output_vbytes(ScriptType::P2shP2wpkh) == 32
    - The two functions are `pub const fn` (CD-41 default — match arms over Copy enum are const-evaluable in stable Rust 2025; fall back to `pub fn` only if const fn fails to compile)
  </behavior>
  <action>
    Place both functions in coordinator/src/bitcoin/tx.rs (CD-40 fallback location for simpler import path — they are co-located with build_coinjoin_psbt which is the primary consumer; fee.rs imports them via `use crate::bitcoin::tx::{script_input_vbytes, script_output_vbytes};` in Task 2). Add `use shared::bip322::ScriptType;` at the top of tx.rs if not already imported.

    DELETE the consts at tx.rs:11 (`INPUT_WEIGHT_VBYTES`) and tx.rs:13 (`OUTPUT_WEIGHT_VBYTES`). KEEP `TX_OVERHEAD_VBYTES = 10` at tx.rs:15 (script-independent, per RESEARCH §"Hardcoded constants").

    Add `pub const fn script_input_vbytes(st: ScriptType) -> u64` with match arms returning 68 / 58 / 91 for P2wpkh / P2tr / P2shP2wpkh. Each return arm MUST carry a 4-6 line BIP-141 derivation comment per PATTERNS.md Add 1 — including for the P2tr arm an explicit "ROADMAP SC#1 cites 57 (floor of 57.5); STATE.md §v1.5 design notes mandates UP-rounding so the coordinator never underpays fees" note so reviewers see the divergence is intentional (RISK-4 hedge from RESEARCH §Risks).

    Add `pub const fn script_output_vbytes(st: ScriptType) -> u64` with match arms returning 31 / 43 / 32. Each arm carries the exact-bytes derivation comment per PATTERNS.md Add 1.

    Extend the existing `make_inputs` test helper at tx.rs:150-157 per PATTERNS.md Add 4: add a new helper `make_inputs_typed(types: &[ScriptType], value_sats: u64) -> Vec<ParticipantInput>` that takes per-input ScriptType, and refactor the existing `make_inputs` to delegate via `make_inputs_typed(&vec![ScriptType::P2wpkh; n], value_sats)`. NOTE: `make_inputs_typed` will need to set `script_type: st` on each ParticipantInput — that struct field is added in Task 2; this task may temporarily compile-error on that field until Task 2 lands. EITHER (a) land Task 1's helpers as a single commit alongside Task 2's struct change, OR (b) defer `make_inputs_typed` to Task 2 and only land the 6 vbyte unit tests in Task 1. Default: defer `make_inputs_typed` to Task 2 (simpler atomic compile boundary; Task 1's 6 vbyte tests do not reference `make_inputs_typed`).

    Add the 6 vbyte-pin tests inline in the existing `#[cfg(test)] mod tests` block at tx.rs:131-224, after the existing 5 tests and before the module close. Bodies per PATTERNS.md Add 5.

    DO NOT yet modify `build_coinjoin_psbt`, `ParticipantInput`, or `fee.rs::estimate_fee_share` — those land in Task 2 (where the per-input weight sum needs the new ParticipantInput.script_type field to read from). The consts deletion at tx.rs:11,13 WILL break `build_coinjoin_psbt` at tx.rs:67-68 (referencing INPUT_WEIGHT_VBYTES / OUTPUT_WEIGHT_VBYTES) AND will break `fee.rs:16` (inline `10 + n * 68 + n * 2 * 31` — does not reference the consts but is the same numerical surface). To preserve a green build between Task 1 and Task 2:
      - REPLACE the const definitions with `pub const fn` definitions in the SAME commit (no transient broken state)
      - In `build_coinjoin_psbt` body, replace `INPUT_WEIGHT_VBYTES` with `script_input_vbytes(ScriptType::P2wpkh)` and `OUTPUT_WEIGHT_VBYTES` with `script_output_vbytes(ScriptType::P2wpkh)` as a TRANSITIONAL step. Task 2 will replace this with the per-input sum. The transitional state produces byte-identical fee values (68/31 lookups return same numbers as old consts) — v1.3 full_round 8/8 still green.
      - In `fee.rs:16`, replace `10 + n * 68 + n * 2 * 31` with `10 + n * script_input_vbytes(ScriptType::P2wpkh) + n * 2 * script_output_vbytes(ScriptType::P2wpkh)` (add `use crate::bitcoin::tx::{script_input_vbytes, script_output_vbytes}; use shared::bip322::ScriptType;` at top of fee.rs). Same byte-identical transitional values.

    NEVER add a new call site for `detect_script_type` in this task (CRIT-01 audit gate). NEVER promote these functions to the `shared/` crate (CONTEXT deferred ideas — out of v1.5 scope).
  </action>
  <verify>
    <automated>cargo test -p coordinator --lib bitcoin::tx::tests::script_input_vbytes -- --nocapture &amp;&amp; cargo test -p coordinator --lib bitcoin::tx::tests::script_output_vbytes -- --nocapture &amp;&amp; cargo test --test integration full_round &amp;&amp; cargo build -p coordinator &amp;&amp; cargo clippy --workspace --all-targets -- -D warnings &amp;&amp; ! grep -E '^[^/]*(INPUT_WEIGHT_VBYTES|OUTPUT_WEIGHT_VBYTES)' coordinator/src/bitcoin/tx.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '^pub const fn script_input_vbytes\\(' coordinator/src/bitcoin/tx.rs` returns exactly one line
    - `grep -nE '^pub const fn script_output_vbytes\\(' coordinator/src/bitcoin/tx.rs` returns exactly one line
    - `grep -nE 'const INPUT_WEIGHT_VBYTES|const OUTPUT_WEIGHT_VBYTES' coordinator/src/bitcoin/tx.rs` returns zero matches (consts deleted; `TX_OVERHEAD_VBYTES = 10` MAY remain — verify with `grep -n 'TX_OVERHEAD_VBYTES' coordinator/src/bitcoin/tx.rs` returns exactly the original line 15 declaration unchanged)
    - The P2tr match arm in `script_input_vbytes` contains the literal string `STATE.md` (the rounding-policy divergence note from PATTERNS.md Add 1) — verify with `grep -A 6 'ScriptType::P2tr =>' coordinator/src/bitcoin/tx.rs | grep -c 'STATE.md'` returns at least 1
    - `cargo test -p coordinator --lib bitcoin::tx::tests::script_input_vbytes_p2wpkh_is_68` passes
    - `cargo test -p coordinator --lib bitcoin::tx::tests::script_input_vbytes_p2tr_is_58_up_rounded` passes (NOT 57)
    - `cargo test -p coordinator --lib bitcoin::tx::tests::script_input_vbytes_p2sh_p2wpkh_is_91` passes
    - `cargo test -p coordinator --lib bitcoin::tx::tests::script_output_vbytes_p2wpkh_is_31` passes
    - `cargo test -p coordinator --lib bitcoin::tx::tests::script_output_vbytes_p2tr_is_43` passes
    - `cargo test -p coordinator --lib bitcoin::tx::tests::script_output_vbytes_p2sh_p2wpkh_is_32` passes
    - `cargo test --test integration full_round` returns 8/8 green (v1.3 invariant — fee math is byte-identical with the transitional P2WPKH-only lookups)
    - `cargo build -p coordinator` succeeds (no warnings about unused INPUT_WEIGHT_VBYTES / OUTPUT_WEIGHT_VBYTES)
    - `cargo clippy --workspace --all-targets -- -D warnings` clean
    - CRIT-01 audit gate: `grep -rn 'detect_script_type' coordinator/src/ shared/src/` returns ONLY the pre-existing call sites at shared/src/bip322/mod.rs (the def site) + coordinator/src/bitcoin/utxo.rs:163,184 (inside dispatch_ownership_proof). Zero new call sites — capture grep output in summary as evidence.
  </acceptance_criteria>
  <done>
    Per-script vbyte lookup table compiles, all 6 vbyte-pin unit tests pass, the two legacy consts are deleted, fee.rs and build_coinjoin_psbt use the new helpers via P2WPKH-only transitional calls (byte-identical fee values), v1.3 full_round 8/8 green, no clippy warnings, zero new detect_script_type call sites.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FEE-02 — thread script_type through UtxoDetails → RegisteredInput → ParticipantInput; rewrite build_coinjoin_psbt + estimate_fee_share to per-script formulas</name>
  <files>coordinator/src/bitcoin/tx.rs, coordinator/src/bitcoin/fee.rs, coordinator/src/bitcoin/utxo.rs, coordinator/src/round/state.rs, coordinator/src/round/input_reg.rs, coordinator/src/round/signing.rs, coordinator/src/api/handlers.rs, coordinator/src/config.rs</files>
  <read_first>
    - coordinator/src/bitcoin/tx.rs lines 17-70 (ParticipantInput struct + build_coinjoin_psbt body)
    - coordinator/src/bitcoin/fee.rs (full file, after Task 1)
    - coordinator/src/bitcoin/utxo.rs lines 37-117 (UtxoDetails + validate_utxo body)
    - coordinator/src/round/state.rs lines 1-100 (imports + RegisteredInput struct at lines 53-68; especially the `#[zeroize(skip)]` precedent on script_pubkey at line 63-65)
    - coordinator/src/round/input_reg.rs lines 1-100 (signature at 35-43 + write site at 82-88) and lines 155-233 (existing tests that construct RegisteredInput literals)
    - coordinator/src/round/signing.rs lines 100-200 (ParticipantInput construction at 124-129 + build_coinjoin_psbt call at 145-154) and lines 330-360 (make_signing_state helper that builds RegisteredInput literals)
    - coordinator/src/round/signing.rs lines 440-540 (ADDITIONAL `RegisteredInput { ... }` literals OUTSIDE `make_signing_state` that also require the new `script_type: ScriptType::P2wpkh` field — enumerated explicitly so the test-fixture refresh in step 7 of <action> is exhaustive):
        * line 448 — inside test `test_blame_non_signer_banned` (single RegisteredInput literal for utxo `txabc:0`, currently fields: utxo_str / change_address / blind_sig_hash / script_pubkey / value_sats)
        * line 523 — inside test `test_missing_output_triggers_blame` (RegisteredInput literal for utxo `tx1:0`, same 5 fields)
        * line 527 — inside test `test_missing_output_triggers_blame` (RegisteredInput literal for utxo `tx2:0`, same 5 fields)
        All 3 literals require adding `script_type: ScriptType::P2wpkh,` (or equivalent default — these tests do not exercise fee math so P2wpkh matches the default test fixture convention). Each test will also need `use shared::bip322::ScriptType;` in scope (the existing `use` statements in these test bodies do not currently import it — add to the test module's `use` block, not per-test). The fixture-refresh acceptance criterion at the bottom of this task verifies via grep that every `RegisteredInput {` literal in signing.rs carries a `script_type:` field.
    - coordinator/src/api/handlers.rs lines 150-200 (estimate_fee_share call at line 165 + validate_utxo call at 178-188) and lines 250-280 (register_input call at 256-263) and lines 470-510 (ParticipantInput construction at 475-481 + build_coinjoin_psbt + estimate_fee_share calls in get_tx)
    - coordinator/src/config.rs lines 158-260 (BipConfig struct + allows + supported + validate methods)
    - .planning/phases/20-mixed-round-fee-accuracy/20-PATTERNS.md §"File: coordinator/src/bitcoin/utxo.rs" through §"File: coordinator/src/config.rs" (every diff hunk has analog + replacement code — follow verbatim)
    - .planning/phases/20-mixed-round-fee-accuracy/20-RESEARCH.md §"System Architecture Diagram" (data flow) + §"Risks/Landmines" Risk 3 (WR-04 byte-identical PSBTs)
    - .planning/phases/20-mixed-round-fee-accuracy/20-CONTEXT.md §D-122 + §D-123 + §D-122a (the locked plumbing decisions)
  </read_first>
  <behavior>
    - After this task, the build compiles and all existing tests still pass; the per-script fee math is wired end-to-end but Task 3's regression tests prove it actually fires.
    - test impacts (existing tests that construct RegisteredInput / ParticipantInput literals): input_reg.rs::tests `register_input_is_sync_and_succeeds` (~line 155), `register_input_rejects_double_registration` (~line 204); signing.rs::tests `make_signing_state` helper (~line 335-341) AND `test_blame_non_signer_banned` at line 448 AND `test_missing_output_triggers_blame` at lines 523 and 527 (3 additional RegisteredInput literals outside make_signing_state — see <read_first> enumeration above); any tx.rs::tests that construct `ParticipantInput` literals (the existing 5 tests use `make_inputs` which becomes `make_inputs_typed(&vec![P2wpkh; n], _)`, so they pick up the new field transparently). Default ScriptType for test fixtures: `ScriptType::P2wpkh` (matches existing test SPK shape).
    - Worst-case-from-BipConfig: estimate_fee_share returns the SAME value at fee_rate=2, n=3, allowed_set={P2wpkh, P2tr, P2shP2wpkh}, output_script_type=P2wpkh as it would compute internally — verify by manual calculation: worst_input_vb = max(68, 58, 91) = 91; output_vb = 31; vsize = 10 + 91*3 + 31*2*3 = 10 + 273 + 186 = 469; (469*2)/3 = 312 sats/participant. (This is what `fee_per_participant_sats` at handlers.rs:505 will return for advertisement.)
    - build_coinjoin_psbt with P2WPKH-only inputs at fee_rate=2, n=3, output_script_type=P2wpkh produces fee_share = (10 + 3*68 + 6*31)*2/3 = (10+204+186)*2/3 = 800/3 = 266 (proves Task 1's transitional values transition cleanly to per-input sum).
  </behavior>
  <action>
    Execute the changes as a SINGLE atomic commit (per CONTEXT D-127 and the planning context's "Phase 20 is internally cohesive at the type level"). The order WITHIN the commit follows Pattern 5 (Sibling-field plumbing) from PATTERNS.md:

    1) `coordinator/src/bitcoin/utxo.rs`: Add `pub script_type: ScriptType` field to `UtxoDetails` (lines 37-40) with doc-comment per PATTERNS.md Modify 1 — "Coordinator-derived script type from detect_script_type(script_pubkey), computed inside dispatch_ownership_proof (CRIT-01 invariant: NEVER client-declared)". At line 116, change `Ok(UtxoDetails { value_sats, script_pubkey })` to `Ok(UtxoDetails { value_sats, script_pubkey, script_type: derived })` — `derived` already exists at line 99 from the existing `let derived = dispatch_ownership_proof(...)`. Ensure `use shared::bip322::ScriptType;` is imported (already present at utxo.rs:5).

    2) `coordinator/src/round/state.rs`: Add `use shared::bip322::ScriptType;` near the existing `use bitcoin::ScriptBuf;` at line 4 if not present. Add a new field to `RegisteredInput` (after `value_sats`) per PATTERNS.md Modify section: `#[zeroize(skip)] pub script_type: shared::bip322::ScriptType,` with doc-comment "Coordinator-derived script type (FEE-02 plumbing). Mirrors script_pubkey in provenance and zeroize policy: public chain data, derivable from script_pubkey, no key material, no privacy concern." The `#[zeroize(skip)]` mirrors the existing line 64 annotation on `script_pubkey` — D-123a binding.

    3) `coordinator/src/round/input_reg.rs`: Extend the `register_input` signature to add `utxo_script_type: shared::bip322::ScriptType,` parameter after `utxo_value_sats` (mirroring the existing `utxo_script_pubkey`/`utxo_value_sats` pattern per Pattern 5). At the `RegisteredInput { ... }` literal at line 82-88, add `script_type: utxo_script_type,` field. Refresh test fixtures at `register_input_is_sync_and_succeeds` and `register_input_rejects_double_registration` to pass `ScriptType::P2wpkh` as the new arg (default test type).

    4) `coordinator/src/bitcoin/tx.rs`: Add `pub script_type: shared::bip322::ScriptType,` field to `ParticipantInput` (lines 17-23) after `change_address` with doc-comment "FEE-02: per-input vbyte selector (coordinator-derived from on-chain SPK at validate_utxo, never client-declared — CRIT-01)". Extend the existing `make_inputs` test helper per PATTERNS.md Add 4: add new helper `make_inputs_typed(types: &[ScriptType], value_sats: u64) -> Vec<ParticipantInput>` that sets per-input ScriptType, and refactor `make_inputs(n, value_sats)` to delegate via `make_inputs_typed(&vec![ScriptType::P2wpkh; n], value_sats)`.

    5) `coordinator/src/bitcoin/tx.rs`: Update `build_coinjoin_psbt` signature (line 53-58) to add `output_script_type: ScriptType,` as the 5th param per PATTERNS.md Add 3. Replace the vsize formula at lines 66-68 with the per-input weight sum: `let total_input_vb: u64 = inputs.iter().map(|inp| script_input_vbytes(inp.script_type)).sum(); let output_vb = script_output_vbytes(output_script_type); let estimated_vsize = TX_OVERHEAD_VBYTES + total_input_vb + (n + num_change_outputs) * output_vb;` PRESERVE `let fee_share = total_fee / n;` at line 70 VERBATIM (integer floor — D-125 byte-equality depends on it; RISK-1 hedge).

    6) `coordinator/src/api/handlers.rs`: At the `ParticipantInput { ... }` construction in `get_tx` (~line 475-481), add `script_type: reg.script_type,` (Copy type, no clone). At the `build_coinjoin_psbt(...)` call (~line 491-499), add `state.config.bip.output_script_type` as the 5th argument. At the `register_input(...)` call at line 256-263, add `utxo_details.script_type,` after `utxo_details.value_sats,` (Copy type).

    7) `coordinator/src/round/signing.rs`: At the `ParticipantInput { ... }` construction at line 124-129 in `assemble_and_broadcast`, add `script_type: reg.script_type,`. At the `build_coinjoin_psbt(...)` call at line 145-154, add `config.bip.output_script_type` as the 5th argument. WR-04 requires this to be the SAME source as the get_tx call site (verify by reading both — both go through `state.config.bip.output_script_type` or `config.bip.output_script_type` where `config` is the same `Arc<CoordinatorConfig>`). Refresh the `make_signing_state` test helper (~line 335-341) to set `script_type: ScriptType::P2wpkh` in any `RegisteredInput` literal it constructs. ALSO refresh the 3 additional `RegisteredInput { ... }` literals OUTSIDE `make_signing_state` enumerated in <read_first> above — line 448 (test `test_blame_non_signer_banned`), line 523, line 527 (both in test `test_missing_output_triggers_blame`) — each requires adding `script_type: ScriptType::P2wpkh,`. If `shared::bip322::ScriptType` is not yet in scope in those test modules, add to the test module's `use` block (do NOT add per-test). Verify exhaustiveness via the fixture-refresh acceptance criterion below: every `RegisteredInput {` literal in signing.rs must carry a `script_type:` field after this task.

    8) `coordinator/src/config.rs`: Add the `BipConfig::allowed_set` helper inside the existing `impl BipConfig { ... }` block (after `supported` at line 217, before `validate` at line 230) per PATTERNS.md "File: coordinator/src/config.rs" Add section:
       ```
       pub fn allowed_set(&self) -> impl Iterator<Item = ScriptType> + '_
       ```
       Body uses array + filter_map per PATTERNS.md exact pattern. Doc-comment: "Iterator over allowed input ScriptTypes. Order is unspecified — callers MUST NOT depend on iteration order (use supported() for the alphabetical canonical order needed by PKARR advertisement). Used by fee::estimate_fee_share to compute worst-case-across-allowed-set vbytes; iteration order is irrelevant because max(...) is commutative."

    9) `coordinator/src/bitcoin/fee.rs`: Rewrite `estimate_fee_share` per PATTERNS.md "File: coordinator/src/bitcoin/fee.rs" Modify section + RESEARCH §"Code Examples" Example 2. New signature: `pub fn estimate_fee_share(bip_config: &BipConfig, n: u32, fee_rate: u64) -> u64`. Body: `let worst_input_vb = bip_config.allowed_set().map(script_input_vbytes).max().expect("BipConfig::validate ensures at least one allow_* flag is true"); let output_vb = script_output_vbytes(bip_config.output_script_type); let estimated_vsize = 10 + worst_input_vb * n + output_vb * 2 * n; (estimated_vsize * fee_rate) / n` — preserves the early-return `if n == 0 { return 0; }`. PRESERVE the integer-divide `(... * fee_rate) / n` (no ceil-divide). Update imports: `use crate::bitcoin::tx::{script_input_vbytes, script_output_vbytes}; use crate::config::BipConfig; use shared::bip322::ScriptType;` (drop the transitional Task 1 import comment if any).

    10) `coordinator/src/api/handlers.rs`: Update both `estimate_fee_share` call sites:
        - Line 165 (`fee_share_pre_lock`): change to `estimate_fee_share(&state.config.bip, max_participants_snap, fee_rate_snap)`
        - Line 505 (`fee_per_participant_sats`): change to `estimate_fee_share(&state.config.bip, n, fee_rate)`
        Sibling pattern: `parse_bitcoin_network(&state.config.network.bitcoin_network)` at handlers.rs:177 (existing nested-config-substruct threading).

    NEVER add a new call site for `detect_script_type` (CRIT-01 audit gate — verified by acceptance criterion grep). NEVER write `script_type` into a PSBT proprietary key or any wire-crossing field (RISK-3 hedge — Risk 3 in RESEARCH §Risks; `ParticipantInput` has no Serialize/Deserialize; consume the new field ONLY in the in-memory vsize loop). NEVER add change-address-type validation (`change_address.script_type() == bip.output_script_type`) — that's deferred v1.6+ per CONTEXT §"Not in scope" and RISK-6 in RESEARCH.

    OPTIONAL (low cost, high audit value — include if executor has bandwidth): Add a `#[cfg(test)] mod tests` block at the end of `coordinator/src/bitcoin/fee.rs` with one test `worst_case_picks_max_allowed_input_vbyte` that constructs a `BipConfig` with all 3 allow flags = true, output_script_type = P2wpkh, n=3, fee_rate=2 and asserts `estimate_fee_share(...)` returns 312 (the derived value above). This covers the fee.rs formula at the unit tier (RESEARCH §"Wave 0 Gap" optional cross-check).
  </action>
  <verify>
    <automated>cargo build -p coordinator &amp;&amp; cargo test -p coordinator --lib &amp;&amp; cargo test --test integration full_round &amp;&amp; cargo test --test integration mixed_script_e2e &amp;&amp; cargo clippy --workspace --all-targets -- -D warnings &amp;&amp; test "$(grep -rcn 'detect_script_type' coordinator/src/ shared/src/ | grep -v '^Binary' | awk -F: '{s+=$NF} END {print s}')" -le "$(git show HEAD~1:coordinator/src/bitcoin/utxo.rs 2&gt;/dev/null | grep -c detect_script_type)$(git show HEAD~1:shared/src/bip322/mod.rs 2&gt;/dev/null | grep -c detect_script_type)0" || true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'pub script_type: shared::bip322::ScriptType' coordinator/src/bitcoin/utxo.rs` returns the new UtxoDetails field
    - `grep -n 'pub script_type: shared::bip322::ScriptType' coordinator/src/round/state.rs` returns the new RegisteredInput field
    - `grep -B 1 'pub script_type: shared::bip322::ScriptType' coordinator/src/round/state.rs | grep -c '#\\[zeroize(skip)\\]'` returns exactly 1 (the new field is annotated)
    - `grep -n 'pub script_type: shared::bip322::ScriptType' coordinator/src/bitcoin/tx.rs` returns the new ParticipantInput field
    - `grep -n 'pub fn allowed_set' coordinator/src/config.rs` returns the new method
    - `grep -n 'pub fn estimate_fee_share(bip_config:' coordinator/src/bitcoin/fee.rs` confirms the new signature
    - `grep -nE 'INPUT_WEIGHT_VBYTES|OUTPUT_WEIGHT_VBYTES' coordinator/src/bitcoin/tx.rs coordinator/src/bitcoin/fee.rs` returns ZERO matches (legacy consts fully gone; this is the FEE-01 audit gate)
    - `grep -nE 'script_input_vbytes\\(inp\\.script_type\\)' coordinator/src/bitcoin/tx.rs` returns at least one match (per-input weight sum is wired in build_coinjoin_psbt)
    - `grep -nE 'script_output_vbytes\\(output_script_type\\)' coordinator/src/bitcoin/tx.rs` returns at least one match (output weight uses the param)
    - `grep -nE 'estimate_fee_share\\(&state\\.config\\.bip' coordinator/src/api/handlers.rs` returns exactly 2 matches (lines ~165 and ~505)
    - WR-04 invariant: `grep -nE 'config\\.bip\\.output_script_type' coordinator/src/api/handlers.rs coordinator/src/round/signing.rs` returns at least one match in EACH file (both build_coinjoin_psbt call sites use the same source)
    - **Fixture refresh exhaustiveness (signing.rs RegisteredInput literals — covers make_signing_state PLUS the 3 additional literals at lines 448, 523, 527):** Let `N = $(grep -cE 'RegisteredInput \\{' coordinator/src/round/signing.rs)` (count of opening RegisteredInput struct-literal braces); let `S = $(grep -c 'script_type:' coordinator/src/round/signing.rs)`. Assert `S >= N` (every RegisteredInput literal carries at least one script_type: field; S may exceed N if ParticipantInput literals in the same file also use the field, which is fine). For Phase 20 this resolves to: N includes 1 literal in make_signing_state + 1 at line 448 (test_blame_non_signer_banned) + 2 at lines 523 and 527 (test_missing_output_triggers_blame) = 4 minimum (more if executor refresh found additional literals); S must be >= 4. Concrete shell check: `[ "$(grep -cE 'RegisteredInput \\{' coordinator/src/round/signing.rs)" -le "$(grep -c 'script_type:' coordinator/src/round/signing.rs)" ]` must exit 0.
    - CRIT-01 audit gate (CRITICAL): pre-phase count of `detect_script_type` call sites = post-phase count. Verify with `grep -rn 'detect_script_type' coordinator/src/ shared/src/ | grep -v '^Binary'` — exact line numbers: shared/src/bip322/mod.rs (def + any internal use), coordinator/src/bitcoin/utxo.rs:163,184 (inside dispatch_ownership_proof v=1 and v=2 branches). Zero new call sites — capture full grep output in summary as evidence.
    - `cargo build -p coordinator` succeeds with no warnings about unused imports or fields
    - `cargo test -p coordinator --lib` passes ALL existing tests (5 in tx.rs + N in input_reg.rs + N in signing.rs + N in config.rs + any fee.rs::tests added) — fixture refresh works correctly. CRITICALLY: this includes `test_blame_non_signer_banned` and `test_missing_output_triggers_blame` (the two signing.rs tests containing the 3 additional RegisteredInput literals refreshed in step 7). If either compile-errors with "missing field `script_type`", step 7's fixture refresh was incomplete — return to <read_first> enumeration.
    - `cargo test --test integration full_round` returns 8/8 green (v1.3 invariant — P2WPKH-only fee math is byte-identical: 68/31 → script_input_vbytes(P2wpkh)=68 / script_output_vbytes(P2wpkh)=31, formula reshape preserves the same number)
    - `cargo test --test integration mixed_script_e2e` returns 1/1 green (v1.4 invariant — the test asserts on broadcast txid + denomination count only, no fee_share numeric assertions per RESEARCH §"v1.4 mixed_script_e2e test", so per-script fee refinement doesn't affect assertions)
    - `cargo clippy --workspace --all-targets -- -D warnings` clean
  </acceptance_criteria>
  <done>
    All 8 files compile together as a single atomic change. ScriptType plumbing path complete: utxo.rs:99 derived → UtxoDetails → register_input arg → RegisteredInput.script_type → ParticipantInput.script_type at both get_tx and assemble_and_broadcast construction sites. build_coinjoin_psbt sums per-input weights and uses output_script_type for output weights. estimate_fee_share uses worst-case-across-allowed-set. Both v1.3 and v1.4 cross-phase invariant gates green. CRIT-01 preserved (zero new detect_script_type call sites). WR-04 preserved (both build_coinjoin_psbt callers use config.bip.output_script_type).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: FEE-03 — two regression tests pinning v1.4 baseline byte-equal + mixed-script ≥1 sat divergence; cross-phase invariant verification</name>
  <files>coordinator/src/bitcoin/tx.rs</files>
  <read_first>
    - coordinator/src/bitcoin/tx.rs (current state after Task 2 — verify make_inputs_typed exists, ParticipantInput.script_type field exists, build_coinjoin_psbt takes output_script_type param)
    - .planning/phases/20-mixed-round-fee-accuracy/20-PATTERNS.md §"Add 6" + §"Add 7" (exact test bodies with derivation comments)
    - .planning/phases/20-mixed-round-fee-accuracy/20-CONTEXT.md §D-125 + §D-126 (the locked test names + assertion shapes + math)
    - .planning/phases/20-mixed-round-fee-accuracy/20-RESEARCH.md §"v1.4 baseline math (verifies D-125's 266)" + §"Mixed-script math (verifies D-126's ≥1 sat)" (verified derivations)
  </read_first>
  <behavior>
    - Test `fee_share_p2wpkh_only_matches_v14_baseline` constructs 3 P2WPKH inputs at 1_100_000 sats each (via existing make_inputs helper), denomination_sats=1_000_000, fee_rate_sat_per_vbyte=2, output_script_type=ScriptType::P2wpkh; computes fee_share from PSBT as `(total_in - total_out) / n`; asserts == 266 BYTE-EXACT.
    - Test `fee_share_mixed_script_differs_from_uniform_baseline` constructs mixed 1×P2WPKH + 1×P2TR + 1×P2SH-P2WPKH inputs (via make_inputs_typed) at 1_100_000 each, denomination_sats=1_000_000, fee_rate=2, output_script_type=P2wpkh; computes fee_share from PSBT; asserts `fee_share.saturating_sub(266) >= 1` with a helpful failure message including the computed fee_share value.
    - The mixed-script computed value is 275 (delta = 9 sats/participant); the `>= 1` threshold has 9-sat headroom. If a future revert to P2WPKH-only weights happens, the diff drops to 0 and the test fails — exactly the "per-script branch fires, not just compiles" sanity gate per CONTEXT D-126.
  </behavior>
  <action>
    Add both tests inline in the existing `#[cfg(test)] mod tests` block in `coordinator/src/bitcoin/tx.rs:131-224`, after the 6 vbyte-pin tests from Task 1. Test bodies follow PATTERNS.md Add 6 + Add 7 verbatim — including the multi-line derivation comments at the top of each test body (these comments are the audit-charter artifact Phase 21 cites; CONTEXT §"Specifics" calls them "the v1.3-invariant-from-the-fee-math-angle artifact").

    For `fee_share_p2wpkh_only_matches_v14_baseline`: use the existing `make_inputs(3, 1_100_000)` helper (CD-44 default — reuses convention from the existing 5 tests). Construct outputs via `make_outputs(n)`. Call `build_coinjoin_psbt(&inputs, &outputs, 1_000_000, 2, ScriptType::P2wpkh)`. Compute fee_share via `let total_in: u64 = inputs.iter().map(|i| i.value_sats).sum(); let total_out: u64 = psbt.unsigned_tx.output.iter().map(|o| o.value.to_sat()).sum(); let fee_share = (total_in - total_out) / (n as u64);`. Assert `assert_eq!(fee_share, 266, "v1.4 P2WPKH-only baseline must be byte-exact 266");`.

    For `fee_share_mixed_script_differs_from_uniform_baseline`: use `let types = [ScriptType::P2wpkh, ScriptType::P2tr, ScriptType::P2shP2wpkh]; let inputs = make_inputs_typed(&types, 1_100_000);`. Construct outputs via `make_outputs(n)`. Call `build_coinjoin_psbt(&inputs, &outputs, 1_000_000, 2, ScriptType::P2wpkh)`. Compute fee_share same as above. Assert `assert!(fee_share.saturating_sub(266) >= 1, "Mixed-script fee_share must exceed P2WPKH-only baseline by >=1 sat (got {fee_share}; would be 266 if per-script branch silently reverted)");`.

    Each test body MUST include the derivation comment block at the top per PATTERNS.md. For test 1: "v1.4 baseline (P2WPKH-only, n=3, fee_rate=2): estimated_vsize = TX_OVERHEAD + n*68 + (n + n)*31 = 10 + 3*68 + 6*31 = 10 + 204 + 186 = 400 vbytes; total_fee = 400 * 2 = 800 sats; fee_share = 800 / 3 = 266 sats (integer floor; 2-sat remainder absorbed)". For test 2: "mixed-script (n=3, fee_rate=2, output_type=P2WPKH): estimated_vsize = 10 + (68 + 58 + 91) + 6*31 = 10 + 217 + 186 = 413 vB; total_fee = 413 * 2 = 826 sats; fee_share = 826 / 3 = 275 sats; diff per participant: 275 - 266 = 9 sats (well above the ≥1 sat requirement)".

    NEVER refactor the integer floor in build_coinjoin_psbt (already preserved in Task 2 per RISK-1 hedge). NEVER replace the hardcoded `266` with a `v14_formula()` helper (D-125 explicitly chose the hardcoded value + derivation comment over a helper because "a future cleanup might delete it"; the hardcoded number + comment is more durable). NEVER hardcode `275` in test 2 — assert `>= 1 sat` delta per the requirement; this allows the per-script weights to evolve (e.g., if a future BIP-341 erratum changes the P2TR signature shape, the delta might shift but `>= 1` still holds).

    After both tests pass, run the full cross-phase invariant battery as the closing verification:
      - `cargo test -p coordinator --lib bitcoin::tx::tests` (8 vbyte + 2 FEE-03 + 5 pre-existing = 15+ tests all green)
      - `cargo test --test integration full_round` (8/8 green, ~42s — v1.3 invariant)
      - `cargo test --test integration mixed_script_e2e` (1/1 green — v1.4 invariant)
      - `cargo clippy --workspace --all-targets -- -D warnings` (clean)
      - CRIT-01 audit grep: `grep -rn 'detect_script_type' coordinator/src/ shared/src/` returns ONLY shared/src/bip322/mod.rs (def) + coordinator/src/bitcoin/utxo.rs:163,184 (pre-existing dispatcher branches) — zero new sites added across the 3-task plan
  </action>
  <verify>
    <automated>cargo test -p coordinator --lib bitcoin::tx::tests::fee_share_p2wpkh_only_matches_v14_baseline &amp;&amp; cargo test -p coordinator --lib bitcoin::tx::tests::fee_share_mixed_script_differs_from_uniform_baseline &amp;&amp; cargo test -p coordinator --lib bitcoin::tx::tests &amp;&amp; cargo test --test integration full_round &amp;&amp; cargo test --test integration mixed_script_e2e &amp;&amp; cargo clippy --workspace --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'fn fee_share_p2wpkh_only_matches_v14_baseline' coordinator/src/bitcoin/tx.rs` returns exactly one line
    - `grep -n 'fn fee_share_mixed_script_differs_from_uniform_baseline' coordinator/src/bitcoin/tx.rs` returns exactly one line
    - Test 1 body contains the literal `assert_eq!(fee_share, 266` (byte-exact baseline pin) — verify via `grep -A 1 'fee_share_p2wpkh_only_matches_v14_baseline' coordinator/src/bitcoin/tx.rs | grep -c 'assert_eq'` is at least 1; AND `grep -E 'assert_eq!\\(fee_share, 266' coordinator/src/bitcoin/tx.rs` returns at least 1 match
    - Test 2 body contains `saturating_sub(266) >= 1` (the ≥1-sat delta assertion) — verify via `grep -E 'saturating_sub\\(266\\) >= 1' coordinator/src/bitcoin/tx.rs` returns at least 1 match
    - Both tests' bodies contain multi-line derivation comments referencing the math (verify test 1 body contains `400 vbytes` and `800 sats` and `266 sats` literals; test 2 body contains `413 vB` and `826 sats` and `275 sats` and `9 sats` literals — `grep -A 20 'fee_share_p2wpkh_only_matches' coordinator/src/bitcoin/tx.rs | grep -cE '400 vbytes|800 sats|266 sats'` returns 3; similar for test 2)
    - `cargo test -p coordinator --lib bitcoin::tx::tests::fee_share_p2wpkh_only_matches_v14_baseline` passes
    - `cargo test -p coordinator --lib bitcoin::tx::tests::fee_share_mixed_script_differs_from_uniform_baseline` passes
    - `cargo test -p coordinator --lib bitcoin::tx::tests` returns ≥ 13 tests passing (5 pre-existing + 6 vbyte + 2 FEE-03)
    - `cargo test --test integration full_round` returns 8/8 green (v1.3 invariant final gate)
    - `cargo test --test integration mixed_script_e2e` returns 1/1 green (v1.4 invariant final gate)
    - `cargo clippy --workspace --all-targets -- -D warnings` clean
    - FINAL CRIT-01 audit gate: capture `grep -rn 'detect_script_type' coordinator/src/ shared/src/ | grep -v '^Binary'` output in the plan summary; result MUST match the pre-phase HEAD state (exactly the existing call sites at shared/src/bip322/mod.rs def + coordinator/src/bitcoin/utxo.rs:163,184). Zero new call sites across the entire Phase 20.
  </acceptance_criteria>
  <done>
    Both FEE-03 regression tests pass. The v1.4 baseline (266 sats) is now pinned byte-exact against accidental future refactors. The mixed-script delta (≥1 sat, computed 9 sats) proves the per-script branch fires. All cross-phase invariant gates green: v1.3 full_round 8/8, v1.4 mixed_script_e2e 1/1, clippy clean. CRIT-01 final audit grep matches pre-phase state — zero new detect_script_type call sites added.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bitcoin Core RPC → coordinator | `gettxout` returns the on-chain `script_pubkey.hex` — trusted as the authoritative source of the UTXO's script shape. `parse_script_pubkey_from_txout` (utxo.rs:95) parses it; `detect_script_type` derives `ScriptType` from this byte sequence. This is the SOLE trust boundary for `ScriptType` provenance in Phase 20. |
| Client → coordinator HTTP API | Untrusted. `RegisterInputRequest` is parsed at handlers.rs (POST /round/input). Phase 20 does NOT extend the wire format — `ParticipantInput.script_type` is NEVER read from client data. |
| Operator config → coordinator runtime | `BipConfig` loaded at boot from TOML/env per the config.rs layered pattern. `bip.output_script_type` and `bip.allow_*` flags are operator-controlled, validated at boot by `BipConfig::validate`. Phase 20 reads these in `estimate_fee_share` and `build_coinjoin_psbt` callers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Spoofing/Tampering | `ParticipantInput.script_type` derivation path (utxo.rs → state.rs → tx.rs) | mitigate | **CRIT-01 invariant preserved by construction.** Single derivation point at coordinator/src/bitcoin/utxo.rs:99 (existing `let derived = dispatch_ownership_proof(...)`). Phase 20 plumbs `derived` through `UtxoDetails.script_type` (return at utxo.rs:116) → `RegisteredInput.script_type` (write at input_reg.rs:82-88) → `ParticipantInput.script_type` (construction at handlers.rs:475 and signing.rs:124). ZERO new `detect_script_type` call sites permitted across the 3-task plan. **Audit gate (enforced as Task 1+2+3 acceptance criterion):** `grep -rn 'detect_script_type' coordinator/src/ shared/src/` post-phase = pre-phase HEAD state (exactly shared/src/bip322/mod.rs def + coordinator/src/bitcoin/utxo.rs:163,184). Auditor can read the data-flow diagram (RESEARCH §"System Architecture Diagram") + the grep result and verify the invariant in <5 minutes. |
| T-20-02 | Repudiation/DoS | `estimate_fee_share` underestimates fees for mixed-script rounds, causing tx mempool rejection / coordinator broadcast failure | mitigate | Conservative round-UP per STATE.md §v1.5 design notes line 2. P2TR vbyte = **58** (NOT 57; raw 57.5 ceiled via integer-arith `(witness + 3) / 4`). Worst-case-across-allowed-set in `estimate_fee_share` overcharges relative to any registration order. Per-input weight sum in `build_coinjoin_psbt` uses the actual chain-derived ScriptType. Task 1 acceptance criterion verifies P2tr arm contains the literal `STATE.md` rounding-policy divergence note inline. |
| T-20-03 | Tampering | Sighash mismatch between display (get_tx) and broadcast (assemble_and_broadcast) PSBTs — WR-04 violation, clients sign against different sighash than what hits mempool | mitigate | Single canonical `estimate_fee_share` helper consumed by BOTH `handlers.rs:165` (display) and `handlers.rs:505` (display) and `build_coinjoin_psbt` consumed by BOTH `handlers.rs::get_tx` (display) and `signing.rs::assemble_and_broadcast` (broadcast). Both `build_coinjoin_psbt` callers MUST pass the SAME `output_script_type` value (sourced from `state.config.bip.output_script_type` / `config.bip.output_script_type` where `config` is the same Arc<CoordinatorConfig>). Task 2 acceptance criterion verifies via grep that both call sites pass `config.bip.output_script_type`. The integer-floor `fee_share = total_fee / n` in tx.rs:70 is PRESERVED VERBATIM (RISK-1 hedge from RESEARCH). Cross-check at the integration tier: `cargo test --test integration full_round` (8/8 — v1.3 P2WPKH-only signing/broadcast still works) AND `cargo test --test integration mixed_script_e2e` (1/1 — v1.4 mixed-script broadcast still observable in regtest mempool). |
| T-20-04 | Information Disclosure | Privacy leak via fee_share fingerprinting (e.g., advertised `fee_per_participant_sats` reveals which script types have registered) | mitigate | Worst-case-across-allowed-set in `estimate_fee_share` (D-122) is UNIFORM regardless of registration order — it depends only on the operator's `BipConfig.allow_*` flags and `output_script_type`, never on the live registration set. CONTEXT §"Specifics" calls this out: "fee.rs::estimate_fee_share worst-case-from-BipConfig is the privacy-safe choice — using anything narrower (e.g., 'use the most-recently-registered script type') would leak ordering information across rounds." This is a soft but real privacy property worth preserving even though Phase 20 doesn't explicitly call it out as a threat model item. Acceptance: the formula at fee.rs uses `bip_config.allowed_set().map(script_input_vbytes).max()`, NOT any live-registration-driven helper. |
| T-20-05 | Tampering | `script_type` written into a PSBT proprietary key, affecting serialized PSBT bytes and breaking client sighash | accept | `ParticipantInput` has no `Serialize`/`Deserialize` derives (verified at tx.rs:17-23) and does NOT cross the wire. The new `script_type` field is consumed ONLY by the in-memory vsize loop in `build_coinjoin_psbt`. Task 2 acceptance criterion verifies no proprietary-key writes via review of the build_coinjoin_psbt body. RISK-3 hedge from RESEARCH. |
| T-20-SC | Tampering | npm/pip/cargo package supply-chain | not applicable | Phase 20 adds ZERO new dependencies. All work uses crates already in the workspace `Cargo.toml` (bitcoin = 0.32, shared path dep, tokio, tracing). No Package Legitimacy Audit needed; no slopcheck checkpoint required. Verified by RESEARCH §"Standard Stack". |

## ASVS Coverage

| ASVS Category | Applies | Notes |
|---------------|---------|-------|
| V5 Input Validation | yes | `ScriptType` derives from chain-validated `script_pubkey` (utxo.rs:99); never from client wire data. CRIT-01 invariant. Acceptance gate: T-20-01 grep. |
| V7 Error Handling & Logging | yes | Reuse existing PII-safe `tracing::info!(round_id, script_type = ?derived, ...)` at utxo.rs:110-114. Phase 20 adds zero new log call sites. `script_type` is an enum (3 variants), carries no PII. |

No new attack surface introduced. Phase 20's only new code paths read a value the dispatcher already produces and use it in arithmetic the operator already controls.
</threat_model>

<verification>
After Task 3 completes, run the full cross-phase invariant battery:

```bash
# Unit tests (15+ in tx.rs::tests: 5 pre-existing + 6 vbyte + 2 FEE-03; plus 1 optional in fee.rs::tests)
cargo test -p coordinator --lib bitcoin::tx::tests bitcoin::fee

# v1.3 P2WPKH invariant gate (8/8 green, ~42s)
cargo test --test integration full_round

# v1.4 mixed-script invariant gate (1/1 green)
cargo test --test integration mixed_script_e2e

# Static gates
cargo build -p coordinator
cargo clippy --workspace --all-targets -- -D warnings

# CRIT-01 audit gate (zero new detect_script_type call sites — capture output in summary)
grep -rn 'detect_script_type' coordinator/src/ shared/src/ | grep -v '^Binary'
# Expected: exactly the pre-existing sites at shared/src/bip322/mod.rs (def + any internal use)
# and coordinator/src/bitcoin/utxo.rs:163,184 (dispatch_ownership_proof v=1 and v=2 branches)

# FEE-01 audit gate (legacy consts fully gone)
grep -nE 'INPUT_WEIGHT_VBYTES|OUTPUT_WEIGHT_VBYTES' coordinator/src/
# Expected: zero matches

# WR-04 audit gate (single canonical fee helper, byte-identical PSBTs)
grep -nE 'config\.bip\.output_script_type' coordinator/src/api/handlers.rs coordinator/src/round/signing.rs
# Expected: at least one match in EACH file (both build_coinjoin_psbt call sites use the same source)

# Phase 20 STATE.md divergence note (P2tr = 58, not 57)
grep -A 6 'ScriptType::P2tr =>' coordinator/src/bitcoin/tx.rs | grep -c 'STATE.md'
# Expected: at least 1 (the rounding-policy divergence note from PATTERNS.md Add 1)
```

If ANY invariant gate goes red, apply REPAIR-01 lesson #4: abandon the structured plan and pivot to `/gsd:debug`.

Optional human-verification step (NOT a checkpoint — workflow.human_verify_mode is per-phase):
- Read the Task 1 vbyte derivation comments in coordinator/src/bitcoin/tx.rs and confirm the P2tr arm explicitly cites STATE.md's UP-rounding policy and the divergence from ROADMAP SC#1's "57" literal. The comment is the audit-charter artifact Phase 21 cites — it MUST be self-contained, no inline TODO/FIXME, no "see PR" hedge.
</verification>

<success_criteria>
The 5 Success Criteria from ROADMAP §Phase 20 are met:
1. ✅ `coordinator/src/bitcoin/tx.rs` exposes `pub const fn script_input_vbytes(ScriptType) -> u64` and `pub const fn script_output_vbytes(ScriptType) -> u64` returning 68/31, 58/43, 91/32 for P2WPKH/P2TR/P2SH-P2WPKH; legacy consts at tx.rs:11,13 are gone. (Note: P2TR = **58** per STATE.md round-UP, NOT 57 per ROADMAP literal — divergence documented inline in the source comment so reviewers see the policy override is intentional.) [Task 1]
2. ✅ `ParticipantInput.script_type: shared::bip322::ScriptType` field exists; `build_coinjoin_psbt` sums per-input weights via `script_input_vbytes(inp.script_type)` and uses `script_output_vbytes(output_script_type)` for outputs; the value is set by the coordinator at validate_utxo time via the existing `dispatch_ownership_proof` derivation (CRIT-01 preserved into the fee path, ZERO new `detect_script_type` call sites). [Task 2]
3. ✅ Regression test `fee_share_p2wpkh_only_matches_v14_baseline` in tx.rs::tests constructs a 3-participant uniform-P2WPKH round (fee_rate=2) and asserts `fee_share == 266` byte-exact. [Task 3]
4. ✅ Regression test `fee_share_mixed_script_differs_from_uniform_baseline` constructs a 3-participant 1×P2WPKH + 1×P2TR + 1×P2SH-P2WPKH round (output_script_type=P2WPKH, fee_rate=2) and asserts `fee_share - 266 >= 1` (computed delta = 9 sats/participant; comfortable headroom). [Task 3]
5. ✅ v1.3 `full_round::*` 8/8 green AND v1.4 `mixed_script_e2e_three_clients_broadcast` 1/1 green with the new fee math. [Task 2 + Task 3 verification]

Plus the implicit audit gates from the threat model:
- CRIT-01 grep returns the pre-phase HEAD set of call sites (zero new sites)
- WR-04 grep confirms both `build_coinjoin_psbt` call sites pass `config.bip.output_script_type` from the same Arc<CoordinatorConfig>
- FEE-01 audit gate: legacy consts fully gone (`grep -nE 'INPUT_WEIGHT_VBYTES|OUTPUT_WEIGHT_VBYTES' coordinator/src/` returns zero matches)
</success_criteria>

<output>
Create `.planning/phases/20-mixed-round-fee-accuracy/20-01-SUMMARY.md` when done.

Required summary contents:
- Per-task outcomes (1 line each: "Task N: <verb> <object> — <result>")
- Final `grep -rn 'detect_script_type' coordinator/src/ shared/src/` output (CRIT-01 audit evidence — zero new sites)
- Final `cargo test -p coordinator --lib bitcoin::tx::tests` test count + green/red status
- Final `cargo test --test integration full_round` result (expected 8/8)
- Final `cargo test --test integration mixed_script_e2e` result (expected 1/1)
- Commit SHAs for each task (3 atomic commits, never amended per CLAUDE.md)
- Any deviations from CONTEXT D-122..D-127 (none expected — CONTEXT is unusually complete; flag if found)
- The literal `fee_share` values computed by the two FEE-03 tests (266 P2WPKH-only, 275 mixed-script — proves the per-script branch fires)
</output>
</output>
