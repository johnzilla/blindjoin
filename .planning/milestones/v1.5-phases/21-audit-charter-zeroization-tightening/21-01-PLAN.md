---
phase: 21-audit-charter-zeroization-tightening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - coordinator/src/blind/rsa.rs
  - coordinator/src/round/state.rs
  - coordinator/src/round/signing.rs
  - coordinator/src/round/manager.rs
  - coordinator/src/round/input_reg.rs
  - coordinator/src/api/handlers.rs
autonomous: true
requirements: [AUDIT-03]
must_haves:
  decisions_covered:
    - "D-128: RoundSecretKey newtype lives INSIDE RsaBlindSigner; RoundStateInner.rsa_signer becomes Option<RsaBlindSigner>"
    - "D-129: Drop body is empty-crypto-body (PII-safe tracing::debug! only); transitive rsa-0.9.10 ZeroizeOnDrop does the cryptographic work per 21-RESEARCH OQ1"
    - "D-130: transition_to(Phase::Idle) at state.rs:194-200 is the SOLE drop trigger (verified by 21-RESEARCH OQ2 — exactly one self.inner = None site in coordinator/src/)"
    - "D-131: Two-test split — structural FSM test round_secret_key_dropped_on_round_end in state.rs (load-bearing) + best-effort scrub test round_secret_key_buffer_overwritten_on_drop in rsa.rs (CD-50 Linux-gated)"
    - "D-132: rsa.rs lines 18-22 D-07 comment REWRITTEN from 'best-effort only' to the bounded statement citing the transitive rsa::RsaPrivateKey Drop chain + Option<RsaBlindSigner> lifetime + transition_to(Phase::Idle) trigger + docs/AUDIT-CHARTER.md#rsa-secret-key-zeroization-window anchor"
    - "D-133: Plan split — 21-01 = AUDIT-03 only (this plan); 21-02 = AUDIT-01 + AUDIT-02"
  truths:
    - "RoundSecretKey newtype exists in coordinator/src/blind/rsa.rs and wraps BjSecretKey"
    - "RsaBlindSigner.secret_key field type is RoundSecretKey (not bare BjSecretKey)"
    - "RoundStateInner.rsa_signer field type is Option<RsaBlindSigner> (not bare RsaBlindSigner)"
    - "Drop for RoundSecretKey is implemented and emits a PII-safe tracing::debug! event under target 'blindjoin::audit'"
    - "The D-07 comment at the top of rsa.rs no longer says 'best-effort only'; it cites the transitive rsa::RsaPrivateKey ZeroizeOnDrop chain, names the bounded lifetime via Option<RsaBlindSigner> on RoundStateInner, names the trigger transition_to(Phase::Idle), and ends with a charter-anchor reference docs/AUDIT-CHARTER.md#rsa-secret-key-zeroization-window"
    - "Test round_secret_key_dropped_on_round_end is green (structural FSM test in state.rs)"
    - "Test round_secret_key_buffer_overwritten_on_drop is present in rsa.rs (best-effort scrub; may be gated #[cfg_attr(not(target_os = \"linux\"), ignore = ...)] per CD-50)"
    - "All 6 test-fixture sites construct rsa_signer: Some(RsaBlindSigner::generate().unwrap()) (or Some(signer) for the local-var form)"
    - "Production call sites at input_reg.rs:71 and handlers.rs:383 use .as_ref().expect(\"rsa_signer must be Some during active round\") (or per-phase variant) to traverse the Option"
    - "manager.rs:63 production field-init wraps the signer in Some(...)"
    - "cargo test --test integration full_round 8/8 green (v1.3 P2WPKH invariant)"
    - "cargo test --test integration mixed_script_e2e 1/1 green (v1.4 multi-script invariant)"
    - "cargo test -p coordinator --lib fee_share 2/2 green (Phase 20 FEE-03 invariant)"
    - "cargo clippy --workspace --all-targets -- -D warnings clean"
    - "cargo audit returns 0 vulnerabilities with the pre-Phase-21 .cargo/audit.toml (refresh in 21-02)"
    - "shared/ is UNTOUCHED (V1.4-CRIT-01 dispatcher-only public surface invariant)"
  artifacts:
    - path: "coordinator/src/blind/rsa.rs"
      provides: "RoundSecretKey newtype + Drop impl + rewritten D-07 doc-comment + new scrub test"
      contains: "pub struct RoundSecretKey"
    - path: "coordinator/src/round/state.rs"
      provides: "rsa_signer: Option<RsaBlindSigner> field + new structural test round_secret_key_dropped_on_round_end"
      contains: "Option<RsaBlindSigner>"
    - path: "coordinator/src/round/signing.rs"
      provides: "4 refreshed test fixtures wrapping rsa_signer in Some(...)"
    - path: "coordinator/src/round/manager.rs"
      provides: "production field-init rsa_signer: Some(signer) + test read site updated"
    - path: "coordinator/src/round/input_reg.rs"
      provides: "production call site uses .as_ref().expect(...) before .blind_sign(...)"
    - path: "coordinator/src/api/handlers.rs"
      provides: "production call site uses .as_ref().expect(...) before .public_key.clone()"
  key_links:
    - from: "coordinator/src/round/state.rs::transition_to"
      to: "coordinator/src/blind/rsa.rs::RoundSecretKey::drop"
      via: "self.inner = None at state.rs:195 → Drop for Option<RoundStateInner> → drop(Option<RsaBlindSigner>) → drop(RsaBlindSigner) → drop(RoundSecretKey)"
      pattern: "self\\.inner = None"
    - from: "coordinator/src/round/input_reg.rs"
      to: "coordinator/src/blind/rsa.rs::RsaBlindSigner::blind_sign"
      via: ".as_ref().expect(...).blind_sign(...)"
      pattern: "rsa_signer\\.as_ref\\(\\)\\.expect"
---

<objective>
Phase 21 Plan 01 closes REQUIREMENTS AUDIT-03: tighten the RSA SecretKey zeroization window from "best-effort upstream limitation" prose to a **structurally-bounded** Rust lifetime expressible as a type signature.

This plan ships:
1. A `RoundSecretKey(BjSecretKey)` newtype inside `coordinator/src/blind/rsa.rs`, with an empty-crypto-body `Drop` impl that emits a PII-safe `tracing::debug!` event (per 21-RESEARCH OQ1 RECOMMENDATION — the transitive `rsa::RsaPrivateKey` `Drop` does the cryptographically meaningful work).
2. Refactor `RsaBlindSigner.secret_key: BjSecretKey` to `RsaBlindSigner.secret_key: RoundSecretKey` (D-128).
3. Refactor `RoundStateInner.rsa_signer: RsaBlindSigner` to `RoundStateInner.rsa_signer: Option<RsaBlindSigner>` (D-128).
4. Rewrite the D-07 comment at `coordinator/src/blind/rsa.rs:18-22` to cite the **transitive** `rsa::RsaPrivateKey` `Drop`+`ZeroizeOnDrop` chain (`rsa-0.9.10/src/key.rs:76-84`), name the bounded lifetime via `Option<RsaBlindSigner>` on `RoundStateInner`, name the trigger `transition_to(Phase::Idle)` (`state.rs:194-200`), and end with the charter-anchor reference `docs/AUDIT-CHARTER.md#rsa-secret-key-zeroization-window` (D-132 + CD-46 + CD-49).
5. Mechanical refresh of 6 test-fixture sites + 1 production field-init to wrap `rsa_signer` in `Some(...)` (per 21-RESEARCH §7).
6. Mechanical refresh of 2 production call sites + 2 test read sites to traverse the new `Option` via `.as_ref().expect("rsa_signer must be Some during active round")` (per 21-RESEARCH §6).
7. Two new tests (D-131):
   - **Structural** (load-bearing): `round_secret_key_dropped_on_round_end` in `coordinator/src/round/state.rs::tests` — mirrors `transition_to_idle_clears_inner` (state.rs:262) and additionally asserts `state.inner.as_ref().unwrap().rsa_signer.is_some()` pre-transition.
   - **Best-effort scrub** (sanity-check): `round_secret_key_buffer_overwritten_on_drop` in `coordinator/src/blind/rsa.rs::tests` — capture DER fingerprint, drop, allocate 8MB probe, sweep. Gated `#[cfg_attr(not(target_os = "linux"), ignore = ...)]` per CD-50.

Purpose: REQUIREMENTS AUDIT-03 says "the structural lifetime bound is the load-bearing claim". After this plan, an auditor can read `coordinator/src/round/state.rs:103` (the `Option<RsaBlindSigner>` type signature) and **see** the lifetime bound rather than read a paragraph asking them to trust the project team's "best-effort" claim. Plan 21-02 then writes the charter prose that cites this type signature.

Output:
- `coordinator/src/blind/rsa.rs` — RoundSecretKey newtype + Drop + rewritten D-07 comment + new scrub test (~+60 LOC).
- `coordinator/src/round/state.rs` — rsa_signer field type + doc-comment + new structural test (~+45 LOC; ~-3 LOC removed).
- `coordinator/src/round/signing.rs` — 4 test-fixture wraps (~+4 LOC, ~-4 LOC).
- `coordinator/src/round/manager.rs` — 1 production wrap + 1 test read site (~+2 LOC).
- `coordinator/src/round/input_reg.rs` — 1 production call site (~+1 LOC).
- `coordinator/src/api/handlers.rs` — 1 production call site (~+1 LOC).

Cross-phase invariant boundary: this plan's CI gate uses the **pre-Phase-21** `.cargo/audit.toml`. The audit.toml refresh ships in 21-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-audit-charter-zeroization-tightening/21-CONTEXT.md
@.planning/phases/21-audit-charter-zeroization-tightening/21-RESEARCH.md
@.planning/phases/21-audit-charter-zeroization-tightening/21-PATTERNS.md

# Files this plan modifies — executor reads these to see current state, not assumptions.
@coordinator/src/blind/rsa.rs
@coordinator/src/round/state.rs

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase 2026-05-31. -->
<!-- Executor uses these directly — no codebase exploration needed. -->

From coordinator/src/blind/rsa.rs (current — pre-Phase-21):

```rust
pub type BjPublicKey = PublicKey<Sha384, PSS, Randomized>;
pub type BjSecretKey = SecretKey<Sha384, PSS, Randomized>;

pub struct RsaBlindSigner {
    pub public_key: BjPublicKey,
    secret_key: BjSecretKey, // ← Phase 21 changes to: secret_key: RoundSecretKey
}

impl RsaBlindSigner {
    pub fn generate() -> Result<Self, blind_rsa_signatures::Error>;
    pub fn public_key_hash(&self) -> [u8; 32];
    pub fn blind_sign(&self, blinded_msg: &BlindMessage) -> Result<BlindSignature, blind_rsa_signatures::Error>;
    pub fn from_der_secret_key(der: &[u8]) -> Result<Self, blind_rsa_signatures::Error>;
    pub fn secret_key_der(&self) -> Result<Vec<u8>, blind_rsa_signatures::Error>;
    pub fn public_key_spki_der(&self) -> Result<Vec<u8>, blind_rsa_signatures::Error>;
}
```

The 6 public methods stay UNCHANGED in signature. Internally, `blind_sign` becomes `self.secret_key.0.blind_sign(...)` (or `self.secret_key.as_inner().blind_sign(...)`); `from_der_secret_key` constructs `Self { public_key, secret_key: RoundSecretKey(secret_key) }`; `secret_key_der` calls `self.secret_key.0.to_der()` (or `self.secret_key.as_inner().to_der()`).

From coordinator/src/round/state.rs (current — pre-Phase-21):

```rust
pub struct RoundStateInner {
    pub rsa_signing_key: Vec<u8>,
    pub rsa_signer: RsaBlindSigner,   // ← Phase 21 changes to: Option<RsaBlindSigner>
    pub round_secret: [u8; 32],
    pub registered_inputs: HashMap<String, RegisteredInput>,
    pub redeemed_tokens: HashSet<[u8; 32]>,
    pub registered_outputs: Vec<RegisteredOutput>,
    pub partial_sigs: HashMap<String, Vec<u8>>,
    pub change_addresses: HashMap<String, String>,
}

impl RoundState {
    pub fn transition_to(&mut self, next: Phase) -> Result<(), TransitionError> {
        // ...
        if next == Phase::Idle {
            self.inner = None; // triggers ZeroizeOnDrop on RoundStateInner — line 195
            self.rsa_pubkey_hash = None;
            self.rsa_pubkey_der = None;
            self.participant_count = 0;
            self.round_id = Uuid::new_v4();
        }
        // ...
    }
}
```

Upstream interface (rsa-0.9.10/src/key.rs:76-84 — verified by 21-RESEARCH):

```rust
impl Drop for RsaPrivateKey {
    fn drop(&mut self) {
        self.d.zeroize();
        self.primes.zeroize();
        self.precomputed.zeroize();
    }
}
impl ZeroizeOnDrop for RsaPrivateKey {}
```

This unconditional upstream `Drop` chain is the cryptographically meaningful work. The Phase 21 `Drop for RoundSecretKey` body therefore does NO crypto — only emits a PII-safe `tracing::debug!` event.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce RoundSecretKey newtype + Drop + rewritten D-07 comment + best-effort scrub test (closes D-128 + D-128a + D-129 + D-132 + CD-46 + CD-47 + CD-49 + CD-50 + AUDIT-03 SC#3 + SC#4-half)</name>
  <files>coordinator/src/blind/rsa.rs</files>
  <read_first>
    - .planning/phases/21-audit-charter-zeroization-tightening/21-CONTEXT.md (D-128, D-128a, D-129, D-129a, D-132, CD-46, CD-47, CD-49, CD-50)
    - .planning/phases/21-audit-charter-zeroization-tightening/21-RESEARCH.md §"Open Question 1 — D-129 / D-129a / CD-47: RoundSecretKey::drop body shape" (the RECOMMENDATION block is the locked Drop body shape) AND §"Best-effort RAM-scan test mechanism (Standard Research #9 / CD-50)" (the locked test mechanism)
    - .planning/phases/21-audit-charter-zeroization-tightening/21-PATTERNS.md §"coordinator/src/blind/rsa.rs — newtype + Drop impl (21-01)" (locked analogs: state.rs:120-149 Drop body, state.rs:82-91 doc-comment, current rsa.rs:18-22 prose being replaced)
    - coordinator/src/blind/rsa.rs (current 163 lines — executor MUST see current state, especially the D-07 comment at lines 18-22 and RsaBlindSigner.secret_key at line 25)
  </read_first>
  <behavior>
    - Test `round_secret_key_buffer_overwritten_on_drop` (new, in `coordinator/src/blind/rsa.rs::tests`): construct an `RsaBlindSigner` via `RsaBlindSigner::generate().unwrap()`, capture `signer.secret_key_der().unwrap()` as `der_fingerprint`, take `let needle: Vec<u8> = der_fingerprint[100..132].to_vec();` (assert needle.len() == 32 AND der_fingerprint.len() >= 200), drop the signer via `drop(signer);`, allocate `let probe: Vec<u8> = vec![0u8; 8 * 1024 * 1024];`, sweep with `probe.windows(needle.len()).any(|w| w == needle.as_slice())`, assert the result is false. Gated `#[cfg_attr(not(target_os = "linux"), ignore = "non-portable heap layout; structural test in state.rs::round_secret_key_dropped_on_round_end is the unconditional gate (D-131)")]`. Best-effort sanity check; not load-bearing.
    - Existing tests in `rsa.rs::tests` (`blind_sign_round_trip`, `public_key_hash_is_32_bytes`, `public_key_hash_is_deterministic`, `unlinkability_two_tokens`, `spki_handshake_round_trip`) remain green unchanged.
  </behavior>
  <action>
    Apply five edits to `coordinator/src/blind/rsa.rs` in this order:

    **Edit A (REPLACE the D-07 comment at lines 18-22 ONLY — do NOT touch line 23+ struct keyword):** Replace the existing 5-line `/// NOTE on memory zeroing (D-07): ...` block with a NEW doc-comment block per D-132. New comment requirements (CD-46 lets executor tighten prose, load-bearing contract is rigid):
    - Opens with `/// NOTE on memory zeroing (D-07 + AUDIT-03):`
    - Names `RoundSecretKey` by symbol.
    - Names `Option<RsaBlindSigner>` on `RoundStateInner` as the lifetime bound.
    - Names `transition_to(Phase::Idle)` as the trigger and cites `state.rs:194-200`.
    - Cites the upstream chain: `<rsa::RsaPrivateKey as Drop>::drop` at `rsa-0.9.10/src/key.rs:76-82` zeroizes `d`, `primes`, `precomputed`; `impl ZeroizeOnDrop for RsaPrivateKey {}` at line 84 (UNCONDITIONAL, no feature flag). Cite verbatim line numbers.
    - States the inversion: the in-place crypto work is delegated to upstream `rsa`; the newtype's value is **lifetime expression**, NOT redundant scrub.
    - Ends with `See docs/AUDIT-CHARTER.md#rsa-secret-key-zeroization-window for the full threat-model treatment.`
    - Style: follow the analog at state.rs:82-91 (one-line summary → blank line → ID-prefixed paragraph → optional NOTE → charter anchor closer).

    **Edit B (MODIFY the `RsaBlindSigner` struct at lines 23-26):** Change `secret_key: BjSecretKey` to `secret_key: RoundSecretKey` (D-128). Preserve `// Never pub — unlinkability depends on key secrecy` inline comment. Preserve `pub public_key: BjPublicKey` (D-128a: public key stays bare; only the SecretKey is wrapped).

    **Edit C (INSERT the new `RoundSecretKey` newtype with `Drop` impl, placed BETWEEN the rewritten D-07 doc-comment (Edit A) and the `RsaBlindSigner` struct (Edit B) OR immediately after the `pub type` aliases at lines 1-11 — executor picks the placement that reads best given the surrounding comment flow, but the symbol MUST be `pub struct RoundSecretKey(BjSecretKey);`):** Add per D-128 + D-128a + D-129 + CD-47 (LOCKED to OQ1 RECOMMENDATION):
    - A doc-comment naming AUDIT-03, citing the Drop chain `transition_to(Phase::Idle) → drop(Option<RoundStateInner>) → drop(Option<RsaBlindSigner>) → drop(RoundSecretKey) → drop(BjSecretKey) → drop(rsa::RsaPrivateKey)` and the upstream zeroization at `rsa-0.9.10/src/key.rs:76-82`.
    - `pub struct RoundSecretKey(BjSecretKey);` (single-tuple field).
    - `impl RoundSecretKey` with two `pub(crate)` methods: `pub(crate) fn new(sk: BjSecretKey) -> Self { Self(sk) }` and `pub(crate) fn as_inner(&self) -> &BjSecretKey { &self.0 }` (Phase 21 keeps these `pub(crate)`; ALL usage is inside `coordinator/src/blind/rsa.rs` itself — `RsaBlindSigner`'s 6 methods call `self.secret_key.as_inner()` or `self.secret_key.0` to delegate).
    - `impl Drop for RoundSecretKey` with EMPTY-CRYPTO-BODY per OQ1 RECOMMENDATION — body is exactly one `tracing::debug!` call: `tracing::debug!(target: "blindjoin::audit", "RoundSecretKey dropped — rsa::RsaPrivateKey ZeroizeOnDrop fires transitively");`. NO `{:?}` formatter on self. NO field interpolation. NO `to_der()` call. NO `mem::replace` with dummy keygen. Per 21-RESEARCH "Anti-Patterns to Avoid" — those approaches are strictly worse than empty body.
    - If a `tracing` import is needed, add `use tracing;` at the top of the file (current file does not import tracing — verify before edit; existing coordinator modules import via the `tracing` crate at workspace level).

    **Edit D (REWIRE the 5 RsaBlindSigner method bodies that touch `self.secret_key`):** Update each of these bodies to traverse the newtype:
    - `generate()` body at line 33-34: change `Ok(Self { public_key: kp.pk, secret_key: kp.sk })` to `Ok(Self { public_key: kp.pk, secret_key: RoundSecretKey::new(kp.sk) })` (or `RoundSecretKey(kp.sk)` — both are valid per the newtype's `pub(crate) fn new` constructor).
    - `blind_sign()` body at line 47-49: change `self.secret_key.blind_sign(blinded_msg)` to `self.secret_key.as_inner().blind_sign(blinded_msg)` (or `self.secret_key.0.blind_sign(blinded_msg)` — pick the more readable; `.as_inner()` is the documented accessor).
    - `from_der_secret_key()` body at line 53-57: change `Ok(Self { public_key, secret_key })` to `Ok(Self { public_key, secret_key: RoundSecretKey::new(secret_key) })`.
    - `secret_key_der()` body at line 60-62: change `self.secret_key.to_der()` to `self.secret_key.as_inner().to_der()`.
    - Public method signatures STAY UNCHANGED (D-128 lock: externals stay stable).

    **Edit E (ADD the best-effort scrub test):** Append inside the `#[cfg(test)] mod tests { ... }` block (currently lines 70-163) a new test function `round_secret_key_buffer_overwritten_on_drop` per the `<behavior>` block above and per 21-RESEARCH §"Code Examples > Proposed Best-Effort Scrub Test (CD-50)" (the source-code sketch in RESEARCH is the executor's template — copy structurally, do not invent a new mechanism). The test body must:
    - Use `RsaBlindSigner::generate().unwrap()` to obtain a signer with a known-RSA-2048 key.
    - Capture `signer.secret_key_der().unwrap()` BEFORE drop.
    - Take a 32-byte middle slice (`der_fingerprint[100..132]`) — assert `der_fingerprint.len() >= 200`.
    - `drop(signer);`
    - Allocate `vec![0u8; 8 * 1024 * 1024]` and sweep with `windows(32).any(|w| w == needle.as_slice())`.
    - Assert NOT found with a message naming `round_secret_key_dropped_on_round_end` (the sibling structural test) as the load-bearing claim.
    - Decorate `#[cfg_attr(not(target_os = "linux"), ignore = "non-portable heap layout; structural test in state.rs::round_secret_key_dropped_on_round_end is the unconditional gate (D-131)")]`. (CD-50 default; if a future toolchain flakes even on Linux, executor may upgrade to bare `#[ignore = "..."]` with reason naming the structural test as the unconditional gate.)

    Do NOT add `Zeroize` / `ZeroizeOnDrop` derives to `RoundSecretKey` (the wrapped `BjSecretKey` provides no `Zeroize` impl — the transitive `rsa::RsaPrivateKey` `Drop` is what does the work). Do NOT promote `RoundSecretKey` to `shared/` (deferred per CONTEXT.md `<deferred>`). Do NOT wrap `RsaBlindSigner.public_key` (D-128a explicit reject).
  </action>
  <verify>
    <automated>cargo test -p coordinator --lib blind::rsa::tests -- --include-ignored 2>&amp;1 | tee /tmp/21-01-task1-test.txt &amp;&amp; grep -E "test result: ok\. [0-9]+ passed" /tmp/21-01-task1-test.txt &amp;&amp; cargo clippy -p coordinator --lib --all-targets -- -D warnings &amp;&amp; grep -q "pub struct RoundSecretKey(BjSecretKey)" coordinator/src/blind/rsa.rs &amp;&amp; grep -q "impl Drop for RoundSecretKey" coordinator/src/blind/rsa.rs &amp;&amp; grep -q "blindjoin::audit" coordinator/src/blind/rsa.rs &amp;&amp; grep -q "rsa-0.9.10/src/key.rs:76-82" coordinator/src/blind/rsa.rs &amp;&amp; grep -q "rsa-secret-key-zeroization-window" coordinator/src/blind/rsa.rs &amp;&amp; grep -q "Option<RsaBlindSigner>" coordinator/src/blind/rsa.rs &amp;&amp; ! grep -q "best-effort only" coordinator/src/blind/rsa.rs</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `pub struct RoundSecretKey(BjSecretKey);` exists in `coordinator/src/blind/rsa.rs` (single-tuple newtype, D-128).
    - Source assertion: `impl Drop for RoundSecretKey` exists; body contains EXACTLY one `tracing::debug!(target: "blindjoin::audit", ...)` call with a static message; body does NOT call `to_der()`, `mem::replace`, `zeroize()`, or interpolate `self` (D-129 lock + 21-RESEARCH Pitfall 2).
    - Source assertion: the rewritten D-07 comment in `coordinator/src/blind/rsa.rs` contains all five required tokens: `RoundSecretKey`, `Option<RsaBlindSigner>`, `transition_to(Phase::Idle)`, `rsa-0.9.10/src/key.rs:76-82`, and the anchor `docs/AUDIT-CHARTER.md#rsa-secret-key-zeroization-window` (D-132 + CD-46 + CD-49).
    - Source assertion: the substring `best-effort only` is ABSENT from `coordinator/src/blind/rsa.rs` (the prose inversion per OQ1 — the old "best-effort only" qualifier is replaced).
    - Source assertion: `RsaBlindSigner.secret_key` field's declared type is `RoundSecretKey` (NOT `BjSecretKey`). Verified by `grep -nE 'secret_key: (RoundSecretKey|BjSecretKey)' coordinator/src/blind/rsa.rs` returning the `RoundSecretKey` form only (the field declaration; the 2 method-body sites at `generate()` and `from_der_secret_key()` also reference RoundSecretKey via `::new()`).
    - Behavior assertion: `cargo test -p coordinator --lib blind::rsa::tests -- --include-ignored` returns 0 failures. On Linux, `round_secret_key_buffer_overwritten_on_drop` runs and passes; on macOS, it is reported as ignored with the reason string (CD-50).
    - Behavior assertion: all 5 pre-existing tests in `rsa.rs::tests` (`blind_sign_round_trip`, `public_key_hash_is_32_bytes`, `public_key_hash_is_deterministic`, `unlinkability_two_tokens`, `spki_handshake_round_trip`) remain green.
    - Behavior assertion: `cargo clippy -p coordinator --lib --all-targets -- -D warnings` exits 0. The empty Drop body must NOT trigger `clippy::needless_drop_impl` because we have a side-effecting body (`tracing::debug!`). If clippy surprises us, the executor adds `#[allow(clippy::needless_drop_impl)]` with an inline comment naming the side-effect; this is unlikely per 21-RESEARCH Pitfall 3 (clippy `restriction` lints are not denied by `-D warnings`).
    - PII-safety: no `{:?}` formatter on `self` or any inner field; only static-string message.
  </acceptance_criteria>
  <done>
    `coordinator/src/blind/rsa.rs` defines `pub struct RoundSecretKey(BjSecretKey)` with the empty-crypto Drop body emitting a `tracing::debug!` event under target `blindjoin::audit`; `RsaBlindSigner.secret_key` is typed `RoundSecretKey`; the rewritten D-07 comment cites the transitive `rsa::RsaPrivateKey` `Drop` chain and ends with the charter anchor; the best-effort scrub test exists with the platform gate per CD-50; all rsa.rs tests pass; clippy is clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor RoundStateInner.rsa_signer to Option&lt;RsaBlindSigner&gt; + propagate to all 4 production + 6 test fixture sites + add structural FSM test (closes D-128 second half + D-130 + D-130a + D-130b + D-131 first bullet + AUDIT-03 SC#3 second half + SC#4 first half)</name>
  <files>coordinator/src/round/state.rs, coordinator/src/round/signing.rs, coordinator/src/round/manager.rs, coordinator/src/round/input_reg.rs, coordinator/src/api/handlers.rs</files>
  <read_first>
    - .planning/phases/21-audit-charter-zeroization-tightening/21-CONTEXT.md §B/§C/§D (D-128, D-130, D-130a, D-130b, D-131 — FSM trigger surface + structural test pattern + Option semantics)
    - .planning/phases/21-audit-charter-zeroization-tightening/21-RESEARCH.md §"Open Question 2 — D-130a: confirm the 4 valid Idle transitions all reach transition_to(Phase::Idle)" (RECOMMENDATION block — confirms single chokepoint at state.rs:195 with no bypass) AND §"Production call sites of inner.rsa_signer.* (Standard Research #6)" (LOCKED 2-site production list at input_reg.rs:71 and handlers.rs:383 + LOCKED 2-site test-read list at state.rs:321 and manager.rs:195) AND §"Test-fixture call sites (Standard Research #7)" (LOCKED 6-site test-fixture wrap list at state.rs:270, state.rs:311, signing.rs:450, signing.rs:496, signing.rs:521, signing.rs:560; PRODUCTION field-init at manager.rs:63) AND §"Clippy lint config (Standard Research #8)" (RECOMMENDATION: `.as_ref().expect("rsa_signer must be Some during &lt;phase&gt;")` is the pattern)
    - .planning/phases/21-audit-charter-zeroization-tightening/21-PATTERNS.md §"coordinator/src/round/state.rs — RoundStateInner.rsa_signer: Option&lt;RsaBlindSigner&gt;" + §"coordinator/src/round/state.rs::tests::round_secret_key_dropped_on_round_end — ADD test" + §"coordinator/src/round/signing.rs, state.rs, output_reg.rs — test-fixture refreshes" + §"coordinator/src/round/input_reg.rs:71 + coordinator/src/api/handlers.rs:383 — production call-site refreshes"
    - coordinator/src/round/state.rs (full file — executor sees current `pub rsa_signer: RsaBlindSigner` at line 103, the existing `transition_to_idle_clears_inner` analog at lines 261-286, the existing `rsa_signer_consistent_with_key_bytes` test at lines 297-328 that reads `inner.rsa_signer.public_key_hash()` at line 321)
  </read_first>
  <behavior>
    - New test `round_secret_key_dropped_on_round_end` in `coordinator/src/round/state.rs::tests` (D-131 first bullet, LOAD-BEARING per REQUIREMENTS): construct a `RoundState` via `RoundState::new_idle()`, set `state.phase = Phase::Signing`, set `state.rsa_pubkey_der = Some(vec![1, 2, 3])`, set `state.inner = Some(RoundStateInner { rsa_signing_key: vec![0xAA; 32], rsa_signer: Some(RsaBlindSigner::generate().unwrap()), round_secret: [0xBB; 32], registered_inputs: Default::default(), redeemed_tokens: HashSet::new(), registered_outputs: vec![], partial_sigs: Default::default(), change_addresses: Default::default() })`. PRE-transition assertion (the new claim beyond `transition_to_idle_clears_inner`): `assert!(state.inner.as_ref().unwrap().rsa_signer.is_some(), "fixture must construct with Some(RsaBlindSigner)");`. Drive `state.transition_to(Phase::Broadcast).unwrap(); state.transition_to(Phase::Idle).unwrap();`. POST-transition assertions: `assert!(state.inner.is_none(), "AUDIT-03: RoundStateInner must be dropped on Idle transition");` and `assert_eq!(state.phase, Phase::Idle);`. Doc-comment opens with `/// AUDIT-03 (D-131):` and explains that this is the load-bearing structural test (the sibling scrub test in rsa.rs is sanity-only).
    - This test name `round_secret_key_dropped_on_round_end` is per CONTEXT.md D-131 (executor MUST use this exact name; ROADMAP SC#4 references `round_secret_key_zeroes_on_drop` as an equivalent contract — CONTEXT.md takes precedence per the orchestrator's must_haves_guidance; both names address the same SC).
    - All 6 pre-existing test-fixture sites construct `rsa_signer: Some(RsaBlindSigner::generate().unwrap())` (or `Some(signer)` for the local-var form at state.rs:311).
    - All 2 production call sites + 2 test read sites traverse the new `Option<RsaBlindSigner>` via `.as_ref().expect("rsa_signer must be Some during <phase>")` (per 21-RESEARCH §8 — pattern is clippy-clean; phase string per RESEARCH §6 table: `"InputReg"` for input_reg.rs:71, `"OutputReg"` for handlers.rs:383 since the call is via `guard.inner.as_ref().ok_or_else(...).rsa_signer...`).
    - The single production field-init at `coordinator/src/round/manager.rs:63` wraps the signer in `Some(...)`.
  </behavior>
  <action>
    Apply edits across 5 files in this order (one logical group per file). Files have NO `files_modified` overlap with Task 1 (rsa.rs) — Task 1 must commit first OR be staged together; the orchestrator decides via wave-1 single-plan-multiple-task semantics (no inter-plan dep).

    **Edit A — `coordinator/src/round/state.rs::RoundStateInner.rsa_signer` field declaration (line 103) and its doc-comment (lines 97-102):**
    Change field type from `pub rsa_signer: RsaBlindSigner` to `pub rsa_signer: Option<RsaBlindSigner>`. Rewrite the doc-comment per 21-PATTERNS §"Option<_> field doc-comment convention":
    - Drop the sentence "Not zeroized on drop (upstream SecretKey limitation; raw bytes above ARE zeroed)." — this is INVERTED by Phase 21 (the wrapped `RoundSecretKey` IS zeroized on drop via the transitive `rsa::RsaPrivateKey` ZeroizeOnDrop chain).
    - Replace with: `/// Parsed RSA blind signer wrapped in Option per AUDIT-03 — Some(_) during an active round, None when Idle. The bounded lifetime (Option set to None at state.rs:195) is the structural mitigation for RUSTSEC-2023-0071 timing-sidechannel exposure (see coordinator/src/blind/rsa.rs::RoundSecretKey for the Drop chain).` + preserve the "Constructed in production by `round::manager::start_round`" sentence.

    **Edit B — `coordinator/src/round/state.rs::tests::transition_to_idle_clears_inner` fixture refresh (line 270):**
    Inside the existing test at lines 261-286, change `rsa_signer: RsaBlindSigner::generate().unwrap(),` (line 270) to `rsa_signer: Some(RsaBlindSigner::generate().unwrap()),`. No other change to this pre-existing test.

    **Edit C — `coordinator/src/round/state.rs::tests::rsa_signer_consistent_with_key_bytes` fixtures refresh (lines 311 + 321):**
    - Line 311: change `rsa_signer: signer,` to `rsa_signer: Some(signer),` (the `signer` local var was constructed by `RsaBlindSigner::generate().unwrap()` 6 lines above at line 304 — wrap the move in `Some(...)`).
    - Line 321: change `assert_eq!(inner.rsa_signer.public_key_hash(), expected_hash,` to `assert_eq!(inner.rsa_signer.as_ref().expect("test fixture: rsa_signer is Some").public_key_hash(), expected_hash,`.

    **Edit D — `coordinator/src/round/state.rs::tests` add new structural test `round_secret_key_dropped_on_round_end`:**
    Insert immediately AFTER the existing `transition_to_idle_clears_inner` test (line 286) and BEFORE `invalid_transition_returns_err` (line 288). Test body per `<behavior>` block above. Doc-comment opens `/// AUDIT-03 (D-131): structural FSM test — load-bearing claim per REQUIREMENTS AUDIT-03 ("structural lifetime bound is load-bearing"). Mirrors transition_to_idle_clears_inner at state.rs:262 with an additional pre-transition assertion that rsa_signer is Some(_) — guaranteeing the Drop chain on Idle transition fires on a non-None RoundSecretKey (which transitively zeroizes the rsa::RsaPrivateKey at rsa-0.9.10/src/key.rs:76-82).`

    **Edit E — `coordinator/src/round/signing.rs` 4 test-fixture refreshes (lines 450, 496, 521, 560):**
    At each of the 4 listed lines, change `rsa_signer: RsaBlindSigner::generate().unwrap(),` to `rsa_signer: Some(RsaBlindSigner::generate().unwrap()),`. Mechanical wrap. NO other field changes; no clippy concern per 21-RESEARCH Pitfall 3.

    **Edit F — `coordinator/src/round/manager.rs:63` production field-init + `coordinator/src/round/manager.rs:195` test-read site:**
    - Line 63 (inside `start_round`, the single production constructor): change `rsa_signer: signer,` to `rsa_signer: Some(signer),`. The local `signer` is produced 1-2 lines above by `RsaBlindSigner::generate()?` (verify by reading 60-70 region pre-edit).
    - Line 195 (inside the `#[cfg(test)] mod tests` block, test name per 21-RESEARCH §6: this is part of an AVAIL-02 consistency check): change `let pk_hash_from_signer = inner.rsa_signer.public_key_hash();` to `let pk_hash_from_signer = inner.rsa_signer.as_ref().expect("test fixture: rsa_signer is Some").public_key_hash();`.

    **Edit G — `coordinator/src/round/input_reg.rs:71` production call site:**
    Locate the line `let blind_sig = inner.rsa_signer.blind_sign(&blind_msg).map_err(...)` (per 21-RESEARCH §6 the line is 71). Change to `let blind_sig = inner.rsa_signer.as_ref().expect("rsa_signer must be Some during InputReg").blind_sign(&blind_msg).map_err(...)`. The closure body inside `.map_err(...)` is unchanged.

    **Edit H — `coordinator/src/api/handlers.rs:380-383` production call site:**
    Locate the multi-line expression that reads `let rsa_public_key = guard.inner.as_ref().ok_or_else(...)?.rsa_signer.public_key.clone();` (per 21-RESEARCH §6 the `.rsa_signer.public_key.clone()` is at line 383). Change the inner `.rsa_signer.public_key.clone()` to `.rsa_signer.as_ref().expect("rsa_signer must be Some during OutputReg").public_key.clone()`. The outer `.inner.as_ref().ok_or_else(...)?` already handles the outer Option correctly — preserve it.

    Do NOT add explicit `signer = None` helpers (D-130b reject). Do NOT modify `coordinator/src/round/output_reg.rs::make_valid_token_sig` — it takes `&RsaBlindSigner` from a local `signer` var and is UNAFFECTED (21-RESEARCH Pitfall 4). Do NOT modify `coordinator/src/round/blame.rs` — `transition_to(Phase::Idle)` calls at blame.rs:220 and other FSM-edge sites already route through state.rs:195 (D-130a verified). Do NOT modify `coordinator/src/round/state.rs::Drop for RoundStateInner` body (lines 120-149) — the existing Drop body's zeroize-the-HashMaps logic is unchanged; the Option<RsaBlindSigner> inner is dropped as part of natural struct drop. (Per CONTEXT.md `<code_context>`: "the Drop body itself may stay structurally identical".)

    After all 5-file edits, run `grep -rn 'rsa_signer' coordinator/src/round/output_reg.rs coordinator/src/round/blame.rs coordinator/src/run.rs coordinator/src/round/manager.rs` to confirm NO further references to `inner.rsa_signer.*` remain that bypass the `Option`. (Expected matches in output_reg.rs go through `&signer` locals, not `inner.rsa_signer.*` — UNAFFECTED.)
  </action>
  <verify>
    <automated>cargo test -p coordinator --lib round::state::tests::round_secret_key_dropped_on_round_end -- --exact &amp;&amp; cargo test -p coordinator --lib round::state::tests -- --skip round_secret_key_dropped_on_round_end &amp;&amp; cargo test -p coordinator --lib round::manager::tests &amp;&amp; cargo test -p coordinator --lib round::signing::tests &amp;&amp; cargo test -p coordinator --lib round::input_reg::tests &amp;&amp; cargo test -p coordinator --lib api::handlers::tests 2>/dev/null || true; cargo build -p coordinator --all-targets &amp;&amp; cargo clippy --workspace --all-targets -- -D warnings &amp;&amp; grep -q "pub rsa_signer: Option<RsaBlindSigner>" coordinator/src/round/state.rs &amp;&amp; grep -q "round_secret_key_dropped_on_round_end" coordinator/src/round/state.rs &amp;&amp; grep -q 'rsa_signer\.as_ref()\.expect("rsa_signer must be Some during InputReg")' coordinator/src/round/input_reg.rs &amp;&amp; grep -q 'rsa_signer\.as_ref()\.expect("rsa_signer must be Some during OutputReg")' coordinator/src/api/handlers.rs &amp;&amp; grep -cE 'rsa_signer: Some\(' coordinator/src/round/signing.rs | awk '{ if ($1 != "4") { print "FAIL: expected 4 Some-wrapped fixtures in signing.rs, got " $1; exit 1 } else print "OK: 4 fixtures wrapped" }' &amp;&amp; grep -cE 'rsa_signer: Some\(' coordinator/src/round/state.rs | awk '{ if ($1 != "2") { print "FAIL: expected 2 Some-wrapped fixtures in state.rs, got " $1; exit 1 } else print "OK: 2 fixtures wrapped" }' &amp;&amp; grep -q 'rsa_signer: Some(signer)' coordinator/src/round/manager.rs</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion (state.rs): the field declaration `pub rsa_signer: Option<RsaBlindSigner>` exists on `RoundStateInner`. The previous `pub rsa_signer: RsaBlindSigner` declaration is gone (grep for `'pub rsa_signer: RsaBlindSigner,'` returns 0 matches).
    - Source assertion (state.rs): a new test function `fn round_secret_key_dropped_on_round_end` exists in `mod tests`, with doc-comment opening `/// AUDIT-03 (D-131)`.
    - Source assertion (state.rs): the doc-comment for the `rsa_signer` field NO LONGER contains "Not zeroized on drop" (the prose was inverted by Phase 21).
    - Source assertion (input_reg.rs): the production call site at the location of `let blind_sig = inner.rsa_signer` uses `.as_ref().expect("rsa_signer must be Some during InputReg").blind_sign(...)`.
    - Source assertion (handlers.rs): the production call site at the location of `.rsa_signer.public_key.clone()` uses `.rsa_signer.as_ref().expect("rsa_signer must be Some during OutputReg").public_key.clone()`.
    - Source assertion (manager.rs): the production field-init at the `start_round` body inside the `RoundStateInner { ... }` struct-literal contains `rsa_signer: Some(signer),`.
    - Source assertion (signing.rs): exactly 4 occurrences of `rsa_signer: Some(` (matching the 4 known fixtures at the 21-RESEARCH-locked lines).
    - Source assertion (state.rs): exactly 2 occurrences of `rsa_signer: Some(` (matching the 2 known fixtures at the 21-RESEARCH-locked lines 270 + 311).
    - Behavior assertion: the new test `round_secret_key_dropped_on_round_end` passes (`cargo test -p coordinator --lib round::state::tests::round_secret_key_dropped_on_round_end -- --exact` returns ok).
    - Behavior assertion: all pre-existing `coordinator/src/round/state.rs::tests` pass (no regression in `transition_to_idle_clears_inner`, `rsa_signer_consistent_with_key_bytes`, `input_reg_to_idle_resets_round`, etc.).
    - Behavior assertion: `cargo build -p coordinator --all-targets` succeeds (the Option refactor compiles cleanly across all 5 modified files; no missed call sites).
    - Behavior assertion: `cargo clippy --workspace --all-targets -- -D warnings` exits 0 (the `.as_ref().expect(...)` pattern does not trip any default-warn lint per 21-RESEARCH §8).
    - Untouched-files assertion: `git diff --name-only HEAD shared/ client/ -- 2>/dev/null` returns empty (V1.4-CRIT-01 dispatcher-only public surface invariant — Phase 21 does NOT modify `shared/` or `client/`).
  </acceptance_criteria>
  <done>
    `RoundStateInner.rsa_signer` is `Option<RsaBlindSigner>`; the new structural FSM test `round_secret_key_dropped_on_round_end` is green; all 6 test-fixture sites and 1 production field-init wrap the signer in `Some(...)`; both production call sites and both test read sites traverse the Option via `.as_ref().expect(...)`; cargo build, cargo clippy, and the targeted cargo test commands all succeed; `shared/` and `client/` are untouched.
  </done>
</task>

<task type="auto">
  <name>Task 3: Full-stack cross-phase invariant verification at the 21-01 boundary (closes AUDIT-03 SC#5 first half + all 6 cross-phase invariants from CONTEXT.md)</name>
  <files>(no source edits — verification only)</files>
  <read_first>
    - .planning/phases/21-audit-charter-zeroization-tightening/21-CONTEXT.md §"Cross-phase invariants (carry to every Phase 21 plan boundary)" (the 6-item list — all must be GREEN before this plan can ship)
    - .planning/ROADMAP.md §"Phase 21" SC#5 (cargo test + cargo clippy + cargo audit invariants)
    - .planning/STATE.md §"Carry-Forward Items" and §"Recent Plan Decisions" (Phase 20 FEE-03 test names that must stay green at this boundary)
  </read_first>
  <behavior>
    - The 4 v1.3/v1.4/v1.5 integration + unit test suites stay green at this plan boundary (Task 1 + Task 2 changes only refactor type signatures + add a Drop body; semantically the runtime path is identical).
    - The cargo audit gate is green with the PRE-Phase-21 .cargo/audit.toml (the audit.toml refresh ships in 21-02; this plan must not regress it).
    - The clippy gate is green at `-D warnings`.
  </behavior>
  <action>
    Run the 6 cross-phase invariant checks in sequence. ALL must pass. On any failure, do NOT mark this plan complete — invoke REPAIR-01 lesson #4: abandon the structured plan, pivot to `/gsd:debug` to triage the regression, then retry this task.

    1. **v1.3 P2WPKH invariant:** `cargo test --test integration full_round -- --nocapture 2>&1 | tee /tmp/21-01-task3-fullround.txt` — expect `test result: ok. 8 passed; 0 failed` in ~42s. If anything is `ignored`, that's pre-existing (not caused by Phase 21) — verify by `git stash` of Phase 21 changes, re-run, compare; if delta then Phase 21 caused regression.
    2. **v1.4 multi-script invariant:** `cargo test --test integration mixed_script_e2e -- --nocapture 2>&1 | tee /tmp/21-01-task3-mixed.txt` — expect `test result: ok. 1 passed; 0 failed`.
    3. **v1.5 fee-accuracy invariant (Phase 20 FEE-03):** `cargo test -p coordinator --lib bitcoin::tx::tests::fee_share_p2wpkh_only_matches_v14_baseline -- --exact` AND `cargo test -p coordinator --lib bitcoin::tx::tests::fee_share_mixed_script_differs_from_uniform_baseline -- --exact` — each expect 1 passed.
    4. **Coordinator full lib test suite:** `cargo test -p coordinator --lib 2>&1 | tee /tmp/21-01-task3-lib.txt` — expect `test result: ok. N passed; 0 failed` (N is whatever the current count is; the constraint is `0 failed`, no regressions). The two NEW tests from Task 1/2 (`round_secret_key_buffer_overwritten_on_drop` and `round_secret_key_dropped_on_round_end`) are part of this run; the scrub test may show `ignored` on macOS.
    5. **Clippy invariant:** `cargo clippy --workspace --all-targets -- -D warnings 2>&1 | tee /tmp/21-01-task3-clippy.txt` — expect exit 0 with no warnings printed.
    6. **Cargo audit invariant (with pre-Phase-21 audit.toml — this is the LAST plan boundary using the unrefreshed audit.toml):** `cargo audit 2>&1 | tee /tmp/21-01-task3-audit.txt` — expect `Success No vulnerable packages found`. The 3 existing ignores (RUSTSEC-2023-0071, RUSTSEC-2025-0141, RUSTSEC-2024-0436) are EXPECTED to be ignored; this plan does not touch audit.toml, so the gate must pass identically to the pre-Phase-21 state.

    On the V1.4-CRIT-01 cross-check (also from CONTEXT.md cross-phase invariants):

    7. **`shared/` untouched:** `git diff --name-only HEAD -- shared/ client/` — expect empty output. Phase 21 plan 01 is strictly coordinator-local.

    Write a one-line completion note to `/tmp/21-01-task3-summary.txt` summarizing the 7 checks (e.g., `21-01 Task 3: 8/8 fullround, 1/1 mixed_e2e, 2/2 FEE-03, N/N lib (M ignored on macOS), 0 clippy warnings, 0 audit vulns, 0 shared/+client/ diff. ALL INVARIANTS GREEN.`).
  </action>
  <verify>
    <automated>cargo test --test integration full_round 2>&amp;1 | grep -E "test result: ok\. 8 passed" &amp;&amp; cargo test --test integration mixed_script_e2e 2>&amp;1 | grep -E "test result: ok\. 1 passed" &amp;&amp; cargo test -p coordinator --lib bitcoin::tx::tests::fee_share_p2wpkh_only_matches_v14_baseline -- --exact 2>&amp;1 | grep -E "test result: ok\. 1 passed" &amp;&amp; cargo test -p coordinator --lib bitcoin::tx::tests::fee_share_mixed_script_differs_from_uniform_baseline -- --exact 2>&amp;1 | grep -E "test result: ok\. 1 passed" &amp;&amp; cargo clippy --workspace --all-targets -- -D warnings &amp;&amp; cargo audit 2>&amp;1 | grep -E "^Success|no vulnerabilities" &amp;&amp; test -z "$(git diff --name-only HEAD -- shared/ client/)"</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: `cargo test --test integration full_round` → 8/8 passed, 0 failed (v1.3 P2WPKH invariant).
    - Behavior assertion: `cargo test --test integration mixed_script_e2e` → 1/1 passed, 0 failed (v1.4 multi-script invariant).
    - Behavior assertion: both Phase 20 FEE-03 tests pass exactly (`fee_share_p2wpkh_only_matches_v14_baseline` + `fee_share_mixed_script_differs_from_uniform_baseline`).
    - Behavior assertion: `cargo clippy --workspace --all-targets -- -D warnings` exits 0; no warning printed (any clippy warning is treated as a failure).
    - Behavior assertion: `cargo audit` exits 0; no new vulnerability surfaces against the pre-Phase-21 audit.toml.
    - CLI assertion: `git diff --name-only HEAD -- shared/ client/` produces empty output (V1.4-CRIT-01 invariant + scope discipline: Phase 21 plan 01 modifies coordinator/ only).
    - CLI assertion: `/tmp/21-01-task3-summary.txt` exists and contains the phrase `ALL INVARIANTS GREEN`.
  </acceptance_criteria>
  <done>
    All 6 cross-phase invariants from CONTEXT.md (v1.3 P2WPKH, v1.4 multi-script, v1.5 Phase 20 FEE-03, clippy clean, cargo audit clean with pre-Phase-21 audit.toml, V1.4-CRIT-01 dispatcher-only) are GREEN at the 21-01 plan boundary. The summary file documents the run.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Coordinator process memory ↔ kernel/allocator | RSA private key material (`d`, `primes`, `precomputed` `BigUint` allocations inside `rsa::RsaPrivateKey`) lives in the coordinator process's heap during a round. The boundary is process termination OR explicit `Drop` invocation. Phase 21 makes the latter expressible as a Rust lifetime via `Option<RsaBlindSigner>` on `RoundStateInner`. |
| FSM transition surface | The single chokepoint `RoundState::transition_to(Phase::Idle)` at `coordinator/src/round/state.rs:194-200` is the SOLE site setting `self.inner = None` (verified by 21-RESEARCH OQ2 grep — no bypass paths). This boundary's integrity is load-bearing for the AUDIT-03 narrative. |
| Drop body ↔ logging subsystem | The `Drop for RoundSecretKey` body emits one `tracing::debug!` event. The boundary here is the tracing layer's ability to format the event WITHOUT touching key material (a malformed `Debug` interpolation would leak the secret to ops logs). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01-S | Spoofing | Refactored production call sites (input_reg.rs:71, handlers.rs:383) | mitigate | `.as_ref().expect("rsa_signer must be Some during <phase>")` panics with a precise message if the FSM invariant is ever violated. Panic in production is preferable to silent wrong-key behavior. Per D-130a / 21-RESEARCH OQ2, no code path can legitimately reach these sites with `None` (all production callers verify `phase == InputReg` or `OutputReg` before reaching here, and the `inner = None` chokepoint is the only nullifier). The `expect()` message is a load-bearing assertion that the FSM invariant holds at runtime. |
| T-21-01-T | Tampering | RsaBlindSigner internals (newtype access) | mitigate | The `RoundSecretKey` newtype is `pub` but its inner field is private (single-tuple `pub struct RoundSecretKey(BjSecretKey);` — the `0` field is private). Access is via `pub(crate) fn new` (constructor) and `pub(crate) fn as_inner(&self) -> &BjSecretKey` (read-only borrow). No `as_inner_mut` is exposed. The `Drop` impl cannot be skipped via destructuring (you cannot move-out of a `Drop` type). This makes the lifetime bound structural at the language level. |
| T-21-01-R | Repudiation | Drop body's tracing event | mitigate | The `tracing::debug!(target: "blindjoin::audit", ...)` event makes the scrub firing AUDITABLE in ops logs. Operators can filter for `target = blindjoin::audit` and confirm the per-round Drop chain ran. No PII; no key material; only a static message. |
| T-21-01-I | Information Disclosure | Drop body's `Debug` interpolation accident | mitigate | Per 21-RESEARCH Pitfall 2: `BjSecretKey` derives `Debug`; a naive `tracing::debug!("dropped: {:?}", self)` WOULD leak DER bytes via the wrapped `RsaPrivateKey`'s auto-derived Debug. The locked Drop body is `tracing::debug!(target: "blindjoin::audit", "RoundSecretKey dropped — rsa::RsaPrivateKey ZeroizeOnDrop fires transitively");` — STATIC string only, NO `{:?}` formatter, NO field interpolation. Acceptance criteria enforces this via grep in Task 1's verify block. |
| T-21-01-I-2 | Information Disclosure | Memory residue after round teardown | mitigate | The transitive `<rsa::RsaPrivateKey as Drop>::drop` at `rsa-0.9.10/src/key.rs:76-82` zeroizes `d`, `primes`, `precomputed`. Unconditional (no feature flag). Verified at the installed-source level by 21-RESEARCH OQ1. Phase 21 expresses the bounded lifetime as `Option<RsaBlindSigner>`; cryptographic correctness is delegated to upstream `rsa` (which the charter §6 lists as out-of-scope per D-135 — "consensus-critical primitive with separate upstream audit posture"). |
| T-21-01-D | Denial of Service | Drop body panic | accept | The locked Drop body is `tracing::debug!` only — `tracing::debug!` is infallible (no allocation in the hot path, no I/O). A panic in the Drop body would cause double-panic process abort during round teardown, which is itself recoverable (a new round starts). The acceptance criterion forbids panicking expressions in Drop. |
| T-21-01-E | Elevation of Privilege | n/a | n/a | This plan does not modify auth or access-control surfaces. |
| T-21-01-FSM-BYPASS | Tampering (FSM integrity) | `transition_to(Phase::Idle)` chokepoint | mitigate | Per 21-RESEARCH OQ2: grep of the entire `coordinator/src/` tree confirms `state.rs:195` is the SOLE `inner = None` assignment. No `HashMap<RoundId, RoundState>` map exists (single-coordinator-single-round model). No drop-on-removal pattern. D-130b explicitly rejects adding a new helper that could bypass the chokepoint. Charter §5 prose (written in 21-02) cites this single site by line number. |
| T-21-01-CRIT-01 | Tampering | V1.4-CRIT-01 dispatcher-only public surface (shared::bip322) | mitigate | Phase 21 does NOT modify `shared/`. Task 3 acceptance criterion includes `git diff --name-only HEAD -- shared/ client/` returns empty — the V1.4-CRIT-01 invariant is preserved by scope discipline. |
| T-21-01-SC | Tampering | npm/pip/cargo installs | n/a | Phase 21 installs no new packages (verified by 21-RESEARCH §"Package Legitimacy Audit": empty table). No `[ASSUMED]`/`[SUS]`/`[SLOP]` legitimacy gate needed. |
</threat_model>

<verification>
After all 3 tasks complete:

1. **All 6 cross-phase invariants green** (Task 3 verify): full_round 8/8, mixed_script_e2e 1/1, FEE-03 2/2, coordinator lib tests all-pass (Linux: scrub test runs and passes; macOS: scrub test reports ignored), clippy clean, cargo audit clean against pre-Phase-21 audit.toml.
2. **AUDIT-03 structural claim green** (Task 2 verify): `round_secret_key_dropped_on_round_end` passes — the LOAD-BEARING test per REQUIREMENTS.
3. **AUDIT-03 best-effort claim green** (Task 1 verify): `round_secret_key_buffer_overwritten_on_drop` passes on Linux, reports ignored on macOS with the documented reason string.
4. **Source-level invariants** (Task 1+2 acceptance grep):
   - `pub struct RoundSecretKey(BjSecretKey);` exists in `coordinator/src/blind/rsa.rs`.
   - `impl Drop for RoundSecretKey` has empty crypto body + PII-safe `tracing::debug!` event.
   - The string `best-effort only` is ABSENT from `coordinator/src/blind/rsa.rs` (the prose inversion per D-132).
   - `RoundStateInner.rsa_signer` is typed `Option<RsaBlindSigner>`.
   - Charter anchor `docs/AUDIT-CHARTER.md#rsa-secret-key-zeroization-window` is referenced in the rewritten D-07 comment.
5. **Scope discipline:** `shared/` and `client/` are untouched (V1.4-CRIT-01 invariant).

Manual verification (no human-check required — all gates are automated):
- The structural test (`round_secret_key_dropped_on_round_end`) is the unconditional CI gate (D-131 load-bearing).
- The scrub test (`round_secret_key_buffer_overwritten_on_drop`) is sanity ceremony; an ignored-on-macOS result is expected per CD-50.
</verification>

<success_criteria>
- AUDIT-03 SC#3 (REQUIREMENTS): `RoundSecretKey` newtype + `Drop` exist; `RoundStateInner.rsa_signer` is `Option<RsaBlindSigner>`; D-07 comment rewritten as bounded statement. ✓ (Task 1 + Task 2)
- AUDIT-03 SC#4 (REQUIREMENTS): `round_secret_key_dropped_on_round_end` (CONTEXT.md D-131 name; ROADMAP alias `round_secret_key_zeroes_on_drop`) green. ✓ (Task 2)
- AUDIT-03 contributes to SC#5: cross-phase invariants green at the 21-01 boundary using the pre-Phase-21 audit.toml. ✓ (Task 3)
- CONTEXT.md `<phase_split_constraint>`: this plan ships AUDIT-03 atomically before 21-02 touches charter prose / audit.toml. ✓ (the plan's dependency graph: 21-02 depends_on: [21-01]).
- All locked decisions D-128, D-128a, D-129, D-130, D-130a, D-130b, D-131, D-132, D-133 first half are honored. ✓ (per Task action references).
- All Claude's-Discretion items CD-46 (D-07 comment prose), CD-47 (DER-roundtrip vs replace-with-dummy → BOTH rejected per OQ1; empty body chosen), CD-50 (RAM-scan test mechanism — 21-RESEARCH §9 sketch) are resolved with rationale documented in the relevant task. ✓
</success_criteria>

<output>
Create `.planning/phases/21-audit-charter-zeroization-tightening/21-01-SUMMARY.md` when done. Summary includes:
- Files changed (per-file line-add/line-delete counts).
- Test counts before/after (pre-Phase-21 coordinator --lib count → post-21-01 count; new tests by name).
- Invariant matrix (Task 3 summary).
- Decision references: D-128 (newtype placement), D-129 (empty-body Drop per OQ1), D-131 (two-test split), D-132 (D-07 comment rewrite), CD-47 (DER-roundtrip rejected per OQ1), CD-50 (RAM-scan test mechanism + #[cfg_attr(not(linux), ignore)]).
- Note: this plan boundary uses the PRE-Phase-21 audit.toml; the audit.toml refresh ships in 21-02.
- Time spent (start → end).
</output>
