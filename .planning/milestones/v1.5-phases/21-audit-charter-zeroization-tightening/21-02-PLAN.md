---
phase: 21-audit-charter-zeroization-tightening
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - docs/AUDIT-CHARTER.md
  - .cargo/audit.toml
  - README.md
autonomous: true
requirements: [AUDIT-01, AUDIT-02]
must_haves:
  decisions_covered:
    - "D-133: Plan split — 21-01 = AUDIT-03; 21-02 = AUDIT-01 + AUDIT-02 (this plan); D-133a: 21-02 ships in a single atomic commit (charter + audit.toml + README) to prevent anchor-drift window"
    - "D-134: Hybrid voice — §3/§6/§8 TABLE; §2/§4/§5/§7 NARRATIVE; §1 TABLE (in-scope modules)"
    - "D-135: §6 out-of-scope extends beyond REQUIREMENTS Tor+PKARR to all 3rd-party crypto crates (arti-client, pkarr, blind-rsa-signatures internals, bip322 internals, rust-bitcoin, secp256k1, bdk_wallet client-side)"
    - "D-136: §7 residual risks split into 3 sub-buckets (cargo-audit-flagged advisories, protocol-level residuals, operational residuals)"
    - "D-137: §8 glossary scope = active v1.4/v1.5 identifiers only (~25-30 entries); retired pre-v1.4 IDs point at archives"
    - "D-138: Anchor style = file:symbol refs (stable across reformats); orientation line numbers are parenthetical only"
    - "D-139: .cargo/audit.toml each ignore-comment closes with bare-path 'See docs/AUDIT-CHARTER.md#<anchor> for the full rationale.' (no markdown link syntax)"
    - "D-140: .cargo/audit.toml 'Reviewed:' header bumped to the actual 21-02 commit date"
    - "D-141: New-advisory detection happens via cargo audit --json BEFORE 21-02 edits; planner decides upgrade-vs-ignore per advisory; per 21-RESEARCH OQ3 the diff is empty so the 3 existing IDs are locked verbatim"
    - "D-142: Flat TOML layout preserved — [advisories] / ignore = [...] with prose comments; no [advisories.ignore.\"RUSTSEC-...\"] sub-tables"
    - "D-143: README.md §Security Model one-paragraph audit-charter callout placed AFTER the Supply-chain hygiene paragraph and BEFORE the Test infrastructure (v1.3) paragraph"
  truths:
    - "docs/AUDIT-CHARTER.md exists, committed in main, with 8 H2 sections in the REQUIREMENTS AUDIT-01 mandated order: §1 in-scope modules (table), §2 threat models per module (narrative), §3 cross-shape rejection properties (table, 9 rows), §4 v=2 OwnershipProof PSBT handling (narrative), §5 RSA SecretKey zeroization window (narrative — bounded form per AUDIT-03), §6 out-of-scope (table), §7 residual risks accepted (narrative, 3 sub-buckets), §8 glossary (table)"
    - "README.md §Security Model contains a one-paragraph audit-charter callout linking to docs/AUDIT-CHARTER.md, placed AFTER the Supply-chain hygiene paragraph and BEFORE the v1.3 Test infrastructure paragraph"
    - ".cargo/audit.toml has the closing line 'See docs/AUDIT-CHARTER.md#<anchor> for the full rationale.' appended to each of the 3 existing comment blocks (RUSTSEC-2023-0071 → #rsa-secret-key-zeroization-window; RUSTSEC-2025-0141 + RUSTSEC-2024-0436 → #residual-risks-cargo-audit-advisories or the resolved anchor slug from CD-49)"
    - ".cargo/audit.toml RUSTSEC-2023-0071 rationale paragraph names the AUDIT-03 bounded-window mitigation explicitly; the previous 'destroys the key via zeroize after the round broadcasts' phrasing is REPLACED with language citing the transitive rsa::RsaPrivateKey Drop chain bounded by Option<RsaBlindSigner> on RoundStateInner — the 'best-effort' framing is GONE per D-139 + 21-RESEARCH OQ1"
    - ".cargo/audit.toml 'Reviewed:' header date bumped from '2026-05-26' to the commit date of 21-02 (D-140)"
    - "The 3 ignore IDs in audit.toml are unchanged: RUSTSEC-2023-0071, RUSTSEC-2025-0141, RUSTSEC-2024-0436 (no new ignores; no removed ignores — per 21-RESEARCH OQ3: cargo audit returns 0 vulns + 0 warnings with the existing 3 ignores)"
    - "cargo audit returns 0 vulnerabilities and 0 warnings against the refreshed .cargo/audit.toml (CI gate stays green)"
    - "All charter file:symbol anchors resolve to real symbols in the codebase (verified by grep — every cited symbol exists)"
    - "All charter #anchor refs in audit.toml resolve to real H2 headings in docs/AUDIT-CHARTER.md (markdown auto-generated slug form)"
    - "Single atomic commit lands docs/AUDIT-CHARTER.md + .cargo/audit.toml + README.md edits together (D-133a — prevents anchor-drift window between artifacts)"
    - "cargo test --test integration full_round 8/8 green (v1.3 P2WPKH invariant — Phase 21 plan 02 makes no code changes; should be invariant)"
    - "cargo test --test integration mixed_script_e2e 1/1 green (v1.4 multi-script invariant)"
    - "cargo clippy --workspace --all-targets -- -D warnings clean (no code changes)"
    - "shared/, client/, coordinator/src/ untouched (21-02 modifies only docs/ + .cargo/ + README.md)"
  artifacts:
    - path: "docs/AUDIT-CHARTER.md"
      provides: "External audit charter — 8 H2 sections per AUDIT-01"
      min_lines: 400
      contains: "## RSA SecretKey Zeroization Window"
    - path: ".cargo/audit.toml"
      provides: "Refreshed cargo-audit configuration — 3 charter-anchor refs + RUSTSEC-2023-0071 paragraph rewrite + Reviewed: bump"
      contains: "docs/AUDIT-CHARTER.md#"
    - path: "README.md"
      provides: "Audit-charter callout paragraph in §Security Model"
      contains: "External audit charter (v1.5)"
  key_links:
    - from: ".cargo/audit.toml"
      to: "docs/AUDIT-CHARTER.md"
      via: "See docs/AUDIT-CHARTER.md#<anchor> for the full rationale. — closing line in each of the 3 ignore comment blocks"
      pattern: "See docs/AUDIT-CHARTER\\.md#"
    - from: "README.md"
      to: "docs/AUDIT-CHARTER.md"
      via: "[docs/AUDIT-CHARTER.md](docs/AUDIT-CHARTER.md) markdown link in §Security Model"
      pattern: "\\[docs/AUDIT-CHARTER\\.md\\]"
    - from: "docs/AUDIT-CHARTER.md §5"
      to: "coordinator/src/blind/rsa.rs::RoundSecretKey"
      via: "file:symbol anchor in §1 in-scope table + §5 prose"
      pattern: "coordinator/src/blind/rsa\\.rs::RoundSecretKey"
---

<objective>
Phase 21 Plan 02 closes REQUIREMENTS AUDIT-01 + AUDIT-02 in a SINGLE ATOMIC COMMIT (D-133a). Lands the externally-visible v1.5 audit-readiness artifacts so an external auditor can read the codebase cold and start work without project-team clarification:

1. **AUDIT-01:** Create `docs/AUDIT-CHARTER.md` with the 8 H2 sections mandated by REQUIREMENTS verbatim (in section ORDER):
   - §1 in-scope modules — TABLE (file:symbol | description | orientation line at v1.5 ship)
   - §2 threat models per module — NARRATIVE (3-6 paragraphs per module, citing V1.4-CRIT-01, V1.4-CRIT-02, V1.4-MIN-02, RSA Marvin Attack)
   - §3 cross-shape rejection properties — TABLE (9 rows enumerated from `shared/tests/bip322_cross_shape.rs`)
   - §4 v=2 OwnershipProof PSBT handling — NARRATIVE (full-BIP-174 shape, RESEARCH Pitfall 1, `decode_psbt_input_witness` + `build_v2_psbt_input_b64` boundary)
   - §5 RSA SecretKey zeroization window — NARRATIVE (RoundSecretKey + bounded lifetime per AUDIT-03; cites the transitive `rsa::RsaPrivateKey` `Drop` chain per 21-RESEARCH OQ1)
   - §6 out-of-scope — TABLE (extended beyond REQUIREMENTS Tor+PKARR to all 3rd-party crypto crates per D-135)
   - §7 residual risks accepted — NARRATIVE (3 sub-buckets per D-136: cargo-audit advisories, protocol-level, operational)
   - §8 glossary — TABLE (~25-30 entries: active v1.4/v1.5 identifiers per D-137)

   Hybrid voice (tables for §1/§3/§6/§8; narrative for §2/§4/§5/§7) per D-134.

2. **AUDIT-01 README integration:** Insert one-paragraph callout in `README.md` §Security Model between the existing "Supply-chain hygiene" paragraph (line 300) and "Test infrastructure (v1.3 Phase 9)" paragraph (line 302). Style follows the established `**Category (vN.x):** prose` convention used by the 5 existing hardening rollups (D-143 + CD-52; 21-RESEARCH §11 confirmed exact insertion point).

3. **AUDIT-02 audit.toml refresh:** Update `.cargo/audit.toml`:
   - Append `# See docs/AUDIT-CHARTER.md#<anchor> for the full rationale.` to each of the 3 existing ignore comment blocks (D-139 + CD-49 — anchor slugs per the table below).
   - Rewrite the RUSTSEC-2023-0071 rationale paragraph to NAME the AUDIT-03 bounded-window mitigation explicitly (no more "best-effort"; cite the transitive `rsa::RsaPrivateKey` `Drop` chain at `rsa-0.9.10/src/key.rs:76-82` bounded by `Option<RsaBlindSigner>` on `RoundStateInner` per 21-RESEARCH OQ1).
   - Bump `# Reviewed: 2026-05-26.` header line to the actual 21-02 commit date (D-140).
   - Keep the 3 ignore IDs verbatim (per 21-RESEARCH OQ3: cargo audit returns 0 vulns + 0 warnings — no new advisories surfaced; no upgrades possible without an upstream `rsa` constant-time rewrite).
   - Flat TOML layout preserved (D-142 lock — `[advisories]\nignore = ["RUSTSEC-..."]`).

Purpose: REQUIREMENTS AUDIT-01 + AUDIT-02 + the closing-loop story across 3 artifacts. After this plan, an auditor reading any one of the 3 artifacts is **one click from the other two**. This coherence — code → charter → audit.toml → README → back to code — is the load-bearing audit deliverable.

Output:
- `docs/AUDIT-CHARTER.md` — NEW file, ~400-1000 LOC of prose + tables across 8 H2 sections.
- `.cargo/audit.toml` — refreshed; 3 anchor lines appended; 1 paragraph rewritten; 1 date bumped; 3 ignore IDs unchanged.
- `README.md` — 1 new paragraph inserted in §Security Model.

Cross-phase invariant boundary: this plan ships in ONE atomic commit per D-133a. The CI gate post-commit must show `cargo audit` 0-vulns with the refreshed audit.toml; the v1.3 + v1.4 integration tests stay green by construction (no code edits).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-audit-charter-zeroization-tightening/21-CONTEXT.md
@.planning/phases/21-audit-charter-zeroization-tightening/21-RESEARCH.md
@.planning/phases/21-audit-charter-zeroization-tightening/21-PATTERNS.md
@.planning/phases/21-audit-charter-zeroization-tightening/21-01-SUMMARY.md

# Files this plan modifies — executor reads these to see current state, not assumptions.
@.cargo/audit.toml
@README.md

# Files this plan READS (charter file:symbol anchor stability — DO NOT modify):
@coordinator/src/blind/rsa.rs
@coordinator/src/round/state.rs
@coordinator/src/bitcoin/utxo.rs
@shared/src/bip322/mod.rs
@shared/tests/bip322_cross_shape.rs
@client/src/round/input.rs
@docs/PROTOCOL.md

# v1.4 ADR + earlier CONTEXT (charter §2/§8 source material):
@.planning/decisions/v1.4-adr.md

<interfaces>
<!-- Key facts the executor needs. These are stable post-21-01 ship. -->

**21-01 outputs that the charter cites:**
- `coordinator/src/blind/rsa.rs::RoundSecretKey` — exists post-21-01 (newtype wrapping `BjSecretKey`).
- `coordinator/src/blind/rsa.rs::RoundSecretKey::drop` — exists post-21-01 (empty crypto body + `tracing::debug!` event).
- `coordinator/src/round/state.rs::RoundStateInner.rsa_signer: Option<RsaBlindSigner>` — exists post-21-01.
- `coordinator/src/round/state.rs::tests::round_secret_key_dropped_on_round_end` — exists post-21-01 (load-bearing structural test).
- `coordinator/src/blind/rsa.rs::tests::round_secret_key_buffer_overwritten_on_drop` — exists post-21-01 (best-effort scrub test).
- The D-07 comment at the top of rsa.rs cites `docs/AUDIT-CHARTER.md#rsa-secret-key-zeroization-window` — the charter MUST emit a §5 H2 heading whose markdown auto-slug matches this anchor.

**21-RESEARCH-verified file:symbol anchors (stable):**
- `coordinator/src/bitcoin/utxo.rs::validate_utxo` (line 67) — CRIT-01 cross-check
- `coordinator/src/bitcoin/utxo.rs::dispatch_ownership_proof` (line 158) — v=1/v=2 dispatcher
- `coordinator/src/bitcoin/utxo.rs::decode_psbt_input_witness` (line 218) — §4 verification boundary
- `client/src/round/input.rs::build_v2_psbt_input_b64` (line 35) — §4 construction boundary
- `shared/src/bip322/mod.rs::sign_simple` (line 283), `verify_simple` (line 257), `detect_script_type` (line 238) — dispatcher-only surface
- `coordinator/src/config.rs::BipConfig::validate` (line 249) — output_script_type boot validation
- `coordinator/src/round/state.rs::transition_to` (line 186) — FSM trigger
- `shared/tests/bip322_cross_shape.rs` — 9 cross-shape rejection tests at lines 90, 105, 118, 133, 148, 169, 190, 204, 218 (per 21-RESEARCH §"9 cross-shape rejection properties (Standard Research #13)")

**The 9 cross-shape rejection tests (charter §3 table source) — 21-RESEARCH §13:**

| # | Test fn name | What it rejects |
|---|-------------|------------------|
| 1 | reject_p2wpkh_spk_with_p2tr_witness | P2WPKH SPK + 1-element witness → InvalidWitnessLength { expected: 2, got: 1 } |
| 2 | reject_p2wpkh_spk_with_p2sh_p2wpkh_witness | P2WPKH SPK + 2-element witness → CrateVerifyFailed (ECDSA fails) |
| 3 | reject_p2tr_spk_with_p2wpkh_witness | P2TR SPK + 2-element witness → InvalidWitnessLength { expected: 1, got: 2 } |
| 4 | reject_p2tr_spk_with_p2sh_p2wpkh_witness | P2TR SPK + 2-element witness → InvalidWitnessLength { expected: 1, got: 2 } |
| 5 | reject_p2sh_p2wpkh_spk_with_p2wpkh_witness | P2SH-P2WPKH SPK + 2-element witness → CrateVerifyFailed (HASH160 mismatch) |
| 6 | reject_p2sh_p2wpkh_spk_with_p2tr_witness | P2SH-P2WPKH SPK + 1-element witness → InvalidWitnessLength { expected: 2, got: 1 } |
| 7 | reject_p2wpkh_spk_with_empty_witness | P2WPKH SPK + empty witness → InvalidWitnessLength { expected: 2, got: 0 } |
| 8 | reject_p2tr_spk_with_empty_witness | P2TR SPK + empty witness → InvalidWitnessLength { expected: 1, got: 0 } |
| 9 | reject_p2sh_p2wpkh_spk_with_empty_witness | P2SH-P2WPKH SPK + empty witness → InvalidWitnessLength { expected: 2, got: 0 } |

**cargo audit state (21-RESEARCH OQ3-verified, 2026-05-31):**
- Current `Cargo.lock` + current 3 ignores → 0 vulnerabilities, 0 warnings.
- With ignores temporarily removed → same 3 IDs surface (RUSTSEC-2023-0071 as vuln; -2025-0141 + -2024-0436 as unmaintained warnings).
- NO new advisories — D-141 detection returns empty. Lock the 3 existing ignores verbatim.
- Advisory DB at research time: last commit `eaf48e7`, last updated 2026-05-29. Re-run `cargo audit` at execution time to confirm freshness (CD-48 allows planner discretion if a new advisory appears between research and execution).

**README.md §Security Model insertion point (21-RESEARCH §11):**
- Header `## Security Model` at line 281.
- "Supply-chain hygiene" paragraph at line 300.
- "Test infrastructure (v1.3 Phase 9)" paragraph at line 302.
- Insert the new "**External audit charter (v1.5):**" paragraph BETWEEN line 300 and line 302.

**Anchor slug mapping (CD-49 — executor may refine):**
- `RUSTSEC-2023-0071` → `#rsa-secret-key-zeroization-window` (charter §5)
- `RUSTSEC-2025-0141` → `#residual-risks-cargo-audit-advisories` (charter §7 sub-bucket a)
- `RUSTSEC-2024-0436` → `#residual-risks-cargo-audit-advisories` (charter §7 sub-bucket a)

These anchors are GITHUB-RENDERED markdown auto-slugs (lowercase, hyphenated, special-char-stripped). Executor MUST ensure the corresponding charter H2 / H3 headings produce these exact slugs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/AUDIT-CHARTER.md — 8 H2 sections per AUDIT-01 + D-134 hybrid voice (closes AUDIT-01 core + SC#1)</name>
  <files>docs/AUDIT-CHARTER.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md §AUDIT-01 (line 30 — verbatim 8-section enumeration)
    - .planning/phases/21-audit-charter-zeroization-tightening/21-CONTEXT.md §G "Charter authoring (AUDIT-01)" — D-134 (hybrid voice per section), D-135 (§6 extends to all 3rd-party crypto crates), D-136 (§7 3 sub-buckets), D-137 (§8 ~25-30 active v1.4/v1.5 identifiers), D-138 (file:symbol anchors not line refs), CD-51 (§4 paragraph count)
    - .planning/phases/21-audit-charter-zeroization-tightening/21-RESEARCH.md §"Charter file:symbol stability (Standard Research #10 / D-138)" (12 verified symbols + 4 NEW post-21-01) AND §"9 cross-shape rejection properties (Standard Research #13)" (the §3 table source) AND §"docs/ directory current contents (Standard Research #12)" (PROTOCOL.md is the only style analog)
    - .planning/phases/21-audit-charter-zeroization-tightening/21-PATTERNS.md §"docs/AUDIT-CHARTER.md — NEW file (21-02)" (PROTOCOL.md frontmatter table + heading hierarchy + cross-reference style)
    - .planning/phases/21-audit-charter-zeroization-tightening/21-01-SUMMARY.md (Plan 21-01 close — confirms RoundSecretKey, Option<RsaBlindSigner>, and both new test names that charter §5 cites)
    - docs/PROTOCOL.md (style analog — top-of-file table, H2/H3 hierarchy, cross-reference link form)
    - coordinator/src/blind/rsa.rs (post-21-01 state — the D-07 comment at lines 18-22 references this charter's #rsa-secret-key-zeroization-window anchor, so the §5 H2 heading MUST auto-slug to that anchor)
    - .planning/decisions/v1.4-adr.md (v1.4 ADR Decisions #1-4 — charter §8 glossary entries + §2 threat-model citations)
  </read_first>
  <action>
    Create `docs/AUDIT-CHARTER.md` with the structure below. Use the H2 SLUG forms listed (markdown auto-slugs are lowercase + hyphenated + special-char-stripped) to ensure the audit.toml anchor refs (Task 2) resolve cleanly. Section ORDER follows REQUIREMENTS AUDIT-01 enumeration verbatim (D-134 + D-138).

    **Top-of-file frontmatter (analog: docs/PROTOCOL.md:1-12):**
    - H1 title: `# blindjoin External Audit Charter`
    - Frontmatter `| Field | Value |` table with rows: Title (full descriptive), Authors (`John Turner (johnturner@gmail.com)`), Status (`v1.5 audit-readiness`), License (`MIT`), Created (`2026-05`), Implementation (`https://github.com/johnzilla/blindjoin`), Companion (`docs/PROTOCOL.md`).
    - One blockquote status callout: `> **Status — v1.5 audit-readiness.** This charter scopes external review of the blindjoin coordinator + client codebase at the v1.5 ship. It enumerates in-scope modules with file:symbol references, threat models per module, the 9 cross-shape rejection properties locked by v1.4, the v=2 OwnershipProof PSBT handling boundary, the RSA SecretKey zeroization window (post AUDIT-03 bounded form), out-of-scope dependencies, residual risks accepted with rationale, and a glossary mapping project terms to plain audit language.`
    - `---` horizontal rule.

    **§1 In-Scope Modules (TABLE — D-134 + D-138 + 21-RESEARCH §10):**

    H2 heading: `## In-Scope Modules` (slug `#in-scope-modules`).

    1-paragraph intro: state that the file:symbol form is the durable anchor (per D-138), the parenthetical line is orientation at the v1.5 ship tag.

    Table with 3 columns `| File:Symbol | Description | Orientation (line at v1.5 ship) |`. Rows (USE THESE EXACT SYMBOLS — 21-RESEARCH §10 verified all exist):
    - `coordinator/src/blind/rsa.rs::RsaBlindSigner` — RFC 9474 RSA blind signer (per-round ephemeral); 6 public methods cached at round bootstrap. — ~line 23
    - `coordinator/src/blind/rsa.rs::RoundSecretKey` — newtype wrapping BjSecretKey; AUDIT-03 lifetime-bound mitigation. — ~line 28 (NEW post-21-01)
    - `coordinator/src/blind/rsa.rs::RoundSecretKey::drop` — empty-body Drop emitting PII-safe tracing event; transitive rsa::RsaPrivateKey Drop chain does the cryptographically meaningful work. — ~line 50 (NEW post-21-01)
    - `coordinator/src/round/state.rs::RoundStateInner` — sensitive round material; manual Drop zeroes Vec/[u8;N] + clears HashMaps. — ~line 92
    - `coordinator/src/round/state.rs::transition_to` — single FSM chokepoint; sets `self.inner = None` on `next == Phase::Idle`, triggering the Drop chain. — ~line 186
    - `coordinator/src/bitcoin/utxo.rs::validate_utxo` — V1.4-CRIT-01 cross-check; derives ScriptType from on-chain script_pubkey, never from client-declared field. — ~line 67
    - `coordinator/src/bitcoin/utxo.rs::dispatch_ownership_proof` — v=1/v=2 OwnershipProof dispatcher; routes v=2 envelope to decode_psbt_input_witness. — ~line 158
    - `coordinator/src/bitcoin/utxo.rs::decode_psbt_input_witness` — v=2 OwnershipProof verification boundary (BIP-174 PSBT decode). — ~line 218
    - `client/src/round/input.rs::build_v2_psbt_input_b64` — v=2 OwnershipProof construction boundary (full-BIP-174 PSBT envelope). — ~line 35
    - `shared/src/bip322/mod.rs::detect_script_type` — script-type detection from script_pubkey bytes. — ~line 238
    - `shared/src/bip322/mod.rs::verify_simple` — BIP-322 Simple verify dispatcher (only public verify entry). — ~line 257
    - `shared/src/bip322/mod.rs::sign_simple` — BIP-322 Simple sign dispatcher (only public sign entry). — ~line 283
    - `shared/src/bip322/p2wpkh::sign` / `p2tr::sign` / `p2sh_p2wpkh::sign` — per-script production sign bodies (pub(crate)), shipped in Phase 19 BIP322-05/06.
    - `coordinator/src/config.rs::BipConfig::validate` — output_script_type boot validation (D-37). — ~line 249
    - `shared/tests/bip322_cross_shape.rs` — 9 cross-shape rejection tests (see §3 below).

    Closing paragraph: cite RoundSecretKey + bounded lifetime by name, point to §5 for the threat-model treatment.

    **§2 Threat Models per Module (NARRATIVE — D-134 + AUDIT-01 verbatim threats):**

    H2 heading: `## Threat Models per Module` (slug `#threat-models-per-module`).

    Sub-sections (use H3 for each named threat):
    - `### V1.4-CRIT-01 — script_type spoofing via client-declared field` (3-5 paragraphs): cite `validate_utxo` deriving ScriptType from on-chain script_pubkey (NOT from the client-declared ParticipantInput.script_type field); cite the 9 cross-shape rejection tests at §3 as the test-level mitigation; cite Phase 19 D-111 (spk↔key cross-check at the top of each sign body) as defense-in-depth; cite `bip322 = "=0.0.10"` exact pin + `bip322-pin-check` CI gate; cite the dispatcher-only public surface (V1.4-CRIT-01 invariant strengthened by Phase 19-02 deletion of `sign_simple_test_only`).
    - `### V1.4-CRIT-02 — silent sighash regression` (3-4 paragraphs): cite the v1.4 ADR Decision #3/#4 byte-equality cross-check via parity tests at `client/tests/wallet_sign_roundtrip.rs`; cite v1.4 sign-body production-shape locked at Phase 19-01; cite the 9 cross-shape tests as belt-and-suspenders (any future sighash mistake would surface via shape mismatch first).
    - `### V1.4-MIN-02 — uniform-script fingerprint via output_script_type` (3-4 paragraphs): describe the privacy tradeoff (mixed-INPUT round with uniform-OUTPUT script gives partial anonymity set vs single-script rounds); cite Phase 18 INTEG-02 liquidity-bot rotation as operational mitigation (operator-set bot ratios CANNOT eliminate the fingerprint, only dilute it); cite v1.5 Phase 20 mixed-input fee accuracy as orthogonal — fee math now correctly handles mixed-input weight regardless of output script.
    - `### RSA Marvin Attack (RUSTSEC-2023-0071) — residual exposure` (4-6 paragraphs): cite the attack model preconditions (long-lived key + unlimited chosen-ciphertext measurements); cite blindjoin's structural countermeasures: (i) per-round ephemeral RSA-2048 keys (D-02); (ii) bounded blind-sign operations per round (default `max_participants = 20`); (iii) **AUDIT-03 RoundSecretKey + Option<RsaBlindSigner> bounded lifetime** — the secret is structurally dropped at `transition_to(Phase::Idle)` (state.rs:194-200), triggering the transitive `<rsa::RsaPrivateKey as Drop>::drop` chain at `rsa-0.9.10/src/key.rs:76-82` which zeroizes `d`, `primes`, `precomputed` unconditionally (per 21-RESEARCH OQ1). Conclude: the Marvin Attack's preconditions (long-lived key + unlimited measurements) do not obtain in this codebase. Out-of-scope: any constant-time RSA decryption is delegated to the upstream `rsa` crate's potential future rewrite (cite as v1.6+ residual in §7).

    **§3 Cross-shape Rejection Properties (TABLE — D-134 + 21-RESEARCH §13):**

    H2 heading: `## Cross-shape Rejection Properties` (slug `#cross-shape-rejection-properties`).

    Intro paragraph: state these 9 tests are at `shared/tests/bip322_cross_shape.rs`; locked at Phase 15 via the 10-variant `Bip322Error` taxonomy (D-31); each test asserts the SPECIFIC `Bip322Error` variant via `matches!()` so silent acceptance of the wrong rejection class is statically impossible (per RESEARCH A3 from 15-RESEARCH.md).

    Table with 4 columns `| # | Test Function | What It Rejects | Bip322Error Variant |`. 9 rows verbatim from the `<interfaces>` block above (test names + behavior).

    **§4 v=2 OwnershipProof PSBT Handling (NARRATIVE — D-134 + CD-51 + 21-RESEARCH OQ1 Pitfall 1):**

    H2 heading: `## v=2 OwnershipProof PSBT Handling` (slug `#v-2-ownershipproof-psbt-handling` — verify markdown auto-slug; the `=` is stripped, so this might be `#v2-ownershipproof-psbt-handling` — executor uses GitHub markdown's exact slug rule).

    4-8 paragraphs covering:
    - The `OwnershipProof.psbt_input_b64` field is encoded as a **full BIP-174 PSBT** (single-input single-output template), NOT a bare `psbt::Input` (RESEARCH Pitfall 1 from Phase 15).
    - Construction boundary: `client/src/round/input.rs::build_v2_psbt_input_b64` (~line 35).
    - Verification boundary: `coordinator/src/bitcoin/utxo.rs::decode_psbt_input_witness` (~line 218) extracts the witness from the PSBT envelope, hands off to `shared::bip322::verify_simple` via the dispatcher.
    - V1.4-CRIT-01 cross-check fires BEFORE `verify_simple`: the SPK type is derived from chain, not client declaration.
    - The v=2 wire format is LOCKED at v1.4 (ADR Decision #3); the Phase 19 production sign bodies preserve byte-equality with v1.4 wire output (verified via parity tests at `client/tests/wallet_sign_roundtrip.rs`).
    - Per-script weight accuracy (Phase 20 FEE-01/02/03) operates on the same chain-derived ScriptType — the v=2 OwnershipProof handling and the fee path share the single derivation point at `validate_utxo`.

    **§5 RSA SecretKey Zeroization Window (NARRATIVE — D-134 + 21-RESEARCH OQ1 + cites Plan 21-01 artifacts):**

    H2 heading: `## RSA SecretKey Zeroization Window` (slug MUST be `#rsa-secret-key-zeroization-window` — this anchor is referenced from `coordinator/src/blind/rsa.rs` D-07 comment AND from `.cargo/audit.toml` RUSTSEC-2023-0071 closing line).

    3-5 paragraphs:
    - Introduce the bounded-lifetime claim: `RoundSecretKey` wraps `BjSecretKey` (which is `blind_rsa_signatures::SecretKey<Sha384, PSS, Randomized>`). `RoundStateInner.rsa_signer: Option<RsaBlindSigner>` (transitively owning `RoundSecretKey`) is the lifetime bound expressible as a Rust type signature.
    - The trigger: `RoundState::transition_to(Phase::Idle)` at `coordinator/src/round/state.rs:194-200` is the SOLE site setting `self.inner = None`. Verified by grep of the entire `coordinator/src/` tree per 21-RESEARCH OQ2. No bypass paths. The 4 valid FSM edges all route through this chokepoint (Broadcast→Idle, Blame→Idle [signing timeout], Blame→Idle [missing output], InputReg→Idle [quorum fail]).
    - The cryptographic work: the wrapped `SecretKey<H,S,M>` holds `inner: rsa::RsaPrivateKey` (`blind-rsa-signatures-0.17.1/src/lib.rs:825-828`). `rsa = 0.9.10` has an UNCONDITIONAL `impl Drop for RsaPrivateKey` at `key.rs:76-82` that zeroizes `d`, `primes`, and `precomputed`, plus `impl ZeroizeOnDrop for RsaPrivateKey {}` at line 84. Both compile without any feature flag. When `self.inner = None` fires, the Drop chain runs: `Option<RoundStateInner>` → `RoundStateInner::drop` (state.rs:120-149) → `Option<RsaBlindSigner>` → `RoundSecretKey::drop` (rsa.rs) → `BjSecretKey::drop` → `<rsa::RsaPrivateKey as Drop>::drop`.
    - The newtype's value: **lifetime expression**, not redundant scrub. The `RoundSecretKey::drop` body is empty of crypto work (one `tracing::debug!` event for ops observability). The cryptographically meaningful zeroization is delegated to the upstream `rsa` crate (which §6 lists as out-of-scope per D-135 — "consensus-critical primitive with separate upstream audit posture").
    - Verification: the structural test `coordinator/src/round/state.rs::tests::round_secret_key_dropped_on_round_end` is the unconditional CI gate (D-131 load-bearing per REQUIREMENTS — "structural lifetime bound is the load-bearing claim"). The best-effort scrub test `coordinator/src/blind/rsa.rs::tests::round_secret_key_buffer_overwritten_on_drop` is sanity ceremony; may be `#[ignore]` on non-Linux per CD-50.

    **§6 Out-of-Scope (TABLE — D-134 + D-135):**

    H2 heading: `## Out-of-Scope Components` (slug `#out-of-scope-components`).

    Intro paragraph: state that "out-of-scope" means the audit charter does NOT request review of these components; their security postures rely on upstream audit work or operational properties cited per-row.

    Table with 2 columns `| Component | Rationale (relies on…) |`. Rows (per D-135 verbatim):
    - `arti-client` (Tor circuit isolation + hidden-service hosting) — relies on upstream Tor Project Arti 2.x audit posture
    - `pkarr` (Mainline DHT discovery layer) — relies on Pubky project audit posture
    - `blind-rsa-signatures` (jedisct1) crate **internals** — RFC 9474 primitive; OUR USAGE shape (AUDIT-03 RoundSecretKey) IS in-scope (§5)
    - `bip322 = "=0.0.10"` (rust-bitcoin org) crate **internals** — BIP-322 verify; OUR 26-LOC adapter at `shared/src/bip322/mod.rs::verify_via_bip322_crate` IS in-scope (§1)
    - `rust-bitcoin` + `secp256k1` (consensus primitives) — relies on upstream rust-bitcoin team audit posture
    - `bdk_wallet` (client-side descriptor wallet) — coordinator never runs bdk_wallet; client's USAGE shape at `client/src/wallet.rs::sign_bip322` IS in-scope insofar as it constructs v=2 OwnershipProof (§4)
    - External pen-test execution — v1.5 *prepares for* an external audit; the engagement itself is a separate milestone

    **§7 Residual Risks Accepted (NARRATIVE — D-134 + D-136, 3 sub-buckets):**

    H2 heading: `## Residual Risks Accepted` (slug `#residual-risks-accepted`).

    Intro paragraph: state that residual risks are dispositions documented at the v1.5 ship — each item has either an accepted-with-rationale OR a planned-v1.6-closure note.

    H3 sub-sections:

    `### Residual Risks — cargo-audit Advisories` (slug `#residual-risks-cargo-audit-advisories` — this anchor is referenced from `.cargo/audit.toml` RUSTSEC-2025-0141 and RUSTSEC-2024-0436 closing lines):
    - RUSTSEC-2023-0071 — Marvin Attack timing sidechannel on `rsa`. Mitigation: AUDIT-03 RoundSecretKey + Option<RsaBlindSigner> bounded lifetime (cite §5 by name). Disposition: **ACCEPTED** with planned v1.6+ contribution to upstream `rsa` crate for constant-time decryption.
    - RUSTSEC-2025-0141 — `bincode 2.0.1` unmaintained. Transitive dep; not directly used by blindjoin. Disposition: **ACCEPTED**; will track upstream for maintained replacement.
    - RUSTSEC-2024-0436 — `paste 1.0.15` proc-macro unmaintained. Compile-time-only macro; no runtime code path. Disposition: **ACCEPTED**; no remediation needed unless a runtime path appears in a future dep update.

    `### Residual Risks — Protocol-level` (slug `#residual-risks-protocol-level`):
    - Heterogeneous-input chain-analysis tradeoff (v1.4 ADR Decision #2): allowing mixed input script types in a single round trades a small chain-analysis signal for the privacy improvement of larger anonymity sets. Documented at `README.md` §Privacy Considerations. Disposition: **ACCEPTED**.
    - V1.4-MIN-02 uniform-output-script fingerprint: partially mitigated by Phase 18 INTEG-02 liquidity-bot rotation; operator-set bot ratios cannot eliminate. Disposition: **ACCEPTED**; v1.6+ may add Wasabi-2.0.3-style per-participant output script choice (out-of-scope per REQUIREMENTS).
    - TEST-EXT-01/02/03 differential-fixture gap: no automated cross-implementation differential tests (e.g., vs `ACken2/bip322-js`), no regtest on-chain anchor test, no v1.3↔v1.4 backwards-compat matrix. Disposition: **DOCUMENTED GAP**; closure deferred to v1.6+.

    `### Residual Risks — Operational` (slug `#residual-risks-operational`):
    - Single-coordinator-per-round trust model: DHT discovery makes coordinators replaceable but not byzantine-fault-tolerant. Disposition: **ACCEPTED** as design constraint (blindjoin is intentionally simple/disposable).
    - Sybil dilution depends on operator-set `min_participants` cap: no cryptographic sybil resistance beyond BIP-322 proof of UTXO ownership. Disposition: **ACCEPTED**; mitigation via operator policy.
    - PKARR replay window: records are versioned but a stale record may resolve momentarily. Disposition: **ACCEPTED**; relies on PKARR protocol design (Pubky upstream).
    - B-03 dynamic fee estimation: blindjoin uses static `fee_rate` config; mempool-aware polling + RBF deferred to v1.6+ pre-mainnet. Disposition: **ACCEPTED** for v1.5; **REQUIRED** before mainnet flip.

    **§8 Glossary (TABLE — D-134 + D-137):**

    H2 heading: `## Glossary` (slug `#glossary`).

    Intro paragraph: scope is active v1.4/v1.5 identifiers (~25-30 entries); retired pre-v1.4 identifiers live in `.planning/milestones/v1.0-1.3-*` archives.

    Table with 2 columns `| Project Term | Plain Audit Language |`. Rows (per D-137 — at minimum these identifiers):
    - V1.4-CRIT-01 — coordinator-side spoofing vector closed by chain-derived script_type
    - V1.4-CRIT-02 — silent sighash regression class closed by byte-equality parity tests
    - V1.4-MIN-02 — uniform-output-script-type fingerprint (partial anonymity set leak)
    - V1.4-MOD-03 — mixed-script E2E acceptance gate (Phase 18 mixed_script_e2e_three_clients_broadcast test)
    - AVAIL-01 / AVAIL-02 — availability hardening (async RPC, per-round key parsing)
    - CR-01 / CR-02 — cryptographic review checkpoints (per v1.4 sprint-0 spikes)
    - WR-01 / WR-04 — wire-format regression checkpoints (per v1.4 ADR Decision #3)
    - D-07 — manual Drop on RoundStateInner (memory zeroization)
    - D-27 — dispatcher-only `shared::bip322` public surface (V1.4-CRIT-01 static type mitigation)
    - D-31 — 10-variant Bip322Error taxonomy with PII-safety
    - D-34 — 9 cross-shape rejection properties
    - D-37 — output_script_type boot validation (`BipConfig::validate`)
    - D-111 — spk↔key cross-check at top of each sign body
    - D-122 / D-124 — Phase 20 per-script vbyte table + ScriptType plumbing
    - D-128 — RoundSecretKey newtype inside RsaBlindSigner (Phase 21)
    - D-130 — transition_to(Phase::Idle) sole FSM trigger for the Drop chain
    - ADR Decision #1 — RSA blind signatures over WabiSabi (RFC 9474)
    - ADR Decision #2 — mixed-rounds privacy tradeoff
    - ADR Decision #3 — v=2 OwnershipProof PSBT wire format (full-BIP-174)
    - ADR Decision #4 — bdk_wallet for client-side P2TR sign
    - AUDIT-01 / AUDIT-02 / AUDIT-03 — this charter, audit.toml refresh, RoundSecretKey newtype (v1.5 Phase 21)
    - PRIV-01 — round-end memory zeroization gate (state.rs::transition_to_idle_clears_inner test)
    - CRIT-01 — chain-derived ScriptType inheritance (v1.4)

    Closing line: `Retired pre-v1.4 identifiers (e.g., REPAIR-01 v1.3 forensics tags, v1.0 PRIV-* numbering) live in .planning/milestones/v1.0-1.3-* archives.`

    **Whole-document style requirements:**
    - Use markdown link form `[label](relative/path/from/this/file)` for any cross-references (e.g., `[blind-rsa-signatures](https://github.com/jedisct1/rust-blind-rsa-signatures)`, `[coordinator/src/blind/rsa.rs](../coordinator/src/blind/rsa.rs)`).
    - No auto-generated TOC (PROTOCOL.md has none).
    - File:symbol anchors throughout the prose — these are the durable refs. Line numbers are orientation only (parenthetical "approx. line NN at v1.5 ship").
    - Length: ~400-1000 lines of prose+tables. Quality is denser tables + tight narrative, NOT verbose prose; an auditor should be able to read sections out of order.
    - Do NOT include any line-number anchors in §1's table EXCEPT the orientation column.
    - Do NOT use H1 headings except for the file title (per PROTOCOL.md convention).
    - The 4 H3 anchors that audit.toml will reference MUST resolve cleanly: `#rsa-secret-key-zeroization-window` (§5 H2), `#residual-risks-cargo-audit-advisories` (§7 H3). Test by running the grep verify command in `<verify>` AFTER writing the file.

    Do NOT add a §0 "Methodology" or §9 "Open Questions" section — the 8 sections are LOCKED per REQUIREMENTS AUDIT-01 verbatim (D-134 lock).
  </action>
  <verify>
    <automated>test -f docs/AUDIT-CHARTER.md &amp;&amp; wc -l docs/AUDIT-CHARTER.md | awk '{ if ($1 &lt; 350) { print "FAIL: charter too short ($1 lines &lt; 350)"; exit 1 } else print "OK: charter is " $1 " lines" }' &amp;&amp; grep -E "^## In-Scope Modules" docs/AUDIT-CHARTER.md &amp;&amp; grep -E "^## Threat Models per Module" docs/AUDIT-CHARTER.md &amp;&amp; grep -E "^## Cross-shape Rejection Properties" docs/AUDIT-CHARTER.md &amp;&amp; grep -E "^## v=2 OwnershipProof PSBT Handling" docs/AUDIT-CHARTER.md &amp;&amp; grep -E "^## RSA SecretKey Zeroization Window" docs/AUDIT-CHARTER.md &amp;&amp; grep -E "^## Out-of-Scope Components" docs/AUDIT-CHARTER.md &amp;&amp; grep -E "^## Residual Risks Accepted" docs/AUDIT-CHARTER.md &amp;&amp; grep -E "^## Glossary" docs/AUDIT-CHARTER.md &amp;&amp; grep -E "^### Residual Risks — cargo-audit Advisories" docs/AUDIT-CHARTER.md &amp;&amp; grep -q "RoundSecretKey" docs/AUDIT-CHARTER.md &amp;&amp; grep -q "Option<RsaBlindSigner>" docs/AUDIT-CHARTER.md &amp;&amp; grep -q "rsa-0.9.10/src/key.rs:76-82" docs/AUDIT-CHARTER.md &amp;&amp; grep -q "transition_to(Phase::Idle)" docs/AUDIT-CHARTER.md &amp;&amp; grep -q "round_secret_key_dropped_on_round_end" docs/AUDIT-CHARTER.md &amp;&amp; grep -cE "^\|" docs/AUDIT-CHARTER.md | awk '{ if ($1 &lt; 30) { print "FAIL: too few table rows ($1 &lt; 30 — expected ~50+ across 4 tables in §1/§3/§6/§8)"; exit 1 } else print "OK: " $1 " table rows" }' &amp;&amp; grep -cE "reject_p2(wpkh|tr|sh_p2wpkh)_spk_with" docs/AUDIT-CHARTER.md | awk '{ if ($1 != "9") { print "FAIL: expected 9 cross-shape test names in §3, got " $1; exit 1 } else print "OK: 9 cross-shape test names enumerated" }'</automated>
  </verify>
  <acceptance_criteria>
    - CLI assertion: `docs/AUDIT-CHARTER.md` exists at path; file length ≥ 350 lines (prose-dense but not bloated).
    - Source assertion: exactly 8 H2 sections present in the AUDIT-01 mandated order (verified by ordered grep). Section headings match the slugs in the `<interfaces>` block (especially `## RSA SecretKey Zeroization Window` which audit.toml + the rsa.rs D-07 comment both reference).
    - Source assertion: the H3 anchor `#residual-risks-cargo-audit-advisories` resolves (i.e., a `### Residual Risks — cargo-audit Advisories` heading exists in §7).
    - Source assertion: the charter cites all post-21-01 symbols: `RoundSecretKey`, `Option<RsaBlindSigner>`, `transition_to(Phase::Idle)`, `round_secret_key_dropped_on_round_end`, `rsa-0.9.10/src/key.rs:76-82` (per the §5 narrative + §1 table).
    - Source assertion: §3 table enumerates ALL 9 cross-shape rejection test names (`reject_p2wpkh_spk_with_p2tr_witness`, `reject_p2wpkh_spk_with_p2sh_p2wpkh_witness`, `reject_p2tr_spk_with_p2wpkh_witness`, `reject_p2tr_spk_with_p2sh_p2wpkh_witness`, `reject_p2sh_p2wpkh_spk_with_p2wpkh_witness`, `reject_p2sh_p2wpkh_spk_with_p2tr_witness`, `reject_p2wpkh_spk_with_empty_witness`, `reject_p2tr_spk_with_empty_witness`, `reject_p2sh_p2wpkh_spk_with_empty_witness`).
    - Source assertion: §1 in-scope table uses file:symbol form (e.g., `coordinator/src/blind/rsa.rs::RoundSecretKey`) for every row.
    - Source assertion: §6 out-of-scope table lists at least 6 components (arti-client, pkarr, blind-rsa-signatures internals, bip322 internals, rust-bitcoin + secp256k1, bdk_wallet, plus external pen-test).
    - Source assertion: §8 glossary has ≥ 25 entries (D-137 requires ~25-30).
    - PII safety: no key material, no participant IDs, no transaction txids in the prose. Cross-references to file:symbol are non-PII.
    - File anchor stability: every cited symbol resolves in the codebase (executor MAY spot-check via grep but Task 3's full anchor-stability sweep is the authoritative gate).
  </acceptance_criteria>
  <done>
    `docs/AUDIT-CHARTER.md` exists with 8 H2 sections in AUDIT-01 mandated order; hybrid voice (tables §1/§3/§6/§8, narrative §2/§4/§5/§7); §5 H2 slug is `#rsa-secret-key-zeroization-window` (referenced from rsa.rs D-07 comment); §7 H3 slug `#residual-risks-cargo-audit-advisories` exists; all 9 cross-shape test names + all post-21-01 symbols + the upstream Drop chain citation appear in the prose.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refresh .cargo/audit.toml with charter anchors + RUSTSEC-2023-0071 rewrite + Reviewed: date bump (closes AUDIT-02 + SC#2)</name>
  <files>.cargo/audit.toml</files>
  <read_first>
    - .planning/REQUIREMENTS.md §AUDIT-02 (line 32 — verbatim 4-bullet specification)
    - .planning/phases/21-audit-charter-zeroization-tightening/21-CONTEXT.md §H "audit.toml refresh (AUDIT-02)" (D-139 bare-path anchor style, D-140 Reviewed: bump, D-141 new-advisories detection-then-decide protocol, D-142 flat TOML layout lock)
    - .planning/phases/21-audit-charter-zeroization-tightening/21-RESEARCH.md §"Open Question 3 — D-141: fresh cargo audit --json diff" (RECOMMENDATION block — lock the 3 existing entries verbatim with charter anchors; no new advisories) AND §".github/workflows/ci.yml cargo audit gate (Standard Research #14)" (CI gate semantics — risk of accidentally dropping an ignore ID)
    - .planning/phases/21-audit-charter-zeroization-tightening/21-PATTERNS.md §".cargo/audit.toml — refresh (21-02)" (own-file pattern — prose-comment-per-ignore convention)
    - .cargo/audit.toml (current 41-line file — executor MUST see exact current state, especially the 3 ignore comment blocks)
  </read_first>
  <action>
    Update `.cargo/audit.toml` in-place. The file currently has 41 lines: a 7-line header (lines 1-7) + a 33-line `[advisories]` section (lines 9-40) with 3 ignore entries each prefaced by a multi-line comment block.

    **Edit A — header line:** Change `# Reviewed: 2026-05-26.` (line 7) to `# Reviewed: <YYYY-MM-DD>.` where `<YYYY-MM-DD>` is the date of the 21-02 commit. Per D-140, do NOT use today's date as a pre-write placeholder; the executor commits via the project's `gsd-sdk query commit` flow which produces a real commit date. Practical: write the current date (2026-05-31 today, or whatever date `date +%Y-%m-%d` returns at execution time), commit, and if the actual commit date differs (e.g., due to a re-run on a later day), amend within the same atomic landing.

    **Edit B — RUSTSEC-2023-0071 rationale paragraph rewrite (currently lines 11-27):** Replace the existing paragraph (the one starting with `# rsa crate (transitive via blind-rsa-signatures) — Marvin Attack timing` and ending with `# this entry will be removed and a remediation plan published.`) with a NEW comment block per D-139 + 21-RESEARCH OQ1. New comment requirements:
    - Open with the same `# rsa crate (transitive via blind-rsa-signatures) — Marvin Attack timing` line (one-line summary preserved).
    - Drop the previous "destroys the key via `zeroize` after the round broadcasts" phrasing — REPLACE with language naming the AUDIT-03 bounded-window mitigation explicitly. Concrete prose template (executor may tighten):
      ```
      # The Marvin Attack model assumes the attacker can submit many chosen
      # ciphertexts against the same long-lived private key and measure
      # decryption timing precisely. blindjoin's structural countermeasures:
      # (i) per-round ephemeral RSA-2048 keypair (D-02); (ii) bounded
      # blind-sign operations per round (default max_participants = 20);
      # (iii) AUDIT-03 RoundSecretKey + Option<RsaBlindSigner> bounded
      # lifetime on RoundStateInner — the secret is dropped at the SOLE
      # FSM chokepoint transition_to(Phase::Idle) (coordinator/src/round/
      # state.rs:194-200), triggering the transitive <rsa::RsaPrivateKey
      # as Drop>::drop chain at rsa-0.9.10/src/key.rs:76-82 which zeroizes
      # d, primes, and precomputed unconditionally. The structural lifetime
      # bound is verified by coordinator/src/round/state.rs::tests::
      # round_secret_key_dropped_on_round_end. The Marvin Attack's
      # preconditions (long-lived key + unlimited measurements) do not
      # obtain in this codebase.
      ```
    - Preserve the closing sentences about the planned external audit (the existing prose lines about "If the audit finds the ephemeral-key mitigation insufficient...this entry will be removed and a remediation plan published.").
    - APPEND a NEW closing line BEFORE the literal `"RUSTSEC-2023-0071",` ID string: `# See docs/AUDIT-CHARTER.md#rsa-secret-key-zeroization-window for the full rationale.` (D-139 + CD-49 anchor slug).

    **Edit C — RUSTSEC-2025-0141 closing-line append (currently lines 30-33):** Inside the existing 4-line comment block for `RUSTSEC-2025-0141` (bincode 2.0.1 unmaintained), APPEND a closing line BEFORE the literal `"RUSTSEC-2025-0141",` ID string: `# See docs/AUDIT-CHARTER.md#residual-risks-cargo-audit-advisories for the full rationale.` (D-139 + CD-49 anchor slug).

    **Edit D — RUSTSEC-2024-0436 closing-line append (currently lines 36-39):** Inside the existing 4-line comment block for `RUSTSEC-2024-0436` (paste 1.0.15 proc-macro unmaintained), APPEND a closing line BEFORE the literal `"RUSTSEC-2024-0436",` ID string: `# See docs/AUDIT-CHARTER.md#residual-risks-cargo-audit-advisories for the full rationale.` (D-139 + CD-49 anchor slug).

    **Edit E — pre-edit cargo audit re-run (CD-48 detection step):** BEFORE applying Edits A-D, run `cargo audit --json` to confirm 21-RESEARCH OQ3's finding still holds. Expected: 0 vulnerabilities, 0 warnings against the current Cargo.lock with the unmodified audit.toml. If a NEW advisory has surfaced since 2026-05-29 (the advisory DB last commit at research time), apply CD-48 default heuristic:
    - Upstream fix available (even minor bump) → bump the dep in `Cargo.toml` + run `cargo update -p <crate>`; do NOT add an ignore.
    - No fix, transitive crypto-adjacent → add a new ignore entry with a comment block following the 3-entry style above, closing line `# See docs/AUDIT-CHARTER.md#residual-risks-cargo-audit-advisories for the full rationale.`, AND add a corresponding bullet to `docs/AUDIT-CHARTER.md` §7 sub-bucket (a). (This is the only path that touches BOTH artifacts in this plan — atomic commit per D-133a prevents drift.)
    - No fix, build-time-only macro → add ignore citing "build-time only, no runtime path."
    - DO NOT silently add an ignore. DO NOT remove an existing ID even if the advisory is fixed upstream (the IDs are LOCKED per OQ3 finding; removal happens in a separate plan).

    **Edit F — post-edit verify:** AFTER applying edits, re-run `cargo audit` and confirm exit code 0 (the 3 ignore IDs are unchanged; no removals or accidental drops). Per 21-RESEARCH §14, the CI gate would fail loudly if any ID is dropped.

    Do NOT change the flat TOML layout (D-142 lock — `[advisories]\nignore = [...]`). Do NOT introduce `[advisories.ignore."RUSTSEC-..."]` sub-tables. Do NOT add markdown link syntax (`[label](url)`) to TOML comments — bare paths only per D-139. Do NOT remove any of the 3 existing IDs.
  </action>
  <verify>
    <automated>cargo audit 2>&amp;1 | grep -E "^Success|0 vulnerabilities found" &amp;&amp; grep -c "RUSTSEC-2023-0071" .cargo/audit.toml | awk '{ if ($1 != "1") { print "FAIL: expected 1 occurrence of RUSTSEC-2023-0071, got " $1; exit 1 } else print "OK: RUSTSEC-2023-0071 preserved" }' &amp;&amp; grep -c "RUSTSEC-2025-0141" .cargo/audit.toml | awk '{ if ($1 != "1") { print "FAIL: expected 1 occurrence of RUSTSEC-2025-0141, got " $1; exit 1 } else print "OK: RUSTSEC-2025-0141 preserved" }' &amp;&amp; grep -c "RUSTSEC-2024-0436" .cargo/audit.toml | awk '{ if ($1 != "1") { print "FAIL: expected 1 occurrence of RUSTSEC-2024-0436, got " $1; exit 1 } else print "OK: RUSTSEC-2024-0436 preserved" }' &amp;&amp; grep -c "See docs/AUDIT-CHARTER.md#" .cargo/audit.toml | awk '{ if ($1 != "3") { print "FAIL: expected 3 charter anchor refs, got " $1; exit 1 } else print "OK: 3 anchor refs present" }' &amp;&amp; grep -q "docs/AUDIT-CHARTER.md#rsa-secret-key-zeroization-window" .cargo/audit.toml &amp;&amp; grep -q "docs/AUDIT-CHARTER.md#residual-risks-cargo-audit-advisories" .cargo/audit.toml &amp;&amp; grep -q "AUDIT-03" .cargo/audit.toml &amp;&amp; grep -q "Option<RsaBlindSigner>" .cargo/audit.toml &amp;&amp; grep -q "rsa-0.9.10/src/key.rs:76-82" .cargo/audit.toml &amp;&amp; grep -q "round_secret_key_dropped_on_round_end" .cargo/audit.toml &amp;&amp; ! grep -q "destroys the key via \`zeroize\` after the round broadcasts" .cargo/audit.toml &amp;&amp; ! grep -q "# Reviewed: 2026-05-26\." .cargo/audit.toml &amp;&amp; grep -E "^# Reviewed: 20[2-3][0-9]-[0-1][0-9]-[0-3][0-9]\." .cargo/audit.toml</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: `cargo audit` returns 0 vulnerabilities and 0 warnings (the 3 ignore IDs are preserved verbatim; CI gate green per 21-RESEARCH OQ3 + §14).
    - Source assertion: exactly 3 occurrences of `See docs/AUDIT-CHARTER.md#` in `.cargo/audit.toml` (one per ignore block per D-139).
    - Source assertion: the anchors `#rsa-secret-key-zeroization-window` and `#residual-risks-cargo-audit-advisories` both appear in audit.toml.
    - Source assertion: the substring `AUDIT-03` appears in the RUSTSEC-2023-0071 rationale paragraph (D-139 verbatim — RUSTSEC-2023-0071 rationale names the AUDIT-03 mitigation by name).
    - Source assertion: the substring `Option<RsaBlindSigner>` appears in the RUSTSEC-2023-0071 rationale paragraph (cites the bounded lifetime per 21-RESEARCH OQ1).
    - Source assertion: the substring `rsa-0.9.10/src/key.rs:76-82` appears in the RUSTSEC-2023-0071 rationale paragraph (cites the upstream Drop chain).
    - Source assertion: the substring `round_secret_key_dropped_on_round_end` appears in the RUSTSEC-2023-0071 rationale paragraph (cites the structural test that verifies the bounded lifetime — symmetry with charter §5 per plan-checker warning 2).
    - Source assertion: the previous phrase `destroys the key via \`zeroize\` after the round broadcasts` is ABSENT (replaced per D-139 + OQ1).
    - Source assertion: the previous `# Reviewed: 2026-05-26.` line is ABSENT (replaced with the 21-02 commit date per D-140).
    - Source assertion: a NEW `# Reviewed: YYYY-MM-DD.` line matching the regex `^# Reviewed: 20[2-3][0-9]-[0-1][0-9]-[0-3][0-9]\.` is PRESENT.
    - Source assertion: each of the 3 ignore IDs (RUSTSEC-2023-0071, RUSTSEC-2025-0141, RUSTSEC-2024-0436) appears exactly once.
    - Layout assertion: the TOML file remains flat `[advisories]\nignore = [...]` (no `[advisories.ignore."..."]` sub-tables per D-142). No markdown link syntax `[label](url)` in comments.
  </acceptance_criteria>
  <done>
    `.cargo/audit.toml` has 3 charter-anchor closing lines (one per ignore block), the RUSTSEC-2023-0071 rationale paragraph is rewritten to name AUDIT-03 bounded-window mitigation + cite the upstream `rsa::RsaPrivateKey` Drop chain at the verified line numbers, the `Reviewed:` date is bumped to the 21-02 commit date, the 3 ignore IDs are unchanged, the flat TOML layout is preserved, and `cargo audit` returns 0 vulnerabilities.
  </done>
</task>

<task type="auto">
  <name>Task 3: Insert README.md §Security Model audit-charter callout + full anchor-stability sweep + atomic-commit landing (closes AUDIT-01 README integration per D-143 + CD-52 + D-133a atomic-commit lock + AUDIT-01/02 SC#1+SC#2 close)</name>
  <files>README.md</files>
  <read_first>
    - .planning/phases/21-audit-charter-zeroization-tightening/21-CONTEXT.md §I "README integration (AUDIT-01)" (D-143 prose template, CD-52 line-confirmation step)
    - .planning/phases/21-audit-charter-zeroization-tightening/21-RESEARCH.md §"README.md §Security Model current shape (Standard Research #11)" (LOCKED insertion point: between line 300 "Supply-chain hygiene" and line 302 "Test infrastructure (v1.3 Phase 9)")
    - .planning/phases/21-audit-charter-zeroization-tightening/21-PATTERNS.md §"README.md §Security Model audit-charter callout — INSERT (21-02)" (the analog hardening-rollup paragraphs at README.md:300 and 304 — established `**Category (vN.x):**` form)
    - README.md (full §Security Model — executor MUST verify the current line numbers before edit; if README has had any edits between research and execution, the insertion point may have shifted — fall back to the SEMANTIC anchor: after the Supply-chain hygiene paragraph, before the Test infrastructure paragraph)
    - docs/AUDIT-CHARTER.md (just-created in Task 1 — used to verify the markdown link target resolves)
  </read_first>
  <action>
    **Edit A — Locate insertion point (CD-52):** Read `README.md` and locate the "Supply-chain hygiene" paragraph (currently line 300 per 21-RESEARCH §11). The insertion point is the BLANK LINE between this paragraph and the "Test infrastructure (v1.3 Phase 9)" paragraph (currently line 302). If line numbers have shifted between research (2026-05-31) and execution, use the SEMANTIC anchor: insert AFTER the "Supply-chain hygiene" paragraph (ends with "...stale key.") and BEFORE the "Test infrastructure (v1.3 Phase 9)" paragraph (starts with "Integration tests under `tests/integration/` no longer silently graceful-skip in CI").

    **Edit B — Insert new paragraph (D-143 + CD-52):** Insert one new paragraph at the located point. Style follows the established `**Category (vN.x):** prose` form used by 5 existing hardening rollups at README.md:296-304.

    Prose template (executor may tighten per D-143's allowance for minor rephrasing; load-bearing contract: opens with `**External audit charter (v1.5):**`, contains a markdown link to `docs/AUDIT-CHARTER.md`, enumerates what the charter contains):

    ```
    **External audit charter (v1.5):** [docs/AUDIT-CHARTER.md](docs/AUDIT-CHARTER.md) enumerates in-scope modules with file:symbol refs, threat models per module, the 9 cross-shape rejection properties locked at v1.4, the v=2 OwnershipProof PSBT handling boundary, the RSA SecretKey zeroization window (RoundSecretKey + bounded lifetime per AUDIT-03), out-of-scope dependencies, residual risks accepted with rationale, and a glossary mapping project terms to plain audit language.
    ```

    One paragraph, no sub-bullets, no embedded code spans except for the markdown link. Blank lines before and after to match the established paragraph spacing.

    **Edit C — Full anchor-stability sweep (closes the loop across 3 artifacts):**

    After Tasks 1 + 2 + this Task 3's Edits A+B are applied, run the following grep cross-verification:

    1. Every `docs/AUDIT-CHARTER.md#<anchor>` reference in `.cargo/audit.toml` and `coordinator/src/blind/rsa.rs` resolves to a real H2/H3 heading in `docs/AUDIT-CHARTER.md`:
       ```
       grep -E "docs/AUDIT-CHARTER.md#[a-z0-9-]+" .cargo/audit.toml coordinator/src/blind/rsa.rs | \
         sed -E 's|.*docs/AUDIT-CHARTER\.md#([a-z0-9-]+).*|\1|' | sort -u
       ```
       For each anchor in the output, verify a corresponding heading exists in `docs/AUDIT-CHARTER.md`. The auto-slug for `## RSA SecretKey Zeroization Window` is `rsa-secret-key-zeroization-window` (markdown auto-slug rule: lowercase, spaces→hyphens, strip non-alphanumeric). The auto-slug for `### Residual Risks — cargo-audit Advisories` is `residual-risks-cargoaudit-advisories` OR `residual-risks--cargo-audit-advisories` depending on the renderer's em-dash handling — TEST the actual GitHub render by previewing the rendered Markdown.

       If a slug mismatch is found between what audit.toml references and what the charter heading produces, FIX the charter heading (preferred — keeps audit.toml stable) OR fix the audit.toml reference. Resolve in ONE re-edit; do NOT split across commits (D-133a atomic-commit lock).

    2. Every charter file:symbol anchor cited in §1 + §2 + §4 + §5 prose resolves to a real symbol in the codebase. Spot-check via grep:
       ```
       for sym in \
         "coordinator/src/blind/rsa.rs::RsaBlindSigner" \
         "coordinator/src/blind/rsa.rs::RoundSecretKey" \
         "coordinator/src/round/state.rs::RoundStateInner" \
         "coordinator/src/round/state.rs::transition_to" \
         "coordinator/src/bitcoin/utxo.rs::validate_utxo" \
         "coordinator/src/bitcoin/utxo.rs::dispatch_ownership_proof" \
         "coordinator/src/bitcoin/utxo.rs::decode_psbt_input_witness" \
         "client/src/round/input.rs::build_v2_psbt_input_b64" \
         "shared/src/bip322/mod.rs::detect_script_type" \
         "shared/src/bip322/mod.rs::verify_simple" \
         "shared/src/bip322/mod.rs::sign_simple" \
         "coordinator/src/config.rs::BipConfig::validate"; do
         file="${sym%::*}"; symbol="${sym##*::}"
         grep -q "$symbol" "$file" || echo "MISSING: $sym"
       done
       ```
       Expected: all 12 symbols resolve. (Per 21-RESEARCH §10: pre-Phase-21 all 12 were verified; Phase 21 plan 01 added the 2 new ones — `RoundSecretKey`, `RoundSecretKey::drop` — so post-21-01 they also resolve.)

    3. Every cross-shape rejection test name cited in charter §3 resolves in `shared/tests/bip322_cross_shape.rs`:
       ```
       for fn_name in \
         reject_p2wpkh_spk_with_p2tr_witness \
         reject_p2wpkh_spk_with_p2sh_p2wpkh_witness \
         reject_p2tr_spk_with_p2wpkh_witness \
         reject_p2tr_spk_with_p2sh_p2wpkh_witness \
         reject_p2sh_p2wpkh_spk_with_p2wpkh_witness \
         reject_p2sh_p2wpkh_spk_with_p2tr_witness \
         reject_p2wpkh_spk_with_empty_witness \
         reject_p2tr_spk_with_empty_witness \
         reject_p2sh_p2wpkh_spk_with_empty_witness; do
         grep -q "fn $fn_name" shared/tests/bip322_cross_shape.rs || echo "MISSING: $fn_name"
       done
       ```
       Expected: all 9 resolve.

    4. The README → charter link is well-formed: `grep -q "\[docs/AUDIT-CHARTER.md\](docs/AUDIT-CHARTER.md)" README.md`.

    **Edit D — Atomic-commit landing (D-133a critical lock):**

    Stage all 3 modified files together:
    - `docs/AUDIT-CHARTER.md` (NEW)
    - `.cargo/audit.toml` (refreshed)
    - `README.md` (callout inserted)

    Commit message body (executor passes via heredoc per Bash tool guidance):
    ```
    docs(21): publish AUDIT-CHARTER.md + refresh audit.toml + README callout

    Closes AUDIT-01 + AUDIT-02. Single atomic commit (D-133a) prevents
    the anchor-drift window between audit.toml and the charter.

    - docs/AUDIT-CHARTER.md: NEW, 8 H2 sections (in-scope, threat models,
      9 cross-shape rejections, v=2 OwnershipProof PSBT, RSA zeroization
      window per AUDIT-03, out-of-scope, residual risks, glossary).
    - .cargo/audit.toml: 3 charter-anchor closing lines, RUSTSEC-2023-0071
      paragraph rewritten to name AUDIT-03 bounded-window mitigation
      (no more "best-effort"; cites transitive rsa::RsaPrivateKey Drop
      chain at rsa-0.9.10/src/key.rs:76-82), Reviewed: date bumped.
    - README.md §Security Model: External audit charter callout
      inserted between Supply-chain hygiene and Test infrastructure
      paragraphs.

    Cross-phase invariants green at this boundary: cargo audit 0/0,
    cargo test --test integration full_round 8/8, mixed_script_e2e 1/1,
    cargo clippy --workspace --all-targets -- -D warnings clean.

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    ```

    Do NOT split the commit. Do NOT push (per the Bash tool guidance: "DO NOT push to the remote repository unless the user explicitly asks").
  </action>
  <verify>
    <automated>grep -q "External audit charter (v1.5)" README.md &amp;&amp; grep -q "\[docs/AUDIT-CHARTER\.md\](docs/AUDIT-CHARTER\.md)" README.md &amp;&amp; sed -n '/Supply-chain hygiene/,/Test infrastructure (v1.3 Phase 9)/p' README.md | grep -q "External audit charter (v1.5)" &amp;&amp; for sym in RoundSecretKey RsaBlindSigner RoundStateInner transition_to validate_utxo dispatch_ownership_proof decode_psbt_input_witness build_v2_psbt_input_b64 detect_script_type verify_simple sign_simple BipConfig; do grep -rq "$sym" coordinator/ client/ shared/ || (echo "MISSING SYMBOL: $sym" &amp;&amp; exit 1); done &amp;&amp; for fn_name in reject_p2wpkh_spk_with_p2tr_witness reject_p2wpkh_spk_with_p2sh_p2wpkh_witness reject_p2tr_spk_with_p2wpkh_witness reject_p2tr_spk_with_p2sh_p2wpkh_witness reject_p2sh_p2wpkh_spk_with_p2wpkh_witness reject_p2sh_p2wpkh_spk_with_p2tr_witness reject_p2wpkh_spk_with_empty_witness reject_p2tr_spk_with_empty_witness reject_p2sh_p2wpkh_spk_with_empty_witness; do grep -q "fn $fn_name" shared/tests/bip322_cross_shape.rs || (echo "MISSING TEST: $fn_name" &amp;&amp; exit 1); done &amp;&amp; cargo audit 2>&amp;1 | grep -E "^Success|0 vulnerabilities found" &amp;&amp; cargo test --test integration full_round 2>&amp;1 | grep -E "test result: ok\. 8 passed" &amp;&amp; cargo test --test integration mixed_script_e2e 2>&amp;1 | grep -E "test result: ok\. 1 passed" &amp;&amp; cargo clippy --workspace --all-targets -- -D warnings &amp;&amp; test -z "$(git diff --name-only HEAD -- coordinator/src/ shared/ client/ 2>/dev/null)"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion (README.md): the literal text `External audit charter (v1.5):` appears in README.md.
    - Source assertion (README.md): a markdown link to `docs/AUDIT-CHARTER.md` (rendered form: `[docs/AUDIT-CHARTER.md](docs/AUDIT-CHARTER.md)`) is present.
    - Placement assertion: the new paragraph is between the "Supply-chain hygiene" paragraph and the "Test infrastructure (v1.3 Phase 9)" paragraph (verified by `sed -n '/Supply-chain hygiene/,/Test infrastructure/p' README.md | grep -q "External audit charter (v1.5)"`).
    - Anchor-stability assertion: all 12 file:symbol anchors cited in charter §1/§2/§4/§5 resolve to real symbols in the codebase (Task 3 Edit C step 2 spot-check passes).
    - Anchor-stability assertion: all 9 cross-shape rejection test names cited in charter §3 resolve in `shared/tests/bip322_cross_shape.rs` (Edit C step 3 passes).
    - Anchor-stability assertion: the audit.toml charter-anchor refs (e.g., `#rsa-secret-key-zeroization-window`) resolve to real H2/H3 headings in `docs/AUDIT-CHARTER.md` (Edit C step 1 passes).
    - Anchor-stability assertion: the rsa.rs D-07 comment's `#rsa-secret-key-zeroization-window` ref resolves (the §5 heading was emitted in Task 1).
    - Behavior assertion: `cargo audit` 0 vulnerabilities + 0 warnings (Task 2 refresh confirmed working).
    - Behavior assertion: `cargo test --test integration full_round` 8/8 + `cargo test --test integration mixed_script_e2e` 1/1 + `cargo clippy --workspace --all-targets -- -D warnings` clean (the 3 cross-phase invariants — Phase 21 plan 02 makes no code changes; all should be invariant from the 21-01 boundary).
    - Scope assertion: `git diff --name-only HEAD -- coordinator/src/ shared/ client/` returns empty (Phase 21 plan 02 modifies only `docs/`, `.cargo/`, and `README.md`).
    - Atomic commit assertion: the commit landed by this task includes EXACTLY the 3 files (`docs/AUDIT-CHARTER.md`, `.cargo/audit.toml`, `README.md`). No partial commits.
  </acceptance_criteria>
  <done>
    `README.md` §Security Model contains the new "**External audit charter (v1.5):**" paragraph linking to `docs/AUDIT-CHARTER.md`, placed between the Supply-chain hygiene and Test infrastructure paragraphs. All 3 cross-artifact anchor types resolve (charter ↔ audit.toml ↔ rsa.rs comment ↔ README link ↔ charter). All cross-phase invariants are green. The 3 files are landed in a single atomic commit per D-133a.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Charter prose ↔ source code | The charter cites file:symbol anchors throughout. Drift between cited symbols and the codebase makes the charter untrustworthy. Mitigation: full anchor-stability sweep at Task 3 Edit C. |
| Charter sections ↔ audit.toml anchor refs | `.cargo/audit.toml` references charter headings by markdown auto-slug. Drift between the slug audit.toml expects and the actual heading the charter emits breaks both directions of the audit narrative. Mitigation: Task 3 Edit C step 1 — explicit grep cross-check. |
| Atomic commit ↔ pre-existing state | D-133a requires all 3 artifacts land together. Splitting the commit creates a window where audit.toml points at anchors that don't yet exist in the charter. Mitigation: Edit D stages all 3 files for a single commit. |
| Audit.toml ↔ CI gate | `cargo audit` exits non-zero on any advisory not in `.cargo/audit.toml`. Accidentally dropping an ignore ID would fail CI loudly. Mitigation: Task 2 acceptance criteria explicitly grep-counts each of the 3 IDs (count must be exactly 1 each). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02-T-DRIFT | Tampering (anchor drift) | Charter ↔ audit.toml anchor pairing | mitigate | Task 3 Edit C step 1 runs an explicit grep cross-check: every `docs/AUDIT-CHARTER.md#<anchor>` ref in audit.toml has a matching H2/H3 heading in the charter. If a slug mismatch is found, fix in the SAME atomic commit (D-133a). |
| T-21-02-T-SYM | Tampering (symbol drift) | Charter file:symbol anchors | mitigate | Task 3 Edit C step 2 spot-checks all 12 cited symbols against the codebase. Any `MISSING SYMBOL` output fails the task. 21-RESEARCH §10 verified all 12 at research time; post-21-01 the 2 NEW symbols (`RoundSecretKey`, `RoundSecretKey::drop`) also resolve. |
| T-21-02-T-SC | Tampering (silent audit.toml regression) | RUSTSEC-2023-0071 paragraph | mitigate | Task 2 acceptance criteria EXPLICITLY check for the absence of the previous "best-effort" prose (`grep -v "best-effort" .cargo/audit.toml`) AND the presence of the new prose tokens (`AUDIT-03`, `Option<RsaBlindSigner>`, `rsa-0.9.10/src/key.rs:76-82`). Prevents prose regression to the v1.4 framing. |
| T-21-02-T-ID-DROP | Tampering (accidental ignore-ID drop) | audit.toml's 3 ignore IDs | mitigate | Task 2 acceptance criteria run `grep -c "RUSTSEC-..." .cargo/audit.toml` for each of the 3 IDs and assert exactly 1 occurrence each. If any ID is accidentally removed during the prose rewrite, this gate fails BEFORE the commit lands. The CI gate (per 21-RESEARCH §14) would also surface this loudly. |
| T-21-02-I-PII | Information Disclosure | Charter prose | mitigate | The charter is a PUBLIC document (commits to `main`, linked from README). PII safety: no key material, no participant IDs, no transaction txids. Project-term IDs (V1.4-CRIT-01, AUDIT-03, etc.) are non-PII. Acceptance criterion: spot-check that no `0x[0-9a-f]{32}` or similar key-byte fragment appears in the charter (use grep). |
| T-21-02-R | Repudiation | `Reviewed:` date semantics | mitigate | D-140 locks the `Reviewed:` date to the actual 21-02 commit date (NOT a future-placeholder, NOT pre-write). This makes the review timestamp auditable — "the review happened when Phase 21 landed, not before, not later." |
| T-21-02-D-LR | Denial of Service (link rot) | README → charter markdown link | mitigate | Use relative path `[docs/AUDIT-CHARTER.md](docs/AUDIT-CHARTER.md)` — survives repo moves. The link is verified to render in GitHub markdown at commit time via Task 3 acceptance grep. |
| T-21-02-D-CARGO | Denial of Service (cargo audit gate flip) | CI `audit` job | mitigate | Task 2 Edit F re-runs `cargo audit` AFTER edits and BEFORE commit. Confirms 0 vulnerabilities. The CI gate post-commit must pass identically (the 3 IDs are preserved). |
| T-21-02-E | Elevation of Privilege | n/a | n/a | This plan touches no auth or access-control surfaces. |
| T-21-02-SC | Tampering | npm/pip/cargo installs | n/a | This plan installs no new packages (verified by 21-RESEARCH `Package Legitimacy Audit`: empty). No `[ASSUMED]`/`[SUS]`/`[SLOP]` legitimacy gate needed. |
| T-21-02-CRIT-01 | Tampering | V1.4-CRIT-01 dispatcher-only invariant | mitigate | Task 3 acceptance criterion: `git diff --name-only HEAD -- coordinator/src/ shared/ client/` returns empty. Phase 21 plan 02 modifies only `docs/` + `.cargo/` + `README.md` — strictly documentation + config. The V1.4-CRIT-01 invariant is preserved by scope discipline. |
</threat_model>

<verification>
After all 3 tasks complete (single atomic commit landed per D-133a):

1. **Charter exists and is structurally sound** (Task 1 verify): 8 H2 sections in mandated order, ≥350 lines, citing all post-21-01 symbols, enumerating all 9 cross-shape tests.

2. **Audit.toml refresh is correct** (Task 2 verify): 3 anchor refs added, RUSTSEC-2023-0071 paragraph rewritten with AUDIT-03 + Option<RsaBlindSigner> + the upstream Drop chain citation, `Reviewed:` date bumped, 3 IDs preserved verbatim.

3. **README callout is placed correctly** (Task 3 Edit A+B): paragraph between Supply-chain hygiene and Test infrastructure, markdown link well-formed.

4. **Cross-artifact anchor stability is verified** (Task 3 Edit C):
   - audit.toml `#anchor` refs → charter headings: RESOLVE.
   - rsa.rs D-07 `#rsa-secret-key-zeroization-window` → charter §5 H2: RESOLVES.
   - Charter file:symbol anchors → codebase symbols: RESOLVE (all 12+2 post-21-01 symbols).
   - Charter §3 cross-shape test names → `shared/tests/bip322_cross_shape.rs`: RESOLVE (all 9).
   - README markdown link → `docs/AUDIT-CHARTER.md`: RESOLVES.

5. **Cross-phase invariants stay green** (Task 3 final automated checks):
   - `cargo audit` 0/0 (with refreshed audit.toml).
   - `cargo test --test integration full_round` 8/8.
   - `cargo test --test integration mixed_script_e2e` 1/1.
   - `cargo clippy --workspace --all-targets -- -D warnings` clean.
   - Phase 20 FEE-03 tests not re-run by this plan (no code changes affect them; the 21-01 boundary's Task 3 already verified them).

6. **Scope discipline:** `coordinator/src/`, `shared/`, `client/` UNTOUCHED (V1.4-CRIT-01 invariant preserved).

7. **Atomic landing** (D-133a): single commit contains exactly the 3 files (`docs/AUDIT-CHARTER.md`, `.cargo/audit.toml`, `README.md`).

Manual verification (no human-check required — all gates are automated):
- The cross-artifact anchor stability is the most fragile boundary; Task 3 Edit C runs explicit grep cross-checks against all anchor types.
- The RUSTSEC-2023-0071 paragraph rewrite is the most prose-heavy change; Task 2 acceptance criteria explicitly check for the absence of the previous "best-effort" framing AND the presence of the new prose tokens.
</verification>

<success_criteria>
- AUDIT-01 SC#1 (REQUIREMENTS + ROADMAP): `docs/AUDIT-CHARTER.md` exists, committed in `main`, linked from README §Security Model, 8 sections in mandated order, hybrid voice per D-134, anchor stability across the 4 artifact types (charter, audit.toml, rsa.rs D-07 comment, README link). ✓ (Task 1 + Task 3)
- AUDIT-02 SC#2 (REQUIREMENTS + ROADMAP): `.cargo/audit.toml` has 3 charter-anchor closing lines (D-139); RUSTSEC-2023-0071 paragraph rewritten to name AUDIT-03 bounded-window mitigation by name (D-139 + 21-RESEARCH OQ1); `Reviewed:` date bumped to 21-02 commit date (D-140); 3 ignore IDs preserved verbatim (21-RESEARCH OQ3 finding); flat TOML layout (D-142); no silent additions (REQUIREMENTS Out-of-Scope). ✓ (Task 2)
- AUDIT-01 + AUDIT-02 contribute to SC#5: `cargo audit` 0-vulns with refreshed audit.toml; cross-phase invariants (full_round 8/8, mixed_script_e2e 1/1, clippy clean) preserved. ✓ (Task 3 automated verify)
- D-133a atomic-commit lock: single landing for charter + audit.toml + README. ✓ (Task 3 Edit D)
- All locked decisions D-134 through D-143 are honored. ✓ (per Task action references)
- All Claude's-Discretion items CD-48 (new advisory decision — OQ3 found none; lock 3 existing), CD-49 (charter anchor naming — `#rsa-secret-key-zeroization-window` + `#residual-risks-cargo-audit-advisories`), CD-51 (§4 paragraph count — 4-8 paragraphs per CONTEXT range), CD-52 (README line confirmation — between line 300 and line 302 verified by 21-RESEARCH §11) are resolved. ✓
</success_criteria>

<output>
Create `.planning/phases/21-audit-charter-zeroization-tightening/21-02-SUMMARY.md` when done. Summary includes:
- Files changed (the 3 file paths + line counts for charter [new], audit.toml [delta], README.md [delta = +1 paragraph]).
- Charter section structure (8 H2 headings + their slugs).
- Audit.toml deltas (3 anchor refs added; RUSTSEC-2023-0071 paragraph rewritten; Reviewed: date bumped; 3 IDs preserved).
- Anchor-stability sweep results (Task 3 Edit C output — every anchor cross-check returned 0 misses).
- Cross-phase invariant matrix (cargo audit, full_round, mixed_script_e2e, clippy — all green).
- Atomic-commit confirmation (single git commit, 3 files).
- Decision references: D-133a (atomic landing), D-134 (8 sections + hybrid voice), D-135 (§6 extended to all 3rd-party crypto), D-136 (§7 3 sub-buckets), D-137 (§8 active v1.4/v1.5 only), D-138 (file:symbol anchors), D-139 (bare-path closing-line), D-140 (Reviewed: bump), D-141 (no new advisories per OQ3), D-142 (flat TOML), D-143 (README callout placement), CD-48 (no new ignores), CD-49 (anchor slugs), CD-51 (§4 length), CD-52 (README line confirmation).
- Phase 21 milestone close: v1.5 Audit-Readiness & Multi-Script Finish ready for ship.
- Time spent (start → end).
</output>
