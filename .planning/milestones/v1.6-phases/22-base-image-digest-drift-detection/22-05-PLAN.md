---
phase: 22-base-image-digest-drift-detection
plan: 05
type: execute
wave: 3
depends_on:
  - 22-01
  - 22-02
  - 22-03
  - 22-04
files_modified:
  - SECURITY.md
  - CONTRIBUTING.md
autonomous: true
requirements:
  - DRIFT-01
requirements_addressed:
  - DRIFT-01

must_haves:
  truths:
    - "`SECURITY.md` §Supply-chain status has a new `### Base-image digests (v1.6 onward)` subsection documenting the canonical manifest, the human-review-only bump policy, the CODEOWNERS gate, the daily drift workflow with workflow_dispatch rehearsal, and the Pitfall 9 upstream-hex idempotency guarantee."
    - "`SECURITY.md` §Known gaps at v1.5 — `Base image digest pins are manual.` bullet is annotated with `~~strikethrough~~` + `**Closed in v1.6**` cross-link to the new subsection."
    - "`SECURITY.md` §v1.6 supply-chain plan — `Automated base-image digest drift check` bullet is annotated with strikethrough + `✓ Shipped in Phase 22` cross-link."
    - "`CONTRIBUTING.md` has a new `## Bumping base-image digests` section AFTER `## Tagging releases` walking contributors through when to bump, how to resolve the new digest, the one-image-per-PR policy, the no-auto-merge mandate, and the regex-check failure recovery path."
  artifacts:
    - path: "SECURITY.md"
      provides: "Operator-facing policy for v1.6 base-image digest discipline (D-05)"
      contains: "Base-image digests (v1.6 onward)"
    - path: "SECURITY.md"
      provides: "Strikethrough closure of v1.5 manual-pin gap"
      contains: "~~Base image digest pins are manual.~~"
    - path: "CONTRIBUTING.md"
      provides: "Contributor-facing PR etiquette for digest bumps (D-05)"
      contains: "Bumping base-image digests"
  key_links:
    - from: "SECURITY.md §Base-image digests (v1.6 onward)"
      to: "docker/digests.txt"
      via: "markdown relative link"
      pattern: "\\[`docker/digests\\.txt`\\]\\(docker/digests\\.txt\\)"
    - from: "SECURITY.md §Base-image digests (v1.6 onward)"
      to: ".github/CODEOWNERS"
      via: "markdown relative link"
      pattern: "\\.github/CODEOWNERS"
    - from: "SECURITY.md §Base-image digests (v1.6 onward)"
      to: ".github/workflows/digest-drift-check.yml"
      via: "markdown relative link"
      pattern: "digest-drift-check\\.yml"
    - from: "CONTRIBUTING.md §Bumping base-image digests"
      to: "SECURITY.md#supply-chain-status"
      via: "markdown anchor link"
      pattern: "SECURITY\\.md#supply-chain-status"
---

<objective>
Update `SECURITY.md` and `CONTRIBUTING.md` to document the Phase 22 supply-chain policy and contributor workflow (D-05).

Purpose: D-05 mandates that the no-auto-merge policy is documented in two places — operator-facing (SECURITY.md) and contributor-facing (CONTRIBUTING.md). These are the prose half of the structural-plus-prose gate (CODEOWNERS is the structural half). Without them, the CODEOWNERS file is opaque to anyone who has not read PITFALLS.md. The SECURITY.md update also closes the v1.5 "manual digest pins" known gap, which is a v1.5-to-v1.6 milestone-close requirement.
Output: Two modified files: `SECURITY.md` (new subsection plus two strikethrough annotations) and `CONTRIBUTING.md` (new section after Tagging releases).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-base-image-digest-drift-detection/22-CONTEXT.md
@.planning/phases/22-base-image-digest-drift-detection/22-RESEARCH.md
@.planning/phases/22-base-image-digest-drift-detection/22-PATTERNS.md
@SECURITY.md
@CONTRIBUTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update `SECURITY.md` §Supply-chain status with v1.6 base-image-digest policy</name>
  <files>SECURITY.md</files>
  <read_first>
    - SECURITY.md (full file — focus on the `## Supply-chain status` section; identify the `### Known gaps at v1.5` and `### v1.6 supply-chain plan` subsections plus the closing "Until those land" caveat)
    - .planning/phases/22-base-image-digest-drift-detection/22-RESEARCH.md §7.1 lines 700-755 (locked SECURITY.md copy: the new subsection content, the v1.5-gap strikethrough rewrite, the v1.6-plan strikethrough rewrite)
    - .planning/phases/22-base-image-digest-drift-detection/22-PATTERNS.md §"SECURITY.md MODIFY" lines 330-393 (audit-charter prose voice; bold-lede bullet style; the strikethrough + "Closed in v1.X" cross-reference annotation pattern; the standing operator-facing caveat preservation rule)
  </read_first>
  <action>
    Make THREE coordinated edits to `SECURITY.md`:

    **Edit 1 — Insert the new `### Base-image digests (v1.6 onward)` subsection.** Drop the block locked verbatim in RESEARCH.md §7.1 lines 707-746 under the existing `## Supply-chain status` heading. Per PATTERNS.md §"SECURITY.md MODIFY" lines 380-385, placement is additive: insert between `### Known gaps at v1.5` and `### v1.6 supply-chain plan` to keep chronological reading order. The subsection covers in this order:
    - Lead paragraph naming blindjoin's two upstream base images, stating they are pinned by digest as of v1.6, with markdown link `[\`docker/digests.txt\`](docker/digests.txt)` and a reference to the composite action at `[\`.github/actions/read-base-digests/\`](.github/actions/read-base-digests/)`.
    - Bold-lede paragraph asserting `docker/digests.txt` as the canonical record of which upstream base images each release was built against.
    - Bold-lede paragraph naming the human-review-only bump policy, with `[`.github/CODEOWNERS`](.github/CODEOWNERS)` link, branch-protection requirement, and the explicit `**Do not auto-merge digest bumps**` directive plus its rationale (xz utils / event-stream-style upstream compromise).
    - Bold-lede `**Drift detection.**` paragraph naming `[.github/workflows/digest-drift-check.yml](.github/workflows/digest-drift-check.yml)`, daily 09:00 UTC cadence, the `docker buildx imagetools inspect` mechanism, and the explicit "opens issues, not PRs, by design" statement. Mentions the retag-vs-substantive triage hint surface (issue body content).
    - Closing paragraph naming the idempotency guarantee (keyed on upstream digest hex, so two different drifts of the same tag yield two different issues), the `workflow_dispatch` Actions-tab rehearsal path, and that workflow_dispatch is the recommended rehearsal path before pulling any digest bump.

    Match the audit-charter prose voice per PATTERNS.md §"audit-charter prose voice" lines 381-388: direct/declarative ("blindjoin's `docker/Dockerfile` derives from..."), bold-lede bullets, markdown links with relative paths, no hedging.

    **Edit 2 — Strike through the v1.5 known gap.** Find the existing `### Known gaps at v1.5 > **Base image digest pins are manual.** ...` bullet (current SECURITY.md, the multi-sentence bullet about manual digest verification) and rewrite it per RESEARCH.md §7.1 line 750 to:

    `- **~~Base image digest pins are manual.~~** **Closed in v1.6** — see [Base-image digests (v1.6 onward)](#base-image-digests-v16-onward).`

    The strikethrough lede plus "Closed in v1.X" cross-link is PATTERNS.md §"SECURITY.md MODIFY" lines 386-389 — the project-wide pattern for moving a gap from "Known" to "shipped".

    **Edit 3 — Strike through the v1.6 plan bullet.** Find the existing `### v1.6 supply-chain plan > **Automated base-image digest drift check** ...` bullet and rewrite it per RESEARCH.md §7.1 line 753 to:

    `- **~~Automated base-image digest drift check~~** ✓ Shipped in Phase 22 — see [Base-image digests (v1.6 onward)](#base-image-digests-v16-onward).`

    Implementing D-05 (operator-facing prose half of the gate).

    DO NOT add a third image to the policy text — the manifest is locked at 2 images (D-02).
    DO NOT remove or alter the other existing v1.5 known-gap bullets (cosign image attestations, PGP signatures, reproducible-build instructions) — only the base-image-digest-pin bullet gets the strikethrough closure here. Those other v1.5 gaps close in Phases 23, 24, 25.
    DO NOT rewrite the section's lead paragraph or the closing "Until those land, **treat the SHA-256 checksum ...**" caveat — PATTERNS.md §"SECURITY.md MODIFY" line 391 calls this the "section's standing operator-facing caveat — keep the form when adding new subsections".
  </action>
  <verify>
    <automated>test -f SECURITY.md &amp;&amp; grep -q '### Base-image digests (v1.6 onward)' SECURITY.md &amp;&amp; grep -q 'docker/digests.txt' SECURITY.md &amp;&amp; grep -q '\.github/CODEOWNERS' SECURITY.md &amp;&amp; grep -q 'digest-drift-check.yml' SECURITY.md &amp;&amp; grep -q '~~Base image digest pins are manual.~~' SECURITY.md &amp;&amp; grep -q 'Closed in v1.6' SECURITY.md &amp;&amp; grep -q '~~Automated base-image digest drift check~~' SECURITY.md &amp;&amp; grep -q 'Shipped in Phase 22' SECURITY.md &amp;&amp; grep -q 'Do not auto-merge digest bumps' SECURITY.md &amp;&amp; grep -q 'docker buildx imagetools inspect' SECURITY.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f SECURITY.md`
    - New subsection heading present: `grep -q '^### Base-image digests (v1.6 onward)$' SECURITY.md`
    - Markdown link to canonical manifest: `grep -q '\[`docker/digests.txt`\](docker/digests.txt)' SECURITY.md`
    - Markdown link to CODEOWNERS file: `grep -q '\[`\.github/CODEOWNERS`\](\.github/CODEOWNERS)' SECURITY.md`
    - Markdown link / reference to drift workflow: `grep -q 'digest-drift-check.yml' SECURITY.md`
    - No-auto-merge directive in bold: `grep -q '\*\*Do not auto-merge digest bumps\*\*' SECURITY.md`
    - Drift mechanism named: `grep -q 'docker buildx imagetools inspect' SECURITY.md`
    - Daily cadence stated: `grep -q '09:00 UTC' SECURITY.md`
    - Strikethrough on the v1.5 manual-pin gap: `grep -q '~~Base image digest pins are manual.~~' SECURITY.md`
    - Closed-in-v1.6 annotation: `grep -q '\*\*Closed in v1.6\*\*' SECURITY.md`
    - Strikethrough on the v1.6-plan bullet: `grep -q '~~Automated base-image digest drift check~~' SECURITY.md`
    - Shipped-in-Phase-22 annotation: `grep -q 'Shipped in Phase 22' SECURITY.md`
    - Standing caveat preserved (PATTERNS.md line 391 — "Until those land" closing paragraph unchanged):
      `grep -q 'Until those land' SECURITY.md`
    - No third-image mention (D-02 invariant — only the two locked images):
      `! grep -qiE 'three (base )?images?|third (base )?image' SECURITY.md`
    - Other v1.5 gap bullets preserved (NOT struck through here — they close in Phases 23-25):
      `grep -q 'cosign' SECURITY.md` (cosign reference present somewhere; do NOT strike it)
    - Markdown still parses (basic sanity — no broken fences or unclosed code blocks introduced):
      `python3 -c "import sys; c=open('SECURITY.md').read(); fences=c.count('\`\`\`'); assert fences % 2 == 0, f'unbalanced fences: {fences}'; print('PASS')"`
  </acceptance_criteria>
  <done>
    `SECURITY.md` has the new `### Base-image digests (v1.6 onward)` subsection inserted into `## Supply-chain status` with all the locked markdown links, bold-lede paragraphs, and the `**Do not auto-merge digest bumps**` directive. Two existing bullets are now `~~strikethrough~~` with the standard "Closed in v1.6" / "Shipped in Phase 22" cross-references. The section's lead paragraph and closing "Until those land" caveat are preserved verbatim. All 15 acceptance assertions pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add `## Bumping base-image digests` section to `CONTRIBUTING.md`</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - CONTRIBUTING.md (full file — focus on the existing `## Tagging releases` section; insertion point is immediately after it)
    - .planning/phases/22-base-image-digest-drift-detection/22-RESEARCH.md §7.2 lines 757-809 (locked CONTRIBUTING.md copy — drop in verbatim under the new heading)
    - .planning/phases/22-base-image-digest-drift-detection/22-PATTERNS.md §"CONTRIBUTING.md MODIFY" lines 397-440 (contributor-facing PR-etiquette voice; imperative-gerund header; bold sub-callouts; fenced bash blocks for exact commands)
  </read_first>
  <action>
    Insert a new `## Bumping base-image digests` section in `CONTRIBUTING.md` immediately AFTER the existing `## Tagging releases` section (per RESEARCH.md §7.2 line 758 and PATTERNS.md §"CONTRIBUTING.md MODIFY" line 399). Content is locked verbatim in RESEARCH.md §7.2 lines 762-809.

    The section structure (per PATTERNS.md §"contributor-facing PR-etiquette voice" lines 432-438):
    - `##` header is imperative-gerund — `Bumping base-image digests`.
    - Opening paragraph stating the rule (which two base images are digest-pinned, where the manifest lives, who consumes it via the composite action) with markdown links `[\`docker/digests.txt\`](docker/digests.txt)` and `[\`.github/actions/read-base-digests/\`](.github/actions/read-base-digests/)`.
    - `**When you'd bump:**` bold-lede paragraph naming the two triggers: a `[digest-drift]` issue from the daily workflow, or proactive refresh.
    - `**How to bump (per PR):**` bold-lede numbered list with five steps:
      1. Resolve the new upstream digest on a clean machine — fenced bash block with `docker buildx imagetools inspect debian:bookworm-slim --format '{{.Manifest.Digest}}'`.
      2. Update the matching line in `docker/digests.txt` to `<image>:<tag>@sha256:<HEX>` — explicitly "only that one line" and "one image per PR" policy (RESEARCH.md §7.2 lines 785-787).
      3. Open a PR. PR body MUST link the originating `[digest-drift]` issue (if any) and classify the change as docs/metadata or substantive per the triage guidance in the issue body (cross-ref `[.planning/research/PITFALLS.md](.planning/research/PITFALLS.md) §8`).
      4. Wait for CODEOWNERS approval. **Do not auto-merge** — link to `[SECURITY.md §Supply-chain status](SECURITY.md#supply-chain-status)`.
      5. Once merged, close the originating `[digest-drift]` issue via `Closes #<N>` in the PR.
    - `**Why not auto-merge?**` bold-lede paragraph citing xz utils (2024) and event-stream (2018) as the threat-model anchors.
    - `**What if the regex check fails?**` bold-lede paragraph explaining the composite action's regex contract (`^[a-zA-Z0-9._/-]+:[a-zA-Z0-9._-]+@sha256:[a-f0-9]{64}$`) and how a malformed line produces an explicit error referencing this section.

    Match the contributor-facing PR-etiquette voice per PATTERNS.md §"contributor-facing PR-etiquette voice" lines 432-438: second-person imperative ("**Before tagging:** add..."), fenced bash blocks for exact commands, markdown links with relative paths, bolded ledes.

    Implementing D-05 (contributor-facing prose half of the gate).

    DO NOT touch the existing `## Tagging releases` section — only insert after it.
    DO NOT add a "v1" semver note — this section is not about release versioning.
    DO NOT recommend `gh pr merge --auto` or any auto-merge command — that defeats the entire policy this section establishes.
  </action>
  <verify>
    <automated>test -f CONTRIBUTING.md &amp;&amp; grep -q '^## Bumping base-image digests$' CONTRIBUTING.md &amp;&amp; grep -q 'docker/digests.txt' CONTRIBUTING.md &amp;&amp; grep -q 'docker buildx imagetools inspect' CONTRIBUTING.md &amp;&amp; grep -q 'Do not auto-merge' CONTRIBUTING.md &amp;&amp; grep -q 'SECURITY.md#supply-chain-status' CONTRIBUTING.md &amp;&amp; grep -q 'CODEOWNERS' CONTRIBUTING.md &amp;&amp; grep -q 'one image per PR\|one image, per PR\|ONE image' CONTRIBUTING.md &amp;&amp; grep -q '^## Tagging releases$' CONTRIBUTING.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f CONTRIBUTING.md`
    - New section heading present: `grep -q '^## Bumping base-image digests$' CONTRIBUTING.md`
    - Pre-existing `## Tagging releases` section still present (insertion was additive): `grep -q '^## Tagging releases$' CONTRIBUTING.md`
    - Insertion order — new section appears AFTER `## Tagging releases`:
      ```bash
      python3 -c "
      with open('CONTRIBUTING.md') as f: lines = f.readlines()
      tag_idx = next(i for i,l in enumerate(lines) if l.strip() == '## Tagging releases')
      bump_idx = next(i for i,l in enumerate(lines) if l.strip() == '## Bumping base-image digests')
      assert tag_idx < bump_idx, f'order wrong: tagging at {tag_idx}, bumping at {bump_idx}'
      print('PASS: Bumping comes after Tagging')
      "
      ```
    - Markdown link to canonical manifest: `grep -q 'docker/digests.txt' CONTRIBUTING.md`
    - Markdown link to composite action: `grep -q 'read-base-digests' CONTRIBUTING.md`
    - Fenced bash block with the resolve command: `grep -q "docker buildx imagetools inspect debian:bookworm-slim" CONTRIBUTING.md`
    - No-auto-merge directive: `grep -q 'Do not auto-merge' CONTRIBUTING.md`
    - Link to SECURITY.md §Supply-chain status: `grep -q 'SECURITY.md#supply-chain-status' CONTRIBUTING.md`
    - CODEOWNERS reference (the gating mechanism): `grep -q 'CODEOWNERS' CONTRIBUTING.md`
    - One-image-per-PR policy: `grep -qE 'one image per PR|ONE image|only that one line' CONTRIBUTING.md`
    - Cross-reference to PITFALLS.md §8 (retag-vs-substantive triage): `grep -q 'PITFALLS.md' CONTRIBUTING.md`
    - Threat-model anchors named (xz utils OR event-stream): `grep -qE 'xz utils|event-stream' CONTRIBUTING.md`
    - Regex contract documented in the "What if the regex check fails?" callout: `grep -q '\[a-f0-9\]{64}' CONTRIBUTING.md`
    - Markdown still parses (no broken fences):
      `python3 -c "import sys; c=open('CONTRIBUTING.md').read(); fences=c.count('\`\`\`'); assert fences % 2 == 0, f'unbalanced fences: {fences}'; print('PASS')"`
    - No `gh pr merge --auto` recommendation present:
      `! grep -q 'pr merge --auto' CONTRIBUTING.md`
  </acceptance_criteria>
  <done>
    `CONTRIBUTING.md` has a new `## Bumping base-image digests` section inserted immediately after `## Tagging releases`. The section names the two pinned images, walks contributors through the 5-step PR workflow with fenced bash blocks, asserts the one-image-per-PR policy, mandates "Do not auto-merge" with cross-reference to SECURITY.md, anchors the rationale in named supply-chain incidents (xz utils / event-stream), and explains the regex-check-failure recovery path. All 15+ assertions pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External contributor → SECURITY.md / CONTRIBUTING.md | Untrusted PR author proposes documentation edits. These files are NOT in CODEOWNERS — any maintainer-approved PR can change the prose. Residual: a malicious PR could weaken the prose policy. |
| Operator reading SECURITY.md → decision to pull a release | The published policy is the operator's contract. Misstating it would erode the audit-trail surface that the v1.5 audit charter built up. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-18 | Tampering | Malicious PR weakens the SECURITY.md no-auto-merge directive or removes the strikethrough closure | accept | SECURITY.md/CONTRIBUTING.md are NOT in CODEOWNERS in Phase 22 (D-05 scoped CODEOWNERS narrowly to manifest + parser action). Mitigated indirectly by branch protection on `main` requiring maintainer review (Plan 06 UAT). Carry-forward: extend CODEOWNERS to cover docs/* in a future phase if contributor volume grows. |
| T-22-19 | Repudiation | Maintainer forgets the documented policy and accepts an auto-merged digest PR | mitigate | The prose explicitly names the threat ("xz utils, 2024; event-stream, 2018") and the structural enforcement (CODEOWNERS + branch protection). Plan 06 UAT verifies the documented commands work. (Pitfall 11 mitigation surface.) |
| T-22-20 | Information Disclosure | Documentation reveals a non-public mitigation or maintainer detail | accept-zero-risk | All content in SECURITY.md and CONTRIBUTING.md updates is locked verbatim in RESEARCH.md §7.1/§7.2 and was researched against public information. No secrets. |
| T-22-SC | Tampering | npm/pip/cargo installs | n/a | Pure documentation edits. No package installs. |

</threat_model>

<verification>
After both tasks complete:

```bash
# 1. SECURITY.md updates land.
grep -c '### Base-image digests (v1.6 onward)' SECURITY.md    # → 1
grep -c '~~Base image digest pins are manual.~~' SECURITY.md   # → 1
grep -c '~~Automated base-image digest drift check~~' SECURITY.md    # → 1
grep -q 'Do not auto-merge digest bumps' SECURITY.md

# 2. CONTRIBUTING.md updates land.
grep -c '^## Bumping base-image digests$' CONTRIBUTING.md    # → 1
grep -q '^## Tagging releases$' CONTRIBUTING.md
grep -q 'docker buildx imagetools inspect' CONTRIBUTING.md

# 3. Markdown integrity — fences balanced in both files.
for f in SECURITY.md CONTRIBUTING.md; do
  python3 -c "import sys; c=open('$f').read(); fences=c.count('\`\`\`'); assert fences % 2 == 0, f'unbalanced fences in $f: {fences}'; print('$f OK')"
done

# 4. Cross-reference integrity — every link in the new content points at a path that
#    exists (Plans 01-04 created docker/digests.txt, .github/CODEOWNERS,
#    .github/actions/read-base-digests/action.yml, .github/workflows/digest-drift-check.yml):
for p in docker/digests.txt .github/CODEOWNERS .github/actions/read-base-digests/action.yml .github/workflows/digest-drift-check.yml; do
  test -e "$p" || echo "WARN: $p missing — earlier-wave plan did not complete"
done
```
</verification>

<success_criteria>
- `SECURITY.md` has the new `### Base-image digests (v1.6 onward)` subsection with all locked content (manifest link, CODEOWNERS link, drift workflow link, no-auto-merge directive, idempotency guarantee, workflow_dispatch rehearsal mention).
- `SECURITY.md` strikes through the v1.5 manual-pin gap with "Closed in v1.6" cross-reference.
- `SECURITY.md` strikes through the v1.6-plan drift-check bullet with "Shipped in Phase 22" cross-reference.
- `SECURITY.md` preserves the lead paragraph and "Until those land" closing caveat.
- `CONTRIBUTING.md` has the new `## Bumping base-image digests` section AFTER `## Tagging releases`, with fenced bash blocks, the 5-step PR workflow, the "Do not auto-merge" mandate, and the xz utils / event-stream anchors.
- Maps to ROADMAP Phase 22 Success Criterion #1 (policy documented in SECURITY.md + CONTRIBUTING.md per Pitfall 11).
</success_criteria>

<output>
Create `.planning/phases/22-base-image-digest-drift-detection/22-05-SUMMARY.md` when done. Include the exact SECURITY.md anchor (`#base-image-digests-v16-onward`) so Plan 06 UAT can verify the cross-references render correctly on github.com.
</output>
