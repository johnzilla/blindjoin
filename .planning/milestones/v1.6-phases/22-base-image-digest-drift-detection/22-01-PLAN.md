---
phase: 22-base-image-digest-drift-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/digests.txt
  - .github/CODEOWNERS
autonomous: true
requirements:
  - DRIFT-01
requirements_addressed:
  - DRIFT-01

must_haves:
  truths:
    - "`docker/digests.txt` exists at repo root and contains exactly 2 well-formed digest lines for `debian:bookworm-slim` and `lukemathwalker/cargo-chef:latest-rust-1` (ROADMAP SC#1 part 1)."
    - "`.github/CODEOWNERS` exists at repo root and maps both `docker/digests.txt` and `.github/actions/read-base-digests/**` to `@johnzilla` (ROADMAP SC#1 part 2 — the structural mechanism behind the no-auto-merge guarantee)."
  artifacts:
    - path: "docker/digests.txt"
      provides: "Canonical base-image digest manifest (one image:tag@sha256:HEX per line)"
      contains: "debian:bookworm-slim@sha256:"
    - path: "docker/digests.txt"
      provides: "Second canonical entry"
      contains: "lukemathwalker/cargo-chef:latest-rust-1@sha256:"
    - path: ".github/CODEOWNERS"
      provides: "Ownership map for digest manifest + parser action"
      contains: "@johnzilla"
  key_links:
    - from: ".github/CODEOWNERS"
      to: "docker/digests.txt"
      via: "path glob → owner handle"
      pattern: "docker/digests\\.txt\\s+@johnzilla"
    - from: ".github/CODEOWNERS"
      to: ".github/actions/read-base-digests/**"
      via: "path glob → owner handle"
      pattern: "\\.github/actions/read-base-digests/\\*\\*\\s+@johnzilla"
---

<objective>
Create the canonical base-image digest manifest (`docker/digests.txt`) and the CODEOWNERS file that gates PR review on it.

Purpose: This plan delivers the foundational source-of-truth artifact (DRIFT-01). Every other Phase 22 artifact reads or governs this file. Without it, the composite action in Plan 02 has nothing to parse; without CODEOWNERS, the "no auto-merge" guarantee documented in D-05 has no structural enforcement.
Output: Two new committed files: `docker/digests.txt` (the manifest) and `.github/CODEOWNERS` (the governance gate).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-base-image-digest-drift-detection/22-CONTEXT.md
@.planning/phases/22-base-image-digest-drift-detection/22-RESEARCH.md
@.planning/phases/22-base-image-digest-drift-detection/22-PATTERNS.md
@docker/Dockerfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create canonical digest manifest `docker/digests.txt`</name>
  <files>docker/digests.txt</files>
  <read_first>
    - .planning/phases/22-base-image-digest-drift-detection/22-RESEARCH.md §3 lines 286-299 (locked file header + data-line shape)
    - .planning/phases/22-base-image-digest-drift-detection/22-PATTERNS.md §"docker/digests.txt (NEW — canonical manifest)" lines 444-477 (header style mirrors Dockerfile lines 1-33)
    - docker/Dockerfile lines 1-33 (analog: top-of-file ARG comment block; same audit-trail header style)
    - .planning/phases/22-base-image-digest-drift-detection/22-CONTEXT.md §decisions (D-02 names the two images, D-03 names the regex contract)
  </read_first>
  <action>
    Create `docker/digests.txt` at the repo root with the header + data structure locked verbatim in RESEARCH.md §3 lines 288-297. Per D-01/D-02, the file MUST contain exactly two data lines: one for `debian:bookworm-slim` and one for `lukemathwalker/cargo-chef:latest-rust-1`. The header is a `#`-commented prose block (RESEARCH.md §3 lines 289-294) that names: (a) the file's role as canonical manifest, (b) the human-review-only bump policy (cross-ref SECURITY.md §Supply-chain status), (c) the parser-contract reference (`.github/actions/read-base-digests/action.yml`).

    Each data line MUST match `^[a-zA-Z0-9._/-]+:[a-zA-Z0-9._-]+@sha256:[a-f0-9]{64}$` (per D-03 / RESEARCH.md §3 regex enforcement table). The actual 64-hex digest values MUST be resolved at execution time on a clean runner via `docker buildx imagetools inspect <image>:<tag> --format '{{.Manifest.Digest}}'` (RESEARCH.md §3 lines 298-299 explicitly defer the digest values to plan-execution time because hardcoding them in research is brittle — the maintainer fills them in on a clean runner).

    Execution recipe (run on a clean ubuntu-24.04 runner or local Docker daemon):
    - `DEBIAN_DIGEST=$(docker buildx imagetools inspect debian:bookworm-slim --format '{{.Manifest.Digest}}')` → produces `sha256:HEX`
    - `CARGO_CHEF_DIGEST=$(docker buildx imagetools inspect lukemathwalker/cargo-chef:latest-rust-1 --format '{{.Manifest.Digest}}')` → produces `sha256:HEX`
    - Compose the two data lines as `debian:bookworm-slim@${DEBIAN_DIGEST}` and `lukemathwalker/cargo-chef:latest-rust-1@${CARGO_CHEF_DIGEST}`.

    Implementing D-01 (manifest is single source of truth for v1.6+ release pipeline) and D-02 (named per-image entries, not a generic JSON list).

    DO NOT add a third image line — D-02 makes additional base images intentional friction.
    DO NOT inline-comment the data lines — RESEARCH.md §3 lines 242-244 confirm the parser strips comments + blanks before validation; inline comments would have to follow `#` line discipline only.
  </action>
  <verify>
    <automated>test -f docker/digests.txt &amp;&amp; grep -vE '^[[:space:]]*(#|$)' docker/digests.txt | grep -cE '^[a-zA-Z0-9._/-]+:[a-zA-Z0-9._-]+@sha256:[a-f0-9]{64}$' | grep -q '^2$' &amp;&amp; grep -q '^debian:bookworm-slim@sha256:' docker/digests.txt &amp;&amp; grep -q '^lukemathwalker/cargo-chef:latest-rust-1@sha256:' docker/digests.txt &amp;&amp; head -1 docker/digests.txt | grep -q '^#'</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f docker/digests.txt` exit 0
    - File has comment header (first line is a `#` comment): `head -1 docker/digests.txt | grep -q '^#'`
    - Exactly 2 non-comment, non-blank data lines (D-03 line-count gate): `grep -vE '^[[:space:]]*(#|$)' docker/digests.txt | grep -c '^' | grep -q '^2$'`
    - Both lines match the locked regex shape (D-03 line-shape gate): `grep -vE '^[[:space:]]*(#|$)' docker/digests.txt | grep -cE '^[a-zA-Z0-9._/-]+:[a-zA-Z0-9._-]+@sha256:[a-f0-9]{64}$' | grep -q '^2$'`
    - Required image prefixes present (D-03 required-images gate):
      - `grep -q '^debian:bookworm-slim@sha256:' docker/digests.txt`
      - `grep -q '^lukemathwalker/cargo-chef:latest-rust-1@sha256:' docker/digests.txt`
    - Header references SECURITY.md (audit trail): `grep -q 'SECURITY.md' docker/digests.txt`
    - Header references the parser action (audit trail): `grep -q 'read-base-digests' docker/digests.txt`
  </acceptance_criteria>
  <done>
    Manifest exists with exactly two well-formed digest lines (one each for `debian:bookworm-slim` and `lukemathwalker/cargo-chef:latest-rust-1`), header references SECURITY.md and the parser action, and all six grep-based acceptance assertions pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create `.github/CODEOWNERS` mapping manifest + parser action to @johnzilla</name>
  <files>.github/CODEOWNERS</files>
  <read_first>
    - .planning/phases/22-base-image-digest-drift-detection/22-RESEARCH.md §6 lines 678-696 (locked file content; rationale for both paths; owner-handle verification)
    - .planning/phases/22-base-image-digest-drift-detection/22-PATTERNS.md §".github/CODEOWNERS (NEW — 2-line file)" lines 481-501 (no in-repo analog; format is GitHub-product-locked)
    - .planning/phases/22-base-image-digest-drift-detection/22-CONTEXT.md §decisions D-05 lines 30 (governance enforcement decision) + §specifics line 102 (CODEOWNERS scope reasoning)
  </read_first>
  <action>
    Create `.github/CODEOWNERS` at the repo root with content locked verbatim in RESEARCH.md §6 lines 680-689. The file has a 6-line `#`-commented prose header (audit-trail style mirroring `docker/digests.txt` and `docker/Dockerfile`) followed by exactly two path-glob lines:

    - `docker/digests.txt                         @johnzilla`
    - `.github/actions/read-base-digests/**       @johnzilla`

    Both paths are load-bearing per D-05 + CONTEXT.md §specifics line 102: changing the parser logic in `.github/actions/read-base-digests/action.yml` is functionally the same level of supply-chain risk as changing a digest. The owner handle `@johnzilla` is verified per RESEARCH.md §6 line 693 (corroborated by `.planning/research/SUMMARY.md` cosign-identity references + SECURITY.md email-handle correspondence).

    The header MUST cite `.planning/research/PITFALLS.md §11` and `SECURITY.md §Supply-chain status` (RESEARCH.md §6 lines 681-686 — the audit-trail header explains *why* CODEOWNERS exists, not just what it maps).

    Implementing D-05 (prose + CODEOWNERS, no extra workflow gate).

    DO NOT add a wildcard `*` fallback line — Phase 22 scopes CODEOWNERS narrowly to the digest manifest + parser. Broad ownership rules are out of scope.
    DO NOT include the parser action's path as a single file (`action.yml`) — D-05 + RESEARCH.md §6 line 688 use the `**` glob to cover any future file the action directory may grow (composite shell helpers, README, etc.).

    Note on branch protection: this file is INERT until the maintainer enables "Require review from Code Owners" on `main`'s branch-protection rule. That toggle is a separate HUMAN-UAT task scheduled in Plan 06; this plan creates only the file.
  </action>
  <verify>
    <automated>test -f .github/CODEOWNERS &amp;&amp; grep -qE '^docker/digests\.txt[[:space:]]+@johnzilla' .github/CODEOWNERS &amp;&amp; grep -qE '^\.github/actions/read-base-digests/\*\*[[:space:]]+@johnzilla' .github/CODEOWNERS &amp;&amp; grep -q 'PITFALLS.md' .github/CODEOWNERS &amp;&amp; grep -q 'SECURITY.md' .github/CODEOWNERS &amp;&amp; head -1 .github/CODEOWNERS | grep -q '^#'</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f .github/CODEOWNERS` exit 0
    - File has prose header (first line is a `#` comment): `head -1 .github/CODEOWNERS | grep -q '^#'`
    - Manifest path mapped to maintainer: `grep -qE '^docker/digests\.txt[[:space:]]+@johnzilla' .github/CODEOWNERS`
    - Parser-action glob mapped to maintainer: `grep -qE '^\.github/actions/read-base-digests/\*\*[[:space:]]+@johnzilla' .github/CODEOWNERS`
    - Header cites the threat-model document (audit trail): `grep -q 'PITFALLS.md' .github/CODEOWNERS`
    - Header cites the operator-facing policy doc (audit trail): `grep -q 'SECURITY.md' .github/CODEOWNERS`
    - Exactly 2 non-comment, non-blank rule lines: `grep -vE '^[[:space:]]*(#|$)' .github/CODEOWNERS | grep -c '^' | grep -q '^2$'`
  </acceptance_criteria>
  <done>
    `.github/CODEOWNERS` exists, contains exactly two rule lines mapping `docker/digests.txt` and `.github/actions/read-base-digests/**` to `@johnzilla`, has the locked prose header citing PITFALLS.md and SECURITY.md, and all seven assertions above pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External contributor → repo `main` branch | Untrusted PR author proposes a digest manifest bump or a CODEOWNERS edit. CODEOWNERS + branch protection is the structural gate. |
| GitHub Actions runner → Docker Hub registry | Used in Plan 04; called out here because this plan's manifest is the canonical input to that resolution path. Not exercised in Plan 01. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | `docker/digests.txt` (any contributor PR modifying the manifest) | mitigate | CODEOWNERS + branch protection requires maintainer review. Branch protection toggle is HUMAN-UAT in Plan 06. (RESEARCH.md §9 row 1: "Direct push to `main` bypassing CODEOWNERS") |
| T-22-02 | Tampering | `.github/actions/read-base-digests/**` (PR that weakens parser regex alongside a digest bump) | mitigate | CODEOWNERS path-glob covers the entire action directory in addition to the manifest file. Both paths in this plan's CODEOWNERS file. (RESEARCH.md §9 row 2: "Action source modified in the same PR as a digest bump") |
| T-22-03 | Elevation of Privilege | Admin override of branch protection ("Allow administrators to bypass") | accept-with-action | Strictly outside this repo's file-tree. Plan 06 HUMAN-UAT verifies "Allow administrators to bypass required reviews" is UNCHECKED. SECURITY.md §Supply-chain status documents the required setting (per Plan 05). (RESEARCH.md §9 row 1 residual) |
| T-22-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan. No supply-chain audit table required. |

</threat_model>

<verification>
After both tasks complete, run end-to-end manifest+CODEOWNERS sanity:

```bash
# 1. Manifest shape (D-03 line-count + line-shape gate, pre-empts composite-action validation).
test -f docker/digests.txt
grep -vE '^[[:space:]]*(#|$)' docker/digests.txt | grep -c '^'    # → 2
grep -vE '^[[:space:]]*(#|$)' docker/digests.txt | grep -cE '^[a-zA-Z0-9._/-]+:[a-zA-Z0-9._-]+@sha256:[a-f0-9]{64}$'    # → 2
grep -c '^debian:bookworm-slim@sha256:' docker/digests.txt        # → 1
grep -c '^lukemathwalker/cargo-chef:latest-rust-1@sha256:' docker/digests.txt    # → 1

# 2. CODEOWNERS shape.
test -f .github/CODEOWNERS
grep -cE '^[^#[:space:]]' .github/CODEOWNERS    # → 2 (non-comment rule lines)
grep -qE '^docker/digests\.txt[[:space:]]+@johnzilla' .github/CODEOWNERS
grep -qE '^\.github/actions/read-base-digests/\*\*[[:space:]]+@johnzilla' .github/CODEOWNERS

# 3. GitHub recognizes the CODEOWNERS file (run AFTER pushing to a branch; deferred to Plan 06 UAT in full):
# gh api "/repos/${OWNER}/blindjoin/contents/.github/CODEOWNERS" --jq '.path'    # → ".github/CODEOWNERS"
```
</verification>

<success_criteria>
- `docker/digests.txt` exists with exactly 2 well-formed `image:tag@sha256:HEX` data lines for the two required images.
- `.github/CODEOWNERS` exists mapping both `docker/digests.txt` and `.github/actions/read-base-digests/**` to `@johnzilla`.
- Both files have prose headers citing the load-bearing policy documents (SECURITY.md, PITFALLS.md, parser-action path).
- All grep-based acceptance assertions in both tasks pass.
- Maps to ROADMAP Phase 22 Success Criterion #1 (canonical manifest exists + governance prevents auto-merge); structural enforcement (branch protection) lands in Plan 06.
</success_criteria>

<output>
Create `.planning/phases/22-base-image-digest-drift-detection/22-01-SUMMARY.md` when done. Include the resolved 64-hex digest values in the SUMMARY for downstream agent reference (Plan 04 drift-check rehearsal needs them; Plan 06 fresh-machine UAT references them).
</output>
