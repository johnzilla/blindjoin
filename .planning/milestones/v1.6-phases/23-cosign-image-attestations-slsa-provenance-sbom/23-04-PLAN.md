---
phase: 23-cosign-image-attestations-slsa-provenance-sbom
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - SECURITY.md
autonomous: true
requirements:
  - ATTEST-01
  - ATTEST-02
  - ATTEST-03
  - ATTEST-04
requirements_addressed:
  - ATTEST-01
  - ATTEST-02
  - ATTEST-03
  - ATTEST-04

must_haves:
  truths:
    - "`SECURITY.md` § Supply-chain status has a new `### Image signatures + attestations (v1.6 onward)` subsection inserted BETWEEN the existing `### Known gaps at v1.5` and the existing `### Base-image digests (v1.6 onward)` subsections (chronological reading order; D-05 + PATTERNS §8)."
    - "The new subsection contains the four operator-facing verify recipes in one fenced bash block: (1) `cosign verify --certificate-identity-regexp 'https://github.com/johnzilla/blindjoin/\\.github/workflows/docker\\.yml@refs/tags/v.*' --certificate-oidc-issuer 'https://token.actions.githubusercontent.com' <image>` (ATTEST-01); (2) `gh attestation verify oci://... --predicate-type https://slsa.dev/provenance/v1` (ATTEST-02); (3) `gh attestation verify oci://... --predicate-type https://spdx.dev/Document` (ATTEST-03); (4) `cosign save --dir ./<dir> <image>` + `cosign verify --local-image ./<dir>` (ATTEST-04 — corrected per RESEARCH §3.4 from CONTEXT D-07's nonexistent `cosign download signature --bundle` CLI)."
    - "Two `> Note:` callout blocks appear below the recipes: Pitfall 10 (GHCR UI 'Unverified' badge unrelated to cosign verification) and Pitfall 13 (cosign 3.0 CLI flag drift) — the latter pins the operator-side cosign version range `>= 2.6.3, < 3.0.0` (D-08 recommended shape + RESEARCH §2.3 sub-question 1 recommended `v2.6.3` lower bound to match CI side)."
    - "The v1.5 'Docker images on `ghcr.io` are unsigned' bullet at the existing `### Known gaps at v1.5` list is annotated with `~~strikethrough~~` + `**Closed in v1.6 Phase 23**` cross-link to the new subsection (PATTERNS §8 + Phase 22 strikethrough analog at SECURITY.md:120)."
    - "The locked --certificate-identity-regexp matches the OIDC subject claim format GitHub emits for matrix-job tag-push runs (RESEARCH §7.6 — the regex correctly handles all three matrix legs since the workflow file `.github/workflows/docker.yml` and tag ref `refs/tags/v.*` are identical across legs; verified via ianlewis.org + qcecuring.com + augmentedmind.de cross-references)."
    - "The existing `### Base-image digests (v1.6 onward)` Phase 22 subsection at SECURITY.md:122-159 stays UNTOUCHED (D-05 + PATTERNS §8 — the rewrite is of the OVERVIEW prose + adds a new subsection ABOVE Phase 22's; the Phase 22 subsection is a sibling, not a target)."
    - "The forward-strikethrough on the tarball-signature bullet is NOT added in this phase — RESEARCH §8 Q3 recommends Phase 24 owns its own anchor + strikethrough; cross-phase forward-references add stale-anchor risk."
  artifacts:
    - path: "SECURITY.md"
      provides: "New ### Image signatures + attestations (v1.6 onward) subsection (D-05)"
      contains: "Image signatures + attestations (v1.6 onward)"
    - path: "SECURITY.md"
      provides: "ATTEST-01 cosign verify recipe with locked --certificate-identity-regexp (Pitfall 1)"
      contains: "cosign verify"
    - path: "SECURITY.md"
      provides: "Locked identity regex matching docker.yml workflow + refs/tags/v.* (Pitfall 1)"
      contains: "https://github.com/johnzilla/blindjoin/\\\\.github/workflows/docker\\\\.yml@refs/tags/v.*"
    - path: "SECURITY.md"
      provides: "ATTEST-02 SLSA provenance retrieval recipe (gh attestation verify oci://...)"
      contains: "https://slsa.dev/provenance/v1"
    - path: "SECURITY.md"
      provides: "ATTEST-03 SPDX SBOM retrieval recipe"
      contains: "https://spdx.dev/Document"
    - path: "SECURITY.md"
      provides: "ATTEST-04 offline-verifiable bundle recipe (cosign save — RESEARCH §3.4 correction)"
      contains: "cosign save --dir"
    - path: "SECURITY.md"
      provides: "Pitfall 10 callout (GHCR UI badge unrelated to cosign verification)"
      contains: "GHCR UI"
    - path: "SECURITY.md"
      provides: "Pitfall 13 callout + operator-side cosign version pin (D-08)"
      contains: ">= 2.6.3, < 3.0.0"
    - path: "SECURITY.md"
      provides: "Strikethrough closure of v1.5 Docker-unsigned gap"
      contains: "~~Docker images on `ghcr.io` are unsigned.~~"
  key_links:
    - from: "SECURITY.md ### Image signatures + attestations (v1.6 onward)"
      to: ".github/workflows/docker.yml"
      via: "markdown relative link + identity-regex naming the workflow file"
      pattern: "docker\\\\.yml"
    - from: "SECURITY.md ### Known gaps at v1.5 (strikethrough Docker bullet)"
      to: "SECURITY.md #image-signatures--attestations-v16-onward"
      via: "markdown anchor cross-link"
      pattern: "#image-signatures--attestations-v16-onward"
    - from: "SECURITY.md ATTEST-04 recipe"
      to: "cosign 2.x save subcommand"
      via: "cosign save --dir ./<dir> ghcr.io/.../<image>:<tag>"
      pattern: "cosign save --dir"
---

<objective>
Rewrite `SECURITY.md` § Supply-chain status to add a new `### Image signatures + attestations (v1.6 onward)` subsection that documents the operator-facing cosign verify + gh attestation verify + cosign save recipes for ATTEST-01/02/03/04. Strike through the v1.5 "Docker images on ghcr.io are unsigned" known-gap bullet with a cross-link to the new subsection.

Purpose: D-05 mandates the SECURITY.md rewrite as the operator-facing surface of the supply-chain hardening this phase ships. Without it, an operator pulling `ghcr.io/.../blindjoin-coordinator:1.6.0` has no documented path to verify the signature and attestations — even though Plans 23-01 and 23-02 produce them. The recipes block is the LOAD-BEARING piece of ROADMAP SC#5 ("SECURITY.md `## Supply-chain status` is updated to remove the 'Docker images on ghcr.io are unsigned' gap and replace it with the canonical `cosign verify` command"). RESEARCH §3.4 corrects CONTEXT D-07's nonexistent `cosign download signature --bundle` CLI to `cosign save --dir` — this is the load-bearing ATTEST-04 recipe. RESEARCH §4 supplies the full locked recipe block verbatim. This plan runs in PARALLEL with Plans 23-01/02/03 in Wave 1 — different file target (SECURITY.md only), no functional dependency on the workflow edits (the recipes document what operators will be able to do once Plan 23-05 Stage 2 confirms the rc.0 tag works end-to-end).
Output: One modified file: `SECURITY.md` (~60 new lines: subsection + recipes block + 2 callouts; ~2 lines modified: the strikethrough on the v1.5 Docker bullet).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-CONTEXT.md
@.planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-RESEARCH.md
@.planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-PATTERNS.md
@.planning/research/PITFALLS.md
@SECURITY.md

<interfaces>
<!-- This plan modifies operator-facing documentation only. -->
<!-- No functional dependency on Plans 23-01/02/03 — runs in Wave 1 parallel. -->

<!-- The recipes documented here will be empirically validated in Plan 23-05 Stage 2 -->
<!-- (HUMAN-UAT against v1.6.0-rc.0 from a fresh ubuntu:24.04 container). If a recipe -->
<!-- fails Stage 2 UAT, the fix is a quick task amending this SECURITY.md section -->
<!-- BEFORE the production v1.6.0 tag (the Pitfall 12 "ship after rehearsal" gate). -->

<!-- Locked identity regex (CONTEXT D-05 + RESEARCH §4 + RESEARCH §7.6 verified for matrix legs): -->
<!--   'https://github.com/johnzilla/blindjoin/\.github/workflows/docker\.yml@refs/tags/v.*' -->
<!--   - johnzilla is the repo owner -->
<!--   - docker\.yml is the workflow file (literal backslash-period in the regex) -->
<!--   - refs/tags/v.* spans v1.6.0, v1.6.0-rc.0, v1.6.1, v1.7.0, ... by design -->
<!--   - RESEARCH §7.6 confirms the matrix leg (coordinator/client/liquidity-bot) does NOT -->
<!--     affect the OIDC subject claim — one regex matches all three images. -->

<!-- Locked recipes (RESEARCH §4 lines 381-450 — copy VERBATIM into SECURITY.md): -->
<!--   1. cosign verify ...                       (ATTEST-01) -->
<!--   2. gh attestation verify oci://... slsa    (ATTEST-02) — gh primary, cosign download attestation 404s -->
<!--   3. gh attestation verify oci://... spdx    (ATTEST-03) -->
<!--   4. cosign save --dir + cosign verify --local-image  (ATTEST-04 — CORRECTED from D-07) -->

<!-- Operator-side cosign version pin (D-08 + RESEARCH §2.3 sub-question 1 + RESEARCH §8 Q1): -->
<!--   `>= 2.6.3, < 3.0.0` — matches the CI side's `cosign-release: 'v2.6.3'` from Plan 23-01. -->

<!-- Existing SECURITY.md structural anchors to preserve (PATTERNS §8 + SECURITY.md:122-159): -->
<!--   - ## Supply-chain status (the parent H2 — keep) -->
<!--   - ### Known gaps at v1.5 (modify: strikethrough the Docker bullet) -->
<!--   - ### Base-image digests (v1.6 onward) (Phase 22 P0-1 — DO NOT TOUCH) -->
<!--   - ### v1.6 supply-chain plan (existing — keep; Plan 23-04 does not modify this) -->
<!-- NEW subsection inserted BETWEEN "Known gaps at v1.5" and "Base-image digests": -->
<!--   - ### Image signatures + attestations (v1.6 onward) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert `### Image signatures + attestations (v1.6 onward)` subsection + strikethrough v1.5 Docker bullet</name>
  <files>SECURITY.md</files>
  <read_first>
    - SECURITY.md (full file — focus on `## Supply-chain status` at line 95, `### Known gaps at v1.5` at line 102 with the 4 bullet list, the `### Base-image digests (v1.6 onward)` subsection at lines 122-159 which STAYS untouched, `### v1.6 supply-chain plan` at line 161)
    - .planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-RESEARCH.md §4 lines 377-462 (the LOCKED recipes block + callouts block — copy VERBATIM into the new subsection; the operator-facing prose + ` ```bash` fenced block + `> Note: ...` callouts)
    - .planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-RESEARCH.md §3.4 lines 344-374 (the ATTEST-04 `cosign save` recipe — the load-bearing CORRECTION to CONTEXT D-07's nonexistent `cosign download signature --bundle` CLI)
    - .planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-RESEARCH.md §2.4 lines 184-213 (the gh attestation verify oci://... primary path vs cosign download attestation 404 caveat — verified at actions/attest-build-provenance issue #162)
    - .planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-RESEARCH.md §2.3 lines 158-181 (cosign version pin trade-off: v2.6.3 lower bound matches CI side per Plan 23-01)
    - .planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-RESEARCH.md §7.6 lines 685-692 (verified: the matrix leg coordinator/client/liquidity-bot does NOT affect the OIDC subject claim; one regex works for all three images)
    - .planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-RESEARCH.md §8 Q3 lines 715-717 (do NOT add the forward-strikethrough for tarball-signature bullet — Phase 24 owns its anchor)
    - .planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-PATTERNS.md §8 lines 395-489 (placement: insert between Known gaps and Base-image digests; bold-lede claims; markdown link form; strikethrough analog from Phase 22 at SECURITY.md:120)
    - .planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-CONTEXT.md §decisions D-05 line 36 (full prose+recipes+callouts skeleton); D-07 line 51 (.bundle distribution — RESEARCH §3.4 correction applies); D-08 line 55 (operator-side cosign version pin shape — recommended range form)
    - .planning/research/PITFALLS.md §Pitfall 1 (--certificate-identity-regexp vs --certificate-identity — the locked regex must be wide enough to survive releases but narrow enough to bind workflow + tag namespace)
    - .planning/research/PITFALLS.md §Pitfall 10 (GHCR UI badge unrelated to cosign verification — operator should NOT rely on the GHCR web UI badge as cosign substitute)
    - .planning/research/PITFALLS.md §Pitfall 12 (fresh-machine UAT — the documented recipe must be runnable from zero; Plan 23-05 Stage 2 empirically validates)
    - .planning/research/PITFALLS.md §Pitfall 13 (cosign 3.0 CLI flag drift — pin a version range)
    - STATE.md §Recent Plan Decisions line 113 (Phase 22 Plan 22-05 lesson: when RESEARCH-locked prose wrapping collides with PLAN-locked literal-byte greps, the literal-byte form wins — applies to the bold marker `**Do not ...**` patterns in this plan)
  </read_first>
  <action>
    Make TWO coordinated edits to `SECURITY.md`:

    **Edit 1 — Insert the new `### Image signatures + attestations (v1.6 onward)` subsection BETWEEN the existing `### Known gaps at v1.5` bullet list (ends ~line 120 with the Phase 22 strikethrough closure) and the existing `### Base-image digests (v1.6 onward)` subsection (starts line 122).** Per D-05 + PATTERNS §8 + RESEARCH §4 lines 381-450, the new subsection has this structure (copy VERBATIM from RESEARCH §4 unless otherwise noted; the markdown source is the locked text):

      - H3 heading: `### Image signatures + attestations (v1.6 onward)` (single line; mirrors Phase 22's `### Base-image digests (v1.6 onward)` H3 anchor convention).
      - Opening paragraph: "Every `ghcr.io/<owner>/blindjoin-{coordinator,client,liquidity-bot}:X.Y.Z` image push from a `vX.Y.Z` tag is:" followed by a numbered list of three items (each starting with a bold-lede claim mirroring the Phase 22 bold-lede style):
        1. **Signed by cosign** via OIDC keyless flow (no maintainer key custody). Signature stored at `sha256-<HEX>.sig`; Fulcio cert + Rekor inclusion proof.
        2. **Attested with a SLSA v1.0 in-toto provenance bundle** (predicate type `https://slsa.dev/provenance/v1`); naming the workflow file + tag ref + source commit + runner image. OCI referrer of the image.
        3. **Attested with a SPDX SBOM** (predicate type `https://spdx.dev/Document`); generated by Syft against the full image filesystem. Sibling OCI referrer.
      - Prerequisite paragraph: "Verification requires **cosign 2.6.3 or compatible** and the **GitHub CLI (`gh`) 2.x or later**. The verify recipes below have been tested on a clean `ubuntu:24.04` container."
      - A fenced ` ```bash ` recipes block with FOUR recipes (verbatim from RESEARCH §4 lines 402-434):
        - Recipe 1: `cosign verify --certificate-identity-regexp 'https://github.com/johnzilla/blindjoin/\.github/workflows/docker\.yml@refs/tags/v.*' --certificate-oidc-issuer 'https://token.actions.githubusercontent.com' ghcr.io/<owner>/blindjoin-<image>:<tag>` + comment naming the substitution `<image> = coordinator | client | liquidity-bot` + expected output.
        - Recipe 2: `gh attestation verify oci://ghcr.io/<owner>/blindjoin-<image>:<tag> --repo <owner>/blindjoin --predicate-type https://slsa.dev/provenance/v1` + expected output (`Loaded N attestation(s) ... ✓ Verified provenance attestation.`).
        - Recipe 3: `gh attestation verify oci://ghcr.io/<owner>/blindjoin-<image>:<tag> --repo <owner>/blindjoin --predicate-type https://spdx.dev/Document` + expected output (same shape as Recipe 2, with SPDX SBOM payload).
        - Recipe 4 (ATTEST-04 — CORRECTED per RESEARCH §3.4): `cosign save --dir ./blindjoin-<image>-<tag> ghcr.io/<owner>/blindjoin-<image>:<tag>` + `cosign verify --local-image ./blindjoin-<image>-<tag> --certificate-identity-regexp '...' --certificate-oidc-issuer '...'` + comment: "After step 1's `cosign save` completes (one-time network), all subsequent `cosign verify --local-image` invocations against this directory verify offline. Recipes 1–3 require network access to Fulcio + Rekor + the OCI registry; recipe 4's verify step does not."
      - TWO `> Note:` callout blocks below the recipes (verbatim from RESEARCH §4 lines 436-449):
        - Pitfall 10 callout: `> **Note: GHCR UI "Unverified" badge** is unrelated to cosign verification (Pitfall 10). GHCR's web view does not consult Rekor by default. **The `cosign verify` CLI output is the source of truth for signature status.** ...`
        - Pitfall 13 + D-08 callout: `> **Note: cosign 3.0 CLI flag drift** (Pitfall 13). The recipes above have been tested with **cosign \`>= 2.6.3, < 3.0.0\`**. cosign 3.0 ... may change CLI flags; when blindjoin's pipeline upgrades to cosign 3.x, the project will publish an updated recipe ...`

    The recipes block uses the locked identity regex `'https://github.com/johnzilla/blindjoin/\.github/workflows/docker\.yml@refs/tags/v.*'` (Pitfall 1 narrow-but-survives-releases form). Per RESEARCH §7.6 the matrix leg does NOT affect the OIDC subject claim, so the regex correctly matches all three images (coordinator/client/liquidity-bot). The ATTEST-04 recipe uses `cosign save --dir` — NOT the nonexistent `cosign download signature --bundle` CONTEXT D-07 originally proposed (corrected at RESEARCH §3.4 — verified at github.com/sigstore/cosign/blob/main/doc/cosign_download_signature.md, no `--bundle` flag exists on that subcommand).

    Per RESEARCH §8 Q3, do NOT add a forward-strikethrough on the tarball-signature bullet at SECURITY.md:104-109 — Phase 24 owns its own anchor + strikethrough; cross-phase forward-references add stale-anchor risk if Phase 24 changes shape.

    **Edit 2 — Strikethrough the v1.5 Docker-images-unsigned bullet at SECURITY.md:110-114.** The current bullet is multi-line:

        - **Docker images on `ghcr.io` are unsigned.** No cosign attestation, no
          Notary v2 signature, no Sigstore witness. Pulling
          `ghcr.io/<owner>/blindjoin-coordinator:1.5.0` verifies the image
          digest matches what the registry advertises, but does not bind the
          image to the maintainer's identity.

    Replace it with the single-line strikethrough form (mirrors the Phase 22 strikethrough at SECURITY.md:120 + PATTERNS §8 lines 484-487):

        - **~~Docker images on `ghcr.io` are unsigned.~~** **Closed in v1.6 Phase 23** — see [Image signatures + attestations (v1.6 onward)](#image-signatures--attestations-v16-onward).

    The anchor `#image-signatures--attestations-v16-onward` is the auto-generated GitHub anchor for the new H3 heading `### Image signatures + attestations (v1.6 onward)` — note: GitHub anchor generation lowercases, replaces spaces with hyphens, drops punctuation EXCEPT `+` becomes nothing (so "signatures + attestations" becomes "signatures--attestations" — two hyphens from the dropped `+`). This matches PATTERNS §8 line 487 exactly. Per the STATE.md §Recent Plan Decisions line 113 lesson (Phase 22 Plan 22-05 single-line bold marker), the strikethrough line MUST be a single physical line in source so any `grep -q '~~Docker images.*ghcr\.io.*unsigned~~'` file-level audit gate matches across newlines.

    Implementing D-05 (SECURITY.md rewrite skeleton) + D-07 corrected to cosign save (RESEARCH §3.4) + D-08 (`>= 2.6.3, < 3.0.0` range) + ROADMAP SC#5 ("SECURITY.md is updated to remove the Docker-unsigned gap and replace it with the canonical cosign verify command").

    DO NOT touch the existing `### Base-image digests (v1.6 onward)` subsection at SECURITY.md:122-159 (Phase 22 P0-1 — D-05 + PATTERNS §8 lines 421-422 explicitly preserve it as a sibling).
    DO NOT add an `<a id="image-verification"></a>` invisible anchor — D-05 picked prose+recipes+callouts over per-artifact anchored subsections; the anchor is a v1.7 carry-forward per CONTEXT §deferred.
    DO NOT add a forward-strikethrough on the tarball-signature bullet at SECURITY.md:104-109 — Phase 24 owns that anchor (RESEARCH §8 Q3).
    DO NOT use the `cosign download signature --bundle` CLI in any recipe — that CLI shape does NOT exist (RESEARCH §3.4 + §7.5 verified at sigstore/cosign docs). Use `cosign save --dir` for ATTEST-04.
    DO NOT use `--certificate-identity` (without `-regexp`) — Pitfall 1 mandates regex form so the recipe survives across releases.
    DO NOT widen the regex beyond `'https://github.com/johnzilla/blindjoin/\.github/workflows/docker\.yml@refs/tags/v.*'` — wider (e.g., `'https://github.com/johnzilla/.*'`) is exploitable per Pitfall 1 (any workflow in any of johnzilla's repos could produce "valid" signatures).
    DO NOT pin a cosign version below `2.6.3` in the recipe text — RESEARCH §2.3 sub-question 1 + RESEARCH §8 Q1 recommend `v2.6.3` to match CI side (`cosign-release: 'v2.6.3'` in Plan 23-01) for operator-CI symmetry. Lower bounds would create test/doc drift if cosign 2.5.3 has a flag that 2.6.3 doesn't.
    DO NOT add `--insecure-ignore-tlog` to any documented recipe — Pitfall 3 mandates Rekor as the operator-facing transparency guarantee; documenting the opt-out invites operators to skip it casually.
    DO NOT split the bullet's bold marker across two lines (Phase 22 Plan 22-05 lesson — single-line literal so file-level greps work).
  </action>
  <verify>
    <automated>test -f SECURITY.md &amp;&amp; grep -q '^### Image signatures + attestations (v1.6 onward)$' SECURITY.md &amp;&amp; grep -q 'cosign verify' SECURITY.md &amp;&amp; grep -q 'gh attestation verify oci://' SECURITY.md &amp;&amp; grep -q 'https://slsa.dev/provenance/v1' SECURITY.md &amp;&amp; grep -q 'https://spdx.dev/Document' SECURITY.md &amp;&amp; grep -q 'cosign save --dir' SECURITY.md &amp;&amp; grep -q 'cosign verify --local-image' SECURITY.md &amp;&amp; grep -q 'certificate-identity-regexp' SECURITY.md &amp;&amp; grep -q 'docker\\\\.yml@refs/tags/v\.\*\|docker\\.yml@refs/tags/v\.\*' SECURITY.md &amp;&amp; grep -q 'token.actions.githubusercontent.com' SECURITY.md &amp;&amp; grep -q 'Pitfall 10\|GHCR UI' SECURITY.md &amp;&amp; grep -q 'Pitfall 13\|cosign 3.0' SECURITY.md &amp;&amp; grep -q '>= 2.6.3, < 3.0.0' SECURITY.md &amp;&amp; grep -q '~~Docker images on `ghcr.io` are unsigned.~~' SECURITY.md &amp;&amp; grep -q '#image-signatures--attestations-v16-onward' SECURITY.md &amp;&amp; grep -q '^### Base-image digests (v1.6 onward)$' SECURITY.md &amp;&amp; ! grep -q 'cosign download signature --bundle' SECURITY.md &amp;&amp; ! grep -q '\-\-insecure-ignore-tlog' SECURITY.md &amp;&amp; ! grep -q "certificate-identity '" SECURITY.md</automated>
  </verify>
  <acceptance_criteria>
    - File still exists and is well-formed markdown: `test -f SECURITY.md`
    - New H3 subsection heading present (auto-anchor `#image-signatures--attestations-v16-onward`): `grep -q '^### Image signatures + attestations (v1.6 onward)$' SECURITY.md`
    - Subsection contains the cosign verify recipe (ATTEST-01): `grep -q 'cosign verify' SECURITY.md`
    - Subsection contains the gh attestation verify recipes (ATTEST-02 + ATTEST-03): `grep -q 'gh attestation verify oci://' SECURITY.md`
    - SLSA predicate type present (ATTEST-02): `grep -q 'https://slsa.dev/provenance/v1' SECURITY.md`
    - SPDX predicate type present (ATTEST-03): `grep -q 'https://spdx.dev/Document' SECURITY.md`
    - cosign save recipe present (ATTEST-04 — corrected from D-07): `grep -q 'cosign save --dir' SECURITY.md && grep -q 'cosign verify --local-image' SECURITY.md`
    - Locked identity regex present (Pitfall 1 narrow form): `grep -q "certificate-identity-regexp 'https://github\.com/johnzilla/blindjoin/\\\\.github/workflows/docker\\\\.yml@refs/tags/v\.\*'" SECURITY.md || grep -F "certificate-identity-regexp 'https://github.com/johnzilla/blindjoin/\\.github/workflows/docker\\.yml@refs/tags/v.*'" SECURITY.md`
    - OIDC issuer URL present: `grep -q 'https://token.actions.githubusercontent.com' SECURITY.md`
    - Pitfall 10 callout present (GHCR UI badge): `grep -q 'GHCR UI' SECURITY.md && grep -q 'Pitfall 10' SECURITY.md`
    - Pitfall 13 callout + cosign version pin (D-08): `grep -q 'Pitfall 13' SECURITY.md && grep -q '>= 2.6.3, < 3.0.0' SECURITY.md`
    - v1.5 Docker-unsigned bullet strikethrough'd with cross-link to new subsection: `grep -q '~~Docker images on \`ghcr.io\` are unsigned.~~' SECURITY.md && grep -q '#image-signatures--attestations-v16-onward' SECURITY.md`
    - Strikethrough text is on a SINGLE line (Phase 22 Plan 22-05 lesson): `awk '/~~Docker images on .ghcr.io. are unsigned.~~/{print NR; getline next_line; if (next_line !~ /image-signatures/) print "OK: cross-link on same line"; else print "OK: cross-link on next line"}' SECURITY.md` produces a single line reference + cross-link.
    - Phase 22 subsection preserved untouched: `grep -q '^### Base-image digests (v1.6 onward)$' SECURITY.md` AND `grep -A 35 '^### Base-image digests (v1.6 onward)$' SECURITY.md | grep -q 'digest-drift-check.yml'`
    - New subsection placed BEFORE the Phase 22 subsection (chronological order — image sign documents v1.6 ATTEST-01..04 which post-dates Phase 22 DRIFT-01..03 in roadmap but reads top-down in SECURITY.md per D-05 + PATTERNS §8): `awk '/### Image signatures \+ attestations/{a=NR} /### Base-image digests/{b=NR} END{exit !(a<b)}' SECURITY.md`
    - cosign download signature --bundle CLI ABSENT (RESEARCH §3.4 + §7.5 — that CLI does not exist): `! grep -q 'cosign download signature --bundle' SECURITY.md`
    - --insecure-ignore-tlog ABSENT (Pitfall 3 invariant): `! grep -q '\-\-insecure-ignore-tlog' SECURITY.md`
    - --certificate-identity (without -regexp) ABSENT (Pitfall 1 invariant): `! grep -qE "certificate-identity '[^=]" SECURITY.md`
    - The forward-strikethrough for tarball bullet is NOT added (RESEARCH §8 Q3): the existing first bullet at SECURITY.md:104-109 about "GitHub Release archives ship a SHA-256 checksum but NO PGP / minisign signature" is UNCHANGED (no `~~` markers, no "Closed in v1.6 Phase 24" annotation). `grep -A 5 'GitHub Release archives ship' SECURITY.md | grep -q 'Closed in v1.6 Phase 24' && echo FAIL || echo PASS`.
    - Anchor sanity check: the auto-generated GitHub anchor `#image-signatures--attestations-v16-onward` matches the H3 heading `### Image signatures + attestations (v1.6 onward)` per GitHub's anchor-generation algorithm (lowercase + space→hyphen + drop `+` resulting in two hyphens around the dropped char): `grep -q '#image-signatures--attestations-v16-onward' SECURITY.md`
    - File length grew by ≥50 lines (recipes block + callouts + new subsection): `[ "$(wc -l < SECURITY.md)" -gt 275 ]` (current 225 + ≥50 new ≈ 275+)
    - All existing sections still present (no accidental deletions):
      - `grep -q '^## Reporting a vulnerability$' SECURITY.md`
      - `grep -q '^## Audit-readiness status$' SECURITY.md`
      - `grep -q '^## Supply-chain status$' SECURITY.md`
      - `grep -q '^### Known gaps at v1.5$' SECURITY.md`
      - `grep -q '^### Base-image digests (v1.6 onward)$' SECURITY.md`
      - `grep -q '^### v1.6 supply-chain plan$' SECURITY.md`
      - `grep -q '^## Release versioning policy$' SECURITY.md`
      - `grep -q '^## Where to find more$' SECURITY.md`
    - Plan 23-05 Stage 2 will EMPIRICALLY validate every recipe in this section runs from a fresh ubuntu:24.04 container. If Stage 2 catches a recipe failure, a quick task amends THIS section BEFORE the production v1.6.0 tag.
  </acceptance_criteria>
  <done>
    `SECURITY.md` has a new `### Image signatures + attestations (v1.6 onward)` subsection inserted between `### Known gaps at v1.5` and the existing `### Base-image digests (v1.6 onward)` subsection. The new subsection contains a 3-item numbered list of what's signed/attested, a prerequisite paragraph naming cosign 2.6.3 + gh 2.x, a fenced bash block with 4 verify recipes (cosign verify + gh attestation verify ×2 + cosign save), and 2 `> Note:` callouts for Pitfall 10 + Pitfall 13. The v1.5 Docker-unsigned bullet is strikethrough'd on a single line with a cross-link to the new subsection. The Phase 22 `### Base-image digests (v1.6 onward)` subsection is untouched. The forward-strikethrough on the tarball bullet is NOT added (Phase 24 owns that anchor). The locked identity regex matches Pitfall 1 + RESEARCH §7.6. The cosign version pin is `>= 2.6.3, < 3.0.0` matching CI side per D-08 + Plan 23-01. The `cosign download signature --bundle` CLI is absent (corrected to `cosign save --dir` per RESEARCH §3.4).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator workstation → SECURITY.md recipes | The recipes ARE the operator-facing trust boundary. A wrong recipe (typo, nonexistent flag, drifted regex) propagates to every operator following the doc. Pitfall 12 catches this via fresh-machine UAT (Plan 23-05 Stage 2). |
| SECURITY.md identity regex → OIDC subject claims | The locked regex `'docker\.yml@refs/tags/v.*'` is narrow on workflow file + tag namespace per Pitfall 1. RESEARCH §7.6 verifies the matrix leg does NOT widen the claim. |
| Operator → Sigstore Fulcio / Rekor (online verify) | Recipes 1-3 require network to Fulcio + Rekor + ghcr.io. Documented assumption; --insecure-ignore-tlog deliberately NOT documented per Pitfall 3. |
| Operator → local FS (offline verify after cosign save) | Recipe 4 enables offline verify after one-time `cosign save` with network. Bounded by the trust the operator places in their local cosign TUF root cache. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-21 | Spoofing | OIDC subject regex too wide — `'https://github.com/johnzilla/.*'` would let any of johnzilla's repos produce a "valid" signature for blindjoin (Pitfall 1) | mitigate | Locked regex binds to BOTH workflow file (`docker\.yml`) AND tag namespace (`refs/tags/v.*`). RESEARCH §7.6 verified the matrix leg does not affect the claim. Plan 23-05 Stage 2 step 6 (negative test against `distroless/static`) empirically confirms the regex doesn't over-match. |
| T-23-22 | Tampering | Recipe contains a nonexistent CLI flag (e.g., `cosign download signature --bundle` from CONTEXT D-07) — operator copy-pastes, gets "Error: unknown flag --bundle" (RESEARCH §7.5) | mitigate | RESEARCH §3.4 + §7.5 corrected the recipe to `cosign save --dir`. Plan 23-05 Stage 2 step 4 EMPIRICALLY runs the recipe from a fresh ubuntu:24.04 container. If it fails, quick task amends before v1.6.0 production tag. |
| T-23-23 | Repudiation | GHCR UI shows "Unverified" badge; operator concludes the image is malicious without running cosign verify (Pitfall 10) | mitigate | Pitfall 10 callout explicitly tells operators the GHCR UI badge is unrelated to cosign verification + names the cosign verify CLI as source of truth. |
| T-23-24 | Tampering | cosign 3.0 ships with renamed flags; documented recipes break for operators on 3.0 (Pitfall 13) | mitigate | D-08 + Pitfall 13 callout pins operator-side cosign to `>= 2.6.3, < 3.0.0`. CI side (Plan 23-01) installs the same range. When cosign 3.0 lands, a quick task updates this section. |
| T-23-25 | Information Disclosure | Documented recipes name `<owner>` and `<image>` as placeholders; operator might paste in private repo info | accept | The recipes use placeholder syntax; operators substitute their own values. No PII in the SECURITY.md text itself. |
| T-23-26 | Denial of Service | Operator runs Recipe 4 (`cosign save`) against a 2GB image and runs out of disk | accept | blindjoin images are <100MB. The recipe documents `--dir ./blindjoin-<image>-<tag>` so operators see the directory naming and can plan storage. |
| T-23-27 | Spoofing | Operator following the SECURITY.md recipe verifies a signature from a different repo whose maintainer used a similar workflow path | mitigate | The locked regex includes `johnzilla/blindjoin` as the owner+repo prefix. A signature from `someother/blindjoin/.github/workflows/docker.yml` would NOT match. Forking the project produces a different OIDC subject (RESEARCH §11 row 2 + RESEARCH §7.6). |
| T-23-28 | Tampering | Markdown anchor `#image-signatures--attestations-v16-onward` doesn't match the H3 heading after a future SECURITY.md restructure (broken link from the strikethrough bullet) | accept-with-action | The strikethrough cross-link is internal. A future restructure must preserve the anchor OR update both ends. Documented in this plan's STATE.md decision record. |
| T-23-SC | Tampering | npm/pip/cargo installs | n/a | This plan modifies only documentation (SECURITY.md). No package installs. No CI workflow changes. The recipe text references operator-side cosign + gh installs which are out of CI scope; HUMAN-UAT in Plan 23-05 Stage 2 covers those install paths empirically. |
</threat_model>

<verification>
After Task 1 completes:

```bash
# 1. New subsection exists with the correct H3 heading.
grep -q '^### Image signatures + attestations (v1.6 onward)$' SECURITY.md

# 2. All 4 recipes present.
grep -q 'cosign verify --certificate-identity-regexp\|cosign verify' SECURITY.md   # Recipe 1 — ATTEST-01
grep -q 'gh attestation verify oci://.*provenance/v1' SECURITY.md  # Recipe 2 — ATTEST-02
grep -q 'gh attestation verify oci://.*spdx.dev/Document' SECURITY.md  # Recipe 3 — ATTEST-03
grep -q 'cosign save --dir' SECURITY.md                           # Recipe 4 — ATTEST-04 (corrected)
grep -q 'cosign verify --local-image' SECURITY.md                 # Recipe 4 second half

# 3. Locked identity regex (Pitfall 1).
grep -F "certificate-identity-regexp 'https://github.com/johnzilla/blindjoin/\\.github/workflows/docker\\.yml@refs/tags/v.*'" SECURITY.md

# 4. OIDC issuer URL.
grep -q 'https://token.actions.githubusercontent.com' SECURITY.md

# 5. Callouts present.
grep -q 'GHCR UI' SECURITY.md
grep -q 'Pitfall 10' SECURITY.md
grep -q 'Pitfall 13' SECURITY.md

# 6. Operator-side cosign version pin (D-08).
grep -q '>= 2.6.3, < 3.0.0' SECURITY.md

# 7. v1.5 Docker-unsigned strikethrough'd with cross-link.
grep -q '~~Docker images on `ghcr.io` are unsigned.~~' SECURITY.md
grep -q '#image-signatures--attestations-v16-onward' SECURITY.md

# 8. Phase 22 Base-image-digests subsection untouched.
grep -q '^### Base-image digests (v1.6 onward)$' SECURITY.md
grep -A 5 '^### Base-image digests (v1.6 onward)$' SECURITY.md | grep -q 'docker/digests.txt'

# 9. The forward-strikethrough on tarball bullet is NOT added (RESEARCH §8 Q3).
grep -A 5 'GitHub Release archives ship' SECURITY.md | grep -q 'Closed in v1.6 Phase 24' && echo "FAIL: forward-strikethrough should not be here" || echo "PASS"

# 10. CORRECTED CLI absent.
! grep -q 'cosign download signature --bundle' SECURITY.md   # RESEARCH §7.5 — this CLI doesn't exist
! grep -q '\-\-insecure-ignore-tlog' SECURITY.md             # Pitfall 3 invariant
! grep -qE "certificate-identity '[^=]" SECURITY.md          # Pitfall 1 — regex form only

# 11. Subsection ordering: new subsection BEFORE Phase 22's.
awk '/### Image signatures \+ attestations/{a=NR} /### Base-image digests/{b=NR} END{exit !(a<b)}' SECURITY.md

# 12. Markdown anchor sanity (verified manually — GitHub's anchor generation lowercases, hyphenates spaces, drops `+` leaving two hyphens around the dropped character).
# The strikethrough's cross-link `#image-signatures--attestations-v16-onward` must match the new H3 anchor.
# This is empirically validated by clicking the cross-link on github.com — Plan 23-05 Stage 2 confirms.

# 13. End-to-end recipe rehearsal — deferred to Plan 23-05 Stage 2 (HUMAN-UAT on fresh ubuntu:24.04 against v1.6.0-rc.0).
```
</verification>

<success_criteria>
- `SECURITY.md` has a new `### Image signatures + attestations (v1.6 onward)` H3 subsection containing the 4-recipe fenced bash block + Pitfall 10 + Pitfall 13 callouts.
- The cosign verify recipe uses the locked identity-regexp `'https://github.com/johnzilla/blindjoin/\.github/workflows/docker\.yml@refs/tags/v.*'` (Pitfall 1).
- The gh attestation verify recipes cover both SLSA (ATTEST-02) and SPDX (ATTEST-03) predicate types.
- The ATTEST-04 `.bundle` recipe uses `cosign save --dir` (RESEARCH §3.4 correction — `cosign download signature --bundle` CLI does NOT exist).
- The operator-side cosign version pin is `>= 2.6.3, < 3.0.0` matching CI side (Plan 23-01's `cosign-release: 'v2.6.3'`) per D-08 + Pitfall 13.
- The v1.5 Docker-unsigned bullet at `### Known gaps at v1.5` is strikethrough'd on a single line with a cross-link to the new subsection.
- The Phase 22 `### Base-image digests (v1.6 onward)` subsection is untouched.
- The forward-strikethrough on the tarball-signature bullet is NOT added (Phase 24's seat per RESEARCH §8 Q3).
- File length grew by ≥50 lines; no existing sections deleted.
- Maps to ROADMAP Phase 23 Success Criterion #5 ("SECURITY.md `## Supply-chain status` is updated to remove the 'Docker images on ghcr.io are unsigned' gap and replace it with the canonical `cosign verify` command (including the explicit callout that the GHCR UI "Unverified" badge is unrelated to cosign verification per Pitfall 10)") + #1 (cosign verify recipe locked to identity-regexp) + #2 (SLSA provenance retrieval recipe) + #3 (SPDX SBOM retrieval recipe) + #4 (`.bundle` for offline verify — corrected to `cosign save --dir`).
</success_criteria>

<output>
Create `.planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-04-SUMMARY.md` when done. Include: (a) the exact line numbers of the new subsection (before and after edit), (b) the literal regex string used in the cosign verify recipe (for cross-reference with Plan 23-05 Stage 2 UAT — which runs the same regex against rc.0), (c) the verified anchor `#image-signatures--attestations-v16-onward` that the strikethrough cross-links to, (d) a note confirming the Phase 22 `### Base-image digests` subsection is byte-identical before and after, (e) the cross-reference to Plan 23-05 (HUMAN-UAT) which empirically validates every recipe in this section from a fresh container.
</output>
</content>
</invoke>