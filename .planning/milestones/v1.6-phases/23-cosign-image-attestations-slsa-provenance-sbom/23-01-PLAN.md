---
phase: 23-cosign-image-attestations-slsa-provenance-sbom
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/docker.yml
autonomous: true
requirements:
  - ATTEST-01
requirements_addressed:
  - ATTEST-01

must_haves:
  truths:
    - "`docker.yml`'s `docker` job declares the four permissions required for cosign keyless signing and the attest-* actions: `contents: read`, `packages: write`, `id-token: write`, and `attestations: write` (D-02 + RESEARCH §2.1 correction — without `attestations: write` the attest-* steps fail with 403 Forbidden)."
    - "The `docker/build-push-action` step has `id: build` so downstream sign + attest steps consume `${{ steps.build.outputs.digest }}` directly (D-01 — no job-output plumbing across matrix legs)."
    - "A new `sigstore/cosign-installer@7e8b541eb2e61bf99390e1afd4be13a184e9ebc5 # v3.10.1` step installs cosign `v2.6.3` on the runner via `cosign-release:` input (D-10 + D-11 + RESEARCH §2.3 — installer v3.X is operator-compatible with the cosign 2.x range locked in SECURITY.md per D-08)."
    - "A new `cosign sign --yes` step runs on each matrix leg (coordinator / client / liquidity-bot), producing the `<image>:sha256-<HEX>.sig` registry tag that ATTEST-01 requires (RESEARCH §3.2)."
    - "The permissions block carries an auditor-grepable deliberately-omitted-scopes comment using paraphrased tokens (`PR-write`, `pages`) so the Phase 22 Plan 22-04 audit gate `! grep -q 'pull-requests:'` continues to hold at file level."
    - "All existing `docker.yml` steps and the `check` job are untouched (D-01 scope discipline)."
  artifacts:
    - path: ".github/workflows/docker.yml"
      provides: "Job-level permissions for cosign OIDC + attest-* API access"
      contains: "id-token: write"
    - path: ".github/workflows/docker.yml"
      provides: "Attestations API write permission for actions/attest-* (RESEARCH §2.1)"
      contains: "attestations: write"
    - path: ".github/workflows/docker.yml"
      provides: "Build-push step id for downstream digest plumbing"
      contains: "id: build"
    - path: ".github/workflows/docker.yml"
      provides: "Cosign installer pinned to v3.10.1 with cosign-release v2.6.3"
      contains: "sigstore/cosign-installer@7e8b541eb2e61bf99390e1afd4be13a184e9ebc5"
    - path: ".github/workflows/docker.yml"
      provides: "Cosign sign step (ATTEST-01) — produces <image>:sha256-<HEX>.sig"
      contains: "cosign sign --yes"
  key_links:
    - from: "docker.yml docker job permissions block"
      to: "Sigstore Fulcio + GitHub attestations API"
      via: "id-token + attestations OIDC + REST"
      pattern: "id-token: write[\\s\\S]*attestations: write"
    - from: "docker.yml cosign sign step"
      to: "ghcr.io/.../blindjoin-<image>@<digest>"
      via: "cosign 2.6.3 keyless OIDC"
      pattern: "cosign sign --yes"
    - from: "docker.yml cosign sign step"
      to: "docker/build-push-action outputs.digest"
      via: "env: DIGEST + steps.build.outputs.digest"
      pattern: "steps\\.build\\.outputs\\.digest"
---

<objective>
Lay the foundation for Phase 23 sign + attest steps in `.github/workflows/docker.yml`: (1) grow the `docker` job's permissions block from 2 scopes to 4 (adding `id-token: write` per D-02 and `attestations: write` per RESEARCH §2.1 correction), (2) add `id: build` to the existing `docker/build-push-action` step so downstream steps can read `outputs.digest`, (3) install cosign 2.6.3 via the v3.10.1 cosign-installer, and (4) sign the just-pushed image with `cosign sign --yes` (ATTEST-01).

Purpose: ATTEST-01 specifically requires the `<digest>.sig` registry-tag form, which only `cosign sign` produces (NOT `actions/attest-build-provenance` — see RESEARCH §2.4 + NEW Pitfall §7.3). The permissions and id:build additions are pre-requisites for EVERY subsequent sign/attest step (Plan 23-02), so this plan is the load-bearing Wave 1 foundation. Pins, version range, and OIDC subject claim all stay consistent with SECURITY.md (Plan 23-04) and the sigstore-pin-check gate (Plan 23-03).
Output: One modified file: `.github/workflows/docker.yml` (adds ~25 new lines: permissions comment block + 2 permission lines + 1 id: line + 2 new steps).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-CONTEXT.md
@.planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-RESEARCH.md
@.planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-PATTERNS.md
@.planning/research/PITFALLS.md
@.github/workflows/docker.yml

<interfaces>
<!-- This plan's outputs (consumed by Plan 23-02 in Wave 2): -->
<!--   - steps.build.outputs.digest — the full `sha256:<HEX>` digest of the just-pushed image -->
<!--   - The cosign 2.6.3 binary on $PATH (installed by cosign-installer step) -->
<!--   - The `id-token` and `attestations` permissions granted to the docker job (consumed by actions/attest-* steps in 23-02) -->
<!--   - The `<image>:sha256-<HEX>.sig` registry tag (the ATTEST-01 deliverable) -->

<!-- Pin contract (D-04 — enforced by sigstore-pin-check in Plan 23-03): -->
<!--   sigstore/cosign-installer@7e8b541eb2e61bf99390e1afd4be13a184e9ebc5 # v3.10.1 -->
<!--     cosign-release: 'v2.6.3' -->
<!--   Format: <owner>/<action>@<40-hex> # v<X.Y.Z>, TWO spaces before # (PATTERNS §"Shared #2") -->

<!-- Permission contract (RESEARCH §2.1 — 4 scopes total): -->
<!--   contents: read    — checkout source -->
<!--   packages: write   — push image + sig to ghcr.io (already present) -->
<!--   id-token: write   — OIDC token for Fulcio cert exchange (D-02) -->
<!--   attestations: write — persist attestations to GitHub API (RESEARCH §2.1 NEW) -->
<!-- DELIBERATELY OMITTED (file-level audit-grepable per Plan 22-04 lesson): -->
<!--   PR-write, pages, issues, deployments — these tokens MUST NOT appear in docker.yml -->

<!-- Step ordering (locked by D-01 + RESEARCH §3): -->
<!--   ... existing steps through docker/build-push-action ... -->
<!--   [+ NEW] id: build on the build-push step -->
<!--   [+ NEW] sigstore/cosign-installer (Plan 23-01) -->
<!--   [+ NEW] cosign sign --yes (Plan 23-01)                          <-- THIS PLAN ENDS HERE -->
<!--   [+ NEW] anchore/sbom-action (Plan 23-02)                        <-- next plan picks up -->
<!--   [+ NEW] actions/attest-sbom (Plan 23-02) -->
<!--   [+ NEW] actions/attest-build-provenance (Plan 23-02) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grow `docker` job permissions block to 4 scopes + add `id: build` to build-push step</name>
  <files>.github/workflows/docker.yml</files>
  <read_first>
    - .github/workflows/docker.yml (full file — focus on lines 28-29 workflow-level permissions, lines 61-63 docker-job permissions, line 110 build-push-action `uses:` line with no current `id:`)
    - .planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-RESEARCH.md §2.1 lines 90-127 (the locked 4-scope permissions block + comment shape; the `attestations: write` correction to CONTEXT D-02)
    - .planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-RESEARCH.md §3.1 lines 219-231 (the `id: build` addition + the explicit "do NOT add `provenance:` or `sbom:` inputs to with:" rule citing Pitfall 5)
    - .planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-PATTERNS.md §"Shared #1" lines 495-554 (the auditor-grepable deliberately-omitted-scopes comment block pattern from Phase 22 Plan 22-04; the literal `PR-write` / `pages` paraphrased tokens that satisfy the `! grep -q 'pull-requests:'` audit gate at file level)
    - .planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-PATTERNS.md §1 lines 59-85 (id: build placement — single line between `uses:` and `with:` on the build-push step)
    - .planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-CONTEXT.md §decisions D-01 + D-02 (inline matrix leg; job-level not workflow-level permissions)
    - .planning/research/PITFALLS.md §Pitfall 2 (id-token: write narrow scope, opaque Fulcio 400 failure mode)
    - .planning/research/PITFALLS.md §Pitfall 5 (one path to SLSA — do NOT add `provenance:`/`sbom:` to build-push-action; the two-attestation failure mode)
  </read_first>
  <action>
    Make TWO coordinated edits to `.github/workflows/docker.yml`:

    **Edit 1 — Replace the `docker` job permissions block at lines 61-63.** The current block is:

        permissions:
          contents: read
          packages: write

    Replace it with a 4-scope block plus a multi-line comment header above it. The replacement block grows to 4 scopes (adding `id-token: write` per D-02 and `attestations: write` per RESEARCH §2.1 correction). The comment block MUST:
      - Cite Phase 23 ATTEST-01/02/03 as the source REQ-IDs.
      - Enumerate each scope with its rationale (cause→effect→source), using the cause-to-effect-to-source structure from PATTERNS §"Shared #3".
      - Cite PITFALLS Pitfall 2 explicitly for `id-token: write` (opaque Fulcio 400 failure mode).
      - Cite RESEARCH §2.1 explicitly for `attestations: write` (this is the correction to CONTEXT.md D-02 which omitted the scope; the 403 Forbidden failure mode).
      - Declare the deliberately-omitted scope list in the audit-grepable paraphrased-token form (`PR-write`, `pages`, `issues`, `deployments`) — NOT the literal `pull-requests:` / `pages:` / `issues:` / `deployments:` strings — so `! grep -q 'pull-requests:'` continues to pass at file level. This follows the Phase 22 Plan 22-04 lesson recorded in STATE.md §Recent Plan Decisions.
    Exact 4-scope ordering: `contents: read`, `packages: write`, `id-token: write`, `attestations: write` (RESEARCH §2.1 lines 120-125 locked order).

    **Edit 2 — Add `id: build` to the existing `docker/build-push-action` step at line 110.** The current step starts:

        - uses: docker/build-push-action@bcafcacb16a39f128d818304e6c9c0c18556b85f # v7.1.0
          with:

    Insert a single line `id: build` between the `uses:` and `with:` lines (so the order is `uses:` → `id: build` → `with:` → existing inputs). Do NOT touch the `with:` block's existing 7 inputs (`context`, `file`, `target`, `push`, `tags`, `labels`, `build-args`, `cache-from`, `cache-to`). Per RESEARCH §3.1 + PITFALLS §5, do NOT add `provenance:` or `sbom:` inputs — those would compete with the actions/attest-* path locked by D-03 and produce two competing attestations per image (the exact failure mode PITFALLS Pitfall 5 names).

    Implementing D-01 (inline matrix leg with `outputs.digest` consumed in-context), D-02 (job-level permissions), and the RESEARCH §2.1 correction (attestations: write required).

    DO NOT move the permissions block to the workflow level — D-02 explicitly locks job-level scope.
    DO NOT add the literal `pull-requests:` token (with colon) anywhere — the auditor-grepable file-level audit gate from Phase 22 Plan 22-04 depends on its absence.
    DO NOT add `id-token: write` or `attestations: write` at the workflow level (line 28-29 block) — they must remain job-scoped to the `docker` job and ABSENT from the `check` job.
    DO NOT add `provenance:` or `sbom:` inputs to the `docker/build-push-action` `with:` block — Pitfall 5 + RESEARCH §3.1 forbid mixing BuildKit-native and GitHub-native attestation paths.
    DO NOT change the build-push-action SHA pin or any other existing `uses:` line — Plan 23-03's sigstore-pin-check gate is narrowly scoped to sigstore-ecosystem and sbom-action; existing pins are out of scope per CONTEXT D-04 narrow-grep choice.
  </action>
  <verify>
    <automated>python3 -c 'import yaml; yaml.safe_load(open(".github/workflows/docker.yml"))' &amp;&amp; grep -q '^[[:space:]]*id-token: write$' .github/workflows/docker.yml &amp;&amp; grep -q '^[[:space:]]*attestations: write$' .github/workflows/docker.yml &amp;&amp; grep -q '^[[:space:]]*id: build$' .github/workflows/docker.yml &amp;&amp; grep -q 'docker/build-push-action@bcafcacb16a39f128d818304e6c9c0c18556b85f' .github/workflows/docker.yml &amp;&amp; grep -q 'Pitfall 2' .github/workflows/docker.yml &amp;&amp; grep -q 'RESEARCH §2.1\|RESEARCH §2\.1' .github/workflows/docker.yml &amp;&amp; ! grep -q 'pull-requests:' .github/workflows/docker.yml &amp;&amp; ! grep -qE '^[[:space:]]+provenance:' .github/workflows/docker.yml &amp;&amp; ! grep -qE '^[[:space:]]+sbom:[[:space:]]*true' .github/workflows/docker.yml</automated>
  </verify>
  <acceptance_criteria>
    - File is valid YAML: `python3 -c 'import yaml; yaml.safe_load(open(".github/workflows/docker.yml"))'` exit 0
    - Job-level `id-token: write` permission present: `grep -q '^[[:space:]]*id-token: write$' .github/workflows/docker.yml`
    - Job-level `attestations: write` permission present (RESEARCH §2.1 correction): `grep -q '^[[:space:]]*attestations: write$' .github/workflows/docker.yml`
    - Existing `contents: read` and `packages: write` scopes preserved: `grep -q '^[[:space:]]*contents: read$' .github/workflows/docker.yml && grep -q '^[[:space:]]*packages: write$' .github/workflows/docker.yml`
    - `id: build` on the build-push-action step: `grep -q '^[[:space:]]*id: build$' .github/workflows/docker.yml`
    - Build-push-action SHA pin unchanged (existing step preserved): `grep -q 'docker/build-push-action@bcafcacb16a39f128d818304e6c9c0c18556b85f # v7.1.0' .github/workflows/docker.yml`
    - Permissions comment cites Pitfall 2 (id-token rationale): `grep -q 'Pitfall 2' .github/workflows/docker.yml`
    - Permissions comment cites RESEARCH §2.1 correction (attestations: write rationale): `grep -qE 'RESEARCH §?2\.1' .github/workflows/docker.yml`
    - Auditor-grepable deliberately-omitted scopes block uses paraphrased tokens (Plan 22-04 lesson): `grep -qE 'PR-write|deliberately omitted|Deliberately omitted' .github/workflows/docker.yml`
    - Audit gate from Phase 22 Plan 22-04 still passes (no literal `pull-requests:` token anywhere): `! grep -q 'pull-requests:' .github/workflows/docker.yml`
    - No competing BuildKit-native attestation inputs (Pitfall 5): `! grep -qE '^[[:space:]]+provenance:[[:space:]]+(mode|true)' .github/workflows/docker.yml && ! grep -qE '^[[:space:]]+sbom:[[:space:]]*true' .github/workflows/docker.yml`
    - Workflow-level permissions block at lines 28-29 unchanged (still `contents: read` only): `awk '/^permissions:/{flag=1; next} flag && /^[a-z]/{flag=0} flag {print}' .github/workflows/docker.yml | head -3 | grep -c 'contents: read'` → 1
    - `check` job at lines 32-52 is untouched (no new permission lines, no new steps): visual diff scope.
  </acceptance_criteria>
  <done>
    `.github/workflows/docker.yml` parses as valid YAML; the `docker` job's permissions block has 4 scopes (contents/packages/id-token/attestations) preceded by an auditor-grepable comment header citing Pitfall 2 + RESEARCH §2.1; the build-push-action step has `id: build` between `uses:` and `with:` with no other changes to its `with:` block; the workflow-level permissions block and `check` job are untouched; the literal `pull-requests:` token is absent from the entire file (Phase 22 Plan 22-04 audit gate continues to hold).
  </done>
</task>

<task type="auto">
  <name>Task 2: Insert `sigstore/cosign-installer` + `cosign sign --yes` steps after build-push step (ATTEST-01)</name>
  <files>.github/workflows/docker.yml</files>
  <read_first>
    - .github/workflows/docker.yml (verify Task 1's `id: build` is present on the build-push-action step before inserting downstream steps)
    - .planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-RESEARCH.md §3.2 lines 233-266 (the locked YAML for both the cosign-installer step and the cosign sign step; --yes rationale; PITFALLS Pitfall 3 do-not-set `--no-tlog-upload` invariant; cosign-release 'v2.6.3' rationale)
    - .planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-RESEARCH.md §2.3 lines 158-181 (SHA pin resolution for sigstore/cosign-installer @ v3.10.1; the v3.X-not-v4.X rationale for cosign 2.x operator-compatibility; cosign-release v2.6.3 vs v2.5.3 trade-off)
    - .planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-PATTERNS.md §2 lines 89-118 (SHA-pin trailing-comment style: <owner>/<action>@<40-hex> # v<X.Y.Z>, TWO spaces before #)
    - .planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-PATTERNS.md §3 lines 121-159 (cosign sign step shape: env: + run: pattern; --yes flag mandatory; named after ATTEST-01)
    - .planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-RESEARCH.md §7.3 lines 666-670 (NEW Pitfall: cosign sign and attest-build-provenance are NOT interchangeable; ATTEST-01 specifically requires <digest>.sig which only cosign sign produces)
    - .planning/research/PITFALLS.md §Pitfall 3 (Rekor transparency log mandatory; do NOT set --no-tlog-upload)
    - .planning/research/PITFALLS.md §Pitfall 1 (cosign verify identity regex — informs the operator-side recipe in Plan 23-04 but the signing side just produces; the regex match is consumer-side)
  </read_first>
  <action>
    Append TWO new steps to the `docker` job in `.github/workflows/docker.yml`, immediately AFTER the modified `docker/build-push-action` step (Task 1) and BEFORE end-of-job. The two steps are the cosign installer and the cosign sign command. Both run inside each matrix leg independently — `strategy.fail-fast: false` (already set at line 65) means a sign failure on one image does not block the others.

    **Step 1 — Install cosign (keyless signing toolchain).** Insert a multi-line `#` comment header citing Phase 23 ATTEST-01, the v3.10.1 installer pin rationale (RESEARCH §2.3 — v3.X is the last installer line that installs cosign 2.x, matching the operator-side `>= 2.6.3, < 3.0.0` range Plan 23-04 documents per D-08), and the v2.6.3 cosign-release pin rationale. The step uses the action pin `sigstore/cosign-installer@7e8b541eb2e61bf99390e1afd4be13a184e9ebc5 # v3.10.1` (40-hex SHA + two spaces + `# v3.10.1`) with `with: { cosign-release: 'v2.6.3' }`. Step name: `Install cosign (keyless signing toolchain)`.

    **Step 2 — Sign image with cosign (ATTEST-01).** Insert a multi-line `#` comment header citing ATTEST-01, the `<image>:sha256-<HEX>.sig` registry-tag form that ATTEST-01 specifically requires (RESEARCH §2.4 + NEW Pitfall §7.3 — distinct from the `actions/attest-build-provenance` referrer-tag form Plan 23-02 produces), the `--yes` non-interactive rationale (would otherwise prompt for transparency-log consent), and the PITFALLS Pitfall 3 invariant that `--no-tlog-upload` MUST NOT be set (Rekor is the operator-facing transparency guarantee). The step uses the `env:` + `run:` shape from PATTERNS §3 — env block exposes `IMAGE: ghcr.io/${{ github.repository_owner }}/blindjoin-${{ matrix.image }}` and `DIGEST: ${{ steps.build.outputs.digest }}`, then `run: cosign sign --yes "${IMAGE}@${DIGEST}"` as a single-line command. Step name: `Sign image with cosign (keyless OIDC) — ATTEST-01`.

    Both steps land AFTER the existing `docker/build-push-action` step (currently line 110-126) and BEFORE end of the `docker` job. Per D-01 (inline matrix leg), `${{ steps.build.outputs.digest }}` is consumed in-context — no job-output plumbing needed across the three matrix legs.

    Implementing ATTEST-01 (cosign keyless OIDC sign producing `<digest>.sig`) and D-01 (inline matrix leg). Pins resolved per D-10/D-11.

    DO NOT use a SHA pin other than `7e8b541eb2e61bf99390e1afd4be13a184e9ebc5` for cosign-installer — Plan 23-03's sigstore-pin-check gate enforces this 40-hex form and Plan 23-04's SECURITY.md cosign-version range is anchored to v2.6.3.
    DO NOT set `cosign-release` higher than v2.6.x — D-08 locks the operator-side range to `< 3.0.0` and cosign 3.0 has CLI flag drift per Pitfall 13.
    DO NOT add `--no-tlog-upload` to `cosign sign` — Pitfall 3 explicitly forbids; Rekor is the operator-facing transparency guarantee for ATTEST-01.
    DO NOT consolidate the two steps into a single composite-action wrapper — CONTEXT.md §code_context "Established Patterns" explicitly rejects composite wrappers (matrix already deduplicates; wrapper adds indirection).
    DO NOT omit the multi-line comment block above either step — the comments-as-contract style per PATTERNS §"Shared #3" is enforced project-wide.
    DO NOT pass `${{ steps.build.outputs.digest }}` directly in the `run:` command body — use the `env:` block + `${IMAGE}@${DIGEST}` interpolation per PATTERNS §3 (keeps the run body free of `${{ … }}` noise; mirrors the install-bitcoind step shape at ci.yml:62-118).
  </action>
  <verify>
    <automated>python3 -c 'import yaml; yaml.safe_load(open(".github/workflows/docker.yml"))' &amp;&amp; grep -q 'sigstore/cosign-installer@7e8b541eb2e61bf99390e1afd4be13a184e9ebc5 # v3.10.1' .github/workflows/docker.yml &amp;&amp; grep -q "cosign-release: 'v2.6.3'" .github/workflows/docker.yml &amp;&amp; grep -q 'cosign sign --yes' .github/workflows/docker.yml &amp;&amp; grep -q 'ATTEST-01' .github/workflows/docker.yml &amp;&amp; grep -q 'Pitfall 3\|PITFALLS Pitfall 3\|Pitfall §3' .github/workflows/docker.yml &amp;&amp; ! grep -q '\-\-no-tlog-upload' .github/workflows/docker.yml &amp;&amp; grep -q 'steps\.build\.outputs\.digest' .github/workflows/docker.yml</automated>
  </verify>
  <acceptance_criteria>
    - File is still valid YAML after edits: `python3 -c 'import yaml; yaml.safe_load(open(".github/workflows/docker.yml"))'` exit 0
    - Cosign installer step exists with the exact RESEARCH §2.3 SHA pin: `grep -q 'sigstore/cosign-installer@7e8b541eb2e61bf99390e1afd4be13a184e9ebc5 # v3.10.1' .github/workflows/docker.yml`
    - Cosign installer `cosign-release:` input pins v2.6.3 (D-11 + RESEARCH §2.3): `grep -q "cosign-release: 'v2.6.3'" .github/workflows/docker.yml`
    - Cosign sign step uses `cosign sign --yes` (the non-interactive flag mandatory in CI): `grep -q 'cosign sign --yes' .github/workflows/docker.yml`
    - Cosign sign step references the build-push digest via env (NOT in-command `${{ … }}`): `grep -q 'DIGEST: \${{ steps\.build\.outputs\.digest }}' .github/workflows/docker.yml`
    - Cosign sign step uses the `${IMAGE}@${DIGEST}` form in `run:`: `grep -q '"\${IMAGE}@\${DIGEST}"' .github/workflows/docker.yml`
    - ATTEST-01 cited in comments (audit-trail surface): `grep -q 'ATTEST-01' .github/workflows/docker.yml`
    - Pitfall 3 cited in comments (transparency-log invariant): `grep -qE 'Pitfall 3|PITFALLS Pitfall 3' .github/workflows/docker.yml`
    - `--no-tlog-upload` ABSENT from the file (Pitfall 3 invariant): `! grep -q '\-\-no-tlog-upload' .github/workflows/docker.yml`
    - SHA-pin trailing-comment style: TWO spaces before `#`, full 40-hex SHA, `# v3.10.1` version comment: `grep -qE 'sigstore/cosign-installer@[a-f0-9]{40}[[:space:]]+# v3\.10\.1' .github/workflows/docker.yml`
    - Per-step comment headers present (PATTERNS §"Shared #3" comments-as-contract): cosign-installer step has at least 4 comment lines above it, cosign sign step has at least 6 comment lines above it (citing ATTEST-01 + Pitfall 3 + the --yes rationale).
    - Sign step name includes the REQ-ID for audit trail: `grep -q 'Sign image with cosign.*ATTEST-01\|ATTEST-01.*Sign image' .github/workflows/docker.yml`
    - Step ordering: cosign-installer line number > build-push-action line number; cosign sign line number > cosign-installer line number. Verifiable via:
      `awk '/docker\/build-push-action@bcafcacb/{a=NR} /sigstore\/cosign-installer@7e8b541/{b=NR} /cosign sign --yes/{c=NR} END{exit !(a<b && b<c)}' .github/workflows/docker.yml`
    - End-to-end smoke (deferred to Plan 23-05 Stage 1 UAT): on a feature branch with the file committed, `gh workflow run docker.yml --ref <branch>` triggers the workflow; per D-06 Stage 1 the `docker` matrix job stays gated on `refs/tags/*` so only the `check` job runs; the YAML compilation + cosign-installer install path is verified by the Stage 1 rehearsal. Full ATTEST-01 end-to-end verification is Plan 23-05 Stage 2 against `v1.6.0-rc.0`.
  </acceptance_criteria>
  <done>
    `.github/workflows/docker.yml` has two new steps (cosign-installer + cosign sign) appended to the `docker` job after the modified build-push step. Both steps have multi-line comment headers citing ATTEST-01 + Pitfall 3 + the relevant RESEARCH sections. The cosign-installer is pinned to `@7e8b541eb2e61bf99390e1afd4be13a184e9ebc5 # v3.10.1` with `cosign-release: 'v2.6.3'`. The sign step uses `env:` + `run: cosign sign --yes "${IMAGE}@${DIGEST}"` per PATTERNS §3. `--no-tlog-upload` is ABSENT from the file. The file remains valid YAML.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → Sigstore Fulcio | TLS-secured OIDC token exchange. Token is short-lived (~10 min); compromised Fulcio could backdate signatures (residual T-23-04 below). |
| GitHub Actions runner → Sigstore Rekor | TLS-secured signature transparency-log upload. A network outage causes `cosign sign` to fail (correct behavior — retry). |
| GitHub Actions runner → ghcr.io | TLS-secured `packages: write` push. Bounded by job-level permission scope. |
| docker job → docker job (cross-matrix-leg) | None. Each leg runs in its own runner FS; no shared state. fail-fast: false isolates per-leg failures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Spoofing | OIDC subject claim manipulation (Pitfall 1) — too-wide regex would let any workflow in any of the maintainer's repos produce a "valid" signature for blindjoin | mitigate | The cert-identity-regexp `'https://github.com/johnzilla/blindjoin/\.github/workflows/docker\.yml@refs/tags/v.*'` is locked at SECURITY.md (Plan 23-04). The signing side (this plan) just produces; verification regex narrowness is enforced operator-side. The OIDC subject claim is determined by GitHub at signing time and includes the workflow file path + tag ref — verified by RESEARCH §7.6 (matrix leg does NOT affect the subject claim, so one regex works for all three matrix legs). |
| T-23-02 | Tampering | Forgotten `id-token: write` / `attestations: write` permissions cause opaque Fulcio 400 / GitHub API 403 — Pitfall 2 + RESEARCH §7.2 | mitigate | Task 1 explicitly adds both permissions with cited rationale in the comment block. The audit-grepable assertion in Task 1's acceptance criteria (`grep -q '^[[:space:]]*attestations: write$'`) is enforced at file level. Plan 23-05 Stage 1 (workflow_dispatch rehearsal) catches this empirically if the comment-vs-code drift occurs. |
| T-23-03 | Tampering | Floating-tag SHA drift on `sigstore/cosign-installer` (Pitfall 4) — `@v3` instead of `@7e8b541...` would silently change installer behavior across releases | mitigate | The action pin uses the explicit 40-hex SHA `7e8b541eb2e61bf99390e1afd4be13a184e9ebc5` with `# v3.10.1` trailing comment. Plan 23-03's `sigstore-pin-check` job greps for this exact form across all workflows in `.github/workflows/` and fails CI if any sigstore-ecosystem action lacks the pin. |
| T-23-04 | Repudiation | Fulcio root key compromise — all keyless signatures everywhere become forgeable (Sigstore community-level threat) | accept | Out of scope for blindjoin — Sigstore TUF root distribution handles rotation. cosign 2.x ships with the TUF-distributed trust root. The HUMAN-UAT in Plan 23-05 Step 6 negative test (`distroless/static` verify) would detect a compromised local TUF cache. |
| T-23-05 | Information Disclosure | Cosign sign step logs the image digest + OIDC subject to runner stdout | accept | No PII; digest + subject are public information by design (Rekor publishes them). The OIDC subject claim format includes `github.com/johnzilla/blindjoin/.github/workflows/docker.yml@refs/tags/vX.Y.Z` which is already a publicly observable URL once the tag exists. |
| T-23-06 | Denial of Service | Rekor outage causes `cosign sign --yes` to hang/fail (Pitfall 3) — release pipeline blocked | accept | Per Pitfall 3: retry, do NOT skip Rekor. `--no-tlog-upload` is FORBIDDEN by Task 2's `! grep` assertion. A Rekor outage is a known operational hazard; release runbook says "retry, not skip". |
| T-23-07 | Elevation of Privilege | Compromised maintainer GitHub account triggers a release; OIDC subject matches the locked regex (the attacker IS in the johnzilla/blindjoin context) | accept | Solo-maintained per SECURITY.md residual; Sigstore Rekor makes the attack PUBLICLY VISIBLE after the fact. Carry-forward to v1.7+ if multi-maintainer review on tag pushes is added. (RESEARCH §11 row 1.) |
| T-23-SC | Tampering | npm/pip/cargo installs | n/a | This plan installs only `sigstore/cosign-installer` (a GitHub Action) which downloads a SHA-pinned cosign binary. No npm/pip/cargo registries touched. The installer's own SHA-pin discipline (Pitfall 4 + the sigstore-pin-check gate in Plan 23-03) is the supply-chain hardening for this surface. The cosign-installer action is GitHub-maintained by the Sigstore organization. |
</threat_model>

<verification>
After both tasks complete:

```bash
# 1. YAML validity.
python3 -c 'import yaml; yaml.safe_load(open(".github/workflows/docker.yml"))'

# 2. 4-scope permissions block on docker job (RESEARCH §2.1).
grep -q '^[[:space:]]*contents: read$' .github/workflows/docker.yml
grep -q '^[[:space:]]*packages: write$' .github/workflows/docker.yml
grep -q '^[[:space:]]*id-token: write$' .github/workflows/docker.yml
grep -q '^[[:space:]]*attestations: write$' .github/workflows/docker.yml

# 3. id: build on build-push step (D-01 in-context digest plumbing).
grep -q '^[[:space:]]*id: build$' .github/workflows/docker.yml

# 4. Phase 22 Plan 22-04 audit gate STILL holds (literal pull-requests: token absent).
! grep -q 'pull-requests:' .github/workflows/docker.yml

# 5. Cosign installer pinned per RESEARCH §2.3.
grep -q 'sigstore/cosign-installer@7e8b541eb2e61bf99390e1afd4be13a184e9ebc5 # v3.10.1' .github/workflows/docker.yml
grep -q "cosign-release: 'v2.6.3'" .github/workflows/docker.yml

# 6. Cosign sign step (ATTEST-01) — exact CLI form, no tlog-upload-skip.
grep -q 'cosign sign --yes "\${IMAGE}@\${DIGEST}"' .github/workflows/docker.yml
! grep -q '\-\-no-tlog-upload' .github/workflows/docker.yml

# 7. Pitfall citations in comments (audit trail).
grep -qE 'Pitfall 2|PITFALLS Pitfall 2' .github/workflows/docker.yml
grep -qE 'Pitfall 3|PITFALLS Pitfall 3' .github/workflows/docker.yml
grep -qE 'RESEARCH §?2\.1' .github/workflows/docker.yml
grep -q 'ATTEST-01' .github/workflows/docker.yml

# 8. No competing BuildKit-native attestation inputs on build-push (Pitfall 5).
! grep -qE '^[[:space:]]+provenance:[[:space:]]+(mode|true)' .github/workflows/docker.yml
! grep -qE '^[[:space:]]+sbom:[[:space:]]*true' .github/workflows/docker.yml

# 9. Smoke rehearsal — full procedure deferred to Plan 23-05 Stage 1 + Stage 2.
# Stage 1 (pre-merge): gh workflow run docker.yml --ref <feature-branch>
#   Expect: check job runs and passes; docker matrix job gated out by refs/tags/* condition.
# Stage 2 (post-rc.0-tag): gh workflow run docker.yml --ref refs/tags/v1.6.0-rc.0
#   Expect: docker matrix job runs; cosign sign produces ghcr.io/.../blindjoin-<image>:sha256-<HEX>.sig
```
</verification>

<success_criteria>
- `.github/workflows/docker.yml` `docker` job permissions block has 4 scopes (contents, packages, id-token, attestations) preceded by an auditor-grepable comment header citing Pitfall 2 + RESEARCH §2.1.
- The existing `docker/build-push-action` step has `id: build` for downstream digest plumbing (D-01).
- Two new steps are appended after build-push: `sigstore/cosign-installer@7e8b541... # v3.10.1` (with cosign-release v2.6.3) and `cosign sign --yes "${IMAGE}@${DIGEST}"` (the ATTEST-01 deliverable).
- File is valid YAML.
- The Phase 22 Plan 22-04 audit gate (`! grep -q 'pull-requests:'`) continues to pass at file level.
- No competing BuildKit-native attestation inputs on the build-push-action `with:` block (Pitfall 5).
- `--no-tlog-upload` is ABSENT from the entire file (Pitfall 3).
- Maps to ROADMAP Phase 23 Success Criterion #1 (image signature reachable via `cosign verify` once the v1.6.0 tag is pushed; the signing side is delivered by this plan, the verifier-side recipe lands in Plan 23-04 SECURITY.md).
</success_criteria>

<output>
Create `.planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-01-SUMMARY.md` when done. Include: (a) the literal lines added (4 permission scopes, 1 id:build line, 2 new steps + comment headers), (b) the exact SHA pins used (cosign-installer + cosign-release), (c) the Phase 22 Plan 22-04 audit-gate result (`! grep -q 'pull-requests:'` continues to PASS), and (d) the cross-reference to Plan 23-02 which appends the attestation steps after the cosign sign step this plan ends with.
</output>
</content>
</invoke>