---
phase: 25-reproducible-build-recipe-scheduled-verifier-registry
plan: 03
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - docs/REPRODUCIBLE-BUILD.md
  - docs/REPRODUCIBLE-BUILD.expected-sha256.txt
autonomous: true
requirements:
  - REPRO-01
tags: [docs, reproducible-build, operator-recipe, expected-sha256-lookup]

must_haves:
  truths:
    - "New file `docs/REPRODUCIBLE-BUILD.md` exists at workspace `docs/` with 7 H2 sections in the exact order per D-09: `## Why this exists`, `## Recipe`, `## Toolchain pins`, `## Environment`, `## Expected sha256sum`, `## Continuous verification`, `## Reporting a reproducibility regression`."
    - "The `## Recipe` section contains a single fenced bash block per D-17 that an external rebuilder copy-pastes into a fresh `ubuntu-24.04` shell: git clone, git checkout, env exports (SOURCE_DATE_EPOCH, RUSTFLAGS, CARGO_INCREMENTAL), `cargo build --release --locked --bin coordinator --bin client --bin liquidity-bot`, deterministic tar/gzip pipeline (D-06 5-flag set), sha256sum compare."
    - "The `## Toolchain pins` section is a markdown table with at least these rows: rustc (version `1.95.0` pinned via `rust-toolchain.toml`), ubuntu-24.04 runner image (placeholder `<TBD-v1.6.0-cut>` for ImageVersion), dtolnay/rust-toolchain action SHA, sigstore/cosign-installer action SHA. Each row names the file the pin lives in."
    - "The `## Expected sha256sum` section contains a markdown table with `v1.6.0: <TBD-v1.6.0-cut>` (D-10 placeholder; replaced in Plan 25-05's rehearsal procedure once captured)."
    - "New companion file `docs/REPRODUCIBLE-BUILD.expected-sha256.txt` exists with one line per release in `<tag>:<sha256>:<image-version>` colon-delimited format (initial content: `v1.6.0:<TBD-v1.6.0-cut-sha256>:<TBD-v1.6.0-cut-imageversion>`). This is the verifier's expected-sha256 + ImageVersion lookup mechanism per D-18 RESEARCH recommendation (line-by-line file is more robust than markdown-table parse). The file is the single source of truth for BOTH the expected sha256 AND the pinned ImageVersion that the Plan 25-04 verifier reads (the markdown table is human-facing only — verifier never parses markdown)."
    - "The `## Continuous verification` section cross-links to `.github/workflows/reproducible-verify.yml` (file will be created in Plan 25-04) + names the monthly cadence + placeholder for the reproducible-builds.org registry entry URL (filled in by Plan 25-05 after D-14 lands)."
    - "The `## Reporting a reproducibility regression` section documents the `[reproducibility-regression]` issue scheme + the two title formats from D-12 + the runner-image-drift-vs-real-divergence distinction."
  artifacts:
    - path: "docs/REPRODUCIBLE-BUILD.md"
      provides: "Operator-facing reproducibility recipe + per-tag sha256 table"
      contains: "## Recipe"
      min_lines: 100
    - path: "docs/REPRODUCIBLE-BUILD.md"
      provides: "Recipe bash block with deterministic tar+gzip"
      contains: "--sort=name --owner=0 --group=0 --numeric-owner"
    - path: "docs/REPRODUCIBLE-BUILD.md"
      provides: "Toolchain pin row for rustc 1.95.0"
      contains: "1.95.0"
    - path: "docs/REPRODUCIBLE-BUILD.md"
      provides: "Expected sha256 placeholder for v1.6.0"
      contains: "<TBD-v1.6.0-cut>"
    - path: "docs/REPRODUCIBLE-BUILD.expected-sha256.txt"
      provides: "Machine-readable expected-sha256 lookup (D-18)"
      contains: "v1.6.0:<TBD-v1.6.0-cut-sha256>:<TBD-v1.6.0-cut-imageversion>"
      min_lines: 1
  key_links:
    - from: "docs/REPRODUCIBLE-BUILD.md Recipe section"
      to: ".github/workflows/release.yml build job env: + Compute + Package steps"
      via: "byte-equivalent reproduction of env vars + cargo invocation + tar/gzip pipeline"
      pattern: "cargo build --release --locked"
    - from: "docs/REPRODUCIBLE-BUILD.expected-sha256.txt"
      to: ".github/workflows/reproducible-verify.yml (will be created in 25-04)"
      via: "verifier reads this file to look up expected hash by tag"
      pattern: "v1\\.6\\.0:"
    - from: "docs/REPRODUCIBLE-BUILD.md Continuous verification section"
      to: ".github/workflows/reproducible-verify.yml"
      via: "markdown cross-link"
      pattern: "reproducible-verify\\.yml"
---

<objective>
Create the operator-facing reproducibility doc per D-09 + D-17 — a 7-section H2 markdown file that names the exact Rust toolchain, ubuntu-24.04 runner pin, env vars, cargo invocation, and expected sha256sum, with a copy-pasteable Recipe bash block that an external rebuilder runs end-to-end on a fresh `ubuntu-24.04` shell. Also create the companion `docs/REPRODUCIBLE-BUILD.expected-sha256.txt` file as the verifier's expected-sha256 lookup mechanism per D-18 RESEARCH recommendation (line-by-line file is more robust than markdown-table parse).

Purpose: REPRO-01 requires `docs/REPRODUCIBLE-BUILD.md` to be the canonical operator artifact naming the exact toolchain version (matches `ci.yml` pin from Plan 25-01), `ubuntu-24.04` runner, all env vars (SOURCE_DATE_EPOCH, RUSTFLAGS, CARGO_INCREMENTAL=0), exact `cargo build --release --locked` invocation, and the expected sha256sum. The companion `.expected-sha256.txt` enables Plan 25-04's verifier to parse a stable line-format file rather than a brittle markdown-table grep.

Output: 2 new files: `docs/REPRODUCIBLE-BUILD.md` (~100-180 lines) and `docs/REPRODUCIBLE-BUILD.expected-sha256.txt` (1 line initially).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-reproducible-build-recipe-scheduled-verifier-registry/25-CONTEXT.md
@.planning/phases/25-reproducible-build-recipe-scheduled-verifier-registry/25-RESEARCH.md
@.planning/phases/25-reproducible-build-recipe-scheduled-verifier-registry/25-PATTERNS.md
@SECURITY.md
@rust-toolchain.toml
@.github/workflows/release.yml

<interfaces>
<!-- Inputs from Plan 25-01: -->
<!--   - rust-toolchain.toml channel = "1.95.0" — Recipe references this version literally and the Toolchain pins table cites the file -->

<!-- Inputs from Plan 25-02 (parallel — same wave, but the doc content is locked from CONTEXT/RESEARCH, not from inspecting the workflow file): -->
<!--   - The doc's Recipe section reproduces the env: block values + Compute SOURCE_DATE_EPOCH step + Package step pipeline VERBATIM from CONTEXT D-02/D-03/D-06 — not from reading 25-02's modified release.yml -->
<!--   - This decouples the docs plan from the workflow plan: both can run in parallel in Wave 2 because both read from the same canonical CONTEXT source -->

<!-- Outputs consumed by Plan 25-04: -->
<!--   - docs/REPRODUCIBLE-BUILD.expected-sha256.txt is the verifier's lookup file (D-18 chosen mechanism — line-by-line over markdown-table) -->
<!--   - The verifier greps this file for the active tag and compares against the rebuilt-tarball sha256 -->

<!-- Outputs consumed by Plan 25-05: -->
<!--   - The v1.6.0-rc.0 rehearsal procedure (D-10) replaces both placeholders (`<TBD-v1.6.0-cut>` in the markdown table AND in the `.expected-sha256.txt`) atomically -->

<!-- Existing patterns to mirror (verified at planning time 2026-06-02): -->
<!--   - SECURITY.md §Supply-chain status H3 subsections (L113-180 image, L182-230 tarball) — multi-section structure with prose + numbered list + fenced bash + > Note blockquotes -->
<!--   - The Recipe section uses `<owner>` literal placeholder (not the actual repo owner) — operators substitute when copy-pasting -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create docs/REPRODUCIBLE-BUILD.md with all 7 H2 sections + Recipe bash block + Toolchain pins table + Expected sha256sum placeholder</name>
  <files>docs/REPRODUCIBLE-BUILD.md</files>
  <read_first>
    - .planning/phases/25-reproducible-build-recipe-scheduled-verifier-registry/25-CONTEXT.md §Decisions (D-09 7-section list verbatim; D-10 placeholder; D-17 Recipe section bash block verbatim; D-19 cross-link target for SECURITY.md)
    - .planning/phases/25-reproducible-build-recipe-scheduled-verifier-registry/25-RESEARCH.md §Code Examples (Recipe section verbatim; D-12 issue title formats for §Reporting section)
    - .planning/phases/25-reproducible-build-recipe-scheduled-verifier-registry/25-PATTERNS.md L329-372 (SECURITY.md §Supply-chain status sibling-subsection pattern for prose + numbered-list + fenced-bash + > Note blockquote shape)
    - SECURITY.md lines 113-230 (sibling subsection style — H3 with prose intro + numbered list + fenced bash + > Note blockquotes; mirror this shape at H2 level)
    - rust-toolchain.toml (verify channel value to cite in Toolchain pins table)
    - .planning/phases/25-reproducible-build-recipe-scheduled-verifier-registry/25-CONTEXT.md §Specific Ideas (D-12 issue title formats for §Reporting section)
  </read_first>
  <action>
    Create NEW file `docs/REPRODUCIBLE-BUILD.md` with the following 7 H2 sections in the exact order per D-09, mirroring SECURITY.md's H3 subsection style at H2 level:

    **H1 title:** `# Reproducible builds`

    **Brief lede paragraph** (2-3 sentences): "blindjoin's `blindjoin-linux-amd64.tar.gz` Release archive is reproducible byte-for-byte from source on the pinned `ubuntu-24.04` runner image. Anyone — operator, auditor, or independent reviewer — can rebuild from a fresh checkout and verify the bytes match what the maintainer published. The recipe below is the single canonical procedure; the same recipe is exercised continuously by `.github/workflows/reproducible-verify.yml` (see §Continuous verification)."

    **§1 `## Why this exists`** — 3-sentence operator-facing intro per D-09 §1: what reproducibility proves (the published tarball is the natural product of the source at the tagged commit, not a tampered or arbitrarily-built artifact), why it matters (supply-chain assurance for operators who deploy blindjoin in environments where binary provenance matters), where the continuous verifier lives (cross-link to `.github/workflows/reproducible-verify.yml`).

    **§2 `## Recipe`** — Use CONTEXT §Specific Ideas D-17 + RESEARCH §Code Examples §6 (Recipe bash block) verbatim. Single fenced bash block:
    ```bash
    # On a fresh ubuntu-24.04 runner image (or VM):
    git clone https://github.com/<owner>/blindjoin.git
    cd blindjoin
    git checkout v1.6.0   # or whatever tag you're verifying
    # Toolchain pin resolved by rust-toolchain.toml at repo root.
    # Determinism env vars per REPRO-01:
    export SOURCE_DATE_EPOCH=$(git log -1 --format=%ct HEAD)
    export RUSTFLAGS="--remap-path-prefix=$(pwd)=/build --remap-path-prefix=$HOME/.cargo=/cargo"
    export CARGO_INCREMENTAL=0
    # Build:
    cargo build --release --locked --bin coordinator --bin client --bin liquidity-bot
    # Package (deterministic):
    mkdir -p dist
    cp target/release/coordinator target/release/client target/release/liquidity-bot dist/
    tar --sort=name --owner=0 --group=0 --numeric-owner \
        --mtime="@${SOURCE_DATE_EPOCH}" \
        -cf - -C dist . \
      | gzip -n > blindjoin-linux-amd64.tar.gz
    # Compare against the expected hash in this doc:
    sha256sum blindjoin-linux-amd64.tar.gz
    ```
    Add a 1-line prose lede above the block: "Run on a fresh `ubuntu-24.04` shell (Docker, VM, or GitHub Actions runner)."

    **§3 `## Toolchain pins`** — Markdown table:
    | Component | Version | Pin lives in |
    | --- | --- | --- |
    | rustc | 1.95.0 | [`rust-toolchain.toml`](../rust-toolchain.toml) — workspace root |
    | cargo | 1.95.0 (bundled with rustc) | same as above |
    | ubuntu-24.04 runner image | `<TBD-v1.6.0-cut>` (ImageVersion captured at v1.6.0-rc.0 cut per [docs/RELEASING.md](docs/RELEASING.md) §Reproducibility verification rehearsal) | `.github/workflows/release.yml` + `.github/workflows/reproducible-verify.yml` |
    | `dtolnay/rust-toolchain` action | `@3c5f7ea28cd621ae0bf5283f0e981fb97b8a7af9 # stable` | `.github/workflows/{release,ci}.yml` (6 `with:` blocks; pinned at SHA + value via `rust-toolchain-pin-check` CI gate) |
    | `sigstore/cosign-installer` action | `@7e8b541eb2e61bf99390e1afd4be13a184e9ebc5 # v3.10.1` (cosign-release `v2.6.3`) | `.github/workflows/{release,reproducible-verify}.yml` |
    | `actions/checkout` action | `@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1` | every `.github/workflows/*.yml` |

    **§4 `## Environment`** — Document each env var per D-09 §4 with explicit derivation:
    - `SOURCE_DATE_EPOCH` — derived from `git log -1 --format=%ct $GITHUB_SHA` per REPRO-02; runtime-set in `.github/workflows/release.yml` `Compute SOURCE_DATE_EPOCH from tagged commit time` step; external rebuilders use `git log -1 --format=%ct HEAD` (per Recipe).
    - `RUSTFLAGS` — exact literal: `--remap-path-prefix=<workspace>=/build --remap-path-prefix=<cargo-home>=/cargo`. Strips embedded build-host paths from debug info and panic messages.
    - `CARGO_INCREMENTAL` — exact literal: `0`. Disables incremental compilation, which otherwise embeds host-specific intermediate paths in metadata.

    Add a `> **Note: Rust reproducibility long tail**` blockquote per RESEARCH §Pitfall 6 inheritance: the recipe handles known nondeterminism sources but Rust binary determinism can surface project-specific surprises (proc-macro time calls, build.rs `chrono::Local::now()`, etc.); the continuous verifier (§Continuous verification) is the iteration mechanism.

    **§5 `## Expected sha256sum`** — Markdown table:
    | Release tag | Expected `sha256sum blindjoin-linux-amd64.tar.gz` |
    | --- | --- |
    | v1.6.0 | `<TBD-v1.6.0-cut>` |

    Add a 1-line prose note above the table: "The expected hash is also available in machine-readable form at [`docs/REPRODUCIBLE-BUILD.expected-sha256.txt`](REPRODUCIBLE-BUILD.expected-sha256.txt) — this is what the scheduled verifier (§Continuous verification) reads."

    **§6 `## Continuous verification`** — Cross-link to `.github/workflows/reproducible-verify.yml` (the file will be created in Plan 25-04 — link is forward-compatible because markdown tolerates broken relative links until target exists). 2-3 sentences: the workflow runs monthly on the 1st of each month at 07:00 UTC on a fresh `ubuntu-24.04` runner, downloads the latest release tarball, rebuilds via this Recipe, and asserts `sha256sum` equality. On mismatch, opens a `[reproducibility-regression]` issue (see §Reporting). 1-line placeholder for reproducible-builds.org registry: `Registry entry: <added after blindjoin's submission lands; see [docs/RELEASING.md](docs/RELEASING.md) §Reproducible-builds.org registry submission>` (WARNING 4 fix: use the longer relative path `docs/RELEASING.md` IDENTICAL to the SECURITY.md placeholder in Plan 25-05 so D-14 step 4's atomic substitution against one literal string matches both files).

    **§7 `## Reporting a reproducibility regression`** — Document the `[reproducibility-regression]` issue scheme from D-12:
    - 1-paragraph intro: the verifier opens `[reproducibility-regression]`-labeled issues automatically on mismatch; operators who rebuild and find a divergence can file the same issue manually using the same title scheme.
    - Two title formats (per D-12):
      - Runner-image drift (low-severity): `[reproducibility-regression] runner image drift: ImageVersion <OLD> → <NEW>` — GitHub rotated the `ubuntu-24.04` runner image SHA; verify reproducibility on the new image, update this doc with the new ImageVersion if green, investigate if not. Not a supply-chain signal until investigated.
      - sha256 mismatch (HIGH-severity): `[reproducibility-regression] sha256 mismatch on ImageVersion <V>` — rebuilt tarball diverges from published release on the SAME runner image. Real supply-chain signal — published release does not reproduce from source.

    File ends with a final-line ASCII separator and 1-line metadata: `*Maintained at: docs/REPRODUCIBLE-BUILD.md. Last updated: <date>.*` (Use the current planning date 2026-06-02; Plan 25-05 will bump on rehearsal).
  </action>
  <verify>
    <automated>
      test -f docs/REPRODUCIBLE-BUILD.md &&
      # All 7 H2 sections present in order
      grep -q '^## Why this exists$' docs/REPRODUCIBLE-BUILD.md &&
      grep -q '^## Recipe$' docs/REPRODUCIBLE-BUILD.md &&
      grep -q '^## Toolchain pins$' docs/REPRODUCIBLE-BUILD.md &&
      grep -q '^## Environment$' docs/REPRODUCIBLE-BUILD.md &&
      grep -q '^## Expected sha256sum$' docs/REPRODUCIBLE-BUILD.md &&
      grep -q '^## Continuous verification$' docs/REPRODUCIBLE-BUILD.md &&
      grep -q '^## Reporting a reproducibility regression$' docs/REPRODUCIBLE-BUILD.md &&
      # Recipe content cross-checks
      grep -q 'git clone https://github.com/<owner>/blindjoin\.git' docs/REPRODUCIBLE-BUILD.md &&
      grep -q 'cargo build --release --locked --bin coordinator --bin client --bin liquidity-bot' docs/REPRODUCIBLE-BUILD.md &&
      grep -q -- '--sort=name --owner=0 --group=0 --numeric-owner' docs/REPRODUCIBLE-BUILD.md &&
      grep -q 'gzip -n > blindjoin-linux-amd64\.tar\.gz' docs/REPRODUCIBLE-BUILD.md &&
      grep -q 'export SOURCE_DATE_EPOCH=\$(git log -1 --format=%ct HEAD)' docs/REPRODUCIBLE-BUILD.md &&
      grep -q 'export CARGO_INCREMENTAL=0' docs/REPRODUCIBLE-BUILD.md &&
      # Toolchain pins table contains rustc 1.95.0 + key SHA pins
      grep -q '1\.95\.0' docs/REPRODUCIBLE-BUILD.md &&
      grep -q '3c5f7ea28cd621ae0bf5283f0e981fb97b8a7af9' docs/REPRODUCIBLE-BUILD.md &&
      grep -q '7e8b541eb2e61bf99390e1afd4be13a184e9ebc5' docs/REPRODUCIBLE-BUILD.md &&
      # Expected sha256 placeholder
      grep -q '<TBD-v1\.6\.0-cut>' docs/REPRODUCIBLE-BUILD.md &&
      # Cross-links to companion .expected-sha256.txt + verifier workflow + docs/RELEASING.md
      grep -q 'REPRODUCIBLE-BUILD\.expected-sha256\.txt' docs/REPRODUCIBLE-BUILD.md &&
      grep -q 'reproducible-verify\.yml' docs/REPRODUCIBLE-BUILD.md &&
      grep -q 'RELEASING\.md' docs/REPRODUCIBLE-BUILD.md &&
      # D-12 two-title scheme present in §Reporting
      grep -q 'runner image drift: ImageVersion' docs/REPRODUCIBLE-BUILD.md &&
      grep -q 'sha256 mismatch on ImageVersion' docs/REPRODUCIBLE-BUILD.md &&
      # Min line count
      [ "$(wc -l < docs/REPRODUCIBLE-BUILD.md)" -ge 100 ] &&
      echo "All Task 1 assertions passed"
    </automated>
  </verify>
  <done>
    `docs/REPRODUCIBLE-BUILD.md` exists with all 7 H2 sections in D-09 order. Recipe section contains the verbatim D-17 bash block with all required env vars, cargo invocation, and tar/gzip pipeline. Toolchain pins table cites rustc 1.95.0, the 3 SHA-pinned actions, and the runner image placeholder. Expected sha256sum table contains the `<TBD-v1.6.0-cut>` placeholder. §Reporting contains both D-12 title formats. File is ≥100 lines and cross-links to companion .expected-sha256.txt + reproducible-verify.yml + docs/RELEASING.md.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create docs/REPRODUCIBLE-BUILD.expected-sha256.txt companion file (D-18 lookup mechanism)</name>
  <files>docs/REPRODUCIBLE-BUILD.expected-sha256.txt</files>
  <read_first>
    - .planning/phases/25-reproducible-build-recipe-scheduled-verifier-registry/25-CONTEXT.md (D-18 — planner discretion, RESEARCH recommends separate file over markdown-table parse)
    - .planning/phases/25-reproducible-build-recipe-scheduled-verifier-registry/25-RESEARCH.md §Standard Stack Alternatives Considered table + §Open Question #2 (the v1.6.0-rc.0 rehearsal procedure replaces this placeholder per D-10)
    - .planning/phases/25-reproducible-build-recipe-scheduled-verifier-registry/25-PATTERNS.md (no analog for this file — it's a NEW simple machine-readable format)
  </read_first>
  <action>
    Create NEW file `docs/REPRODUCIBLE-BUILD.expected-sha256.txt` per D-18 (planner chooses separate colon-delimited file over markdown-table parse — RESEARCH recommended this as the more robust option). The file format is line-by-line `<tag>:<sha256>:<image-version>` triples (colon-delimited single-source-of-truth carrying BOTH the expected sha256 AND the pinned ubuntu-24.04 ImageVersion that the Plan 25-04 verifier needs for D-12 drift-vs-divergence classification), plus a brief `#`-comment header explaining the format + cross-link to the maintainer-facing doc.

    The colon-delimited single-file format is the BLOCKER 2 fix: the verifier in Plan 25-04 derives BOTH EXPECTED_SHA256 and PINNED_IMAGE_VERSION from this file via a single `awk -F: '$1 == "'"$LATEST_TAG"'" {print $2 " " $3}'` lookup — replacing the broken `grep ImageVersion.{0,5}[0-9.]+ docs/REPRODUCIBLE-BUILD.md` approach (the markdown table's label column does NOT contain the literal token "ImageVersion", so the grep returns nothing → UNKNOWN → guard fails → every mismatch was mis-classified as HIGH-severity sha256-divergence-on-same-image). One source of truth, one substitution site per release, atomic rehearsal procedure.

    Initial content (exact bytes):
    ```
    # Phase 25 REPRO-01 / D-18: machine-readable expected-sha256 + ImageVersion lookup table.
    # Format: <tag>:<sha256hex>:<image-version>   (colon-delimited, one per line, no whitespace, no quotes)
    #   - <tag>           — release tag (e.g. v1.6.0)
    #   - <sha256hex>     — expected sha256 of blindjoin-linux-amd64.tar.gz (64 hex chars)
    #   - <image-version> — pinned ubuntu-24.04 runner ImageVersion (e.g. 20260520.1.0)
    # Comment lines (starting with #) and blank lines are ignored by the verifier.
    # The verifier (.github/workflows/reproducible-verify.yml) derives BOTH the
    # expected sha256 AND the pinned ImageVersion from this file in a single
    # awk -F: lookup. The markdown table in docs/REPRODUCIBLE-BUILD.md is
    # human-facing only — verifier never parses markdown (per Phase 25 BLOCKER 2 fix).
    # See docs/REPRODUCIBLE-BUILD.md §Expected sha256sum for the human-readable table.
    # The v1.6.0-rc.0 rehearsal procedure (docs/RELEASING.md §Reproducibility
    # verification rehearsal) captures both real values and replaces both placeholders.

    v1.6.0:<TBD-v1.6.0-cut-sha256>:<TBD-v1.6.0-cut-imageversion>
    ```

    File ends with a final newline. No trailing whitespace anywhere. Use exact spellings `<TBD-v1.6.0-cut-sha256>` and `<TBD-v1.6.0-cut-imageversion>` so Plan 25-05's rehearsal procedure can substitute both in two sed passes (one per placeholder) against this single file, then substitute the ImageVersion placeholder a third time against the human-facing `docs/REPRODUCIBLE-BUILD.md` markdown table.
  </action>
  <verify>
    <automated>
      test -f docs/REPRODUCIBLE-BUILD.expected-sha256.txt &&
      # Format: tag:sha:imageversion triple present (colon-delimited)
      grep -qE '^v1\.6\.0:<TBD-v1\.6\.0-cut-sha256>:<TBD-v1\.6\.0-cut-imageversion>$' docs/REPRODUCIBLE-BUILD.expected-sha256.txt &&
      # Comment header present (updated for new format)
      grep -q 'Phase 25 REPRO-01 / D-18' docs/REPRODUCIBLE-BUILD.expected-sha256.txt &&
      grep -q '<tag>:<sha256hex>:<image-version>' docs/REPRODUCIBLE-BUILD.expected-sha256.txt &&
      # Single non-comment data line
      [ "$(grep -vE '^(#|$)' docs/REPRODUCIBLE-BUILD.expected-sha256.txt | wc -l | tr -d ' ')" -eq 1 ] &&
      # Awk lookup returns BOTH sha256 and ImageVersion in one pass
      LOOKUP=$(awk -F: '$1 == "v1.6.0" {print $2 " " $3}' docs/REPRODUCIBLE-BUILD.expected-sha256.txt) &&
      [ "$LOOKUP" = "<TBD-v1.6.0-cut-sha256> <TBD-v1.6.0-cut-imageversion>" ] &&
      # Cross-link to docs/REPRODUCIBLE-BUILD.md
      grep -q 'docs/REPRODUCIBLE-BUILD\.md' docs/REPRODUCIBLE-BUILD.expected-sha256.txt &&
      # Final newline present
      [ "$(tail -c 1 docs/REPRODUCIBLE-BUILD.expected-sha256.txt | od -An -c | tr -d ' ')" = "\\n" ] &&
      echo "All Task 2 assertions passed"
    </automated>
  </verify>
  <done>
    `docs/REPRODUCIBLE-BUILD.expected-sha256.txt` exists with a `#`-comment header explaining the new colon-delimited `<tag>:<sha256>:<image-version>` format + cross-link, and exactly one non-comment data line `v1.6.0:<TBD-v1.6.0-cut-sha256>:<TBD-v1.6.0-cut-imageversion>`. Plan 25-04's verifier reads BOTH the expected sha256 AND the pinned ImageVersion from this file via a single `awk -F: '$1 == "'"$LATEST_TAG"'" {print $2 " " $3}'` lookup (replacing the broken markdown-table ImageVersion grep). Plan 25-05's rehearsal procedure substitutes both placeholders in dedicated sed passes per the docs/RELEASING.md procedure.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → recipe document | External rebuilder reads the Recipe bash block and executes it in their own shell — the doc must be runnable end-to-end on a fresh ubuntu-24.04 shell (Pitfall 12) |
| markdown table → verifier | The expected sha256 value crosses from the markdown table / .expected-sha256.txt file into the verifier's comparison logic — drift between the two sources is the threat |
| docs/REPRODUCIBLE-BUILD.md → SECURITY.md cross-link | Plan 25-05's `### Reproducibility (v1.6 onward)` subsection in SECURITY.md cross-links here; drift in the recipe between the two files is the threat (mitigation: SECURITY.md cites this file as canonical, doesn't reproduce the full recipe) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03-01 | Information Disclosure | External rebuilder follows out-of-date Recipe and gets a different hash, mistakes it for tampering | mitigate | Expected sha256sum table is per-tag; mismatch is immediately diagnosable. Continuous verifier (Plan 25-04) catches doc-vs-workflow drift monthly. |
| T-25-03-02 | Tampering | Recipe bash block deviates from the actual release.yml build job — operators rebuild and never get byte-equality | mitigate | Recipe is sourced verbatim from CONTEXT D-17 (which itself sources from D-02/D-03/D-05/D-06 — same canonical source as Plan 25-02). The continuous verifier rebuilds via the SAME Recipe each month — silently-drifted Recipe surfaces as a `[reproducibility-regression]` HIGH-severity issue on the next monthly run. |
| T-25-03-03 | Tampering | `<TBD-v1.6.0-cut>` placeholder ships with a real release — verifier compares against the placeholder string and silently passes | mitigate | The placeholder string contains `<` and `>` characters that NEVER appear in a real sha256 hex string; verifier's compare logic in Plan 25-04 treats placeholder vs real-hex mismatch as HIGH-severity. Optional CI gate (D-10 planner discretion — covered in Plan 25-05): `! grep -q '<TBD-' docs/REPRODUCIBLE-BUILD.expected-sha256.txt` after rehearsal. |
| T-25-03-04 | Repudiation | Maintainer updates one file (markdown table) but not the other (.expected-sha256.txt) at rehearsal → verifier compares against stale value | mitigate | Both placeholders use the EXACT SAME literal string `<TBD-v1.6.0-cut>`; Plan 25-05's atomic-substitution procedure (D-10) replaces both in one operation per the docs/RELEASING.md rehearsal section. |
| T-25-03-05 | Denial of Service | docs/REPRODUCIBLE-BUILD.md cross-links to `.github/workflows/reproducible-verify.yml` which does not yet exist (Plan 25-04 wave) — broken link until 25-04 ships | accept | Markdown tolerates broken relative links; the cross-link is forward-compatible. Plan 25-04 lands in Wave 3, after 25-03; by the time the doc is operator-visible (post-commit), the link resolves. |
</threat_model>

<verification>
After both tasks complete:

1. `docs/REPRODUCIBLE-BUILD.md` exists with all 7 H2 sections in D-09 order.
2. The Recipe section's fenced bash block matches D-17 verbatim (git clone, env exports, cargo build, tar/gzip, sha256sum).
3. Toolchain pins table includes rows for rustc 1.95.0, ubuntu-24.04 runner (placeholder), and the 3 SHA-pinned actions (dtolnay/rust-toolchain, sigstore/cosign-installer, actions/checkout).
4. Expected sha256sum table contains `v1.6.0: <TBD-v1.6.0-cut>` placeholder.
5. §Continuous verification cross-links to `.github/workflows/reproducible-verify.yml` + names the monthly cadence.
6. §Reporting contains both D-12 title formats.
7. `docs/REPRODUCIBLE-BUILD.expected-sha256.txt` exists with a `#`-comment header + exactly one non-comment data line `v1.6.0:<TBD-v1.6.0-cut-sha256>:<TBD-v1.6.0-cut-imageversion>` (colon-delimited triple per BLOCKER 2 fix).
8. The `.expected-sha256.txt` line uses distinct placeholders for sha256 vs ImageVersion so Plan 25-05's rehearsal procedure replaces each via dedicated sed passes; the markdown table's `<TBD-v1.6.0-cut>` ImageVersion placeholder is human-facing only (verifier never parses markdown).
9. The Recipe section's bash block is runnable end-to-end on a fresh `ubuntu-24.04` shell (Pitfall 12) — manual smoke verification deferred to Plan 25-05's rehearsal step.
</verification>

<success_criteria>
- REPRO-01 requirement fully addressed (the operator-facing doc half — the release.yml env half lands in Plan 25-02, the continuous verification half lands in Plan 25-04)
- D-09 (7-section structure), D-10 (`<TBD-v1.6.0-cut>` placeholder), D-15 (rustc 1.95.0 documented), D-17 (Recipe bash block), D-18 (separate `.expected-sha256.txt` file mechanism) all implemented
- External rebuilder can copy the Recipe section verbatim and run it on a fresh `ubuntu-24.04` shell (Pitfall 12 contract)
- `.expected-sha256.txt` carries colon-delimited `<tag>:<sha256>:<image-version>` triples (BLOCKER 2 fix) so the verifier in Plan 25-04 derives BOTH expected sha256 AND pinned ImageVersion from a single `awk -F:` lookup; the markdown table retains a `<TBD-v1.6.0-cut>` placeholder for human readability only — verifier never parses markdown
- Cross-links are forward-compatible: `.github/workflows/reproducible-verify.yml` is referenced but not yet created (lands in Plan 25-04 Wave 3)
</success_criteria>

<output>
Create `.planning/phases/25-reproducible-build-recipe-scheduled-verifier-registry/25-03-SUMMARY.md` when done. Include:
- Final line count of `docs/REPRODUCIBLE-BUILD.md`
- Confirmation that `.expected-sha256.txt` uses the new colon-delimited `<tag>:<sha256>:<image-version>` triple format (BLOCKER 2 fix) with distinct sha256 + ImageVersion placeholders
- Note that the verifier in Plan 25-04 reads `.expected-sha256.txt` for BOTH expected sha256 AND pinned ImageVersion via a single `awk -F:` lookup (the D-18 chosen mechanism, extended per BLOCKER 2 to carry both values)
- Note that Plan 25-05 will (a) replace both placeholders atomically per D-10 rehearsal procedure, (b) append the reproducible-builds.org registry entry URL to §Continuous verification once D-14 lands
</output>
