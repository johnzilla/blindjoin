---
phase: 03-client-cli
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - tests/integration/full_round.rs
autonomous: true
requirements:
  - TEST-09
  - TEST-10
  - TEST-11
  - TEST-12

must_haves:
  truths:
    - "Replay token attack: coordinator returns 422 when a previously used unblinded token is submitted again for output registration"
    - "Invalid UTXO attack: coordinator returns 4xx when UTXO does not exist in chain"
    - "Wrong denomination attack: coordinator returns 4xx when output amount does not match denomination"
    - "Tampered PSBT: client verify_and_sign returns Err when PSBT has fewer denomination outputs than participants_registered"
    - "Round restart after blame: after blame fires and non-signer is banned, remaining participants can run a new round that completes successfully"
    - "All adversarial and restart tests skip gracefully when bitcoind is not in PATH"
  artifacts:
    - path: "tests/integration/full_round.rs"
      provides: "adversarial_scenarios and round_restart_after_blame test functions"
      contains: "async fn adversarial_scenarios"
  key_links:
    - from: "tests/integration/full_round.rs adversarial_scenarios"
      to: "coordinator /round/output replay check"
      via: "POST /round/output with same unblinded_token twice → second returns error"
    - from: "tests/integration/full_round.rs round_restart_after_blame"
      to: "coordinator FSM restart after blame"
      via: "blame fires, coordinator returns to input_reg, remaining clients complete"
---

<objective>
Add adversarial integration tests (TEST-11) and round-restart-after-blame test (TEST-12)
to tests/integration/full_round.rs.

Purpose: Tests-09 and TEST-10 already pass from Phase 1/2 work. This plan closes the
remaining two test requirements by implementing adversarial and restart scenarios.

Output:
- adversarial_scenarios: 4 sub-scenarios in one test (replay token, invalid UTXO, wrong denomination, tampered PSBT)
- round_restart_after_blame: blame fires, non-signer banned, two remaining participants
  complete a fresh round successfully
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-client-cli/03-01-SUMMARY.md
@tests/integration/full_round.rs
@shared/src/protocol.rs
@coordinator/src/api/handlers.rs
@coordinator/src/round/blame.rs
@client/src/round/sign.rs
@client/src/round/mod.rs
</context>

<interfaces>
<!-- Coordinator error handling for adversarial inputs:
  POST /round/output with replayed token → HTTP 422 (REPLAY_TOKEN error code)
  POST /round/input with non-existent UTXO → HTTP 404 or 422 (UTXO_NOT_FOUND)
  POST /round/output with wrong denomination → HTTP 422 (WRONG_DENOMINATION)
  client::round::sign::verify_and_sign with tampered PSBT → Err (output count mismatch, CLI-04)

Error response shape (from shared/errors.rs):
  {"error": {"code": "REPLAY_TOKEN", "message": "...", "round_id": "..."}}

The adversarial tests make raw HTTP requests (not through the client library) to inject
invalid data that the client library would reject before sending.
-->

<!-- Round restart after blame flow:
  1. Coordinator starts in InputReg (min_participants=2, max_participants=3)
  2. 3 clients register inputs and outputs
  3. Client 0 does NOT sign (blame fires after signing timeout)
  4. Coordinator returns to InputReg (blame restart with min_participants=2 remaining)
  5. Same 2 remaining clients register inputs + outputs + sign in the new round
  6. CoinJoin tx appears in mempool

  NOTE: after blame restart, the coordinator transitions back to InputReg but with
  the banned UTXO (client 0) blocked. Clients 1 and 2 must re-register.
  The round may need min_participants=2 to succeed with only 2 participants.
  Set min_participants=2, max_participants=3 in spawn_coordinator_with_blame (already done).

  The round_restart_after_blame test reuses spawn_coordinator_with_blame from existing
  test helpers, which sets signing_timeout=2s and min_participants=2.
-->

<!-- Tampered PSBT test: This tests client-side rejection, not coordinator rejection.
  Use the existing verify_and_sign function with a crafted PSBT that has one fewer
  denomination output than participants_registered in InputRegState.
  No bitcoind needed for this sub-test — construct PSBT in memory.
  Add as a unit test directly in client/src/round/sign.rs OR as a dedicated
  adversarial_tampered_psbt_rejected test in integration/full_round.rs.
  Since it needs no bitcoind: add as a standalone #[tokio::test] in full_round.rs.
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Adversarial integration tests (TEST-11)</name>
  <files>tests/integration/full_round.rs</files>
  <read_first>
    - tests/integration/full_round.rs — full file (append new tests after existing ones)
    - shared/src/protocol.rs — wire types for raw HTTP adversarial requests
    - coordinator/src/api/handlers.rs — error codes returned for invalid inputs
    - client/src/round/sign.rs — verify_and_sign for tampered PSBT scenario
    - client/src/round/mod.rs — InputRegState (tampered PSBT test needs it)
  </read_first>
  <action>
    Append four test functions to tests/integration/full_round.rs.

    **Test 1: adversarial_replay_token (TEST-11 sub-scenario 1)**
    - Requires bitcoind (skip gracefully if missing).
    - Setup: fund 3 UTXOs, spawn coordinator (min=3, max=3, denomination=100_000).
    - Run all 3 clients through input registration.
    - Client 0: complete output registration successfully (first call → 200 accepted).
    - Client 0: attempt output registration a SECOND time with the SAME unblinded_token and signature.
    - Assert: second POST /round/output returns HTTP 4xx (coordinator rejects replay).
    - Use reqwest::Client directly for the second call (bypass client library).
    - The coordinator's replayed token check is in handlers.rs post_output — it checks
      AppState.round_state redeemed_tokens HashSet.
    - Assert response status is NOT 200 (expect 422 or 400).

    **Test 2: adversarial_invalid_utxo (TEST-11 sub-scenario 2)**
    - Does NOT require bitcoind for the adversarial check itself, but coordinator's
      UTXO validation hits bitcoind. Use an in-process coordinator with a real bitcoind.
    - Spawn coordinator (min=3, denomination=100_000). Start in input_reg.
    - Submit POST /round/input with a fabricated outpoint that does not exist on chain:
      utxo_outpoint: "a0a0a0a0a0a0a0a0a0a0a0a0a0a0a0a0a0a0a0a0a0a0a0a0a0a0a0a0a0a0a0a0:0"
      ownership_proof: valid-looking but for a non-existent UTXO.
    - Assert: response status is 4xx (UTXO_NOT_FOUND or similar).
    - Actually: the coordinator calls bitcoind RPC gettxout — if UTXO not found, returns error.
    - The coordinator uses BitcoinRpc.get_tx_out() which returns None for non-existent UTXOs.
    - Expect HTTP 422 or 404.
    - This test DOES require bitcoind (so we can have a real RPC endpoint). Use the corepc-node setup.

    **Test 3: adversarial_wrong_denomination (TEST-11 sub-scenario 3)**
    - Setup: 3 clients register inputs successfully.
    - Client 0 submits POST /round/output with amount_sats != denomination_sats (e.g., 50_000 vs 100_000).
    - Assert: response is 4xx (coordinator rejects wrong denomination output).
    - Use raw reqwest to send OutputRegRequest with amount_sats: 50_000.
    - Expect HTTP 422.

    **Test 4: adversarial_tampered_psbt_rejected (TEST-11 sub-scenario 4)**
    - Does NOT require bitcoind. Pure in-memory test.
    - Construct a fake InputRegState with participants_registered=3 and denomination_sats=100_000.
    - Build a minimal PSBT (or use bitcoin::Psbt::from unsigned tx) with only 2 denomination
      outputs (not 3).
    - Call verify_and_sign logic directly (or extract the output count check to a pure function
      `check_psbt_outputs(psbt, denom_sats, participants) -> Result<()>` in sign.rs and test that).
    - Assert: check returns Err containing "output censorship" or "denomination outputs".
    - This is the tampered PSBT safety net from CLI-04 (Task 2 of Plan 01).
    - If check_psbt_outputs is not extracted, use the full verify_and_sign flow with a
      mock coordinator URL that returns a tampered PSBT (use a tiny axum server).
    - SIMPLEST: extract `pub fn check_psbt_denomination_outputs(psbt: &Psbt, denomination_sats: u64, expected_count: u32) -> Result<()>` from sign.rs and unit-test it in sign.rs test block AND reference it in the integration test via `client::round::sign::check_psbt_denomination_outputs`.

    Each test is its own `#[tokio::test] async fn` function.
    All four skip gracefully when bitcoind is unavailable (tests 1-3 use `corepc_node::exe_path()` guard; test 4 needs no guard).

    Implementation notes:
    - For replay/denomination tests: reuse the FundedSetup + spawn_coordinator pattern from existing tests.
    - Extract a helper `fund_and_spawn(exe, denomination, min_participants)` at the top of
      full_round.rs to reduce duplication (optional — only if it significantly shortens the file).
    - Raw HTTP requests: use `reqwest::Client::new().post(url).json(&body).send().await`.
    - Error code assertions: `.status().is_client_error()` is sufficient (don't hard-code 422).
  </action>
  <verify>
    <automated>cd /home/john/vault/projects/github.com/blindjoin && cargo test --test integration adversarial 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test integration adversarial` shows 4 new tests (or 3 if tampered_psbt is unit test) passing
    - `grep "adversarial_replay_token\|adversarial_invalid_utxo\|adversarial_wrong_denomination\|adversarial_tampered" tests/integration/full_round.rs` returns at least 3 matches
    - `cargo test --test integration` shows all previously passing tests still pass
    - No test panics or hangs (each adversarial test completes within 30s)
    - `grep "exe_path\|skipping" tests/integration/full_round.rs | wc -l` shows graceful skip guards in new tests
  </acceptance_criteria>
  <done>
    Adversarial scenarios (replay, invalid UTXO, wrong denomination, tampered PSBT) all
    produce correct rejections. Tests are gracefully skipped when bitcoind is unavailable.
  </done>
</task>

<task type="auto">
  <name>Task 2: Round-restart-after-blame integration test (TEST-12)</name>
  <files>tests/integration/full_round.rs</files>
  <read_first>
    - tests/integration/full_round.rs — full file (the blame_non_signer_timeout test shows
      the pattern for blame setup; this test extends that by running a second full round)
    - coordinator/src/round/blame.rs — on_signing_timeout, BlameOutcome::RestartWithout
    - coordinator/src/round/state.rs — Phase enum, transitions after blame restart
    - coordinator/src/api/handlers.rs — post_input ban check behavior after blame
  </read_first>
  <action>
    Append test function `round_restart_and_completion_after_blame` to full_round.rs.

    **Test flow:**
    1. Skip if bitcoind unavailable (corepc_node::exe_path() guard).
    2. Fund 3 UTXOs on regtest (same setup as existing tests). Use denomination=100_000.
    3. Spawn coordinator: min_participants=2, max_participants=3, signing_timeout=2s,
       ban_duration=3600s. Use spawn_coordinator_with_blame (already exists in test file).
    4. All 3 clients register inputs + outputs in round 1. Client 0 does NOT sign.
    5. Wait for signing timeout to fire (tokio::time::sleep(Duration::from_secs(4))).
    6. Assert coordinator is in input_reg phase (blame restarted round).
       GET /info → round_state == "input_reg"
    7. Assert client 0's UTXO is banned: POST /round/input with client 0's UTXO
       → HTTP 403 (UTXO_BANNED error).
    8. Clients 1 and 2 register inputs in the new round.
    9. Clients 1 and 2 register outputs.
    10. Clients 1 and 2 sign the PSBT.
    11. Wait for coordinator to broadcast (sleep 2s).
    12. Assert CoinJoin tx is in mempool (raw mempool check via corepc_node).
    13. Assert the tx has exactly 2 denomination outputs.

    Implementation notes:
    - The blame timeout task is already wired in spawn_coordinator_with_blame to call
      on_signing_timeout → BlameOutcome::RestartWithout → coordinator transitions to InputReg.
    - After blame restart, the coordinator is in InputReg with the banned UTXO blocked.
    - Clients 1 and 2 need fresh BIP-322 proofs for the new round (new round_id from /info).
    - The client library's register_input reads round_id from InfoResponse and builds the
      BIP-322 message around it — so calling register_input again with the new /info round_id
      produces a fresh valid proof automatically.
    - The test spawns FRESH client task handles for clients 1 and 2 for round 2 (re-register).
    - Ban check for client 0: send raw POST /round/input with client 0's UTXO after blame.
      Assert HTTP 403.

    Error format for banned UTXO: the coordinator returns {"error": {"code": "UTXO_BANNED", ...}}
    with HTTP 403. Use `.status().as_u16() == 403` assertion.

    Ensure the test does not hardcode timing-sensitive sleeps beyond what's needed:
    - After blame: sleep 4s (signing_timeout=2s + 2s buffer). Already established pattern.
    - After round 2 signing: sleep 2s for broadcast.
  </action>
  <verify>
    <automated>cd /home/john/vault/projects/github.com/blindjoin && cargo test --test integration round_restart 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test integration round_restart_and_completion_after_blame` passes (when bitcoind available) or shows graceful skip
    - `cargo test --test integration` shows all 5+ tests passing (3 original + 2+ new)
    - `grep "round_restart_and_completion_after_blame" tests/integration/full_round.rs` returns the function
    - The test asserts: banned UTXO gets 403, second round tx appears in mempool with 2 denomination outputs
  </acceptance_criteria>
  <done>
    Round-restart-after-blame test: non-signer banned, remaining 2 clients complete a fresh
    round and CoinJoin tx appears in mempool with 2 denomination outputs.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → coordinator HTTP | Integration tests send adversarial inputs to coordinator |
| coordinator → bitcoind RPC | Coordinator validates UTXOs against live chain state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Repudiation | Replay token in output registration | mitigate | TEST-11 adversarial_replay_token verifies coordinator rejects replayed unblinded tokens |
| T-03-08 | Spoofing | Fabricated UTXO in input registration | mitigate | TEST-11 adversarial_invalid_utxo verifies coordinator rejects non-existent UTXOs via RPC check |
| T-03-09 | Tampering | Wrong denomination output | mitigate | TEST-11 adversarial_wrong_denomination verifies coordinator enforces denomination equality |
| T-03-10 | Tampering | Tampered PSBT missing outputs | mitigate | TEST-11 adversarial_tampered_psbt verifies client-side CLI-04 check refuses to sign |
| T-03-11 | Denial of Service | Non-signer stalls round indefinitely | mitigate | TEST-12 verifies blame fires, non-signer banned, remaining participants complete round |
| T-03-12 | Elevation of Privilege | Banned UTXO re-registers after blame | mitigate | TEST-12 verifies banned UTXO receives HTTP 403 on second registration attempt |
</threat_model>

<verification>
After both tasks complete:

```bash
# All unit tests pass
cargo test -p client
cargo test -p coordinator
cargo test -p shared

# All integration tests pass (5+ total)
cargo test --test integration -- --nocapture 2>&1 | grep -E "PASSED|passed|failed"

# Adversarial tests specifically
cargo test --test integration adversarial -- --nocapture

# Restart test specifically
cargo test --test integration round_restart -- --nocapture
```
</verification>

<success_criteria>
1. `cargo test --test integration` shows 5+ tests: full_round_three_clients, blame_non_signer_timeout, coordinator_info_endpoint_fields, adversarial_*, round_restart_and_completion_after_blame
2. All adversarial scenarios (replay, invalid UTXO, wrong denomination, tampered PSBT) produce correct rejections
3. round_restart_and_completion_after_blame: second round completes with 2 denomination outputs in mempool
4. Banned UTXO receives HTTP 403 when attempting re-registration after blame
5. All tests skip gracefully when bitcoind is not in PATH (no false failures in CI without bitcoind)
</success_criteria>

<output>
After completion, create `.planning/phases/03-client-cli/03-02-SUMMARY.md`
</output>
