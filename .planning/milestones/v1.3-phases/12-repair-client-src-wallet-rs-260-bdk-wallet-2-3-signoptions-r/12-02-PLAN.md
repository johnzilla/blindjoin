---
phase: 12-repair-client-src-wallet-rs-260-bdk-wallet-2-3-signoptions-r
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - tests/integration/full_round.rs
autonomous: true
requirements:
  - REPAIR-01
tags:
  - integration-tests
  - full-round
  - unmute
  - bitcoind-pinned
  - phase-10-carve-out
  - per-test-commit-cycle
  - plan-11-02-verbatim
  - canonical-first
  - d-11-escape-valve
must_haves:
  truths:
    - "All six #[ignore = TODO(Phase-10): ...] attribute lines in tests/integration/full_round.rs (originally at lines 164, 462, 730, 854, 911, 1236) are removed — one per atomic commit, in canonical-first then file order (D-04 inheriting Plan 11-02's D-05)"
    - "The 1st unmute is full_round_three_clients (originally line 164) — mandatory canonical-first gate; if it fails after Plan 12-01's wallet fix is in history, a 5th orthogonal blocker has appeared and Plan 12-02 HALTS per D-11 escape-valve (Phase 13 absorbs per D-12)"
    - "The remaining 5 unmutes follow in file order: blame_non_signer_timeout (originally line 462), adversarial_replay_token (originally line 730), adversarial_invalid_utxo (originally line 854), adversarial_wrong_denomination (originally line 911), round_restart_and_completion_after_blame (originally line 1236)"
    - "Each of the 8 full_round tests passes locally against pinned brew bitcoind v31 via the CONTRIBUTING.md canonical invocation (BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round) — 2 originally-passing + 6 newly-unmuted = 8 passed / 0 failed / 0 ignored"
    - "Each of the 6 unmute commits has its PASS verdict line captured in the commit body (CD-1 minimal stamp), plus TWO SHA references: cc20f6fbca4d292bf7b394a3850b18d244b5b602 (Phase 11 Plan 01 RSA fix) AND Plan 12-01's wallet-fix SHA (re-captured at execute time via `git log --grep='^fix(12):' --format=%H -1`), plus `bitcoind --version | head -1` output (CD-4)"
    - "Zero test-body edits, zero drive-by edits, zero new dependencies, zero CI changes, zero source-of-record-doc edits during the cycle — only the 6 attribute lines are deleted (Plan 11-02 D-07 inherited verbatim)"
    - "If the 1st unmute (full_round_three_clients) fails, the executor halts per D-11 (no later unmute attempted, no #[ignore] line deleted, CHECKPOINT REACHED emitted naming the new failure signature). If any 5th orthogonal blocker surfaces during the cycle on any of the remaining 5 unmutes, the executor halts on FIRST encounter, no later iteration is attempted, earlier commits remain bisect-clean (D-11 escape-valve = ZERO pre-authorized in-flight scope expansion)"
    - "After all 6 unmutes land green, REPAIR-01 is locally closed: REQUIREMENTS.md REPAIR-01 status flips to [x], ROADMAP.md Phase 12 entry is updated. REPAIR-02 status is UNCHANGED — it closes on PR observation of the corepc-node-feature-pin-check CI job (already landed at 4026f50) per Phase 11 D-11 inherited"
    - "Decision coverage: D-03 (REPAIR-01 closes in Phase 12 on green local suite), D-04 (reuse Plan 11-02 unmute spec verbatim — canonical-first order, per-test commit cycle, CD-1 body shape), D-05 (two-plan structure; this is plan 12-02 depending on 12-01), D-11 (escape-valve halt-and-surface on 5th orthogonal blocker), D-12 (Phase 13 absorbs any 5th-blocker overflow), D-13 (mark REPAIR-01 [x] in REQUIREMENTS.md and update ROADMAP.md after green suite), D-14 (v1.3 ship notes NOT in scope), CD-1 (commit subjects shift from test(11): to test(12):), CD-2 ($(brew --prefix) form preserved), CD-4 (capture bitcoind --version in commit bodies)"
  artifacts:
    - path: "tests/integration/full_round.rs"
      provides: "Six tests unmuted (no #[ignore] attributes remain on the six canonical Phase-10 carve-out sites); all 8 full_round tests run end-to-end without --include-ignored"
      contains: "async fn full_round_three_clients"
  key_links:
    - from: "tests/integration/full_round.rs (6 unmuted tests)"
      to: "client/src/wallet.rs (Wallet::sign with trust_witness_utxo: true, Plan 12-01 wallet fix)"
      via: "Each full round exercises sign_psbt_input via round::sign::verify_and_sign on each per-client task"
      pattern: "trust_witness_utxo: true"
    - from: "tests/integration/full_round.rs (6 unmuted tests)"
      to: "client/src/round/input.rs:40 (BjPublicKey::from_spki, Phase 11 Plan 01 RSA decode fix at cc20f6f)"
      via: "The input registration phase of each full round exercises the SPKI decode path"
      pattern: "BjPublicKey::from_spki"
    - from: "tests/integration/full_round.rs (6 unmuted tests)"
      to: "tests/integration/mod.rs (require_bitcoind!, BitcoindGuard, fund_regtest, FundedSetup — Phase 9/10 fixtures)"
      via: "Each unmuted test uses the post-Fix-A shared fixtures unchanged"
      pattern: "crate::fund_regtest|require_bitcoind!"
---

<objective>
Re-execute Plan 11-02's six-unmute cycle verbatim against the post-Plan-12-01 codebase (now containing the bdk_wallet 2.3 SignOptions repair at cc20f6f-from-Phase-11 + Plan-12-01-wallet-fix in history). Remove the six `#[ignore = "TODO(Phase-10): ..."]` attribute lines in `tests/integration/full_round.rs` — one atomic commit per unmute, in canonical-first then file order (D-04 inheriting Plan 11-02 D-05), each with a CD-1 PASS-proof body stamp containing the per-test cargo invocation, the PASS verdict line, TWO SHA references (cc20f6f from Phase 11 + Plan 12-01's wallet-fix SHA), and `bitcoind --version | head -1` output (CD-4).

Purpose: This plan closes REPAIR-01 locally (D-03 + D-13). The 1st unmute (`full_round_three_clients` at originally line 164) is the canonical happy-path gate — if it passes against the Plan-12-01-repaired wallet, the remaining 5 in file order are mechanical; if it fails, a 5th orthogonal blocker has appeared (after Phase 11 Plan 11-02's 4 known blockers) and the D-11 escape-valve fires (Phase 13 absorbs per D-12). REPAIR-02 is NOT covered in-phase — it closes on PR observation when the `corepc-node-feature-pin-check` CI job (already landed at commit 4026f50 per Phase 10) is observed green on the Phase 11+12 combined PR.

Output: Six atomic commits, each `test(12): unmute <test_name> (Phase-10 carve-out N/6)`, each with a body containing the single-line cargo invocation, the PASS verdict line, two SHA references (RSA fix + Wallet fix), and `bitcoind --version | head -1` output. After the 6th commit lands, `cargo test --test integration full_round` reports 8 passed / 0 failed / 0 ignored locally. REQUIREMENTS.md REPAIR-01 flips to `[x]` and ROADMAP.md Phase 12 entry is updated (D-13) as the post-cycle wrap-up.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-repair-client-src-wallet-rs-260-bdk-wallet-2-3-signoptions-r/12-CONTEXT.md
@.planning/phases/12-repair-client-src-wallet-rs-260-bdk-wallet-2-3-signoptions-r/12-RESEARCH.md
@.planning/phases/12-repair-client-src-wallet-rs-260-bdk-wallet-2-3-signoptions-r/12-PATTERNS.md
@.planning/phases/12-repair-client-src-wallet-rs-260-bdk-wallet-2-3-signoptions-r/12-01-PLAN.md
@.planning/phases/11-coordinator-rsa-pubkey-encoding-full-round-rs-unmute-complet/11-02-PLAN.md
@.planning/phases/11-coordinator-rsa-pubkey-encoding-full-round-rs-unmute-complet/11-02-SUMMARY.md
@CONTRIBUTING.md
@tests/integration/full_round.rs
@tests/integration/mod.rs

<interfaces>
<!-- The six attribute pairs the executor edits. The line BELOW each #[ignore] (the async fn declaration) and the line ABOVE (#[tokio::test]) are preserved byte-identical. -->

The six unmute sites, in canonical-first then file order per D-04 (inheriting Plan 11-02 D-05). Line numbers are the ORIGINAL pre-Phase-11 positions, captured here for reference; after each delete in this cycle, all subsequent line numbers SHIFT DOWN BY ONE per prior delete. The executor MUST re-grep for `#[ignore = "TODO(Phase-10):` adjacent to the named test fn before each iteration — DO NOT trust the pre-Phase-11 line numbers literally after the first delete.

  Order  Original Line  Test fn                                            Unmute number
  -----  -------------  -------------------------------------------------- -------------
  1st    164            full_round_three_clients                           carve-out 1/6  (MANDATORY canonical-first gate per D-04)
  2nd    462            blame_non_signer_timeout                           carve-out 2/6
  3rd    730            adversarial_replay_token                           carve-out 3/6
  4th    854            adversarial_invalid_utxo                           carve-out 4/6
  5th    911            adversarial_wrong_denomination                     carve-out 5/6
  6th    1236           round_restart_and_completion_after_blame           carve-out 6/6

The attribute pair shape at all six sites (verified at planning time — re-verify at execute time):

  #[tokio::test]                                                                                       <- line ABOVE (preserved byte-identical)
  #[ignore = "TODO(Phase-10): RPC schema drift on listunspent/getrawtransaction -- see TODO.md"]      <- DELETE THIS ONE LINE
  async fn <test_fn>() {                                                                               <- line BELOW (preserved byte-identical)

Each unmute = ONE line deletion only. NO whitespace edits, NO comment substitution, NO test-body edits.

<!-- Canonical test invocation — copy-pasted from CONTRIBUTING.md §"Running ignored (Phase-10) tests locally". DO NOT INVENT A NEW ONE. -->

Per-test (used for each commit's PASS-proof body — CD-2 keeps the $(brew --prefix) form for reviewer reproducibility):

  BLINDJOIN_REQUIRE_BITCOIND=1 \
    BITCOIND_EXE=$(brew --prefix)/bin/bitcoind \
    cargo test --test integration full_round::<test_fn> -- --ignored 2>&1 \
    | tee target/integration-test-12-02-N.log

The `-- --ignored` flag is required during the cycle: it runs a test that STILL has #[ignore] on it (the PASS proof is captured BEFORE the line is deleted in the same commit). Once the attribute is removed, the test runs by default. The executor's per-iteration order: (1) run the test with `--ignored` and capture the PASS verdict, (2) delete the #[ignore] line, (3) commit with the captured verdict in the body.

Whole-file final invocation (after all 6 unmutes land — no `--ignored` because no #[ignore] attributes remain):

  BLINDJOIN_REQUIRE_BITCOIND=1 \
    BITCOIND_EXE=$(brew --prefix)/bin/bitcoind \
    cargo test --test integration full_round 2>&1 \
    | tee target/integration-test-12-02-final.log

Expected verdict: `test result: ok. 8 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out`.

<!-- The two SHA references each commit body must contain (CD-1 + RESEARCH.md §"Plan 11-02 Compatibility" / PATTERNS.md §"Phase 12 commit body convention"). -->

  RSA_FIX_SHA   = cc20f6fbca4d292bf7b394a3850b18d244b5b602   (Phase 11 Plan 01; known from 11-02-SUMMARY.md; verifiable at execute time via `git log --grep='^fix(11): switch client RSA pubkey decode' --format=%H -1`)
  WALLET_FIX_SHA = <computed at execute time via `git log --grep='^fix(12): trust_witness_utxo' --format=%H -1`>   (Plan 12-01 wallet fix)

If WALLET_FIX_SHA is empty at execute time, HALT — Plan 12-01 was not run (depends_on violation). If WALLET_FIX_SHA does not match the expected commit subject prefix `fix(12):`, HALT — the dependency may be a different Phase-12 fix or a hand-typed commit.

<!-- The six prescribed commit subjects, in canonical-first then file order — CD-1 with phase number shifted from 11 to 12. -->

  test(12): unmute full_round_three_clients (Phase-10 carve-out 1/6)
  test(12): unmute blame_non_signer_timeout (Phase-10 carve-out 2/6)
  test(12): unmute adversarial_replay_token (Phase-10 carve-out 3/6)
  test(12): unmute adversarial_invalid_utxo (Phase-10 carve-out 4/6)
  test(12): unmute adversarial_wrong_denomination (Phase-10 carve-out 5/6)
  test(12): unmute round_restart_and_completion_after_blame (Phase-10 carve-out 6/6)

<!-- The CD-1 commit body shape for each of the six commits (Phase 12 adapts Phase 11's shape by adding a second SHA reference and a bitcoind --version line). -->

  cargo test --test integration full_round::<test_fn> -- --ignored
  <paste VERDICT verbatim — e.g. "test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 7 filtered out">

  bitcoind --version: <paste `bitcoind --version | head -1` output — e.g. "Bitcoin Core Daemon version v31.0.0">
  RSA fix: cc20f6fbca4d292bf7b394a3850b18d244b5b602  (Phase 11 Plan 01)
  Wallet fix: <WALLET_FIX_SHA>  (Phase 12 Plan 01)

Use a HEREDOC for each commit message to preserve newlines.

<!-- Plan 11-02 verbatim carry-forward acknowledgment. -->

Per CONTEXT.md D-04 and PATTERNS.md §"Surface 2", Plan 12-02 reuses Plan 11-02's unmute spec verbatim. The only deltas from 11-02-PLAN.md are:
  (1) Each commit body cites TWO SHAs (RSA fix + Wallet fix) instead of one.
  (2) Each commit body includes `bitcoind --version | head -1` output (CD-4 — catches a silent brew bump between Phase 11 and Phase 12).
  (3) Commit subjects shift from `test(11):` to `test(12):`.
  (4) The per-test PASS-proof log files are named `target/integration-test-12-02-N.log` (was `11-02-N.log`).
  (5) D-08 escape-valve becomes D-11 escape-valve (same protocol; orthogonal-blocker-number tracking continues — Phase 11 absorbed 4 (vout-after-mine, bdk 2.3 wallet API, RSA SPKI, bdk_wallet 2.3 SignOptions); a 5th in Phase 12 triggers halt-and-surface and Phase 13 absorbs).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Unmute full_round_three_clients (originally line 164) — canonical-first MANDATORY gate</name>
  <files>tests/integration/full_round.rs</files>
  <read_first>
    - .planning/phases/12-repair-client-src-wallet-rs-260-bdk-wallet-2-3-signoptions-r/12-CONTEXT.md (D-04 reuse Plan 11-02 verbatim; D-11 escape-valve; D-12 Phase 13 absorbs any 5th-blocker; CD-1 commit subjects shift to test(12):; CD-2 brew --prefix form; CD-4 bitcoind --version capture)
    - .planning/phases/12-repair-client-src-wallet-rs-260-bdk-wallet-2-3-signoptions-r/12-PATTERNS.md §"Surface 2" (the verbatim attribute pair shape and the two-SHA commit body delta)
    - .planning/phases/11-coordinator-rsa-pubkey-encoding-full-round-rs-unmute-complet/11-02-PLAN.md (the locked-in spec — Plan 12-02 mirrors Task 1 structure here)
    - .planning/phases/11-coordinator-rsa-pubkey-encoding-full-round-rs-unmute-complet/11-02-SUMMARY.md §"Failure Diagnosis" and §"Resume protocol" (the 4 known orthogonal blockers; this task is the resume execution after Plan 12-01 closes the 4th)
    - tests/integration/full_round.rs lines 160-200 (current state of `full_round_three_clients` and the #[ignore] attribute originally at line 164 — confirm the test body is unchanged from Plan 11-02's HALTED state; the #[ignore] line should still be present per 11-02-SUMMARY §"Working-Tree State")
    - tests/integration/mod.rs (the shared Phase 9/10 fixtures: require_bitcoind!, BitcoindGuard, fund_regtest, FundedSetup — read but do NOT modify)
    - CONTRIBUTING.md §"Running ignored (Phase-10) tests locally" (the canonical local invocation pattern — the executor uses this verbatim for the PASS-proof capture, with `$(brew --prefix)` per CD-2)
    - client/src/wallet.rs (Plan 12-01's edit — confirm the sign call now passes `SignOptions { trust_witness_utxo: true, ..SignOptions::default() }`; this is the dependency that makes the canonical-first unmute possible)
  </read_first>
  <behavior>
    - Test 1 (Plan 12-01 dependency check): `git log --grep='^fix(12): trust_witness_utxo' --format=%H -1` returns a non-empty 40-character SHA. If empty, HALT — Plan 12-01 was not run; depends_on contract violated.
    - Test 2 (Phase 11 dependency check): `git log --grep='^fix(11): switch client RSA pubkey decode' --format=%H -1` returns `cc20f6fbca4d292bf7b394a3850b18d244b5b602` or another 40-char SHA matching the same subject pattern. If empty, HALT — Phase 11 Plan 01 was not in history.
    - Test 3 (capture-then-unmute, in order): run `BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round::full_round_three_clients -- --ignored 2>&1 | tee target/integration-test-12-02-1.log` and capture the PASS verdict line (`test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 7 filtered out`). If the test FAILS (not PASSES), HALT immediately — do NOT delete the #[ignore] line, do NOT commit, do NOT proceed to Task 2. Emit the CHECKPOINT REACHED marker per D-11 with the failure mode (a 5th orthogonal blocker has appeared after Plan 12-01's Option A repair); Phase 13 absorbs per D-12. Pre-authorized in-flight scope expansion is ZERO.
    - Test 4 (post-edit verification): after deleting the #[ignore] line, re-running `cargo test --test integration full_round::full_round_three_clients` WITHOUT `-- --ignored` still PASSES (the test now runs by default).
    - Test 5 (whole-file sanity, after this commit only): `cargo test --test integration full_round` reports 3 passed (2 originally passing + 1 just unmuted), 5 ignored (the other 5 carve-outs still carry their #[ignore]).
    - Test 6 (D-11 escape-valve negative case): if the canonical-first test FAILED with a NEW signature different from `Missing non-witness UTXO` (Plan 12-01's repair stayed valid but a 5th blocker is downstream of the sign call), the executor halts with a precise diagnostic — naming the new failure signature, the test step that failed, and the proposed minimal repair — and does NOT proceed to Task 2.
  </behavior>
  <action>
    Execute the per-test cycle for the 1st unmute in this exact order. This task mirrors Plan 11-02 Task 1 verbatim with the only deltas being: (a) commit subject prefix `test(12):` instead of `test(11):`, (b) two SHA references in commit body, (c) `bitcoind --version` line in commit body, (d) log filename uses `12-02` prefix, (e) escape-valve is D-11 (not D-08).

    (a) Confirm BOTH dependencies are in `git log` history and capture their SHAs into shell variables:

          RSA_FIX_SHA=$(git log --grep="^fix(11): switch client RSA pubkey decode" --format=%H -1)
          WALLET_FIX_SHA=$(git log --grep="^fix(12): trust_witness_utxo" --format=%H -1)

        If RSA_FIX_SHA is empty, HALT — Phase 11 Plan 01 was not run. If WALLET_FIX_SHA is empty, HALT — Plan 12-01 was not run (depends_on: [12-01] violation). Both SHAs are 40 hex characters; verify with `[ ${#RSA_FIX_SHA} = 40 ] && [ ${#WALLET_FIX_SHA} = 40 ]`. These `git log --grep` queries are the AUTHORITATIVE source for the SHAs; do NOT rely on summary files (they may not exist yet, and commit history is the canonical truth).

        Capture `bitcoind --version` once (CD-4, used in this commit body and re-used in subsequent task commit bodies — but re-capture per-iteration in Task 2 in case a brew bump occurs mid-cycle):

          BITCOIND_VERSION=$(bitcoind --version | head -1)

        Verify BITCOIND_VERSION contains `v31` (the expected pinned brew version). If it shows v30.x, v32.x, or any other major version, surface a WARNING in the commit body — a brew bump may have occurred since Plan 12-01's sanity capture; the cycle still proceeds but the version-pin asymmetry is recorded.

    (b) Re-locate the current `#[ignore]` line for `full_round_three_clients` by name (line numbers are stable in this task since no prior delete has occurred; verify anyway):

          IGN_LINE=$(awk '$0 ~ "#\\[ignore = \"TODO\\(Phase-10\\)" {ig=NR} $0 ~ "async fn full_round_three_clients\\(\\)" {print ig; exit}' tests/integration/full_round.rs)

        Expected: 164 (per planning snapshot and 11-02-SUMMARY working-tree state). Accept any value as long as `sed -n "${IGN_LINE}p" tests/integration/full_round.rs` outputs a line beginning with `#[ignore = "TODO(Phase-10):`. If IGN_LINE is empty or the sed output does not match, HALT — the working tree has drifted from the planning snapshot.

    (c) Run the per-test PASS-proof invocation:

          BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind \
            cargo test --test integration full_round::full_round_three_clients -- --ignored 2>&1 \
            | tee target/integration-test-12-02-1.log

        Capture the verdict line (the line matching `test result: ok. 1 passed` OR `test result: FAILED`) into `VERDICT_1`.

    (d) If the verdict line is NOT a PASS (the verdict contains `FAILED` or the run panicked), HALT IMMEDIATELY per D-11 escape-valve. Do NOT delete the #[ignore] line. Do NOT commit. Emit a CHECKPOINT REACHED marker containing:

          - The failure mode (the verbatim panic message OR the `test result: FAILED` line + the surrounding context).
          - Whether the failure signature matches `Missing non-witness UTXO` (which would indicate Plan 12-01's Option A repair did not take effect — this is a Plan 12-01 rollback condition) OR a NEW signature (which is the 5th orthogonal blocker — Phase 13 absorbs per D-12).
          - A proposed minimal repair (if obvious from the failure mode).
          - The explicit note: "D-11 escape-valve invoked: canonical-first unmute failed — Plan 12-02 HALTS, Phase 13 absorbs the new blocker (D-12). Pre-authorized in-flight scope expansion is ZERO. Tasks 2-6 of Plan 12-02 are NOT attempted in this case."

        Earlier commits (i.e., Plan 12-01's wallet-fix commit, plus Phase 11 Plans 11-01 and 11-02-SUMMARY's writeup commit) remain in history — they are bisect-clean.

    (e) On PASS: delete exactly line ${IGN_LINE} from `tests/integration/full_round.rs`. The deleted line is `#[ignore = "TODO(Phase-10): RPC schema drift on listunspent/getrawtransaction -- see TODO.md"]`. The line above (`#[tokio::test]`) and the line below (`async fn full_round_three_clients() {`) stay byte-identical. NO whitespace edits, NO comment substitution, NO test-body changes (D-04 inherits Plan 11-02 D-07: "no drive-by edits to other tests, helpers, or unrelated code"). Verify with `git diff tests/integration/full_round.rs --stat` that exactly ONE line is deleted and nothing else changes (expected `1 file changed, 0 insertions(+), 1 deletion(-)`).

    (f) Stage and commit atomically. Commit subject: `test(12): unmute full_round_three_clients (Phase-10 carve-out 1/6)`. Commit body (CD-1 minimal stamp + CD-4 bitcoind-version capture):

          cargo test --test integration full_round::full_round_three_clients -- --ignored
          <paste VERDICT_1 verbatim — e.g. "test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 7 filtered out">

          bitcoind --version: <paste BITCOIND_VERSION verbatim — e.g. "Bitcoin Core Daemon version v31.0.0">
          RSA fix: ${RSA_FIX_SHA}  (Phase 11 Plan 01)
          Wallet fix: ${WALLET_FIX_SHA}  (Phase 12 Plan 01)

        Use a HEREDOC for the commit message:

          git add tests/integration/full_round.rs
          git commit -m "$(cat <<'EOF'
          test(12): unmute full_round_three_clients (Phase-10 carve-out 1/6)

          cargo test --test integration full_round::full_round_three_clients -- --ignored
          <VERDICT_1>

          bitcoind --version: <BITCOIND_VERSION>
          RSA fix: <RSA_FIX_SHA>  (Phase 11 Plan 01)
          Wallet fix: <WALLET_FIX_SHA>  (Phase 12 Plan 01)
          EOF
          )"

        (Substitute the captured shell variables literally before commit time; the HEREDOC quoting prevents shell expansion of `$(brew --prefix)` and the URL fragments in the verdict line — the literal text reproduces on any reviewer's machine per CD-2.)

    (g) Post-commit verification:

          BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind \
            cargo test --test integration full_round 2>&1 \
            | tee target/integration-test-12-02-1-postcommit.log

        Run WITHOUT `-- --ignored` (the canonical-first test is now unmuted; the remaining 5 carve-outs are still ignored). Expected verdict: `test result: ok. 3 passed; 0 failed; 5 ignored; 0 measured; 0 filtered out` (2 originally-passing + 1 just-unmuted = 3 passed; 5 still-ignored). If the count is wrong, HALT and report.

    NO other files are modified by this task. NO entries added to TODO.md. NO doc updates. NO test-body edits. NO Cargo.toml edits. NO CI changes. The Plan 12-01 wallet-fix commit AND the Phase 11 cc20f6f RSA fix commit MUST both be in history before this task runs (depends_on: [12-01] + transitively [11-01]).
  </action>
  <verify>
    <automated>BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round::full_round_three_clients 2>&1 | grep -E "test result: ok\. 1 passed" && [ "$(grep -c 'TODO(Phase-10)' tests/integration/full_round.rs)" = "5" ] && git log -1 --format=%s | grep -q "^test(12): unmute full_round_three_clients (Phase-10 carve-out 1/6)$" && git log -1 --format=%b | grep -q "Wallet fix:" && git log -1 --format=%b | grep -q "RSA fix:"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion (the deletion is precise): `awk 'NR==163{print} NR==164{print}' tests/integration/full_round.rs` outputs exactly two lines whose contents are `#[tokio::test]` (originally line 163, still at 163 after delete) and `async fn full_round_three_clients() {` (was line 165, now shifted to line 164). The `#[ignore]` line is GONE from this region.
    - Source assertion (deletion count): `[ "$(grep -c 'TODO(Phase-10)' tests/integration/full_round.rs)" = "5" ]` exits 0 — exactly 5 #[ignore] lines remain (one removed from the original 6; this is the authoritative deletion proof and does NOT depend on the post-delete line number).
    - Test command assertion (default run): `BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round::full_round_three_clients 2>&1 | grep -E "test result: ok\. 1 passed; 0 failed"` returns a match (the test now runs and passes WITHOUT `-- --ignored`).
    - Test command assertion (whole-file incremental): `BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round 2>&1 | grep -E "test result: ok\. 3 passed; 0 failed; 5 ignored"` returns a match (2 already-passing + 1 just-unmuted = 3 passed; 5 still-ignored).
    - CLI assertion (commit subject): `git log -1 --format=%s` outputs exactly `test(12): unmute full_round_three_clients (Phase-10 carve-out 1/6)`.
    - CLI assertion (commit body — cargo invocation + verdict line): `git log -1 --format=%b` contains the literal string `cargo test --test integration full_round::full_round_three_clients -- --ignored` and the literal string `test result: ok. 1 passed`.
    - CLI assertion (commit body — bitcoind version line, CD-4): `git log -1 --format=%b | grep -E "bitcoind --version: Bitcoin Core Daemon version v"` returns a match. The captured version SHOULD contain `v31` per the pinned brew expectation; if it shows a different major version, the commit still lands but the body documents the asymmetry.
    - CLI assertion (commit body — RSA fix SHA): `git log -1 --format=%b | grep -E "RSA fix: [0-9a-f]{40}"` returns a match. The SHA SHOULD be `cc20f6fbca4d292bf7b394a3850b18d244b5b602` per 11-02-SUMMARY; accept any 40-char hex.
    - CLI assertion (commit body — Wallet fix SHA): `git log -1 --format=%b | grep -E "Wallet fix: [0-9a-f]{40}"` returns a match. The Wallet-fix SHA MUST be the Plan 12-01 commit SHA (verifiable via `git log --grep='^fix(12): trust_witness_utxo' --format=%H -1`); the two SHAs in the commit body MUST differ from each other.
    - Source assertion (single-line delete diff): `git diff HEAD~1 -- tests/integration/full_round.rs | grep '^-' | wc -l | tr -d ' '` returns exactly `1` (one deleted line; the `---` header line is filtered by the leading `-` regex — verify via the more precise `git diff HEAD~1 --stat -- tests/integration/full_round.rs` which should show `1 file changed, 0 insertions(+), 1 deletion(-)`).
    - Boundary preservation assertion: `git diff HEAD~1 --name-only` returns exactly `tests/integration/full_round.rs` (no other file touched by this commit).
    - Halt assertion (negative test — D-11 escape-valve): if the test FAILED in step (c), no commit exists, the #[ignore] line is still present in the file (grep count for `TODO(Phase-10)` returns 6 not 5), and the executor emitted a CHECKPOINT REACHED marker naming the failure mode and citing D-11. This is a recovery state, not a failure mode — Phase 13 absorbs per D-12.
    - Halt assertion (depends_on violation): if Plan 12-01 was not in history (WALLET_FIX_SHA was empty in step (a)), no edit or commit was made, and the executor surfaced the depends_on violation for inspection.
  </acceptance_criteria>
  <done>
    The `#[ignore]` attribute on `full_round_three_clients` is removed; the test passes locally WITHOUT `-- --ignored` against pinned brew bitcoind v31 via the CONTRIBUTING.md canonical invocation; whole-file `cargo test --test integration full_round` shows 3 passed / 5 ignored; one atomic commit `test(12): unmute full_round_three_clients (Phase-10 carve-out 1/6)` has landed with CD-1 + CD-4 body (cargo invocation + PASS verdict + bitcoind --version + two SHA references: RSA fix from Phase 11 + Wallet fix from Plan 12-01). If the canonical-first test FAILED, the executor has HALTED per D-11 and surfaced a CHECKPOINT REACHED marker naming the 5th orthogonal blocker — Task 2 is NOT attempted, Phase 13 absorbs per D-12.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Unmute the remaining 5 carve-out tests in file order (originally lines 462, 730, 854, 911, 1236)</name>
  <files>tests/integration/full_round.rs</files>
  <read_first>
    - .planning/phases/12-repair-client-src-wallet-rs-260-bdk-wallet-2-3-signoptions-r/12-CONTEXT.md (D-04 reuse Plan 11-02 verbatim — canonical-first then file order; D-11 escape-valve halt-and-surface on any 5th orthogonal blocker; D-12 Phase 13 absorbs; D-13 REPAIR-01 closure bookkeeping after green suite; CD-1 commit subjects test(12):; CD-2 brew --prefix form; CD-4 bitcoind --version capture per-iteration)
    - .planning/phases/12-repair-client-src-wallet-rs-260-bdk-wallet-2-3-signoptions-r/12-PATTERNS.md §"Surface 2" (the verbatim attribute pair shape identical at all 5 remaining sites; the per-test commit cycle pattern; the two-SHA commit body convention)
    - .planning/phases/11-coordinator-rsa-pubkey-encoding-full-round-rs-unmute-complet/11-02-PLAN.md Task 2 (the locked-in spec — Plan 12-02 Task 2 mirrors this verbatim with the deltas listed in <interfaces>)
    - tests/integration/full_round.rs (after Task 1's commit lands, re-read the file to confirm the remaining 5 #[ignore] sites are present — note that after deleting the canonical-first #[ignore], all subsequent line numbers SHIFT DOWN BY ONE per prior delete; the executor MUST re-grep for #[ignore] before each unmute, not trust the pre-Phase-11 line numbers from the spec)
    - tests/integration/mod.rs (the shared fixtures, unchanged — depend on, do NOT modify)
    - CONTRIBUTING.md §"Running ignored (Phase-10) tests locally" (the per-test invocation pattern reused 5 more times)
    - (RSA fix SHA: re-capture via `git log --grep='^fix(11): switch client RSA pubkey decode' --format=%H -1`. Wallet fix SHA: re-capture via `git log --grep='^fix(12): trust_witness_utxo' --format=%H -1`. Do NOT depend on summary files — `git log --grep` is the authoritative source.)
  </read_first>
  <behavior>
    - Test 1: After this task completes, `grep -c "TODO(Phase-10)" tests/integration/full_round.rs` returns 0 (all 6 carve-outs unmuted).
    - Test 2: `BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round 2>&1 | grep -E "test result: ok\. 8 passed; 0 failed; 0 ignored"` returns a match — all 8 full_round tests pass locally. This is the REPAIR-01 local-closure proof (D-03 + D-13).
    - Test 3: Each of the 5 newly-added commits carries the prescribed subject (`test(12): unmute <fn> (Phase-10 carve-out N/6)`) and a CD-1 + CD-4 body containing the per-test cargo invocation, the PASS verdict line, `bitcoind --version | head -1` output, and BOTH SHA references (RSA fix from Phase 11 + Wallet fix from Plan 12-01).
    - Test 4 (D-11 escape-valve): if any of the 5 unmutes FAILS during its capture step, the executor halts WITHOUT deleting the #[ignore] line for that test (and without attempting any later unmute), and emits a CHECKPOINT REACHED marker per D-11. Pre-authorized in-flight scope expansion is ZERO. A 5th orthogonal blocker (any test failure that is NOT the `Missing non-witness UTXO` signature Plan 12-01 already cleared) triggers the same halt-and-surface protocol; Phase 13 absorbs per D-12.
    - Test 5 (post-cycle bookkeeping, D-13): after all 6 unmute commits land green, REQUIREMENTS.md REPAIR-01 status flips from `[ ]` to `[x]`, the Traceability table updates the Phase column to "Phase 10/11/12" and Plan column to "12-02", and ROADMAP.md Phase 12 entry's status reflects completion. REPAIR-02 status remains `[ ]` (PR-observation-gated per Phase 11 D-11 inherited; not in-plan).
  </behavior>
  <action>
    Execute the per-test cycle 5 times, in this exact order. The order is mandatory per D-04 (file order after the canonical-first test in Task 1) and the commits must each be atomic (one #[ignore] removal per commit). This task mirrors Plan 11-02 Task 2 verbatim with the same five deltas as Plan 12-02 Task 1 (commit-subject phase number, two SHAs in body, bitcoind --version line, log filename, D-11 escape-valve).

    Pre-loop setup (run once, before the iteration):

      RSA_FIX_SHA=$(git log --grep="^fix(11): switch client RSA pubkey decode" --format=%H -1)
      WALLET_FIX_SHA=$(git log --grep="^fix(12): trust_witness_utxo" --format=%H -1)
      [ ${#RSA_FIX_SHA} = 40 ] && [ ${#WALLET_FIX_SHA} = 40 ] || { echo "HALT: dependency SHAs not captured"; exit 1; }

    For EACH iteration where N takes values 2, 3, 4, 5, 6 (in that order), the test fn mapping is:

      N=2 → test fn `blame_non_signer_timeout` (originally line 462, shifted to ~461 after Task 1's deletion)
      N=3 → test fn `adversarial_replay_token` (originally line 730, shifted by N-2 prior deletes)
      N=4 → test fn `adversarial_invalid_utxo` (originally line 854, shifted by N-2 prior deletes)
      N=5 → test fn `adversarial_wrong_denomination` (originally line 911, shifted by N-2 prior deletes)
      N=6 → test fn `round_restart_and_completion_after_blame` (originally line 1236, shifted by N-2 prior deletes)

    Per-iteration procedure (DO NOT short-circuit — each iteration is its own atomic commit per Plan 11-02 D-06 inherited):

    (a) Re-capture `bitcoind --version` per iteration (CD-4 — catches an unlikely brew bump mid-cycle):

          BITCOIND_VERSION=$(bitcoind --version | head -1)

        If BITCOIND_VERSION at iteration N differs from BITCOIND_VERSION at iteration N-1, surface a WARNING but proceed — the cycle's PASS proofs document the version at each step.

    (b) Re-locate the current #[ignore] line for the test by name (line numbers shift after each delete; re-grep is authoritative):

          IGN_LINE=$(awk -v t="<test_fn>" '$0 ~ "#\\[ignore = \"TODO\\(Phase-10\\)" {ig=NR} $0 ~ "async fn "t"\\(\\)" {print ig; exit}' tests/integration/full_round.rs)

        Verify IGN_LINE is a non-empty integer and that `sed -n "${IGN_LINE}p" tests/integration/full_round.rs` outputs a line beginning with `#[ignore = "TODO(Phase-10):`. If not, HALT — the working tree has drifted from expectation.

    (c) Run the per-test PASS-proof invocation:

          BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind \
            cargo test --test integration full_round::<test_fn> -- --ignored 2>&1 \
            | tee target/integration-test-12-02-${N}.log

        Capture the verdict line into `VERDICT_${N}`.

    (d) If the verdict is NOT a PASS, HALT IMMEDIATELY per D-11 escape-valve. Do NOT delete the #[ignore] line. Do NOT commit. Do NOT attempt any later iteration in this loop. Emit a CHECKPOINT REACHED marker containing:

          - The test fn name and the iteration number (N/6).
          - The failure mode (panic message / assertion / cargo verdict — verbatim).
          - Whether the failure signature is `Missing non-witness UTXO` (impossible if Plan 12-01 landed correctly and Task 1 passed; if observed, indicates an unrelated regression — likely a brew bitcoind upgrade or a stale build) OR a NEW signature (the 5th orthogonal blocker — Phase 13 absorbs per D-12).
          - A proposed minimal repair (if obvious from the failure mode).
          - The explicit note: "D-11 escape-valve invoked: 5th orthogonal blocker (or canonical-follow-on failure) at iteration N/6 — Plan 12-02 HALTS, Phase 13 absorbs (D-12). Pre-authorized in-flight scope expansion is ZERO. Earlier successful commits in this task remain bisect-clean."

        Earlier successful commits in this task (and Task 1's commit, and Plan 12-01's wallet-fix commit, and Phase 11's commits) remain — they are bisect-clean.

    (e) On PASS: delete exactly line ${IGN_LINE} from `tests/integration/full_round.rs`. The deleted line is the `#[ignore]` attribute. The line above (`#[tokio::test]`) and the line below (`async fn <test_fn>() {`) stay byte-identical. NO whitespace edits, NO comment substitution, NO test-body edits. Verify with `git diff --stat tests/integration/full_round.rs` that exactly ONE line is deleted and nothing else changed (`1 file changed, 0 insertions(+), 1 deletion(-)`).

    (f) Stage and commit atomically. Commit subject: `test(12): unmute <test_fn> (Phase-10 carve-out N/6)`. Commit body (CD-1 minimal stamp + CD-4 bitcoind-version capture, identical shape to Task 1's commit body — only the test fn name, verdict, and version capture vary):

          cargo test --test integration full_round::<test_fn> -- --ignored
          <paste VERDICT_${N} verbatim>

          bitcoind --version: <paste BITCOIND_VERSION captured at step (a) verbatim>
          RSA fix: ${RSA_FIX_SHA}  (Phase 11 Plan 01)
          Wallet fix: ${WALLET_FIX_SHA}  (Phase 12 Plan 01)

        Use a HEREDOC for the commit message:

          git add tests/integration/full_round.rs
          git commit -m "$(cat <<'EOF'
          test(12): unmute <test_fn> (Phase-10 carve-out N/6)

          cargo test --test integration full_round::<test_fn> -- --ignored
          <VERDICT_N>

          bitcoind --version: <BITCOIND_VERSION>
          RSA fix: <RSA_FIX_SHA>  (Phase 11 Plan 01)
          Wallet fix: <WALLET_FIX_SHA>  (Phase 12 Plan 01)
          EOF
          )"

    (g) Verify post-commit incrementally. The expected pass-count grows by one after each iteration: after the 2nd unmute (N=2) expect 4 passed / 4 ignored; after the 3rd (N=3) expect 5 passed / 3 ignored; after the 4th (N=4) expect 6 passed / 2 ignored; after the 5th (N=5) expect 7 passed / 1 ignored; after the 6th (N=6) expect 8 passed / 0 ignored. Compute the expected count via explicit shell arithmetic (N is the loop variable, NOT regex syntax) and grep for it:

          EXPECTED=$((2 + N)) && BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind \
            cargo test --test integration full_round 2>&1 \
            | grep -E "test result: ok\. ${EXPECTED} passed"

        If the count is wrong, HALT.

    After all 5 iterations complete (N=2 through N=6), run the final whole-file invocation from CONTRIBUTING.md verbatim (WITHOUT `--ignored`, because no #[ignore] attributes remain):

          BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind \
            cargo test --test integration full_round 2>&1 \
            | tee target/integration-test-12-02-final.log

        Expected verdict: `test result: ok. 8 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out`. If anything else, the executor halts and reports — but this state should already be impossible if each iteration's step (c) PASSed.

    Post-cycle bookkeeping (D-13 — REPAIR-01 closure):

    (h) Update `.planning/REQUIREMENTS.md`:
        - In the "Test Repair (REPAIR)" section, change `- [ ] **REPAIR-01**:` to `- [x] **REPAIR-01**:` (the line beginning with "REPAIR-01"). The status indicator was already `[x]` in the planning snapshot per the file read; if it is, no change needed and verify the Traceability table.
        - In the Traceability table at the bottom, change the REPAIR-01 row to `| REPAIR-01 | Phase 10/11/12 | 12-02 | complete |`. If REPAIR-02 row exists, leave it unchanged.

    (i) Update `.planning/ROADMAP.md`:
        - Locate the Phase 12 entry. Update `**Plans:** 0 plans` (or whatever placeholder count) to `**Plans:** 2 plans` and the plans list to include both `12-01-PLAN.md — wallet repair (Option A — trust_witness_utxo: true with D-08 block comment)` and `12-02-PLAN.md — six-test unmute cycle (Plan 11-02 carry-forward verbatim with two-SHA commit bodies)`.
        - If the Progress table at the bottom has a Phase 12 row, mark it complete. If REPAIR-01 has a row referencing Phase 10 / Phase 11, update to also reference Phase 12.
        - DO NOT touch v1.3 milestone summary lines (`- ✅ v1.3 ...` header — that ship-note bookkeeping is D-14 explicitly OUT of Phase 12 scope and belongs to /gsd-ship).

    (j) Commit the bookkeeping as a SEPARATE atomic commit AFTER the 6th unmute commit (so the unmute commits remain pure attribute-deletes and bisect-clean):

          git add .planning/REQUIREMENTS.md .planning/ROADMAP.md
          git commit -m "$(cat <<'EOF'
          docs(12): close REPAIR-01 locally on full_round green suite

          REQUIREMENTS.md: REPAIR-01 marked [x]; Traceability updated to Phase 10/11/12, plan 12-02.
          ROADMAP.md: Phase 12 plans list populated (12-01 wallet repair, 12-02 unmute cycle); Phase 12 status complete.

          Final whole-file PASS verdict:
            cargo test --test integration full_round
            test result: ok. 8 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out

          bitcoind --version: <BITCOIND_VERSION>
          RSA fix: <RSA_FIX_SHA>  (Phase 11 Plan 01)
          Wallet fix: <WALLET_FIX_SHA>  (Phase 12 Plan 01)
          Six unmute commits: <6 SHAs from `git log --grep='^test(12): unmute' --format=%H` in oldest-first order>

          REPAIR-02 status NOT touched in this plan — closes on PR observation of the
          corepc-node-feature-pin-check CI job (already landed at 4026f50 per Phase 10)
          on the Phase 11+12 combined PR (per Phase 11 D-11 inherited).

          v1.3 ship notes / /gsd-complete-milestone NOT in scope for Phase 12 (D-14).
          EOF
          )"

    REPAIR-02 closure note: this plan does NOT touch `.github/workflows/ci.yml`. REPAIR-02 closes on PR observation when the CI `corepc-node-feature-pin-check` job (already landed at commit 4026f50 per 11-02-SUMMARY) is observed green on the Phase 11+12 combined PR. The executor of this plan does NOT self-attest REPAIR-02; it lands the code, the PR closes the requirement. The bookkeeping commit MUST NOT change REPAIR-02's `[ ]` status.

    v1.3 ship notes are NOT in scope (D-14). The bookkeeping commit captures REPAIR-01 closure locally; ship notes / `/gsd-complete-milestone v1.3` are handled by a separate wrap-up phase or `/gsd-ship` after CI confirms the PR green.
  </action>
  <verify>
    <automated>BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round 2>&1 | grep -E "test result: ok\. 8 passed; 0 failed; 0 ignored" && [ "$(grep -c 'TODO(Phase-10)' tests/integration/full_round.rs)" = "0" ] && [ "$(git log --grep='^test(12): unmute .* (Phase-10 carve-out [2-6]/6)$' --oneline | wc -l | tr -d ' ')" = "5" ] && grep -E "^- \[x\] \*\*REPAIR-01\*\*" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion (all six #[ignore] gone): `grep -c "TODO(Phase-10)" tests/integration/full_round.rs` returns exactly 0.
    - Source assertion (no residual #[ignore] of any form on the six unmuted tests): `awk '/#\[ignore/ {ig=NR; next} /async fn (full_round_three_clients|blame_non_signer_timeout|adversarial_replay_token|adversarial_invalid_utxo|adversarial_wrong_denomination|round_restart_and_completion_after_blame)/ {if (ig == NR - 1) print "FAIL: " $0; ig=0}' tests/integration/full_round.rs` produces no `FAIL:` lines — no `#[ignore]` attribute is adjacent to any of the six unmuted test fns.
    - Source assertion (test bodies preserved — D-04 inherits D-07): `grep -cE "^async fn (full_round_three_clients|blame_non_signer_timeout|adversarial_replay_token|adversarial_invalid_utxo|adversarial_wrong_denomination|round_restart_and_completion_after_blame)\(\)" tests/integration/full_round.rs` returns 6 (test fn names preserved, only attributes removed).
    - Test command assertion (REPAIR-01 closure proof): `BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round 2>&1 | grep -E "test result: ok\. 8 passed; 0 failed; 0 ignored"` returns a match. This is the D-13 closure trigger.
    - Test command assertion (per-test): for each unmuted test fn, `BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round::<fn_name> 2>&1 | grep -E "test result: ok\. 1 passed"` returns a match (each individual test passes WITHOUT `-- --ignored`).
    - CLI assertion (5 unmute commits from this task): `git log --grep='^test(12): unmute .* (Phase-10 carve-out [2-6]/6)$' --oneline | wc -l | tr -d ' '` returns exactly `5` — five commits with the prescribed pattern. Combined with Task 1's commit, total = 6 unmute commits.
    - CLI assertion (commit subjects in canonical-first then file order): `git log --grep='^test(12): unmute' --reverse --format=%s` outputs exactly the six prescribed subjects in this order:
        `test(12): unmute full_round_three_clients (Phase-10 carve-out 1/6)`  (from Task 1)
        `test(12): unmute blame_non_signer_timeout (Phase-10 carve-out 2/6)`
        `test(12): unmute adversarial_replay_token (Phase-10 carve-out 3/6)`
        `test(12): unmute adversarial_invalid_utxo (Phase-10 carve-out 4/6)`
        `test(12): unmute adversarial_wrong_denomination (Phase-10 carve-out 5/6)`
        `test(12): unmute round_restart_and_completion_after_blame (Phase-10 carve-out 6/6)`
    - CLI assertion (per commit body — cargo invocation + verdict): for each of the 5 commits in this task, `git log -1 --format=%b <commit>` contains the literal strings `cargo test --test integration full_round::<corresponding_fn>` and `test result: ok. 1 passed`.
    - CLI assertion (per commit body — CD-4 bitcoind --version): for each of the 5 commits in this task, `git log -1 --format=%b <commit>` contains a line matching `bitcoind --version: Bitcoin Core Daemon version v`. (The version SHOULD contain `v31`; non-v31 is documented but accepted.)
    - CLI assertion (per commit body — two SHAs): for each of the 5 commits in this task, `git log -1 --format=%b <commit>` contains a line matching `RSA fix: [0-9a-f]{40}` AND a line matching `Wallet fix: [0-9a-f]{40}`. The two SHAs MUST differ from each other (RSA fix is from Phase 11; Wallet fix is from Plan 12-01).
    - Source assertion (per commit — single-line delete): for each of the 5 commits in this task, `git diff <commit>~1 <commit> --stat -- tests/integration/full_round.rs` shows `1 file changed, 0 insertions(+), 1 deletion(-)`.
    - Boundary preservation assertion (unmute commits): for the 6 unmute commits (5 from this task + Task 1's), `git diff HEAD~7..HEAD~1 --name-only | sort -u` returns ONLY `tests/integration/full_round.rs` (the bookkeeping commit at HEAD is the 7th commit in this plan; it modifies REQUIREMENTS.md and ROADMAP.md and is excluded from the boundary check for unmutes). Verify with `git log --grep='^test(12): unmute' --name-only --format=%H | grep -v '^[0-9a-f]\{40\}$\|^$' | sort -u` returning exactly `tests/integration/full_round.rs`.
    - Boundary preservation assertion (no Cargo.toml or CI changes): `git diff HEAD~7..HEAD -- 'Cargo.toml' '**/Cargo.toml' .github/workflows/` returns empty.
    - REPAIR-01 closure assertion: `grep -E "^- \[x\] \*\*REPAIR-01\*\*" .planning/REQUIREMENTS.md` returns a match (REPAIR-01 status is `[x]`). The Traceability table row for REPAIR-01 references Phase 12 and plan 12-02.
    - REPAIR-02 non-attestation assertion: REQUIREMENTS.md's REPAIR-02 status is UNCHANGED at `[ ]` (D-11 inherited from Phase 11; REPAIR-02 closes on PR observation, not in-plan). Verify with `grep -E "^- \[ \] \*\*REPAIR-02\*\*" .planning/REQUIREMENTS.md` returning a match.
    - ROADMAP.md bookkeeping assertion: the Phase 12 entry's `**Plans:**` line reflects `2 plans`, and the plans list includes `12-01-PLAN.md` and `12-02-PLAN.md` entries. v1.3 milestone header (`- ✅ v1.3 ...` style summary line) is UNCHANGED (D-14 — that's /gsd-ship's job).
    - Halt assertion (negative test, D-11 escape-valve): if any iteration's step (c) test FAILED, no commit was made for that iteration, the corresponding #[ignore] line is still present, all later iterations in this task were skipped, the bookkeeping commit was NOT made, REQUIREMENTS.md REPAIR-01 status was NOT flipped to `[x]`, and the executor has emitted a CHECKPOINT REACHED marker citing D-11 and naming the failing test + the proposed minimal repair. Earlier successful commits in this task remain bisect-clean.
  </acceptance_criteria>
  <done>
    All 6 #[ignore] attributes are gone from `tests/integration/full_round.rs`; `cargo test --test integration full_round` reports 8 passed / 0 failed / 0 ignored locally against pinned brew bitcoind v31; 5 atomic commits from this task plus Task 1's commit total 6 bisectable unmute commits with the prescribed subjects and CD-1 + CD-4 bodies (cargo invocation + PASS verdict + bitcoind --version + RSA fix SHA + Wallet fix SHA); 1 atomic bookkeeping commit at HEAD updates REQUIREMENTS.md (REPAIR-01 [x]) and ROADMAP.md (Phase 12 plans list); REPAIR-02 status unchanged (closes on PR observation per Phase 11 D-11 inherited); no boundary file modified by the unmute commits; v1.3 ship notes NOT touched (D-14). If the executor halted via D-11 on any iteration, earlier commits in the sequence remain, a CHECKPOINT REACHED marker is recorded, the bookkeeping commit is NOT made, REQUIREMENTS.md REPAIR-01 stays `[ ]`, and the next session resumes via Phase 13 (D-12).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner ↔ pinned brew bitcoind v31 binary | Local cargo test process spawns a real bitcoind via `corepc-node` (post-Phase-9 fixture). The trust boundary is the version-pin in `.bitcoind-version` (30.2 per CONTRIBUTING.md, brew-installed v31 for local-dev — Phase 9 documented this asymmetry; Phase 12 inherits it unchanged). CD-4's `bitcoind --version | head -1` capture per commit body anchors the asymmetry in commit history. |
| Plan 12-01 wallet repair ↔ Plan 12-02 unmute proofs | Each unmute commit's body references the Plan 12-01 wallet-fix commit SHA AND the Phase 11 Plan 01 RSA-fix SHA (cc20f6f...). The integrity of these references rests on `depends_on: [12-01]` — both fixes must be in history before any Plan 12-02 commit. The `git log --grep` SHA captures in Task 1 (a) and Task 2 pre-loop are the authoritative dependency verification. |
| .planning/REQUIREMENTS.md REPAIR-01 status ↔ ROADMAP.md Phase 12 status | D-13 specifies the post-cycle bookkeeping commit flips REPAIR-01 to `[x]` and updates ROADMAP.md. The two files must be updated atomically (in the same bookkeeping commit) to prevent a half-state where one file claims complete and the other does not. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06 | Tampering | Test-body edit smuggled into an unmute commit (D-04 inherits Plan 11-02 D-07's no-drive-by-edits rule) | mitigate | Per-task acceptance criteria assert `git diff <commit>~1 <commit> --stat` shows `1 file changed, 0 insertions(+), 1 deletion(-)` for each unmute commit. A test-body edit would surface as `+N insertions(+)` and fail acceptance. Plan 11-02 D-07 carries forward verbatim. |
| T-12-07 | Repudiation | Commit body lacks PASS-proof or one of the two required SHAs; reviewer cannot bisect or trace | mitigate | CD-1 body convention is enforced via acceptance criteria — each unmute commit body must contain the per-test cargo invocation, the verdict line, the `bitcoind --version` capture (CD-4), AND BOTH SHA references (RSA fix from Phase 11 + Wallet fix from Plan 12-01). The two SHAs together make the bisect target unambiguous. |
| T-12-08 | Tampering | Wire-format / commitment domain / coordinator-side drift slipping in alongside unmutes | mitigate (PRESERVED) | Plan 12-02 modifies ONLY `tests/integration/full_round.rs` (during the 6 unmute commits) plus `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` (in the bookkeeping commit only). Acceptance criteria explicitly check no other file is touched by the unmute commits. The wire format frozen by Phase 1 D-02, the commitment domain `SHA-256(SPKI_bytes)`, coordinator RSA decoding (cc20f6f), and the client wallet sign-call repair (Plan 12-01) are all OUTSIDE this plan's modification surface — guaranteed by the file-scope restriction. |
| T-12-09 | Denial of Service | A future hidden blocker (a "5th orthogonal blocker" per D-11) is silently absorbed by the executor instead of being surfaced for user decision | mitigate (POLICY) | D-11 escape-valve is encoded explicitly in Tasks 1 and 2: any per-test failure during step (c) triggers HALT + CHECKPOINT REACHED marker, with explicit "pre-authorized in-flight scope expansion is ZERO." Phase 11 already absorbed 3 Fix-then-resume blockers (vout-after-mine, bdk 2.3 wallet API, RSA SPKI) plus the 4th (bdk_wallet 2.3 SignOptions, addressed by Plan 12-01); D-11 codifies that a 5th deserves user input regardless of perceived smallness. Acceptance criteria include a halt-recovery state as a valid (non-failure) terminal condition; Phase 13 absorbs per D-12. |
| T-12-10 | Information Disclosure | PASS-proof log files (`target/integration-test-12-02-*.log`) checked into the repo | accept | The `tee target/...` pattern routes logs to the cargo `target/` directory, which is gitignored per Phase 9-05's CONTRIBUTING.md doc. No log content is checked in; only the verdict LINE (not the whole log) appears in the commit body. |
| T-12-11 | Spoofing | A test that passes against brew bitcoind v31 (local) but would fail against the pinned v30.2 (CI) | accept (deferred to Phase 11 D-11 inherited) | Local PASS-proof is captured against brew bitcoind v31 per CONTRIBUTING.md "Running integration tests" — the same invocation that produced the Phase 10 blocker and the Plan 11-02 halt. CI runs against pinned v30.2 (per `.bitcoind-version`). REPAIR-02 explicitly closes on PR observation in CI, not on local attestation (Phase 11 D-11). If CI surfaces a v30/v31 divergence on the Phase 11+12 PR, that becomes Phase 13 scope (D-12). CD-4's `bitcoind --version` capture in each commit body documents the version asymmetry explicitly. |
| T-12-12 | Tampering | Line-number drift between the documented sites (originally 164, 462, 730, 854, 911, 1236) and the actual file after each delete | mitigate | Task 2's action explicitly re-greps for the #[ignore] line by adjacent test-fn name at each iteration (step (b)), not by trusting the pre-Phase-11 line numbers. Acceptance criteria assert each #[ignore] line is found via name-based lookup before deletion. Task 1's `IGN_LINE` capture also uses name-based lookup as a sanity check. |
| T-12-13 | Tampering | depends_on violation — Plan 12-02 runs before Plan 12-01's wallet fix is in history | mitigate | Task 1 step (a) and Task 2 pre-loop both verify `WALLET_FIX_SHA` is non-empty (40-char hex from `git log --grep='^fix(12): trust_witness_utxo' --format=%H -1`) BEFORE any cargo test invocation. If empty, the executor HALTs with a depends_on violation marker. The wave structure (12-02 in wave 2, 12-01 in wave 1) makes this a planning-time invariant in addition to the runtime check. |
| T-12-14 | Tampering | Bookkeeping commit (D-13 — REPAIR-01 flip + ROADMAP update) lands BEFORE the green suite is proven | mitigate | The bookkeeping commit is the LAST step in Task 2, AFTER all 5 iterations complete AND the final whole-file `cargo test --test integration full_round` run reports `8 passed / 0 failed / 0 ignored`. If any iteration HALTs, the bookkeeping commit is NOT made and REQUIREMENTS.md REPAIR-01 stays `[ ]`. The "post-cycle bookkeeping" section in Task 2 explicitly steps (h)-(j) AFTER the post-loop final invocation. |
| T-12-15 | Repudiation | REPAIR-02 silently flipped to `[x]` in the bookkeeping commit despite not being in-plan | mitigate (POLICY) | Acceptance criteria explicitly assert REPAIR-02 status is UNCHANGED in REQUIREMENTS.md after the bookkeeping commit (`grep -E "^- \[ \] \*\*REPAIR-02\*\*"` returns a match). D-04 (inheriting Plan 11-02 D-11) keeps REPAIR-02 PR-observation-gated. The bookkeeping commit body explicitly states "REPAIR-02 status NOT touched in this plan — closes on PR observation..." to anchor the policy in commit history. |
| T-12-SC | Tampering | npm/pip/cargo installs (none in this plan) | accept | Plan 12-02 introduces ZERO new dependencies. No package-legitimacy gate required. `Cargo.toml` files are not edited; no `cargo add`, no `cargo install`. The plan exercises only existing pinned dependencies (corepc-node feature-pinned per Phase 10 Task 1, bitcoind v30.2 per `.bitcoind-version`, bdk_wallet 2.3.0 per Cargo.lock, all crates as locked). Boundary preservation assertion explicitly checks no Cargo.toml or CI changes. |
</threat_model>

<verification>
After both tasks complete (and assuming neither HALT was triggered):

1. `BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round 2>&1 | tee target/integration-test-12-02-final.log` reports `test result: ok. 8 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out`. This is the REPAIR-01 local-green closure proof (D-03 + D-13).
2. `grep -c "TODO(Phase-10)" tests/integration/full_round.rs` returns 0.
3. No `#[ignore]` attribute is adjacent to any of the six unmuted test fns (per the awk check in Task 2 acceptance criteria).
4. `git log --grep='^test(12): unmute' --oneline | wc -l | tr -d ' '` returns `6` (Task 1 + 5 from Task 2).
5. `git log --grep='^test(12): unmute' --reverse --format=%s` outputs exactly the six prescribed commit subjects in canonical-first then file order (1/6 through 6/6).
6. Each of the 6 unmute commit bodies contains its per-test cargo invocation, its `test result: ok. 1 passed` verdict line, a `bitcoind --version:` line (CD-4), AND both `RSA fix: <SHA>` and `Wallet fix: <SHA>` lines. The two SHAs differ from each other in every commit body.
7. `git log --grep='^docs(12): close REPAIR-01' --oneline | wc -l | tr -d ' '` returns `1` (the bookkeeping commit landed).
8. `grep -E "^- \[x\] \*\*REPAIR-01\*\*" .planning/REQUIREMENTS.md` returns a match — REPAIR-01 status is `[x]`.
9. `grep -E "^- \[ \] \*\*REPAIR-02\*\*" .planning/REQUIREMENTS.md` returns a match — REPAIR-02 status is UNCHANGED at `[ ]` (PR-observation-gated per Phase 11 D-11).
10. ROADMAP.md Phase 12 entry's `**Plans:**` line reflects `2 plans`, and the plans list includes both `12-01-PLAN.md` and `12-02-PLAN.md`. The v1.3 milestone summary header is UNCHANGED (D-14).
11. `git diff HEAD~7..HEAD -- tests/integration/mod.rs coordinator/src/api/handlers.rs coordinator/src/round/manager.rs coordinator/src/blind/rsa.rs shared/src/protocol.rs .github/workflows/ci.yml CONTRIBUTING.md client/src/wallet.rs client/src/round/input.rs` returns empty — none of these preservation-boundary files is modified by Plan 12-02. (client/src/wallet.rs was modified by Plan 12-01 in the prior commit range; Plan 12-02 does not touch it.)
12. `git diff HEAD~7..HEAD -- 'Cargo.toml' '**/Cargo.toml'` returns empty — no dependency changes in Plan 12-02.

If a HALT was triggered during Task 1 or Task 2:
- A CHECKPOINT REACHED marker exists in the conversation log naming the failing test, the failure mode (verbatim), whether the signature matches `Missing non-witness UTXO` (Plan 12-01 rollback signal) or a NEW signature (5th orthogonal blocker per D-11), and the proposed minimal repair.
- All commits made before the HALT remain (bisect-clean).
- REPAIR-01 is NOT yet closed; REQUIREMENTS.md REPAIR-01 stays `[ ]`; the bookkeeping commit was NOT made.
- Phase 13 (D-12) absorbs the new blocker and the remaining unmutes.
</verification>

<success_criteria>
- [ ] All 6 `#[ignore = "TODO(Phase-10): ..."]` attribute lines removed from `tests/integration/full_round.rs` (one per atomic commit, six unmute commits total: Task 1 produces 1, Task 2 produces 5).
- [ ] Commit order is canonical-first then file order (D-04 inherits Plan 11-02 D-05): full_round_three_clients (1/6) → blame_non_signer_timeout (2/6) → adversarial_replay_token (3/6) → adversarial_invalid_utxo (4/6) → adversarial_wrong_denomination (5/6) → round_restart_and_completion_after_blame (6/6).
- [ ] `cargo test --test integration full_round` reports 8 passed / 0 failed / 0 ignored locally against pinned brew bitcoind v31 via the CONTRIBUTING.md canonical invocation (REPAIR-01 closure proof per D-03 + D-13).
- [ ] Each of the 6 unmute commits has a CD-1 + CD-4 shaped body: per-test cargo invocation + verdict line + `bitcoind --version | head -1` + RSA fix SHA reference (Phase 11 cc20f6f...) + Wallet fix SHA reference (Plan 12-01).
- [ ] No test body, helper, or non-target file is modified by the 6 unmute commits — only the 6 attribute lines are deleted (D-04 inherits Plan 11-02 D-07).
- [ ] D-11 escape-valve gate is honored: any per-test failure HALTs and surfaces; pre-authorized in-flight scope expansion is ZERO; earlier successful commits remain bisect-clean; Phase 13 absorbs per D-12.
- [ ] D-13 bookkeeping commit lands AFTER the green suite is proven: REQUIREMENTS.md REPAIR-01 flips to `[x]` and Traceability table updates to reference Phase 12 plan 12-02; ROADMAP.md Phase 12 entry reflects 2 plans complete.
- [ ] REPAIR-02 status is UNCHANGED at `[ ]` in REQUIREMENTS.md — closes on PR observation per Phase 11 D-11 inherited (the corepc-node-feature-pin-check CI job, already landed at 4026f50, must be observed green on the Phase 11+12 combined PR).
- [ ] No CI changes, no new dependencies, no `Cargo.toml` edits, no v1.3 ship-notes work (D-14 defers ship notes to /gsd-ship after CI confirms PR green).
- [ ] No edits to client/src/wallet.rs (Plan 12-01's modification surface) or to coordinator/ or shared/ in Plan 12-02 — file scope is strictly `tests/integration/full_round.rs` for the unmute commits and `.planning/REQUIREMENTS.md` + `.planning/ROADMAP.md` for the bookkeeping commit.
</success_criteria>

<output>
Create `.planning/phases/12-repair-client-src-wallet-rs-260-bdk-wallet-2-3-signoptions-r/12-02-SUMMARY.md` when done. The summary should record:
- The 6 unmute commit SHAs (in canonical-first then file order) and the bookkeeping commit SHA (D-13).
- The final whole-file PASS verdict (`test result: ok. 8 passed; 0 failed; 0 ignored`) with `bitcoind --version | head -1` output (CD-4).
- Confirmation that REPAIR-01 is satisfied locally (D-03 + D-13) — and that the bookkeeping commit flipped REQUIREMENTS.md REPAIR-01 to `[x]` and updated ROADMAP.md Phase 12 entry.
- Explicit note that REPAIR-02 closure is DEFERRED to PR observation per Phase 11 D-11 inherited — a wrap-up phase or `/gsd-ship` flips it after CI green on the Phase 11+12 combined PR.
- The two SHA references used in all 6 unmute commit bodies (RSA fix from Phase 11; Wallet fix from Plan 12-01).
- If any HALT was triggered: the CHECKPOINT REACHED context (which test, which failure mode — verbatim, which iteration N/6, whether signature was `Missing non-witness UTXO` or NEW), and a pointer to where Phase 13 (D-12) should pick up. Earlier successful commits remain — list them.
- No ship-notes work performed (D-14 — handled by a wrap-up phase or /gsd-ship after CI confirms green).
</output>
</content>
</invoke>