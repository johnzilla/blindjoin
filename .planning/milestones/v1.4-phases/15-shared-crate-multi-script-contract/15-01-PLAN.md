---
phase: 15-shared-crate-multi-script-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/src/protocol.rs
  - shared/src/lib.rs
  - shared/Cargo.toml
  - shared/tests/ownership_proof_roundtrip.rs
autonomous: true
requirements: [ADVERT-04]
requirements_addressed: [ADVERT-04]
tags: [bip322, wire-format, ownership-proof, serde, roundtrip]
must_haves:
  truths:
    - "shared::protocol::OwnershipProof has the four-field v2 envelope (version: u8, witness_stack: Vec<Vec<u8>>, psbt_input_b64: Option<String>, script_type: Option<ScriptType>) per D-22 and D-24"
    - "OwnershipProof::from_json_hex_str decodes both v1.3 array-of-hex JSON AND new flat-struct JSON per CD-7 two-phase try-parse"
    - "OwnershipProof::to_json_hex_str emits the v1.3 array-of-hex shape iff version == 1 && psbt_input_b64.is_none() && script_type.is_none(); otherwise emits flat-struct JSON per CD-7"
    - "Five D-13 roundtrip cases (v2 P2WPKH, v2 P2TR, v2 P2SH-P2WPKH, v1 legacy array-of-hex decode, unknown version permissive decode) plus the corrupted-base64 sibling case ALL pass via cargo test -p shared --test ownership_proof_roundtrip"
    - "ScriptType is defined as a stub enum in shared/src/bip322.rs (existing file) so the OwnershipProof.script_type field type-checks WITHOUT introducing the full dispatcher (which 15-02 owns); the stub ScriptType is re-exported via shared::bip322::ScriptType"
    - "ScriptType serde encoding matches the ADVERT-02 wire form: snake_case with explicit rename for the kebab-case P2SH-P2WPKH variant (RESEARCH Open Question #3 recommendation)"
    - "v1.3 cross-phase invariant: cargo test --test full_round (existing integration suite) remains green after 15-01 lands"
    - "Wire-format roundtrip test ships FIRST as its own atomic commit per CD-10 and v1.3 REPAIR-01 lesson #1; 15-02 and 15-03 cannot land in the same commit"
  artifacts:
    - path: "shared/tests/ownership_proof_roundtrip.rs"
      provides: "5 D-13 roundtrip test functions + corrupted-base64 sibling case"
      contains: ["v2_roundtrip_p2wpkh", "v2_roundtrip_p2tr", "v2_roundtrip_p2sh_p2wpkh", "v1_legacy_decode_array_of_hex", "unknown_version_permissive_decode", "corrupted_base64_in_psbt_input_permissive_decode"]
    - path: "shared/src/protocol.rs"
      provides: "OwnershipProof v2 envelope + two-phase try-parse helpers"
      contains: ["pub struct OwnershipProof", "pub version: u8", "pub witness_stack: Vec<Vec<u8>>", "pub psbt_input_b64: Option<String>", "pub script_type: Option<", "fn default_proof_version", "fn from_json_hex_str", "fn to_json_hex_str"]
    - path: "shared/Cargo.toml"
      provides: "base64 direct dep (per RESEARCH A8 fallback path used in the roundtrip test for realistic v2 wire data); NO bip322/thiserror/proptest yet — those land in 15-02 and 15-03"
      contains: ["base64"]
  key_links:
    - from: "shared/src/protocol.rs"
      to: "shared/src/bip322.rs"
      via: "use crate::bip322::ScriptType"
      pattern: "use crate::bip322::ScriptType"
    - from: "shared/tests/ownership_proof_roundtrip.rs"
      to: "shared::protocol::OwnershipProof + shared::bip322::ScriptType"
      via: "integration test imports public API"
      pattern: "use shared::(protocol::OwnershipProof|bip322::ScriptType)"
---

<objective>
Land the OwnershipProof v2 wire envelope and its five D-13 roundtrip test cases as a SINGLE atomic commit, BEFORE the bip322 module split (15-02) or per-script verifier tests (15-03) land.

Purpose: Enforce v1.3 REPAIR-01 lesson #1 verbatim — the wire-format roundtrip test ships first so `git bisect` can identify it cleanly if Phase 16 or Phase 17 ever re-triggers a wire-format regression (CD-10 LOCKED). This plan introduces ZERO bip322-crate dependency and ZERO dispatcher logic; only the serde wire shape + a stub `ScriptType` enum that 15-02 will replace with the full dispatcher-backed enum.

Output: A green `cargo test -p shared --test ownership_proof_roundtrip` plus a green `cargo test --test full_round` (existing integration suite, cross-phase invariant).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-shared-crate-multi-script-contract/15-CONTEXT.md
@.planning/phases/15-shared-crate-multi-script-contract/15-RESEARCH.md
@.planning/phases/15-shared-crate-multi-script-contract/15-PATTERNS.md
@.planning/decisions/v1.4-adr.md
@shared/src/protocol.rs
@shared/src/bip322.rs
@shared/src/lib.rs
@shared/Cargo.toml
@Cargo.toml

<interfaces>
Wire form for OwnershipProof v2 (per CONTEXT D-22..D-25):
  - version: u8 with #[serde(default = "default_proof_version")] -> 1
  - witness_stack: Vec<Vec<u8>> with #[serde(default)]
  - psbt_input_b64: Option<String> with #[serde(skip_serializing_if = "Option::is_none")]
  - script_type: Option<ScriptType> with #[serde(skip_serializing_if = "Option::is_none")]

ScriptType (stub form for 15-01 — 15-02 expands with dispatcher integration):
  - pub enum ScriptType { P2wpkh, P2tr, P2shP2wpkh }
  - derives Debug + Clone + Copy + PartialEq + Eq + Serialize + Deserialize
  - #[serde(rename_all = "snake_case")] on enum
  - #[serde(rename = "p2sh-p2wpkh")] on P2shP2wpkh variant (matches ADVERT-02 kebab-case wire form per RESEARCH Open Question #3)

Two-phase try-parse signature (per CD-7 / RESEARCH Pattern 3 lines 489-509):
  - fn from_json_hex_str(s: &str) -> Result<Self, String>
    Phase 1: serde_json::from_str::<Vec<String>>(s) -> v1.3 array-of-hex
    Phase 2: serde_json::from_str::<Self>(s) -> flat-struct (v1 + v2)
  - fn to_json_hex_str(&self) -> String
    v1.3-form branch when (version == 1 && psbt_input_b64.is_none() && script_type.is_none())
    flat-struct branch otherwise
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add stub ScriptType enum to shared/src/bip322.rs and add base64 dev-dep to shared/Cargo.toml</name>
  <files>shared/src/bip322.rs, shared/Cargo.toml</files>
  <read_first>
    - shared/src/bip322.rs (full file — confirm current top-of-file imports and that no ScriptType exists yet)
    - shared/Cargo.toml (full file — confirm current dep list; do NOT add bip322, thiserror, or proptest here in 15-01)
    - .planning/phases/15-shared-crate-multi-script-contract/15-CONTEXT.md (D-22, D-24, D-28; CD-7, CD-10)
    - .planning/phases/15-shared-crate-multi-script-contract/15-RESEARCH.md (Open Question #3 recommendation for snake_case + kebab-rename; Architectural Responsibility Map row "Script-type detection")
    - .planning/phases/15-shared-crate-multi-script-contract/15-PATTERNS.md (Shared Pattern 4 workspace dep inheritance)
    - Cargo.toml (workspace root — confirm no bip322, thiserror, proptest direct pins beyond the existing thiserror = "1" at line 17 and proptest = "1" at line 28)
  </read_first>
  <behavior>
    - Test: serde_json::to_string(&ScriptType::P2wpkh) returns "\"p2wpkh\"" (snake_case lowercase)
    - Test: serde_json::to_string(&ScriptType::P2tr) returns "\"p2tr\""
    - Test: serde_json::to_string(&ScriptType::P2shP2wpkh) returns "\"p2sh-p2wpkh\"" (kebab-case via explicit rename, NOT snake_case "p2sh_p2wpkh")
    - Test: serde_json::from_str::<ScriptType>("\"p2sh-p2wpkh\"").unwrap() == ScriptType::P2shP2wpkh (deserialise from kebab)
    - Test: ScriptType derives Copy + Clone + Debug + PartialEq + Eq (compile-time bounds check via let _: fn() -> () = || { let a = ScriptType::P2wpkh; let _b = a; let _c: ScriptType = a; assert_eq!(a, ScriptType::P2wpkh); };)
    These tests live inline in the existing #[cfg(test)] mod tests at the bottom of shared/src/bip322.rs.
  </behavior>
  <action>
    Add a new public enum named ScriptType to shared/src/bip322.rs (alongside the existing primitives bip322_message_hash, build_bip322_to_spend, build_bip322_to_sign). Place the enum definition AFTER the existing primitives and BEFORE the #[cfg(test)] mod tests block. The enum has three variants P2wpkh, P2tr, P2shP2wpkh and derives Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize. Apply #[serde(rename_all = "snake_case")] at the enum level AND #[serde(rename = "p2sh-p2wpkh")] on the P2shP2wpkh variant (so the wire form matches ADVERT-02's kebab-case verbatim per RESEARCH Open Question #3 recommendation). Add the three inline #[test] fns described in the behavior block to the existing tests submodule. Do NOT introduce a bip322 crate dep, a thiserror dep, or any dispatcher logic — that all lands in 15-02. Do NOT introduce Bip322Error here. The flat shared/src/bip322.rs file STAYS in this plan; 15-02 is responsible for splitting it into the directory module per D-04.

    Add base64 = "0.22" to the [dependencies] block of shared/Cargo.toml (between hex = "0.4" and the end of [dependencies]). This is the RESEARCH A8 fallback path — base64 is needed in this plan for the roundtrip test's realistic v2 psbt_input_b64 fixture data (encoding a tiny synthetic PSBT-input byte string). Do NOT add bip322, thiserror, or proptest yet — those are 15-02 / 15-03 work. Decision IDs referenced: D-22, D-24, D-28 (ScriptType lives in shared::bip322, re-exported via OwnershipProof.script_type field), CD-7, CD-10; RESEARCH Open Question #3.
  </action>
  <verify>
    <automated>cargo build -p shared --tests 2>&amp;1 | tee /tmp/15-01-task1-build.log; cargo test -p shared --lib bip322::tests::scripttype_ 2>&amp;1 | tee /tmp/15-01-task1-test.log; grep -E '^base64\s*=\s*"0\.22"' shared/Cargo.toml &amp;&amp; grep -E 'pub enum ScriptType' shared/src/bip322.rs &amp;&amp; grep -E '#\[serde\(rename\s*=\s*"p2sh-p2wpkh"\)\]' shared/src/bip322.rs</automated>
  </verify>
  <acceptance_criteria>
    - shared/src/bip322.rs contains a line matching `pub enum ScriptType`
    - shared/src/bip322.rs contains the three variant identifiers P2wpkh, P2tr, P2shP2wpkh
    - shared/src/bip322.rs contains a line matching `#[serde(rename_all = "snake_case")]` on the enum
    - shared/src/bip322.rs contains a line matching `#[serde(rename = "p2sh-p2wpkh")]` on the P2shP2wpkh variant
    - shared/src/bip322.rs contains a derive line including all of: Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize
    - shared/Cargo.toml [dependencies] contains exactly one line matching `^base64\s*=\s*"0\.22"$`
    - shared/Cargo.toml does NOT contain a `bip322` line (15-02 adds that)
    - shared/Cargo.toml does NOT contain a `thiserror` line (15-02 adds that)
    - shared/Cargo.toml [dev-dependencies] does NOT contain a `proptest` line (15-03 adds that)
    - `cargo build -p shared --tests` exits 0
    - `cargo test -p shared --lib bip322::tests::scripttype_` exits 0 (and the inline tests assert the snake_case + kebab serde outputs)
    - `cargo test --test full_round` exits 0 (cross-phase invariant; v1.3 P2WPKH integration suite remains green)
  </acceptance_criteria>
  <done>ScriptType enum exists in shared/src/bip322.rs with the snake_case + kebab-case serde shape verified by inline tests; base64 = "0.22" is the only new dep in shared/Cargo.toml; no bip322 / thiserror / proptest deps added yet; full_round integration suite stays green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Evolve shared::protocol::OwnershipProof to the v2 four-field flat envelope with CD-7 two-phase try-parse encode/decode</name>
  <files>shared/src/protocol.rs</files>
  <read_first>
    - shared/src/protocol.rs (full file — confirm current 2-field OwnershipProof at lines 105-139, the existing from_json_hex_str/to_json_hex_str signatures, and the T-01-04 no-deny-unknown-fields invariant at lines 3-5)
    - shared/src/bip322.rs (after Task 1 — confirm ScriptType import path)
    - .planning/phases/15-shared-crate-multi-script-contract/15-CONTEXT.md (D-22 four-field shape verbatim; D-23 String envelope transport; D-24 script_type sibling field; D-25 version default = 1; CD-7 two-phase try-parse)
    - .planning/phases/15-shared-crate-multi-script-contract/15-RESEARCH.md (Pattern 3 two-phase try-parse code template lines 472-527; Pitfall 7 keep Result<Self, String> to avoid module cycle)
    - .planning/phases/15-shared-crate-multi-script-contract/15-PATTERNS.md (the "EVOLVED OwnershipProof" section lines 277-360 with exact serde attribute placement; Shared Patterns 2, 3, 7)
    - coordinator/src/api/handlers.rs (around line 136 — confirm the existing from_json_hex_str call site that must keep working; v1.3 wire compatibility check)
    - client/src/round/input.rs (around lines 64-65 — confirm the existing OwnershipProof { witness_stack } construction site that must keep working unchanged on the v1.3 wire path)
    - tests/integration/full_round.rs (full file — confirm the existing integration test path that must remain green; this is the cross-phase invariant)
  </read_first>
  <behavior>
    The inline #[cfg(test)] tests in shared/src/protocol.rs assert the construction shape works; the EXTERNAL roundtrip-test integration file in Task 3 owns the five D-13 cases.
    - Test: OwnershipProof default-via-serde from `"{}"` JSON deserialises with version = 1, empty witness_stack, no psbt_input_b64, no script_type (D-25)
    - Test: round-trip JSON of `{"version":2,"witness_stack":[],"psbt_input_b64":"AA==","script_type":"p2wpkh"}` preserves all four fields after serde_json roundtrip
    - Test: OwnershipProof with version=1, empty psbt_input_b64, empty script_type encodes via to_json_hex_str to the v1.3 array-of-hex form (compatibility branch per CD-7)
    - Test: from_json_hex_str of the v1.3 array shape `["3045","02ab"]` returns version=1 with two-element witness_stack and no v2 fields
    - Test: from_json_hex_str of the v2 flat-struct JSON returns the matching struct
    - Test: from_json_hex_str of `{"version":3,"witness_stack":[]}` is PERMISSIVE (returns Ok with version=3); the verify dispatch in 15-02 owns rejecting unknown versions
  </behavior>
  <action>
    Replace the existing OwnershipProof struct definition and its impl block in shared/src/protocol.rs (currently at lines 105-139, the 2-field witness_stack-only shape) with the D-22 four-field shape verbatim. Add the new struct AFTER the existing doc comment (which can be updated to describe the v2 envelope). The struct derives Debug, Clone, Serialize, Deserialize. Fields in order: `pub version: u8` with `#[serde(default = "default_proof_version")]` per D-25; `pub witness_stack: Vec<Vec<u8>>` with `#[serde(default)]` per D-22; `pub psbt_input_b64: Option<String>` with `#[serde(skip_serializing_if = "Option::is_none")]` per D-22; `pub script_type: Option<crate::bip322::ScriptType>` with `#[serde(skip_serializing_if = "Option::is_none")]` per D-22 and D-24. Add a free function `fn default_proof_version() -> u8 { 1 }` immediately after the struct. Do NOT add #[serde(deny_unknown_fields)] per the T-01-04 invariant called out at protocol.rs lines 3-5 (RESEARCH Anti-Patterns; D-06).

    Replace from_json_hex_str body with the CD-7 two-phase try-parse: Phase 1 calls serde_json::from_str::<Vec<String>>(s); on Ok, hex-decodes each item into witness_stack and returns Self with version=1 and both Option fields = None. On Phase 1 Err, falls back to serde_json::from_str::<Self>(s) and returns that, mapping the serde error to a String. KEEP the return type `Result<Self, String>` unchanged per RESEARCH Pitfall 7 (typing it as Bip322Error would create a module cycle with shared::bip322; the typed dispatcher error stays inside bip322).

    Replace to_json_hex_str body with the CD-7 branch: if `self.version == 1 && self.psbt_input_b64.is_none() && self.script_type.is_none()`, emit the v1.3 array-of-hex form (Vec<String> hex-encode of witness_stack) — preserves bit-exact v1.3 wire compatibility for the cross-phase invariant. Otherwise emit `serde_json::to_string(self)` (the flat-struct JSON, covers both v1-explicit and v2 envelopes).

    Add `#[cfg(test)] mod tests` (or extend if one exists) at the bottom of shared/src/protocol.rs with the six small unit tests described in the behavior block. Decision IDs referenced: D-22, D-23, D-24, D-25, D-06 (T-01-04 invariant preserved), CD-7, RESEARCH Pitfall 7. Do NOT touch coordinator/src/api/handlers.rs:136 or client/src/round/input.rs:64-65 — both call sites continue to compile because the helper signatures (from_json_hex_str returns Result<Self, String>; to_json_hex_str returns String) are unchanged AND the v1.3 wire encoding path is preserved by the branch in to_json_hex_str. Do NOT delete or rename the existing OwnershipProof doc comment paragraphs; revise them in-place to describe the v2 envelope semantics.
  </action>
  <verify>
    <automated>cargo build -p shared --tests 2>&amp;1 | tee /tmp/15-01-task2-build.log; cargo test -p shared --lib protocol::tests 2>&amp;1 | tee /tmp/15-01-task2-test.log; cargo build --workspace 2>&amp;1 | tee /tmp/15-01-task2-workspace.log; cargo test --test full_round 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - shared/src/protocol.rs contains exactly one `pub struct OwnershipProof` whose body declares all four fields: `pub version: u8`, `pub witness_stack: Vec<Vec<u8>>`, `pub psbt_input_b64: Option<String>`, `pub script_type: Option<crate::bip322::ScriptType>`
    - shared/src/protocol.rs contains `#[serde(default = "default_proof_version")]` immediately above the version field
    - shared/src/protocol.rs contains `#[serde(default)]` immediately above the witness_stack field
    - shared/src/protocol.rs contains TWO occurrences of `#[serde(skip_serializing_if = "Option::is_none")]` above the psbt_input_b64 and script_type fields
    - shared/src/protocol.rs contains the helper `fn default_proof_version() -> u8 { 1 }`
    - shared/src/protocol.rs does NOT contain `#[serde(deny_unknown_fields)]` anywhere
    - shared/src/protocol.rs's from_json_hex_str function signature stays as `pub fn from_json_hex_str(s: &str) -> Result<Self, String>` (unchanged return type per Pitfall 7)
    - shared/src/protocol.rs's to_json_hex_str function signature stays as `pub fn to_json_hex_str(&self) -> String` (unchanged return type)
    - `cargo build -p shared --tests` exits 0
    - `cargo build --workspace` exits 0 (coordinator/handlers.rs and client/round/input.rs continue to compile against the evolved struct because their call sites only use witness_stack + the helpers, and the v1.3-format wire output is preserved by the CD-7 branch)
    - `cargo test -p shared --lib protocol::tests` exits 0 (the six inline unit tests pass)
    - `cargo test --test full_round` exits 0 (v1.3 integration suite stays green — cross-phase invariant)
  </acceptance_criteria>
  <done>OwnershipProof has the v2 four-field shape with the CD-7 two-phase try-parse encode/decode; v1.3 wire-format bit-exact compatibility is preserved by the to_json_hex_str compatibility branch; existing coordinator and client call sites compile and run unchanged; full_round integration suite stays green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create shared/tests/ownership_proof_roundtrip.rs with the 5 D-13 cases + corrupted-base64 sibling; atomic commit per CD-10</name>
  <files>shared/tests/ownership_proof_roundtrip.rs</files>
  <read_first>
    - shared/src/protocol.rs (after Task 2 — confirm OwnershipProof v2 shape + helper signatures)
    - shared/src/bip322.rs (after Task 1 — confirm ScriptType variants + serde wire form)
    - shared/Cargo.toml (after Task 1 — confirm base64 = "0.22" available for fixture construction)
    - .planning/phases/15-shared-crate-multi-script-contract/15-CONTEXT.md (D-13 five-case enumeration verbatim; CD-7 two-phase try-parse semantics; CD-10 atomic commit constraint; cross-phase invariant note)
    - .planning/phases/15-shared-crate-multi-script-contract/15-RESEARCH.md (Example 5 lines 845-908 — five-case test code verbatim; Pitfall 5 P2SH-P2WPKH network-agnostic SPK; Pitfall 7 from_json_hex_str returns String error)
    - .planning/phases/15-shared-crate-multi-script-contract/15-PATTERNS.md (the ownership_proof_roundtrip.rs section lines 404-446; Shared Pattern 7 serde_json::from_str + assert roundtrip from shared/src/errors.rs:46-76)
    - shared/src/errors.rs (full file — confirm the existing roundtrip-assert pattern at lines 46-76; the new file follows that same shape)
    - tests/integration/full_round.rs (full file — cross-phase invariant; confirm 15-01 changes do not break)
  </read_first>
  <behavior>
    The file contains exactly six #[test] fns (the 5 D-13 cases + one sibling for the corrupted-base64 robustness check). All six are integration-style tests in shared/tests/ (separate crate from shared/src/). Each test imports `use shared::bip322::ScriptType` and `use shared::protocol::OwnershipProof`. The fixtures use realistic-shaped data (base64 strings, hex witness items) without any bip322 crate calls — this plan introduces NO bip322 crate dep. The corrupted-base64 case asserts the JSON decode is permissive (returns Ok); the actual base64 decode failure surfaces downstream in 15-02 / 15-03 / Phase 16 work as Bip322Error::DecodeError.
    - Test 1 `v2_roundtrip_p2wpkh`: build OwnershipProof { version: 2, witness_stack: vec![], psbt_input_b64: Some(realistic_base64_string), script_type: Some(ScriptType::P2wpkh) }; encode via to_json_hex_str; decode via from_json_hex_str; assert all four fields equal
    - Test 2 `v2_roundtrip_p2tr`: same shape, script_type: Some(ScriptType::P2tr); JSON form has "p2tr"
    - Test 3 `v2_roundtrip_p2sh_p2wpkh`: same shape, script_type: Some(ScriptType::P2shP2wpkh); JSON form contains the kebab-case "p2sh-p2wpkh" (assert raw JSON substring)
    - Test 4 `v1_legacy_decode_array_of_hex`: input string is `r#"["3045022100abcd","02ab1234"]"#`; from_json_hex_str returns Ok with version=1, witness_stack.len()=2, both Option fields None
    - Test 5 `unknown_version_permissive_decode`: input string is `r#"{"version":3,"witness_stack":[]}"#`; from_json_hex_str returns Ok with version=3 (permissive at decode per D-25; verify-dispatch rejection lives in 15-02)
    - Test 6 (sibling) `corrupted_base64_in_psbt_input_permissive_decode`: input string is `r#"{"version":2,"psbt_input_b64":"not-base64-!!!","script_type":"p2wpkh"}"#`; from_json_hex_str returns Ok (the JSON layer is permissive; downstream base64 decode is 15-02/Phase 16 work)
  </behavior>
  <action>
    Create the new file shared/tests/ownership_proof_roundtrip.rs (the shared/tests/ directory does not yet exist; this is the first file under it). Use the RESEARCH Example 5 code template (lines 845-908 of 15-RESEARCH.md) verbatim with these implementation specifics: use a small valid base64 string for psbt_input_b64 fixtures (e.g., "cHNidP8BAAA=" — a six-byte placeholder; do NOT require it to be a valid PSBT byte stream because no decoder runs in 15-01). For the three v2 roundtrip tests, after encoding via to_json_hex_str, assert that the JSON output contains BOTH the "version":2 substring AND the expected script_type wire string ("p2wpkh", "p2tr", or "p2sh-p2wpkh"). For test 5 (unknown_version_permissive_decode), explicitly assert `parsed.version == 3` and document inline that the rejection lives at the verify-dispatch layer landing in 15-02 (per D-25). For test 6 (corrupted_base64_in_psbt_input_permissive_decode), assert `parsed.version == 2`, `parsed.psbt_input_b64.as_deref() == Some("not-base64-!!!")`, and document that downstream base64 decode failure surfaces as Bip322Error::DecodeError in 15-02. Do NOT import bip322 crate (not added yet). Do NOT import thiserror (not added yet). Do NOT use proptest (not added yet) — these are 5 enumerated #[test] fns + 1 sibling, not property tests. Decision IDs referenced: D-13 (five-case shape verbatim), D-25 (permissive decode of unknown version), CD-7 (two-phase try-parse), CD-10 (atomic commit FIRST per REPAIR-01 lesson #1). After file creation, run the full cross-phase invariant gate before declaring the task done. Then create a SEPARATE atomic commit for this plan's changes (CD-10 demands 15-01 ships as its own commit before 15-02 lands).
  </action>
  <verify>
    <automated>cargo test -p shared --test ownership_proof_roundtrip 2>&amp;1 | tee /tmp/15-01-task3-test.log; cargo test --test full_round 2>&amp;1 | tail -40; cargo build --workspace 2>&amp;1 | tail -20; test -f shared/tests/ownership_proof_roundtrip.rs &amp;&amp; grep -c '^#\[test\]' shared/tests/ownership_proof_roundtrip.rs</automated>
  </verify>
  <acceptance_criteria>
    - shared/tests/ownership_proof_roundtrip.rs exists as a regular file
    - `grep -c '^#\[test\]' shared/tests/ownership_proof_roundtrip.rs` outputs exactly `6`
    - shared/tests/ownership_proof_roundtrip.rs contains all six test fn names: `v2_roundtrip_p2wpkh`, `v2_roundtrip_p2tr`, `v2_roundtrip_p2sh_p2wpkh`, `v1_legacy_decode_array_of_hex`, `unknown_version_permissive_decode`, `corrupted_base64_in_psbt_input_permissive_decode`
    - shared/tests/ownership_proof_roundtrip.rs contains `use shared::bip322::ScriptType` and `use shared::protocol::OwnershipProof`
    - shared/tests/ownership_proof_roundtrip.rs does NOT contain `use bip322::` (the crate dep lands in 15-02)
    - shared/tests/ownership_proof_roundtrip.rs does NOT contain `use thiserror` (lands in 15-02)
    - shared/tests/ownership_proof_roundtrip.rs does NOT contain `use proptest` (lands in 15-03)
    - The v2_roundtrip_p2sh_p2wpkh test's encoded JSON output contains the substring "p2sh-p2wpkh" (kebab-case; assert in test body)
    - `cargo test -p shared --test ownership_proof_roundtrip` exits 0 with `test result: ok. 6 passed; 0 failed`
    - `cargo test --test full_round` exits 0 (cross-phase invariant)
    - `cargo build --workspace` exits 0
  </acceptance_criteria>
  <done>shared/tests/ownership_proof_roundtrip.rs contains the 5 D-13 cases + 1 sibling for corrupted-base64 robustness; all six pass; full_round integration suite stays green; this plan is ready to ship as its OWN atomic commit per CD-10 / REPAIR-01 lesson #1, BEFORE 15-02 introduces the bip322 crate dep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client wire -> coordinator wire (`InputRegRequest.ownership_proof: String`) | JSON-shaped envelope crosses an untrusted network boundary; permissive decode without strict version policing is the new attack surface |
| serde_json deserialization -> shared::protocol::OwnershipProof | Untrusted JSON drives default-filling + Option fields; the no-deny-unknown-fields invariant means forward-compat is preserved but unrecognised fields are silently dropped |
| bip322 crate dep additions (FUTURE — not in 15-01) | Bip322 / thiserror / proptest direct deps will arrive in 15-02 / 15-03; supply-chain gate is enforced when those land, not here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-V1.4-MOD-01 | Tampering | shared::protocol::OwnershipProof wire-format envelope | mitigate | Wire-format roundtrip test ships FIRST as its own atomic commit per CD-10 / D-13 / v1.3 REPAIR-01 lesson #1; the `version: u8` envelope with #[serde(default)] enables v1<->v2 coexistence without ambiguity; 5 D-13 roundtrip cases + 1 corrupted-base64 sibling case live in shared/tests/ownership_proof_roundtrip.rs and run on every `cargo test -p shared` invocation. The serialized v1.3 array-of-hex compatibility branch in to_json_hex_str preserves bit-exact wire compatibility, gating the v1.3 cross-phase invariant. |
| T-15-01-V1.4-CRIT-01-precursor | Spoofing | OwnershipProof.script_type sibling field | mitigate (partial — full mitigation in 15-02 / Phase 16) | 15-01 establishes script_type as Option<ScriptType> on the wire envelope (D-24 sibling field, NOT inferred from PSBT contents). The full V1.4-CRIT-01 mitigation (dispatcher-only pub API + 9-combination cross-shape rejection matrix) lands in 15-02 + 15-03; this plan's role is laying the wire-shape foundation that the dispatcher then enforces. Coordinator-side `detect_script_type(on_chain_spk)` cross-check against the declared field is Phase 16 work per D-10. |
| T-15-01-T-01-04 | Tampering | serde forward-compat semantics | accept | NO #[serde(deny_unknown_fields)] on OwnershipProof per D-06 / T-01-04 — protocol evolution is supported by silently dropping unknown fields. v1.3 REPAIR-02 carry-forward and existing comment at shared/src/protocol.rs:3-5 lock this invariant. |
| T-15-01-MEMEXH | Denial of Service | OwnershipProof.psbt_input_b64 unbounded base64 string | accept | 15-01 does not decode the base64 payload; the JSON layer is permissive. Memory exhaustion mitigation lives at the coordinator's tower-http per-route body-size limit (Phase 8 work, already shipped). 15-01's corrupted_base64_in_psbt_input_permissive_decode test asserts non-panicking JSON decode of corrupt payloads. |
| T-15-01-PII | Information Disclosure | OwnershipProof error messages from from_json_hex_str | mitigate | from_json_hex_str returns `Result<Self, String>` per Pitfall 7; the String error path includes only JSON parse error class + hex decode error class. No UTXO outpoint, address, key, or amount is interpolated into the error string. Project constraint "no PII logging" honoured at the wire-decode layer. |

**Mitigation:** Phase 15 is in the cryptographic-verification path; V1.4-MOD-01 is the load-bearing threat for this plan. The atomic-commit ordering enforced by CD-10 is the procedural mitigation.
</threat_model>

<verification>
Phase-level checks after this plan lands:
- `cargo test -p shared --test ownership_proof_roundtrip` exits 0 with 6 passing tests
- `cargo test --test full_round` exits 0 (cross-phase invariant for v1.3 integration suite)
- `cargo build --workspace` exits 0
- `grep -E '^bip322' shared/Cargo.toml` exits non-zero (the bip322 dep is NOT yet added in 15-01)
- `grep -E '^thiserror' shared/Cargo.toml` exits non-zero (thiserror is NOT yet added in 15-01)
- `grep -E '^base64\s*=\s*"0\.22"' shared/Cargo.toml` exits 0 (the only new direct dep in 15-01)
- The git commit for 15-01 contains exactly the four files in `files_modified` and zero others (CD-10 atomic-commit constraint, git-bisect-friendly)
</verification>

<success_criteria>
Plan 15-01 is complete when:
1. ADVERT-04's roundtrip-test-FIRST requirement (REQUIREMENTS.md, REPAIR-01 lesson #1) is materially satisfied: shared/tests/ownership_proof_roundtrip.rs ships with 5 D-13 cases + 1 sibling and all six pass.
2. The OwnershipProof v2 envelope (D-22, D-23, D-24, D-25, CD-7) is in place at shared/src/protocol.rs with the two-phase try-parse encode/decode preserving v1.3 wire compatibility.
3. ScriptType stub enum (P2wpkh, P2tr, P2shP2wpkh) is exposed at shared::bip322::ScriptType with the snake_case + kebab-case serde wire form per RESEARCH Open Question #3.
4. base64 = "0.22" is the ONLY new direct dep introduced in this plan (RESEARCH A8 fallback path; bip322 / thiserror / proptest are deferred to 15-02 / 15-03 per CD-10).
5. Existing coordinator (handlers.rs:136) and client (round/input.rs:64-65) call sites compile and execute unchanged; cargo build --workspace exits 0.
6. v1.3 cross-phase invariant holds: `cargo test --test full_round` exits 0 after this plan lands.
7. Plan ships as a single atomic commit per CD-10 (REPAIR-01 lesson #1 verbatim).
</success_criteria>

<output>
Create `.planning/phases/15-shared-crate-multi-script-contract/15-01-SUMMARY.md` when done. SUMMARY should record: which D-13 cases are now covered, the exact diff to OwnershipProof's serde attribute placement (so 15-02 can reason about cycle-avoidance), the realistic-base64 fixture chosen for v2 roundtrip tests, any deviation from CD-7 default behaviour (none expected), and the cross-phase-invariant verification result (full_round green).
</output>
