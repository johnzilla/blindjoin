---
phase: 16-coordinator-integration-advertisement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - coordinator/src/config.rs
  - shared/src/protocol.rs
  - coordinator/src/api/handlers.rs
autonomous: true
requirements: [ADVERT-01]
requirements_addressed: [ADVERT-01]
tags: [coordinator, config, wire-format, info-response, bip-allowlist]

must_haves:
  truths:
    - "D-35: BipConfig lives in a top-level [bip] section in coordinator.toml with BLINDJOIN__COORDINATOR__BIP__* env-var prefix; pub bip: BipConfig field on CoordinatorConfig carries #[serde(default)] for v1.3 config-file compat."
    - "D-36: BipConfig::validate() rejects all-false (at least one allow_* must be true) with an error message naming at least one BLINDJOIN__COORDINATOR__BIP__ALLOW_* env-var override path; fail-fast at boot per Phase 8 hardening pattern."
    - "D-37: output_script_type lives in [bip] alongside the allow_* flags; BipConfig::validate() asserts allows(output_script_type), refusing to start when output_script_type=p2tr but allow_p2tr=false."
    - "D-38: BipConfig is a 4-field + 3-method struct: fields allow_p2wpkh/allow_p2tr/allow_p2sh_p2wpkh/output_script_type; methods allows/supported/validate; serde defaults wire to default_true and default_output_script_type."
    - "D-42: InfoResponse gains supported_script_types: Vec<ScriptType> + output_script_type: ScriptType, both with #[serde(default)] returning P2wpkh-only legacy values for v1.3↔v1.4 bidirectional roundtrip."
    - "D-52: Bip322Error variants surfacing from BipConfig::validate() map to startup-time anyhow errors; no new wire ErrorCode introduced (carries to 16-02 dispatcher wiring)."
    - "An operator who omits `[bip]` from coordinator.toml boots cleanly with all three script types allowed and output_script_type=p2wpkh (v1.3 config-file backwards compat)."
    - "A v1.4 client calling GET /round/info against the v1.4 coordinator receives a JSON body with `supported_script_types` as an array of kebab-case strings and `output_script_type` as a single kebab-case string."
    - "A v1.3 client decoding a v1.4 `/round/info` response succeeds (silently drops the two new fields) — `InfoResponse` does not declare deny_unknown_fields."
    - "A v1.4 coordinator decoding a v1.3 `InfoResponse` JSON (no new fields present) succeeds with `supported_script_types = [P2wpkh]` and `output_script_type = P2wpkh` via #[serde(default)] hooks."
    - "v1.3 cross-phase invariant holds: `cargo test --test integration full_round` exits 0 at this plan boundary (only additive struct fields with serde defaults are introduced)."
  artifacts:
    - path: "coordinator/src/config.rs"
      provides: "BipConfig struct + 4 fields + 3 methods (allows / supported / validate) + 2 default_* fns + pub bip on CoordinatorConfig + validate() extension + with_defaults() field"
      contains: "pub struct BipConfig"
    - path: "shared/src/protocol.rs"
      provides: "InfoResponse.supported_script_types + InfoResponse.output_script_type + 2 default_legacy_* fns"
      contains: "supported_script_types"
    - path: "coordinator/src/api/handlers.rs"
      provides: "get_info populates the two new InfoResponse fields from state.config.bip"
      contains: "state.config.bip.supported()"
  key_links:
    - from: "coordinator/src/config.rs::CoordinatorConfig::validate"
      to: "coordinator/src/config.rs::BipConfig::validate"
      via: "self.bip.validate()? chained at end of existing fn body"
      pattern: "self\\.bip\\.validate\\(\\)"
    - from: "coordinator/src/api/handlers.rs::get_info"
      to: "coordinator/src/config.rs::BipConfig::supported"
      via: "state.config.bip.supported() read into InfoResponse field"
      pattern: "config\\.bip\\.supported"
    - from: "shared/src/protocol.rs::InfoResponse"
      to: "shared/src/bip322/mod.rs::ScriptType"
      via: "use crate::bip322::ScriptType in protocol.rs"
      pattern: "use crate::bip322::ScriptType"
---

<objective>
Land the v1.4 **wire/config-first atomic commit** for Phase 16 per D-53: introduce `BipConfig` in `coordinator/src/config.rs` with fail-fast `validate()`, extend `shared::protocol::InfoResponse` with two new fields (`supported_script_types: Vec<ScriptType>` + `output_script_type: ScriptType`) gated by `#[serde(default)]` for v1.3↔v1.4 bidirectional compatibility, populate the `/round/info` handler at `coordinator/src/api/handlers.rs::get_info` from `state.config.bip`, and update the `CoordinatorConfig::with_defaults()` test helper so coordinator unit tests still compile (RESEARCH A4).

Purpose: Establish the v1.4 config domain and wire shape BEFORE the behavior change (16-02) and BEFORE the advertisement change (16-03). This honors v1.3 REPAIR-01 lesson #1 — wire format ships first, behavior follows. Maps requirement **ADVERT-01** (config struct + validation + Info-Response wire-form half of the advertisement contract; PKARR half lands in 16-03).

Output: One atomic commit containing the new struct, extended wire type, handler population, with_defaults() update, and unit-test coverage of the serde-default v1.3↔v1.4 roundtrip plus the all-false validate() rejection plus the env-var bool override roundtrip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/decisions/v1.4-adr.md
@.planning/phases/16-coordinator-integration-advertisement/16-CONTEXT.md
@.planning/phases/16-coordinator-integration-advertisement/16-RESEARCH.md
@.planning/phases/15-shared-crate-multi-script-contract/15-01-SUMMARY.md
@.planning/phases/15-shared-crate-multi-script-contract/15-02-SUMMARY.md

# Source code (read before editing — see <read_first> per task)
@coordinator/src/config.rs
@shared/src/protocol.rs
@coordinator/src/api/handlers.rs
@shared/src/bip322/mod.rs

<interfaces>
<!-- LOCKED API surface inherited from Phase 15 — Phase 16 consumes verbatim. -->

From shared/src/bip322/mod.rs (Phase 15 outputs):
```rust
// Serde wire form: "p2wpkh" | "p2tr" | "p2sh-p2wpkh" (kebab-case rename on P2shP2wpkh).
#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
#[serde(rename_all = "snake_case")]
pub enum ScriptType {
    P2wpkh,
    P2tr,
    #[serde(rename = "p2sh-p2wpkh")]
    P2shP2wpkh,
}
```

From shared/src/protocol.rs (existing — Phase 15 file-top invariant binds Phase 16):
```rust
// NO #[serde(deny_unknown_fields)] on any struct — forward compat per D-06 / T-01-04.
// All structs silently drop unknown fields, allowing protocol evolution without breaking
// older clients or coordinators.

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct InfoResponse {
    pub version: String,
    pub network: String,
    pub denomination_sats: u64,
    pub min_participants: u32,
    pub max_participants: u32,
    pub round_state: String,
    pub participants_registered: u32,
    pub rsa_pubkey_hash: Option<String>,
    pub rsa_pubkey_der_b64: Option<String>,
    pub round_id: Option<uuid::Uuid>,
    // Phase 16 appends two new fields here (with #[serde(default)] for bidirectional compat).
}
```

From coordinator/src/config.rs (existing pattern at lines 25-72 — default_* fn idiom):
```rust
#[serde(default = "default_ban_file_path")]
pub ban_file_path: String,
// ...
fn default_ban_file_path() -> String { "ban_list.jsonl".to_string() }
```

From coordinator/src/config.rs (CoordinatorConfig::validate at lines 157-185):
```rust
pub fn validate(&self) -> anyhow::Result<()> {
    let c = &self.coordinator;
    anyhow::ensure!(
        (1..=60_000).contains(&c.rate_limit_info_per_min),
        "coordinator.rate_limit_info_per_min must be in 1..=60_000; got {}. \
         Set BLINDJOIN__COORDINATOR__RATE_LIMIT_INFO_PER_MIN to a value in that range.",
        c.rate_limit_info_per_min,
    );
    // ... 3 more anyhow::ensure! blocks ...
    Ok(())
}
```

From coordinator/src/api/handlers.rs::get_info (lines 46-68):
```rust
pub async fn get_info(State(state): State<AppState>) -> Json<InfoResponse> {
    let guard = state.round.read().await;
    // ... build rsa fields ...
    Json(InfoResponse {
        version: env!("CARGO_PKG_VERSION").to_string(),
        network: state.config.network.bitcoin_network.clone(),
        // ... existing fields ...
        round_id: Some(guard.round_id),
    })
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add BipConfig struct + validate() + read-side methods in coordinator/src/config.rs</name>
  <files>coordinator/src/config.rs</files>
  <read_first>
    - coordinator/src/config.rs (full file — lines 1-216; understand the existing per-field default_* fn pattern at lines 54-72 and the validate() pattern at lines 157-185 verbatim before adding the new section)
    - shared/src/bip322/mod.rs lines 140-160 (ScriptType enum + serde rename_all = "snake_case" + explicit rename = "p2sh-p2wpkh")
    - .planning/phases/16-coordinator-integration-advertisement/16-CONTEXT.md §D-35..D-38 (BipConfig field shape verbatim) + §D-40 (alphabetical canonical CSV order) + §CD-11 (alphabetical supported())
    - .planning/phases/16-coordinator-integration-advertisement/16-RESEARCH.md §"Pattern 1: BipConfig field shape (D-38)" lines 237-300 + §Pitfall 5 (config 0.15 env-var bool parsing — `"true"`/`"false"` only, NOT `"1"`/`"0"`) + §Assumptions A4 (with_defaults() must include new field literally)
  </read_first>
  <behavior>
    Test 1 — `bip_config_default_via_serde_from_empty_object`: `serde_json::from_str::<BipConfig>("{}")` returns a struct with `allow_p2wpkh=true`, `allow_p2tr=true`, `allow_p2sh_p2wpkh=true`, `output_script_type=ScriptType::P2wpkh` (all defaults fire per D-38).
    Test 2 — `bip_config_validate_rejects_all_false`: `BipConfig { allow_p2wpkh: false, allow_p2tr: false, allow_p2sh_p2wpkh: false, output_script_type: ScriptType::P2wpkh }.validate()` returns `Err(_)` whose `to_string()` contains the substring `"at least one allow_*"` AND `"BLINDJOIN__COORDINATOR__BIP__ALLOW_P2WPKH"` (D-36 error-message contract per RESEARCH §Specific Ideas lines 286-302).
    Test 3 — `bip_config_validate_rejects_output_not_in_allowed_set`: `BipConfig { allow_p2wpkh: true, allow_p2tr: false, allow_p2sh_p2wpkh: false, output_script_type: ScriptType::P2tr }.validate()` returns `Err(_)` whose `to_string()` contains `"output_script_type"` AND `"BLINDJOIN__COORDINATOR__BIP__OUTPUT_SCRIPT_TYPE"` (D-37 — output must be in allowed set).
    Test 4 — `bip_config_validate_accepts_defaults`: `BipConfig` built from `serde_json::from_str("{}")` `.validate()` returns `Ok(())`.
    Test 5 — `bip_config_supported_returns_alphabetical_canonical_order`: For an all-true `BipConfig`, `.supported()` returns `vec![ScriptType::P2shP2wpkh, ScriptType::P2tr, ScriptType::P2wpkh]` (CD-11 alphabetical: p2sh-p2wpkh < p2tr < p2wpkh by wire-form string).
    Test 6 — `bip_config_supported_skips_disallowed`: For `BipConfig { allow_p2wpkh: true, allow_p2tr: false, allow_p2sh_p2wpkh: true, .. }`, `.supported()` returns `vec![ScriptType::P2shP2wpkh, ScriptType::P2wpkh]` (only allowed types, alphabetical).
    Test 7 — `bip_config_allows_matches_field`: `.allows(ScriptType::P2tr)` returns `self.allow_p2tr` for each of the 3 variants.
    Test 8 — `bip_config_env_var_override_bool_roundtrip`: Build via `config::Config::builder().add_source(config::Environment::with_prefix("BLINDJOIN").separator("__").try_parsing(true)).build()?.try_deserialize::<CoordinatorConfig>()?` with env `BLINDJOIN__COORDINATOR__BIP__ALLOW_P2TR=false` set (and BLINDJOIN__NETWORK__BITCOIN_NETWORK + the other required NetworkConfig fields set to dummy values via env so deserialization succeeds); assert `cfg.bip.allow_p2tr == false` AND `cfg.bip.allow_p2wpkh == true` (default). This test goes in `coordinator/src/config.rs::tests` and uses `std::env::set_var` / `remove_var` in a single `#[serial_test::serial]`-marked block — or, if serial_test is not in dev-deps, scoped tightly with a unique prefix per Pitfall 5.
    Test 9 — `bip_config_env_var_override_output_script_type_kebab_case`: With env `BLINDJOIN__COORDINATOR__BIP__OUTPUT_SCRIPT_TYPE=p2sh-p2wpkh` set (and `BLINDJOIN__COORDINATOR__BIP__ALLOW_P2SH_P2WPKH=true` to keep validate happy), assert `cfg.bip.output_script_type == ScriptType::P2shP2wpkh` (CD-13 wire-form lowercase kebab-case).
  </behavior>
  <action>
    Edit `coordinator/src/config.rs`:

    1. Add a `use shared::bip322::ScriptType;` import at the top (after the existing `use config::{Config, File, Environment};` and `use serde::Deserialize;` lines).

    2. Add 2 new module-level default fns immediately after the existing `default_coordinator_public_addr` at line 97 (mirrors the existing pattern at lines 54-72): `fn default_true() -> bool { true }` and `fn default_output_script_type() -> ScriptType { ScriptType::P2wpkh }`.

    3. Insert the new struct definition immediately after the `impl Default for DiscoveryConfig` block (around line 109), BEFORE `pub struct CoordinatorConfig`. Per D-38 verbatim:
       - `#[derive(Debug, Deserialize, Clone)]` on `pub struct BipConfig`.
       - 4 fields: `pub allow_p2wpkh: bool`, `pub allow_p2tr: bool`, `pub allow_p2sh_p2wpkh: bool` (each `#[serde(default = "default_true")]`), and `pub output_script_type: ScriptType` (`#[serde(default = "default_output_script_type")]`).
       - Each `allow_*` field carries a doc-comment naming the env-var override (`BLINDJOIN__COORDINATOR__BIP__ALLOW_*`) AND noting "Use 'true' or 'false' (lowercase strings) — '0' / '1' do not deserialize as bool" per RESEARCH §Pitfall 5.
       - `output_script_type` doc-comment names CD-13: env-var override accepts wire-form lowercase kebab-case ("p2wpkh" / "p2tr" / "p2sh-p2wpkh").

    4. Add `impl BipConfig` with 3 methods per D-38:
       - `pub fn allows(&self, st: ScriptType) -> bool` — match on ScriptType variant, return the matching allow_* field.
       - `pub fn supported(&self) -> Vec<ScriptType>` — alphabetical canonical order per CD-11. Inline the order as RESEARCH lines 272-278: push `P2shP2wpkh` first if `allow_p2sh_p2wpkh`, then `P2tr` if `allow_p2tr`, then `P2wpkh` if `allow_p2wpkh`. NOT sort-based — the inline order locks the byte-length math for 16-03's PKARR budget assertion.
       - `pub fn validate(&self) -> anyhow::Result<()>` — 2 `anyhow::ensure!` blocks verbatim from RESEARCH §Specifics lines 286-302 (rejects all-false; rejects output not in allowed set). Error messages MUST name the env-var override path per Phase 8 pattern.

    5. Add `impl Default for BipConfig` — returns the all-true default-output struct (so test code can call `BipConfig::default()` ergonomically; matches the existing `Default for DiscoveryConfig` pattern at lines 101-109).

    6. Add `pub bip: BipConfig` field to `pub struct CoordinatorConfig` (existing at lines 111-117), with `#[serde(default)]` for v1.3 coordinator.toml backwards compat (legacy config files have no `[bip]` section per D-35).

    7. Extend `CoordinatorConfig::validate()` body (existing at lines 157-185): immediately before the final `Ok(())` add `self.bip.validate()?;` (one line, per RESEARCH §"Example 1: Existing CoordinatorConfig::validate extension pattern" lines 620-625).

    8. Update `CoordinatorConfig::with_defaults()` (existing at lines 188-215) to include `bip: BipConfig::default(),` as the final field literal per Assumption A4. Without this, the existing coordinator unit-test fixtures that call `with_defaults()` will fail to compile after Phase 16.

    9. Add the `#[cfg(test)] mod tests { ... }` block at the end of the file with the 9 unit tests listed in `<behavior>`. Test 8 + Test 9 require env-var manipulation; either add `serial_test = "3"` to `coordinator/Cargo.toml` `[dev-dependencies]` OR isolate via a unique random prefix per test (preferred — keeps deps unchanged per RESEARCH §"Zero new dependencies" mandate). Recommendation: use a per-test fn `make_env_prefix() -> String` returning `format!("BJTEST_{}__", std::process::id())` and parse via `Environment::with_prefix(&prefix)` — no global env mutation needed. Per D-13 (env-var deserialize), this works because `Environment` reads `std::env` at parse time; constructing the prefix per test means tests are independent.

    Do NOT add new dependencies. Do NOT modify any other field of `CoordinatorConfig`. Do NOT modify `load()`. Do NOT modify the network / coordinator / discovery sections.

    CRIT-01 grep gate: this task does not touch `coordinator/src/bitcoin/utxo.rs`, so `grep -c "CRIT-01" coordinator/src/bitcoin/utxo.rs` stays at 0 (it becomes 2 in 16-02). No assertion in this plan.
  </action>
  <verify>
    <automated>cargo test -p coordinator --lib config::tests 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    - `grep -E '^pub struct BipConfig' coordinator/src/config.rs` returns exactly 1 line.
    - `grep -E 'pub bip: BipConfig' coordinator/src/config.rs` returns exactly 1 line (field on CoordinatorConfig with #[serde(default)]).
    - `grep -E 'fn (allows|supported|validate)\(' coordinator/src/config.rs | grep -c -E '^\s+pub fn'` returns ≥ 3 (one impl per method).
    - `grep -c "BLINDJOIN__COORDINATOR__BIP__" coordinator/src/config.rs` returns ≥ 3 (referenced in validate() error messages + doc comments).
    - `cargo test -p coordinator --lib config::tests` exits 0 with at least 9 tests passing including the 9 named in <behavior>.
    - `cargo build --workspace` exits 0 (with_defaults() still compiles).
    - `grep -n "bip:" coordinator/src/config.rs` shows the field initialized in `with_defaults()` (Assumption A4).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend shared::protocol::InfoResponse with supported_script_types + output_script_type</name>
  <files>shared/src/protocol.rs</files>
  <read_first>
    - shared/src/protocol.rs (full file — focus on lines 1-28 InfoResponse + lines 105-200 OwnershipProof for the #[serde(default)] pattern Phase 15 uses)
    - shared/src/bip322/mod.rs lines 140-160 (ScriptType public surface — already exported)
    - .planning/phases/16-coordinator-integration-advertisement/16-CONTEXT.md §D-42 verbatim (InfoResponse field shape + default_legacy_supported + default_legacy_output)
    - .planning/phases/16-coordinator-integration-advertisement/16-RESEARCH.md §"Pattern 4: InfoResponse JSON array form (D-42)" lines 416-454 + §Pitfall 3 (v1.3 client resolver tolerates unknown fields — verified at client/src/discover.rs:75-80)
    - .planning/phases/15-shared-crate-multi-script-contract/15-01-SUMMARY.md (precedent for serde-default v1.3 wire-compat pattern from Phase 15-01 OwnershipProof v2 envelope)
  </read_first>
  <behavior>
    Test 1 — `info_response_v1_3_wire_decodes_with_legacy_defaults`: `serde_json::from_str::<InfoResponse>(v1_3_wire)` where `v1_3_wire` is a complete InfoResponse JSON body OMITTING the two new fields succeeds, with `supported_script_types == vec![ScriptType::P2wpkh]` (default_legacy_supported fires) AND `output_script_type == ScriptType::P2wpkh` (default_legacy_output fires). Wire fixture for `v1_3_wire`: a literal JSON string containing only the 10 existing fields with realistic values (version="0.1.0", network="signet", denomination_sats=1000000, min_participants=3, max_participants=20, round_state="idle", participants_registered=0, rsa_pubkey_hash=null, rsa_pubkey_der_b64=null, round_id=null). This proves v1.4 coordinator code can deserialize a v1.3 wire response.
    Test 2 — `info_response_v1_4_roundtrip_preserves_new_fields`: Build `InfoResponse` with `supported_script_types = vec![ScriptType::P2shP2wpkh, ScriptType::P2tr, ScriptType::P2wpkh]` AND `output_script_type = ScriptType::P2tr`; serialize via `serde_json::to_string`; deserialize; assert both new fields roundtrip bit-exact.
    Test 3 — `info_response_v1_4_emits_kebab_case_on_wire`: Build `InfoResponse` with `supported_script_types = vec![ScriptType::P2shP2wpkh, ScriptType::P2tr, ScriptType::P2wpkh]` + `output_script_type = ScriptType::P2shP2wpkh`; serialize; assert the resulting JSON string contains the substring `"p2sh-p2wpkh"` (kebab-case rename verified for the new fields per Phase 15 D-Q3 RESOLVED).
    Test 4 — `info_response_v1_3_decoder_against_v1_4_wire_tolerates_extras`: `serde_json::from_str::<V1_3_InfoResponse>(v1_4_wire)` where `V1_3_InfoResponse` is a locally-declared shadow struct mirroring the v1.3 shape (the 10 existing fields without the 2 new ones) succeeds — i.e., deserialization silently drops the new fields because `InfoResponse` does NOT declare `#[serde(deny_unknown_fields)]` (file-top invariant). Per Pitfall 3. This test goes in `shared/src/protocol.rs::tests`.
  </behavior>
  <action>
    Edit `shared/src/protocol.rs`:

    1. Add `use crate::bip322::ScriptType;` at the top of the file (after the existing `use serde::{Deserialize, Serialize};` at line 1).

    2. Extend `pub struct InfoResponse` (existing at lines 13-28) with TWO new fields appended after the existing `round_id` field. Field order per D-42:
       - `#[serde(default = "default_legacy_supported")] pub supported_script_types: Vec<ScriptType>,`
       - `#[serde(default = "default_legacy_output")] pub output_script_type: ScriptType,`

    3. Add 2 module-level default fns immediately AFTER the InfoResponse struct (before `pub struct InputRegRequest`):
       - `fn default_legacy_supported() -> Vec<ScriptType> { vec![ScriptType::P2wpkh] }`
       - `fn default_legacy_output() -> ScriptType { ScriptType::P2wpkh }`

    4. Document the new fields with doc-comments naming the relevant decisions: D-42 (per-field shape) + the file-top invariant (no `deny_unknown_fields` so v1.3 clients silently drop unknowns) + the v1.3↔v1.4 bidirectional compat contract (a v1.3 wire response missing the fields decodes to the legacy P2WPKH defaults).

    5. Add the 4 unit tests listed in `<behavior>` to the existing `#[cfg(test)] mod tests { }` block at lines 203-273 (do NOT replace existing tests; append).

    Do NOT add `#[serde(deny_unknown_fields)]` — the file-top comment at lines 3-5 binds this prohibition. Do NOT modify the OwnershipProof / InputRegRequest / InputRegResponse / OutputRegRequest / OutputRegResponse / SignRequest / RoundTxResponse structs. Do NOT change any other field's serde attributes.

    Self-check after edits: `cargo test -p shared --lib protocol::tests` exits 0; the 5 Phase 15 existing tests + the 4 new tests = 9 total in `protocol::tests`.
  </action>
  <verify>
    <automated>cargo test -p shared --lib protocol::tests 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    - `grep -E 'pub supported_script_types: Vec&lt;ScriptType&gt;' shared/src/protocol.rs` returns exactly 1 line.
    - `grep -E 'pub output_script_type: ScriptType' shared/src/protocol.rs` returns exactly 1 line (excluding the OwnershipProof.script_type which is `Option&lt;ScriptType&gt;`).
    - `grep -c 'fn default_legacy_' shared/src/protocol.rs` returns exactly 2.
    - `cargo test -p shared --lib protocol::tests` exits 0 with the 4 new tests passing.
    - `cargo test --test integration full_round` exits 0 (cross-phase invariant) — additive struct field changes with serde defaults preserve v1.3 wire bit-exactness because the existing 10 fields serialize first in the existing order.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Populate new InfoResponse fields in get_info handler + integration assertion</name>
  <files>coordinator/src/api/handlers.rs, tests/integration/round_bootstrap.rs</files>
  <read_first>
    - coordinator/src/api/handlers.rs lines 1-68 (existing get_info + InfoResponse construction)
    - coordinator/src/config.rs (post-Task 1 state — confirm `state.config.bip.supported()` + `state.config.bip.output_script_type` are reachable through AppState)
    - shared/src/protocol.rs (post-Task 2 state — confirm InfoResponse has the two new fields)
    - tests/integration/round_bootstrap.rs (existing integration test that exercises GET /info — read the assertion pattern verbatim so the new assertion follows the same shape)
    - .planning/phases/16-coordinator-integration-advertisement/16-CONTEXT.md §D-42 + RESEARCH §"Pattern 4" lines 446-454 (the populate-from-config snippet)
  </read_first>
  <behavior>
    Test 1 — `get_info_returns_supported_from_config_default` (integration test): Bring up the coordinator with `CoordinatorConfig::with_defaults()` (so all 3 script types are allowed via `BipConfig::default()`); GET `/info`; deserialize the response body to `InfoResponse`; assert `supported_script_types == vec![ScriptType::P2shP2wpkh, ScriptType::P2tr, ScriptType::P2wpkh]` (alphabetical CD-11) AND `output_script_type == ScriptType::P2wpkh` (default).
    Test 2 — `get_info_returns_supported_filtered_by_allowlist` (integration test): Bring up the coordinator with a custom config where `allow_p2tr = false`; GET `/info`; assert `supported_script_types == vec![ScriptType::P2shP2wpkh, ScriptType::P2wpkh]` (P2tr filtered out, alphabetical canonical preserved).
  </behavior>
  <action>
    Edit `coordinator/src/api/handlers.rs::get_info` (existing at lines 46-68):

    1. Inside the `Json(InfoResponse { ... })` construction, AFTER the existing `round_id: Some(guard.round_id),` line, add TWO new field initializers:
       - `supported_script_types: state.config.bip.supported(),`
       - `output_script_type: state.config.bip.output_script_type,`

    2. Per CD-14, the `state.config.bip` access path is enabled by Task 1's `pub bip: BipConfig` field on `CoordinatorConfig`. `AppState.config` is `Arc&lt;CoordinatorConfig&gt;` (verify by grepping `coordinator/src/api/mod.rs` if uncertain — Phase 8 wired it as Arc); the `state.config.bip.supported()` call returns `Vec&lt;ScriptType&gt;` and copies cheaply.

    3. Locate the existing integration tests directory and find the file that asserts on GET `/info` for the bootstrap path (likely `tests/integration/round_bootstrap.rs`). Append TWO new `#[tokio::test]` fns matching the behavior list above:
       - `get_info_supports_all_three_script_types_with_defaults` — uses `with_defaults()` + asserts the all-3 alphabetical case.
       - `get_info_filters_supported_by_allowlist` — constructs a `CoordinatorConfig::with_defaults()` then mutates `cfg.bip.allow_p2tr = false` before passing into the test's coordinator bring-up, asserts the filtered result.

    4. If round_bootstrap.rs has a private helper for bringing up a test coordinator that does NOT accept a custom `CoordinatorConfig`, the second test must use a local pattern matching it (likely via `tower::ServiceExt::oneshot` on the router built with a mutated AppState). Read the existing bootstrap test pattern verbatim — do NOT introduce a new harness; reuse what exists.

    Do NOT modify any other handler. Do NOT modify the InputRegRequest decode path at line 136. Do NOT modify the AppState struct. Do NOT modify the round bootstrap logic outside of adding the two new tests.

    Self-check after edits: the existing get_info test passes (no regression) AND the 2 new integration tests pass.
  </action>
  <verify>
    <automated>cargo test --test integration -- round_bootstrap 2>&amp;1 | tail -25</automated>
  </verify>
  <done>
    - `grep -E 'supported_script_types: state\.config\.bip\.supported\(\)' coordinator/src/api/handlers.rs` returns exactly 1 line.
    - `grep -E 'output_script_type: state\.config\.bip\.output_script_type' coordinator/src/api/handlers.rs` returns exactly 1 line.
    - `cargo test --test integration -- round_bootstrap` exits 0 with the 2 new test fns named in <behavior> passing.
    - `cargo test --test integration full_round` exits 0 (cross-phase invariant) — handler change only adds 2 fields to the JSON response; v1.3 clients ignore them.
    - `cargo build --workspace` exits 0.
    - `cargo clippy --workspace --all-targets -- -D warnings` exits 0 (no new clippy lints).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TOML config file → coordinator process | Operator-controlled; validate at startup before any subsystem reads (Phase 8 hardening pattern) |
| Env-var overlay (BLINDJOIN__*) → coordinator process | Operator-controlled at process spawn; same trust as config file |
| GET /info response → client | Coordinator-controlled; clients are untrusted but the response is informational only (no client-supplied data echoed back) |
| v1.3 client decoding v1.4 InfoResponse | Wire-format evolution boundary — relies on `#[serde(deny_unknown_fields)]` ABSENCE (file-top invariant at shared/src/protocol.rs:3-5) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | `BipConfig` env-var override | mitigate | `try_parsing(true)` deserializes via serde's bool path; invalid values produce a startup error (config::ConfigError surfaces through CoordinatorConfig::load); `validate()` rejects the all-false combination at startup before any handler runs (D-36, Phase 8 fail-fast pattern). |
| T-16-02 | Denial of Service | Misconfigured all-false BipConfig (silent silent acceptance regression) | mitigate | `validate()` fail-fast at boot per D-36; coordinator does not start if validate() returns Err. Error message names env-var override path so operator can self-recover. |
| T-16-03 | Information Disclosure | `/round/info` JSON payload | accept | `supported_script_types` + `output_script_type` are PUBLIC advertisement (the whole point of ADVERT-01); D-09 LOCKED — coordinator advertises supported SET only, NEVER per-round per-script-type registration counts (privacy anti-feature per CONTEXT). No PII in either field. |
| T-16-04 | Information Disclosure | InfoResponse log lines | mitigate | get_info handler does NOT log the response body (verified by code-read — existing handler emits zero tracing! calls). Phase 16 adds zero new log lines in this plan. |
| T-16-MOD-01 | Tampering | Wire-format evolution (v1.3 ↔ v1.4 InfoResponse compat) | mitigate | `#[serde(default = "default_legacy_supported")]` + `#[serde(default = "default_legacy_output")]` on new fields preserve v1.3 wire decode path; file-top invariant at shared/src/protocol.rs:3-5 binds the no-deny_unknown_fields rule so v1.3 clients silently drop unknown fields. Verified in Test 4 of Task 2. |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | Phase 16 adds ZERO new dependencies (RESEARCH §"Installation" lines 116-119 verified against Cargo.lock); no package legitimacy gate needed. `cargo audit` CI gate remains in force. |

**ASVS L1 baseline note:** This plan touches only static type definitions and serde derives. No new input-validation surface (the validate() path at startup is operator-controlled, not request-driven). No new code execution path from untrusted input. The mitigation effort is concentrated in (a) the validate() fail-fast and (b) the wire-format evolution invariant — both of which are unit-tested in this plan.
</threat_model>

<verification>
- `cargo test -p coordinator --lib config::tests` — 9 new tests pass (BipConfig serde defaults, validate() rejects all-false, validate() rejects output mismatch, allows/supported semantics, env-var override roundtrips).
- `cargo test -p shared --lib protocol::tests` — 4 new InfoResponse tests pass (v1.3 legacy decode → defaults, v1.4 roundtrip preserves new fields, kebab-case wire form, v1.3 decoder against v1.4 wire tolerates extras).
- `cargo test --test integration round_bootstrap` — 2 new integration tests pass (supported_script_types populated from config default + filtered by allowlist).
- `cargo test --test integration full_round` — 8 existing tests still pass (cross-phase invariant — Task 2/3 changes are additive with serde defaults; v1.3 wire bit-exactness preserved).
- `cargo build --workspace` — exits 0; with_defaults() compiles with the new `bip:` field literal.
- `cargo clippy --workspace --all-targets -- -D warnings` — no new lints.
- `cargo audit` — clean (no new deps).
- `grep -c "CRIT-01" coordinator/src/bitcoin/utxo.rs` — returns 0 (this plan does not touch utxo.rs; the count becomes 2 in 16-02).
</verification>

<success_criteria>
- BipConfig struct + 3 methods (allows / supported / validate) + 2 default_* helpers + pub bip field on CoordinatorConfig + CoordinatorConfig::validate() extension + with_defaults() update all live in coordinator/src/config.rs.
- shared::protocol::InfoResponse has 2 new fields with serde-default v1.3 compat; 4 unit tests verify both directions of the wire-format evolution boundary.
- coordinator/src/api/handlers.rs::get_info populates the 2 new InfoResponse fields from state.config.bip; 2 integration tests verify the wire output matches the operator's allowlist config.
- Phase 16 cross-phase invariant: `cargo test --test integration full_round` exits 0 (no v1.3 wire shape changes; only additive serde-default fields).
- Zero new direct dependencies in any Cargo.toml.
- D-35, D-36, D-37, D-38, D-42, CD-11, CD-13 all observably implemented (per the assertions in `<verify>` and `<done>`).
- One atomic commit per CD-10 + REPAIR-01 lesson #1: "feat(16-01): BipConfig + InfoResponse wire-format extension".
</success_criteria>

<output>
Create `.planning/phases/16-coordinator-integration-advertisement/16-01-SUMMARY.md` when done, including:
- Files modified with line ranges
- BipConfig struct definition verbatim (final form after Task 1)
- The two new InfoResponse fields verbatim (final form after Task 2)
- Test counts: coordinator config::tests +9, shared protocol::tests +4, integration round_bootstrap +2
- Confirmation that `cargo test --test integration full_round` exits 0 (cross-phase invariant gate)
- Any deviations from the planned shape (e.g., if serial_test was needed for env-var tests, document the rationale)
- The atomic commit hash
</output>
