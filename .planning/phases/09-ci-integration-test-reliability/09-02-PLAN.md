---
phase: 09-ci-integration-test-reliability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/mod.rs
autonomous: true
requirements:
  - TEST-02
  - TEST-03
  - TEST-04
user_setup: []

must_haves:
  truths:
    - "Any integration test that needs bitcoind can call ONE locally-scoped invocation (`require_bitcoind!()` macro) that panics when `BLINDJOIN_REQUIRE_BITCOIND=1` and skips by returning otherwise (per D-07, D-08)"
    - "Any integration test that wants a regtest bitcoind can call ONE async helper (`bootstrap_regtest_bitcoind()`) that returns `(BitcoindGuard, RpcCreds)` with the daemon already mining 101 blocks (per D-13, D-14)"
    - "The `BitcoindGuard` returned by the bootstrap calls `node.stop()` on drop, and corepc-node's own `Node::Drop` runs `process.kill()` as belt-and-suspenders — so panic-unwinding in the test ALWAYS terminates the child bitcoind (per D-11, D-12)"
    - "bitcoind's child stdio is routed to /dev/null (via `Conf::view_stdout = false`) so the daemon never inherits cargo's stdout pipe — the pipe-hang root cause (per amended D-15)"
  artifacts:
    - path: "tests/integration/mod.rs"
      provides: "Shared test fixtures: require_bitcoind_inner() fn, require_bitcoind! macro, BitcoindGuard struct + Drop impl, RpcCreds struct, bootstrap_regtest_bitcoind() async fn"
      min_lines: 80
      contains: "pub struct BitcoindGuard"
  key_links:
    - from: "tests/integration/mod.rs require_bitcoind! macro"
      to: "BLINDJOIN_REQUIRE_BITCOIND env var (set by 09-01)"
      via: "std::env::var(\"BLINDJOIN_REQUIRE_BITCOIND\")"
      pattern: "BLINDJOIN_REQUIRE_BITCOIND"
    - from: "tests/integration/mod.rs BitcoindGuard"
      to: "corepc_node::Node::stop()"
      via: "Drop::drop calls self.node.take().map(|mut n| n.stop().ok())"
      pattern: "fn drop"
    - from: "tests/integration/mod.rs bootstrap_regtest_bitcoind()"
      to: "tests/integration/{full_round,rate_limiting,round_bootstrap}.rs (consumers in 09-03 + 09-04)"
      via: "let (guard, creds) = bootstrap_regtest_bitcoind().await;"
      pattern: "bootstrap_regtest_bitcoind"
---

<objective>
Add the four shared fixtures that every bitcoind-dependent integration test will consume to `tests/integration/mod.rs`:
  1. `require_bitcoind_inner() -> Option<String>` — env-var-gated bitcoind discovery (panics on miss when `BLINDJOIN_REQUIRE_BITCOIND=1`, else returns None for skip)
  2. `require_bitcoind!()` macro — wraps the inner fn so `return` exits the calling test, not the whole binary
  3. `BitcoindGuard { node: Option<Node> }` struct with `impl Drop` calling `node.stop()` — RAII replacement for `Box::leak`
  4. `bootstrap_regtest_bitcoind() -> (BitcoindGuard, RpcCreds)` async helper — single locus of `Node::with_conf` + cookie extraction + 101-block mining, with `Conf::view_stdout=false` and `-printtoconsole=0`

Purpose: This is the helper substrate every callsite migration (09-03, 09-04) consumes. Defining it here as Wave-1 work lets Plans 09-03 and 09-04 run in parallel against a stable contract. The macro form (vs a plain function) is load-bearing — `let exe = require_bitcoind();` cannot itself `return` from the calling `#[tokio::test]`, so the helper MUST be a macro (RESEARCH.md Pattern 2 + Assumption A5).

Output: `tests/integration/mod.rs` expanded from its current 4-line `mod ban_list_persistence; mod full_round; ...` declaration to ~80-120 lines containing the four fixtures above, with the existing `mod X;` declarations preserved at the top. No callsites are migrated in this plan — that's 09-03 and 09-04. This plan only creates the contracts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ci-integration-test-reliability/09-CONTEXT.md
@.planning/phases/09-ci-integration-test-reliability/09-RESEARCH.md
@.planning/phases/09-ci-integration-test-reliability/09-PATTERNS.md
@tests/integration/mod.rs
@tests/integration/round_bootstrap.rs
@coordinator/src/network/tor.rs

<interfaces>
<!-- These are the exact API contracts later plans (09-03, 09-04) will consume.
     They MUST be created exactly as specified — downstream migration plans
     depend on these signatures verbatim. -->

Public surface from tests/integration/mod.rs after this plan:

```
// Re-exported / module-public items the four integration test files use:

pub fn require_bitcoind_inner() -> Option<String>;
//   Some(path) if corepc_node::exe_path() succeeds.
//   panic!() if env BLINDJOIN_REQUIRE_BITCOIND == "1" and exe_path() errors.
//   None if env BLINDJOIN_REQUIRE_BITCOIND is unset/other and exe_path() errors.

#[macro_export]
macro_rules! require_bitcoind { () => { ... } }
//   Expands to:
//     match $crate::require_bitcoind_inner() {
//         Some(p) => p,
//         None => return,
//     }
//   Callers use:  let exe = require_bitcoind!();
//   (Note: `exe` is unused at the callsite in the new pattern — bootstrap
//   handles exe internally — but the macro must still bind a value so
//   callers can choose to use it. See acceptance criterion below.)

pub struct RpcCreds {
    pub url: String,
    pub user: String,
    pub pass: String,
}

pub struct BitcoindGuard {
    node: Option<corepc_node::Node>,  // private field; access via .node() borrow
}

impl BitcoindGuard {
    pub fn new(node: corepc_node::Node) -> Self;
    pub fn node(&self) -> &corepc_node::Node;
    //   Borrow the inner Node for RPC calls (mining additional blocks,
    //   fetching new addresses, etc.). Panics if guard already taken
    //   (which only happens after drop, so a live guard always returns).
}

impl Drop for BitcoindGuard {
    fn drop(&mut self) { ... }
    //   1. self.node.take() to consume the Option
    //   2. let _ = n.stop();   // RPC-call bitcoind's `stop` then wait for child
    //   3. n falls out of scope; corepc-node's Node::Drop runs process.kill() as belt-and-suspenders
}

pub async fn bootstrap_regtest_bitcoind() -> (BitcoindGuard, RpcCreds);
//   - Calls require_bitcoind!() internally for exe discovery
//   - spawn_blocking + Node::with_conf with conf.network="regtest",
//     conf.view_stdout=false (Stdio::null), conf.args including "-printtoconsole=0" + "-fallbackfee=0.0001"
//   - Extracts cookie credentials, mines 101 blocks
//   - Returns (BitcoindGuard, RpcCreds) where the guard MUST be held in scope
//     for the test's full duration; dropping it earlier kills bitcoind
```

EXISTING file contents (the four lines that must be preserved at the top after this plan):
```
mod ban_list_persistence;
mod full_round;
mod rate_limiting;
mod round_bootstrap;
```

EXISTING analogs (READ these before writing — patterns to mirror):
- coordinator/src/network/tor.rs:44-66 — ConnectionGuard RAII (rationale-heavy doc comment, named struct around an owned resource, named field, inherent impl with `new`). BitcoindGuard mirrors this BUT diverges by needing an explicit `impl Drop` (ConnectionGuard relies on implicit Drop of OwnedSemaphorePermit; BitcoindGuard must call node.stop() RPC before letting Node::Drop run process.kill).
- coordinator/src/run.rs:296-298 — the canonical `std::env::var("BLINDJOIN_*").map(|v| v == "1").unwrap_or(false)` idiom. Mirror this in require_bitcoind_inner.
- tests/integration/rate_limiting.rs:92-128 — the current bootstrap_regtest_bitcoind that ends in `Box::leak(node_box)`. Lift the body (`Node::with_conf` + cookie extract + mine 101) but REPLACE the leak with `BitcoindGuard::new(node)` and ADD `conf.view_stdout = false` + `conf.args = vec!["-printtoconsole=0", "-fallbackfee=0.0001"]` before the spawn.
- tests/integration/round_bootstrap.rs:59-89 — older, better-commented sibling of the rate_limiting bootstrap. Mirror its doc-comment style.

CARGO test layout: `coordinator/Cargo.toml` declares the integration test binary at line 73-75:
```
[[test]]
name = "integration"
path = "../tests/integration/mod.rs"
```
This means `tests/integration/mod.rs` is the crate root of the integration test binary. `#[macro_export]` macros defined here are available as `crate::require_bitcoind!()` inside the binary AND as `$crate::require_bitcoind!()` inside themselves. Each `mod X;` submodule (full_round, rate_limiting, ...) accesses items via `crate::require_bitcoind_inner` / `crate::require_bitcoind!`. Document this in a comment in mod.rs so future contributors don't relitigate.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add require_bitcoind_inner() fn + require_bitcoind! macro to tests/integration/mod.rs</name>
  <files>tests/integration/mod.rs</files>
  <read_first>
    - tests/integration/mod.rs (current 4-line file)
    - .planning/phases/09-ci-integration-test-reliability/09-CONTEXT.md decisions D-07 + D-08
    - .planning/phases/09-ci-integration-test-reliability/09-RESEARCH.md "Pattern 2: env-var-gated skip helper" (full text including the macro footgun discussion of std::process::exit) and "Code Examples → require_bitcoind!() macro"
    - .planning/phases/09-ci-integration-test-reliability/09-PATTERNS.md "BLINDJOIN_* Env-Var Gate" section
    - coordinator/src/run.rs lines 296-298 (the canonical project env-var idiom)
    - coordinator/Cargo.toml lines 73-75 (the [[test]] declaration that makes mod.rs the integration test binary's crate root)
  </read_first>
  <behavior>
    - `require_bitcoind_inner()` returns `Some(path)` when corepc_node::exe_path() returns Ok(path)
    - `require_bitcoind_inner()` returns `None` when corepc_node::exe_path() errors AND env BLINDJOIN_REQUIRE_BITCOIND is unset OR != "1"
    - `require_bitcoind_inner()` panics with a message naming both BLINDJOIN_REQUIRE_BITCOIND and BITCOIND_EXE when corepc_node::exe_path() errors AND env BLINDJOIN_REQUIRE_BITCOIND == "1"
    - The `require_bitcoind!()` macro, when expanded inside a `fn foo()` whose body is `let _ = require_bitcoind!();`, causes `foo()` to return early via `return` if the inner fn returns None — proven by the macro expanding to `match ... { Some(p) => p, None => return, }`
    - The macro is exported at crate root (`#[macro_export]`) so submodules access it as `crate::require_bitcoind!()`
  </behavior>
  <action>Modify `tests/integration/mod.rs` to:

(a) Preserve the four existing lines at the top exactly as they are (`mod ban_list_persistence;` through `mod round_bootstrap;`).

(b) Below the `mod X;` declarations, add a 3-5 line file-level doc comment block (`//!`) explaining the file's dual role: (i) declares the integration submodules; (ii) hosts shared fixtures (require_bitcoind macro, BitcoindGuard, bootstrap_regtest_bitcoind). Mention that this is the crate root of the integration test binary per `[[test]] name = "integration"` in coordinator/Cargo.toml:73, which is why `#[macro_export]` macros here are reachable as `crate::require_bitcoind!()` from submodules.

(c) Add a `pub fn require_bitcoind_inner() -> Option<String>` function. Behavior:
  - Call `corepc_node::exe_path()`.
  - On Ok(path), return `Some(path)`.
  - On Err(e), check `std::env::var("BLINDJOIN_REQUIRE_BITCOIND").as_deref() == Ok("1")` (the canonical `BLINDJOIN_*` idiom from coordinator/src/run.rs:296-298 — boolean-by-presence with the literal string `"1"`).
    - If true: `panic!("bitcoind required but not found ({e}). BLINDJOIN_REQUIRE_BITCOIND=1 is set — this is CI mode. Check that BITCOIND_EXE points to a valid binary.")`. The panic message MUST name both env vars by name so the failing CI log lets a developer triage without re-reading source.
    - If false: `eprintln!("bitcoind not found ({e}), skipping (local-dev mode; set BLINDJOIN_REQUIRE_BITCOIND=1 to fail instead)"); None`.
  - Add a doc comment (`///`) explaining: this is the inner accessor; tests should use the `require_bitcoind!()` macro instead so the `return` exits the calling test, not the binary; reference RESEARCH.md Pattern 2 footgun about std::process::exit aborting the whole binary.

(d) Add a `#[macro_export]` macro `require_bitcoind` that expands exactly to:
```
match $crate::require_bitcoind_inner() { Some(p) => p, None => return, }
```
Each callsite invokes via `let exe = require_bitcoind!();` — the unused-variable warning for `exe` is acceptable (callers in Plan 09-03 may not use the path directly since bootstrap_regtest_bitcoind also calls require_bitcoind! internally). Document this trade-off in the macro's doc comment.

No callsite migration in this task — those are Plans 09-03 / 09-04. Only the contract is created here.</action>
  <verify>
    <automated>cargo test --test integration --no-run 2>&1 | tail -5 && grep -c 'pub fn require_bitcoind_inner' tests/integration/mod.rs | grep -q '^1$' && grep -c '#\[macro_export\]' tests/integration/mod.rs | grep -q '^1$' && grep -c 'macro_rules! require_bitcoind' tests/integration/mod.rs | grep -q '^1$' && grep -c 'BLINDJOIN_REQUIRE_BITCOIND' tests/integration/mod.rs | grep -q '^1$'</automated>
  </verify>
  <done>tests/integration/mod.rs declares the original 4 submodules AND defines `pub fn require_bitcoind_inner() -> Option<String>` and `#[macro_export] macro_rules! require_bitcoind` with the body documented above. The integration test binary still compiles (cargo test --no-run exits 0). No existing test file in tests/integration/ has been edited yet.</done>
  <acceptance_criteria>
    - Source (4 existing module declarations preserved): `grep -c '^mod ban_list_persistence;' tests/integration/mod.rs` returns 1 AND `grep -c '^mod full_round;' tests/integration/mod.rs` returns 1 AND `grep -c '^mod rate_limiting;' tests/integration/mod.rs` returns 1 AND `grep -c '^mod round_bootstrap;' tests/integration/mod.rs` returns 1
    - Source (inner fn present): `grep -c 'pub fn require_bitcoind_inner() -> Option<String>' tests/integration/mod.rs` returns 1
    - Source (macro is exported, not just defined): `grep -c '#\[macro_export\]' tests/integration/mod.rs` returns at least 1
    - Source (macro defined): `grep -c 'macro_rules! require_bitcoind' tests/integration/mod.rs` returns 1
    - Source (macro body returns from caller — load-bearing): `grep -A4 'macro_rules! require_bitcoind' tests/integration/mod.rs | grep -q 'None => return'` exits 0
    - Source (env var is the gate): `grep -c 'BLINDJOIN_REQUIRE_BITCOIND' tests/integration/mod.rs` returns at least 1
    - Source (panic message names both env vars for triage): `grep -c 'BITCOIND_EXE' tests/integration/mod.rs` returns at least 1
    - Source (matches project canonical idiom `.as_deref() == Ok("1")` OR equivalent `.map(|v| v == "1").unwrap_or(false)`): `grep -E 'as_deref\(\) == Ok\("1"\)|map\(\|v\| v == "1"\)\.unwrap_or\(false\)' tests/integration/mod.rs` returns at least 1 line
    - Behavior (binary still compiles): `cargo test --test integration --no-run 2>&1` exits 0
    - Source (no callsite migration in this task — those are 09-03/04): `grep -c 'require_bitcoind!' tests/integration/full_round.rs` returns 0 AND `grep -c 'require_bitcoind!' tests/integration/rate_limiting.rs` returns 0 AND `grep -c 'require_bitcoind!' tests/integration/round_bootstrap.rs` returns 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add BitcoindGuard struct + RpcCreds + bootstrap_regtest_bitcoind helper</name>
  <files>tests/integration/mod.rs</files>
  <read_first>
    - tests/integration/mod.rs (current state after Task 1)
    - tests/integration/round_bootstrap.rs (lines 59-89 — the well-commented bootstrap to mirror; lines 96-128 of rate_limiting.rs are the alternative form)
    - tests/integration/rate_limiting.rs lines 92-128 (the existing `bootstrap_regtest_bitcoind(exe: String) -> (String, String, String)` to lift)
    - coordinator/src/network/tor.rs lines 44-66 (ConnectionGuard RAII analog — rationale-heavy doc comment, named struct, inherent impl)
    - .planning/phases/09-ci-integration-test-reliability/09-CONTEXT.md decisions D-11, D-12, D-13, D-14, D-15 (amended) + "Research-driven amendments" section
    - .planning/phases/09-ci-integration-test-reliability/09-RESEARCH.md "Pattern 1: RAII guard returned from spawn_blocking" + "Pitfall 3" (view_stdout: false rationale) + "Pitfall 4" (Send semantics) + sources line 733 (corepc-node 0.12 source: Node::stop() at line 549, Node::Drop runs process.kill() at line 580)
    - .planning/phases/09-ci-integration-test-reliability/09-PATTERNS.md "tests/integration/mod.rs (test fixture module — new shared helpers)" + "Shared Patterns → RAII Drop Guard for OS Resources"
  </read_first>
  <behavior>
    - `RpcCreds { url, user, pass }` is a plain `pub struct` with three `pub: String` fields and `#[derive(Clone, Debug)]`
    - `BitcoindGuard { node: Option<corepc_node::Node> }` has a private `node` field accessed via two public methods: `new(node: Node) -> Self` and `node(&self) -> &Node` (panics if guard already taken, which a live guard cannot do)
    - `impl Drop for BitcoindGuard::drop(&mut self)`: takes the Option, calls `let _ = n.stop()`, then lets `n` fall out of scope so corepc-node's own `Node::Drop` runs `process.kill()` as belt-and-suspenders. NO panicking inside drop.
    - `bootstrap_regtest_bitcoind() -> (BitcoindGuard, RpcCreds)` async function:
      - First line: `let exe = require_bitcoind!();` (uses the macro from Task 1)
      - `tokio::task::spawn_blocking` closure that constructs `Conf::default()`, sets `conf.network = "regtest"`, sets `conf.view_stdout = false` (per amended D-15 — routes Stdio to null), pushes `"-printtoconsole=0"` AND `"-fallbackfee=0.0001"` into `conf.args` (matches the args field of corepc-node 0.12 Conf — `args: Vec<&'a str>`)
      - Calls `Node::with_conf(&exe, &conf)`, extracts cookie via `node.params.get_cookie_values()`, builds `RpcCreds { url, user, pass }`, mines 101 blocks via `node.client.generate_to_address(101, &mine_addr)`, returns `(BitcoindGuard::new(node), creds)`
      - The `.await.expect("regtest bootstrap spawn_blocking panicked")` follows the repo-canonical pattern (verified across 8 callsites per PATTERNS.md)
  </behavior>
  <action>Append to `tests/integration/mod.rs` (below the require_bitcoind_inner fn + macro from Task 1):

(a) `RpcCreds` struct. Public. `#[derive(Clone, Debug)]`. Three `pub fields: url: String, user: String, pass: String`. Doc comment naming it the canonical bitcoind credentials handoff between the bootstrap helper and consuming tests. Note in the doc comment: `user` and `pass` come from the bitcoind cookie file (Node::params::get_cookie_values), not from any configured credentials.

(b) `BitcoindGuard` struct. Public. Field `node: Option<corepc_node::Node>` — private. Add a multi-line `///` doc comment in the style of `coordinator/src/network/tor.rs:44-66`'s ConnectionGuard doc comment:
  - First paragraph: states the invariant (the test must hold this guard for the test's full duration; dropping it kills bitcoind).
  - Second paragraph: explains the `Option<Node>` shape (we need .take() in drop to consume the Node so n.stop() can run before Node::Drop's automatic process.kill()).
  - Third paragraph: documents the divergence from ConnectionGuard — `ConnectionGuard` relies on implicit Drop of the inherent OwnedSemaphorePermit; `BitcoindGuard` requires an explicit `impl Drop` because `node.stop()` is an RPC call we must make before relying on Node::Drop's process.kill() fallback.
  Two inherent methods:
  - `pub fn new(node: corepc_node::Node) -> Self { Self { node: Some(node) } }`
  - `pub fn node(&self) -> &corepc_node::Node` — returns `self.node.as_ref().expect("BitcoindGuard already taken; this can only happen after drop")`. A live guard always succeeds. (Consumers in 09-03 will use this to mine additional blocks or derive addresses without taking ownership.)
  
  `impl Drop for BitcoindGuard`:
  - `fn drop(&mut self) { if let Some(mut n) = self.node.take() { let _ = n.stop(); /* n drops here; corepc-node's Node::Drop runs process.kill() as belt-and-suspenders (verified corepc-node-0.12.0/node/src/lib.rs:580) */ } }`
  - Do NOT panic inside drop. `let _ = n.stop()` swallows the Err; the fallback is Node::Drop's process.kill().

(c) `pub async fn bootstrap_regtest_bitcoind() -> (BitcoindGuard, RpcCreds)`. Doc comment naming it the single locus for daemon bring-up per D-13 + D-14; mention the consumers (full_round.rs, rate_limiting.rs, round_bootstrap.rs) and that callers must hold the returned guard for the test's full duration. Body:
  - `let exe = require_bitcoind!();` (first line; uses the macro from Task 1 — if BLINDJOIN_REQUIRE_BITCOIND is unset and bitcoind is missing, this returns from the calling test cleanly)
  - `tokio::task::spawn_blocking(move || { ... }).await.expect("regtest bootstrap spawn_blocking panicked")` — the panic-message format mirrors the existing repo idiom (verified at round_bootstrap.rs:89, rate_limiting.rs:128).
  - Inside the closure: import `bitcoin::Address` and `corepc_node::{Conf, Node}`. Build `let mut conf = Conf::default(); conf.network = "regtest"; conf.view_stdout = false; conf.args.push("-printtoconsole=0"); conf.args.push("-fallbackfee=0.0001");`. (Verify the exact `conf.args` field type from corepc-node 0.12 — RESEARCH.md verified `args: Vec<&'a str>`. The lifetime parameter means string literals work directly; no `.into()` needed.)
  - Call `Node::with_conf(&exe, &conf).expect("start regtest bitcoind")`.
  - Extract cookie: `let cookie = node.params.get_cookie_values().expect("read cookie file").expect("parse cookie values");`. Build `let rpc_url = node.rpc_url(); let creds = RpcCreds { url: rpc_url, user: cookie.user.clone(), pass: cookie.password.clone() };`.
  - Mine 101 blocks (so block_count > 0 for startup_health_check): `let mine_addr: Address = node.client.new_address().expect("get new address"); node.client.generate_to_address(101, &mine_addr).expect("generate 101 blocks");`.
  - Return `(BitcoindGuard::new(node), creds)`.

Add a comment above the spawn_blocking explaining: corepc-node's `Node::with_conf` is synchronous; tokio's `spawn_blocking` is the standard bridge. The returned `Node` is `Send` (verified RESEARCH.md Pitfall 4 + Assumption A1), so the BitcoindGuard wrapping it crosses .await cleanly.

The 4 existing `mod X;` declarations at the top of the file MUST remain at the top of the file unchanged.

No callsite migration in this task. Plan 09-03 and 09-04 do that.</action>
  <verify>
    <automated>cargo test --test integration --no-run 2>&1 | tail -10 && grep -c 'pub struct BitcoindGuard' tests/integration/mod.rs | grep -q '^1$' && grep -c 'pub struct RpcCreds' tests/integration/mod.rs | grep -q '^1$' && grep -c 'pub async fn bootstrap_regtest_bitcoind' tests/integration/mod.rs | grep -q '^1$' && grep -c 'impl Drop for BitcoindGuard' tests/integration/mod.rs | grep -q '^1$' && grep -c 'view_stdout = false' tests/integration/mod.rs | grep -q '^1$' && grep -c -- '-printtoconsole=0' tests/integration/mod.rs | grep -q '^1$' && grep -c 'Box::leak' tests/integration/mod.rs | grep -q '^0$'</automated>
  </verify>
  <done>tests/integration/mod.rs contains: original 4 mod declarations; require_bitcoind_inner fn + require_bitcoind! macro (from Task 1); RpcCreds struct; BitcoindGuard struct + impl Drop; bootstrap_regtest_bitcoind async fn. The file compiles as part of `cargo test --test integration --no-run`. No callsite in tests/integration/{full_round,rate_limiting,round_bootstrap}.rs has been edited yet. No `Box::leak` appears in tests/integration/mod.rs.</done>
  <acceptance_criteria>
    - Source (BitcoindGuard struct present): `grep -c 'pub struct BitcoindGuard' tests/integration/mod.rs` returns 1
    - Source (BitcoindGuard has Option<Node> field for take-in-drop pattern): `grep -c 'node: Option<corepc_node::Node>' tests/integration/mod.rs` returns 1
    - Source (Drop impl present): `grep -c 'impl Drop for BitcoindGuard' tests/integration/mod.rs` returns 1
    - Source (Drop calls node.stop()): `grep -A6 'impl Drop for BitcoindGuard' tests/integration/mod.rs | grep -q 'n.stop()'` exits 0
    - Source (RpcCreds struct present with 3 fields): `grep -c 'pub struct RpcCreds' tests/integration/mod.rs` returns 1 AND `grep -A5 'pub struct RpcCreds' tests/integration/mod.rs | grep -c 'pub' | awk '$1 >= 3 { exit 0 } { exit 1 }'` exits 0
    - Source (bootstrap helper present with correct signature): `grep -c 'pub async fn bootstrap_regtest_bitcoind() -> (BitcoindGuard, RpcCreds)' tests/integration/mod.rs` returns 1
    - Source (view_stdout=false amendment honored): `grep -c 'view_stdout = false' tests/integration/mod.rs` returns 1
    - Source (printtoconsole arg also passed — defense in depth per D-15): `grep -c -- '-printtoconsole=0' tests/integration/mod.rs` returns 1
    - Source (bootstrap calls the macro internally so consumers don't double-skip): `grep -c 'require_bitcoind!' tests/integration/mod.rs` returns at least 1
    - Source (NO Box::leak anywhere in mod.rs — the whole point of this plan): `grep -c 'Box::leak' tests/integration/mod.rs` returns 0
    - Source (4 original module declarations preserved at top): `head -5 tests/integration/mod.rs | grep -c '^mod ' ` returns 4
    - Behavior (binary compiles): `cargo test --test integration --no-run 2>&1` exits 0
    - Behavior (no new clippy warnings introduced — the workflow runs `cargo clippy --workspace --all-targets -- -D warnings`): `cargo clippy --workspace --all-targets -- -D warnings 2>&1` exits 0
    - Source (other test files untouched by this task — those are 09-03/04): `git diff --name-only tests/integration/ | grep -v '^tests/integration/mod\.rs$' | wc -l` returns 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test binary → bitcoind child process | The test binary spawns bitcoind via corepc-node and communicates via cookie-authenticated RPC. Bitcoind runs with regtest network (disposable state, no real funds), no PII flows, OS-level isolation is sufficient |
| test binary → cargo's stdout pipe | The chain that was broken pre-Phase 9: bitcoind inherited cargo's stdout and held it open, hanging the pipe. The Phase 9 fix (view_stdout=false in this plan) terminates this inheritance |
| BitcoindGuard.drop → OS process tree | Drop calls n.stop() (RPC); fallback is Node::Drop's process.kill(). If both fail, OS reaps the orphan eventually but the test process has already moved on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Denial of Service | BitcoindGuard::drop's `n.stop()` blocking call | mitigate | `n.stop()` waits up to corepc-node's internal default for graceful exit; if it hangs, Node::Drop's process.kill() runs unconditionally (verified corepc-node-0.12.0/node/src/lib.rs:580). Net effect: drop always returns within a bounded window, possibly via SIGKILL, never blocks forever |
| T-09-06 | Tampering | Stdout-pipe inheritance from bitcoind to cargo | mitigate | `Conf::view_stdout = false` routes child stdio to `Stdio::null()` (verified corepc-node-0.12.0/node/src/lib.rs:319). Bitcoind's stdout/stderr never reach cargo's pipe, eliminating the documented TODO.md root-cause (~lines 47-55) of integration-suite hangs |
| T-09-07 | Spoofing | Tests skip silently in CI when bitcoind is unreachable, hiding regressions | mitigate | require_bitcoind_inner panics (not just skips) when BLINDJOIN_REQUIRE_BITCOIND=1; CI sets that env var in Plan 09-01. Panic message names both env vars so triage is local-only |
| T-09-08 | Information disclosure | Panic message in require_bitcoind_inner could leak local filesystem layout | accept | The error variant from corepc_node::exe_path() may include a path; this runs in CI on ephemeral runners and locally during developer iteration. No PII or production secrets are involved. Low risk |
</threat_model>

<verification>
- `cargo test --test integration --no-run` exits 0 (the contract compiles)
- `grep -c 'pub struct BitcoindGuard' tests/integration/mod.rs` returns 1
- `grep -c 'pub async fn bootstrap_regtest_bitcoind() -> (BitcoindGuard, RpcCreds)' tests/integration/mod.rs` returns 1
- `grep -c '#\[macro_export\]' tests/integration/mod.rs` returns at least 1
- `grep -c 'Box::leak' tests/integration/mod.rs` returns 0
- `cargo clippy --workspace --all-targets -- -D warnings` exits 0
- No tests/integration/{full_round,rate_limiting,round_bootstrap}.rs file has been modified by this plan (the migration happens in 09-03 and 09-04)
</verification>

<success_criteria>
- tests/integration/mod.rs contains:
  - Original 4 `mod X;` declarations at the top (preserved)
  - `pub fn require_bitcoind_inner() -> Option<String>`
  - `#[macro_export] macro_rules! require_bitcoind` expanding to `match $crate::require_bitcoind_inner() { Some(p) => p, None => return, }`
  - `pub struct RpcCreds { pub url, pub user, pub pass: String }` with `#[derive(Clone, Debug)]`
  - `pub struct BitcoindGuard { node: Option<corepc_node::Node> }` with rationale-heavy doc comment + inherent methods + explicit `impl Drop` calling `n.stop()`
  - `pub async fn bootstrap_regtest_bitcoind() -> (BitcoindGuard, RpcCreds)` that internally calls `require_bitcoind!()`, configures Conf with `view_stdout = false` and `-printtoconsole=0` + `-fallbackfee=0.0001` args, mines 101 blocks, returns the (guard, creds) tuple
- `cargo test --test integration --no-run` exits 0
- `cargo clippy --workspace --all-targets -- -D warnings` exits 0
- No `Box::leak` in tests/integration/mod.rs
- No callsite migration (full_round/rate_limiting/round_bootstrap untouched)
- TEST-02 (panic-on-miss helper available), TEST-03 (BitcoindGuard substrate for clean panic exit), TEST-04 (BitcoindGuard substrate for no-leaked-process) are now addressable — their full closure requires the callsite migrations in Plans 09-03 + 09-04
</success_criteria>

<output>
Create `.planning/phases/09-ci-integration-test-reliability/09-02-SUMMARY.md` when done. Summary must include: (a) the exact public signatures added; (b) confirmation `cargo test --no-run` and `cargo clippy -- -D warnings` both passed; (c) any divergence from the planned interface contracts (so downstream plans can adapt without re-discovering).
</output>

## PLAN COMPLETE
