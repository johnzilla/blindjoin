---
phase: 09-ci-integration-test-reliability
plan: 03
type: execute
wave: 2
depends_on:
  - 09-02
files_modified:
  - tests/integration/full_round.rs
autonomous: true
requirements:
  - TEST-03
  - TEST-04
user_setup: []

must_haves:
  truths:
    - "Every test function in tests/integration/full_round.rs that needs bitcoind uses the shared require_bitcoind macro from 09-02 — no inline match corepc_node::exe_path blocks remain"
    - "Every test function in tests/integration/full_round.rs that previously created bitcoind via inline Node::with_conf now uses the shared bootstrap_regtest_bitcoind() helper from 09-02, holding the returned BitcoindGuard for the test's full duration"
    - "No Box::leak call appears anywhere in tests/integration/full_round.rs — bitcoind lifetime is RAII-managed end-to-end"
    - "The 6 RPC-schema-drift-broken tests carry #[ignore = TODO(Phase-10) ...] markers so CI lists them as ignored without executing them (per D-10 amended)"
    - "The 2 currently-passing tests (adversarial_tampered_psbt_rejected, coordinator_info_endpoint_fields) remain non-ignored and continue to compile/pass"
  artifacts:
    - path: "tests/integration/full_round.rs"
      provides: "Migrated integration tests using shared mod.rs helpers; 6 of 8 #[tokio::test] functions carry #[ignore = TODO(Phase-10) ...]"
      contains: "use crate::"
  key_links:
    - from: "tests/integration/full_round.rs"
      to: "tests/integration/mod.rs"
      via: "use crate of require_bitcoind macro + bootstrap_regtest_bitcoind + BitcoindGuard + RpcCreds"
      pattern: "use crate::"
    - from: "Each test function (8 total)"
      to: "BitcoindGuard lifetime"
      via: "let (bitcoind_guard, creds) = bootstrap_regtest_bitcoind().await; (held until end-of-scope)"
      pattern: "bootstrap_regtest_bitcoind"
    - from: "6 specific test fns"
      to: "Phase 10 (REPAIR-01 carve-out)"
      via: "#[ignore = TODO(Phase-10) RPC schema drift ...]"
      pattern: "TODO.Phase-10."
---

<objective>
Migrate `tests/integration/full_round.rs` to consume the shared fixtures created in Plan 09-02 (`require_bitcoind!()`, `bootstrap_regtest_bitcoind()`, `BitcoindGuard`, `RpcCreds`), eliminating all 3 `Box::leak` callsites, all 6 inline `match corepc_node::exe_path()` skip blocks, and the 3 inline `Node::with_conf` bootstrap sequences. Add `#[ignore]` markers to the 6 known-broken RPC-schema-drift tests (per D-10 amended).

Purpose: This is the largest callsite migration in Phase 9. `full_round.rs` is approximately 1665 lines holding 8 `#[tokio::test]` functions and the test helper `fund_regtest`. It contains 3 of the 4 total `Box::leak` callsites in the codebase and 6 of the 8 inline skip blocks. After this plan + 09-04, the codebase has zero `Box::leak` in tests/integration/ and zero inline skip blocks — every bitcoind-dependent test uses the same shared fixture surface.

Output:
- `tests/integration/full_round.rs` with: (a) `use crate::{bootstrap_regtest_bitcoind, BitcoindGuard, RpcCreds};` added near the top; (b) each of 6 inline skip blocks replaced by deletion (bootstrap helper handles skip internally) OR by a leading `let _ = require_bitcoind!();`; (c) each of 3 inline `Node::with_conf` bootstrap sequences replaced with calls to `bootstrap_regtest_bitcoind().await`, holding the returned `BitcoindGuard` for the test's full duration; (d) all 3 `Box::leak(node_box)` lines deleted; (e) 6 specific test functions marked with the canonical `#[ignore = "TODO(Phase-10): ..."]` attribute; (f) the 2 non-broken tests (`adversarial_tampered_psbt_rejected`, `coordinator_info_endpoint_fields`) preserved as non-ignored.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ci-integration-test-reliability/09-CONTEXT.md
@.planning/phases/09-ci-integration-test-reliability/09-RESEARCH.md
@.planning/phases/09-ci-integration-test-reliability/09-PATTERNS.md
@tests/integration/full_round.rs
@tests/integration/mod.rs

<interfaces>
<!-- The public API surface from 09-02's tests/integration/mod.rs that this
     plan consumes. Executor MUST grep tests/integration/mod.rs first to
     confirm the actual signatures shipped match these contracts before
     relying on the values below. -->

From `tests/integration/mod.rs` (created in Plan 09-02):

  pub fn require_bitcoind_inner() -> Option<String>;
  #[macro_export] macro_rules! require_bitcoind { () => { ... } }
    // Use: let exe = require_bitcoind!();
    // Returns from caller on skip, panics in CI when env BLINDJOIN_REQUIRE_BITCOIND="1".

  #[derive(Clone, Debug)]
  pub struct RpcCreds { pub url: String, pub user: String, pub pass: String }

  pub struct BitcoindGuard { /* private */ }
  impl BitcoindGuard {
      pub fn new(node: corepc_node::Node) -> Self;
      pub fn node(&self) -> &corepc_node::Node;
  }
  impl Drop for BitcoindGuard { /* calls node.stop() then Node::Drop process.kill */ }

  pub async fn bootstrap_regtest_bitcoind() -> (BitcoindGuard, RpcCreds);
    // Wraps the entire daemon-bring-up sequence. Calls require_bitcoind!() internally.
    // Caller MUST hold the guard for the test's full duration.

KEY callsites in tests/integration/full_round.rs (line numbers verified via grep):

| Function                              | #[tokio::test] line | Skip block      | Box::leak | RPC-drift broken? |
| full_round_three_clients              | L154-155            | L156-163        | L269      | YES -> #[ignore]  |
| spawn_coordinator_with_blame          | L445 (helper)       | -               | -         | -                 |
| blame_non_signer_timeout              | L542-543            | L545-551        | L616      | YES -> #[ignore]  |
| fund_regtest (helper)                 | L738 (helper)       | (none)          | L800      | (no #[ignore])    |
| adversarial_replay_token              | L921-922            | L923 (inline)   | (via fund)| YES -> #[ignore]  |
| adversarial_invalid_utxo              | L1048-1049          | L1050           | (via fund)| YES -> #[ignore]  |
| adversarial_wrong_denomination        | L1108-1109          | L1110           | (via fund)| YES -> #[ignore]  |
| adversarial_tampered_psbt_rejected    | L1216-1217          | (via fund)      | (via fund)| NO — leave alone  |
| coordinator_info_endpoint_fields      | L1292-1293          | (verify)        | (verify)  | NO — leave alone  |
| round_restart_and_completion_after_blame | L1420-1421       | L1422           | (via fund)| YES -> #[ignore]  |

Total counts:
- 8 #[tokio::test] functions in the file (per grep)
- 6 of them get #[ignore = "TODO(Phase-10): RPC schema drift on listunspent/getrawtransaction — see TODO.md"]
- 2 of them (adversarial_tampered_psbt_rejected, coordinator_info_endpoint_fields) do NOT get ignore markers
- 3 Box::leak callsites to remove (L269, L616, L800)
- 6 inline match corepc_node::exe_path skip blocks to replace
- 3 inline Node::with_conf bootstrap sequences to consolidate (in full_round_three_clients, blame_non_signer_timeout, fund_regtest)

The migration of fund_regtest (line 738, a helper called by 5 of the 8 tests) is the most surgical change. Strategy: change signature from `fund_regtest(exe: String) -> FundedSetup` to `fund_regtest() -> (BitcoindGuard, FundedSetup)`. Callers update from `let setup = fund_regtest(exe).await;` to `let (bitcoind_guard, setup) = fund_regtest().await;` and hold the guard.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate self-contained bitcoind-bootstrap test functions in full_round.rs (full_round_three_clients, blame_non_signer_timeout, and fund_regtest helper)</name>
  <files>tests/integration/full_round.rs</files>
  <read_first>
    - tests/integration/full_round.rs (full file — at minimum lines 1-30 imports, 140-300 full_round_three_clients body, 540-630 blame_non_signer_timeout body, 735-810 fund_regtest helper body)
    - tests/integration/mod.rs (state after 09-02 — confirm the actual exported signatures match the contracts in the interfaces block above)
    - tests/integration/round_bootstrap.rs (the simplest analog of the post-migration form — uses bootstrap_regtest_bitcoind() directly with no funding work after, the cleanest before/after reference)
    - .planning/phases/09-ci-integration-test-reliability/09-PATTERNS.md "tests/integration/full_round.rs" section (specific replacement patterns at PATTERNS.md lines 143-254)
    - .planning/phases/09-ci-integration-test-reliability/09-RESEARCH.md Pitfall 4 (Send semantics — BitcoindGuard crosses spawn_blocking boundary cleanly)
  </read_first>
  <behavior>
    - After this task, tests/integration/full_round.rs contains zero Box::leak calls in the 3 functions touched (full_round_three_clients, blame_non_signer_timeout, fund_regtest)
    - All 3 functions use bootstrap_regtest_bitcoind() to bring up bitcoind and hold a BitcoindGuard for their full scope
    - fund_regtest signature changes from `async fn fund_regtest(exe: String) -> FundedSetup` to `async fn fund_regtest() -> (BitcoindGuard, FundedSetup)`; its 5 callers (the 4 adversarial tests + round_restart_and_completion_after_blame) are updated by Task 2
    - The 2 self-contained tests call bootstrap_regtest_bitcoind().await directly; their funding/setup work proceeds using guard.node() borrows OR by reconstructing an RPC client from RpcCreds (whichever compiles cleanly given Send/Sync of corepc_node::Node)
    - Binary compiles: cargo test --test integration --no-run exits 0
    - Clippy clean: cargo clippy --workspace --all-targets -- -D warnings exits 0
  </behavior>
  <action>Modify tests/integration/full_round.rs:

(a) **Add imports.** Near the top of the file (after the existing use statements; verify location by reading lines 1-30 first), add a single import line: `use crate::{bootstrap_regtest_bitcoind, BitcoindGuard, RpcCreds};`. The require_bitcoind macro is `#[macro_export]`-ed at the crate root (tests/integration/mod.rs) and may be invokable as bare `require_bitcoind!()` thanks to macro_export reaching crate scope; if rustc complains, fall back to `crate::require_bitcoind!()` at each callsite.

(b) **Migrate full_round_three_clients (L154-279).**
  - Delete the 8-line inline skip block at L156-163 (the `let exe = match corepc_node::exe_path() { ... return; ... };`). The bootstrap helper calls require_bitcoind!() internally; skip-or-panic happens before any test work runs.
  - Replace the spawn_blocking + Node::with_conf block (currently L165-279 ending in `Box::leak(node_box)`) as follows:
    - First line of test body: `let (bitcoind_guard, creds) = bootstrap_regtest_bitcoind().await;` — brings up the daemon, mines 101 blocks, returns the guard.
    - Then keep the test-specific funding work in a spawn_blocking that receives the RPC creds (and any Node-derived handles) by clone. Two options — pick whichever compiles cleanly:
      (i) **Reconstruct a blocking RPC client from creds** inside the spawn_blocking. Build a fresh `corepc_node::Client` (or a `reqwest::blocking::Client` doing raw JSON-RPC) from `creds.url`, `creds.user`, `creds.pass`. This avoids any guard-ownership-in-closure complications. Simplest if the existing fund-3-UTXOs logic only needs RPC, not Node-specific methods like Node::client.new_address().
      (ii) **Use guard.node() inside the spawn_blocking via Arc<BitcoindGuard>.** Wrap once: `let guard = Arc::new(bitcoind_guard); let guard_clone = guard.clone(); tokio::task::spawn_blocking(move || { let node = guard_clone.node(); ... });`. The catch: corepc_node::Node may not be Sync; if so, Arc<BitcoindGuard> can move into one spawn_blocking but not be borrowed across threads simultaneously. Acceptable for the single-spawn-blocking case.
    - After spawn_blocking returns, the test's post-bootstrap logic continues. `bitcoind_guard` must remain in scope until end-of-test. If wrapped in Arc, drop the test-side clone; the bitcoind binding inside the closure keeps it alive only through spawn_blocking — explicitly keep an outer binding.
  - The final FundedSetup struct is unchanged in shape (rpc_url, rpc_user, rpc_pass, utxos[3]); construct it from `creds.url`, `creds.user`, `creds.pass`, and the utxos vec from the funding step.

(c) **Migrate blame_non_signer_timeout (L542-616).** Same pattern as (b):
  - Delete the 7-line skip block at L545-551.
  - Replace the L553-616 spawn_blocking + Box::leak with `bootstrap_regtest_bitcoind().await` + a follow-up spawn_blocking (or Tokio blocking task) for the test-specific funding. Hold the BitcoindGuard for the rest of the test.

(d) **Migrate the fund_regtest helper (L738-810).** This is the most surgical change because it is a helper called by 5 other tests (the 4 adversarial tests + round_restart_and_completion_after_blame; addressed in Task 2).
  - Change the signature from `async fn fund_regtest(exe: String) -> FundedSetup` to `async fn fund_regtest() -> (BitcoindGuard, FundedSetup)`.
  - Inside the body: call `let (guard, creds) = bootstrap_regtest_bitcoind().await;` instead of taking `exe` and constructing Node inline.
  - The funding logic (derive 3 P2WPKH addresses, fund each with a UTXO, mine confirmation block, return UTXOs) stays largely as-is. Use creds for RPC URL/user/pass; if needed, obtain the node handle via `guard.node()` inside a follow-up spawn_blocking. Document inline why ownership of `guard` is returned to the caller (the caller must hold it for the test's full duration).
  - Return `(guard, FundedSetup { rpc_url: creds.url, rpc_user: creds.user, rpc_pass: creds.pass, utxos })`.

(e) **Confirm NO Box::leak remains in this file after Task 1.** Even though the 4-5 adversarial/restart tests have not been touched yet (Task 2), the Box::leak calls at L269, L616, L800 are all inside the 3 functions edited in this task (full_round_three_clients, blame_non_signer_timeout, fund_regtest). After Task 1: `grep -c 'Box::leak' tests/integration/full_round.rs` MUST return 0.

(f) **DO NOT add #[ignore] markers in this task.** Those go on in Task 2 as a separate, easily-revertible commit. Task 1 focuses on structural migration; the markers are pure metadata.

(g) **DO NOT change test assertions or test business logic.** Only the bitcoind-bringup boilerplate changes. If a test was asserting `info.round_state == "input_reg"`, that assertion remains identical.</action>
  <verify>
    <automated>cargo test --test integration --no-run 2>&1 | tail -20 ; cargo clippy --workspace --all-targets -- -D warnings 2>&1 | tail -10 ; grep -c 'Box::leak' tests/integration/full_round.rs ; grep -c 'bootstrap_regtest_bitcoind().await' tests/integration/full_round.rs ; grep -c 'fn fund_regtest()' tests/integration/full_round.rs</automated>
  </verify>
  <done>tests/integration/full_round.rs compiles cleanly (cargo test --test integration --no-run exits 0); contains zero Box::leak calls; imports bootstrap_regtest_bitcoind, BitcoindGuard, RpcCreds from crate::; the 3 functions full_round_three_clients, blame_non_signer_timeout, and fund_regtest use bootstrap_regtest_bitcoind().await; fund_regtest's signature is now async fn fund_regtest() -> (BitcoindGuard, FundedSetup); clippy is clean.</done>
  <acceptance_criteria>
    - Source (Box::leak fully removed from this file): `grep -c 'Box::leak' tests/integration/full_round.rs` returns 0
    - Source (imports added): `grep -c 'use crate::{bootstrap_regtest_bitcoind' tests/integration/full_round.rs` returns 1
    - Source (at least 3 calls to the shared bootstrap): the count from `grep -c 'bootstrap_regtest_bitcoind().await' tests/integration/full_round.rs` is at least 3 (one per migrated function: full_round_three_clients, blame_non_signer_timeout, fund_regtest's body)
    - Source (fund_regtest signature updated): `grep -c 'fn fund_regtest()' tests/integration/full_round.rs` returns 1 AND `grep -c 'fn fund_regtest(exe: String)' tests/integration/full_round.rs` returns 0
    - Behavior (binary compiles): `cargo test --test integration --no-run 2>&1` exits 0
    - Behavior (clippy clean): `cargo clippy --workspace --all-targets -- -D warnings 2>&1` exits 0
    - Source (test count unchanged — 8 #[tokio::test] before, 8 after; no tests deleted): `grep -c '#\[tokio::test\]' tests/integration/full_round.rs` returns 8
    - Source (no #[ignore] markers added yet — those are Task 2): `grep -c '#\[ignore' tests/integration/full_round.rs` returns 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate fund_regtest callers and add #[ignore] markers to the 6 Phase-10 carve-out tests</name>
  <files>tests/integration/full_round.rs</files>
  <read_first>
    - tests/integration/full_round.rs (current state after Task 1; lines 920-1450 hold the 5 fund_regtest-calling test functions)
    - .planning/phases/09-ci-integration-test-reliability/09-CONTEXT.md decision D-10 (amended) + canonical_refs naming the 6 broken tests
    - .planning/phases/09-ci-integration-test-reliability/09-PATTERNS.md "6 tests requiring #[ignore] markers" section (verbatim text for the ignore marker)
    - TODO.md lines 57-69 (the "Resolved 2026-05-26 / Integration test harness reliability" follow-up — source of the RPC-schema-drift framing the #[ignore] reason string references)
  </read_first>
  <behavior>
    - Each of the 4 adversarial fund_regtest-calling tests (adversarial_replay_token, adversarial_invalid_utxo, adversarial_wrong_denomination, adversarial_tampered_psbt_rejected) AND round_restart_and_completion_after_blame is updated from the old `let setup = fund_regtest(exe).await;` form to `let (bitcoind_guard, setup) = fund_regtest().await;` and holds bitcoind_guard for the rest of the test
    - The single-line inline skip blocks at lines approximately 923, 1050, 1110, 1422 are deleted (the bootstrap helper called by fund_regtest now handles skip-or-panic via require_bitcoind!())
    - Exactly 6 #[ignore = "TODO(Phase-10): ..."] markers exist in the file, on these specific tests: full_round_three_clients, blame_non_signer_timeout, adversarial_replay_token, adversarial_invalid_utxo, adversarial_wrong_denomination, round_restart_and_completion_after_blame
    - The 2 non-ignored tests are adversarial_tampered_psbt_rejected and coordinator_info_endpoint_fields
    - Test binary compiles AND clippy is clean
  </behavior>
  <action>Modify tests/integration/full_round.rs:

(a) **Update the 5 fund_regtest callers** (lines approximately 921, 1049, 1109, 1217, 1421) — the 4 adversarial tests AND round_restart_and_completion_after_blame:
  - Delete any inline `let exe = match corepc_node::exe_path() { Err(_) => return };` block (where present at L923, L1050, L1110, L1422).
  - Replace `let setup: FundedSetup = fund_regtest(exe).await;` (or equivalent) with `let (bitcoind_guard, setup): (BitcoindGuard, FundedSetup) = fund_regtest().await;`.
  - Ensure bitcoind_guard is held to end-of-test. Use a leading-underscore binding `let _bitcoind_guard = bitcoind_guard;` after the destructure (the clippy-safe form for "hold to end of scope, name documents intent") OR simply leave the named binding in scope (the destructure already gives `bitcoind_guard` a name and the test body doesn't need to reference it; rustc will warn unused-variable on `bitcoind_guard` — handle by adding `let _bitcoind_guard = bitcoind_guard;` directly after the destructure).

(b) **Check coordinator_info_endpoint_fields (L1292-1293).** Read lines 1292-1420 to determine whether this test uses bitcoind. If it does, apply the same migration as (a). If not (purely tests /info endpoint response shape with a coordinator and no bitcoind), leave it alone.

(c) **Add #[ignore] markers to the 6 broken tests.** For each of the 6 functions, insert the attribute on a NEW line BETWEEN the existing `#[tokio::test]` line and the `async fn <name>()` line.

The 6 functions and approximate locations:
  1. full_round_three_clients (L154-155)
  2. blame_non_signer_timeout (L542-543)
  3. adversarial_replay_token (L921-922)
  4. adversarial_invalid_utxo (L1048-1049)
  5. adversarial_wrong_denomination (L1108-1109)
  6. round_restart_and_completion_after_blame (L1420-1421)

The exact attribute (must be byte-for-byte identical so a future `grep -c 'TODO(Phase-10)' tests/` finds them all):

  #[ignore = "TODO(Phase-10): RPC schema drift on listunspent/getrawtransaction -- see TODO.md"]

NOTE: The CONTEXT.md and PATTERNS.md prose use the em-dash character U+2014. For source-file determinism and grep-friendliness, use the ASCII fallback `--` (two hyphens with single space on each side) — this is the form documented above and is what acceptance criteria match against. Document the choice in the SUMMARY so future planners know the em-dash is the intent and `--` is the on-disk form.

(d) **DO NOT add #[ignore] to:**
  - adversarial_tampered_psbt_rejected (L1216-1217) — passing per PATTERNS.md L252
  - coordinator_info_endpoint_fields (L1292-1293) — passing per PATTERNS.md L253

These two are the visible passing tests that prove TEST-02 ("integration tests actually execute in CI"). They MUST remain non-ignored for CI's PASS verdict to be observable.

(e) **Final consistency check.** Run the verify command. Confirm exactly 6 #[ignore lines exist and that each pairs with one of the 6 named functions.</action>
  <verify>
    <automated>cargo test --test integration --no-run 2>&1 | tail -20 ; cargo clippy --workspace --all-targets -- -D warnings 2>&1 | tail -10 ; grep -c 'Box::leak' tests/integration/full_round.rs ; grep -c 'TODO(Phase-10)' tests/integration/full_round.rs ; grep -c '^#\[ignore' tests/integration/full_round.rs ; grep -c 'fund_regtest(exe' tests/integration/full_round.rs ; grep -v '^//' tests/integration/full_round.rs | grep -c 'match corepc_node::exe_path'</automated>
  </verify>
  <done>tests/integration/full_round.rs compiles cleanly; contains 0 Box::leak; contains exactly 6 #[ignore = "TODO(Phase-10): ..."] markers on the 6 named functions; the 5 (or 4 if coordinator_info_endpoint_fields doesn't use bitcoind) fund_regtest callers use the new signature; no fund_regtest(exe...) calls remain; no inline match corepc_node::exe_path skip blocks remain in non-comment lines; clippy is clean.</done>
  <acceptance_criteria>
    - Source (Box::leak still 0 — preserved from Task 1): `grep -c 'Box::leak' tests/integration/full_round.rs` returns 0
    - Source (exactly 6 Phase-10 TODO markers): `grep -c 'TODO(Phase-10)' tests/integration/full_round.rs` returns 6
    - Source (exactly 6 #[ignore = ...] attributes that mention Phase-10): `grep -c '#\[ignore = "TODO(Phase-10)' tests/integration/full_round.rs` returns 6
    - Source (the canonical reason string with -- fallback): `grep -c 'RPC schema drift on listunspent/getrawtransaction -- see TODO.md' tests/integration/full_round.rs` returns 6
    - Source (each of the 6 broken functions has an ignore: full_round_three_clients): `grep -B1 'async fn full_round_three_clients' tests/integration/full_round.rs | grep -c '#\[ignore'` returns 1
    - Source (blame_non_signer_timeout has ignore): `grep -B1 'async fn blame_non_signer_timeout' tests/integration/full_round.rs | grep -c '#\[ignore'` returns 1
    - Source (adversarial_replay_token has ignore): `grep -B1 'async fn adversarial_replay_token' tests/integration/full_round.rs | grep -c '#\[ignore'` returns 1
    - Source (adversarial_invalid_utxo has ignore): `grep -B1 'async fn adversarial_invalid_utxo' tests/integration/full_round.rs | grep -c '#\[ignore'` returns 1
    - Source (adversarial_wrong_denomination has ignore): `grep -B1 'async fn adversarial_wrong_denomination' tests/integration/full_round.rs | grep -c '#\[ignore'` returns 1
    - Source (round_restart_and_completion_after_blame has ignore): `grep -B1 'async fn round_restart_and_completion_after_blame' tests/integration/full_round.rs | grep -c '#\[ignore'` returns 1
    - Source (adversarial_tampered_psbt_rejected does NOT have ignore): `grep -B1 'async fn adversarial_tampered_psbt_rejected' tests/integration/full_round.rs | grep -c '#\[ignore'` returns 0
    - Source (coordinator_info_endpoint_fields does NOT have ignore): `grep -B1 'async fn coordinator_info_endpoint_fields' tests/integration/full_round.rs | grep -c '#\[ignore'` returns 0
    - Source (no fund_regtest callers pass exe): `grep -c 'fund_regtest(exe' tests/integration/full_round.rs` returns 0
    - Source (no inline corepc_node::exe_path skip blocks in non-comment lines): `grep -v '^[[:space:]]*//' tests/integration/full_round.rs | grep -c 'match corepc_node::exe_path'` returns 0
    - Source (8 #[tokio::test] preserved): `grep -c '#\[tokio::test\]' tests/integration/full_round.rs` returns 8
    - Behavior (binary compiles): `cargo test --test integration --no-run 2>&1` exits 0
    - Behavior (clippy clean): `cargo clippy --workspace --all-targets -- -D warnings 2>&1` exits 0
    - Behavior (passing tests still pass when bitcoind is locally available — gated by env): `BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration adversarial_tampered_psbt_rejected -- --nocapture 2>&1 | grep -q 'test result: ok'` exits 0 when run locally with bitcoind installed; if bitcoind is NOT available locally, this acceptance criterion is deferred to CI verification (which Plan 09-01 enables). Document in SUMMARY which case ran.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test binary <-> bitcoind regtest child | Regtest network, disposable state, cookie-auth RPC; no real funds; OS-level process isolation is sufficient |
| test attribute metadata <-> CI reader | #[ignore] markers are advisory; cargo respects them by skipping execution but listing them in output. The grep-friendly TODO(Phase-10) token in the reason string ensures Phase 10 can find every marker via grep |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-09 | Tampering | A future PR removes an #[ignore] marker on one of the 6 RPC-drift tests, causing CI red without that PR repairing the underlying test | accept | Phase 10 explicitly removes these markers one-by-one as it repairs each test; the TODO(Phase-10) token is the discovery mechanism. A future-PR removal that doesn't repair is caught by CI going red — visible signal, easy revert |
| T-09-10 | Repudiation | A test passes locally but fails in CI because of bitcoind version mismatch | mitigate | Plan 09-01's .bitcoind-version pins v30.2 in CI and CONTRIBUTING.md (Plan 09-05) recommends the same to local developers. Mismatch becomes visible in test output via the rpc_url log line — fixable by local upgrade |
| T-09-11 | DoS | A migrated test holds BitcoindGuard incorrectly (drops it before the test body finishes), terminating bitcoind mid-test | mitigate | Acceptance criteria require the binding stay in scope (let _bitcoind_guard = bitcoind_guard; or unused-name binding from destructure). Clippy --D warnings catches misuse. If a test fails with bitcoind-not-found mid-execution, the failure is local to that test (does not cascade) |
</threat_model>

<verification>
- cargo test --test integration --no-run exits 0
- cargo clippy --workspace --all-targets -- -D warnings exits 0
- grep -c 'Box::leak' tests/integration/full_round.rs returns 0
- grep -c 'TODO(Phase-10)' tests/integration/full_round.rs returns 6
- grep -c '#[ignore = "TODO(Phase-10)' tests/integration/full_round.rs returns 6
- grep -c 'fund_regtest()' tests/integration/full_round.rs returns at least 6 (1 definition + 5 callers; if coordinator_info_endpoint_fields uses bitcoind, count is 7)
- grep -c '#[tokio::test]' tests/integration/full_round.rs returns 8 (no tests deleted)
- grep -B1 'async fn adversarial_tampered_psbt_rejected' returns no #[ignore] line
- grep -B1 'async fn coordinator_info_endpoint_fields' returns no #[ignore] line

Runtime verification (requires bitcoind locally OR a CI run):
- cargo test --test integration --list shows the 6 ignored tests as `<name>: test (ignored)` and the 2 non-ignored as `<name>: test`
- Locally with brew bitcoind: BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration adversarial_tampered_psbt_rejected -- --nocapture finishes with `test result: ok`
</verification>

<success_criteria>
- tests/integration/full_round.rs contains zero Box::leak calls
- tests/integration/full_round.rs imports bootstrap_regtest_bitcoind, BitcoindGuard, RpcCreds from `crate::`
- The 3 internal-bootstrap functions (full_round_three_clients, blame_non_signer_timeout, fund_regtest) use bootstrap_regtest_bitcoind().await
- fund_regtest's signature is `async fn fund_regtest() -> (BitcoindGuard, FundedSetup)`
- All 5 fund_regtest callers use the new tuple-destructure form
- Exactly 6 #[ignore = "TODO(Phase-10): ..."] markers exist on the 6 named functions
- adversarial_tampered_psbt_rejected and coordinator_info_endpoint_fields are NOT marked ignored
- cargo test --test integration --no-run exits 0
- cargo clippy --workspace --all-targets -- -D warnings exits 0
- TEST-03 + TEST-04 partial closure: full closure also requires Plan 09-04 to migrate rate_limiting.rs + round_bootstrap.rs (the last 1 Box::leak and 3 skip blocks)
</success_criteria>

<output>
Create `.planning/phases/09-ci-integration-test-reliability/09-03-SUMMARY.md` when done. Summary must include: (a) the exact fund_regtest call-pattern used (Arc<BitcoindGuard> vs reconstruct-client-from-creds); (b) whether coordinator_info_endpoint_fields was migrated or left alone; (c) the exact ignore marker string used (with or without em-dash); (d) confirmation of all grep counts in acceptance criteria; (e) whether the local-bitcoind PASS check for adversarial_tampered_psbt_rejected was run or deferred to CI.
</output>

## PLAN COMPLETE
