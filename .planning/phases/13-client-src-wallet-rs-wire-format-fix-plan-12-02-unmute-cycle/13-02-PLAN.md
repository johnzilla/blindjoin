---
phase: 13-client-src-wallet-rs-wire-format-fix-plan-12-02-unmute-cycle
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - tests/integration/full_round.rs
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: []
tags:
  - integration-tests
  - full-round
  - unmute
  - bitcoind-pinned
  - phase-10-carve-out
  - per-test-commit-cycle
  - three-sha-body
  - closeout-doc-reconciliation
must_haves:
  truths:
    - "All six #[ignore = TODO(Phase-10): ...] attribute lines in tests/integration/full_round.rs (canonical-first lines 164, 462, 730, 854, 911, 1236 pre-Plan-13-02; line numbers shift after each delete — re-grep by adjacent test fn name per D-05/T-11-13) are removed — one per commit, in canonical-first then file order"
    - "Six atomic test-unmute commits with subjects `test(13): unmute <test_fn> (Phase-10 carve-out N/6)` for N=1..6 land in canonical-first then file order: full_round_three_clients (1/6) → blame_non_signer_timeout (2/6) → adversarial_replay_token (3/6) → adversarial_invalid_utxo (4/6) → adversarial_wrong_denomination (5/6) → round_restart_and_completion_after_blame (6/6)"
    - "Each of the 6 unmute commit bodies carries the three-SHA extension of CD-1 (per D-05): the per-test cargo invocation, the PASS verdict line, `bitcoind --version | head -1` output (CD-4), and three prerequisite SHAs — Phase 11 RSA fix `cc20f6fbca4d292bf7b394a3850b18d244b5b602`, Phase 12 wallet-trust fix `0bbcf3c76ca251c14aa64216ca6955be1f880b9a`, Phase 13 Plan 13-01 wire-format fix SHA (captured at execute time via `git log --grep='^fix(13): encode partial sig' --format=%H -1`)"
    - "Each unmute commit's `git diff <commit>~1 <commit> --stat` is exactly `1 file changed, 0 insertions(+), 1 deletion(-)` — only the single #[ignore] line is removed; zero drive-by edits to other tests, helpers, or unrelated code (D-07 from Plan 11-02; T-11-13 line-drift discipline inherited)"
    - "Each of the 8 full_round tests (2 originally passing + 6 just-unmuted) passes locally against pinned brew bitcoind v31 via the CONTRIBUTING.md canonical invocation (BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round)"
    - "If the canonical-first unmute (1/6 full_round_three_clients) FAILS, the executor halts immediately and emits a CHECKPOINT REACHED marker per D-12; commits 2-6 are NOT attempted; Phase 14 absorbs the 6th orthogonal blocker per D-13. Pre-authorized in-flight scope expansion is ZERO."
    - "If any of unmutes 2-6 FAILS during its PASS-proof capture, the executor halts at first encounter, does NOT delete the #[ignore] line for that test, does NOT commit, does NOT attempt later iterations; earlier successful commits remain bisect-clean; a CHECKPOINT REACHED marker is emitted per D-12"
    - "After the six unmute commits land, a SEVENTH atomic closeout commit reconciles the D-14 doc drift: (a) if all 8 full_round tests are green, REQUIREMENTS.md line 20 REPAIR-01 stays `[x]` with corrected provenance in the commit message AND ROADMAP.md Phase 13 entry is marked complete in the SAME commit (per Phase 12 D-13 convention); (b) if any test is red, REPAIR-01 flips back to `[ ]` AND ROADMAP.md Phase 13 entry is NOT marked complete"
    - "Decision coverage: D-04 Phase 13 owns REPAIR-01 closure, D-05 verbatim Plan 11-02 spec + three-SHA body extension, D-06 two-plan structure (this is Plan 2, depends_on 13-01), D-12 escape-valve halt-and-surface protocol, D-13 Phase 14 absorbs 6th-blocker overflow, D-14 closeout doc reconciliation (REQUIREMENTS.md + ROADMAP.md in SAME commit, conditional on 8/8 green), D-15 v1.3 ship notes out of scope, CD-2 brew-prefix invocation form, CD-4 bitcoind version capture, CD-5 single-session execution (recommended)"
  artifacts:
    - path: "tests/integration/full_round.rs"
      provides: "Six tests unmuted (no #[ignore = TODO(Phase-10) ...] attributes remain); all 8 full_round tests run end-to-end without `-- --ignored`"
      contains: "async fn full_round_three_clients"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Line 20 REPAIR-01 status reconciled per D-14 (stays `[x]` if green, flips to `[ ]` if red); single-character `[ ]`/`[x]` flip in the closeout commit"
      contains: "REPAIR-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 13 entry marked complete in the closeout commit (only if 8/8 green per D-14); same-commit coupling with REQUIREMENTS.md flip"
      contains: "Phase 13"
  key_links:
    - from: "tests/integration/full_round.rs (6 unmuted tests)"
      to: "client/src/wallet.rs lines ~279-282 (the post-Plan-13-01 bitcoin::Witness encoder)"
      via: "The signing phase of each full round exercises the wire-format encoder Plan 13-01 fixed"
      pattern: "bitcoin::Witness::new"
    - from: "tests/integration/full_round.rs (6 unmuted tests)"
      to: "client/src/round/input.rs:40 (Plan 11-01 RSA from_spki decode at cc20f6f) + client/src/wallet.rs:273 (Plan 12-01 trust_witness_utxo: true at 0bbcf3c)"
      via: "Each unmuted test exercises all three prerequisite fixes end-to-end"
      pattern: "BjPublicKey::from_spki|trust_witness_utxo"
    - from: ".planning/REQUIREMENTS.md line 20 (REPAIR-01 status)"
      to: ".planning/ROADMAP.md Phase 13 entry"
      via: "D-14 same-commit closeout reconciliation (atomic doc commit, conditional on 8/8 green)"
      pattern: "REPAIR-01.*\\[(x| )\\]"
---

<objective>
Re-execute Plan 12-02's six-unmute cycle verbatim modulo the three-SHA body extension. Plan 11-02-PLAN.md is the pinned template (third iteration of the same spec); only the SHA-reference list grows from two-SHA (Plan 12-02 attempt) to three-SHA (RSA `cc20f6f` + wallet-trust `0bbcf3c` + Plan 13-01's wire-format fix SHA captured at execute time). After the six unmute commits land, a SEVENTH atomic closeout commit reconciles the D-14 doc drift in `.planning/REQUIREMENTS.md` line 20 (REPAIR-01) AND marks `.planning/ROADMAP.md` Phase 13 entry complete — both in the SAME commit (per Phase 12 D-13 same-commit convention), conditional on 8/8 green.

Purpose: With Plan 13-01's wire-format fix in history (Plan 12-02's 5th orthogonal blocker cleared), the canonical-first gate (`full_round::full_round_three_clients`) should PASS — Plan 13-01's D-07 sanity capture already proved this in the working tree, and this plan's Task 1 re-runs the test with the `#[ignore]` removed to capture the bisect-proof for the unmute commit itself. The remaining 5 tests in file order are mechanical mirror-image executions of the same per-test cycle. If any test fails, D-12 halts the executor and Phase 14 absorbs the 6th orthogonal blocker per D-13. REPAIR-01 closes locally (D-04 / D-14) only when all 8 tests are green; partial-green (e.g., 7/8) flips REPAIR-01 back to `[ ]` and leaves Phase 13 incomplete in the ROADMAP.

Output: Six atomic test-unmute commits (one per `#[ignore]` removal, canonical-first then file order) each with a three-SHA CD-1 body, plus a seventh atomic closeout commit reconciling REQUIREMENTS.md line 20 + ROADMAP.md Phase 13 entry (per D-14). Total: seven commits. After the cycle, `BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round` reports `test result: ok. 8 passed; 0 failed; 0 ignored` against pinned brew bitcoind v31, and REQUIREMENTS.md line 20 REPAIR-01 truthfully reflects suite state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-client-src-wallet-rs-wire-format-fix-plan-12-02-unmute-cycle/13-CONTEXT.md
@.planning/phases/13-client-src-wallet-rs-wire-format-fix-plan-12-02-unmute-cycle/13-PATTERNS.md
@.planning/phases/13-client-src-wallet-rs-wire-format-fix-plan-12-02-unmute-cycle/13-01-PLAN.md
@.planning/phases/11-coordinator-rsa-pubkey-encoding-full-round-rs-unmute-complet/11-02-PLAN.md
@.planning/phases/12-repair-client-src-wallet-rs-260-bdk-wallet-2-3-signoptions-r/12-02-SUMMARY.md
@CONTRIBUTING.md
@tests/integration/full_round.rs
@tests/integration/mod.rs

<interfaces>
<!-- The six unmute sites, in canonical-first order per D-05. Source: `grep -n "TODO(Phase-10)" tests/integration/full_round.rs` (verified 2026-05-28). Same six sites Plan 12-02 was going to unmute. -->

Order  Pre-Plan-13-02 line  Async fn (preserved unchanged)                                Commit subject (subject prefix `test(13):`)
-----  ------------------- ------------------------------------------------------------- ----------------------------------------------------------------------
1st    164                  async fn full_round_three_clients()  (MANDATORY 1st)         test(13): unmute full_round_three_clients (Phase-10 carve-out 1/6)
2nd    462                  async fn blame_non_signer_timeout()                          test(13): unmute blame_non_signer_timeout (Phase-10 carve-out 2/6)
3rd    730                  async fn adversarial_replay_token()                          test(13): unmute adversarial_replay_token (Phase-10 carve-out 3/6)
4th    854                  async fn adversarial_invalid_utxo()                          test(13): unmute adversarial_invalid_utxo (Phase-10 carve-out 4/6)
5th    911                  async fn adversarial_wrong_denomination()                    test(13): unmute adversarial_wrong_denomination (Phase-10 carve-out 5/6)
6th    1236                 async fn round_restart_and_completion_after_blame()          test(13): unmute round_restart_and_completion_after_blame (Phase-10 carve-out 6/6)

The line ABOVE each `#[ignore]` is `#[tokio::test]` — preserved unchanged. The line BELOW each `#[ignore]` is `async fn <test_name>() {` — preserved unchanged. Each unmute = ONE line deletion, nothing else.

After each delete, all subsequent line numbers SHIFT DOWN BY 1. Per T-11-13 (inherited from Plan 11-02-PLAN.md), Plan 13-02 re-greps for the `#[ignore]` line by adjacent test-fn name at EACH iteration; do NOT trust the pre-Plan-13-02 line numbers above for unmutes 2-6.

<!-- Canonical local test invocation pattern (CONTRIBUTING.md §"Running integration tests"), reused byte-for-byte for each PASS-proof capture per CD-2. -->

Per-test:
  BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind \
    cargo test --test integration full_round::<test_name> -- --ignored 2>&1 \
    | tee target/integration-test-13-02-${N}.log

Whole-file (after all 6 unmutes):
  BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind \
    cargo test --test integration full_round 2>&1 \
    | tee target/integration-test-13-02-final.log

The `-- --ignored` flag is required to run a test that STILL has `#[ignore]` on it. The per-test PASS proof during the cycle is captured BEFORE the line is deleted in the same commit. Once the attribute is removed, the test runs by default — the post-commit incremental verification uses cargo WITHOUT `--ignored`.

<!-- Three prerequisite SHAs that MUST be in history before Plan 13-02 starts. Plan 13-02 Task 1 verifies all three. -->

Phase 11 RSA fix:        cc20f6fbca4d292bf7b394a3850b18d244b5b602  (static — locked in CONTEXT.md D-05)
Phase 12 wallet-trust:   0bbcf3c76ca251c14aa64216ca6955be1f880b9a  (static — locked in CONTEXT.md D-05)
Phase 13 wire-format:    $(git log --grep="^fix(13): encode partial sig" --format=%H -1)  (dynamic — Plan 13-01's commit SHA, captured at execute time)

<!-- Per-commit body shape (three-SHA extension of CD-1, per D-05 of Phase 13 CONTEXT.md). Source: 13-PATTERNS.md §"Per-commit body shape" lines 221-233. -->

cargo test --test integration full_round::<test_fn> -- --ignored
test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; N filtered out

bitcoind --version | head -1: Bitcoin Core daemon version v31.0.0 bitcoind

Prereqs in history:
  RSA fix (Phase 11):           cc20f6fbca4d292bf7b394a3850b18d244b5b602
  Wallet-trust fix (Phase 12):  0bbcf3c76ca251c14aa64216ca6955be1f880b9a
  Wire-format fix (Phase 13):   <Plan 13-01 SHA — captured via git log --grep at execute time>

<!-- D-14 closeout commit shape. Source: 13-PATTERNS.md §".planning/REQUIREMENTS.md line 20" (lines 265-282) + §".planning/ROADMAP.md Phase 13 entry" (lines 286-309). -->

The closeout commit (Task 3) is ONE atomic commit that touches exactly two files:
  - .planning/REQUIREMENTS.md (Traceability table row for REPAIR-01 — flip `Phase 10 | TBD | active` to `Phase 13 | 13-01, 13-02 | closed` per D-14 GREEN case; OR flip line 20 status `[x]` → `[ ]` per D-14 RED case)
  - .planning/ROADMAP.md (Phase 13 entry — replace placeholder Goal/Plans/list with real summary and `[x]` entries IF 8/8 green; otherwise leave incomplete and surface failure mode in commit body)

Closeout commit subject (default, planner discretion within Phase 12 D-13 style):
  docs(13): reconcile REPAIR-01 status + close Phase 13 entry (8/8 full_round tests green)  ← GREEN case
  docs(13): flip REPAIR-01 back to [ ] — Phase 13 incomplete (N/8 full_round tests green)   ← RED case (N < 8)

Closeout commit body MUST cite:
  - The final whole-file PASS verdict line (or FAIL summary if red)
  - The six unmute commit SHAs (in canonical-first then file order)
  - The three prerequisite SHAs (RSA + wallet-trust + Plan 13-01 wire-format)
  - The conditional D-14 disposition: "8/8 green → REPAIR-01 stays [x], Phase 13 marked complete" OR "N/8 green → REPAIR-01 flipped to [ ], Phase 13 NOT marked complete, Phase 14 opens"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Unmute full_round_three_clients (line 164) — canonical-first MANDATORY gate; capture three-SHA body</name>
  <files>tests/integration/full_round.rs</files>
  <read_first>
    - .planning/phases/13-client-src-wallet-rs-wire-format-fix-plan-12-02-unmute-cycle/13-CONTEXT.md (D-04 Phase 13 owns REPAIR-01 closure, D-05 verbatim Plan 11-02 spec + three-SHA body, D-06 two-plan structure / depends_on 13-01, D-12 escape-valve halt-and-surface, D-13 Phase 14 absorbs 6th-blocker overflow, CD-2 brew-prefix form, CD-4 bitcoind version capture, CD-5 single-session)
    - .planning/phases/13-client-src-wallet-rs-wire-format-fix-plan-12-02-unmute-cycle/13-PATTERNS.md §"tests/integration/full_round.rs (test — six attribute-only deletions)" (the attribute pair shape; the six prescribed commit subjects; the per-commit body shape; the line-drift discipline T-11-13)
    - .planning/phases/11-coordinator-rsa-pubkey-encoding-full-round-rs-unmute-complet/11-02-PLAN.md Task 1 (the pinned template Plan 13-02 reuses verbatim modulo three-SHA body extension and `test(11):` → `test(13):` subject rewrite)
    - tests/integration/full_round.rs lines 160-200 (current state of `full_round_three_clients` and the #[ignore] attribute at line 164 — confirm `grep -c "TODO(Phase-10)" tests/integration/full_round.rs` returns 6 BEFORE this task starts; if 5 or less, a prior unmute leaked through and the executor MUST halt)
    - tests/integration/mod.rs (the shared fixtures: require_bitcoind!, BitcoindGuard, fund_regtest, FundedSetup — depend on, do NOT modify)
    - CONTRIBUTING.md §"Running integration tests" (the canonical local invocation pattern — used byte-for-byte for the PASS-proof capture; CD-2 keeps `$(brew --prefix)` form literal in commit body)
    - .planning/phases/12-repair-client-src-wallet-rs-260-bdk-wallet-2-3-signoptions-r/12-02-SUMMARY.md §"Recovery Path — Phase 13" (the seed; confirms canonical-first ordering and three-SHA body shape)
  </read_first>
  <behavior>
    - Test 1 (prerequisite verification, BEFORE any edit): all three prerequisite SHAs are in history. `git log --format=%H | grep -qxF cc20f6fbca4d292bf7b394a3850b18d244b5b602` (Phase 11 RSA fix), `git log --format=%H | grep -qxF 0bbcf3c76ca251c14aa64216ca6955be1f880b9a` (Phase 12 wallet-trust fix), and `WIRE_FORMAT_SHA=$(git log --grep='^fix(13): encode partial sig' --format=%H -1)` returns a non-empty 40-char SHA (Phase 13 Plan 13-01 wire-format fix). If any is missing, HALT.
    - Test 2 (six-site precondition): `grep -c "TODO(Phase-10)" tests/integration/full_round.rs` returns exactly 6 (the same six sites Plan 12-02 was going to unmute; nothing leaked through prior phases). If not 6, HALT.
    - Test 3 (capture-then-unmute, in order): run `BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round::full_round_three_clients -- --ignored 2>&1 | tee target/integration-test-13-02-1.log` and capture the PASS verdict line (`test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 7 filtered out`). If the test FAILS, HALT — do NOT delete the #[ignore] line, do NOT commit, do NOT proceed to Task 2. Emit a CHECKPOINT REACHED marker per D-12 naming the failure mode and a proposed minimal repair; explicitly note that Phase 13 Plan 13-01's D-07 sanity capture PASSed but this Task 1 capture failed → "the wire-format fix unblocked the working tree but a new orthogonal blocker has surfaced; Phase 14 absorbs per D-13."
    - Test 4 (post-edit verification): after deleting the #[ignore] line at the current position of `full_round_three_clients`, re-running `cargo test --test integration full_round::full_round_three_clients` WITHOUT `--ignored` still PASSES (the test now runs by default).
    - Test 5 (whole-file sanity, after this commit only): `cargo test --test integration full_round` reports 3 passed (2 originally passing + 1 just unmuted), 5 ignored (the other 5 carve-outs still carry their #[ignore]).
    - Test 6 (commit shape): exactly one commit lands on `tests/integration/full_round.rs` with subject `test(13): unmute full_round_three_clients (Phase-10 carve-out 1/6)` and a three-SHA body containing the per-test cargo invocation, the PASS verdict, the bitcoind version, and all three prerequisite SHAs.
  </behavior>
  <action>
    Execute the per-test cycle for the 1st unmute in this exact order. Inherited verbatim from Plan 11-02-PLAN.md Task 1, modulo three-SHA body extension per D-05 of Phase 13 CONTEXT.md and `test(11):` → `test(13):` subject rewrite.

    (a) Capture all three prerequisite SHAs into shell variables BEFORE any edit:
        - `RSA_FIX_SHA=cc20f6fbca4d292bf7b394a3850b18d244b5b602` (static — locked in CONTEXT.md D-05)
        - `WALLET_TRUST_SHA=0bbcf3c76ca251c14aa64216ca6955be1f880b9a` (static — locked in CONTEXT.md D-05)
        - `WIRE_FORMAT_SHA=$(git log --grep="^fix(13): encode partial sig" --format=%H -1)` (dynamic — Plan 13-01's commit SHA)

        Verify each SHA is in history (do NOT trust the literals blindly): `git cat-file -e ${RSA_FIX_SHA} && git cat-file -e ${WALLET_TRUST_SHA} && git cat-file -e ${WIRE_FORMAT_SHA}`. If any cat-file fails or WIRE_FORMAT_SHA is empty, HALT — emit a CHECKPOINT REACHED marker citing "Plan 13-01 was not run / commit missing; Plan 13-02 cannot open." Also verify the six-site precondition: `[ "$(grep -c "TODO(Phase-10)" tests/integration/full_round.rs)" = "6" ]`. If not 6, HALT — a prior phase leaked an unmute through.

    (b) Run the per-test PASS-proof invocation byte-for-byte:
        `BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round::full_round_three_clients -- --ignored 2>&1 | tee target/integration-test-13-02-1.log`

        Capture the verdict line (matching `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 7 filtered out`) into shell variable `VERDICT_1`. Capture `bitcoind --version | head -1` output into `BITCOIND_VERSION` (CD-4).

    (c) If VERDICT_1 is NOT a PASS (`test result: FAILED`, test panic, or any HTTP non-200 in the log other than the expected protocol flow), HALT IMMEDIATELY. Do NOT delete the #[ignore] line. Do NOT commit. Emit a CHECKPOINT REACHED marker with:
        - The failure mode (panic message, the failing assertion line, or the cargo verdict).
        - A proposed minimal repair scoped to the 6th orthogonal blocker.
        - The explicit note: "D-12 escape-valve invoked: canonical-first unmute (1/6) failed AFTER wire-format fix landed (Plan 13-01 D-07 sanity PASSed; this Task 1 unmute capture FAILED — a new orthogonal blocker has surfaced). Phase 13 halts; Phase 14 absorbs per D-13. Tasks 2 and 3 (closeout) are NOT attempted. Pre-authorized in-flight scope expansion is ZERO."

    (d) On VERDICT_1 = PASS: delete EXACTLY one line — the `#[ignore = "TODO(Phase-10): RPC schema drift on listunspent/getrawtransaction -- see TODO.md"]` line currently at position 164 (verified by step (a) — if line 164 is NOT this attribute line, re-grep by adjacent test-fn name via T-11-13 discipline and use that line number instead). The line above (line 163, `#[tokio::test]`) and the line below (line 165, `async fn full_round_three_clients() {`) stay byte-identical. NO whitespace edits. NO comment substitution. NO test-body changes (D-07 from Plan 11-02 inherited: "no drive-by edits to other tests, helpers, or unrelated code").

        Verify the edit's shape: `git diff tests/integration/full_round.rs --stat` reports `1 file changed, 0 insertions(+), 1 deletion(-)`. If the stat shows any `insertions(+)`, HALT — a drive-by edit slipped in; revert via `git checkout -- tests/integration/full_round.rs` and re-attempt the targeted delete.

    (e) Stage and commit atomically using the Bash tool with a HEREDOC. Commit subject (exact string):
        `test(13): unmute full_round_three_clients (Phase-10 carve-out 1/6)`

        Commit body (three-SHA CD-1 stamp per D-05, exact shape from 13-PATTERNS.md §"Per-commit body shape" lines 221-233):

            cargo test --test integration full_round::full_round_three_clients -- --ignored
            <paste VERDICT_1 verbatim — e.g. "test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 7 filtered out">

            bitcoind --version | head -1: <paste BITCOIND_VERSION verbatim — e.g. "Bitcoin Core daemon version v31.0.0 bitcoind">

            Prereqs in history:
              RSA fix (Phase 11):           ${RSA_FIX_SHA}
              Wallet-trust fix (Phase 12):  ${WALLET_TRUST_SHA}
              Wire-format fix (Phase 13):   ${WIRE_FORMAT_SHA}

        CD-2 form preservation: keep `$(brew --prefix)/bin/bitcoind` literal in the cargo invocation line (do NOT substitute the resolved path).

    (f) Verify post-commit: `BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round 2>&1 | tee target/integration-test-13-02-1-postcommit.log` (WITHOUT `--ignored` — one test is now unmuted). Expected verdict line: `test result: ok. 3 passed; 0 failed; 5 ignored`. If the count is wrong, HALT and report — neither Task 2 nor Task 3 should run against an unexpected pass/ignore distribution.

    NO other files are modified. NO entries added to TODO.md. NO doc updates (REQUIREMENTS.md + ROADMAP.md are Task 3's surface per D-14). NO test-body edits. The three prerequisite SHAs must be in history before this task runs (asserted in step (a)).
  </action>
  <verify>
    <automated>BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round::full_round_three_clients 2>&1 | grep -qE "test result: ok\. 1 passed" && [ "$(grep -c 'TODO(Phase-10)' tests/integration/full_round.rs)" = "5" ] && git log -1 --format=%s | grep -qxF "test(13): unmute full_round_three_clients (Phase-10 carve-out 1/6)" && git log -1 --format=%b | grep -qF "cc20f6fbca4d292bf7b394a3850b18d244b5b602" && git log -1 --format=%b | grep -qF "0bbcf3c76ca251c14aa64216ca6955be1f880b9a" && git log -1 --format=%b | grep -qE "Wire-format fix \(Phase 13\): +[0-9a-f]{40}"</automated>
  </verify>
  <acceptance_criteria>
    - Prerequisite assertion (step a): `git cat-file -e cc20f6fbca4d292bf7b394a3850b18d244b5b602 && git cat-file -e 0bbcf3c76ca251c14aa64216ca6955be1f880b9a && [ -n "$(git log --grep='^fix(13): encode partial sig' --format=%H -1)" ]` exits 0 — all three prerequisite SHAs are in history.
    - Source assertion: `grep -c "TODO(Phase-10)" tests/integration/full_round.rs` returns exactly 5 (down from 6 — one removed, five still present for Task 2). This is the authoritative deletion proof; it does NOT depend on the post-delete line number, which shifts after the line is removed.
    - Source assertion: `grep -n "^async fn full_round_three_clients" tests/integration/full_round.rs` returns exactly one line — the test fn is still present (test body preserved, only the attribute removed).
    - Source assertion: the line IMMEDIATELY above the `async fn full_round_three_clients() {` line is `#[tokio::test]` (NOT `#[ignore = ...]`). Verify with: `LINE=$(grep -n "^async fn full_round_three_clients" tests/integration/full_round.rs | cut -d: -f1) && PREV=$((LINE - 1)) && sed -n "${PREV}p" tests/integration/full_round.rs | grep -qxF '#[tokio::test]'`.
    - Test command assertion: `BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round::full_round_three_clients 2>&1 | grep -qE "test result: ok\. 1 passed; 0 failed"` (the test now runs and passes WITHOUT `--ignored`).
    - Test command assertion: `BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round 2>&1 | grep -qE "test result: ok\. 3 passed; 0 failed; 5 ignored"` (incremental whole-file run: 2 already-passing + 1 just unmuted = 3 passed; 5 still-ignored = the remaining carve-outs).
    - CLI assertion: `git log -1 --format=%s | grep -qxF 'test(13): unmute full_round_three_clients (Phase-10 carve-out 1/6)'` returns 0 exit.
    - CLI assertion (three-SHA body): `git log -1 --format=%b` contains the literal strings `cargo test --test integration full_round::full_round_three_clients -- --ignored`, `test result: ok. 1 passed`, `bitcoind --version`, `Bitcoin Core daemon version v`, `RSA fix (Phase 11):`, `cc20f6fbca4d292bf7b394a3850b18d244b5b602`, `Wallet-trust fix (Phase 12):`, `0bbcf3c76ca251c14aa64216ca6955be1f880b9a`, `Wire-format fix (Phase 13):`, AND a 40-char hex SHA matching `^[0-9a-f]{40}$` on the Wire-format line. Verify with eight `grep -qF`/`grep -qE` checks.
    - CD-2 form preservation: `git log -1 --format=%b | grep -qF '$(brew --prefix)/bin/bitcoind'` (literal `$(brew --prefix)`, NOT the resolved `/opt/homebrew/bin/bitcoind` path).
    - Single-file diff assertion: `git diff HEAD~1 HEAD --stat` shows `1 file changed, 0 insertions(+), 1 deletion(-)`. Run `git diff HEAD~1 HEAD --shortstat | grep -qE '1 file changed,.*1 deletion' && ! git diff HEAD~1 HEAD --shortstat | grep -qE '[0-9]+ insertion'` (zero insertions — pure deletion).
    - Single-file scope assertion: `git diff HEAD~1 HEAD --name-only` outputs exactly one line = `tests/integration/full_round.rs`. No other file is touched by this commit.
    - Halt-recovery assertion (negative test): if step (b) returned a FAIL/panic, no commit exists for this task, the #[ignore] line at position 164 is still present (`grep -c "TODO(Phase-10)" tests/integration/full_round.rs` still returns 6), and the executor has emitted a CHECKPOINT REACHED marker citing D-12. Task 2 and Task 3 are NOT attempted. This is a recovery state, not a task failure.
  </acceptance_criteria>
  <done>
    The `#[ignore]` attribute at position 164 (pre-Plan-13-02) is removed; `full_round_three_clients` passes locally without `--ignored`; whole-file `cargo test --test integration full_round` shows 3 passed / 5 ignored; one atomic commit with the prescribed subject and the three-SHA body has landed; the three prerequisite SHA references (RSA + wallet-trust + Plan 13-01 wire-format) are in the body. If the canonical-first test FAILED, the executor has HALTED, the working-tree is unchanged from pre-task state (no #[ignore] line was deleted), a CHECKPOINT REACHED marker citing D-12 has been emitted, and Tasks 2 + 3 are NOT attempted — Phase 14 absorbs the 6th orthogonal blocker per D-13.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Unmute the remaining 5 carve-out tests in file order (originally lines 462, 730, 854, 911, 1236 — re-grep by test-fn name per T-11-13 line-drift discipline)</name>
  <files>tests/integration/full_round.rs</files>
  <read_first>
    - .planning/phases/13-client-src-wallet-rs-wire-format-fix-plan-12-02-unmute-cycle/13-CONTEXT.md (D-05 file-order rule for unmutes 2-6, D-12 escape-valve, CD-2 brew-prefix, CD-4 bitcoind version capture, CD-5 single-session execution)
    - .planning/phases/13-client-src-wallet-rs-wire-format-fix-plan-12-02-unmute-cycle/13-PATTERNS.md §"tests/integration/full_round.rs" (the attribute pair shape — identical at all 5 remaining sites; the six prescribed commit subjects; the per-commit three-SHA body shape; the T-11-13 line-drift discipline)
    - .planning/phases/11-coordinator-rsa-pubkey-encoding-full-round-rs-unmute-complet/11-02-PLAN.md Task 2 (the pinned template Plan 13-02 reuses verbatim modulo three-SHA body extension and `test(11):` → `test(13):` subject rewrite)
    - tests/integration/full_round.rs (after Task 1's commit lands, re-read the file to confirm the remaining 5 #[ignore] sites — line numbers SHIFT DOWN BY 1 after Task 1's deletion; re-grep for each #[ignore] by adjacent test-fn name BEFORE each delete, NOT by trusting the pre-Plan-13-02 line numbers from the spec)
    - tests/integration/mod.rs (the shared fixtures, unchanged — depend on, do NOT modify)
    - CONTRIBUTING.md §"Running integration tests" (the per-test invocation pattern reused 5 more times)
  </read_first>
  <behavior>
    - Test 1: After this task completes, `grep -c "TODO(Phase-10)" tests/integration/full_round.rs` returns 0 (all 6 carve-outs unmuted).
    - Test 2: `BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round 2>&1 | grep -qE "test result: ok\. 8 passed; 0 failed; 0 ignored"` returns 0 exit — all 8 full_round tests pass locally.
    - Test 3: Each of the 5 newly-added commits carries the prescribed `test(13): unmute <fn> (Phase-10 carve-out N/6)` subject and a three-SHA body containing the per-test cargo invocation, the verdict line, `bitcoind --version | head -1`, and all three prerequisite SHAs (RSA + wallet-trust + Plan 13-01 wire-format).
    - Test 4 (escape-valve): if any of the 5 unmutes FAILS during its capture step, the executor halts WITHOUT deleting the #[ignore] line for that test (and without attempting any later unmute), and emits a CHECKPOINT REACHED marker per D-12. Pre-authorized in-flight scope expansion is ZERO. A 6th orthogonal blocker (i.e., any test failure that is NOT the same wire-format signature already cleared by Plan 13-01) triggers the same halt-and-surface protocol; Phase 14 absorbs per D-13.
    - Test 5 (single-file scope, per commit): each of the 5 commits in this task changes exactly one file (`tests/integration/full_round.rs`) with exactly one deleted line and zero added/modified lines (`git diff <commit>~1 <commit> --stat` shows `1 file changed, 0 insertions(+), 1 deletion(-)`).
  </behavior>
  <action>
    Execute the per-test cycle 5 times, in this exact order. The order is mandatory per D-05 (file order after the canonical-first test) and the commits must each be atomic (one #[ignore] removal per commit). Inherited verbatim from Plan 11-02-PLAN.md Task 2 modulo three-SHA body extension and `test(11):` → `test(13):` subject rewrite.

    Capture the three prerequisite SHAs ONCE before the loop (reuse from Task 1's variables if same session, otherwise re-capture):
      - `RSA_FIX_SHA=cc20f6fbca4d292bf7b394a3850b18d244b5b602`
      - `WALLET_TRUST_SHA=0bbcf3c76ca251c14aa64216ca6955be1f880b9a`
      - `WIRE_FORMAT_SHA=$(git log --grep="^fix(13): encode partial sig" --format=%H -1)`

    For EACH iteration where N is a shell loop variable taking values 2, 3, 4, 5, 6 (in that order) with the mapping:

      N=2 → test fn `blame_non_signer_timeout` (originally line 462, shifted to ~461 after Task 1's deletion)
      N=3 → test fn `adversarial_replay_token` (originally line 730)
      N=4 → test fn `adversarial_invalid_utxo` (originally line 854)
      N=5 → test fn `adversarial_wrong_denomination` (originally line 911)
      N=6 → test fn `round_restart_and_completion_after_blame` (originally line 1236)

    Per-iteration procedure (DO NOT short-circuit — each iteration is its own atomic commit):

    (a) Re-locate the current #[ignore] line for the test by name (T-11-13 line-drift discipline):
        `LINE=$(awk -v t="<test_fn>" '$0 ~ "#\\[ignore = \\\"TODO\\(Phase-10\\)" {ig=NR} $0 ~ "async fn "t"\\(\\)" {print ig; exit}' tests/integration/full_round.rs)`

        Verify LINE is a non-empty integer and that `sed -n "${LINE}p" tests/integration/full_round.rs` outputs a line beginning with `#[ignore = "TODO(Phase-10):`. If LINE is empty or the sed output does NOT begin with `#[ignore`, HALT — the file has drifted from the expected shape.

    (b) Run the per-test PASS-proof invocation byte-for-byte:
        `BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round::<test_fn> -- --ignored 2>&1 | tee target/integration-test-13-02-${N}.log`

        Capture the verdict line into shell variable `VERDICT_${N}`. Capture `bitcoind --version | head -1` output into `BITCOIND_VERSION_${N}` (CD-4 — recapture per iteration to detect a mid-session brew bump, though it should match BITCOIND_VERSION from Task 1).

    (c) If VERDICT_${N} is NOT a PASS, HALT IMMEDIATELY. Do NOT delete the #[ignore] line. Do NOT commit. Do NOT attempt any later iteration in this loop. Emit a CHECKPOINT REACHED marker with:
        - The test fn name, the iteration number (N/6), the failure mode (panic message / assertion / cargo verdict).
        - A proposed minimal repair scoped to the 6th orthogonal blocker.
        - The explicit note: "D-12 escape-valve invoked: 6th orthogonal blocker (or canonical happy-path follow-on failure) surfaced during unmute ${N}/6 — Phase 13 halts; Phase 14 absorbs per D-13. Earlier successful commits in this task remain bisect-clean. Task 3 (closeout) is NOT attempted — the closeout commit is conditional on 8/8 green per D-14. Pre-authorized in-flight scope expansion is ZERO."

    (d) On VERDICT_${N} = PASS: delete exactly line LINE from `tests/integration/full_round.rs`. The deleted line is the #[ignore] attribute. The line above (`#[tokio::test]`) and the line below (`async fn <test_fn>() {`) stay byte-identical. NO whitespace edits, NO comment substitution, NO test-body edits.

        Verify the edit's shape: `git diff tests/integration/full_round.rs --stat` reports `1 file changed, 0 insertions(+), 1 deletion(-)`. If any insertion shows, HALT — drive-by edit; revert via `git checkout -- tests/integration/full_round.rs` and re-attempt the targeted delete.

    (e) Stage and commit atomically using the Bash tool with a HEREDOC. Commit subject (exact string, with `<test_fn>` substituted): `test(13): unmute <test_fn> (Phase-10 carve-out N/6)`.

        Commit body (three-SHA CD-1 stamp per D-05, identical shape to Task 1's commit body — only the `<test_fn>` and VERDICT vary):

            cargo test --test integration full_round::<test_fn> -- --ignored
            <paste VERDICT_${N} verbatim>

            bitcoind --version | head -1: <paste BITCOIND_VERSION_${N} verbatim>

            Prereqs in history:
              RSA fix (Phase 11):           ${RSA_FIX_SHA}
              Wallet-trust fix (Phase 12):  ${WALLET_TRUST_SHA}
              Wire-format fix (Phase 13):   ${WIRE_FORMAT_SHA}

        Use a HEREDOC for the commit message. CD-2 form preservation: keep `$(brew --prefix)/bin/bitcoind` literal in the cargo invocation line.

    (f) Verify post-commit incrementally. The expected pass-count grows by one after each iteration: after the 2nd unmute (N=2) expect 4 passed / 4 ignored; after the 3rd (N=3) expect 5 passed / 3 ignored; …; after the 6th (N=6) expect 8 passed / 0 ignored. Compute the expected count via explicit shell arithmetic (N is the loop variable, NOT regex syntax) and grep for it:

        `EXPECTED=$((2 + N)) && BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round 2>&1 | grep -qE "test result: ok\. ${EXPECTED} passed"`

        If the count is wrong, HALT.

    After all 5 iterations complete (N=2 through N=6), run the final whole-file invocation from CONTRIBUTING.md verbatim:

      `BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round 2>&1 | tee target/integration-test-13-02-final.log`

    Capture the final verdict line into `FINAL_VERDICT` (used by Task 3's commit body). Expected: `test result: ok. 8 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out`. If anything else, the executor halts and reports — but this state should already be impossible if each iteration's step (b) PASSed.

    NO changes to REQUIREMENTS.md or ROADMAP.md in this task — Task 3's closeout commit handles both per D-14 (same-commit atomic doc reconciliation).
  </action>
  <verify>
    <automated>BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round 2>&1 | grep -qE "test result: ok\. 8 passed; 0 failed; 0 ignored" && [ "$(grep -c 'TODO(Phase-10)' tests/integration/full_round.rs)" = "0" ] && [ "$(git log --grep='^test(13): unmute .* (Phase-10 carve-out [2-6]/6)$' --oneline | wc -l | tr -d ' ')" = "5" ]</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "TODO(Phase-10)" tests/integration/full_round.rs` returns exactly 0 (all six #[ignore] attributes are gone).
    - Source assertion: each of the 6 test fns still exists by name — `grep -cE '^async fn (full_round_three_clients|blame_non_signer_timeout|adversarial_replay_token|adversarial_invalid_utxo|adversarial_wrong_denomination|round_restart_and_completion_after_blame)\(\)' tests/integration/full_round.rs` returns 6 (test bodies preserved, only attributes removed).
    - Source assertion: NO `#[ignore = "TODO(Phase-10)"]` markers remain anywhere in the file. Run `! grep -q 'TODO(Phase-10)' tests/integration/full_round.rs`.
    - Test command assertion: `BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round 2>&1 | grep -qE "test result: ok\. 8 passed; 0 failed; 0 ignored"` returns 0 exit. This is the REPAIR-01 local-green proof (D-04 / D-14).
    - Test command assertion (per-test, the 5 newly-unmuted ones): for each of `blame_non_signer_timeout`, `adversarial_replay_token`, `adversarial_invalid_utxo`, `adversarial_wrong_denomination`, `round_restart_and_completion_after_blame`, the invocation `BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round::<fn_name> 2>&1 | grep -qE "test result: ok\. 1 passed"` returns 0 exit (each individual test passes without `--ignored`).
    - CLI assertion (5 commits): `git log --grep='^test(13): unmute .* (Phase-10 carve-out [2-6]/6)$' --oneline | wc -l` returns exactly 5. Combined with Task 1's commit, total = 6 unmute commits.
    - CLI assertion (commit subjects, oldest first): `git log --grep='^test(13): unmute' --reverse --format=%s` outputs exactly these 6 lines in this order:
        `test(13): unmute full_round_three_clients (Phase-10 carve-out 1/6)`  (from Task 1)
        `test(13): unmute blame_non_signer_timeout (Phase-10 carve-out 2/6)`
        `test(13): unmute adversarial_replay_token (Phase-10 carve-out 3/6)`
        `test(13): unmute adversarial_invalid_utxo (Phase-10 carve-out 4/6)`
        `test(13): unmute adversarial_wrong_denomination (Phase-10 carve-out 5/6)`
        `test(13): unmute round_restart_and_completion_after_blame (Phase-10 carve-out 6/6)`
    - CLI assertion (per-commit body, three-SHA): for each of the 5 commits in this task, `git log -1 --format=%b <commit>` contains: `cargo test --test integration full_round::<corresponding_fn>`, `test result: ok. 1 passed`, `bitcoind --version`, `Bitcoin Core daemon version v`, `cc20f6fbca4d292bf7b394a3850b18d244b5b602`, `0bbcf3c76ca251c14aa64216ca6955be1f880b9a`, AND a 40-char hex SHA matching `^[0-9a-f]{40}$` on the `Wire-format fix (Phase 13):` line — same SHA as Task 1's commit (verify via `git log --grep='^test(13): unmute' --format=%b | grep -oE 'Wire-format fix \(Phase 13\): +[0-9a-f]{40}' | sort -u | wc -l` returns exactly 1 — same SHA across all six unmute commits).
    - Source assertion (per commit, single-line delete): each of the 5 commits in this task changes exactly one file (`tests/integration/full_round.rs`) with exactly one deleted line and zero added/modified lines. Verify across all 5 with: `for COMMIT in $(git log --grep='^test(13): unmute .* (Phase-10 carve-out [2-6]/6)$' --format=%H); do git diff ${COMMIT}~1 ${COMMIT} --shortstat | grep -qE '1 file changed,.*1 deletion' || exit 1; done` exits 0.
    - Boundary preservation assertion: NO commits in this task modify `tests/integration/mod.rs`, `client/src/wallet.rs` (that was Plan 13-01's surface), `coordinator/src/round/signing.rs`, `coordinator/src/api/handlers.rs`, `coordinator/src/round/manager.rs`, `coordinator/src/blind/rsa.rs`, `shared/src/protocol.rs`, `.github/workflows/ci.yml`, `CONTRIBUTING.md`, `.planning/REQUIREMENTS.md` (that's Task 3's surface), `.planning/ROADMAP.md` (that's Task 3's surface), or any Cargo.toml. Verify with `git diff HEAD~5..HEAD --name-only` showing ONLY `tests/integration/full_round.rs` (the 5 Task-2 commits cover this range; Task 1 is in HEAD~6..HEAD~5; Plan 13-01 is in HEAD~7..HEAD~6 — OUT OF SCOPE for this assertion).
    - Halt assertion (negative test, D-12): if any iteration's step (b) test FAILED, no commit was made for that iteration, the corresponding #[ignore] line is still present, all later iterations in this task were skipped, Task 3 was NOT attempted, and the executor has emitted a CHECKPOINT REACHED marker citing D-12 and naming the failing test + the proposed minimal repair. Earlier successful commits in this task remain bisect-clean.
  </acceptance_criteria>
  <done>
    All 6 #[ignore] attributes are gone from `tests/integration/full_round.rs`; `cargo test --test integration full_round` reports 8 passed / 0 failed / 0 ignored locally against pinned brew bitcoind v31; 5 atomic commits from this task plus Task 1's commit total 6 bisectable unmute commits with the prescribed `test(13):` subjects and three-SHA bodies (RSA + wallet-trust + Plan 13-01 wire-format SHAs in every body); no boundary file modified (REQUIREMENTS.md + ROADMAP.md untouched — Task 3's surface per D-14); FINAL_VERDICT captured for Task 3's commit-body consumption. If the executor halted via D-12 on any iteration, earlier commits remain bisect-clean, a CHECKPOINT REACHED marker has been emitted, Task 3 is NOT attempted, and Phase 14 absorbs the 6th orthogonal blocker per D-13.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Closeout commit — D-14 same-commit reconciliation of REQUIREMENTS.md (REPAIR-01 Traceability row) + ROADMAP.md Phase 13 entry</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/13-client-src-wallet-rs-wire-format-fix-plan-12-02-unmute-cycle/13-CONTEXT.md (D-04 Phase 13 owns REPAIR-01 closure, D-14 closeout doc reconciliation with conditional disposition, D-15 v1.3 ship notes out of scope)
    - .planning/phases/13-client-src-wallet-rs-wire-format-fix-plan-12-02-unmute-cycle/13-PATTERNS.md §".planning/REQUIREMENTS.md line 20" (lines 265-282 — the doc-drift reconciliation pattern; current `[x]` state despite never-green suite) AND §".planning/ROADMAP.md Phase 13 entry" (lines 286-309 — the same-commit coupling pattern from Phase 12 D-13)
    - .planning/REQUIREMENTS.md line 20 (current REPAIR-01 status — verify it currently shows `[x]` per the documented doc drift) AND lines 47-54 (Traceability table — REPAIR-01 row currently shows `Phase 10 | TBD | active`)
    - .planning/ROADMAP.md §"Phase 13" lines 139-147 (current Phase 13 entry — `**Goal:** [To be planned]`, `**Plans:** 0 plans`, `Plans:` list with `[ ] TBD`; verify this is the state) AND Progress table lines 96-107 (Phase 13 row to update or append)
    - .planning/phases/12-repair-client-src-wallet-rs-260-bdk-wallet-2-3-signoptions-r/12-CONTEXT.md D-13 (the per-row bookkeeping convention this commit inherits)
  </read_first>
  <behavior>
    - Test 1 (precondition): Task 2 completed without HALT and the final whole-file invocation produced `test result: ok. 8 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out`. If Task 2 HALTed (any unmute failed), this Task 3 is NOT executed; the closeout commit is conditional on 8/8 green per D-14. The executor MUST verify this by re-running the whole-file invocation BEFORE editing any doc file.
    - Test 2 (REQUIREMENTS.md edit, GREEN case): the Traceability table row for REPAIR-01 is updated from `| REPAIR-01 | Phase 10 | TBD | active |` to `| REPAIR-01 | Phase 13 | 13-01, 13-02 | closed |`. Line 20 (the `- [x] **REPAIR-01**:` requirement line) stays `[x]` — the requirement actually closes; only the Traceability row's phase attribution + status is the load-bearing edit.
    - Test 3 (REQUIREMENTS.md edit, RED case — should NOT trigger if Task 2 PASSed all 6): if for any reason Task 2 had a HALT-recovery that was then user-overridden to proceed (unusual), line 20 starts with `- [ ] **REPAIR-01**:` (status flips to `[ ]`) AND the Traceability row stays `active`.
    - Test 4 (ROADMAP.md edit, GREEN case): the Phase 13 entry's `**Goal:**` line is replaced from `[To be planned]` to a concrete one-line summary; `**Plans:**` count is updated from `0` to `2`; the `Plans:` list replaces `- [ ] TBD (run /gsd-plan-phase 13 to break down)` with two completed `[x]` entries naming the two PLAN files (`13-01-PLAN.md` and `13-02-PLAN.md`) under Wave 1 / Wave 2 headers; the Progress table for Phase 13 (lines 96-107) is updated with `2/2`, `Complete`, and the actual completion date (e.g., `2026-05-28`).
    - Test 5 (single atomic commit): both files (`.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`) land in ONE commit. `git diff HEAD~1 HEAD --name-only` returns exactly two lines, both starting with `.planning/`.
    - Test 6 (commit-body provenance per D-14): the commit body cites the final whole-file PASS verdict, the six unmute commit SHAs (canonical-first then file order), the three prerequisite SHAs (RSA + wallet-trust + Plan 13-01 wire-format), and the conditional D-14 disposition ("8/8 green → REPAIR-01 stays [x], Phase 13 marked complete").
    - Test 7 (boundary preservation): NO code files are modified by this commit. NO Cargo.toml, NO test files, NO source files. Only the two `.planning/*.md` files.
  </behavior>
  <action>
    Execute the closeout doc reconciliation in this exact order. This task ONLY runs if Task 2 completed all 6 unmutes without HALT. If Task 2 HALTed, this task is NOT executed; Phase 14 absorbs per D-12 / D-13 and the closeout commit will be made by Phase 14's wrap-up.

    (a) Precondition verification: re-run the whole-file invocation BEFORE any edit to confirm 8/8 green:
        `BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round 2>&1 | tee target/integration-test-13-02-closeout-verify.log`
        Capture the verdict line into `CLOSEOUT_VERDICT`. If `CLOSEOUT_VERDICT` is NOT `test result: ok. 8 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out`, HALT — Task 3 cannot proceed against a non-green suite (D-14 RED-case is reserved for the documented edge where a user overrides a HALT to proceed; the default flow is green-only).

    (b) Capture provenance variables (used in the commit body):
        - `RSA_FIX_SHA=cc20f6fbca4d292bf7b394a3850b18d244b5b602`
        - `WALLET_TRUST_SHA=0bbcf3c76ca251c14aa64216ca6955be1f880b9a`
        - `WIRE_FORMAT_SHA=$(git log --grep="^fix(13): encode partial sig" --format=%H -1)`
        - `UNMUTE_SHAS=$(git log --grep='^test(13): unmute' --reverse --format='%H %s')` — captures all 6 unmute commit SHAs with their subjects, oldest-first (canonical-first then file order). Verify the count is exactly 6 via `echo "${UNMUTE_SHAS}" | wc -l`; if not 6, HALT.
        - `COMPLETION_DATE=$(date +%Y-%m-%d)` — used in the ROADMAP.md Progress table edit.

    (c) Edit `.planning/REQUIREMENTS.md` Traceability table (lines 47-54). Use the Edit tool to replace exactly the line:
            `| REPAIR-01 | Phase 10 | TBD | active |`
        with:
            `| REPAIR-01 | Phase 13 | 13-01, 13-02 | closed |`
        This is the load-bearing edit in the GREEN case — corrects both the phase attribution (was Phase 10, actually closed in Phase 13) and the status (active → closed). Line 20 (`- [x] **REPAIR-01**: ...`) stays untouched (the closure is now real).

        (RED case, only if user override: instead of the above, edit line 20 changing `- [x] **REPAIR-01**:` to `- [ ] **REPAIR-01**:` AND leave the Traceability row at `active`. This branch is not the default flow and requires a user-override CHECKPOINT decision.)

    (d) Edit `.planning/ROADMAP.md` Phase 13 entry (lines 139-147). Use the Edit tool to replace the entire block:

            ### Phase 13: client/src/wallet.rs wire-format fix + Plan 12-02 unmute cycle re-execution

            **Goal:** [To be planned]
            **Requirements**: TBD
            **Depends on:** Phase 12
            **Plans:** 0 plans

            Plans:
            - [ ] TBD (run /gsd-plan-phase 13 to break down)

        WITH (the same 8-line header replaced; planner-discretion phrasing on the Goal text but keep the section's structural shape):

            ### Phase 13: client/src/wallet.rs wire-format fix + Plan 12-02 unmute cycle re-execution

            **Goal:** Close REPAIR-01 locally by fixing the client wire-format encoder at `client/src/wallet.rs:279-281` (raw DER → consensus-serialized `bitcoin::Witness`) and re-executing the six-test unmute cycle from Plan 12-02 verbatim against pinned brew bitcoind v31. All 8 `full_round::*` tests pass.
            **Requirements**: REPAIR-01 (closed locally — see Plan 13-02 closeout commit; REPAIR-02 closure remains tied to combined Phase 11+12+13 PR observation per Phase 11 D-11)
            **Depends on:** Phase 12
            **Plans:** 2/2 plans executed

            Plans:
            **Wave 1**

            - [x] 13-01-PLAN.md — wire-format fix (client/src/wallet.rs: raw DER → bitcoin::Witness consensus serialization) — single atomic commit with D-07 canonical-first PASS-proof in body

            **Wave 2** *(blocked on Wave 1 completion)*

            - [x] 13-02-PLAN.md — six-test unmute cycle (canonical-first then file order; three-SHA CD-1 bodies; D-14 closeout doc reconciliation)

        Also update the Progress table (lines 96-107) — append a new row for Phase 13 if not present, OR update an existing Phase 13 placeholder row. The new row (use ${COMPLETION_DATE} for the date column):

            | 13. client/src/wallet.rs wire-format fix + Plan 12-02 unmute cycle re-execution | v1.3 | 2/2 | Complete | ${COMPLETION_DATE} |

    (e) Verify the edits BEFORE staging: `git diff .planning/REQUIREMENTS.md` shows the Traceability row flip (or the line-20 flip in RED case); `git diff .planning/ROADMAP.md` shows the Phase 13 entry replacement + Progress table update. `git diff --name-only` shows EXACTLY two files. NO other file is touched.

    (f) Stage and commit atomically using the Bash tool with a HEREDOC:
        `git add .planning/REQUIREMENTS.md .planning/ROADMAP.md`
        Verify staging: `git diff --staged --name-only` outputs exactly two lines, both `.planning/*.md`.

        Commit subject (GREEN case, exact string for verify-grep):
            `docs(13): reconcile REPAIR-01 status + close Phase 13 entry (8/8 full_round tests green)`

        Commit body (D-14 provenance per 13-PATTERNS.md §".planning/REQUIREMENTS.md" lines 277-280):

            Final whole-file verdict:
            <paste CLOSEOUT_VERDICT verbatim — e.g. "test result: ok. 8 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out">

            Six unmute commits (canonical-first then file order):
            <paste UNMUTE_SHAS verbatim — six lines of "SHA test(13): unmute <fn> (Phase-10 carve-out N/6)">

            Prereqs in history:
              RSA fix (Phase 11):           ${RSA_FIX_SHA}
              Wallet-trust fix (Phase 12):  ${WALLET_TRUST_SHA}
              Wire-format fix (Phase 13):   ${WIRE_FORMAT_SHA}

            D-14 disposition: 8/8 full_round tests green locally against pinned brew bitcoind v31 → REPAIR-01 stays [x] (closure is now real; provenance corrected from Phase 10 to Phase 13 attribution in REQUIREMENTS.md Traceability table). ROADMAP.md Phase 13 entry marked complete. REPAIR-02 closure stays tied to PR observation per Phase 11 D-11 / Phase 12 D-13 (NOT self-attested by this commit).

            D-15: v1.3 ship notes and /gsd-complete-milestone v1.3 are OUT OF SCOPE for Phase 13 — handled by a wrap-up phase or /gsd-ship after CI confirms the combined Phase 11+12+13 PR is green.

        CD-2 form preservation: do NOT inline the resolved bitcoind path. CD-4: the final whole-file PASS verdict already implicitly attests bitcoind version via Task 2's six commit bodies (each contains `bitcoind --version | head -1`).

    (g) Verify post-commit: `git log -1 --format=%s` matches the closeout subject; `git log -1 --format=%b` contains the literal strings `Final whole-file verdict`, `Six unmute commits`, `Prereqs in history`, all three prerequisite SHAs, and the `D-14 disposition` line. `git diff HEAD~1 HEAD --name-only` outputs exactly two lines: `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` (in either order). NO other file is touched.

    NO code-file changes in this task. NO Cargo.toml edits. NO test-file edits. NO CONTRIBUTING.md edits. NO STATE.md edits (the STATE.md update is /gsd-execute-phase's wrap-up concern, per project convention). D-15 v1.3 ship notes and `/gsd-complete-milestone v1.3` are explicitly OUT OF SCOPE.
  </action>
  <verify>
    <automated>BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round 2>&1 | grep -qE "test result: ok\. 8 passed; 0 failed; 0 ignored" && git log -1 --format=%s | grep -qxF "docs(13): reconcile REPAIR-01 status + close Phase 13 entry (8/8 full_round tests green)" && [ "$(git diff HEAD~1 HEAD --name-only | wc -l | tr -d ' ')" = "2" ] && git diff HEAD~1 HEAD --name-only | sort | paste -sd' ' - | grep -qF ".planning/REQUIREMENTS.md .planning/ROADMAP.md" && git log -1 --format=%b | grep -qF "cc20f6fbca4d292bf7b394a3850b18d244b5b602" && git log -1 --format=%b | grep -qF "0bbcf3c76ca251c14aa64216ca6955be1f880b9a" && git log -1 --format=%b | grep -qF "D-14 disposition"</automated>
  </verify>
  <acceptance_criteria>
    - Precondition assertion: the final whole-file invocation (executed in step a, BEFORE any edit) returned `test result: ok. 8 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out`. Re-verify: `BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round 2>&1 | grep -qE 'test result: ok\. 8 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out'`.
    - Source assertion (REQUIREMENTS.md Traceability row, GREEN case): `grep -qE '^\| REPAIR-01 \| Phase 13 \| 13-01, 13-02 \| closed \|$' .planning/REQUIREMENTS.md` (the row is flipped). AND: `! grep -qE '^\| REPAIR-01 \| Phase 10 \| TBD \| active \|$' .planning/REQUIREMENTS.md` (the old row is gone).
    - Source assertion (REQUIREMENTS.md line 20 unchanged in GREEN case): `sed -n '20p' .planning/REQUIREMENTS.md | grep -qF '- [x] **REPAIR-01**:'` (the requirement line still shows `[x]` — closure is real, not a flip-back).
    - Source assertion (ROADMAP.md Phase 13 entry): `grep -qF '13-01-PLAN.md' .planning/ROADMAP.md` AND `grep -qF '13-02-PLAN.md' .planning/ROADMAP.md` AND `grep -qF '**Plans:** 2/2 plans executed' .planning/ROADMAP.md` AND `! grep -qF '[To be planned]' .planning/ROADMAP.md` (the placeholder is gone; if a different `[To be planned]` exists for an unrelated phase, tighten the negative grep to a Phase-13-scoped range via `awk '/^### Phase 13:/,/^### Phase 14:/' .planning/ROADMAP.md | grep -qF '[To be planned]'` returning false).
    - Source assertion (ROADMAP.md Progress table): the Phase 13 row in the Progress table (lines ~96-107) reports `2/2` plans, `Complete` status, and a completion date matching `^[0-9]{4}-[0-9]{2}-[0-9]{2}$`. Run `grep -qE '^\| 13\. .* \| v1\.3 \| 2/2 \| Complete \| [0-9]{4}-[0-9]{2}-[0-9]{2} \|$' .planning/ROADMAP.md`.
    - Single-commit two-file assertion: `git diff HEAD~1 HEAD --name-only` outputs exactly two lines, sorted = `.planning/REQUIREMENTS.md` `.planning/ROADMAP.md`. Run `[ "$(git diff HEAD~1 HEAD --name-only | wc -l | tr -d ' ')" = "2" ]` AND `git diff HEAD~1 HEAD --name-only | sort | paste -sd' ' - | grep -qxF '.planning/REQUIREMENTS.md .planning/ROADMAP.md'`.
    - CLI assertion (commit subject, GREEN case): `git log -1 --format=%s | grep -qxF 'docs(13): reconcile REPAIR-01 status + close Phase 13 entry (8/8 full_round tests green)'`.
    - CLI assertion (commit body provenance): `git log -1 --format=%b` contains all of: `Final whole-file verdict`, `test result: ok. 8 passed; 0 failed; 0 ignored`, `Six unmute commits`, six 40-char SHAs (verify with `git log -1 --format=%b | grep -oE '\b[0-9a-f]{40}\b' | wc -l` returning at least 9 — six unmute SHAs + three prerequisite SHAs), `Prereqs in history`, `cc20f6fbca4d292bf7b394a3850b18d244b5b602`, `0bbcf3c76ca251c14aa64216ca6955be1f880b9a`, the Plan-13-01 wire-format SHA (matching `^[0-9a-f]{40}$`), `D-14 disposition`, `REPAIR-02 closure stays tied to PR observation`, `D-15`, `OUT OF SCOPE`.
    - Boundary preservation assertion: NO code file is modified by this commit. Run `git diff HEAD~1 HEAD -- '*.rs' '*.toml' '*.yml' '*.lock' CONTRIBUTING.md tests/ client/ coordinator/ shared/` and confirm empty output. Only the two `.planning/*.md` files are touched.
    - Halt-recovery assertion (negative test, GREEN-only flow): if precondition (step a) returned non-8/8-green, no commit exists matching the closeout subject, neither .planning file is modified, and the executor has emitted a CHECKPOINT REACHED marker citing D-14 + Task 2's HALT state. This is a recovery state — Phase 14 absorbs.
  </acceptance_criteria>
  <done>
    `.planning/REQUIREMENTS.md` Traceability table REPAIR-01 row reads `| REPAIR-01 | Phase 13 | 13-01, 13-02 | closed |`; line 20 stays `- [x] **REPAIR-01**:` (closure is real, not flipped). `.planning/ROADMAP.md` Phase 13 entry has a concrete Goal, `**Plans:** 2/2 plans executed`, two `[x]` PLAN entries under Wave 1 / Wave 2, and a Progress-table row marked `2/2 | Complete | <date>`. ONE atomic commit touches exactly these two files with subject `docs(13): reconcile REPAIR-01 status + close Phase 13 entry (8/8 full_round tests green)` and a body containing the final whole-file PASS verdict, all six unmute commit SHAs, the three prerequisite SHAs (RSA + wallet-trust + Plan 13-01 wire-format), the D-14 disposition statement, and the D-15 out-of-scope note. No code file is modified. If the GREEN precondition was not satisfied, no commit was made; Phase 14 absorbs.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner ↔ pinned brew bitcoind v31 | Local cargo test process spawns a real bitcoind via `corepc-node`. The trust boundary is the version-pin captured in each unmute commit body via `bitcoind --version | head -1` (CD-4). CI runs against `.bitcoind-version`-pinned v30.2 (Phase 9 asymmetry — inherited unchanged); the v30/v31 divergence is caught by REPAIR-02 PR observation per Phase 11 D-11. |
| Plan 13-01 wire-format fix + Plan 11/12 prior fixes ↔ Plan 13-02 unmute proofs | Each unmute commit body references three prerequisite SHAs (RSA `cc20f6f`, wallet-trust `0bbcf3c`, Plan 13-01 wire-format). The integrity of these references rests on `depends_on: [13-01]` + the static SHA verification in Task 1 step (a). The Plan 13-01 commit must be in history before any Plan 13-02 unmute commit. |
| Plan 13-02 unmute commits ↔ REQUIREMENTS.md REPAIR-01 line-20 truth | Pre-Phase-13, REPAIR-01 was shown `[x]` despite the suite never having been green (documented doc drift in CONTEXT.md D-14). The closeout commit (Task 3) reconciles this drift via the Traceability table flip from `Phase 10 | TBD | active` to `Phase 13 | 13-01, 13-02 | closed` — line 20 stays `[x]` in the GREEN case (real closure) or flips to `[ ]` in the RED case (override). The Task 3 precondition gate (re-run whole-file invocation BEFORE editing) prevents the doc from being out of sync with the suite. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02 | Tampering | Drive-by edits in unmute commits could regress unrelated tests or smuggle privacy regressions (e.g., a test-body edit that weakens an assertion, a logging-of-input-output-mapping added under cover of "unmute") | mitigate | Per-task acceptance criteria assert `git diff <commit>~1 <commit> --stat` shows `1 file changed, 0 insertions(+), 1 deletion(-)` for each of the six unmute commits (Task 1's commit + Task 2's five commits). A test-body edit would surface as `+N insertions(+)` and fail acceptance. D-07 from Plan 11-02 inherited verbatim: "no drive-by edits to other tests, helpers, or unrelated code." The boundary-preservation grep in Task 2's acceptance criteria explicitly excludes `tests/integration/mod.rs`, `client/src/wallet.rs`, `coordinator/src/round/signing.rs`, etc. |
| T-13-03 | Spoofing | Brew bitcoind silently bumped off v31 mid-session (e.g., a `brew upgrade` between Task 1 and Task 2) → unmute commits attest PASS against different consensus rules than CI's pinned v30.2 | mitigate | CD-4 captures `bitcoind --version | head -1` in EVERY unmute commit body (six captures across Task 1 + Task 2, plus the Plan 13-01 capture). The per-commit `BITCOIND_VERSION_${N}` re-capture in Task 2 step (b) detects a mid-session bump. The asymmetry between brew v31 (local-dev) and CI's pinned v30.2 is a known Phase 9 inheritance; REPAIR-02 (PR observation per Phase 11 D-11 / Phase 12 D-13) is the cross-check that catches a v30/v31 divergence affecting the integration suite. |
| T-13-04 | Repudiation / Integrity | REQUIREMENTS.md line 20 falsely flipped to `[x]` when the full_round suite is red → loss of REPAIR-01 truth; OR the Traceability row attribution falsely names Phase 10 instead of Phase 13 → loss of provenance | mitigate | D-14 explicit conditional: "if any test red, REPAIR-01 flips back to `[ ]`"; Task 3's precondition gate (step a) re-runs the whole-file invocation BEFORE editing — Task 3 cannot proceed against a non-green suite in the default flow. The Traceability row edit (`Phase 10 | TBD | active` → `Phase 13 | 13-01, 13-02 | closed`) is the load-bearing provenance correction. RED case (`- [x]` → `- [ ]` on line 20) is reserved for user-override CHECKPOINT scenarios, not the default flow. The closeout commit body cites all six unmute SHAs + three prerequisite SHAs, so the truth of the `[x]` status is reproducible via `git bisect` / commit-body inspection. |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | Plan 13-02 introduces ZERO new dependencies. No `Cargo.toml` edit, no `cargo add`, no `cargo install`. Tasks 1 + 2 only delete `#[ignore]` attributes from a test file; Task 3 only edits two `.planning/*.md` files. No package-legitimacy gate required. |

None of these threats reach `high` severity individually. The drive-by-edit risk (T-13-02) is structurally mitigated by per-commit `1 deletion` assertions; the brew-version risk (T-13-03) is documented per commit; the doc-drift risk (T-13-04) is gated by Task 3's precondition re-run + the explicit Traceability provenance correction. Plan 13-02 ships under ASVS L1 with this register; no threat blocks merging.
</threat_model>

<verification>
After all three tasks complete (and assuming no HALT was triggered in Tasks 1 or 2):

1. `BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round 2>&1 | tee target/integration-test-13-02-final-verify.log` reports `test result: ok. 8 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out`. This is the REPAIR-01 local-green proof (D-04 / D-14).
2. `grep -c "TODO(Phase-10)" tests/integration/full_round.rs` returns 0 (all six #[ignore] attributes are gone).
3. `git log --grep='^test(13): unmute' --oneline | wc -l` returns exactly 6 (six unmute commits land in canonical-first then file order).
4. `git log --grep='^test(13): unmute' --reverse --format=%s` outputs the six prescribed commit subjects in order: `test(13): unmute full_round_three_clients (Phase-10 carve-out 1/6)`, `test(13): unmute blame_non_signer_timeout (Phase-10 carve-out 2/6)`, `test(13): unmute adversarial_replay_token (Phase-10 carve-out 3/6)`, `test(13): unmute adversarial_invalid_utxo (Phase-10 carve-out 4/6)`, `test(13): unmute adversarial_wrong_denomination (Phase-10 carve-out 5/6)`, `test(13): unmute round_restart_and_completion_after_blame (Phase-10 carve-out 6/6)`.
5. Each of the 6 commit bodies contains the per-test cargo invocation, the `test result: ok. 1 passed` verdict line, `bitcoind --version | head -1` output, AND all three prerequisite SHAs (RSA `cc20f6f...`, wallet-trust `0bbcf3c...`, Plan 13-01 wire-format SHA — same SHA across all six bodies, verified via `git log --grep='^test(13): unmute' --format=%b | grep -oE 'Wire-format fix \(Phase 13\): +[0-9a-f]{40}' | sort -u | wc -l` returning 1).
6. `git log -1 --format=%s` matches the closeout subject `docs(13): reconcile REPAIR-01 status + close Phase 13 entry (8/8 full_round tests green)`; `git log -1 --format=%b` contains the final whole-file PASS verdict, all six unmute commit SHAs, all three prerequisite SHAs, the D-14 disposition, and the D-15 out-of-scope note.
7. `.planning/REQUIREMENTS.md` Traceability table REPAIR-01 row reads `| REPAIR-01 | Phase 13 | 13-01, 13-02 | closed |`; line 20 stays `- [x] **REPAIR-01**:`.
8. `.planning/ROADMAP.md` Phase 13 entry has a concrete Goal, `**Plans:** 2/2 plans executed`, two `[x]` PLAN entries (`13-01-PLAN.md` Wave 1, `13-02-PLAN.md` Wave 2), and a Progress-table row marked `2/2 | Complete | <date>`.
9. NO code file (`*.rs`, `*.toml`, `*.yml`, `*.lock`, `CONTRIBUTING.md`, `tests/integration/mod.rs`) was modified by any Plan 13-02 commit — verified via `git diff <Plan-13-02-base>..HEAD -- '*.rs' '*.toml' '*.yml' '*.lock' CONTRIBUTING.md tests/integration/mod.rs` returning empty. Only `tests/integration/full_round.rs` (six unmute commits) + `.planning/REQUIREMENTS.md` + `.planning/ROADMAP.md` (closeout commit) are touched.
10. REQUIREMENTS.md REPAIR-02 status is UNCHANGED — still `[ ]` on line 21. Closure stays tied to PR observation per Phase 11 D-11 / Phase 12 D-13.

If a HALT was triggered during Task 1 or Task 2:
- A CHECKPOINT REACHED marker exists in the conversation log citing D-12, naming the failing test, the failure mode, the iteration number (N/6), and a proposed minimal repair.
- All commits made before the HALT remain bisect-clean.
- Task 3 was NOT attempted — the closeout commit is conditional on 8/8 green per D-14.
- REPAIR-01 is NOT closed; Phase 14 (D-13) absorbs the 6th orthogonal blocker and the remaining unmutes + closeout.
- REQUIREMENTS.md + ROADMAP.md remain UNCHANGED — no doc drift introduced by a halted Plan 13-02.
</verification>

<success_criteria>
- [ ] All 6 `#[ignore = "TODO(Phase-10): ..."]` attribute lines removed from `tests/integration/full_round.rs` (one per commit, six atomic commits, one deletion each).
- [ ] Commit order is canonical-first then file order (D-05): full_round_three_clients (1/6) → blame_non_signer_timeout (2/6) → adversarial_replay_token (3/6) → adversarial_invalid_utxo (4/6) → adversarial_wrong_denomination (5/6) → round_restart_and_completion_after_blame (6/6).
- [ ] `BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round` reports `test result: ok. 8 passed; 0 failed; 0 ignored` locally against pinned brew bitcoind v31.
- [ ] Each of the 6 unmute commits has a three-SHA CD-1 body: per-test cargo invocation + verdict line + `bitcoind --version | head -1` output + Phase 11 RSA fix SHA (`cc20f6fbca4d292bf7b394a3850b18d244b5b602`) + Phase 12 wallet-trust SHA (`0bbcf3c76ca251c14aa64216ca6955be1f880b9a`) + Phase 13 Plan 13-01 wire-format SHA (same across all six bodies).
- [ ] Each unmute commit's `git diff <commit>~1 <commit> --stat` is `1 file changed, 0 insertions(+), 1 deletion(-)` (no drive-by edits, no test-body changes, no whitespace edits — D-07 from Plan 11-02 inherited; T-11-13 line-drift discipline applied per iteration).
- [ ] D-12 escape-valve gate honored: any per-test failure HALTs and surfaces a CHECKPOINT REACHED marker; earlier successful commits remain bisect-clean; pre-authorized in-flight scope expansion is ZERO; Phase 14 absorbs per D-13.
- [ ] Task 3 closeout commit lands ONLY in the 8/8-green case (default flow). The commit touches exactly two files (`.planning/REQUIREMENTS.md` + `.planning/ROADMAP.md`) in ONE atomic commit (per Phase 12 D-13 same-commit convention extended by D-14).
- [ ] REQUIREMENTS.md Traceability table REPAIR-01 row flipped from `Phase 10 | TBD | active` to `Phase 13 | 13-01, 13-02 | closed`. Line 20 stays `- [x] **REPAIR-01**:` (closure is real).
- [ ] ROADMAP.md Phase 13 entry replaced (concrete Goal, `**Plans:** 2/2 plans executed`, two `[x]` PLAN entries under Wave 1 / Wave 2) + Progress table row updated (`2/2 | Complete | <date>`).
- [ ] Closeout commit subject: `docs(13): reconcile REPAIR-01 status + close Phase 13 entry (8/8 full_round tests green)` (GREEN case default). Commit body cites the final whole-file PASS verdict, all six unmute commit SHAs, all three prerequisite SHAs, the D-14 disposition, and the D-15 out-of-scope note.
- [ ] No code file modified by Plan 13-02 (no `*.rs`, no `*.toml`, no `*.yml`, no CONTRIBUTING.md, no `tests/integration/mod.rs`). Only `tests/integration/full_round.rs` + the two `.planning/*.md` files.
- [ ] REPAIR-02 status UNCHANGED on REQUIREMENTS.md line 21 (still `[ ]`) — closure stays tied to combined Phase 11+12+13 PR observation per Phase 11 D-11 / Phase 12 D-13, NOT self-attested in-phase.
- [ ] D-15: v1.3 ship notes and `/gsd-complete-milestone v1.3` are explicitly OUT OF SCOPE — handled by a wrap-up phase or `/gsd-ship` after CI confirms the combined Phase 11+12+13 PR.
</success_criteria>

<output>
Create `.planning/phases/13-client-src-wallet-rs-wire-format-fix-plan-12-02-unmute-cycle/13-02-SUMMARY.md` when done. The summary should record:
- The six unmute commit SHAs in canonical-first then file order (verbatim from `git log --grep='^test(13): unmute' --reverse --format='%H %s'`).
- The closeout commit SHA (from `git log --grep='^docs(13): reconcile REPAIR-01' --format=%H -1`).
- The final whole-file PASS verdict (`test result: ok. 8 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out`) and the captured `bitcoind --version | head -1` output (CD-4).
- Confirmation that REPAIR-01 is satisfied locally (D-04 / D-14) — and that this is the closure trigger that flipped the REQUIREMENTS.md Traceability row from `Phase 10 | TBD | active` to `Phase 13 | 13-01, 13-02 | closed`.
- Explicit note that REPAIR-02 closure is DEFERRED to PR observation per Phase 11 D-11 / Phase 12 D-13 — the wrap-up phase or `/gsd-ship` flips it.
- If any HALT was triggered: the CHECKPOINT REACHED context (which test, which failure mode, which iteration N/6), and an explicit pointer to where Phase 14 should pick up (D-13). In the HALT case, NO closeout commit was made and REQUIREMENTS.md + ROADMAP.md remain UNCHANGED.
- No ship-notes work performed (D-15 — handled by a wrap-up phase or `/gsd-ship` after CI confirms green).
</output>
