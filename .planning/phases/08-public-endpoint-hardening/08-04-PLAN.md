---
phase: 08-public-endpoint-hardening
plan: 04
type: execute
wave: 3
depends_on: [08-02, 08-03]
files_modified:
  - tests/integration/rate_limiting.rs
  - tests/integration/mod.rs
autonomous: true
requirements: []
tags: [integration-test, rate-limit, timeout, connection-cap, regression-guard]

must_haves:
  truths:
    - "D-01: this test asserts DoS-mitigation behavior only — HTTP 429 + `Retry-After` (volume), HTTP 408 (slow client). It makes NO sybil-resistance claim. Sybil resistance remains BIP-322 ownership proofs at input registration, untouched. The SUMMARY.md emitted by Task 2 repeats this framing verbatim so the PR description matches CONTEXT D-01 and reviewers do not conflate DoS mitigation with sybil prevention."
    - "D-06: an in-process `coordinator::run(cfg)` integration test exists at `tests/integration/rate_limiting.rs` that proves the Phase 8 mitigations end-to-end."
    - "D-02 + D-03: the test floods `/info` past `rate_limit_info_per_min` (set TIGHT — e.g. 3 rpm) and asserts at least one response has HTTP 429 status AND a `retry-after` header (verifies tower_governor wired with GlobalKeyExtractor returns the right response shape, not 500)."
    - "D-04 (timeout) + RESEARCH Pitfall 4 (slow-body): the test MUST implement a `request_timeout_returns_408` test fn — set `request_timeout_secs = 1`, fire a request against a route whose handler blocks past 1s, assert the response status is HTTP 408 REQUEST_TIMEOUT. The test may be `#[ignore]`d ONLY if marked `#[ignore = \"requires slow-handler harness; run via --include-ignored\"]` and re-enabled in CI via `--include-ignored`; default-run skip is forbidden."
    - "RESEARCH §\"Open Questions\" Q3 resolution: the connection-cap end-to-end assertion is DEFERRED per A4 — clearnet-only test infra cannot meaningfully exercise the tor-only `max_concurrent_connections` semaphore from Plan 03. Coverage stands via Plan 03 grep audits (the cap is enforced before `axum::serve` runs, so the clearnet path is by design uncapped). A clear `// TODO` comment in the test file records the deferral; this is the explicit Q3 answer."
    - "Test registered in `tests/integration/mod.rs` (otherwise it does not compile — PATTERNS hard requirement)."
    - "Test follows the existing graceful-skip-if-no-bitcoind pattern from `tests/integration/round_bootstrap.rs:45-54`."
    - "Test cleans up with `run_handle.abort()` before returning (PATTERNS recurring pattern)."
  artifacts:
    - path: "tests/integration/rate_limiting.rs"
      provides: "End-to-end assertion of Phase 8 mitigations (429 + Retry-After mandatory; 408 timeout mandatory; connection cap deferred per A4)"
      contains: "TOO_MANY_REQUESTS"
      min_lines: 80
    - path: "tests/integration/mod.rs"
      provides: "Module registration for rate_limiting test"
      contains: "mod rate_limiting"
  key_links:
    - from: "tests/integration/rate_limiting.rs"
      to: "coordinator::run(cfg)"
      via: "tokio::spawn(coordinator::run(cfg))"
      pattern: "coordinator::run\\("
    - from: "tests/integration/rate_limiting.rs"
      to: "HTTP /info endpoint"
      via: "reqwest::Client::new().get(format!(\"{coordinator_url}/info\"))"
      pattern: "/info"
---

<objective>
Wave 3 (depends on Plans 02 and 03). Integration test that proves the Phase 8 work product end-to-end. Without this test, the Phase 8 mitigations are unverified at runtime — only static grep audits exist (Plans 02 and 03). The test follows CONTEXT D-06: in-process `coordinator::run()` path, hammer a write or read endpoint past its configured rate-limit, assert HTTP 429 + `Retry-After`. Per RESEARCH §"Open Questions" Q3 resolution, the test ALSO asserts the request-timeout (HTTP 408) path. The connection-cap end-to-end assertion is explicitly deferred per A4 (clearnet test infra cannot exercise tor-only semantics). Per PATTERNS hard requirement, the new file is registered in `tests/integration/mod.rs`.

Purpose: Catch regressions that the grep-level audits in Plans 02/03 cannot — specifically, the GlobalKeyExtractor wiring (Pitfall 1: if the extractor was wrong, the test would see HTTP 500, not 429), the ServiceBuilder ordering (Pitfall 3: if reversed, timeout might never fire), the JSON envelope shape (A5: confirms the body matches the project convention), AND the request-timeout (D-04 contract: handlers slower than `request_timeout_secs` are cut with HTTP 408).
Output: New `tests/integration/rate_limiting.rs` modeled on `tests/integration/round_bootstrap.rs`'s scaffolding with at least two test fns (`info_endpoint_returns_429_when_flooded` AND `request_timeout_returns_408`); new `mod rate_limiting;` line in `tests/integration/mod.rs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-public-endpoint-hardening/08-CONTEXT.md
@.planning/phases/08-public-endpoint-hardening/08-RESEARCH.md
@.planning/phases/08-public-endpoint-hardening/08-PATTERNS.md
@.planning/phases/08-public-endpoint-hardening/08-01-PLAN.md
@.planning/phases/08-public-endpoint-hardening/08-02-PLAN.md
@.planning/phases/08-public-endpoint-hardening/08-03-PLAN.md
@tests/integration/round_bootstrap.rs
@tests/integration/mod.rs
@coordinator/src/config.rs
@coordinator/src/run.rs

<interfaces>
<!-- The integration test scaffolding pattern is established; copy-and-adapt. -->

From tests/integration/round_bootstrap.rs (full file is the template):
- `async fn reserve_free_port() -> u16` (lines 26-33) — copy verbatim into new file
- `corepc_node::exe_path()` graceful-skip pattern (lines 45-54) — copy verbatim
- `tokio::task::spawn_blocking(move || { use corepc_node::{Conf, Node}; ... Box::leak(node_box); (rpc_url, rpc_user, rpc_pass) })` regtest bootstrap (lines 59-89) — copy verbatim
- `CoordinatorConfig { ... CoordinatorSection { ... } ... DiscoveryConfig { ... } }` literal (lines 110-138) — copy and override the four Plan 01 fields with TIGHT values for fast breach
- `tokio::spawn(async move { if let Err(e) = coordinator::run(cfg).await { ... } })` spawn pattern (lines 141-145) — copy verbatim
- HTTP-ready polling loop (lines 148-170) — copy as a "wait for server up" preamble before the actual flood
- `run_handle.abort();` cleanup (line 221) — required before every return path

From tests/integration/mod.rs (current 3-line file):
- `mod ban_list_persistence;`
- `mod full_round;`
- `mod round_bootstrap;`
Need to add: `mod rate_limiting;` (alphabetical position: before `round_bootstrap`).

From reqwest crate (already in dev-dependencies):
- `reqwest::Client::new()`, `.get(url).send().await`, `.status()`, `.headers().contains_key("retry-after")`
- `reqwest::StatusCode::TOO_MANY_REQUESTS` (= 429), `reqwest::StatusCode::REQUEST_TIMEOUT` (= 408)

From coordinator (post Plans 02 and 03):
- `coordinator::run(cfg)` accepts a `CoordinatorConfig` with four new `CoordinatorSection` fields
- `/info` endpoint is rate-limited at `rate_limit_info_per_min` rpm (shared read bucket)
- `/round/input`, `/round/output`, `/round/sign` are rate-limited at `rate_limit_writes_per_min` rpm (shared write bucket)
- All routes are timed out at `request_timeout_secs` (uniform)
- The Tor accept loop (NOT exercised here — clearnet test) is capped at `max_concurrent_connections`; the clearnet path is uncapped per A4
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write rate_limiting integration test (429 + Retry-After AND 408 timeout)</name>
  <files>tests/integration/rate_limiting.rs</files>
  <read_first>
    - tests/integration/round_bootstrap.rs (read in full, 225 lines) — the scaffolding template (graceful-skip, regtest bootstrap, port reservation, run spawn, HTTP-ready poll, run_handle.abort cleanup)
    - tests/integration/mod.rs (read in full, 3 lines) — confirms current registration; this task does NOT modify mod.rs (Task 2 of this plan does)
    - .planning/phases/08-public-endpoint-hardening/08-RESEARCH.md §"Code Examples Example 2" — the full test skeleton with the flood loop and 429-with-retry-after assertion; near-verbatim usable
    - .planning/phases/08-public-endpoint-hardening/08-RESEARCH.md §"Open Questions (RESOLVED)" Q3 (connection-cap test DEFERRED per A4) and Q2 (per-route timeout — uniform per A3)
    - .planning/phases/08-public-endpoint-hardening/08-PATTERNS.md §"tests/integration/rate_limiting.rs (NEW integration test)" — the 8-point copy-and-adapt recipe
    - .planning/phases/08-public-endpoint-hardening/08-CONTEXT.md §"Test approach (D-06)" — defines minimum scope of this test
    - coordinator/src/config.rs (post-Plan-01) lines 13-32 — confirms the CoordinatorSection schema the test must construct
  </read_first>
  <behavior>
    - `tests/integration/rate_limiting.rs` declares TWO mandatory `#[tokio::test]` functions:
      1. `info_endpoint_returns_429_when_flooded` — mirrors RESEARCH Example 2 verbatim adapted; runs by default
      2. `request_timeout_returns_408` — exercises D-04 timeout contract; may be `#[ignore = "..."]`d at the test-attribute level ONLY if invoked via `cargo test ... -- --include-ignored` in CI / verify block. Default-run skip (e.g. early-return + `eprintln`) is FORBIDDEN — D-04 must be runtime-proven somewhere.
    - Both tests start with the `corepc_node::exe_path()` graceful-skip pattern from `round_bootstrap.rs:45-54` (returns from the test with `eprintln!` if bitcoind absent — graceful-skip on missing infra is acceptable; intentional default-run skip of the test body itself is not)
    - Regtest bitcoind bootstrap matches `round_bootstrap.rs:59-89` (spawn_blocking + Conf::default() with regtest network + Box::leak)
    - `reserve_free_port` helper duplicated inside the file (PATTERNS analog explicitly recommends keeping it local rather than refactoring)
    - For the 429 test: `CoordinatorConfig` constructed with `rate_limit_info_per_min: 3`, `rate_limit_writes_per_min: 3` (TIGHT, for fast breach in the test window), `request_timeout_secs: 30` (irrelevant for the 429 test path), `max_concurrent_connections: 256`
    - For the 408 test: `CoordinatorConfig` constructed with `rate_limit_info_per_min: 600`, `rate_limit_writes_per_min: 600` (LOOSE — don't trip rate-limit before timeout fires), `request_timeout_secs: 1` (TIGHT — handler-slower-than-1s gets cut), `max_concurrent_connections: 256`
    - Other CoordinatorSection / NetworkConfig / DiscoveryConfig fields are set identically to the `round_bootstrap.rs:110-138` pattern (regtest, free-port listen_addr, temp pkarr_key_file + ban_file via `tempfile::tempdir()`, 3600s heartbeat to keep PKARR quiet during test window)
    - HTTP-ready preamble polls `/info` until first success (max 10s deadline; panic + `run_handle.abort()` on timeout — same pattern as `round_bootstrap.rs:148-170`)
    - 429 flood loop: 20 sequential `http.get(/info).send().await` calls. With `rate_limit_info_per_min = 3` and `burst_size = 3` (Plan 02's per_min_to_governor convention), the bucket exhausts after ~3 successful requests and the rest see 429
    - 429 primary assertion: at least one response in the flood has `status == TOO_MANY_REQUESTS` AND `headers().contains_key("retry-after")` (lowercase per reqwest's header normalization)
    - 429 JSON envelope assertion (A5 verification): when a 429 is seen, parse the body via `resp.json::<serde_json::Value>().await` and assert the shape contains `"error"` → `"code"` == `"RATE_LIMITED"` (this confirms Plan 02's `rate_limit_to_json` is wired)
    - 408 test approach (executor picks one of these two paths — both satisfy the D-04 runtime proof):
      - **Path A — slow handler in test bin via direct invocation:** Issue a `reqwest::Client::builder().timeout(Duration::from_secs(5))` GET to a route whose handler can be made to sleep > 1s. If `/info` or `/round/tx` does not currently sleep, this path may be infeasible without test-only handler injection (which T-06-02 forbids).
      - **Path B — slow request via `reqwest` body trickle (preferred):** Use `reqwest::Body::wrap_stream` with a slow `tokio_util::io::ReaderStream` or a hand-rolled `async_stream::stream!` that yields one byte then sleeps 2s. Post against `/round/input` (which reads the body). The `tower_http::timeout::TimeoutLayer` fires regardless of handler implementation — the request itself becomes slow, the timeout layer wraps around it. With `request_timeout_secs = 1`, response status MUST be `reqwest::StatusCode::REQUEST_TIMEOUT` (= 408). Document the chosen path in the test fn's doc comment.
    - 408 assertion: `assert_eq!(resp.status(), reqwest::StatusCode::REQUEST_TIMEOUT, "expected 408; got {}", resp.status());`
    - Connection-cap (Q3): NOT asserted in this file (A4 deferral). Add a TODO comment block at the top of the file explaining the deferral: `// TODO(Phase-8 Q3, A4): connection-cap (max_concurrent_connections) end-to-end test deferred — clearnet test infra cannot exercise the tor-only semaphore (Plan 03 only attaches the cap inside the arti accept loop). Coverage stands via Plan 03 grep audits. Tor-mode integration harness is a future-phase deliverable.`
    - Cleanup: `run_handle.abort()` before EVERY return path in BOTH tests (success and failure — copy the pattern from `round_bootstrap.rs:153` and `:221`)
  </behavior>
  <action>
    Create `tests/integration/rate_limiting.rs` by adapting the template from `tests/integration/round_bootstrap.rs` and the test skeleton from RESEARCH §"Code Examples Example 2". The file's module-level doc comment must state: (a) what this test proves (Phase 8 rate-limit + 429 + Retry-After end-to-end AND timeout + 408 end-to-end); (b) why it requires bitcoind (`coordinator::run()` startup_health_check); (c) the scope decisions per A4 (connection-cap not asserted on clearnet path; tor-mode test deferred to future-phase arti harness — coverage stands via Plan 03 grep audits); (d) the chosen 408-test path (Path A direct slow handler OR Path B slow-body via reqwest). Required imports at top: `std::time::Duration`. Required helper: copy `reserve_free_port` verbatim from `round_bootstrap.rs:26-33`. Required test fn #1: `#[tokio::test] async fn info_endpoint_returns_429_when_flooded()` containing in order: (1) `corepc_node::exe_path()` graceful-skip; (2) `tokio::task::spawn_blocking` regtest bootstrap (verbatim from `round_bootstrap.rs:59-89`); (3) `reserve_free_port()` + `tempfile::tempdir()` for pkarr_key_file and ban_file; (4) `CoordinatorConfig` literal with the four new fields set to `rate_limit_info_per_min: 3, rate_limit_writes_per_min: 3, request_timeout_secs: 30, max_concurrent_connections: 256`; (5) `let run_handle = tokio::spawn(async move { if let Err(e) = coordinator::run(cfg).await { eprintln!("..."); } });`; (6) HTTP-ready polling loop (deadline 10s) — abort and panic on timeout; (7) flood loop: `for _ in 0..20 { let resp = http.get(&info_url).send().await.expect("send"); if resp.status() == reqwest::StatusCode::TOO_MANY_REQUESTS && resp.headers().contains_key("retry-after") { /* parse body and assert JSON envelope code = "RATE_LIMITED"; */ saw_429 = true; break; } }`; (8) `assert!(saw_429, "expected at least one 429 with retry-after header within 20 flood requests; got only 2xx — rate-limit may not be wired");`; (9) `run_handle.abort();`. Required test fn #2: `#[tokio::test] async fn request_timeout_returns_408()` containing the same scaffolding (graceful-skip → regtest bootstrap → port + temp files → `CoordinatorConfig` literal with `rate_limit_info_per_min: 600, rate_limit_writes_per_min: 600, request_timeout_secs: 1, max_concurrent_connections: 256` → run_handle spawn → HTTP-ready poll) followed by either Path A or Path B from <behavior> above to induce a slow request, then `assert_eq!(resp.status(), reqwest::StatusCode::REQUEST_TIMEOUT, "expected 408 from request_timeout_secs=1; got {}", resp.status());` and `run_handle.abort();`. If you cannot identify a way to make a request slow without modifying production code (which T-06-02 forbids), mark the test `#[ignore = "requires slow-body harness — invoke via --include-ignored"]` so it is still discoverable by the verify block's `--include-ignored` invocation; document the choice in the SUMMARY. Do NOT add a `Cargo.toml` dev-dependency (`reqwest`, `tempfile`, `tokio`, `coordinator`, `shared`, `corepc-node`, `bitcoin` are already declared via the workspace `[[test]] integration` entry); if Path B genuinely requires `tokio-util` or `async-stream`, surface this as a checker-style finding and stop (do not silently add deps). Do NOT modify `tests/integration/mod.rs` here — Task 2 does that.
  </action>
  <verify>
    <automated>cargo test --test integration rate_limiting:: --no-run 2>&1 | grep -E "error\[" | tee /tmp/p08-04-task1-errors.txt && test ! -s /tmp/p08-04-task1-errors.txt</automated>
  </verify>
  <acceptance_criteria>
    - Source: `tests/integration/rate_limiting.rs` exists and is at least 80 lines (scaffolding + flood loop + 408 test + assertions)
    - Source: `tests/integration/rate_limiting.rs` contains `#[tokio::test]` and a fn named `info_endpoint_returns_429_when_flooded` (or close variant — executor may rename if more accurate)
    - Source: `tests/integration/rate_limiting.rs` contains `#[tokio::test]` and a fn named `request_timeout_returns_408` (MANDATORY per D-04 runtime proof — may carry `#[ignore = \"...\"]` if Path B requires `--include-ignored` invocation, but the fn body must contain real 408-assertion logic, NOT an early return + eprintln skip)
    - Source: contains the literal `corepc_node::exe_path()` (graceful-skip pattern)
    - Source: contains `coordinator::run(cfg)` (in-process spawn — D-06 mandate)
    - Source: contains `reqwest::StatusCode::TOO_MANY_REQUESTS` AND `"retry-after"` (the 429 assertion)
    - Source: contains `reqwest::StatusCode::REQUEST_TIMEOUT` (the 408 assertion — D-04 runtime proof)
    - Source: contains `RATE_LIMITED` (JSON envelope code per A5)
    - Source: contains `run_handle.abort()` on at least one path per test fn (cleanup)
    - Source: contains the `TODO(Phase-8 Q3, A4)` comment explaining the connection-cap deferral
    - Behavior: `cargo test --test integration rate_limiting:: --no-run` exits 0 (test compiles — Task 2 module registration is the prerequisite for it to be findable; until Task 2 runs, `--no-run` may still succeed at the per-file level if cargo picks up the unregistered file via the workspace test target — see Task 2 for the real runtime gate)
    - Behavior: `cargo test --test integration rate_limiting::request_timeout_returns_408 -- --include-ignored` exits 0 (bitcoind-available path: real 408 observed; bitcoind-absent: graceful-skip; either is success — but the test fn MUST be discoverable, NOT default-skipped at the fn body level)
  </acceptance_criteria>
  <done>The integration test file exists and compiles. It does not yet RUN end-to-end until Task 2 registers the module. The file asserts the FULL Phase 8 contract: D-02/D-03 (429 + Retry-After + JSON envelope code RATE_LIMITED) AND D-04 (408 from `request_timeout_secs = 1`). Q3 connection-cap deferral is documented inline with a TODO comment per A4.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Register the new rate_limiting module + run the tests (bitcoind permitting)</name>
  <files>tests/integration/mod.rs</files>
  <read_first>
    - tests/integration/mod.rs (read in full, 3 lines) — current registration
    - .planning/phases/08-public-endpoint-hardening/08-PATTERNS.md §"Module registration" — confirms the new `mod rate_limiting;` line is required for the file to be found by `cargo test --test integration`
    - tests/integration/rate_limiting.rs (created by Task 1 of this plan) — verify the test function names you'll invoke
  </read_first>
  <behavior>
    - `tests/integration/mod.rs` gains a single new line: `mod rate_limiting;` (alphabetical position: between `mod full_round;` and `mod round_bootstrap;`)
    - The full file now reads: `mod ban_list_persistence;`, `mod full_round;`, `mod rate_limiting;`, `mod round_bootstrap;`
    - `cargo test --test integration rate_limiting:: -- --include-ignored` actually invokes BOTH new tests (no longer skipped as "no matching tests")
    - When bitcoind IS available: both tests run end-to-end and exit 0 (proving 429 + Retry-After + JSON envelope behavior AND the 408 timeout contract)
    - When bitcoind is NOT available: both tests print `bitcoind not found ... skipping ...` and return Ok (graceful-skip — does not fail CI)
  </behavior>
  <action>
    Modify `tests/integration/mod.rs` by inserting `mod rate_limiting;` between the existing `mod full_round;` and `mod round_bootstrap;` lines (alphabetical position). The file should now be exactly 4 lines. Then run `cargo test --test integration rate_limiting:: -- --nocapture --include-ignored` and observe the outcome. `--include-ignored` is required so the `request_timeout_returns_408` test runs regardless of whether Task 1 attached `#[ignore = "..."]` to it. If bitcoind is available in PATH (`corepc_node::exe_path()` returns Ok), BOTH tests execute end-to-end and must pass — this is the runtime proof of Phase 8 D-02/D-03/D-04. If bitcoind is unavailable, both tests print the graceful-skip message and exit 0; this is acceptable per the established pattern. Either outcome is success for THIS task. If the 429 test runs AND fails: (a) status was not 429 → check Plan 02 wiring (was `GovernorLayer` actually attached to `/info`? was `GlobalKeyExtractor` used?), (b) no `retry-after` header → tower_governor version mismatch (Plan 01 should have pinned 0.8), (c) status 500 → almost certainly Pitfall 1 (PeerIpKeyExtractor still in use), (d) body shape wrong → A5 envelope handler mis-wired in Plan 02. If the 408 test runs AND fails: (a) status was 200 → timeout layer not wired or `request_timeout_secs` not read from config (Plan 02 mis-wired), (b) status was 408 but emitted before request body completes → may indicate Pitfall 3 layer-ordering bug in Plan 02 mod.rs (ServiceBuilder must wrap timeout outside body-limit), (c) test hangs → timeout layer is missing entirely; check Plan 02 Task 3 wiring. Investigate and either fix the upstream plan's code OR file a checker-style finding for revision-mode replanning. Do NOT silently disable the test; do NOT remove `#[ignore]` attribute (if any) without also adjusting the verify command; do NOT loosen the assertions.
  </action>
  <verify>
    <automated>cargo test --test integration rate_limiting:: -- --nocapture --include-ignored 2>&1 | tail -60 ; cargo build --all-targets 2>&1 | grep -E "error\["</automated>
  </verify>
  <acceptance_criteria>
    - Source: `tests/integration/mod.rs` contains `mod rate_limiting;` (single new line)
    - Source: `tests/integration/mod.rs` is exactly 4 lines (no extra whitespace / no other changes)
    - Behavior: `cargo test --test integration rate_limiting:: -- --include-ignored` runs at least TWO test functions (output contains `running 2 tests` or similar — confirms the registration worked AND that both 429 and 408 tests are discoverable)
    - Behavior (bitcoind-available path): both tests exit 0 with `test info_endpoint_returns_429_when_flooded ... ok` AND `test request_timeout_returns_408 ... ok` lines in the output (this is the end-to-end Phase 8 proof; D-02/D-03/D-04 verified at runtime)
    - Behavior (bitcoind-absent path): both tests exit 0 with `... ok` lines AND stderr lines containing `bitcoind not found` (graceful-skip; standard pattern)
    - Behavior: `cargo build --all-targets` exits 0 (final, full-workspace clean compile — every plan's edits compose)
  </acceptance_criteria>
  <done>The Phase 8 work product is verified end-to-end at runtime (when bitcoind is available). The integration tests are registered, discoverable by `cargo test ... --include-ignored`, follow the existing graceful-skip pattern, and assert the four things Plan 02's grep audits cannot prove on their own: (a) GlobalKeyExtractor wiring is correct (status is 429, not 500), (b) tower_governor's automatic Retry-After header is emitted, (c) the JSON envelope code matches A5, (d) `tower_http::timeout::TimeoutLayer` fires with 408 status when handlers exceed `request_timeout_secs`. The complete Phase 8 deliverable now ships with a regression guard covering D-02, D-03, and D-04. D-05 connection-cap is grep-audit-only per A4 deferral (documented in SUMMARY).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test client → in-process coordinator HTTP API | The integration test deliberately crosses the trust boundary repeatedly (the flood loop is the attack simulation; the slow-body request simulates a slow-loris); the assertion checks the defense behaves correctly |
| `bitcoind` (regtest) → coordinator startup_health_check | Test infra dependency; graceful-skip on absence is part of the pattern (no security concern, just a CI-availability concern) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Repudiation (Regression-blind) | Phase 8 mitigations could silently regress (e.g. someone removes `.key_extractor(GlobalKeyExtractor)` and the layer panics on every request; or someone unwires the timeout layer and slow-loris becomes effective again) | mitigate | This entire plan is the regression guard. The 429 test asserts the FULL request/response contract for D-02/D-03/D-05; the 408 test asserts D-04. Both are runtime proofs, not just compile-time presence of code (which is what Plan 02 grep audits do). |
| T-08-04-02 | Tampering (Test-only backdoors) | Adding a `#[cfg(test)]` branch in `coordinator/src/` to make the test pass (e.g., a slow-handler injection point) | accept (forbidden) | Per T-06-02 (referenced in `tests/integration/round_bootstrap.rs:17`): NO test-only backdoors in production code. The test uses `coordinator::run(cfg)` directly — same path as production. The 408 test induces slowness from the CLIENT side (slow body via reqwest), not via handler injection. Any divergence is a phase-failure, not a workaround. |
| T-08-04-03 | Information Disclosure (Test secrets) | Bitcoind regtest cookies / ports / RPC creds appearing in test output | accept | Regtest is ephemeral, cookies are session-scoped, ports are OS-assigned (`reserve_free_port`). No production secrets exposed. PATTERNS confirms `round_bootstrap.rs` follows this same model. |
| T-08-04-04 | Denial of Service (Test hang) | Test never sees a 429/408 and blocks waiting | mitigate | Hard deadline on the 429 flood loop (20 iterations, ~few seconds). `reqwest::Client::builder().timeout(Duration::from_secs(5))` on the 408 test ensures the test client itself does not hang past `request_timeout_secs + reasonable slack`. `run_handle.abort()` on every return path. Bitcoind absence is graceful-skip, not a hang. |
| T-08-04-05 | Tampering (Skip-instead-of-fix) | Future-someone adds `#[ignore]` to silence a failing test, OR replaces test body with an early-return + eprintln to fake graceful-skip | mitigate (procedural) | Task 1 acceptance_criteria explicitly require the 408 fn body to contain real 408-assertion logic; `#[ignore]` is permitted ONLY if verify command uses `--include-ignored` (which it does). Task 2 acceptance_criteria require `running 2 tests` in output — fewer than 2 means a fn was silently removed. Investigation-first if test fails. |

**What this plan does NOT mitigate (called out per security_threat_model):**
- The connection-cap from Plan 03 is NOT asserted end-to-end in this clearnet test (A4 resolution + RESEARCH §"Open Questions (RESOLVED)" Q3). Tor-mode integration test would require additional arti harness — explicit deferral, documented in SUMMARY. Coverage stands via Plan 03 grep audits.
- Per-peer / per-IP semantics are NEVER asserted — `GlobalKeyExtractor` is the explicit design choice per CONTEXT D-01 + RESEARCH Pitfall 1; "per-peer" rate-limiting is OUT OF SCOPE for this phase.
</threat_model>

<verification>
- `cargo test --test integration rate_limiting:: -- --nocapture --include-ignored` exits 0
- Test output (bitcoind-available): contains BOTH `test info_endpoint_returns_429_when_flooded ... ok` AND `test request_timeout_returns_408 ... ok`, and zero panics
- Test output (bitcoind-absent): contains `bitcoind not found ... skipping ...` and `... ok` for both fns
- `grep -c "TOO_MANY_REQUESTS" tests/integration/rate_limiting.rs` returns ≥ 1
- `grep -c "REQUEST_TIMEOUT" tests/integration/rate_limiting.rs` returns ≥ 1 (the 408 assertion — D-04 runtime proof)
- `grep -c "retry-after" tests/integration/rate_limiting.rs` returns ≥ 1 (lowercase per reqwest normalization)
- `grep -c "RATE_LIMITED" tests/integration/rate_limiting.rs` returns ≥ 1 (A5 envelope code)
- `grep -c "request_timeout_returns_408" tests/integration/rate_limiting.rs` returns ≥ 1 (fn name presence guard against silent removal)
- `grep -c "TODO(Phase-8 Q3, A4)" tests/integration/rate_limiting.rs` returns ≥ 1 (deferral documentation per A4 resolution)
- `grep -c "mod rate_limiting" tests/integration/mod.rs` returns 1
- `cargo build --all-targets` exits 0 (full-workspace final compile)
- `cargo clippy --all-targets -- -D warnings` exits 0 (full-workspace clippy clean)
</verification>

<success_criteria>
Phase 8 is verifiable at runtime. The new `tests/integration/rate_limiting.rs` integration test runs via the same `coordinator::run(cfg)` path the production binary uses (T-06-02 compliance — no test-only backdoors), and ASSERTS BOTH (a) `/info` flooded past `rate_limit_info_per_min` returns HTTP 429 + `Retry-After` header + JSON envelope with `code: "RATE_LIMITED"` (D-02, D-03, D-05, A5) AND (b) a slow request against the coordinator with `request_timeout_secs = 1` returns HTTP 408 REQUEST_TIMEOUT (D-04 contract runtime proof). The test follows the existing graceful-skip pattern on missing bitcoind. The test is registered in `tests/integration/mod.rs`. Connection-cap end-to-end assertion is deferred per A4 (clearnet-only test infra cannot meaningfully exercise the tor-only cap; Plan 03 grep audits stand and the deferral is documented inline with a TODO comment). `cargo test ... --include-ignored`, `cargo build --all-targets`, and `cargo clippy -D warnings` all pass clean. Phase 8 is shippable; mainnet/public deployment is now unblocked per the original BACKLOG.md B-01 deferral note.
</success_criteria>

<output>
Create `.planning/phases/08-public-endpoint-hardening/08-04-SUMMARY.md` documenting: (a) the two test functions implemented (`info_endpoint_returns_429_when_flooded` AND `request_timeout_returns_408`), (b) the chosen 408-test path (Path A direct slow handler OR Path B slow-body via reqwest) and whether `#[ignore = "..."]` was attached, (c) the outcome of running `cargo test --test integration rate_limiting:: -- --include-ignored` (bitcoind-available or graceful-skip), (d) the explicit deferrals — connection-cap test deferred per A4 with the TODO-comment evidence, (e) confirmation that no `#[cfg(test)]` branches were added to production code (T-06-02), (f) the end-to-end Phase 8 status: CONTEXT decisions D-01..D-06 are now all proven at the appropriate level (D-01 framing is a comment / PR-description concern; D-02 + D-03 + D-04 are runtime-asserted; D-05 GlobalKeyExtractor + connection-cap are config-driven + grep-audited; D-06 is the test itself).
</output>
</output>
