---
phase: 15-shared-crate-multi-script-contract
plan: 03
type: execute
wave: 3
depends_on: ["15-01", "15-02"]
files_modified:
  - shared/Cargo.toml
  - shared/tests/fixtures/bip322/basic-test-vectors.json
  - shared/tests/fixtures/bip322/p2sh_p2wpkh_supplement.json
  - shared/tests/fixtures/bip322/README.md
  - shared/tests/per_script_vectors.rs
  - shared/tests/bip322_cross_shape.rs
autonomous: true
requirements: [BIP322-04]
requirements_addressed: [BIP322-04]
tags: [bip322, property-tests, cross-shape-rejection, vendored-fixture, basic-test-vectors]
must_haves:
  truths:
    - "shared/tests/fixtures/bip322/basic-test-vectors.json is the vendored snapshot from bitcoin/bips@<commit-sha> with a header comment recording source URL + commit SHA + capture date per D-33"
    - "shared/tests/fixtures/bip322/p2sh_p2wpkh_supplement.json supplements the upstream gap (upstream has 0 P2SH-P2WPKH cases per RESEARCH Pitfall 6); supplement is lifted from the bip322 v0.0.10 crate's lib.rs:46-48 + lib.rs:299-321 test constants, format-aligned to the upstream simple array entry shape so the test harness iterates both files uniformly"
    - "shared/tests/fixtures/bip322/README.md records the commit SHA + capture date + curl command per D-33"
    - "shared/tests/per_script_vectors.rs runs positive sign↔verify property tests against the vendored fixture + supplement, covering P2WPKH AND P2TR AND P2SH-P2WPKH per BIP322-04"
    - "shared/tests/bip322_cross_shape.rs contains the 9 enumerated #[test] fns per D-34 with the exact names verbatim and each asserting a specific Bip322Error variant via matches!"
    - "All 9 cross-shape rejection tests pass on the green path AND assert specific Bip322Error variants (V1.4-CRIT-01 mitigation statically provable per RESEARCH A3)"
    - "shared/Cargo.toml [dev-dependencies] adds proptest = { workspace = true } for property-test scaffolding"
    - "v1.3 cross-phase invariant: cargo test --test full_round (existing integration suite) remains green after 15-03 lands"
    - "Atomic commit (third in CD-10 sequence: 15-01 -> 15-02 -> 15-03)"
  artifacts:
    - path: "shared/tests/fixtures/bip322/basic-test-vectors.json"
      provides: "Vendored upstream BIP-322 test vectors (P2WPKH + P2TR; upstream has no P2SH-P2WPKH)"
      contains: ["simple"]
    - path: "shared/tests/fixtures/bip322/p2sh_p2wpkh_supplement.json"
      provides: "P2SH-P2WPKH supplement matching the upstream simple entry shape; lifted from bip322 crate test constants"
      contains: ["p2sh-p2wpkh", "private_keys", "bip322_signatures"]
    - path: "shared/tests/fixtures/bip322/README.md"
      provides: "Vendor provenance: source URL, commit SHA, capture date, curl command per D-33"
      contains: ["bitcoin/bips", "captured 20"]
    - path: "shared/tests/per_script_vectors.rs"
      provides: "Per-script positive vector tests against both fixture files; covers P2WPKH + P2TR + P2SH-P2WPKH"
      contains: ["include_str!", "basic-test-vectors.json", "p2sh_p2wpkh_supplement.json"]
    - path: "shared/tests/bip322_cross_shape.rs"
      provides: "9 enumerated #[test] fns per D-34 for the off-diagonal (spk × witness) cross-shape rejection matrix"
      contains: ["reject_p2wpkh_spk_with_p2tr_witness", "reject_p2wpkh_spk_with_p2sh_p2wpkh_witness", "reject_p2tr_spk_with_p2wpkh_witness", "reject_p2tr_spk_with_p2sh_p2wpkh_witness", "reject_p2sh_p2wpkh_spk_with_p2wpkh_witness", "reject_p2sh_p2wpkh_spk_with_p2tr_witness", "reject_p2wpkh_spk_with_empty_witness", "reject_p2tr_spk_with_empty_witness", "reject_p2sh_p2wpkh_spk_with_empty_witness"]
    - path: "shared/Cargo.toml"
      provides: "proptest dev-dep for property-test scaffolding"
      contains: ["proptest"]
  key_links:
    - from: "shared/tests/per_script_vectors.rs"
      to: "shared::bip322::verify_simple + sign_simple-test-path via #[cfg(test)] sign_for_tests in each per-script file"
      via: "uses dispatcher API only (D-27 invariant)"
      pattern: "shared::bip322::(verify_simple|sign_simple)"
    - from: "shared/tests/bip322_cross_shape.rs"
      to: "shared::bip322::verify_simple + Bip322Error variants"
      via: "asserts specific variant on each off-diagonal combination"
      pattern: "matches!\\(\\s*result,\\s*Err\\(Bip322Error::"
    - from: "shared/tests/per_script_vectors.rs"
      to: "shared/tests/fixtures/bip322/basic-test-vectors.json + p2sh_p2wpkh_supplement.json"
      via: "include_str! at compile time per D-33"
      pattern: "include_str!\\(\"fixtures/bip322/"
---

<objective>
Land the BIP322-04 per-script property tests (positive vectors against the vendored basic-test-vectors.json + P2SH-P2WPKH supplement) AND the 9-combination cross-shape rejection matrix per D-34 verbatim, completing the V1.4-CRIT-01 (script-type spoofing) and V1.4-CRIT-02 (silent sighash failures) mitigations for the Phase 15 boundary.

Purpose: Make every off-diagonal (script_pubkey × witness-shape) rejection an enumerated #[test] fn with a self-documenting name, so a code reviewer can scan shared/tests/bip322_cross_shape.rs and see exactly which CRIT-01 spoofing vector each test closes. Make per-script positive vectors run against the OFFICIAL upstream test data (vendored at a commit SHA per D-33) so silent sighash regressions surface immediately. RESEARCH A3 disposition: the 9 tests MUST actually run and assert specific Bip322Error variants on the green path before the plan declares done — silent acceptance of any cell in the matrix is V1.4-CRIT-01 unmitigated.

Output: Green `cargo test -p shared --test per_script_vectors`, green `cargo test -p shared --test bip322_cross_shape` with 9 enumerated passing #[test] fns, green `cargo test -p shared --test ownership_proof_roundtrip` (15-01 suite still green), green `cargo test --test full_round` (cross-phase invariant).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-shared-crate-multi-script-contract/15-CONTEXT.md
@.planning/phases/15-shared-crate-multi-script-contract/15-RESEARCH.md
@.planning/phases/15-shared-crate-multi-script-contract/15-PATTERNS.md
@.planning/phases/15-shared-crate-multi-script-contract/15-01-PLAN.md
@.planning/phases/15-shared-crate-multi-script-contract/15-02-PLAN.md
@.planning/decisions/v1.4-adr.md
@shared/src/bip322/mod.rs
@shared/src/bip322/p2wpkh.rs
@shared/src/bip322/p2tr.rs
@shared/src/bip322/p2sh_p2wpkh.rs
@shared/src/protocol.rs
@shared/Cargo.toml
@Cargo.toml

<interfaces>
Vendored fixture layout per D-33 + RESEARCH Pitfall 6:

  shared/tests/fixtures/bip322/basic-test-vectors.json
    Header (first line preserved from upstream OR as a JSON-incompatible comment if upstream JSON is strict; use the README.md sibling for the provenance metadata so the JSON file itself is parser-clean). Schema (per RESEARCH §"Sources Secondary" line 1064): top-level object with keys `tx_hashes`, `simple`, `error`. The `simple` array entries have the shape:
      { "message": String, "private_keys": [WIF strings], "address": String, "bip322_signatures": [base64 strings] }
    or
      { "message": String, "private_keys": [WIF strings], "address": String, "type": String, "witness_script": String, "bip322_signatures": [base64 strings] }
    For Phase 15 BIP322-04: classify each `simple` entry by detecting the address prefix (bc1p... = P2TR, bc1q... = P2WPKH; mainnet 3... NEVER appears in basic-test-vectors.json per RESEARCH Pitfall 6) and run sign↔verify against the appropriate per-script path.

  shared/tests/fixtures/bip322/p2sh_p2wpkh_supplement.json
    Schema (matches upstream `simple` array entry shape so per_script_vectors.rs can iterate uniformly):
      [
        {
          "message": String,
          "private_keys": [WIF strings],
          "address": String,           // mainnet 3... (e.g., "3HSVzEhCFuH9Z3wvoWTexy7BMVVp3PjS6f")
          "type": "p2sh-p2wpkh",        // explicit type marker
          "witness_script": "",          // empty; P2SH-P2WPKH has no witness_script (unlike P2WSH)
          "bip322_signatures": [base64 strings]
        }
      ]
    Source: bip322 v0.0.10 crate at ~/.cargo/registry/src/.../bip322-0.0.10/src/lib.rs:46-48 + lib.rs:299-321 (NESTED_SEGWIT_ADDRESS, NESTED_SEGWIT_PRIVATE_KEY, two sign-verify test cases). Header in README.md explains the supplement provenance.

  shared/tests/fixtures/bip322/README.md
    Records:
      # BIP-322 test vector vendor provenance
      ## basic-test-vectors.json
      Source: https://raw.githubusercontent.com/bitcoin/bips/<COMMIT_SHA>/bip-0322/basic-test-vectors.json
      Captured: 20YY-MM-DD via `curl -L <URL> > basic-test-vectors.json`
      Upstream commit SHA: <SHA>
      ## p2sh_p2wpkh_supplement.json
      Source: bip322 v0.0.10 crate, ~/.cargo/registry/src/.../bip322-0.0.10/src/lib.rs:46-48 + lib.rs:299-321
      Captured: 20YY-MM-DD
      Reason: upstream basic-test-vectors.json contains 0 P2SH-P2WPKH vectors (RESEARCH Pitfall 6)

Compile-time fixture loading (per D-33 / PATTERNS.md line 524):

  const BASIC_VECTORS: &str = include_str!("fixtures/bip322/basic-test-vectors.json");
  const P2SH_P2WPKH_SUPPLEMENT: &str = include_str!("fixtures/bip322/p2sh_p2wpkh_supplement.json");

Test harness for per_script_vectors.rs (per RESEARCH §"Phase Requirements" BIP322-04 row + CONTEXT D-33):

  Parse each fixture as serde_json::Value (loose Value type to handle the optional `type` field).
  For each entry in `simple[]` (or the supplement's top-level array):
    Detect script type:
      - if `type` field is present and equals "p2sh-p2wpkh" -> ScriptType::P2shP2wpkh
      - else parse address; if address starts with bc1p (mainnet) or tb1p (testnet/signet) -> ScriptType::P2tr; if address starts with bc1q or tb1q -> ScriptType::P2wpkh
      - if neither matches, skip the entry (no out-of-scope shapes like P2WSH multisig from upstream `simple` should appear, but be defensive)
    Reconstruct the Witness from each bip322_signatures[i] (it is a base64-encoded witness stack from BIP-322 Simple — decode base64 -> deserialize as bitcoin::Witness via the canonical bitcoin::consensus encoding per the bip322 crate's verify path).
    Recover the script_pubkey from the address via bitcoin::Address::from_str().require_network(bitcoin::Network::Bitcoin).script_pubkey() (mainnet for upstream vectors per RESEARCH Pitfall 5: SPK is network-agnostic at the byte layer; address prefix is just for parsing).
    Call shared::bip322::verify_simple(script_type, &spk, &witness, message.as_bytes(), bitcoin::Network::Bitcoin) and assert Ok(()).

  After the verify-against-upstream pass, run a round-trip pass per script type using the #[cfg(test)] sign_for_tests helpers from each per-script file (P2WPKH + P2TR + P2SH-P2WPKH). For P2WPKH, alternatively call shared::bip322::sign_simple (which is fully implemented per CD-6); for P2TR + P2SH-P2WPKH call sign_for_tests directly because sign_simple is todo!() in 15-02. Assert that the produced witness verifies via shared::bip322::verify_simple.

  Per RESEARCH A3 disposition: the BIP322-04 gate requires at least one passing positive vector per script type. If basic-test-vectors.json contains M P2WPKH + N P2TR cases and the supplement contains P P2SH-P2WPKH cases, assert M >= 1 AND N >= 1 AND P >= 1 (compile-time fixture parse) so a future fixture bump that drops a script type is caught.

Cross-shape rejection harness for bip322_cross_shape.rs (per CONTEXT D-34 verbatim):

  9 enumerated #[test] fns with these EXACT names:
    reject_p2wpkh_spk_with_p2tr_witness
    reject_p2wpkh_spk_with_p2sh_p2wpkh_witness
    reject_p2tr_spk_with_p2wpkh_witness
    reject_p2tr_spk_with_p2sh_p2wpkh_witness
    reject_p2sh_p2wpkh_spk_with_p2wpkh_witness
    reject_p2sh_p2wpkh_spk_with_p2tr_witness
    reject_p2wpkh_spk_with_empty_witness
    reject_p2tr_spk_with_empty_witness
    reject_p2sh_p2wpkh_spk_with_empty_witness

  Each test:
    1. Constructs a known SPK of one type (P2WPKH from ScriptBuf::new_p2wpkh(wpubkey_hash); P2TR from ScriptBuf::new_p2tr(&secp, x_only_pubkey, None); P2SH from ScriptBuf::new_p2sh(&script_hash))
    2. Constructs a known witness of a DIFFERENT type (or empty for arity edge cases)
       - P2TR witness: 1 element, 64 bytes (generate via the p2tr::sign_for_tests helper, OR fabricate a 64-byte dummy)
       - P2WPKH witness: 2 elements [sig_bytes, pubkey_bytes]
       - P2SH-P2WPKH witness: 2 elements [sig_bytes, pubkey_bytes] — same shape as P2WPKH at the witness level
       - empty: Witness::new() with 0 elements
    3. Calls shared::bip322::verify_simple(declared_type, &spk, &witness, b"test", Network::Regtest)
    4. Asserts a specific Bip322Error variant via matches!()

  Expected variants per test (RESEARCH A3 disposition — these MUST be enforced):
    reject_p2wpkh_spk_with_p2tr_witness -> Err(Bip322Error::InvalidWitnessLength { expected: 2, got: 1 }) (arity check fires in p2wpkh::verify before the adapter delegates)
    reject_p2wpkh_spk_with_p2sh_p2wpkh_witness -> Err(Bip322Error::CrateVerifyFailed { .. }) (arity matches at 2, but the underlying signature does not validate over the P2WPKH sighash)
    reject_p2tr_spk_with_p2wpkh_witness -> Err(Bip322Error::InvalidWitnessLength { expected: 1, got: 2 }) (arity check fires in p2tr::verify)
    reject_p2tr_spk_with_p2sh_p2wpkh_witness -> Err(Bip322Error::InvalidWitnessLength { expected: 1, got: 2 })
    reject_p2sh_p2wpkh_spk_with_p2wpkh_witness -> Err(Bip322Error::CrateVerifyFailed { .. }) (arity matches at 2, but the HASH160 cross-check inside the crate's verify_full_p2wpkh(is_p2sh=true) fails because the pubkey in the witness does not hash to the P2SH SPK's hash)
    reject_p2sh_p2wpkh_spk_with_p2tr_witness -> Err(Bip322Error::InvalidWitnessLength { expected: 2, got: 1 })
    reject_p2wpkh_spk_with_empty_witness -> Err(Bip322Error::InvalidWitnessLength { expected: 2, got: 0 })
    reject_p2tr_spk_with_empty_witness -> Err(Bip322Error::InvalidWitnessLength { expected: 1, got: 0 })
    reject_p2sh_p2wpkh_spk_with_empty_witness -> Err(Bip322Error::InvalidWitnessLength { expected: 2, got: 0 })

Helper fns shared between the 9 cross-shape tests (placed in the same file as the tests, or in a tests/common/ submodule — Cargo's integration test convention permits a `mod` declaration with a shared helpers file at `shared/tests/common/mod.rs`):
  fn make_known_p2wpkh_spk() -> ScriptBuf  -- uses SecretKey::from_slice(&[0x02; 32]) + Secp256k1 + wpubkey_hash + ScriptBuf::new_p2wpkh
  fn make_known_p2tr_spk() -> ScriptBuf    -- uses SecretKey::from_slice(&[0x03; 32]) + tap_tweak + XOnlyPublicKey + ScriptBuf::new_p2tr (untweaked alternative)
  fn make_known_p2sh_p2wpkh_spk() -> ScriptBuf -- uses SecretKey::from_slice(&[0x04; 32]) + wpubkey_hash + ScriptBuf::new_p2wpkh as inner -> hash160 -> ScriptBuf::new_p2sh
  fn make_p2wpkh_witness_for(message: &[u8], spk: &Script, key: &SecretKey) -> Witness -- calls shared::bip322::sign_simple(ScriptType::P2wpkh, spk, key, message)
  fn make_p2tr_witness_for(message: &[u8], spk: &Script, key: &SecretKey) -> Witness -- calls the p2tr::sign_for_tests #[cfg(test)] helper (need to expose via pub(crate) since the integration test is an external crate — workaround: build the test from scratch in this file using the same 8-step BIP-341 sequence, OR add a pub-via-cfg(test) helper in mod.rs that re-exports sign_for_tests)

NOTE on test helper visibility: shared/tests/ files are external integration tests (separate crate). The `pub(crate)` helpers in shared/src/bip322/p*.rs files are not reachable from integration tests. Two options:
  Option A (RECOMMENDED): expose a #[cfg(test)] pub fn sign_simple_test_only(script_type, spk, key, message) -> Result<Witness, Bip322Error> in shared/src/bip322/mod.rs that branches to the per-script sign_for_tests helpers. This keeps production sign_simple's P2TR/P2SH-P2WPKH branches as todo!() while the integration tests can drive end-to-end sign↔verify cycles. The new fn is gated by #[cfg(test)] so it does not affect the production API.
  Option B: rebuild the test signers from scratch inline in shared/tests/bip322_cross_shape.rs + per_script_vectors.rs. Duplicates code and increases drift risk.

15-03 selects Option A: add a #[cfg(test)] pub fn sign_simple_test_only to shared/src/bip322/mod.rs that mirrors sign_simple but routes ScriptType::P2tr -> p2tr::sign_for_tests and ScriptType::P2shP2wpkh -> p2sh_p2wpkh::sign_for_tests; ScriptType::P2wpkh -> p2wpkh::sign (production body works fine here). This is a minimal #[cfg(test)] addition to mod.rs; it does NOT change the public production API. Decision IDs: extends CD-6 default with the test-only dispatcher mirror.

Property-test scaffolding (proptest dev-dep): 15-03 introduces proptest = { workspace = true } to shared/Cargo.toml [dev-dependencies] per Phase 14 CARRY constraint + CONTEXT D-34 reference. The 9 cross-shape rejection tests are enumerated #[test] fns (NOT proptest! macros) per D-34's "no proptest shrink output to interpret" rationale. The proptest dep is reserved for future sign↔verify property tests if BIP322-04 needs to extend coverage; if proptest is not used in 15-03 implementation, the dep is still landed for 15-03's per_script_vectors round-trip pass that may use proptest! macros for sign↔verify roundtrip over random messages (this is RECOMMENDED but not REQUIRED — at minimum the dep is landed because Phase 15-CONTEXT/RESEARCH/PATTERNS all reference it explicitly and the cargo audit baseline should reflect proptest in the dev-dep chain).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor BIP-322 basic-test-vectors.json + create P2SH-P2WPKH supplement + provenance README; add proptest dev-dep; expose #[cfg(test)] pub fn sign_simple_test_only in shared::bip322 for integration-test sign↔verify path</name>
  <files>shared/tests/fixtures/bip322/basic-test-vectors.json, shared/tests/fixtures/bip322/p2sh_p2wpkh_supplement.json, shared/tests/fixtures/bip322/README.md, shared/Cargo.toml, shared/src/bip322/mod.rs</files>
  <read_first>
    - shared/src/bip322/mod.rs (after 15-02 — confirm the production sign_simple structure so the #[cfg(test)] sign_simple_test_only mirrors it correctly)
    - shared/src/bip322/p2wpkh.rs (after 15-02 — confirm pub(crate) fn sign + sign_for_tests; production sign is fully implemented per CD-6)
    - shared/src/bip322/p2tr.rs (after 15-02 — confirm pub(crate) fn sign body is todo!() and sign_for_tests exists)
    - shared/src/bip322/p2sh_p2wpkh.rs (after 15-02 — confirm pub(crate) fn sign body is todo!() and sign_for_tests exists)
    - shared/Cargo.toml (after 15-02 — confirm bip322 = "=0.0.10" + thiserror = workspace; proptest NOT yet present)
    - Cargo.toml (workspace root line 28 — confirm proptest = "1" workspace pin)
    - .planning/phases/15-shared-crate-multi-script-contract/15-CONTEXT.md (D-33 vendored snapshot rules: header recording source + commit SHA + capture date; D-34 supplement requirement implied via RESEARCH Pitfall 6; CD-10 atomic commit ordering — this is the THIRD commit in CD-10)
    - .planning/phases/15-shared-crate-multi-script-contract/15-RESEARCH.md §"Common Pitfalls" Pitfall 6 (upstream basic-test-vectors.json has 0 P2SH-P2WPKH cases; supplement from bip322 crate's lib.rs:46-48 + lib.rs:299-321 is the v1.4 minimum); §"Architectural Responsibility Map" row "Test fixtures"
    - .planning/phases/15-shared-crate-multi-script-contract/15-PATTERNS.md (the fixtures sections at lines 530-557; PATTERNS.md "Key Insight" lines 770-779)
    - ~/.cargo/registry/src/index.crates.io-*/bip322-0.0.10/src/lib.rs (lines 46-48 NESTED_SEGWIT_ADDRESS + NESTED_SEGWIT_PRIVATE_KEY constants; lines 299-321 the two P2SH-P2WPKH test cases simple_sign_p2sh_p2wpkh + roundtrip_p2sh_p2wpkh_simple. Lift the message + signature constants verbatim into the supplement JSON.)
  </read_first>
  <behavior>
    The fetched basic-test-vectors.json file deserialises as a JSON object with top-level keys including `simple` (an array). It contains at least 1 P2WPKH and 1 P2TR vector entry. The supplement contains at least 1 P2SH-P2WPKH entry whose `address`, `private_keys`, `message`, and `bip322_signatures` fields are lifted from the bip322 crate's test constants. The README.md records source URL, commit SHA, capture date, and curl command for both fixtures. The #[cfg(test)] pub fn sign_simple_test_only in shared/src/bip322/mod.rs compiles without changing the production API surface and is callable from shared/tests/ integration tests.

    Sanity checks (executor runs locally before declaring done):
    - `jq '.simple | length' shared/tests/fixtures/bip322/basic-test-vectors.json` returns an integer >= 2
    - `jq '[.simple[] | select(.address | startswith("bc1q"))] | length' shared/tests/fixtures/bip322/basic-test-vectors.json` returns >= 1 (P2WPKH count)
    - `jq '[.simple[] | select(.address | startswith("bc1p"))] | length' shared/tests/fixtures/bip322/basic-test-vectors.json` returns >= 1 (P2TR count)
    - `jq 'length' shared/tests/fixtures/bip322/p2sh_p2wpkh_supplement.json` returns >= 1 (P2SH-P2WPKH count)
    - `jq -r '.[0].type' shared/tests/fixtures/bip322/p2sh_p2wpkh_supplement.json` returns `"p2sh-p2wpkh"`
  </behavior>
  <action>
    Vendor the upstream BIP-322 test vectors. Use curl to fetch https://raw.githubusercontent.com/bitcoin/bips/master/bip-0322/basic-test-vectors.json (or pin to a specific commit SHA — the executor should run `git ls-remote https://github.com/bitcoin/bips refs/heads/master` to capture the current HEAD SHA before downloading, then download via that SHA in the URL: `https://raw.githubusercontent.com/bitcoin/bips/<SHA>/bip-0322/basic-test-vectors.json`). Save to shared/tests/fixtures/bip322/basic-test-vectors.json. Do NOT modify the file contents; preserve the upstream JSON shape verbatim (the JSON file itself does NOT carry a comment line — provenance lives in the sibling README.md per D-33).

    Create shared/tests/fixtures/bip322/p2sh_p2wpkh_supplement.json as a JSON array containing 1-2 supplement entries. Lift the test constants from the bip322 v0.0.10 crate at ~/.cargo/registry/src/index.crates.io-*/bip322-0.0.10/src/lib.rs. Specifically: NESTED_SEGWIT_ADDRESS at line 46 (the "3HSVzEhCFuH9Z3wvoWTexy7BMVVp3PjS6f" mainnet address); NESTED_SEGWIT_PRIVATE_KEY at line 47-48 (the WIF). The crate's lib.rs:299-321 contains two #[test] fns (simple_sign_p2sh_p2wpkh + roundtrip_p2sh_p2wpkh_simple) — each test signs a specific message and produces a specific base64 signature. Read those test bodies to extract: the message string, the signed witness as base64 (either from the test's expected output, or generated at test-runtime via the bip322 crate's create_to_sign + sign_simple — but for the JSON supplement file, prefer to capture them statically by examining the crate's test outputs at the source). If the crate tests assert against constant signatures, use those constants; if the tests assert via roundtrip-style "sign then verify" without a fixed expected signature, run the bip322 crate's sign_simple at executor time and capture the produced witness as base64 into the supplement file (cargo-fmt the JSON). Each supplement entry MUST contain: `message` (String), `private_keys` (array of one WIF string), `address` (mainnet 3... string), `type` (literal "p2sh-p2wpkh"), `witness_script` (empty String ""), `bip322_signatures` (array of one base64 string).

    Create shared/tests/fixtures/bip322/README.md with the provenance metadata. Format per the <interfaces> block specification: # BIP-322 test vector vendor provenance header, then two ## sections (basic-test-vectors.json + p2sh_p2wpkh_supplement.json) with Source URL, capture date (TODAY's date), upstream commit SHA, and curl command for the basic-test-vectors.json; for the supplement record the bip322 crate version + lib.rs line refs + reason (upstream gap per RESEARCH Pitfall 6).

    Add proptest = { workspace = true } to shared/Cargo.toml under a new [dev-dependencies] section (currently absent). The workspace already pins proptest = "1" at root Cargo.toml:28; the per-crate declaration uses workspace re-export per Shared Pattern 4. Do NOT add proptest to [dependencies] — it is dev-only.

    Add a #[cfg(test)] pub fn sign_simple_test_only to shared/src/bip322/mod.rs that mirrors sign_simple but routes:
      ScriptType::P2wpkh -> p2wpkh::sign(spk, key, message)
      ScriptType::P2tr -> Ok(p2tr::sign_for_tests(spk, key, message))
      ScriptType::P2shP2wpkh -> Ok(p2sh_p2wpkh::sign_for_tests(spk, key, message))
    Note that p2tr::sign_for_tests and p2sh_p2wpkh::sign_for_tests currently return Witness (not Result) per RESEARCH Pattern 4; wrap them in Ok(). The sign_simple_test_only signature is identical to sign_simple. This fn is gated by #[cfg(test)] so it is NOT part of the production API surface and does NOT contradict D-27's dispatcher-only invariant for production code. It exists solely to give integration tests in shared/tests/ a single callable that drives end-to-end sign↔verify for all three script types. Decision IDs referenced: D-27 (production-only dispatcher; #[cfg(test)] mirror is acceptable per RESEARCH Pattern 4), CD-6 (extends test-path with the consolidated dispatcher), D-33 (vendored fixture rules), RESEARCH Pitfall 6 (supplement rationale).
  </action>
  <verify>
    <automated>test -s shared/tests/fixtures/bip322/basic-test-vectors.json &amp;&amp; test -s shared/tests/fixtures/bip322/p2sh_p2wpkh_supplement.json &amp;&amp; test -s shared/tests/fixtures/bip322/README.md &amp;&amp; jq '.simple | length' shared/tests/fixtures/bip322/basic-test-vectors.json &amp;&amp; jq 'length' shared/tests/fixtures/bip322/p2sh_p2wpkh_supplement.json &amp;&amp; jq -r '.[0].type' shared/tests/fixtures/bip322/p2sh_p2wpkh_supplement.json | grep -F 'p2sh-p2wpkh' &amp;&amp; grep -E 'proptest\s*=\s*\{\s*workspace\s*=\s*true' shared/Cargo.toml &amp;&amp; grep -E '#\[cfg\(test\)\]\s*pub fn sign_simple_test_only|pub fn sign_simple_test_only' shared/src/bip322/mod.rs &amp;&amp; cargo build -p shared --tests 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - shared/tests/fixtures/bip322/basic-test-vectors.json exists, is non-empty, and parses as valid JSON (`jq . shared/tests/fixtures/bip322/basic-test-vectors.json` exits 0)
    - `jq '.simple | length' shared/tests/fixtures/bip322/basic-test-vectors.json` outputs an integer >= 2
    - `jq '[.simple[] | select(.address | startswith("bc1q") or startswith("tb1q"))] | length' shared/tests/fixtures/bip322/basic-test-vectors.json` outputs >= 1 (at least one P2WPKH vector)
    - `jq '[.simple[] | select(.address | startswith("bc1p") or startswith("tb1p"))] | length' shared/tests/fixtures/bip322/basic-test-vectors.json` outputs >= 1 (at least one P2TR vector)
    - shared/tests/fixtures/bip322/p2sh_p2wpkh_supplement.json exists, is non-empty, and parses as valid JSON
    - `jq 'length' shared/tests/fixtures/bip322/p2sh_p2wpkh_supplement.json` outputs >= 1
    - `jq -r '.[0].type' shared/tests/fixtures/bip322/p2sh_p2wpkh_supplement.json` outputs `p2sh-p2wpkh` (kebab-case literal)
    - shared/tests/fixtures/bip322/README.md exists, contains the literal substring `bitcoin/bips`, contains a capture date of the form `20YY-MM-DD`, and contains the literal substring `curl`
    - shared/Cargo.toml contains a [dev-dependencies] section that includes `proptest = { workspace = true }`
    - shared/src/bip322/mod.rs contains the literal line `pub fn sign_simple_test_only` AND it is preceded (within 3 lines above) by `#[cfg(test)]`
    - `cargo build -p shared --tests` exits 0 (the new fn compiles; the new fixtures are picked up by include_str! when 15-03 Tasks 2-3 land them in the test files)
    - `cargo audit` exits 0 (proptest enters the dev-dep chain cleanly)
  </acceptance_criteria>
  <done>The vendored basic-test-vectors.json + p2sh_p2wpkh_supplement.json + provenance README.md are in place at shared/tests/fixtures/bip322/; proptest workspace dev-dep is added to shared/Cargo.toml; the #[cfg(test)] sign_simple_test_only dispatcher mirror is exposed at shared::bip322 so integration tests can drive end-to-end sign↔verify for all three script types without violating the production dispatcher-only public surface (D-27).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create shared/tests/per_script_vectors.rs — per-script positive sign↔verify tests against the vendored fixture + P2SH-P2WPKH supplement, covering P2WPKH + P2TR + P2SH-P2WPKH per BIP322-04</name>
  <files>shared/tests/per_script_vectors.rs</files>
  <read_first>
    - shared/tests/fixtures/bip322/basic-test-vectors.json (after Task 1 — confirm the parsed structure matches the schema expected by the harness)
    - shared/tests/fixtures/bip322/p2sh_p2wpkh_supplement.json (after Task 1 — confirm shape)
    - shared/src/bip322/mod.rs (after 15-02 + Task 1 — confirm public API surface: ScriptType, verify_simple, sign_simple, sign_simple_test_only; confirm Bip322Error variant set)
    - shared/src/bip322/p2wpkh.rs (after 15-02 — confirm pub(crate) fn sign is the fully-implemented body)
    - .planning/phases/15-shared-crate-multi-script-contract/15-CONTEXT.md (D-33 fixture loading via include_str!; D-34 cross-shape matrix is in a SEPARATE file from positive vectors)
    - .planning/phases/15-shared-crate-multi-script-contract/15-RESEARCH.md §"Phase Requirements" BIP322-04 (P2SH-P2WPKH supplement gap); A3 disposition (run all positive tests on the green path before declaring done); A10 (arity check pre-flight redundancy is acceptable)
    - .planning/phases/15-shared-crate-multi-script-contract/15-PATTERNS.md (per_script_vectors.rs section at lines 504-528; Shared Pattern 6 test fn naming as documentation)
  </read_first>
  <behavior>
    Integration test file at shared/tests/per_script_vectors.rs imports the dispatcher API. Loads both fixture files at compile time via include_str!. Parses with serde_json::Value to handle the optional `type` field on supplement entries.

    Test fns (separate #[test] per script type):
    - test_p2wpkh_vectors_verify_via_dispatcher: iterates basic-test-vectors.json simple entries where address starts with bc1q or tb1q, reconstructs the witness from each bip322_signatures[0] (base64 -> bytes -> bitcoin::Witness via consensus deserialize), recovers spk via Address::from_str + script_pubkey, calls shared::bip322::verify_simple(ScriptType::P2wpkh, &spk, &witness, message.as_bytes(), Network::Bitcoin) and asserts Ok(()). Asserts at least 1 vector ran (so a future fixture bump that drops P2WPKH is caught).
    - test_p2tr_vectors_verify_via_dispatcher: same pattern for bc1p / tb1p addresses, ScriptType::P2tr.
    - test_p2sh_p2wpkh_supplement_verify_via_dispatcher: iterates p2sh_p2wpkh_supplement.json top-level array, ScriptType::P2shP2wpkh.
    - test_p2wpkh_sign_verify_roundtrip_via_dispatcher: generates a deterministic key (SecretKey::from_slice(&[0x05; 32])), derives the P2WPKH SPK, calls shared::bip322::sign_simple(ScriptType::P2wpkh, ...) for a known message, then verify_simple with the produced witness; asserts Ok(()).
    - test_p2tr_sign_verify_roundtrip_via_dispatcher: same pattern but using sign_simple_test_only (because production sign_simple's P2TR branch is todo!() per CD-6), with the deterministic key derived to a P2TR SPK via tap_tweak + XOnlyPublicKey + ScriptBuf::new_p2tr.
    - test_p2sh_p2wpkh_sign_verify_roundtrip_via_dispatcher: same pattern but for P2SH-P2WPKH using sign_simple_test_only.

    Helper fns are defined inline at the top of the file (no shared/tests/common/ submodule — single file per the per_script_vectors.rs target). Helpers include base64_to_witness (uses base64 0.22 + bitcoin consensus decode), address_to_spk (uses bitcoin::Address::from_str), classify_simple_entry (returns Option<ScriptType> based on address prefix or explicit type field).

    Sanity checks (executor runs locally before declaring done):
    - cargo test -p shared --test per_script_vectors exits 0
    - cargo test -p shared --test per_script_vectors -- --nocapture shows each test reporting the count of vectors it processed (so a future fixture-drop bug is visible in CI logs)
  </behavior>
  <action>
    Create shared/tests/per_script_vectors.rs as an integration test file (external crate per Cargo convention; imports shared::* paths). At the top: `const BASIC_VECTORS: &str = include_str!("fixtures/bip322/basic-test-vectors.json");` and `const P2SH_P2WPKH_SUPPLEMENT: &str = include_str!("fixtures/bip322/p2sh_p2wpkh_supplement.json");` per D-33.

    Implement helper fns (each is a free fn at module scope):
    - `fn classify_address(addr: &str, explicit_type: Option<&str>) -> Option<shared::bip322::ScriptType>` — returns the script type from explicit `type` field if present (matches "p2sh-p2wpkh" -> Some(P2shP2wpkh); "p2wpkh" -> Some(P2wpkh); "p2tr" -> Some(P2tr)), else from address prefix (bc1q|tb1q -> P2wpkh; bc1p|tb1p -> P2tr; 3|2 [mainnet/testnet P2SH] -> P2shP2wpkh), else None (skip the entry)
    - `fn address_to_spk(addr: &str) -> Option<bitcoin::ScriptBuf>` — uses bitcoin::Address::from_str(addr).require_network(bitcoin::Network::Bitcoin) for mainnet bc1*/3*, with fallback to Network::Signet for tb1*/2*; returns None if parse fails
    - `fn base64_to_witness(b64: &str) -> Option<bitcoin::Witness>` — uses base64::engine::general_purpose::STANDARD.decode + bitcoin::consensus::deserialize::<bitcoin::Witness>; returns None on any decode error

    Implement the six #[test] fns described in the behavior block. Each test asserts at least 1 vector ran (a count >= 1 assertion at the end of the iteration) so a future fixture bump that drops a script type is caught at CI time. For the sign↔verify roundtrip tests, use a deterministic SecretKey (e.g., SecretKey::from_slice(&[0x05; 32]) for P2WPKH; &[0x06; 32] for P2TR; &[0x07; 32] for P2SH-P2WPKH) and a known message (e.g., "blindjoin:15-03:per-script-vector-test"). Use Network::Bitcoin for the upstream vectors (the addresses are mainnet); use Network::Regtest for the deterministic round-trip tests.

    Per RESEARCH A3 disposition: after writing the tests, run them locally and confirm each iterates at least one vector. If basic-test-vectors.json has the expected structure but classify_address returns None for some entries (e.g., a P2WSH multisig case from the upstream `simple` array per RESEARCH §"Pitfall 6"), skip them silently — only positive coverage of P2WPKH + P2TR + P2SH-P2WPKH is required by BIP322-04. Do NOT introduce a Bip322Error reference in the positive-vector tests — those are for the cross-shape matrix in Task 3. Decision IDs referenced: D-33 (compile-time fixture loading), CD-6 (sign_simple_test_only for P2TR/P2SH-P2WPKH roundtrip), RESEARCH A3 (positive coverage required for all three script types), RESEARCH Pitfall 6 (P2SH-P2WPKH supplement consumed here), Network::Bitcoin per RESEARCH Pitfall 5 (SPK is network-agnostic at byte layer).
  </action>
  <verify>
    <automated>test -f shared/tests/per_script_vectors.rs &amp;&amp; grep -c '^#\[test\]' shared/tests/per_script_vectors.rs &amp;&amp; cargo test -p shared --test per_script_vectors 2>&amp;1 | tee /tmp/15-03-task2-test.log &amp;&amp; grep -E '(test result: ok|test result: FAILED)' /tmp/15-03-task2-test.log &amp;&amp; cargo test --test full_round 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - shared/tests/per_script_vectors.rs exists as a regular file
    - `grep -c '^#\[test\]' shared/tests/per_script_vectors.rs` outputs at least `6`
    - shared/tests/per_script_vectors.rs contains all six test fn names: `test_p2wpkh_vectors_verify_via_dispatcher`, `test_p2tr_vectors_verify_via_dispatcher`, `test_p2sh_p2wpkh_supplement_verify_via_dispatcher`, `test_p2wpkh_sign_verify_roundtrip_via_dispatcher`, `test_p2tr_sign_verify_roundtrip_via_dispatcher`, `test_p2sh_p2wpkh_sign_verify_roundtrip_via_dispatcher`
    - shared/tests/per_script_vectors.rs contains `include_str!("fixtures/bip322/basic-test-vectors.json")` AND `include_str!("fixtures/bip322/p2sh_p2wpkh_supplement.json")`
    - shared/tests/per_script_vectors.rs imports the public dispatcher API ONLY (use shared::bip322::{ScriptType, verify_simple, sign_simple, sign_simple_test_only}; NO direct use of shared::bip322::p2wpkh, p2tr, or p2sh_p2wpkh modules — those are pub(crate) and inaccessible)
    - `cargo test -p shared --test per_script_vectors` exits 0 with `test result: ok. 6 passed` (or more, if extra helper test fns were added)
    - Each #[test] fn iterates at least 1 vector (asserted via a count >= 1 in the test body)
    - `cargo test --test full_round` exits 0 (cross-phase invariant)
  </acceptance_criteria>
  <done>shared/tests/per_script_vectors.rs runs the BIP322-04 positive vectors against the vendored basic-test-vectors.json + supplement, covering P2WPKH + P2TR + P2SH-P2WPKH; sign↔verify roundtrip works for all three script types via sign_simple (P2WPKH) + sign_simple_test_only (P2TR + P2SH-P2WPKH); each test asserts at least one vector ran; full_round integration suite stays green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create shared/tests/bip322_cross_shape.rs — 9 enumerated #[test] fns per D-34 verbatim with specific Bip322Error variant assertions; close V1.4-CRIT-01 statically</name>
  <files>shared/tests/bip322_cross_shape.rs</files>
  <read_first>
    - shared/src/bip322/mod.rs (after Task 1 — confirm Bip322Error variants for matches!() patterns, ScriptType variants, and the dispatcher API)
    - shared/src/bip322/p2wpkh.rs (after 15-02 — arity check is witness.len() != 2 -> InvalidWitnessLength { expected: 2, got })
    - shared/src/bip322/p2tr.rs (after 15-02 — arity check is witness.len() != 1 -> InvalidWitnessLength { expected: 1, got })
    - shared/src/bip322/p2sh_p2wpkh.rs (after 15-02 — arity check is witness.len() != 2 -> InvalidWitnessLength { expected: 2, got })
    - .planning/phases/15-shared-crate-multi-script-contract/15-CONTEXT.md (D-34 verbatim — the 9 enumerated names + the design rationale; cross-shape matrix is the V1.4-CRIT-01 mitigation)
    - .planning/phases/15-shared-crate-multi-script-contract/15-RESEARCH.md (§"Code Examples" Example 6 lines 912-933 — the matches!()-on-error pattern; A3 disposition — all 9 tests MUST run on the green path AND assert specific variants)
    - .planning/phases/15-shared-crate-multi-script-contract/15-PATTERNS.md (the bip322_cross_shape.rs section at lines 448-501; Shared Pattern 1 thiserror derive + matches!() patterns; Shared Pattern 6 test fn naming as documentation)
    - coordinator/src/bitcoin/utxo.rs (around lines 213-236 — analog structural twin for matches!()-on-error pattern; bip322_wrong_witness_length is the closest existing test in the repo)
  </read_first>
  <behavior>
    Integration test file at shared/tests/bip322_cross_shape.rs with EXACTLY 9 #[test] fns, named verbatim per D-34. Each test:
    1. Constructs a known SPK of one type using a deterministic helper.
    2. Constructs a known witness of a DIFFERENT type (or empty for arity edge cases).
    3. Calls shared::bip322::verify_simple(declared_type, &spk, &witness, b"test", Network::Regtest).
    4. Asserts a specific Bip322Error variant via matches!().

    Helper fns are defined inline at the top of the file. Specifically:
    - fn make_known_p2wpkh_spk() -> bitcoin::ScriptBuf — deterministic key SecretKey::from_slice(&[0x10; 32]) + PublicKey::from_secret_key + wpubkey_hash + ScriptBuf::new_p2wpkh
    - fn make_known_p2tr_spk() -> bitcoin::ScriptBuf — deterministic key SecretKey::from_slice(&[0x11; 32]) + Keypair + XOnlyPublicKey + tap_tweak + ScriptBuf::new_p2tr (use untweaked branch to keep it simple; the bip322 crate's verify will fail anyway since these are CROSS-SHAPE tests, not roundtrip tests)
    - fn make_known_p2sh_p2wpkh_spk() -> bitcoin::ScriptBuf — deterministic key SecretKey::from_slice(&[0x12; 32]) + wpubkey_hash + ScriptBuf::new_p2wpkh (the unwrapped redeem) + script_hash via ScriptBuf::script_hash + ScriptBuf::new_p2sh
    - fn make_p2wpkh_shaped_witness() -> bitcoin::Witness — 2 elements: a 72-byte dummy DER sig + a 33-byte dummy compressed pubkey (does not need to be valid; the arity is what's tested)
    - fn make_p2tr_shaped_witness() -> bitcoin::Witness — 1 element: a 64-byte dummy buffer
    - fn make_p2sh_p2wpkh_shaped_witness() -> bitcoin::Witness — same shape as make_p2wpkh_shaped_witness (2 elements)
    - fn make_empty_witness() -> bitcoin::Witness — Witness::new() with 0 elements

    Per the <interfaces> block expected-variant table:
    - reject_p2wpkh_spk_with_p2tr_witness: matches!(result, Err(Bip322Error::InvalidWitnessLength { expected: 2, got: 1 }))
    - reject_p2wpkh_spk_with_p2sh_p2wpkh_witness: matches!(result, Err(Bip322Error::CrateVerifyFailed { .. })) — arity at 2 matches; the crate's verify_full_p2wpkh fails on signature/HASH160
    - reject_p2tr_spk_with_p2wpkh_witness: matches!(result, Err(Bip322Error::InvalidWitnessLength { expected: 1, got: 2 }))
    - reject_p2tr_spk_with_p2sh_p2wpkh_witness: matches!(result, Err(Bip322Error::InvalidWitnessLength { expected: 1, got: 2 }))
    - reject_p2sh_p2wpkh_spk_with_p2wpkh_witness: matches!(result, Err(Bip322Error::CrateVerifyFailed { .. })) — arity at 2 matches; the crate's verify_full_p2wpkh(is_p2sh=true) fails on HASH160 cross-check (dummy pubkey does not hash to P2SH SPK's hash)
    - reject_p2sh_p2wpkh_spk_with_p2tr_witness: matches!(result, Err(Bip322Error::InvalidWitnessLength { expected: 2, got: 1 }))
    - reject_p2wpkh_spk_with_empty_witness: matches!(result, Err(Bip322Error::InvalidWitnessLength { expected: 2, got: 0 }))
    - reject_p2tr_spk_with_empty_witness: matches!(result, Err(Bip322Error::InvalidWitnessLength { expected: 1, got: 0 }))
    - reject_p2sh_p2wpkh_spk_with_empty_witness: matches!(result, Err(Bip322Error::InvalidWitnessLength { expected: 2, got: 0 }))

    Per RESEARCH A3 disposition: ALL 9 tests MUST be run and ALL 9 MUST assert specific variants (not just `assert!(result.is_err())` — that allows silent acceptance of the wrong rejection class, which is the V1.4-CRIT-01 vector). Use `matches!(result, Err(Bip322Error::VARIANT { ... }))` patterns explicitly. Provide a clear failure message on assert! so a future regression that flips an arity check to permissive surfaces in the test output (e.g., `assert!(matches!(...), "expected InvalidWitnessLength {{ expected: 2, got: 1 }}, got {result:?}")`).
  </behavior>
  <action>
    Create shared/tests/bip322_cross_shape.rs as an integration test file (external crate per Cargo convention; imports shared::bip322::*). At the top of the file, define the helper fns described in the behavior block.

    Implement the 9 #[test] fns with the exact names from D-34 verbatim (the function names matter — a code reviewer scanning the file sees which CRIT-01 spoofing vector each test closes by reading the fn name). For each test, follow the structural pattern from RESEARCH Example 6 (lines 912-933 of 15-RESEARCH.md):

      #[test]
      fn reject_<spk>_spk_with_<witness>_witness() {
          let spk = make_known_<spk>_spk();
          let witness = make_<witness>_shaped_witness();  // or make_empty_witness() for arity edge cases
          let result = shared::bip322::verify_simple(
              shared::bip322::ScriptType::<DeclaredType>,
              &spk,
              &witness,
              b"test",
              bitcoin::Network::Regtest,
          );
          assert!(
              matches!(result, Err(shared::bip322::Bip322Error::<ExpectedVariant>)),
              "expected <ExpectedVariant>, got {result:?}"
          );
      }

    For the `<DeclaredType>`: per the test name, the caller declares the type of the SPK (e.g., reject_p2wpkh_spk_with_p2tr_witness declares ScriptType::P2wpkh). This is the V1.4-CRIT-01 spoofing surface: the test simulates an attacker calling verify_simple with one type but a witness for another type.

    For the `<ExpectedVariant>`: use the matches!() patterns from the <interfaces> block expected-variant table.

    For the make_known_p2tr_spk helper, use the tap_tweak path so the resulting SPK matches what bitcoin::Address::p2tr would produce. Specifically:
      let secp = Secp256k1::new();
      let key = SecretKey::from_slice(&[0x11; 32]).unwrap();
      let (xonly, _parity) = Keypair::from_secret_key(&secp, &key).x_only_public_key();
      let tweaked = xonly.tap_tweak(&secp, None).0;
      ScriptBuf::new_p2tr_tweaked(tweaked)
    OR the simpler ScriptBuf::new_p2tr(&secp, xonly, None) which does the tweaking internally.

    Run the tests locally. If any test fails to produce the expected variant (e.g., CrateVerifyFailed surfaces a different error class than expected), update the matches!() pattern to the actual variant produced by the green-path run — but document why in a comment, and confirm the variant is still in the rejection class set described by RESEARCH A3 (the V1.4-CRIT-01 mitigation requires SOME specific rejection, not silent acceptance; the choice between InvalidWitnessLength vs CrateVerifyFailed vs UnrecognisedScriptPubkey is acceptable as long as the test still proves rejection).

    Do NOT use proptest! macros — D-34's rationale rejects proptest for the cross-shape matrix because "failures localise to one function name; no proptest shrink output to interpret". The 9 tests are explicit #[test] fns. The proptest workspace dev-dep added in Task 1 is reserved for future BIP322-04 extensions. Decision IDs referenced: D-34 (verbatim 9-name list), RESEARCH A3 (specific-variant assertions enforced), V1.4-CRIT-01 (this file IS the static mitigation), CD-10 (atomic commit ordering — this is the third commit in CD-10).
  </action>
  <verify>
    <automated>test -f shared/tests/bip322_cross_shape.rs &amp;&amp; grep -c '^#\[test\]' shared/tests/bip322_cross_shape.rs &amp;&amp; cargo test -p shared --test bip322_cross_shape 2>&amp;1 | tee /tmp/15-03-task3-test.log &amp;&amp; grep -E 'test result: ok\. 9 passed' /tmp/15-03-task3-test.log &amp;&amp; cargo test --test full_round 2>&amp;1 | tail -40 &amp;&amp; cargo test -p shared 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - shared/tests/bip322_cross_shape.rs exists as a regular file
    - `grep -c '^#\[test\]' shared/tests/bip322_cross_shape.rs` outputs exactly `9` (no more, no fewer — D-34 enumeration is exact)
    - shared/tests/bip322_cross_shape.rs contains ALL 9 EXACT test fn names from D-34: `reject_p2wpkh_spk_with_p2tr_witness`, `reject_p2wpkh_spk_with_p2sh_p2wpkh_witness`, `reject_p2tr_spk_with_p2wpkh_witness`, `reject_p2tr_spk_with_p2sh_p2wpkh_witness`, `reject_p2sh_p2wpkh_spk_with_p2wpkh_witness`, `reject_p2sh_p2wpkh_spk_with_p2tr_witness`, `reject_p2wpkh_spk_with_empty_witness`, `reject_p2tr_spk_with_empty_witness`, `reject_p2sh_p2wpkh_spk_with_empty_witness`
    - shared/tests/bip322_cross_shape.rs imports the public dispatcher API ONLY (use shared::bip322::{ScriptType, verify_simple, Bip322Error}; NO direct use of per-script pub(crate) modules)
    - Every #[test] fn body contains exactly one call to `shared::bip322::verify_simple(` AND exactly one `matches!(` invocation against a specific `Bip322Error::<Variant>` pattern (no `assert!(result.is_err())` shortcut allowed per RESEARCH A3)
    - shared/tests/bip322_cross_shape.rs does NOT contain `proptest!` macro invocations (D-34 explicitly rejects proptest for the cross-shape matrix)
    - `cargo test -p shared --test bip322_cross_shape` exits 0 with `test result: ok. 9 passed; 0 failed`
    - `cargo test -p shared` (full shared crate test suite: lib tests + ownership_proof_roundtrip + per_script_vectors + bip322_cross_shape) exits 0
    - `cargo test --test full_round` exits 0 (cross-phase invariant)
    - `cargo build --workspace` exits 0
    - `cargo audit` exits 0 (proptest + bip322 + thiserror + base64 + snafu + snafu-derive all clean)
  </acceptance_criteria>
  <done>shared/tests/bip322_cross_shape.rs contains the 9 enumerated #[test] fns per D-34 verbatim; every test asserts a specific Bip322Error variant via matches!() (no silent-acceptance shortcuts per RESEARCH A3); V1.4-CRIT-01 (script-type spoofing) is now statically provable at the shared/ crate boundary — Phase 16's coordinator-side cross-check between declared script_type and on-chain SPK (D-10 / ADVERT-03) adds the second defensive layer; v1.3 cross-phase invariant holds; this plan ships as the THIRD atomic commit in the CD-10 sequence (15-01 -> 15-02 -> 15-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream bitcoin/bips repo -> vendored basic-test-vectors.json | Supply-chain boundary: the fixture is fetched once, vendored at a commit SHA, and committed to the repo. Future drift is caught by re-vendoring (an explicit code change reviewable in PR) — not by CI fetch at build time per D-33. |
| bip322 v0.0.10 crate (source-of-truth for P2SH-P2WPKH supplement) -> vendored p2sh_p2wpkh_supplement.json | Same supply-chain boundary: the supplement is lifted from the crate's test constants at executor time and pinned in the repo. If the crate updates and changes the constants, the supplement stays stable (pinned to =0.0.10). |
| integration test (external crate) -> shared::bip322 public surface | Integration tests cannot reach pub(crate) helpers; they go through the dispatcher only. The #[cfg(test)] sign_simple_test_only mirror in mod.rs is the explicit test-only escape hatch and is documented as such. |
| 9 cross-shape rejection tests -> Bip322Error variant assertions | Each test asserts a specific variant via matches!(); silent acceptance of the wrong rejection class is a V1.4-CRIT-01 regression and would surface as a test failure (RESEARCH A3 disposition). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03-V1.4-CRIT-01 | Spoofing, Elevation of Privilege | shared::bip322::verify_simple cross-shape behaviour | mitigate | 9 enumerated #[test] fns per D-34 verbatim, each asserting a specific Bip322Error variant via matches!() (RESEARCH A3 enforced: assert!(result.is_err()) shortcuts are forbidden). The 9 tests cover every off-diagonal (script_pubkey × witness-shape) cell + the 3 arity-edge empty-witness cells. Combined with D-27's pub(crate)-only per-script files (15-02), V1.4-CRIT-01 is statically unreachable at the shared/ crate boundary. Phase 16's coordinator-side declared-vs-derived cross-check (D-10 / ADVERT-03) adds the runtime defensive layer. |
| T-15-03-V1.4-CRIT-02 | Tampering | per-script sighash logic | mitigate | Per-script positive-vector tests in shared/tests/per_script_vectors.rs run against the vendored basic-test-vectors.json + p2sh_p2wpkh_supplement.json. Each script type independently passes its vectors (P2WPKH, P2TR, P2SH-P2WPKH). The bip322 = "=0.0.10" crate's correctness is the upstream defence; our per-script property tests are the primary defence against silent sighash regressions per V1.4-CRIT-02. If the bip322 crate ships a regression at a future pin bump (v1.5+ trigger), per_script_vectors.rs surfaces it immediately. |
| T-15-03-V1.4-MOD-07 | Tampering | bip322_message_hash single-source-of-truth | mitigate | Every per-script verifier (15-02) calls super::bip322_message_hash via the dispatcher; the upstream vectors validate this primitive byte-for-byte against bitcoin/bips canonical implementation. If V1.4-MOD-07 ever regresses (e.g., a typo in the BIP0322 tag string), per_script_vectors.rs's upstream-vector pass fails. |
| T-15-03-FixtureDrift | Tampering | Vendored basic-test-vectors.json | mitigate | D-33 mandates vendoring at a commit SHA recorded in shared/tests/fixtures/bip322/README.md. The fixture is `include_str!`-ed at compile time, so a tampered fixture would change the compiled binary's behaviour and be caught by code review of the JSON diff. CI does NOT fetch at build time (RESEARCH supply-chain hardening). |
| T-15-03-SupplementProvenance | Repudiation (supply-chain) | P2SH-P2WPKH supplement | accept (with provenance) | The supplement is lifted from the bip322 v0.0.10 crate's lib.rs:46-48 + lib.rs:299-321 test constants. Provenance is recorded in shared/tests/fixtures/bip322/README.md. A future review can re-verify by re-reading the crate's lib.rs (the crate is pinned to =0.0.10 via CI grep gate from 15-02). Accept the v1.4 minimum coverage; v1.5 TEST-EXT-01 (cross-impl differential fixtures via bip322-js) is the gap-closer. |
| T-15-03-PII | Information Disclosure | Test failure output | mitigate | The matches!() assertion failure messages format the result Debug printing, which uses Bip322Error's thiserror-derived Display impl. Per 15-02 threat model, Bip322Error::Display includes only enum-payload metadata (u8 version, usize witness lengths, ScriptType, bitcoin::Network) — no UTXO outpoints, addresses, or keys. CI logs are PII-clean. |
| T-15-03-A3 | Tampering | Test assertion strength | mitigate | RESEARCH A3 disposition: every cross-shape test asserts a SPECIFIC variant (not just is_err()). The acceptance criteria enforce this via grep for matches!() patterns; the verify command runs the tests and confirms `test result: ok. 9 passed`. A silent-acceptance regression (e.g., a future refactor flips an arity check to a permissive default) would fail the test for the specific cell — pinpointing the regression to one function. |
| T-15-03-T-01-04 | Tampering | NO #[serde(deny_unknown_fields)] anywhere | accept (preserved) | The vendored fixture JSON files preserve their upstream schema; the test harness uses serde_json::Value (loose) to skip entries with unrecognised script types (P2WSH multisig from upstream `simple` array per RESEARCH Pitfall 6). Forward-compat invariant preserved. |
</threat_model>

<verification>
Phase-level checks after this plan lands (the full Phase 15 boundary):
- `cargo test -p shared --test ownership_proof_roundtrip` exits 0 (15-01 D-13 cases all pass)
- `cargo test -p shared --test per_script_vectors` exits 0 (BIP322-04 positive vectors all pass for P2WPKH + P2TR + P2SH-P2WPKH)
- `cargo test -p shared --test bip322_cross_shape` exits 0 with `test result: ok. 9 passed; 0 failed` (V1.4-CRIT-01 statically mitigated at shared/)
- `cargo test -p shared` exits 0 (full shared crate test suite: lib + 3 integration test targets)
- `cargo test -p coordinator --lib bitcoin::utxo` exits 0 (15-02 variant remap survives)
- `cargo test --test full_round` exits 0 (cross-phase invariant; v1.3 P2WPKH integration suite green)
- `cargo build --workspace` exits 0
- `cargo audit` exits 0 with 0 advisories (bip322 + snafu + snafu-derive + thiserror + base64 + proptest all clean)
- CI grep gate `bip322-pin-check` (15-02 Task 3) passes on the current tree
- The 9 cross-shape test names match D-34 verbatim (grep gate in 15-03 Task 3 acceptance criteria)
- The vendored fixture's commit SHA is recorded in shared/tests/fixtures/bip322/README.md and the JSON parses cleanly
</verification>

<success_criteria>
Plan 15-03 (and by extension the Phase 15 milestone boundary) is complete when:
1. BIP322-04 materially satisfied: per_script_vectors.rs covers P2WPKH (from upstream basic-test-vectors.json) + P2TR (from upstream basic-test-vectors.json) + P2SH-P2WPKH (from p2sh_p2wpkh_supplement.json per RESEARCH Pitfall 6); each test iterates >= 1 vector; sign↔verify roundtrip exercises all three script types via the dispatcher API + sign_simple_test_only mirror.
2. V1.4-CRIT-01 statically mitigated at shared/: bip322_cross_shape.rs contains the 9 enumerated #[test] fns per D-34 verbatim, every test asserts a specific Bip322Error variant via matches!() per RESEARCH A3; combined with 15-02's pub(crate)-only per-script API surface, an external caller cannot spoof script_type at the shared/ API layer.
3. D-33 vendored fixture: shared/tests/fixtures/bip322/basic-test-vectors.json is the pinned upstream snapshot; shared/tests/fixtures/bip322/p2sh_p2wpkh_supplement.json is the bip322 crate-lifted supplement; shared/tests/fixtures/bip322/README.md records source URL, commit SHA, capture date, and curl command for both.
4. proptest = { workspace = true } added to shared/Cargo.toml [dev-dependencies] per Phase 14 CARRY constraint + CONTEXT D-34 reference; cargo audit clean.
5. #[cfg(test)] sign_simple_test_only fn at shared/src/bip322/mod.rs gives integration tests an end-to-end sign↔verify path for P2TR and P2SH-P2WPKH (the production sign_simple branches stay todo!() per CD-6 + ADR #4; Phase 17 swaps in bdk).
6. v1.3 cross-phase invariant holds: `cargo test --test full_round` exits 0 at the Phase 15 boundary.
7. Atomic commit (the third in the CD-10 sequence: 15-01 -> 15-02 -> 15-03).
8. Phase 15 ROADMAP success criteria materially satisfied:
   - Criterion #1 (per-script sign↔verify round-trip property tests against vendored basic-test-vectors.json): satisfied via 15-03 Task 2.
   - Criterion #2 (9 cross-shape rejection combinations fail with expected Bip322Error variants): satisfied via 15-03 Task 3 with RESEARCH A3 enforcement.
   - Criterion #3 (wire-format roundtrip test ships FIRST per v1.3 REPAIR-01 lesson #1): satisfied via 15-01 atomic commit.
   - Criterion #4 (shared compiles with exact-pinned deps; Cargo.lock reflects no minor-version drift on bdk_wallet/bitcoin/bip322): satisfied via 15-02's bip322 = "=0.0.10" + CI grep gate; bdk_wallet caret-pin retighten is deferred per RESEARCH A7.
   - Criterion #5 (v1.3 P2WPKH-only full_round::* integration tests remain green): satisfied via cross-phase invariant gate enforced in 15-01 Task 3, 15-02 all tasks, 15-03 all tasks.
</success_criteria>

<output>
Create `.planning/phases/15-shared-crate-multi-script-contract/15-03-SUMMARY.md` when done. SUMMARY should record: the basic-test-vectors.json commit SHA + vendor date; the supplement provenance (bip322 crate version + lib.rs line refs); the count of P2WPKH + P2TR + P2SH-P2WPKH positive vectors actually exercised by per_script_vectors.rs (numerical, for future fixture-bump audits); the final variant-class table for the 9 cross-shape rejection tests (what variant each test actually asserts, in case the green-path run diverged from the <interfaces>-block predictions and the matches!() patterns were adjusted with documented justification); the final list of all Bip322Error variants exercised across all three Phase 15 plans (D-31 coverage audit); whether the proptest dev-dep is used (any proptest! macros, or reserved-for-future); the cross-phase-invariant verification result (`full_round` green); and the Phase 15 milestone readiness call: Phase 16 (BIP322-* requirements 01..04 + ADVERT-04 mapped) can now derive against the shared::bip322 dispatcher contract without re-litigating the wire shape or the verify dispatch.
</output>
