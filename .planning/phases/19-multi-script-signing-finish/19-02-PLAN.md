---
phase: 19-multi-script-signing-finish
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - shared/src/bip322/mod.rs
  - shared/src/bip322/p2tr.rs
  - shared/src/bip322/p2sh_p2wpkh.rs
  - shared/src/bip322/p2wpkh.rs
  - shared/tests/per_script_vectors.rs
  - tests/integration/multi_script_validate.rs
  - tests/integration/mod.rs
autonomous: true
requirements: [BIP322-07]
tags: [bip322, surface-shrink, audit-readiness, cleanup]

must_haves:
  truths:
    - "`#[doc(hidden)] pub fn sign_simple_test_only` is removed from `shared/src/bip322/mod.rs` — the V1.4-CRIT-01 dispatcher-only public surface has no test-only hole."
    - "No `pub(crate) fn sign_for_tests` remains in any of `p2tr.rs`, `p2sh_p2wpkh.rs`, or `p2wpkh.rs`."
    - "`shared/tests/per_script_vectors.rs` and `tests/integration/multi_script_validate.rs` call ONLY `sign_simple` (production dispatcher) — no test-only mirror callsites remain."
    - "All 9 cross-shape rejection cases in `tests/integration/multi_script_validate.rs` now exercise the production `sign_simple` path; all positive-vector tests in `shared/tests/per_script_vectors.rs` exercise production sign bodies."
    - "v1.3 invariant `full_round` 8/8 + v1.4 invariant `mixed_script_e2e` 1/1 still green; shared lib + per_script_vectors + bip322_cross_shape + wallet_sign_roundtrip all green; `cargo clippy --workspace --all-targets -- -D warnings` clean; 3 CI grep gates (`bip322-pin-check`, `crit-01-grep-check`, `crit-01-client-grep-check`) unchanged."
    - "CONTEXT decisions honored by this plan: D-107 (sign_simple return type stays Result<Witness, Bip322Error> after the test-only mirror is removed — dispatcher contract unchanged), D-108 (helper unit test from Plan 19-01 stays green), D-109 (helper `pub fn` sibling to sign_simple stays), D-110 (helper body unchanged), D-111 (cross-check stays in place — Plan 19-02 makes NO modifications to sign bodies), D-112 (Bip322Error::ScriptTypeMismatch variant remains the only error for spk↔key mismatches; PII-safety test stays green), D-113 (cross-check algorithm unchanged), D-114 (sign_schnorr_no_aux_rand unchanged), D-115 (no bdk_wallet API touched — Plan 19-02 is shared/-internal + test-file migration only), D-116 (production sign bodies from Plan 19-01 are the only sign path post-deletion — `sign_for_tests` mirror removed), D-117 (`_spk` → `spk` rename from Plan 19-01 unchanged), D-118 (P2TR parity test from Plan 19-01 stays green and now exercises a path with NO test-only escape), D-119 (P2SH-P2WPKH parity test stays green), D-120 (this IS Plan 19-02 — the second plan in the sequenced pair per CONTEXT § G), D-121 (wave 2 with depends_on [19-01] — strictly sequential after production sign body exists). CD-36 (variant doc-comment from Plan 19-01 stays — no further edits to ScriptTypeMismatch), CD-39 (refresh tests/integration/mod.rs:707,723 comments — default = fold into 19-02)."
  artifacts:
    - path: "shared/src/bip322/mod.rs"
      provides: "Public surface shrunk — sign_simple_test_only removed"
      contains: "pub fn sign_simple"
    - path: "shared/src/bip322/p2tr.rs"
      provides: "sign_for_tests deleted; only `pub(crate) fn sign` + `pub(crate) fn verify` remain"
    - path: "shared/src/bip322/p2sh_p2wpkh.rs"
      provides: "sign_for_tests deleted; only `pub(crate) fn sign` + `pub(crate) fn verify` remain"
    - path: "shared/src/bip322/p2wpkh.rs"
      provides: "Unused sign_for_tests alias deleted; production `pub(crate) fn sign` unchanged"
    - path: "shared/tests/per_script_vectors.rs"
      provides: "All callsites migrated to sign_simple; production sign bodies are now exercised by the positive-vector suite"
    - path: "tests/integration/multi_script_validate.rs"
      provides: "sign_witness helper now calls sign_simple; production sign witnesses verify against coordinator's CRIT-01 cross-check"
    - path: "tests/integration/mod.rs"
      provides: "Doc-comment refreshes — no stale references to removed sign_simple_test_only"
  key_links:
    - from: "shared/tests/per_script_vectors.rs::274 (P2TR positive-vector test)"
      to: "shared::bip322::sign_simple → p2tr::sign (production body from Plan 19-01 Task 1)"
      via: "dispatcher dispatch — the per-script-vector test now exercises production sign"
      pattern: "sign_simple\\(ScriptType::P2tr"
    - from: "shared/tests/per_script_vectors.rs::311 (P2SH-P2WPKH positive-vector test)"
      to: "shared::bip322::sign_simple → p2sh_p2wpkh::sign (production body from Plan 19-01 Task 2)"
      via: "dispatcher dispatch"
      pattern: "sign_simple\\(ScriptType::P2shP2wpkh"
    - from: "tests/integration/multi_script_validate.rs::sign_witness helper"
      to: "shared::bip322::sign_simple → per-script production bodies"
      via: "dispatcher call; 9 cross-shape rejection cases consume the helper"
      pattern: "sign_simple\\(\\s*handle\\.script_type"
---

<objective>
Close BIP322-07: delete `#[doc(hidden)] pub fn sign_simple_test_only` from `shared/src/bip322/mod.rs:274-314` and delete `pub(crate) fn sign_for_tests` from each of the three per-script modules (`p2tr.rs:46-95`, `p2sh_p2wpkh.rs:52-108`, `p2wpkh.rs:74-95`). Migrate ALL callers — 3 callsites in `shared/tests/per_script_vectors.rs` (1 import + 2 test bodies) + 3 callsites in `tests/integration/multi_script_validate.rs` (1 import + 1 helper-fn body with 2 changes) + 2 doc-comment refreshes in `tests/integration/mod.rs` — to the production dispatcher `sign_simple`. Verify zero residual references via grep and clippy.

Purpose: The V1.4-CRIT-01 dispatcher-only invariant becomes load-bearing at the TYPE LEVEL with no holes — `shared::bip322`'s public surface shrinks to `{ScriptType, Bip322Error, detect_script_type, verify_simple, sign_simple, p2sh_p2wpkh_final_script_sig, bip322_message_hash, build_bip322_to_spend, build_bip322_to_sign}` (CONTEXT §"Phase Boundary" line 15). The test-only escape hatch that was a CD-6 concession in v1.4 is gone. The Phase 21 audit charter can now describe `shared::bip322` as a dispatcher-only surface without a "but also there's this `#[doc(hidden)]` thing" footnote.

This plan depends on Plan 19-01 — without production sign bodies (Plan 19-01 Tasks 1 + 2), the per-script-vector + cross-shape integration tests would fail when they're migrated off `sign_simple_test_only` (which routed to `sign_for_tests`) onto `sign_simple` (which routes to production `sign`). With Plan 19-01 merged, the production bodies are byte-identical to the lifted-from `sign_for_tests` bodies per D-116, so migration is a pure surface-shrink — the witnesses being verified are unchanged.

Output:
- `shared/src/bip322/mod.rs` — 41 lines deleted (lines 274-314 inclusive, the entire `sign_simple_test_only` block including the 27-line explanatory comment header).
- `shared/src/bip322/p2tr.rs` — `sign_for_tests` fn at lines 46-95 deleted (50 lines).
- `shared/src/bip322/p2sh_p2wpkh.rs` — `sign_for_tests` fn at lines 52-108 deleted (57 lines).
- `shared/src/bip322/p2wpkh.rs` — `sign_for_tests` fn at lines 74-95 deleted (22 lines; the unused alias with `#[allow(dead_code)]`).
- `shared/tests/per_script_vectors.rs` — import refresh + 2 callsite migrations + 2 comment refreshes.
- `tests/integration/multi_script_validate.rs` — import refresh + helper-fn body migration.
- `tests/integration/mod.rs` — 2 doc-comment refreshes per CD-39 default (folded into this plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-multi-script-signing-finish/19-CONTEXT.md
@.planning/phases/19-multi-script-signing-finish/19-RESEARCH.md
@.planning/phases/19-multi-script-signing-finish/19-PATTERNS.md
@.planning/phases/19-multi-script-signing-finish/19-01-SUMMARY.md

<interfaces>
At this plan's start, Plan 19-01 has already merged. The state of the workspace is:

From `shared/src/bip322/mod.rs` (post-Plan-19-01):
- `pub fn sign_simple` — production dispatcher, UNCHANGED (its `match` arms route to production per-script `sign` bodies, which now ship real implementations from Plan 19-01).
- `#[doc(hidden)] pub fn sign_simple_test_only` at lines 274-314 — STILL EXISTS at this plan's start; THIS PLAN DELETES IT.
- `pub fn p2sh_p2wpkh_final_script_sig` — new helper from Plan 19-01 Task 3; UNCHANGED by this plan.
- Three new unit tests from Plan 19-01 Task 3 — UNCHANGED by this plan.
- `Bip322Error::ScriptTypeMismatch` variant with updated doc-comment per CD-36 — UNCHANGED by this plan.

From `shared/src/bip322/{p2tr,p2sh_p2wpkh,p2wpkh}.rs` (post-Plan-19-01):
- Production `pub(crate) fn sign` shipped in `p2tr.rs` and `p2sh_p2wpkh.rs` (Plan 19-01 Tasks 1 + 2).
- `pub(crate) fn sign_for_tests` STILL EXISTS in all three per-script modules; THIS PLAN DELETES IT.

From `shared/tests/per_script_vectors.rs` (post-Plan-19-01):
- Import line 21: `use shared::bip322::{sign_simple, sign_simple_test_only, verify_simple, Bip322Error, ScriptType};` — THIS PLAN drops `sign_simple_test_only`.
- Line 274: `sign_simple_test_only(ScriptType::P2tr, ...)` callsite — THIS PLAN swaps to `sign_simple`.
- Line 311: `sign_simple_test_only(ScriptType::P2shP2wpkh, ...)` callsite — THIS PLAN swaps to `sign_simple`.
- Lines 271-272 and 308-309: explanatory comments referencing CD-6 + the test-only path — THIS PLAN refreshes them.

From `tests/integration/multi_script_validate.rs` (post-Plan-19-01):
- Import line 23: `use shared::bip322::{sign_simple_test_only, Bip322Error, ScriptType};` — THIS PLAN swaps to `sign_simple`.
- Helper `sign_witness` body at lines 113-121: 2 textual changes (`sign_simple_test_only(...)` → `sign_simple(...)` and the `.expect("sign_simple_test_only should produce a valid witness")` string update).

From `tests/integration/mod.rs` (post-Plan-19-01):
- Line 707 (module-level comment): `//      shared::bip322::sign_simple_test_only.` — refresh to `sign_simple`.
- Lines 721-723 (`TypedUtxoHandle::secret_key` field doc): `/// ... shared::bip322::sign_simple_test_only.` — refresh to `sign_simple`.

From `.github/workflows/ci.yml` (RESEARCH Q4 — UNCHANGED by this plan):
- `bip322-pin-check` (lines ~214-236) — gates `bip322 = "=0.0.10"` pin; no edits needed.
- `crit-01-grep-check` — gates `CRIT-01` count `>= 2` in `coordinator/src/bitcoin/utxo.rs`; no edits needed.
- `crit-01-client-grep-check` — gates `CRIT-01` count `>= 1` in `client/src/round/input.rs`; no edits needed.
- `corepc-node-feature-pin-check` — orthogonal; no edits needed.

Assertion-shape verification (RESEARCH Q5): every migrated test's assertion is a `verify_simple(...).is_ok()` or `validate_ownership_proof_typed(...).is_ok()` / `matches!(...)` — NOT a hardcoded witness-byte equality. Production sign witnesses from Plan 19-01 (lifted verbatim from `sign_for_tests` per D-116) are interchangeable; tests stay green by construction.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete sign_simple_test_only from mod.rs + sign_for_tests from all 3 per-script modules</name>
  <files>shared/src/bip322/mod.rs, shared/src/bip322/p2tr.rs, shared/src/bip322/p2sh_p2wpkh.rs, shared/src/bip322/p2wpkh.rs</files>
  <read_first>
    - shared/src/bip322/mod.rs (lines 274-314 — the entire `sign_simple_test_only` block to delete, INCLUDING the explanatory comment header at lines 274-300 AND the `#[doc(hidden)] pub fn` body at lines 302-314)
    - shared/src/bip322/p2tr.rs (lines 46-95 — `pub(crate) fn sign_for_tests` to delete in full, including its doc comment)
    - shared/src/bip322/p2sh_p2wpkh.rs (lines 52-108 — `pub(crate) fn sign_for_tests` to delete in full)
    - shared/src/bip322/p2wpkh.rs (lines 74-95 — `pub(crate) fn sign_for_tests` to delete; carries `#[allow(dead_code)]` attribute per RESEARCH Q5 C3; production `pub(crate) fn sign` at lines 22-72 is UNCHANGED)
    - .planning/phases/19-multi-script-signing-finish/19-CONTEXT.md §"G. Plan structure / sequencing" (D-120, D-121) — Plan 19-02 task list + sequential-wave rationale; §"Phase Boundary" lines 15 + 32 — final v1.5 public-surface shape
    - .planning/phases/19-multi-script-signing-finish/19-RESEARCH.md §Q4 — confirmation that CI grep gates are unaffected by these deletions + §Q5 C3 — confirmation that `p2wpkh::sign_for_tests` deletion produces NO leftover dead-code warning (the `#[allow(dead_code)]` is removed with the fn)
    - .planning/phases/19-multi-script-signing-finish/19-PATTERNS.md §"Deletion — sign_simple_test_only (Plan 19-02)" + §"Modification 2 — sign_for_tests deletion" in each per-script file section
  </read_first>
  <action>
    Four deletions, no insertions:

    Deletion 1 — `shared/src/bip322/mod.rs:274-314`. Delete the entire block: the 27-line explanatory comment header starting at line 274 AND the `#[doc(hidden)] pub fn sign_simple_test_only` body at lines 302-314. The dispatcher `sign_simple` at lines 261-272 is UNCHANGED. The new `p2sh_p2wpkh_final_script_sig` helper inserted by Plan 19-01 Task 3 (between line 272 and the deleted block) stays put — the deletion lifts the surrounding context but leaves the helper. After deletion the file should flow: `... sign_simple body ending } ... p2sh_p2wpkh_final_script_sig helper ... verify_via_bip322_crate at line 323 (or its new shifted line number) ...`.

    Deletion 2 — `shared/src/bip322/p2tr.rs:46-95`. Delete `pub(crate) fn sign_for_tests` in full, including its 14-line doc comment. The production `pub(crate) fn sign` at lines 33-44 (post-Plan-19-01 contains real body, not `todo!()`) is UNCHANGED. The `pub(crate) fn verify` at lines 18-31 is UNCHANGED.

    Deletion 3 — `shared/src/bip322/p2sh_p2wpkh.rs:52-108`. Delete `pub(crate) fn sign_for_tests` in full, including its 16-line doc comment. The production `pub(crate) fn sign` at lines 39-50 (post-Plan-19-01 contains real body) is UNCHANGED. The `pub(crate) fn verify` at lines 24-37 is UNCHANGED.

    Deletion 4 — `shared/src/bip322/p2wpkh.rs:74-95`. Delete the unused alias `pub(crate) fn sign_for_tests` in full, including its 14-line doc comment AND its `#[allow(dead_code)]` attribute. The production `pub(crate) fn sign` at lines 22-72 is UNCHANGED. The `pub(crate) fn verify` at lines 46-72 ... NOTE: re-read p2wpkh.rs before editing — the `verify`/`sign` line ranges in CONTEXT may differ post-Plan-19-01; delete by IDENTIFIER (`fn sign_for_tests`), not by line number, to be safe.

    Order of operations: do Deletion 1 LAST so the file's line numbers in the per-script files are not affected by mod.rs edits. (Per-script files have no cross-references to mod.rs lines, but doing mod.rs last keeps any incidental tooling output consistent.)

    Do NOT touch any callers in this task (`shared/tests/per_script_vectors.rs`, `tests/integration/multi_script_validate.rs`, `tests/integration/mod.rs`) — Task 2 handles caller migration. At the END of this task, `cargo build -p shared` will FAIL because callers still reference `sign_simple_test_only`. That's expected and gated by Task 2.

    Per RESEARCH §Q4: no `.github/workflows/*.yml` edits are needed in this task or in this plan.
  </action>
  <verify>
    <automated>grep -c 'sign_simple_test_only' shared/src/bip322/mod.rs &amp;&amp; grep -c 'fn sign_for_tests' shared/src/bip322/p2tr.rs shared/src/bip322/p2sh_p2wpkh.rs shared/src/bip322/p2wpkh.rs</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'sign_simple_test_only' shared/src/bip322/mod.rs` returns 0.
    - Source: `grep -c '#\[doc(hidden)\]' shared/src/bip322/mod.rs` returns 0 (no other `#[doc(hidden)]` should be in this module — verify the only one being removed was the test-only mirror).
    - Source: `grep -c 'fn sign_for_tests' shared/src/bip322/p2tr.rs` returns 0.
    - Source: `grep -c 'fn sign_for_tests' shared/src/bip322/p2sh_p2wpkh.rs` returns 0.
    - Source: `grep -c 'fn sign_for_tests' shared/src/bip322/p2wpkh.rs` returns 0.
    - Source: `grep -c 'pub(crate) fn sign(' shared/src/bip322/p2tr.rs` returns 1 (production sign body unchanged).
    - Source: `grep -c 'pub(crate) fn sign(' shared/src/bip322/p2sh_p2wpkh.rs` returns 1.
    - Source: `grep -c 'pub(crate) fn sign(' shared/src/bip322/p2wpkh.rs` returns 1.
    - Source: `grep -c 'pub fn p2sh_p2wpkh_final_script_sig' shared/src/bip322/mod.rs` returns 1 (Plan 19-01 helper preserved).
    - Source: `grep -c 'pub fn sign_simple(' shared/src/bip322/mod.rs` returns 1 (dispatcher unchanged).
    - Source: `grep -c 'verify_via_bip322_crate' shared/src/bip322/mod.rs` returns at least 1 (the verify-side crate adapter is unchanged per CONTEXT D-LOCKED scope).
    - Build state: `cargo build -p shared` is expected to FAIL at this task boundary (callers in `shared/tests/per_script_vectors.rs` + `tests/integration/multi_script_validate.rs` still reference the deleted symbol). This is gated by Task 2; do NOT attempt to fix the failure in this task.
  </acceptance_criteria>
  <done>
    All 4 deletions complete. `sign_simple_test_only` and 3 `sign_for_tests` symbols no longer exist in `shared/`. Production `sign` bodies + dispatcher + `p2sh_p2wpkh_final_script_sig` helper are unchanged. Workspace build is broken pending Task 2 caller migration.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate callers + refresh doc-comments + verify CI gates + clippy</name>
  <files>shared/tests/per_script_vectors.rs, tests/integration/multi_script_validate.rs, tests/integration/mod.rs</files>
  <read_first>
    - shared/tests/per_script_vectors.rs (the full file — focus on line 21 import, lines 268-280 P2TR test, lines 305-317 P2SH-P2WPKH test, the explanatory comments at lines 271-272 and 308-309, AND the line 366 `Bip322Error` import-usage check at `_bip322_error_path_check` per RESEARCH Q5 C6 — `Bip322Error` import STAYS)
    - tests/integration/multi_script_validate.rs (the full file — focus on line 23 import + the `sign_witness` helper at lines 113-121 with 2 textual changes)
    - tests/integration/mod.rs (the full file — focus on line 707 module-level comment + lines 721-723 `TypedUtxoHandle::secret_key` field doc; per CD-39 default these are folded into this plan)
    - .planning/phases/19-multi-script-signing-finish/19-RESEARCH.md §Q5 — full migration diffs for all 6 callsites (`Site 1` through `Site 6`) + assertion-shape verification table proving production-sign witnesses are interchangeable with test-only-mirror witnesses + predicted clippy warnings C1-C6 (all pre-empted by these migration tasks) + cleanup task spec for Task 6 (post-deletion grep verify) + Task 7 (local clippy gate)
    - .planning/phases/19-multi-script-signing-finish/19-PATTERNS.md §"File: shared/tests/per_script_vectors.rs (modified — Plan 19-02 ONLY)" + §"File: tests/integration/multi_script_validate.rs (modified — Plan 19-02 ONLY)" + §"File: tests/integration/mod.rs (modified — Plan 19-02 ONLY)" + §"Shared Patterns" §"Pattern 5" (`.expect(...)` NOT `?` at integration test callsites)
    - .planning/phases/19-multi-script-signing-finish/19-CONTEXT.md §"G. Plan structure / sequencing" (D-120) + CD-39 (default = fold tests/integration/mod.rs comment refreshes into this plan)
  </read_first>
  <action>
    Six edits across three files + 2 grep+clippy verify subtasks:

    Edit 1 — `shared/tests/per_script_vectors.rs:21` import refresh. Drop `sign_simple_test_only` from the use list. Final form: `use shared::bip322::{sign_simple, verify_simple, Bip322Error, ScriptType};`. Per RESEARCH Q5 C6: `Bip322Error` stays — used by `_bip322_error_path_check` at line 366.

    Edit 2 — `shared/tests/per_script_vectors.rs:271-280` P2TR positive-vector test body. Swap `sign_simple_test_only(ScriptType::P2tr, &spk, &key, message).expect("sign_simple_test_only p2tr")` → `sign_simple(ScriptType::P2tr, &spk, &key, message).expect("sign_simple p2tr")`. Refresh the explanatory comment at lines 271-272 (per RESEARCH Q5 Site 2 diff): replace the "CD-6 / sign_for_tests / Sprint-0-B" prose with "P2TR production sign_simple ships in Phase 19 Plan 19-01 (D-116 lifted sign_for_tests body verbatim into production sign + D-111 cross-check at top)." per the PATTERNS.md §"Modification 2" diff template.

    Edit 3 — `shared/tests/per_script_vectors.rs:308-317` P2SH-P2WPKH positive-vector test body. Same shape as Edit 2: swap `sign_simple_test_only(ScriptType::P2shP2wpkh, ...)` → `sign_simple(ScriptType::P2shP2wpkh, ...)` with matching `.expect("sign_simple p2sh-p2wpkh")` string update. Refresh the explanatory comment at lines 308-309 with "P2SH-P2WPKH production sign_simple ships in Phase 19 Plan 19-01 (D-116 lifted sign_for_tests body verbatim + D-111 cross-check + D-117 spk-used-directly)." per PATTERNS.md §"Modification 3" diff.

    Edit 4 — `tests/integration/multi_script_validate.rs:23` import refresh. Swap `sign_simple_test_only` → `sign_simple`. Final form: `use shared::bip322::{sign_simple, Bip322Error, ScriptType};` (per RESEARCH Q5 Site 4).

    Edit 5 — `tests/integration/multi_script_validate.rs:113-121` `sign_witness` helper body. Per RESEARCH Q5 Site 5 diff: swap `sign_simple_test_only(...)` → `sign_simple(...)` and `.expect("sign_simple_test_only should produce a valid witness")` → `.expect("sign_simple should produce a valid witness")`. The helper signature and call shape stay identical; only the function name + expect string change.

    Edit 6 — `tests/integration/mod.rs:707,721-723` doc-comment refreshes per CD-39 default (folded into this plan). Per RESEARCH Q5 Site 6:
    - Line 707 module-level comment: `//      shared::bip322::sign_simple_test_only.` → `//      shared::bip322::sign_simple.`
    - Lines 721-723 `TypedUtxoHandle::secret_key` field doc: `/// ... shared::bip322::sign_simple_test_only.` → `/// ... shared::bip322::sign_simple.` (preserve the wrapping prose around the symbol).

    Subtask A — Grep-verify zero stale references. Run:
    ```
    grep -rn -E '(sign_simple_test_only|fn sign_for_tests)' \
      shared/ tests/ client/ coordinator/ liquidity-bot/ \
      --include='*.rs' --include='*.md'
    ```
    Expected: zero matches in `*.rs` files. `*.md` matches are acceptable ONLY within `.planning/phases/19-multi-script-signing-finish/` (planning artifacts that reference the removed symbol as part of the project record). If any `*.rs` match returns, fix it before completing this task.

    Subtask B — Clippy gate. Run `cargo clippy --workspace --all-targets -- -D warnings`. Per RESEARCH Q5 predicted warnings C1-C6 are all pre-empted by Edits 1-6 above. Any warning that surfaces is a new issue requiring fix; do NOT proceed to verification until clippy is clean.

    Per CD-39 default: this task includes Edit 6 (the `tests/integration/mod.rs` comment refreshes). No follow-up commit.

    Per RESEARCH Q4: no `.github/workflows/*.yml` edits needed — all 3 CI grep gates stay green.
  </action>
  <verify>
    <automated>cargo build --workspace &amp;&amp; cargo test -p shared --test per_script_vectors &amp;&amp; cargo test --test integration multi_script_validate &amp;&amp; cargo clippy --workspace --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'sign_simple_test_only' shared/tests/per_script_vectors.rs` returns 0.
    - Source: `grep -c 'sign_simple_test_only' tests/integration/multi_script_validate.rs` returns 0.
    - Source: `grep -c 'sign_simple_test_only' tests/integration/mod.rs` returns 0.
    - Grep (workspace-wide): `bash -c "grep -rn -E '(sign_simple_test_only|fn sign_for_tests)' shared/ tests/ client/ coordinator/ liquidity-bot/ --include='*.rs' 2>/dev/null | wc -l"` returns 0.
    - Source (import shape): `grep -n 'use shared::bip322::' shared/tests/per_script_vectors.rs` shows `sign_simple` and `verify_simple` and `Bip322Error` and `ScriptType` but NOT `sign_simple_test_only`.
    - Source (import shape): `grep -n 'use shared::bip322::' tests/integration/multi_script_validate.rs` shows `sign_simple` and `Bip322Error` and `ScriptType` but NOT `sign_simple_test_only`.
    - Source (helper call): `grep -n 'sign_simple(' tests/integration/multi_script_validate.rs` shows the helper now calls `sign_simple(handle.script_type, ...)`.
    - Build: `cargo build --workspace` exits 0 (the Task 1 breakage is fixed by these caller migrations).
    - Test: `cargo test -p shared` lib + integration green at unchanged counts (Plan 19-01 added 3 unit tests in Task 3; this plan adds no new tests — `shared` library count is 31 + 3 = 34 unit tests if the lib counter increments by 3).
    - Test: `cargo test -p shared --test per_script_vectors` 7/7 — now exercises production sign bodies (load-bearing assertion of this plan per CONTEXT D-120 closing line).
    - Test: `cargo test -p shared --test bip322_cross_shape` 9/9.
    - Test: `cargo test -p client --test wallet_sign_roundtrip` 9/9 (Plan 19-01 added 2 parity tests).
    - Test (boundary, must STAY green): `cargo test --test integration full_round` 8/8 (v1.3 invariant).
    - Test (boundary, must STAY green): `cargo test --test integration mixed_script_e2e` 1/1 (v1.4 invariant).
    - Test: `cargo test --test integration multi_script_validate` — all D-54 cases pass; the 9 cross-shape rejection cases now exercise production `sign_simple` (load-bearing per CONTEXT D-120 closing line).
    - Lint: `cargo clippy --workspace --all-targets -- -D warnings` exits 0.
    - CI: simulate `bip322-pin-check` locally with `grep -nE '^bip322\s*=' Cargo.toml shared/Cargo.toml | grep -v '"=0.0.10"'` returns 0 lines (exact-pin preserved). `crit-01-grep-check`: `grep -c 'CRIT-01' coordinator/src/bitcoin/utxo.rs` returns at least 2 (unchanged by this plan). `crit-01-client-grep-check`: `grep -c 'CRIT-01' client/src/round/input.rs` returns at least 1 (unchanged by this plan).
  </acceptance_criteria>
  <done>
    All 6 callsites migrated to `sign_simple`. Zero residual references to `sign_simple_test_only` or `fn sign_for_tests` in workspace `*.rs` files. Workspace build green; clippy clean; per_script_vectors + bip322_cross_shape + multi_script_validate + wallet_sign_roundtrip + full_round + mixed_script_e2e all pass. The V1.4-CRIT-01 dispatcher-only invariant is now load-bearing at the type level with no test-only hole.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-only API → production callers | At the START of this plan, `sign_simple_test_only` is `#[doc(hidden)] pub fn` — technically reachable from any crate that depends on `shared`. The risk is that production code (client, coordinator) gains a transitive call to the test-only mirror, silently bypassing the production dispatcher's contract surface. THIS PLAN ELIMINATES THE BOUNDARY by deleting the symbol. |
| Workspace test suite → shared::bip322 dispatcher | After migration, all callers reach production `sign` bodies via `sign_simple`. The dispatcher's match arms (`ScriptType::P2tr => p2tr::sign`, etc.) are the only path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-B | Tampering / Elevation of Privilege | `shared::bip322::sign_simple_test_only` (`#[doc(hidden)] pub fn`) as a sign-side escape hatch | mitigate (eliminate) | Task 1 DELETES the symbol entirely. Task 2 Subtask A grep-verifies zero residual references workspace-wide (including `*.rs` and `*.md` outside the planning artifacts). After this plan, no caller can route around the dispatcher. The V1.4-CRIT-01 dispatcher-only invariant becomes load-bearing at the type level. |
| T-19-A (carry-forward from Plan 19-01) | Spoofing / Tampering | `shared::bip322::p2tr::sign` + `shared::bip322::p2sh_p2wpkh::sign` D-111 cross-check | mitigate (already shipped in Plan 19-01) | Plan 19-01 Tasks 1 + 2 already shipped the D-111 cross-check. This plan inherits the mitigation; verification: the 9 cross-shape rejection cases in `tests/integration/multi_script_validate.rs` (now migrated to call `sign_simple`) exercise the CRIT-01 cross-check + D-111 cross-check end-to-end. |
| T-19-D | Information Disclosure (PII via error Display) | `Bip322Error` Display output (especially `ScriptTypeMismatch` reused per D-112) | mitigate (existing) | No new error variants in this plan (pure deletion + caller migration). Existing PII-safety test at `shared/src/bip322/mod.rs:512-565` stays green; Task 2 verification gates this. |
</threat_model>

<verification>
At the Plan 19-02 boundary, ALL of the following must hold simultaneously:

1. `cargo build --workspace` — clean build, no unresolved references to `sign_simple_test_only` or `fn sign_for_tests`.
2. Workspace-wide grep `grep -rn -E '(sign_simple_test_only|fn sign_for_tests)' shared/ tests/ client/ coordinator/ liquidity-bot/ --include='*.rs'` returns ZERO matches.
3. `cargo test -p shared` — lib unit tests green (count includes the 3 new tests from Plan 19-01 Task 3).
4. `cargo test -p shared --test per_script_vectors` — 7/7 green; the migrated P2TR + P2SH-P2WPKH positive-vector tests now exercise production sign bodies (load-bearing per CONTEXT D-120 closing line).
5. `cargo test -p shared --test bip322_cross_shape` — 9/9 green.
6. `cargo test -p client --test wallet_sign_roundtrip` — 9/9 green.
7. `cargo test --test integration full_round` — 8/8 green (v1.3 invariant).
8. `cargo test --test integration mixed_script_e2e` — 1/1 green (v1.4 invariant).
9. `cargo test --test integration multi_script_validate` — all D-54 cases pass; cross-shape rejections exercise production sign.
10. `cargo clippy --workspace --all-targets -- -D warnings` — clean (RESEARCH Q5 predicted warnings C1-C6 all pre-empted by Task 2 edits).
11. CI grep gates stay green: `bip322-pin-check` (Cargo.toml `bip322` pin unchanged at `=0.0.10`); `crit-01-grep-check` (≥ 2 `CRIT-01` hits in `coordinator/src/bitcoin/utxo.rs`); `crit-01-client-grep-check` (≥ 1 `CRIT-01` hit in `client/src/round/input.rs`).
12. `shared::bip322` public surface (post-Plan-19-02) shrunk to: `{ScriptType, Bip322Error, detect_script_type, verify_simple, sign_simple, p2sh_p2wpkh_final_script_sig, bip322_message_hash, build_bip322_to_spend, build_bip322_to_sign}` per CONTEXT §"Phase Boundary" line 15. Verify by `cargo doc -p shared` + spot-check the generated module index (or run `grep -E '^pub (fn|enum|struct)' shared/src/bip322/mod.rs` and confirm the set matches).
</verification>

<success_criteria>
Plan 19-02 is complete when:

- [ ] `shared/src/bip322/mod.rs::sign_simple_test_only` is deleted (entire lines 274-314 block including comment header).
- [ ] `shared/src/bip322/p2tr.rs::sign_for_tests` is deleted (entire fn lines 46-95).
- [ ] `shared/src/bip322/p2sh_p2wpkh.rs::sign_for_tests` is deleted (entire fn lines 52-108).
- [ ] `shared/src/bip322/p2wpkh.rs::sign_for_tests` is deleted (entire fn lines 74-95, with its `#[allow(dead_code)]` attribute).
- [ ] `shared/tests/per_script_vectors.rs` import + 2 callsites + 2 comment blocks migrated to `sign_simple`.
- [ ] `tests/integration/multi_script_validate.rs` import + `sign_witness` helper-fn body migrated to `sign_simple`.
- [ ] `tests/integration/mod.rs` 2 doc-comment refreshes complete (per CD-39 default).
- [ ] Workspace-wide grep for `sign_simple_test_only` or `fn sign_for_tests` returns ZERO matches in `*.rs` files.
- [ ] All 12 verification items above hold simultaneously.
- [ ] CI grep gates (`bip322-pin-check`, `crit-01-grep-check`, `crit-01-client-grep-check`) stay green — no `.github/workflows/*.yml` edits.
- [ ] V1.4-CRIT-01 dispatcher-only public surface is load-bearing at the type level — `shared::bip322` exposes exactly `{ScriptType, Bip322Error, detect_script_type, verify_simple, sign_simple, p2sh_p2wpkh_final_script_sig, bip322_message_hash, build_bip322_to_spend, build_bip322_to_sign}`.
</success_criteria>

<output>
Create `.planning/phases/19-multi-script-signing-finish/19-02-SUMMARY.md` when done. SUMMARY must record:
- Files modified (7): `shared/src/bip322/mod.rs`, `shared/src/bip322/p2tr.rs`, `shared/src/bip322/p2sh_p2wpkh.rs`, `shared/src/bip322/p2wpkh.rs`, `shared/tests/per_script_vectors.rs`, `tests/integration/multi_script_validate.rs`, `tests/integration/mod.rs`.
- Lines deleted (cumulative): ~170 lines (~41 from `mod.rs::sign_simple_test_only`, ~50 from `p2tr.rs::sign_for_tests`, ~57 from `p2sh_p2wpkh.rs::sign_for_tests`, ~22 from `p2wpkh.rs::sign_for_tests`).
- Decisions implemented: BIP322-07 (REQUIREMENTS.md), D-120 (Plan 19-02 task list), D-121 (wave 2 sequential), CD-39 (mod.rs comment refresh folded in).
- Threat-model items mitigated: T-19-B (sign_simple_test_only deletion); T-19-A + T-19-D carry forward from Plan 19-01 verified unchanged.
- Final `shared::bip322` public surface: `{ScriptType, Bip322Error, detect_script_type, verify_simple, sign_simple, p2sh_p2wpkh_final_script_sig, bip322_message_hash, build_bip322_to_spend, build_bip322_to_sign}` — V1.4-CRIT-01 dispatcher-only invariant now load-bearing at the type level with no test-only hole.
- v1.5 milestone position: Phase 19 closed; Phase 20 (FEE-01/02/03) unblocked.
</output>
