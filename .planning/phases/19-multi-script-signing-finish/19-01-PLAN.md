---
phase: 19-multi-script-signing-finish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/src/bip322/p2tr.rs
  - shared/src/bip322/p2sh_p2wpkh.rs
  - shared/src/bip322/mod.rs
  - client/tests/wallet_sign_roundtrip.rs
autonomous: true
requirements: [BIP322-05, BIP322-06]
tags: [bip322, signing, schnorr, ecdsa, multi-script, audit-readiness]

must_haves:
  truths:
    - "shared::bip322::p2tr::sign returns a 1-element witness with a 64-byte BIP-341 Schnorr SIGHASH_DEFAULT signature; verify_schnorr accepts it; identical bytes to BdkClientWallet::sign_bip322 for the same (key, message)."
    - "shared::bip322::p2sh_p2wpkh::sign returns a 2-element witness [der_sig+SIGHASH_ALL, compressed_pubkey] that verify_simple(ScriptType::P2shP2wpkh, ...) accepts on the produced (spk, witness, message)."
    - "shared::bip322::p2sh_p2wpkh_final_script_sig(pubkey) returns a 23-byte ScriptBuf [0x16, 0x00, 0x14, <20-byte HASH160(pubkey)>] — the BIP-141 P2SH wrapper scriptSig."
    - "Calling p2tr::sign or p2sh_p2wpkh::sign with a script_pubkey whose derivation does not match the supplied SecretKey returns Err(Bip322Error::ScriptTypeMismatch{declared, derived}) BEFORE producing any witness (D-111 defense-in-depth cross-check)."
    - "At the Plan 19-01 boundary, sign_simple_test_only and the per-script sign_for_tests helpers still exist; existing positive-vector + cross-shape + full_round + mixed_script_e2e tests stay green."
    - "CONTEXT decisions honored by this plan: D-107 (sign_simple Result<Witness, Bip322Error> return type unchanged — no Bip322ProofPieces struct), D-108 (p2sh_p2wpkh_final_script_sig_derives_correctly unit test pins BIP-141 wire shape), D-109 (helper is `pub fn` SIBLING to sign_simple in mod.rs, NOT in p2sh_p2wpkh.rs), D-110 (helper body produces single OP_PUSHBYTES_22 push of the 22-byte redeem — RESEARCH Q3 corrects the byte-count to 23, not 24), D-111 (spk↔key cross-check in BOTH per-script sign bodies), D-112 (Bip322Error::ScriptTypeMismatch variant REUSED — no new variant; doc-comment updated for dual meaning per CD-36), D-113 (cross-check algorithm: P2TR via tap_tweak + new_p2tr_tweaked; P2SH-P2WPKH via wpubkey_hash + new_p2sh), D-114 (P2TR uses sign_schnorr_no_aux_rand — deterministic, required for D-118 byte-equality), D-115 (RESEARCH Q1 confirmed bdk_wallet 2.3 uses sign_schnorr_no_aux_rand — byte-equality assertion safe, no downgrade), D-116 (sign_for_tests bodies LIFTED near-verbatim into production sign), D-117 (P2SH-P2WPKH `_spk` → `spk` load-bearing after cross-check), D-118 (p2tr_shared_sign_matches_bdk_sign_byte_for_byte parity test in client/tests/wallet_sign_roundtrip.rs), D-119 (p2sh_p2wpkh_shared_sign_matches_bdk_sign_byte_for_byte parity test, RFC 6979 deterministic), D-120 (Plan 19-01 contains BIP322-05 + BIP322-06 + helper + parity tests — Plan 19-02 follows with removals), D-121 (wave 1 — Plan 19-02 is wave 2 depends_on [19-01]). CD-34 (Builder::push_slice with PushBytes try_from per RESEARCH Q3), CD-35 (per-script parity test function names), CD-36 (variant doc-comment update — default = yes), CD-37 (cross-check rejection unit tests added per default = yes), CD-38 (inline `// Plan 19-01 Task N` provenance comments in sign bodies — default = yes)."
  artifacts:
    - path: "shared/src/bip322/p2tr.rs"
      provides: "Production p2tr::sign body (no todo!())"
      contains: "sign_schnorr_no_aux_rand"
    - path: "shared/src/bip322/p2sh_p2wpkh.rs"
      provides: "Production p2sh_p2wpkh::sign body (no todo!())"
      contains: "p2wpkh_signature_hash"
    - path: "shared/src/bip322/mod.rs"
      provides: "p2sh_p2wpkh_final_script_sig helper + 3 new unit tests"
      exports: ["p2sh_p2wpkh_final_script_sig"]
    - path: "client/tests/wallet_sign_roundtrip.rs"
      provides: "Two byte-equality parity tests (P2TR + P2SH-P2WPKH bdk vs shared::bip322)"
      contains: "p2tr_shared_sign_matches_bdk_sign_byte_for_byte"
  key_links:
    - from: "shared/src/bip322/p2tr.rs::sign"
      to: "shared/src/bip322/mod.rs::sign_simple dispatcher"
      via: "match arm ScriptType::P2tr => p2tr::sign(spk, key, message)"
      pattern: "ScriptType::P2tr.*p2tr::sign"
    - from: "shared/src/bip322/p2sh_p2wpkh.rs::sign"
      to: "shared/src/bip322/mod.rs::sign_simple dispatcher"
      via: "match arm ScriptType::P2shP2wpkh => p2sh_p2wpkh::sign(spk, key, message)"
      pattern: "ScriptType::P2shP2wpkh.*p2sh_p2wpkh::sign"
    - from: "client/tests/wallet_sign_roundtrip.rs"
      to: "shared::bip322::sign_simple + client::wallet::BdkClientWallet::sign_bip322"
      via: "byte-equality assertion on witness produced by both paths"
      pattern: "assert_eq!\\(bdk_signed.witness, *shared_witness"
---

<objective>
Ship production `pub(crate) fn sign` bodies for `shared::bip322::p2tr` and `shared::bip322::p2sh_p2wpkh` — replace the `todo!("Phase 17 WALLET-02 wires bdk_wallet sign per ADR #4")` placeholders with the BIP-341 Schnorr keypath sequence (P2TR) and the BIP-143 ECDSA sequence (P2SH-P2WPKH), both lifted near-verbatim from the existing `sign_for_tests` helpers per CONTEXT D-116. Add the D-111 spk-vs-key cross-check at the top of each new body (defense-in-depth audit prerequisite). Expose the new `pub fn p2sh_p2wpkh_final_script_sig(pubkey) -> ScriptBuf` helper sibling to `sign_simple` per D-109. Add three unit tests in `shared/src/bip322/mod.rs::tests` (helper byte-shape + two cross-check rejection cases per CD-37 default = yes). Add two byte-equality parity tests in `client/tests/wallet_sign_roundtrip.rs` (P2TR + P2SH-P2WPKH) proving `shared::bip322::sign_simple` produces witnesses byte-equal to `BdkClientWallet::sign_bip322` for the same `(key, message)` — RESEARCH Q1 verifies byte-equality is safe (bdk_wallet 2.3 uses `sign_schnorr_no_aux_rand` for P2TR; ECDSA is RFC 6979 deterministic).

Purpose: Close BIP322-05 + BIP322-06 — production sign bodies that the Phase 21 audit charter can describe as load-bearing implementation (not `todo!()`). The D-111 cross-check is the audit-charter's "structural mitigation" claim for the sign-side T-19-A threat. The byte-equality parity tests are the SC#1 strongest-available closure (T-19-C mitigation). At this plan boundary, the test-only escape hatches (`sign_simple_test_only` + per-script `sign_for_tests`) STILL EXIST — Plan 19-02 removes them after this plan proves production sign works.

Output:
- `shared/src/bip322/p2tr.rs::sign` — production Schnorr body, signature `pub(crate) fn sign(spk: &Script, key: &SecretKey, message: &[u8]) -> Result<Witness, super::Bip322Error>`, no `todo!()`.
- `shared/src/bip322/p2sh_p2wpkh.rs::sign` — production BIP-143 body, same signature shape, `_spk` renamed to `spk` (load-bearing after cross-check per D-117), no `todo!()`.
- `shared/src/bip322/mod.rs::p2sh_p2wpkh_final_script_sig` — new `pub fn` sibling to `sign_simple`, infallible, 23-byte ScriptBuf output.
- `shared/src/bip322/mod.rs::tests` — three new `#[test] fn`s (helper byte-shape + 2 cross-check rejections).
- `client/tests/wallet_sign_roundtrip.rs` — two new `#[tokio::test] async fn`s (P2TR + P2SH-P2WPKH parity).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-multi-script-signing-finish/19-CONTEXT.md
@.planning/phases/19-multi-script-signing-finish/19-RESEARCH.md
@.planning/phases/19-multi-script-signing-finish/19-PATTERNS.md

<interfaces>
From `shared/src/bip322/mod.rs` (existing — unchanged by Plan 19-01):
- `pub fn bip322_message_hash(message: &[u8]) -> [u8; 32]` (lines 45-53)
- `pub fn build_bip322_to_spend(spk: &Script, msg_hash: &[u8; 32]) -> Transaction` (~lines 58-71)
- `pub fn build_bip322_to_sign(to_spend: &Transaction) -> Transaction` (~lines 89-138)
- `pub fn detect_script_type(spk: &Script) -> Result<ScriptType, Bip322Error>` — used by D-111 cross-check to populate `declared` field on the rejection error
- `pub fn sign_simple(script_type: ScriptType, spk: &Script, key: &SecretKey, message: &[u8]) -> Result<Witness, Bip322Error>` (dispatcher at lines 261-272; UNCHANGED — its `match` arms already route to `p2tr::sign` / `p2sh_p2wpkh::sign`, which currently `todo!()` and will ship production bodies in this plan)
- `pub fn verify_simple(script_type: ScriptType, spk: &Script, witness: &Witness, message: &[u8], network: Network) -> Result<(), Bip322Error>`
- `pub enum Bip322Error { ..., ScriptTypeMismatch { declared: ScriptType, derived: ScriptType }, ... }` (variant at lines 184-188; reused per D-112 — no new variant)
- `pub enum ScriptType { P2wpkh, P2tr, P2shP2wpkh }`

From `shared/src/bip322/p2wpkh.rs` (reference production sign body — Phase 19 mirrors this shape):
- `pub(crate) fn sign(spk: &Script, key: &SecretKey, message: &[u8]) -> Result<Witness, super::Bip322Error>` (lines 46-72; shipped Phase 15)

From `client/src/wallet.rs` (parity-test caller — UNCHANGED by Plan 19-01):
- `impl BdkClientWallet { pub fn from_descriptor(descriptor, outpoint, utxo_address, network, script_type) -> Result<Self, ...> }` (~lines 135-140; accepts single-key WIF descriptors `tr(<WIF>)` and `sh(wpkh(<WIF>))` per RESEARCH Q2)
- `impl BdkClientWallet { pub fn script_pubkey(&self) -> &Script; pub fn sign_bip322(&self, message: &str) -> Result<SignedOwnershipProof, ...> }` (~lines 496-602)
- `pub struct SignedOwnershipProof { pub witness: Witness, pub witness_stack: Vec<Vec<u8>>, pub script_type: ScriptType, pub final_script_sig: Option<ScriptBuf>, ... }`

From `client/tests/wallet_sign_roundtrip.rs` (existing fixtures — reused verbatim):
- `const TEST_WIF: &str = ...` (line 40 — canonical Bitcoin Core regtest "Hello World" WIF)
- `const DUMMY_OUTPOINT: OutPoint = ...` (lines 32-33)
- `const TEST_MESSAGE: &str = ...` (line 35)
- Existing test count: 7 functions (per PATTERNS.md correction; CONTEXT D-120 "4 existing" is OFF)

From `bitcoin` 0.32.8 / `secp256k1` 0.29.1 / `bdk_wallet` 2.3.0 (pinned in Cargo.lock):
- `bitcoin::secp256k1::Secp256k1::new()`, `sign_schnorr_no_aux_rand(&Message, &Keypair) -> schnorr::Signature`, `sign_ecdsa(&Message, &SecretKey) -> ecdsa::Signature`
- `bitcoin::secp256k1::Keypair::from_secret_key(&Secp256k1, &SecretKey)`
- `bitcoin::key::TapTweak::tap_tweak(self, &Secp256k1, Option<TapNodeHash>) -> TweakedKeypair`
- `bitcoin::sighash::SighashCache::new(&Transaction)`, `.taproot_key_spend_signature_hash(input_index, &Prevouts, TapSighashType) -> TapSighash`, `.p2wpkh_signature_hash(input_index, &Script, Amount, EcdsaSighashType) -> LegacySighash`
- `bitcoin::ScriptBuf::new_p2tr_tweaked(TweakedPublicKey)`, `new_p2wpkh(&WPubkeyHash)`, `new_p2sh(&ScriptHash)`
- `bitcoin::PublicKey::new(secp_pk)`, `.wpubkey_hash() -> Option<WPubkeyHash>` (always `Some(_)` for compressed)
- `bitcoin::script::Builder::new().push_slice(<&PushBytes>).into_script() -> ScriptBuf`
- `<&bitcoin::script::PushBytes>::try_from(&[u8]) -> Result<&PushBytes, PushBytesError>` (use for variable-length slices; RESEARCH Q3 verifies `redeem.as_bytes()` requires this path)
- `bdk_wallet` 2.3 emits Schnorr via `sign_schnorr_no_aux_rand` per RESEARCH Q1 (HIGH confidence) — byte-equality parity is SAFE.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship p2tr::sign production body + D-111 cross-check</name>
  <files>shared/src/bip322/p2tr.rs</files>
  <read_first>
    - shared/src/bip322/p2tr.rs (the full file, including the existing `pub(crate) fn sign` skeleton with `todo!()` at lines 33-44 AND the `pub(crate) fn sign_for_tests` lift source at lines 46-95)
    - shared/src/bip322/p2wpkh.rs (lines 46-72 — sibling reference production sign body Phase 19 mirrors structurally)
    - shared/src/bip322/mod.rs (lines 45-138 — `bip322_message_hash`, `build_bip322_to_spend`, `build_bip322_to_sign` script-neutral primitives; lines 184-188 — `Bip322Error::ScriptTypeMismatch` variant; lines 261-272 — `sign_simple` dispatcher that routes to this fn)
    - .planning/phases/19-multi-script-signing-finish/19-CONTEXT.md §"D. P2TR Schnorr nonce strategy" (D-114, D-115) + §"E. Production sign body promotion strategy" (D-116, D-117) + §"C. Defense-in-depth: spk ↔ key cross-check inside `sign`" (D-111, D-112, D-113)
    - .planning/phases/19-multi-script-signing-finish/19-RESEARCH.md §Q1 (bdk_wallet uses `sign_schnorr_no_aux_rand` — byte-equality SAFE; subtle gotcha re `Message::from(sighash)` vs `Message::from_digest(sighash.to_byte_array())`)
    - .planning/phases/19-multi-script-signing-finish/19-PATTERNS.md §"File: shared/src/bip322/p2tr.rs (modified)" — "Modification 1" Adaptation notes 1-5 AND Shared Patterns §"Pattern 1" (production sign body shape) AND §"Pattern 6" (inline plan-comment provenance per CD-38)
  </read_first>
  <behavior>
    - sign(spk, key, message) succeeds when spk byte-equals ScriptBuf::new_p2tr_tweaked(tap_tweak(Keypair::from_secret_key(secp, key), &secp, None).to_keypair().x_only_public_key().0.dangerous_assume_tweaked()); returns Witness containing one element of length 64 (BIP-341 SIGHASH_DEFAULT — no sighash byte appended).
    - sign(spk, key, message) returns Err(Bip322Error::ScriptTypeMismatch { declared, derived: ScriptType::P2tr }) when the derived expected_spk does not byte-equal spk; `declared` = `detect_script_type(spk)?`.
    - Round-trip via verify_simple(ScriptType::P2tr, spk, &witness, message, Network::Signet) returns Ok(()).
    - Determinism: two calls with the same (spk, key, message) produce byte-identical witness (sign_schnorr_no_aux_rand — no aux-rand).
    - No bdk_wallet dependency reachable from this fn (CONTEXT Phase 14 ADR Decision #4 + Phase 15 CD-6).
  </behavior>
  <action>
    Replace the body of `pub(crate) fn sign` at `shared/src/bip322/p2tr.rs:33-44` (currently `todo!("Phase 17 WALLET-02 wires bdk_wallet sign per ADR #4")`). The signature stays exactly as declared: `pub(crate) fn sign(spk: &Script, key: &SecretKey, message: &[u8]) -> Result<Witness, super::Bip322Error>`. Drop the leading underscores from the param names at lines 39-41 (they become load-bearing).

    Body steps (lift `sign_for_tests:46-95` verbatim, then transform per CONTEXT D-116 + D-111 + CD-38 — see PATTERNS.md §"Modification 1" Adaptation notes 1-5 for the exact step list):

    Step A — Add inline comment at top per CD-38 default: `// Plan 19-01 Task 1 — BIP322-05 production body; lifted from sign_for_tests per D-116 + D-111 cross-check.`

    Step B — Insert D-111 spk-vs-key cross-check FIRST (before any sighash work). Algorithm per D-113 P2TR rule: build `secp = Secp256k1::new()`, `keypair = Keypair::from_secret_key(&secp, key)`, `tweaked = keypair.tap_tweak(&secp, None)`, `tweaked_xonly = tweaked.to_keypair().x_only_public_key().0`, `expected_spk = ScriptBuf::new_p2tr_tweaked(tweaked_xonly.dangerous_assume_tweaked())`. Compare `expected_spk.as_script()` byte-equal to `spk`. On mismatch return `Err(super::Bip322Error::ScriptTypeMismatch { declared: super::detect_script_type(spk)?, derived: super::ScriptType::P2tr })`. The `?` propagates a different error variant (e.g. `UnsupportedScriptType`) when `spk` is not a recognised single-key shape — that's the correct behavior (we never reach the sign step on an unrecognisable spk).

    Step C — Reuse `secp` + `keypair` + `tweaked` from Step B. The remaining body lifts `sign_for_tests:78-94` verbatim: `msg_hash = super::bip322_message_hash(message)`, `to_spend = super::build_bip322_to_spend(spk, &msg_hash)`, `to_sign = super::build_bip322_to_sign(&to_spend)`, `mut cache = SighashCache::new(&to_sign)`, `sighash = cache.taproot_key_spend_signature_hash(0, &Prevouts::All(&[TxOut { value: Amount::ZERO, script_pubkey: spk.to_owned() }]), TapSighashType::Default).expect("sighash on well-formed to_sign")`, `sig = secp.sign_schnorr_no_aux_rand(&Message::from_digest(sighash.to_byte_array()), &tweaked.to_keypair())`.

    Step D — Build witness: `let mut w = Witness::new(); w.push(sig.as_ref()); Ok(w)`. Wrap in `Ok(...)` (D-116 instruction).

    Imports: lift the existing `use` block from `sign_for_tests` lines 65-69 to the top of this fn body (or convert to module-level if cleaner). The existing `pub(crate) fn sign_for_tests` at lines 46-95 STAYS for now; Plan 19-02 deletes it.

    Do NOT delete `sign_for_tests` in this task.

    Do NOT add a P2WPKH cross-check (per CONTEXT Deferred Ideas and PATTERNS no-D-111-for-P2WPKH note).
  </action>
  <verify>
    <automated>cargo build -p shared &amp;&amp; cargo test -p shared --lib bip322 &amp;&amp; cargo test -p shared --test per_script_vectors</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'todo!' shared/src/bip322/p2tr.rs` returns 0 (production body shipped; `sign_for_tests` lift source has no `todo!` either).
    - Source: `grep -c 'sign_schnorr_no_aux_rand' shared/src/bip322/p2tr.rs` returns at least 2 (once in `sign`, once in `sign_for_tests` which still exists).
    - Source: `grep -c 'ScriptTypeMismatch' shared/src/bip322/p2tr.rs` returns at least 1 (D-111 cross-check).
    - Source: `grep -n 'pub(crate) fn sign(' shared/src/bip322/p2tr.rs` shows production signature unchanged (param names are `spk`, `key`, `message` — no leading underscores).
    - Source: `grep -c 'fn sign_for_tests' shared/src/bip322/p2tr.rs` returns 1 (still load-bearing at this plan boundary).
    - Test (boundary, must STAY green): `cargo test -p shared` shows lib tests passing AND `cargo test -p shared --test per_script_vectors` 7/7 passing (the P2TR per-script-vector test verifies the new sign body's witness round-trips via verify_simple).
    - Test (boundary, must STAY green): `cargo test -p shared --test bip322_cross_shape` 9/9.
    - Lint: `cargo clippy -p shared --all-targets -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>
    p2tr::sign returns `Result<Witness, Bip322Error>` with production Schnorr keypath body (no `todo!()`); D-111 cross-check fires before any sighash work; existing per_script_vectors P2TR positive-vector test still passes (now exercising the production body through `sign_simple_test_only` which dispatches via `sign_simple` to `sign`).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship p2sh_p2wpkh::sign production body + D-111 cross-check + D-117 spk-used-directly</name>
  <files>shared/src/bip322/p2sh_p2wpkh.rs</files>
  <read_first>
    - shared/src/bip322/p2sh_p2wpkh.rs (the full file, including the existing `pub(crate) fn sign` skeleton with `todo!()` at lines 39-50 AND the `pub(crate) fn sign_for_tests` lift source at lines 52-108)
    - shared/src/bip322/p2wpkh.rs (lines 46-72 — sibling production sign body reference)
    - shared/src/bip322/mod.rs (same primitives + `Bip322Error::ScriptTypeMismatch` variant + `detect_script_type` dispatcher)
    - .planning/phases/19-multi-script-signing-finish/19-CONTEXT.md §"E. Production sign body promotion strategy" (D-116, D-117 — `_spk` becomes load-bearing `spk` after cross-check) + §"C. Defense-in-depth: spk ↔ key cross-check inside `sign`" (D-111, D-112, D-113 — P2SH-P2WPKH cross-check algorithm: derive `redeem` from `compressed.wpubkey_hash()`, build `expected_spk = ScriptBuf::new_p2sh(&redeem.script_hash())`, compare byte-equal)
    - .planning/phases/19-multi-script-signing-finish/19-PATTERNS.md §"File: shared/src/bip322/p2sh_p2wpkh.rs (modified)" — "Modification 1" Adaptation notes 1-7 AND Shared Patterns §"Pattern 1" (production sign body shape)
  </read_first>
  <behavior>
    - sign(spk, key, message) succeeds when spk byte-equals ScriptBuf::new_p2sh(&ScriptBuf::new_p2wpkh(&PublicKey::new(PublicKey::from_secret_key(secp, key)).wpubkey_hash().expect("compressed")).script_hash()); returns Witness with exactly 2 elements [der_sig_with_sighash_all, compressed_pubkey_serialized_33_bytes].
    - sign(spk, key, message) returns Err(Bip322Error::ScriptTypeMismatch { declared, derived: ScriptType::P2shP2wpkh }) when expected_spk does not byte-equal spk.
    - Sighash is computed over the UNWRAPPED P2WPKH redeem (NOT the outer P2SH SPK) — this is structural per BIP-143 and unchanged from `sign_for_tests`.
    - The `spk` param is now load-bearing for `build_bip322_to_spend` (per D-117 — replaces the footgun rebuild-from-key in `sign_for_tests:85-86`).
    - Determinism: ECDSA via secp.sign_ecdsa is RFC 6979 deterministic — two calls with same (spk, key, message) produce byte-identical witness.
    - verify_simple(ScriptType::P2shP2wpkh, &spk, &witness, message, network) returns Ok(()) on the produced witness.
  </behavior>
  <action>
    Replace the body of `pub(crate) fn sign` at `shared/src/bip322/p2sh_p2wpkh.rs:39-50` (currently `todo!("Phase 17 WALLET-02 wires bdk_wallet sign per ADR #4")`). Signature stays at lines 39-43 unchanged in shape — but rename `_spk` to `spk` (line 45 per PATTERNS.md), drop `_key`/`_message` underscores. Result type stays `Result<Witness, super::Bip322Error>`.

    Body steps (lift `sign_for_tests:52-108` then transform per CONTEXT D-116 + D-111 + D-117 + CD-38 — see PATTERNS.md §"Modification 1" Adaptation notes 1-7):

    Step A — Add inline comment per CD-38 default: `// Plan 19-01 Task 2 — BIP322-06 production body; lifted from sign_for_tests per D-116 + D-111 cross-check + D-117 spk-used-directly.`

    Step B — Insert D-111 spk-vs-key cross-check FIRST. Algorithm per D-113 P2SH-P2WPKH rule: `secp = Secp256k1::new()`, `pubkey = bitcoin::secp256k1::PublicKey::from_secret_key(&secp, key)`, `compressed = PublicKey::new(pubkey)`, `unwrapped_p2wpkh = ScriptBuf::new_p2wpkh(&compressed.wpubkey_hash().expect("compressed key"))`, `expected_spk = ScriptBuf::new_p2sh(&unwrapped_p2wpkh.script_hash())`. Compare `expected_spk.as_script()` byte-equal to `spk`. On mismatch return `Err(super::Bip322Error::ScriptTypeMismatch { declared: super::detect_script_type(spk)?, derived: super::ScriptType::P2shP2wpkh })`.

    Step C — Reuse `secp`, `pubkey`, `compressed`, `unwrapped_p2wpkh` from Step B. The remaining body lifts `sign_for_tests:81-107` with the D-117 swap: `msg_hash = super::bip322_message_hash(message)`, `to_spend = super::build_bip322_to_spend(spk, &msg_hash)` (**use `spk` directly per D-117** — DROP the `let p2sh_spk = ScriptBuf::new_p2sh(...)` rebuild line at the analog's line 85, since the cross-check just proved `spk == expected_spk` byte-equal), `to_sign = super::build_bip322_to_sign(&to_spend)`, `mut cache = SighashCache::new(&to_sign)`, `sighash = cache.p2wpkh_signature_hash(0, &unwrapped_p2wpkh, Amount::ZERO, EcdsaSighashType::All).expect("sighash on well-formed to_sign")` (**sighash uses `unwrapped_p2wpkh`** — BIP-143 structural rule, NOT `spk`).

    Step D — Build witness: `secp_msg = Message::from_digest(sighash.to_byte_array())`, `sig = secp.sign_ecdsa(&secp_msg, key)`, `mut sig_bytes = sig.serialize_der().to_vec()`, `sig_bytes.push(0x01)` (SIGHASH_ALL byte), then `let mut w = Witness::new(); w.push(sig_bytes); w.push(pubkey.serialize()); Ok(w)`.

    Imports: lift the existing `use` block from `sign_for_tests` lines 72-75 to the top of this fn body. The `sign_for_tests` fn at lines 52-108 STAYS for now (Plan 19-02 deletes it).
  </action>
  <verify>
    <automated>cargo build -p shared &amp;&amp; cargo test -p shared --lib bip322 &amp;&amp; cargo test -p shared --test per_script_vectors</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'todo!' shared/src/bip322/p2sh_p2wpkh.rs` returns 0.
    - Source: `grep -c 'p2wpkh_signature_hash' shared/src/bip322/p2sh_p2wpkh.rs` returns at least 2 (once in `sign`, once in `sign_for_tests` which still exists).
    - Source: `grep -c 'ScriptTypeMismatch' shared/src/bip322/p2sh_p2wpkh.rs` returns at least 1 (D-111).
    - Source: production `sign` param renamed — `grep -nE 'pub\(crate\) fn sign\(' shared/src/bip322/p2sh_p2wpkh.rs` shows `spk:` not `_spk:` for the production sign (the `sign_for_tests` at lines 52-108 keeps its `_spk:` until Plan 19-02 deletion).
    - Source: `grep -c 'fn sign_for_tests' shared/src/bip322/p2sh_p2wpkh.rs` returns 1 (still load-bearing at this plan boundary).
    - Test (boundary, must STAY green): `cargo test -p shared --test per_script_vectors` 7/7 (P2SH-P2WPKH positive-vector test now exercises the production body).
    - Test (boundary, must STAY green): `cargo test -p shared --test bip322_cross_shape` 9/9.
    - Test (boundary, must STAY green): `cargo test --test integration full_round` 8/8 (v1.3 invariant — shared/-internal change must not regress this).
    - Test (boundary, must STAY green): `cargo test --test integration mixed_script_e2e` 1/1 (v1.4 invariant).
    - Lint: `cargo clippy -p shared --all-targets -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>
    p2sh_p2wpkh::sign returns `Result<Witness, Bip322Error>` with production BIP-143 body (no `todo!()`); D-111 cross-check fires before any sighash work; D-117 spk-used-directly removes the rebuild-from-key footgun (param `_spk` → `spk`); per_script_vectors + cross_shape + full_round + mixed_script_e2e all green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add p2sh_p2wpkh_final_script_sig helper + 3 unit tests in mod.rs</name>
  <files>shared/src/bip322/mod.rs</files>
  <read_first>
    - shared/src/bip322/mod.rs (the full file — focus on `sign_simple` body ending at line 272, the `sign_simple_test_only` block starting at line 274 (untouched in this task), the `Bip322Error` variant at lines 184-188, the existing `#[cfg(test)] mod tests` block starting at ~line 339 and ending at line 567 with the PII-safety test, and the `fixture_secret_key` / `fixture_p2wpkh_spk` / `fixture_p2tr_spk` / `fixture_p2sh_spk` fixtures at lines 441-474)
    - .planning/phases/19-multi-script-signing-finish/19-CONTEXT.md §"B. New `p2sh_p2wpkh_final_script_sig` helper" (D-109, D-110) — signature shape, body sketch, infallibility rationale + §"A. P2SH-P2WPKH sign API surface" (D-108) — unit test addition + CD-34 (Builder convention) + CD-37 (cross-check unit tests default = yes) + CD-36 (doc-comment update for Bip322Error::ScriptTypeMismatch dual-meaning)
    - .planning/phases/19-multi-script-signing-finish/19-RESEARCH.md §Q3 — full body source for `p2sh_p2wpkh_final_script_sig` (the `<&PushBytes>::try_from(...).expect(...)` path is mandatory because `&[u8]` doesn't impl AsRef<PushBytes>) + the byte-count correction (helper output is 23 bytes, NOT 24 as CONTEXT D-110 states — verify at runtime with `dbg!` before pinning the constant)
    - .planning/phases/19-multi-script-signing-finish/19-PATTERNS.md §"File: shared/src/bip322/mod.rs (modified)" — "Add 1" (helper body, ANALOG = `bip322_message_hash` at lines 45-53) + "Add 2" (unit test body, ANALOG = `detect_script_type_returns_p2wpkh_for_p2wpkh_spk` at lines 476-480) + "Add 3" (D-111 cross-check rejection tests, ANALOG = `detect_script_type_rejects_op_return_with_unsupported_script_type` at lines 495-499) + Shared Patterns §"Pattern 3" (fixture seed) + §"Pattern 4" (PII-safe ScriptTypeMismatch reuse)
  </read_first>
  <behavior>
    - p2sh_p2wpkh_final_script_sig(pubkey) is infallible (takes `&secp256k1::PublicKey`, which is always 33-byte compressed, so `bitcoin::PublicKey::new(_).wpubkey_hash()` always returns `Some(_)`); returns a `ScriptBuf` of exactly 23 bytes with shape `[0x16, 0x00, 0x14, <20-byte HASH160(pubkey)>]`.
    - For the fixture key (0x42-seed), the helper output's byte 0 is `0x16` (OP_PUSHBYTES_22), byte 1 is `0x00` (OP_0), byte 2 is `0x14` (OP_PUSHBYTES_20), bytes 3..23 equal `bitcoin::PublicKey::new(secp_pk).wpubkey_hash().expect(...).as_ref()` byte-for-byte.
    - sign_simple(ScriptType::P2tr, &non_p2tr_fixture_spk, &fixture_key, b"x") returns Err(Bip322Error::ScriptTypeMismatch { declared: <derived from spk>, derived: ScriptType::P2tr }).
    - sign_simple(ScriptType::P2shP2wpkh, &p2tr_fixture_spk, &fixture_key, b"x") returns Err(Bip322Error::ScriptTypeMismatch { declared: ScriptType::P2tr, derived: ScriptType::P2shP2wpkh }).
  </behavior>
  <action>
    Three additions to `shared/src/bip322/mod.rs`:

    Addition 1 — Insert the helper sibling to `sign_simple`. Insert location: after `sign_simple`'s closing `}` at line 272, BEFORE the `sign_simple_test_only` block header comment at line 274. Body per RESEARCH §Q3 (lift verbatim) and PATTERNS.md §"Add 1":
    - Signature: `pub fn p2sh_p2wpkh_final_script_sig(pubkey: &bitcoin::secp256k1::PublicKey) -> ScriptBuf`.
    - Body: derive `compressed = bitcoin::PublicKey::new(*pubkey)`, `wpkh = compressed.wpubkey_hash().expect("compressed pubkey always has wpubkey_hash")`, `redeem = ScriptBuf::new_p2wpkh(&wpkh)`. Build the scriptSig via `bitcoin::blockdata::script::Builder::new().push_slice(<&bitcoin::script::PushBytes>::try_from(redeem.as_bytes()).expect("22-byte redeem fits push limit (520 bytes)")).into_script()`. The `<&PushBytes>::try_from(...)` path is MANDATORY (RESEARCH Q3): `&[u8]` does NOT implement `AsRef<PushBytes>`; only fixed-size arrays `[u8; N]` do via the `from_array!` macro, and `redeem.as_bytes()` returns `&[u8]` (not `&[u8; 22]`).
    - Doc comment: brief 3-4 line summary citing BIP-141 and noting infallibility.

    Addition 2 — Add the helper byte-shape unit test inside `mod tests` block at end of file (insert before the closing `}` of `mod tests`, after the existing PII-safety test at line 567). Test name per CONTEXT D-108: `p2sh_p2wpkh_final_script_sig_derives_correctly`. Body per PATTERNS.md §"Add 2" (lift verbatim):
    - Uses existing `fixture_secret_key()` at lines 441-443 (reused — DO NOT add new fixture).
    - Constructs `pk = PublicKey::from_secret_key(&secp, &sk)`.
    - Asserts `script_sig.as_bytes().len() == 23` (RESEARCH Q3 byte-count correction — CONTEXT D-110 says 24, the correct count is 23).
    - Asserts `bytes[0] == 0x16`, `bytes[1] == 0x00`, `bytes[2] == 0x14`.
    - Asserts `&bytes[3..23] == compressed.wpubkey_hash().expect("compressed").as_ref()`.
    - **Smoke-check first:** before committing, add a temporary `dbg!(script_sig.as_bytes().len());` print and run the test once locally to confirm the runtime length is 23 — remove the `dbg!` before final commit (per RESEARCH Q3 [ASSUMED] caveat).

    Addition 3 — Add 2 cross-check rejection tests per CD-37 default = yes. Insert immediately after Addition 2. Test names: `p2tr_sign_rejects_p2sh_p2wpkh_spk_with_p2tr_key` and `p2sh_p2wpkh_sign_rejects_p2tr_spk_with_p2sh_p2wpkh_key` (planner-context spec; see naming note below). Use the `sign_simple(...).expect_err(...) + matches!(err, Bip322Error::ScriptTypeMismatch { declared: ..., derived: ... })` shape from PATTERNS.md §"Add 3". Use the EXISTING `fixture_p2wpkh_spk`, `fixture_p2tr_spk`, `fixture_p2sh_spk`, `fixture_secret_key` fixtures at lines 441-474 — DO NOT add new fixtures.

    Naming reconciliation: the planner-context names use a P2SH-P2WPKH-spk-with-P2TR-key pairing for the first test (and vice versa for the second). PATTERNS bodies sketched P2WPKH-spk versions for convenience. The load-bearing assertion is the `ScriptTypeMismatch { declared: <whatever spk fixture detects to>, derived: <key's script type> }` shape — so:
    - Test 1: `p2tr_sign_rejects_p2sh_p2wpkh_spk_with_p2tr_key` — call `sign_simple(ScriptType::P2tr, &fixture_p2sh_spk(), &fixture_secret_key(), b"x").expect_err(...)`; assert matches `ScriptTypeMismatch { declared: ScriptType::P2shP2wpkh, derived: ScriptType::P2tr }`.
    - Test 2: `p2sh_p2wpkh_sign_rejects_p2tr_spk_with_p2sh_p2wpkh_key` — call `sign_simple(ScriptType::P2shP2wpkh, &fixture_p2tr_spk(), &fixture_secret_key(), b"x").expect_err(...)`; assert matches `ScriptTypeMismatch { declared: ScriptType::P2tr, derived: ScriptType::P2shP2wpkh }`.

    If `fixture_p2sh_spk` does not exist in the fixtures block, either (a) add one mirroring `fixture_p2tr_spk` (lines ~459-474) using `ScriptBuf::new_p2sh(&ScriptBuf::new_p2wpkh(&compressed.wpubkey_hash().expect("compressed")).script_hash())` derived from `fixture_secret_key()`, OR (b) substitute `fixture_p2wpkh_spk()` and rename Test 1 to `p2tr_sign_rejects_p2wpkh_spk_with_p2tr_key` with `declared: ScriptType::P2wpkh`. Pick whichever costs fewer lines.

    Per CD-36 default — update the doc-comment on the `Bip322Error::ScriptTypeMismatch` variant at lines 184-188 to note the dual meaning: "Also returned by `p2tr::sign` and `p2sh_p2wpkh::sign` when the supplied `spk` does not match the derivation from the supplied `SecretKey` — `declared` = the script type derived from the on-chain `script_pubkey` arg via `detect_script_type`, `derived` = the script type the SecretKey corresponds to (per D-112 in 19-CONTEXT.md)."

    Per CD-38 default — also update the `sign_simple` dispatcher's doc comment at lines 258-260 to remove the reference to `todo!()` for P2TR + P2SH-P2WPKH (the production bodies ship in this plan).
  </action>
  <verify>
    <automated>cargo test -p shared --lib bip322 &amp;&amp; cargo clippy -p shared --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'pub fn p2sh_p2wpkh_final_script_sig' shared/src/bip322/mod.rs` returns 1.
    - Source: the new helper is a sibling of `sign_simple`, inserted between line 272 (close of `sign_simple`) and the `sign_simple_test_only` block at line 274 — verify with `grep -n 'pub fn p2sh_p2wpkh_final_script_sig\|sign_simple_test_only\|fn sign_simple' shared/src/bip322/mod.rs` (the helper appears between the two).
    - Source: helper body contains `<&bitcoin::script::PushBytes>::try_from` (`grep -c 'PushBytes' shared/src/bip322/mod.rs` returns at least 1 NEW occurrence).
    - Test: `cargo test -p shared --lib bip322::tests::p2sh_p2wpkh_final_script_sig_derives_correctly` exits 0 — asserts `bytes.len() == 23` (not 24 — CONTEXT D-110 off-by-one corrected per RESEARCH Q3).
    - Test: `cargo test -p shared --lib bip322::tests::p2tr_sign_rejects_p2sh_p2wpkh_spk_with_p2tr_key` exits 0 (OR the renamed `p2tr_sign_rejects_p2wpkh_spk_with_p2tr_key` per fixture availability).
    - Test: `cargo test -p shared --lib bip322::tests::p2sh_p2wpkh_sign_rejects_p2tr_spk_with_p2sh_p2wpkh_key` exits 0.
    - Behavior: `sign_simple(ScriptType::P2tr, &fixture_p2sh_or_p2wpkh_spk(), &fixture_secret_key(), b"x")` returns `Err(Bip322Error::ScriptTypeMismatch { declared: <ScriptType matching the spk>, derived: ScriptType::P2tr })`.
    - Behavior: `sign_simple(ScriptType::P2shP2wpkh, &fixture_p2tr_spk(), &fixture_secret_key(), b"x")` returns `Err(Bip322Error::ScriptTypeMismatch { declared: ScriptType::P2tr, derived: ScriptType::P2shP2wpkh })`.
    - Source: no temporary `dbg!(` left in the file — `grep -c 'dbg!' shared/src/bip322/mod.rs` returns 0.
    - Test (boundary, must STAY green): `cargo test -p shared` lib + integration suites all green; PII-safety test at lines 512-565 still passes unchanged (variant doc-comment update does not affect Display output per CONTEXT D-112).
    - Lint: `cargo clippy -p shared --all-targets -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>
    `p2sh_p2wpkh_final_script_sig` ships as a `pub fn` sibling to `sign_simple`; 3 new unit tests pass (helper byte-shape + 2 cross-check rejections); `Bip322Error::ScriptTypeMismatch` variant doc-comment notes the dual meaning per D-112; `sign_simple` doc-comment no longer references `todo!()`; all boundary tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add 2 byte-equality parity tests in client/tests/wallet_sign_roundtrip.rs</name>
  <files>client/tests/wallet_sign_roundtrip.rs</files>
  <read_first>
    - client/tests/wallet_sign_roundtrip.rs (the full file — 7 existing test fns per PATTERNS.md, including `p2tr_descriptor_sign_roundtrip_verifies` at lines 74-95 and `p2sh_p2wpkh_descriptor_sign_roundtrip_verifies` at lines 98-122; reusable constants `TEST_WIF` at line 40, `DUMMY_OUTPOINT` at lines 32-33, `TEST_MESSAGE` at line 35, `NET = Network::Signet` at line 46; insertion point is AFTER the last `#[tokio::test]` fn ending at line 179, BEFORE the `dummy_outpoint_is_well_formed` non-tokio test at line 184)
    - client/src/wallet.rs (lines 135-140 — `BdkClientWallet::from_descriptor` signature; lines 496-602 — `sign_bip322` body, returns `SignedOwnershipProof { witness, witness_stack, script_type, final_script_sig, ... }`)
    - shared/src/bip322/mod.rs (the new `sign_simple` arm that now dispatches to the Task 1 + Task 2 production bodies — must be merged before this task runs)
    - .planning/phases/19-multi-script-signing-finish/19-CONTEXT.md §"F. Parity test (BIP322-05 SC#1 closure)" (D-118, D-119) — full assertion shape and fixture-location rationale + CD-35 (test name discretion)
    - .planning/phases/19-multi-script-signing-finish/19-RESEARCH.md §Q1 (bdk uses `sign_schnorr_no_aux_rand` — byte-equality SAFE for P2TR; ECDSA is RFC 6979 deterministic — always SAFE for P2SH-P2WPKH) + §Q2 (full test-shape with `tr({WIF})` and `sh(wpkh({WIF}))` single-key descriptors + Note A on `utxo_address` derivation from the same key)
    - .planning/phases/19-multi-script-signing-finish/19-PATTERNS.md §"File: client/tests/wallet_sign_roundtrip.rs (modified)" — "Add 1" full body for `p2tr_shared_sign_matches_bdk_sign_byte_for_byte` + "Add 2" full body for `p2sh_p2wpkh_shared_sign_matches_bdk_sign_byte_for_byte` (both bodies are lift-verbatim from RESEARCH Q2 with derived `utxo_address` per Note A)
  </read_first>
  <behavior>
    - Both parity tests construct a single-key descriptor wallet from the SAME `TEST_WIF` constant the file already defines (RESEARCH Q2 verified bdk_wallet 2.3 accepts `tr(<WIF>)` and `sh(wpkh(<WIF>))` directly).
    - Both tests recover the SecretKey via `bitcoin::PrivateKey::from_wif(TEST_WIF).expect("test WIF is valid").inner` so the bdk descriptor sign path and the shared::bip322 sign path see the SAME SecretKey.
    - Both tests assert `bdk_signed.witness == shared_witness` byte-equal. Safe per RESEARCH Q1: P2TR uses `sign_schnorr_no_aux_rand` (deterministic); P2SH-P2WPKH uses `sign_ecdsa` (RFC 6979 deterministic).
    - Both tests USE `Network::Regtest` (NOT `NET = Network::Signet`) — the `TEST_WIF` is the canonical Bitcoin Core regtest "Hello World" WIF and bdk's WIF parsing requires the network to match (PATTERNS §"Adaptation notes" line 5).
    - Tests inherit the file's existing `#[tokio::test] async fn` convention (PATTERNS §"Add 1" insertion point).
  </behavior>
  <action>
    Insert 2 new `#[tokio::test] async fn`s into `client/tests/wallet_sign_roundtrip.rs`. Insertion point: AFTER the last existing `#[tokio::test]` fn (`signed_proof_script_type_matches_wallet_script_type` ending at line 179), BEFORE the `dummy_outpoint_is_well_formed` non-tokio test at line 184. Preserve the file's logical ordering: all `#[tokio::test]` fns first, defensive sanity test last.

    Step A — Add a shared constants + helper block at the top of the file (or just before the new test fns) per PATTERNS.md §"Add 1" body:
    - `const PARITY_TEST_MESSAGE: &str = "blindjoin:19-01:parity:byte-for-byte";`
    - `fn parity_secret_key() -> bitcoin::secp256k1::SecretKey { bitcoin::PrivateKey::from_wif(TEST_WIF).expect("test WIF is valid").inner }`
    - A pair of small helpers `fn parity_p2tr_address() -> bitcoin::Address` and `fn parity_p2sh_p2wpkh_address() -> bitcoin::Address` per RESEARCH Q2 Note A:
      - P2TR address: derive `pk = PublicKey::from_secret_key(&secp, &sk)`, `keypair = Keypair::from_secret_key(&secp, &sk)`, `tweaked = TapTweak::tap_tweak(keypair, &secp, None)`, `tweaked_xonly = tweaked.to_keypair().x_only_public_key().0`, `Address::p2tr_tweaked(tweaked_xonly.dangerous_assume_tweaked(), Network::Regtest)`.
      - P2SH-P2WPKH address: derive `pk = PublicKey::from_secret_key(&secp, &sk)`, `compressed = bitcoin::PublicKey::new(pk)`, `wpkh = compressed.wpubkey_hash().expect("compressed key")`, `redeem = ScriptBuf::new_p2wpkh(&wpkh)`, `Address::p2sh(&redeem, Network::Regtest).expect("p2sh derivation")`.

    Step B — Add the P2TR parity test per PATTERNS.md §"Add 1" body (lift verbatim):
    - Name: `p2tr_shared_sign_matches_bdk_sign_byte_for_byte` (D-118 / CD-35 default).
    - Descriptor: `format!("tr({TEST_WIF})")`.
    - Wallet: `BdkClientWallet::from_descriptor(&descriptor, DUMMY_OUTPOINT, &parity_p2tr_address().to_string(), Network::Regtest, ScriptType::P2tr).expect("P2TR single-key WIF descriptor should construct")`.
    - Call `wallet.sign_bip322(PARITY_TEST_MESSAGE).expect("bdk sign_bip322 should succeed")` → `bdk_signed`.
    - Call `shared::bip322::sign_simple(ScriptType::P2tr, &wallet.script_pubkey(), &parity_secret_key(), PARITY_TEST_MESSAGE.as_bytes()).expect("shared::bip322::sign_simple P2TR should succeed")` → `shared_witness`.
    - Assert `assert_eq!(bdk_signed.witness, shared_witness, "P2TR bdk vs shared::bip322 witnesses must be byte-equal (D-118)")`.

    Step C — Add the P2SH-P2WPKH parity test per PATTERNS.md §"Add 2" body (lift verbatim, same shape as Step B with the P2SH-P2WPKH derivation):
    - Name: `p2sh_p2wpkh_shared_sign_matches_bdk_sign_byte_for_byte` (D-119 / CD-35 default).
    - Descriptor: `format!("sh(wpkh({TEST_WIF}))")`.
    - Wallet: `BdkClientWallet::from_descriptor(&descriptor, DUMMY_OUTPOINT, &parity_p2sh_p2wpkh_address().to_string(), Network::Regtest, ScriptType::P2shP2wpkh).expect("P2SH-P2WPKH single-key WIF descriptor should construct")`.
    - Same sign-and-assert pattern as Step B.

    Step D — Defensive sanity assertion (PATTERNS.md §"Spk-vs-bdk-derived-address cross-check"): before the `sign_bip322` call in each test, add `assert_eq!(wallet.script_pubkey(), <expected_spk_derived_from_key>);` so a regression in bdk's single-key descriptor parsing fails the test loudly before the sign call. Use `ScriptBuf::new_p2tr_tweaked(...)` for P2TR and `ScriptBuf::new_p2sh(&ScriptBuf::new_p2wpkh(&wpkh).script_hash())` for P2SH-P2WPKH (RESEARCH Q2 Note A).

    Per CD-35 default — do NOT consolidate into a parameterised test; keep the 2 separate `#[tokio::test] async fn`s for symmetry with the existing per-script tests in the file.
  </action>
  <verify>
    <automated>cargo test -p client --test wallet_sign_roundtrip p2tr_shared_sign_matches_bdk_sign_byte_for_byte &amp;&amp; cargo test -p client --test wallet_sign_roundtrip p2sh_p2wpkh_shared_sign_matches_bdk_sign_byte_for_byte &amp;&amp; cargo test -p client --test wallet_sign_roundtrip</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'p2tr_shared_sign_matches_bdk_sign_byte_for_byte' client/tests/wallet_sign_roundtrip.rs` returns 1.
    - Source: `grep -c 'p2sh_p2wpkh_shared_sign_matches_bdk_sign_byte_for_byte' client/tests/wallet_sign_roundtrip.rs` returns 1.
    - Source: both tests carry `#[tokio::test]` attribute — `grep -B1 'p2tr_shared_sign_matches_bdk' client/tests/wallet_sign_roundtrip.rs | grep -c 'tokio::test'` returns 1; same for p2sh_p2wpkh.
    - Source: both tests call `shared::bip322::sign_simple(...)` (NOT `sign_simple_test_only`) — `grep -c 'shared::bip322::sign_simple(' client/tests/wallet_sign_roundtrip.rs` returns at least 2.
    - Test: `cargo test -p client --test wallet_sign_roundtrip p2tr_shared_sign_matches_bdk_sign_byte_for_byte` exits 0 (byte-equality holds per RESEARCH Q1 — sign_schnorr_no_aux_rand on both sides).
    - Test: `cargo test -p client --test wallet_sign_roundtrip p2sh_p2wpkh_shared_sign_matches_bdk_sign_byte_for_byte` exits 0 (byte-equality always holds for ECDSA — RFC 6979 deterministic).
    - Test (boundary, must STAY green): `cargo test -p client --test wallet_sign_roundtrip` shows 9/9 passing (7 existing + 2 new — per PATTERNS.md correction to CONTEXT D-120's "4 existing" miscount).
    - Test (boundary, must STAY green): `cargo test --test integration full_round` 8/8.
    - Test (boundary, must STAY green): `cargo test --test integration mixed_script_e2e` 1/1.
    - Lint: `cargo clippy -p client --all-targets -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>
    Two new byte-equality parity tests pass — confirming `shared::bip322::sign_simple` produces witnesses byte-equal to `BdkClientWallet::sign_bip322` for the same `(key, message)` for both P2TR and P2SH-P2WPKH; SC#1 closure (T-19-C mitigation) is empirical, not just structural.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → shared::bip322 sign | Caller provides (script_pubkey, SecretKey, message); sign produces a witness that any subsequent verifier (coordinator, p2p relay, on-chain) will check against the on-chain SPK. A mismatched (spk, key) pair silently producing a wrong-but-structurally-valid witness is the primary sign-side threat. |
| shared::bip322 sign → on-chain verifier | The produced witness must verify under BIP-322 against the on-chain SPK. Determinism of the signature is load-bearing for the parity-test closure of SC#1. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-A | Spoofing / Tampering | `shared::bip322::p2tr::sign` + `shared::bip322::p2sh_p2wpkh::sign` (the new production bodies in Tasks 1 + 2) | mitigate | D-111 spk-vs-key cross-check at the TOP of each new sign body; returns `Bip322Error::ScriptTypeMismatch { declared, derived }` EARLY when spk does not match the derivation from the supplied SecretKey. Verification: Task 3 adds 2 unit tests in `mod.rs::tests` (`p2tr_sign_rejects_p2sh_p2wpkh_spk_with_p2tr_key` + `p2sh_p2wpkh_sign_rejects_p2tr_spk_with_p2sh_p2wpkh_key`) asserting the rejection error variant and field values. |
| T-19-C | Information Disclosure (silent divergence) | shared::bip322 sign vs bdk_wallet sign on the same (key, message) | mitigate | Task 4 adds 2 byte-equality parity tests (`p2tr_shared_sign_matches_bdk_sign_byte_for_byte` + `p2sh_p2wpkh_shared_sign_matches_bdk_sign_byte_for_byte`) in `client/tests/wallet_sign_roundtrip.rs` asserting `bdk_signed.witness == shared_witness` for the SAME `TEST_WIF`-derived SecretKey. RESEARCH Q1 confirms bdk_wallet 2.3 uses `sign_schnorr_no_aux_rand` (deterministic), so byte-equality holds. ECDSA is RFC 6979 deterministic — always byte-equal. |
| T-19-D | Information Disclosure (PII via error Display) | `Bip322Error::ScriptTypeMismatch` Display output | mitigate (reuse existing) | Per D-112 the existing PII-safe variant is reused unchanged — its Display interpolates only `ScriptType` enum values, never key/spk/address bytes. Existing PII-safety test at `shared/src/bip322/mod.rs:512-565` covers this variant and stays green at the Task 3 boundary (CD-36 variant doc-comment update does not affect Display impl). Verification: `cargo test -p shared --lib bip322::tests::bip322_error_display_is_pii_safe` (or the analogous test name in the existing block) stays green. |
</threat_model>

<verification>
At the Plan 19-01 boundary, ALL of the following must hold simultaneously:

1. `cargo build --workspace` — clean build, no `todo!()` macro reachable in `shared::bip322::p2tr::sign` or `shared::bip322::p2sh_p2wpkh::sign`.
2. `cargo test -p shared` — 31/31 unit tests green (CONTEXT-stated count) + adds 3 new (helper byte-shape + 2 cross-check rejections from Task 3).
3. `cargo test -p shared --test per_script_vectors` — 7/7 green (per-script positive-vector tests now exercise production sign bodies via `sign_simple_test_only` → `sign_simple` → `sign`).
4. `cargo test -p shared --test bip322_cross_shape` — 9/9 green.
5. `cargo test -p client --test wallet_sign_roundtrip` — 9/9 green (7 existing per PATTERNS correction + 2 new parity from Task 4).
6. `cargo test --test integration full_round` — 8/8 green (v1.3 invariant — shared/-internal change must not regress).
7. `cargo test --test integration mixed_script_e2e` — 1/1 green (v1.4 invariant).
8. `cargo test --test integration multi_script_validate` — all green at pre-Plan-19-01 count (Plan 19-02 migrates the callsites; this plan boundary leaves them on `sign_simple_test_only`).
9. `cargo clippy --workspace --all-targets -- -D warnings` — clean.
10. CI grep gates `bip322-pin-check` + `crit-01-grep-check` + `crit-01-client-grep-check` — all green (no Cargo.toml or coordinator/client guard-file edits touched).
11. Grep: `grep -c 'sign_simple_test_only' shared/src/bip322/mod.rs` returns at least 1 (still exists at this plan boundary — Plan 19-02 deletes it).
12. Grep: `grep -c 'fn sign_for_tests' shared/src/bip322/{p2tr,p2sh_p2wpkh,p2wpkh}.rs` returns 3 (all still exist at this plan boundary).
</verification>

<success_criteria>
Plan 19-01 is complete when:

- [ ] `shared/src/bip322/p2tr.rs::sign` ships production Schnorr keypath body (no `todo!()`); D-111 spk-vs-key cross-check at top; returns `Result<Witness, Bip322Error>`.
- [ ] `shared/src/bip322/p2sh_p2wpkh.rs::sign` ships production BIP-143 body (no `todo!()`); D-111 cross-check at top; D-117 `spk` used directly (param `_spk` → `spk`); returns `Result<Witness, Bip322Error>`.
- [ ] `shared/src/bip322/mod.rs::p2sh_p2wpkh_final_script_sig` exists as `pub fn` sibling to `sign_simple`; infallible; returns 23-byte `ScriptBuf`.
- [ ] 3 new unit tests in `shared/src/bip322/mod.rs::tests`: helper byte-shape (asserts `len() == 23` per RESEARCH Q3 correction) + 2 cross-check rejections (assert `ScriptTypeMismatch { declared, derived }` variant with correct field values).
- [ ] 2 new `#[tokio::test] async fn`s in `client/tests/wallet_sign_roundtrip.rs`: P2TR + P2SH-P2WPKH parity, both asserting `bdk_signed.witness == shared_witness` byte-equal.
- [ ] `Bip322Error::ScriptTypeMismatch` variant doc-comment updated per CD-36 to note dual meaning (sign-side spk-vs-key derivation mismatch + verify-side declared-vs-derived script-type mismatch).
- [ ] `sign_simple` dispatcher doc-comment updated per CD-38 to remove `todo!()` reference.
- [ ] All 12 verification items above hold simultaneously.
- [ ] `sign_simple_test_only` + per-script `sign_for_tests` STILL EXIST (Plan 19-02 deletes them).
</success_criteria>

<output>
Create `.planning/phases/19-multi-script-signing-finish/19-01-SUMMARY.md` when done. SUMMARY must record:
- Files modified (4): `shared/src/bip322/p2tr.rs`, `shared/src/bip322/p2sh_p2wpkh.rs`, `shared/src/bip322/mod.rs`, `client/tests/wallet_sign_roundtrip.rs`.
- Decisions implemented: D-107, D-108, D-109, D-110 (with RESEARCH Q3 byte-count correction noted), D-111, D-112, D-113, D-114, D-115 (RESEARCH Q1 confirmed bdk uses `sign_schnorr_no_aux_rand` — no downgrade), D-116, D-117, D-118, D-119, CD-34, CD-35, CD-36, CD-37, CD-38.
- Threat-model items mitigated: T-19-A (D-111 cross-check), T-19-C (parity tests), T-19-D (reused PII-safe variant).
- Boundary test counts: `cargo test -p shared` lib + `per_script_vectors` 7/7 + `bip322_cross_shape` 9/9 + `wallet_sign_roundtrip` 9/9 + `full_round` 8/8 + `mixed_script_e2e` 1/1.
- Carry-forward note for Plan 19-02: `sign_simple_test_only` + per-script `sign_for_tests` STILL EXIST; positive-vector tests at `per_script_vectors.rs:274,311` and `multi_script_validate.rs:114-120` STILL CALL the test-only mirror at this boundary.
</output>
