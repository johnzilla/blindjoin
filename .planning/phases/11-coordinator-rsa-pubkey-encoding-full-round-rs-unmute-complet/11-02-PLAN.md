---
phase: 11-coordinator-rsa-pubkey-encoding-full-round-rs-unmute-complet
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - tests/integration/full_round.rs
autonomous: true
requirements:
  - REPAIR-01
tags:
  - integration-tests
  - full-round
  - unmute
  - bitcoind-pinned
  - phase-10-carve-out
  - per-test-commit-cycle
must_haves:
  truths:
    - "All six #[ignore = TODO(Phase-10): ...] attribute lines in tests/integration/full_round.rs (lines 164, 462, 730, 854, 911, 1236) are removed — one per commit, in canonical-first order"
    - "The 1st unmute is full_round_three_clients (line 164) — mandatory canonical-first per D-05; the remaining 5 follow in file order (462, 730, 854, 911, 1236)"
    - "Each of the 8 full_round tests passes locally against pinned brew bitcoind v31 via the CONTRIBUTING.md canonical invocation (BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round)"
    - "Each of the 6 unmute commits has its PASS verdict line captured in the commit body (CD-1 minimal stamp), plus a SHA reference to the Plan 11-01 RSA fix commit"
    - "Zero test-body edits, zero drive-by edits, zero new dependencies, zero CI changes — only the 6 attribute lines are deleted (D-07)"
    - "If the 1st unmute (full_round_three_clients) fails, the executor halts and emits a CHECKPOINT REACHED marker per D-08; tests 2-6 are NOT attempted in that case"
    - "If any 4th orthogonal blocker surfaces during the cycle, the executor halts and surfaces per D-08; pre-authorized in-flight scope expansion is ZERO"
    - "Decision coverage: D-05 (canonical-first unmute order — full_round_three_clients FIRST), D-06 (no batch removal — six bisectable commits), D-07 (per-test atomic commit cycle, no drive-by edits), D-08 (escape-valve halt-and-surface protocol), D-09 (Phase 12 absorbs any new blocker or non-unmute scope), D-10 (Phase 11 closes REPAIR-01 on green local suite — flip REQUIREMENTS.md to [x]), D-11 (REPAIR-02 closes on Phase 11 PR observation in CI — NOT self-attested in-phase), D-12 (v1.3 ship notes out of scope — wrap-up phase or /gsd-ship handles them), CD-1 (PASS-proof stamp in commit body)"
  artifacts:
    - path: "tests/integration/full_round.rs"
      provides: "Six tests unmuted (no #[ignore] attributes remain on the canonical 6 sites); all 8 full_round tests run end-to-end"
      contains: "async fn full_round_three_clients"
  key_links:
    - from: "tests/integration/full_round.rs (6 unmuted tests)"
      to: "client/src/round/input.rs:40 (RSA decode via from_spki, Plan 11-01)"
      via: "The input registration phase of each full round exercises the SPKI decode path"
      pattern: "BjPublicKey::from_spki"
    - from: "tests/integration/full_round.rs (6 unmuted tests)"
      to: "tests/integration/mod.rs (require_bitcoind!, BitcoindGuard, fund_regtest, FundedSetup — Phase 9/10 fixtures)"
      via: "Each unmuted test uses the post-Fix-A shared fixtures unchanged"
      pattern: "crate::fund_regtest|require_bitcoind!"
---

<objective>
Complete Phase 10's blocked Task 3 by removing the six `#[ignore = "TODO(Phase-10): ..."]` attribute lines in `tests/integration/full_round.rs` — one atomic commit per unmute, in canonical-first order (D-05), each with a PASS-proof stamp in the commit body (CD-1). With the Plan 11-01 RSA decode fix in place plus Phase 10's Fix A (d99b3a4) and Fix WIF-D (e02ce55) already landed, the three known blockers are cleared and the suite should run end-to-end green against pinned brew bitcoind v31.

Purpose: This plan closes REPAIR-01 locally (D-10). The 1st unmute (`full_round_three_clients` at line 164) is the canonical happy-path gate — if it passes, the remaining 5 in file order are mechanical; if it fails, the next blocker surfaces against the simplest test (best diagnostic signal) and the executor halts per D-08. REPAIR-02 is NOT covered in-phase per D-11 — it closes on PR observation when the `corepc-node-feature-pin-check` CI job (already landed at commit 4026f50) is observed green on the Phase 11 PR.

Output: Six atomic commits, each one `test(11): unmute <test_name> (Phase-10 carve-out N/6)`, each with a body containing the single-line cargo invocation, the verdict line, and the Plan 11-01 RSA fix commit SHA. After the 6th commit lands, `cargo test --test integration full_round` reports 8 passed / 0 failed locally.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-coordinator-rsa-pubkey-encoding-full-round-rs-unmute-complet/11-CONTEXT.md
@.planning/phases/11-coordinator-rsa-pubkey-encoding-full-round-rs-unmute-complet/11-PATTERNS.md
@.planning/phases/11-coordinator-rsa-pubkey-encoding-full-round-rs-unmute-complet/11-01-PLAN.md
@CONTRIBUTING.md
@tests/integration/full_round.rs
@tests/integration/mod.rs

<interfaces>
<!-- The exact six attribute pairs the executor edits. The line BELOW each #[ignore] (the async fn declaration) is preserved unchanged. -->

The six unmute sites, in canonical-first order per D-05:

  Order  Line   #[ignore] text                                                                                   Async fn (preserved)
  ----- -----  -----------------------------------------------------------------------------------------------  ----------------------------------------------------
  1st   164    #[ignore = "TODO(Phase-10): RPC schema drift on listunspent/getrawtransaction -- see TODO.md"]   async fn full_round_three_clients()  (MANDATORY 1st)
  2nd   462    same                                                                                              async fn blame_non_signer_timeout()
  3rd   730    same                                                                                              async fn adversarial_replay_token()
  4th   854    same                                                                                              async fn adversarial_invalid_utxo()
  5th   911    same                                                                                              async fn adversarial_wrong_denomination()
  6th   1236   same                                                                                              async fn round_restart_and_completion_after_blame()

The line ABOVE each #[ignore] is `#[tokio::test]` — preserved unchanged. Each unmute = ONE line deletion, nothing else.

<!-- Canonical test invocation, copy-pasted from CONTRIBUTING.md "Running ignored (Phase-10) tests locally" (do NOT invent a new one) -->

Whole-file (after all 6 unmutes):
  BLINDJOIN_REQUIRE_BITCOIND=1 \
    BITCOIND_EXE=$(brew --prefix)/bin/bitcoind \
    cargo test --test integration full_round 2>&1 \
    | tee target/integration-test.log

Per-test (used for each commit's PASS-proof body):
  BLINDJOIN_REQUIRE_BITCOIND=1 \
    BITCOIND_EXE=$(brew --prefix)/bin/bitcoind \
    cargo test --test integration full_round::<test_name> -- --ignored 2>&1

Note: the `-- --ignored` flag is required to run a test that STILL has #[ignore] on it; the per-test PASS proof during the cycle is captured BEFORE the line is deleted in the same commit. Once the attribute is removed, the test runs by default. The executor's order per commit: (1) run the test with `--ignored` and capture the PASS verdict, (2) delete the #[ignore] line, (3) commit with the captured verdict in the body. After this plan completes the 6th commit, the final whole-file invocation above is run WITHOUT `--ignored` (because no #[ignore] attributes remain) and is expected to show 8 passed / 0 failed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Unmute full_round_three_clients (line 164) — canonical-first MANDATORY gate</name>
  <files>tests/integration/full_round.rs</files>
  <read_first>
    - .planning/phases/11-coordinator-rsa-pubkey-encoding-full-round-rs-unmute-complet/11-CONTEXT.md (D-05 canonical-first rule, D-07 per-test cycle discipline, D-08 escape-valve gate, CD-1 minimal-stamp commit body)
    - .planning/phases/11-coordinator-rsa-pubkey-encoding-full-round-rs-unmute-complet/11-PATTERNS.md §"Surface 3" (the exact attribute pair shape at line 164 and the commit-message convention)
    - tests/integration/full_round.rs lines 160-200 (current state of `full_round_three_clients` and the #[ignore] attribute at line 164 — confirm the test body is unchanged from Phase 10's last edit)
    - tests/integration/mod.rs (the shared fixtures the test depends on: require_bitcoind!, BitcoindGuard, fund_regtest, FundedSetup — read but do NOT modify)
    - CONTRIBUTING.md §"Running ignored (Phase-10) tests locally" lines 39-50 (the canonical local invocation pattern — the executor uses this verbatim for the PASS-proof capture)
  </read_first>
  <behavior>
    - Test 1 (capture-then-unmute, in order): run `BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round::full_round_three_clients -- --ignored 2>&1 | tee target/integration-test-11-02-1.log` and capture the PASS verdict line (`test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 7 filtered out`). If the test FAILS (not PASSES), HALT immediately — do NOT delete the #[ignore] line, do NOT commit, do NOT proceed to Task 2. Emit the CHECKPOINT REACHED marker per D-08 with the failure mode and a proposed minimal repair. Pre-authorized in-flight scope expansion is ZERO.
    - Test 2 (post-edit verification): after deleting line 164, re-running `cargo test --test integration full_round::full_round_three_clients` WITHOUT `--ignored` still PASSES (the test now runs by default).
    - Test 3 (whole-file sanity, after this commit only): `cargo test --test integration full_round` reports 3 passed (2 originally passing + 1 just unmuted), 5 ignored (the other 5 carve-outs still carry their #[ignore]).
  </behavior>
  <action>
    Execute the per-test cycle for the 1st unmute in this exact order:

    (a) Confirm Plan 11-01's two commits are in `git log` and capture the `fix(11):` SHA into a shell variable: `RSA_FIX_SHA=$(git log --grep="^fix(11): switch client RSA pubkey decode" --format=%H -1)`. If empty, HALT — Plan 11-01 was not run. This `git log --grep` query is the AUTHORITATIVE source for the SHA; do NOT rely on `11-01-SUMMARY.md` (it may not exist yet at execute time, and the SHA in commit history is the canonical truth).

    (b) Run the per-test invocation: `BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round::full_round_three_clients -- --ignored 2>&1 | tee target/integration-test-11-02-1.log`. Capture the verdict line (the line matching `test result: ok. 1 passed`) into `VERDICT_1`.

    (c) If the verdict line is NOT a PASS (`test result: FAILED` or test panic), HALT IMMEDIATELY. Do NOT delete the #[ignore] line. Do NOT commit. Emit a CHECKPOINT REACHED marker with: the failure mode (the panic message, the failing assertion, or the cargo verdict), a proposed minimal repair, and the explicit note "D-08 escape-valve invoked: canonical-first unmute failed — Phase 11 halts, Phase 12 (if needed) absorbs the new blocker (D-09)." Tasks 2-6 are NOT attempted.

    (d) On PASS: delete exactly line 164 from `tests/integration/full_round.rs`. The deleted line is `#[ignore = "TODO(Phase-10): RPC schema drift on listunspent/getrawtransaction -- see TODO.md"]`. The line above (line 163, `#[tokio::test]`) and the line below (line 165, `async fn full_round_three_clients() {`) stay byte-identical. NO whitespace edits, NO comment substitution, NO test-body changes (D-07: "no drive-by edits to other tests, helpers, or unrelated code"). Verify with `git diff tests/integration/full_round.rs` that exactly ONE line is deleted and nothing else changes.

    (e) Stage and commit atomically. Commit subject: `test(11): unmute full_round_three_clients (Phase-10 carve-out 1/6)`. Commit body (CD-1 minimal stamp):

        cargo test --test integration full_round::full_round_three_clients -- --ignored
        <paste VERDICT_1 verbatim — e.g. "test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 7 filtered out">

        RSA fix: <RSA_FIX_SHA>

    Use a HEREDOC for the commit message to preserve newlines.

    (f) Verify post-commit: `cargo test --test integration full_round 2>&1 | tee target/integration-test-11-02-1-postcommit.log` (without `--ignored`, since one test is now unmuted). Expected: 3 passed, 5 ignored. If the count is wrong, HALT and report.

    NO other files are modified. NO entries added to TODO.md. NO doc updates. NO test-body edits. The Plan-11-01 fix commit must already be in history before this task runs (depends_on: [11-01]).
  </action>
  <verify>
    <automated>BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round::full_round_three_clients 2>&1 | grep -E "test result: ok\. 1 passed" && [ "$(grep -c 'TODO(Phase-10)' tests/integration/full_round.rs)" = "5" ] && git log -1 --format=%s | grep -q "^test(11): unmute full_round_three_clients (Phase-10 carve-out 1/6)$"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `sed -n '163p;164p;165p' tests/integration/full_round.rs` outputs exactly three lines whose contents are (in order) `#[tokio::test]`, `async fn full_round_three_clients() {`, and the first line of the test body. The `#[ignore]` line is GONE from this range.
    - Source assertion: `[ "$(grep -c 'TODO(Phase-10)' tests/integration/full_round.rs)" = "5" ]` exits 0 — the count is exactly 5 (down from 6 — one removed, five still present for Tasks 2-6). This is the authoritative deletion proof; it does NOT depend on the post-delete line number, which shifts after the line is removed.
    - Test command assertion: `BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round::full_round_three_clients 2>&1 | grep -E "test result: ok\. 1 passed; 0 failed"` returns a match (the test now runs and passes WITHOUT `--ignored`).
    - Test command assertion: `BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round 2>&1 | grep -E "test result: ok\. 3 passed; 0 failed; 5 ignored"` returns a match (incremental whole-file run after this commit: 2 already-passing + 1 just unmuted = 3 passed; 5 still-ignored = the remaining carve-outs).
    - CLI assertion: `git log -1 --format=%s` outputs exactly `test(11): unmute full_round_three_clients (Phase-10 carve-out 1/6)`.
    - CLI assertion: `git log -1 --format=%b` contains the literal string `cargo test --test integration full_round::full_round_three_clients -- --ignored`, the literal string `test result: ok. 1 passed`, and the literal string `RSA fix: ` followed by the Plan 11-01 fix-commit SHA (40 hex chars).
    - Source assertion: `git diff HEAD~1 -- tests/integration/full_round.rs` shows exactly one deleted line and zero added/modified lines (the `git diff --stat` output is `1 file changed, 0 insertions(+), 1 deletion(-)`).
    - Source assertion: no other file is modified by this commit — `git diff HEAD~1 --stat | wc -l` returns exactly 2 (one summary line + one file line), naming only `tests/integration/full_round.rs`.
    - Halt assertion (negative test): if the test FAILED in step (b), no commit exists, line 164 is still present in the file, and the executor emitted a CHECKPOINT REACHED marker. This is a recovery state, not a failure mode.
  </acceptance_criteria>
  <done>
    Line 164's #[ignore] attribute is removed; `full_round_three_clients` passes locally without `--ignored`; whole-file `cargo test --test integration full_round` shows 3 passed / 5 ignored; one atomic commit with the prescribed subject and CD-1 body has landed; the RSA fix SHA reference is in the body. If the canonical-first test FAILED, the executor has HALTED and surfaced a CHECKPOINT REACHED marker — Task 2 is NOT attempted.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Unmute the remaining 5 carve-out tests in file order (lines 462, 730, 854, 911, 1236)</name>
  <files>tests/integration/full_round.rs</files>
  <read_first>
    - .planning/phases/11-coordinator-rsa-pubkey-encoding-full-round-rs-unmute-complet/11-CONTEXT.md (D-05 file-order rule for unmutes 2-6, D-06 no batch removal, D-07 per-test cycle, D-08 escape-valve, CD-1 stamp)
    - .planning/phases/11-coordinator-rsa-pubkey-encoding-full-round-rs-unmute-complet/11-PATTERNS.md §"Surface 3" (the attribute pair shape — identical at all 5 remaining sites) and §"Phase 10 D-07 commit-message convention" (the 6 prescribed commit subjects)
    - tests/integration/full_round.rs (after Task 1's commit lands, re-read the file to confirm the remaining 5 #[ignore] sites are at their expected line numbers — note that after deleting line 164, all subsequent line numbers SHIFT DOWN BY ONE per prior delete; the executor must re-grep for #[ignore] before each unmute, not trust the pre-Phase-11 line numbers from the spec)
    - tests/integration/mod.rs (the shared fixtures, unchanged — depend on, do NOT modify)
    - CONTRIBUTING.md §"Running ignored (Phase-10) tests locally" (the per-test invocation pattern reused 5 more times)
    - (RSA fix SHA: re-capture via `git log --grep='^fix(11): switch client RSA pubkey decode to from_spki' -1 --format=%H` per Task 1's authoritative pattern — do NOT depend on `11-01-SUMMARY.md`, which may not exist yet at execute time.)
  </read_first>
  <behavior>
    - Test 1: After this task completes, `grep -c "TODO(Phase-10)" tests/integration/full_round.rs` returns 0 (all 6 carve-outs unmuted).
    - Test 2: `BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round 2>&1 | grep -E "test result: ok\. 8 passed; 0 failed; 0 ignored"` returns a match — all 8 full_round tests pass locally.
    - Test 3: Each of the 5 newly-added commits carries the prescribed subject and a CD-1 body containing the per-test cargo invocation, the verdict line, and the Plan 11-01 RSA fix SHA reference.
    - Test 4 (escape-valve): if any of the 5 unmutes FAILS during its capture step, the executor halts WITHOUT deleting the #[ignore] line for that test (and without attempting any later unmute), and emits a CHECKPOINT REACHED marker per D-08. Pre-authorized in-flight scope expansion is ZERO. A 4th orthogonal blocker (i.e., any test failure that is NOT the same RSA-decode signature already cleared by Plan 11-01) triggers the same halt-and-surface protocol.
  </behavior>
  <action>
    Execute the per-test cycle 5 times, in this exact order. The order is mandatory per D-05 (file order after the canonical-first test) and the commits must each be atomic (one #[ignore] removal per commit; D-06 explicitly rejects batch removal).

    For EACH iteration where N is a shell loop variable taking values 2, 3, 4, 5, 6 (in that order) with the mapping:

      N=2 → test fn `blame_non_signer_timeout` (originally line 462, shifted to ~461 after Task 1's deletion)
      N=3 → test fn `adversarial_replay_token` (originally line 730)
      N=4 → test fn `adversarial_invalid_utxo` (originally line 854)
      N=5 → test fn `adversarial_wrong_denomination` (originally line 911)
      N=6 → test fn `round_restart_and_completion_after_blame` (originally line 1236)

    Per-iteration procedure (DO NOT short-circuit — each iteration is its own atomic commit per D-06):

    (a) Re-locate the current #[ignore] line for the test by name: `LINE=$(awk -v t="<test_fn>" '$0 ~ "#\\[ignore = \\\"TODO\\(Phase-10\\)" {ig=NR} $0 ~ "async fn "t"\\(\\)" {print ig; exit}' tests/integration/full_round.rs)`. Verify LINE is a non-empty integer and that `sed -n "${LINE}p" tests/integration/full_round.rs` outputs a line beginning with `#[ignore = "TODO(Phase-10):`.

    (b) Run the per-test PASS-proof invocation: `BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round::<test_fn> -- --ignored 2>&1 | tee target/integration-test-11-02-${N}.log`. Capture the verdict line into `VERDICT_${N}`.

    (c) If the verdict is NOT a PASS, HALT IMMEDIATELY. Do NOT delete the #[ignore] line. Do NOT commit. Do NOT attempt any later iteration in this loop. Emit a CHECKPOINT REACHED marker with: the test fn name, the iteration number (N/6), the failure mode (panic message / assertion / cargo verdict), and the explicit note "D-08 escape-valve invoked: 4th orthogonal blocker (or canonical happy-path follow-on failure) — Phase 11 halts, Phase 12 absorbs (D-09). Pre-authorized in-flight scope expansion is ZERO." Earlier successful commits in this task remain — they are bisect-clean.

    (d) On PASS: delete exactly line LINE from `tests/integration/full_round.rs`. The deleted line is the #[ignore] attribute. The line above (`#[tokio::test]`) and the line below (`async fn <test_fn>() {`) stay byte-identical. NO whitespace edits, NO comment substitution, NO test-body edits. Verify with `git diff` that exactly ONE line is deleted and nothing else changed.

    (e) Stage and commit atomically. Commit subject: `test(11): unmute <test_fn> (Phase-10 carve-out N/6)`. Commit body (CD-1 minimal stamp, identical shape to Task 1's commit):

        cargo test --test integration full_round::<test_fn> -- --ignored
        <paste VERDICT_${N} verbatim>

        RSA fix: <RSA_FIX_SHA from Plan 11-01>

    Use a HEREDOC for the commit message.

    (f) Verify post-commit incrementally. The expected pass-count grows by one after each iteration: after the 2nd unmute (N=2) expect 4 passed / 4 ignored; after the 3rd (N=3) expect 5 passed / 3 ignored; …; after the 6th (N=6) expect 8 passed / 0 ignored. Compute the expected count via explicit shell arithmetic (N is the loop variable, NOT regex syntax) and grep for it:

        EXPECTED=$((2 + N)) && BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round 2>&1 | grep -E "test result: ok\. ${EXPECTED} passed"

    If the count is wrong, HALT.

    After all 5 iterations complete (N=2 through N=6), run the final whole-file invocation from CONTRIBUTING.md verbatim:

      BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round 2>&1 | tee target/integration-test-11-02-final.log

    Expected verdict line: `test result: ok. 8 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out`. If anything else, the executor halts and reports — but this state should already be impossible if each iteration's step (b) PASSed.

    REPAIR-02 closure note: this plan does NOT touch `.github/workflows/ci.yml`. REPAIR-02 closes on PR observation when the CI `corepc-node-feature-pin-check` job (already landed at commit 4026f50, per Phase 10) is observed green on the Phase 11 PR (D-11). The executor of this plan does NOT self-attest REPAIR-02; it lands the code, the PR closes the requirement. Do not modify REQUIREMENTS.md's REPAIR-02 status in this plan; that is a wrap-up phase concern (D-12).
  </action>
  <verify>
    <automated>BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round 2>&1 | grep -E "test result: ok\. 8 passed; 0 failed; 0 ignored" && [ "$(grep -c 'TODO(Phase-10)' tests/integration/full_round.rs)" = "0" ] && [ "$(git log --grep='^test(11): unmute .* (Phase-10 carve-out [2-6]/6)$' --oneline | wc -l | tr -d ' ')" = "5" ]</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "TODO(Phase-10)" tests/integration/full_round.rs` returns exactly 0 (all six #[ignore] attributes are gone).
    - Source assertion: `grep -c "#\[ignore" tests/integration/full_round.rs` returns 0 (no residual #[ignore] of any form remains on the 6 unmuted tests; if other unrelated #[ignore] markers exist in the file, this assertion must be tightened — verify by inspection at execution time).
    - Source assertion: each of the 6 test fns still exists by name — `grep -c "^async fn full_round_three_clients\|^async fn blame_non_signer_timeout\|^async fn adversarial_replay_token\|^async fn adversarial_invalid_utxo\|^async fn adversarial_wrong_denomination\|^async fn round_restart_and_completion_after_blame" tests/integration/full_round.rs` returns 6 (test bodies preserved, only attributes removed — D-07).
    - Test command assertion: `BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round 2>&1 | grep -E "test result: ok\. 8 passed; 0 failed; 0 ignored"` returns a match. This is the REPAIR-01 closure proof (D-10).
    - Test command assertion (per-test): for each unmuted test fn, `BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round::<fn_name> 2>&1 | grep -E "test result: ok\. 1 passed"` returns a match (each individual test passes without `--ignored`).
    - CLI assertion: `git log --grep='^test(11): unmute .* (Phase-10 carve-out [2-6]/6)$' --oneline | wc -l` returns exactly 5 — five commits with the prescribed pattern. Combined with Task 1's commit, total = 6 unmute commits (matching D-06's "no batch removal" — six bisectable commits).
    - CLI assertion: the six commit subjects in order (oldest first) are exactly:
        `test(11): unmute full_round_three_clients (Phase-10 carve-out 1/6)`  (from Task 1)
        `test(11): unmute blame_non_signer_timeout (Phase-10 carve-out 2/6)`
        `test(11): unmute adversarial_replay_token (Phase-10 carve-out 3/6)`
        `test(11): unmute adversarial_invalid_utxo (Phase-10 carve-out 4/6)`
        `test(11): unmute adversarial_wrong_denomination (Phase-10 carve-out 5/6)`
        `test(11): unmute round_restart_and_completion_after_blame (Phase-10 carve-out 6/6)`
       Verify with `git log --grep='^test(11): unmute' --reverse --format=%s` matching this list exactly.
    - CLI assertion (per commit): for each of the 5 commits in this task, `git log -1 --format=%b <commit>` contains the literal strings `cargo test --test integration full_round::<corresponding_fn>` and `test result: ok. 1 passed` and `RSA fix: <40-char SHA matching Plan 11-01's fix commit>`.
    - Source assertion (per commit): each of the 5 commits in this task changes exactly one file (`tests/integration/full_round.rs`) with exactly one deleted line and zero added/modified lines (`git diff <commit>~1 <commit> --stat` shows `1 file changed, 0 insertions(+), 1 deletion(-)`).
    - Boundary preservation assertion: NO commits in this plan modify `tests/integration/mod.rs`, `coordinator/src/api/handlers.rs`, `coordinator/src/round/manager.rs`, `coordinator/src/blind/rsa.rs` lines 1-68, `shared/src/protocol.rs`, `.github/workflows/ci.yml`, `CONTRIBUTING.md`, or any Cargo.toml. Verify with `git diff HEAD~6..HEAD --stat` showing ONLY `tests/integration/full_round.rs` (the six Plan 11-02 commits cover this range). Plan 11-01's two files (`client/src/round/input.rs` and `coordinator/src/blind/rsa.rs`) are in the prior commit range `HEAD~8..HEAD~6` and are OUT OF SCOPE for this assertion — they should NOT appear in `HEAD~6..HEAD --stat`.
    - Halt assertion (negative test, D-08): if any iteration's step (b) test FAILED, no commit was made for that iteration, the corresponding #[ignore] line is still present, all later iterations in this task were skipped, and the executor has emitted a CHECKPOINT REACHED marker citing D-08 and naming the failing test + the proposed minimal repair. Earlier successful commits in this task remain bisect-clean.
    - REPAIR-02 non-attestation assertion: REQUIREMENTS.md's REPAIR-02 status is UNCHANGED by this plan (still `[ ]`). REPAIR-02 closes on PR observation per D-11, not via this plan's commits. Verify with `git diff HEAD~6..HEAD -- .planning/REQUIREMENTS.md` returning empty.
  </acceptance_criteria>
  <done>
    All 6 #[ignore] attributes are gone from `tests/integration/full_round.rs`; `cargo test --test integration full_round` reports 8 passed / 0 failed / 0 ignored locally; 5 atomic commits from this task plus Task 1's commit total 6 bisectable unmute commits with the prescribed subjects and CD-1 bodies; no boundary file modified; REQUIREMENTS.md REPAIR-02 status unchanged (closes on PR observation per D-11). If the executor halted via D-08 on any iteration, earlier commits in the sequence remain, a CHECKPOINT REACHED marker is recorded, and the next session resumes via Phase 12 (D-09).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner ↔ pinned bitcoind v31 binary | Local cargo test process spawns a real bitcoind via `corepc-node`. The trust boundary is the version-pin in `.bitcoind-version` (30.2 per CONTRIBUTING.md, brew-installed v31 for local-dev — Phase 9 documented this asymmetry; Phase 11 inherits it unchanged). |
| Plan 11-01 RSA decode swap ↔ Plan 11-02 unmute proofs | Each unmute commit's body references the Plan 11-01 fix-commit SHA. The integrity of this reference rests on `depends_on: [11-01]` — the Plan 11-01 commits must be in history before any Plan 11-02 commit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Tampering | Test-body edit smuggled into an unmute commit | mitigate | Per-task acceptance criteria assert `git diff <commit>~1 <commit> --stat` shows `0 insertions(+), 1 deletion(-)` for each unmute commit. A test-body edit would surface as `+N insertions(+)` and fail acceptance. D-07 enforces "no drive-by edits to other tests, helpers, or unrelated code" at the policy level. |
| T-11-08 | Repudiation | Commit body lacks PASS-proof; reviewer cannot bisect | mitigate | CD-1 body convention is enforced via acceptance criteria — each commit body must contain the per-test cargo invocation, the verdict line, AND the Plan 11-01 RSA fix commit SHA reference. Bisect cleanliness is the explicit rationale per D-06. |
| T-11-09 | Tampering | Wire-format / commitment domain drift slipping in alongside unmutes | mitigate (PRESERVED) | Plan 11-02 modifies ONLY `tests/integration/full_round.rs`. Acceptance criteria explicitly check no other file is touched. The wire format frozen by D-02 (Phase 1) and the commitment domain `SHA-256(SPKI_bytes)` are entirely outside this plan's modification surface — guaranteed by the file-scope restriction. |
| T-11-10 | Denial of Service | A future hidden blocker (a "4th orthogonal blocker" per D-08) is silently absorbed by the executor instead of being surfaced for user decision | mitigate (POLICY) | D-08 escape-valve is encoded explicitly in Tasks 1 and 2: any per-test failure during step (b) triggers HALT + CHECKPOINT REACHED marker, with explicit "pre-authorized in-flight scope expansion is ZERO." Phase 10 already absorbed three Fix-then-resume blockers; D-08 codifies that a 4th deserves user input regardless of perceived smallness. Acceptance criteria include a halt-recovery state as a valid (non-failure) terminal condition. |
| T-11-11 | Information Disclosure | PASS-proof log files (`target/integration-test-11-02-*.log`) checked into the repo | accept | The `tee target/...` pattern routes logs to the cargo `target/` directory, which is gitignored per Phase 9-05's CONTRIBUTING.md doc. No log content is checked in; only the verdict LINE (not the whole log) appears in the commit body. |
| T-11-12 | Spoofing | A test that passes against bitcoind v31 but would fail against the pinned v30.2 in CI | accept (deferred to D-11) | Local PASS-proof is captured against brew bitcoind v31 per CONTEXT.md "Specific Ideas" — the same invocation that produced the Phase 10 blocker. CI runs against pinned v30.2 (per `.bitcoind-version`). REPAIR-02 explicitly closes on PR observation in CI, not on local attestation (D-11). If CI surfaces a v30/v31 divergence, that becomes Phase 12 scope (D-09). |
| T-11-SC | Tampering | npm/pip/cargo installs (none in this plan) | accept | Plan 11-02 introduces ZERO new dependencies. No package-legitimacy gate required. `Cargo.toml` files are not edited; no `cargo add`, no `cargo install`. The plan exercises only existing pinned dependencies (corepc-node feature-pinned per Phase 10 Task 1, bitcoind v30.2 per `.bitcoind-version`, all crates as locked in `Cargo.lock`). |
| T-11-13 | Tampering | Line-number drift between the documented sites (164, 462, 730, 854, 911, 1236) and the actual file after each delete | mitigate | Task 2's action explicitly re-greps for the #[ignore] line by adjacent test-fn name at each iteration (step (a)), not by trusting the pre-Phase-11 line numbers. Acceptance criteria assert each #[ignore] line is found via name-based lookup before deletion. |
</threat_model>

<verification>
After both tasks complete (and assuming neither HALT was triggered):

1. `BLINDJOIN_REQUIRE_BITCOIND=1 BITCOIND_EXE=$(brew --prefix)/bin/bitcoind cargo test --test integration full_round 2>&1 | tee target/integration-test-11-02-final.log` reports `test result: ok. 8 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out`. This is the REPAIR-01 local-green closure proof (D-10).
2. `grep -c "TODO(Phase-10)" tests/integration/full_round.rs` returns 0.
3. `grep -c "#\[ignore" tests/integration/full_round.rs` returns 0 on the six unmuted tests' sites (verify by inspection if any unrelated #[ignore] exists elsewhere in the file).
4. `git log --grep='^test(11): unmute' --oneline | wc -l` returns 6.
5. `git log --grep='^test(11): unmute' --reverse --format=%s` outputs exactly the six prescribed commit subjects in canonical-first then file order (1/6 through 6/6).
6. Each of the 6 commit bodies contains its per-test cargo invocation, its `test result: ok. 1 passed` verdict line, and the same Plan 11-01 RSA fix SHA.
7. `git diff HEAD~8 -- tests/integration/mod.rs coordinator/src/api/handlers.rs coordinator/src/round/manager.rs shared/src/protocol.rs .github/workflows/ci.yml CONTRIBUTING.md .planning/REQUIREMENTS.md` returns empty — none of these preservation-boundary files is modified by Phase 11.
8. `git diff HEAD~8 -- 'Cargo.toml' '**/Cargo.toml'` returns empty — no dependency changes.
9. REQUIREMENTS.md's REPAIR-02 status is unchanged (still `[ ]`); it closes on PR observation per D-11, not via this plan.

If a HALT was triggered during Task 1 or Task 2:
- A CHECKPOINT REACHED marker exists in the conversation log naming the failing test, the failure mode, and the proposed minimal repair.
- All commits made before the HALT remain (bisect-clean).
- REPAIR-01 is NOT yet closed; Phase 12 (D-09) absorbs the new blocker and the remaining unmutes.
</verification>

<success_criteria>
- [ ] All 6 #[ignore = "TODO(Phase-10): ..."] attribute lines removed from `tests/integration/full_round.rs` (one per commit, six atomic commits).
- [ ] Commit order is canonical-first then file order (D-05): full_round_three_clients (1/6) → blame_non_signer_timeout (2/6) → adversarial_replay_token (3/6) → adversarial_invalid_utxo (4/6) → adversarial_wrong_denomination (5/6) → round_restart_and_completion_after_blame (6/6).
- [ ] `cargo test --test integration full_round` reports 8 passed / 0 failed / 0 ignored locally against pinned brew bitcoind v31 via the CONTRIBUTING.md canonical invocation.
- [ ] Each of the 6 commits has a CD-1-shaped body: per-test cargo invocation + verdict line + Plan 11-01 RSA fix SHA reference.
- [ ] No test body, helper, or non-target file is modified — only the 6 attribute lines are deleted (D-07).
- [ ] D-08 escape-valve gate is honored: any per-test failure HALTs and surfaces; pre-authorized in-flight scope expansion is ZERO; earlier successful commits remain bisect-clean.
- [ ] REPAIR-01 is locally closed (D-10) — mark `[x]` in REQUIREMENTS.md as part of the post-execution wrap-up (NOT in-task; the wrap-up is a separate step or is handled by /gsd-execute-phase's summary writer per project convention).
- [ ] REPAIR-02 is NOT touched in this plan; it closes on PR observation per D-11 (the CI corepc-node-feature-pin-check job, already landed at 4026f50, must be observed green on the Phase 11 PR).
- [ ] No CI changes, no new dependencies, no `Cargo.toml` edits, no v1.3 ship-notes work (D-12 defers ship notes to a wrap-up phase or `/gsd-ship`).
</success_criteria>

<output>
Create `.planning/phases/11-coordinator-rsa-pubkey-encoding-full-round-rs-unmute-complet/11-02-SUMMARY.md` when done. The summary should record:
- The 6 commit SHAs (in canonical-first then file order).
- The final whole-file PASS verdict (`test result: ok. 8 passed; 0 failed; 0 ignored`).
- Confirmation that REPAIR-01 is satisfied locally (D-10) — and that this is the closure trigger to mark `[x]` in REQUIREMENTS.md.
- Explicit note that REPAIR-02 closure is DEFERRED to PR observation per D-11 — the wrap-up phase or `/gsd-ship` flips it.
- If any HALT was triggered: the CHECKPOINT REACHED context (which test, which failure mode, which iteration N/6), and a pointer to where Phase 12 should pick up (D-09).
- No ship-notes work performed (D-12 — handled by a wrap-up phase or /gsd-ship after CI confirms green).
</output>
