---
phase: 23-cosign-image-attestations-slsa-provenance-sbom
plan: 02
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - .github/workflows/docker.yml
autonomous: true
requirements:
  - ATTEST-02
  - ATTEST-03
requirements_addressed:
  - ATTEST-02
  - ATTEST-03

must_haves:
  truths:
    - "A new `anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610 # v0.24.0` step generates `sbom.spdx.json` by scanning the just-pushed image (RESEARCH §2.2 + §3.3 — corrects CONTEXT D-03 which incorrectly asserted `actions/attest-sbom` bundles Syft; the action does NOT generate SBOMs)."
    - "A new `actions/attest-sbom@bd218ad0dbcb3e146bd073d1d9c6d78e08aa8a0b # v2.4.0` step signs the `sbom.spdx.json` file as a SLSA in-toto SBOM attestation and pushes it to the OCI registry alongside the image (ATTEST-03 attest half)."
    - "A new `actions/attest-build-provenance@96278af6caaf10aea03fd8d33a09a777ca52d62f # v3.2.0` step emits a SLSA v1.0 in-toto provenance attestation naming `docker.yml` as the builder workflow + the tag ref + the source commit (ATTEST-02)."
    - "Each matrix leg (coordinator / client / liquidity-bot) produces its own SBOM scan + both attestations independently; `fail-fast: false` (already set) means a per-leg failure does not block the others."
    - "All three new actions are v3.X/v2.X (NOT v4.X wrapper versions) per RESEARCH §2.3 + §7.4 — v4 of attest-build-provenance and attest-sbom are deprecation wrappers around `actions/attest`; v3.2.0 / v2.4.0 are the last releases that do what CONTEXT D-03 names. v1.7 carry-forward: migrate to `actions/attest@v4` as a single consolidated step."
    - "The build-push-action `with:` block stays free of `provenance:` / `sbom:` inputs (Pitfall 5 + RESEARCH §3.1 — BuildKit-native attestations would compete with the GitHub-native path locked by D-03 and produce two attestations per image)."
    - "Per-step subject identification uses `subject-name: ghcr.io/<owner>/blindjoin-<image>` + `subject-digest: ${{ steps.build.outputs.digest }}` consumed from Plan 23-01's `id: build` (D-01 in-context plumbing across matrix legs)."
  artifacts:
    - path: ".github/workflows/docker.yml"
      provides: "Syft SBOM generation step (ATTEST-03 generator half)"
      contains: "anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610"
    - path: ".github/workflows/docker.yml"
      provides: "SBOM attestation step (ATTEST-03 attest half)"
      contains: "actions/attest-sbom@bd218ad0dbcb3e146bd073d1d9c6d78e08aa8a0b"
    - path: ".github/workflows/docker.yml"
      provides: "SLSA v1.0 build provenance attestation (ATTEST-02)"
      contains: "actions/attest-build-provenance@96278af6caaf10aea03fd8d33a09a777ca52d62f"
    - path: ".github/workflows/docker.yml"
      provides: "SPDX-JSON SBOM format declared (REQUIREMENTS ATTEST-03 verbatim)"
      contains: "format: spdx-json"
    - path: ".github/workflows/docker.yml"
      provides: "Push attestations to registry alongside image (D-01 inline registry-attached path)"
      contains: "push-to-registry: true"
  key_links:
    - from: "docker.yml anchore/sbom-action step"
      to: "docker.yml actions/attest-sbom step"
      via: "sbom.spdx.json file written by generator, consumed by attest"
      pattern: "output-file: sbom\\.spdx\\.json[\\s\\S]*sbom-path: 'sbom\\.spdx\\.json'"
    - from: "docker.yml actions/attest-sbom step"
      to: "ghcr.io/.../blindjoin-<image>@<digest> referrer manifest"
      via: "push-to-registry: true + subject-name/subject-digest"
      pattern: "subject-digest: \\$\\{\\{ steps\\.build\\.outputs\\.digest \\}\\}"
    - from: "docker.yml actions/attest-build-provenance step"
      to: "GitHub attestations API + OCI registry referrer"
      via: "OIDC + push-to-registry: true"
      pattern: "actions/attest-build-provenance@96278af"
---

<objective>
Add three steps to `.github/workflows/docker.yml`'s `docker` job, immediately AFTER the `cosign sign` step from Plan 23-01: (1) `anchore/sbom-action` to generate `sbom.spdx.json` via Syft, (2) `actions/attest-sbom` to sign the SBOM file as a SLSA in-toto attestation (ATTEST-03), and (3) `actions/attest-build-provenance` to emit a SLSA v1.0 provenance attestation (ATTEST-02). All three actions are SHA-pinned to v3.X/v2.X (NOT v4.X wrappers) per RESEARCH §2.3.

Purpose: ATTEST-03 specifically says "SBOM attestation (SPDX format, generated by Syft)" — and the load-bearing RESEARCH §2.2 correction is that `actions/attest-sbom` does NOT generate the SBOM (CONTEXT D-03 misread the README). The plan therefore needs TWO steps for ATTEST-03 (Syft generator + attest signer) plus ONE step for ATTEST-02 (build provenance). All three push attestations to the same OCI registry as the image, consumed in-context with no job-output plumbing. This is the second half of the docker.yml sign+attest pipeline; Plan 23-01 delivered the foundation + cosign sign; this plan completes the attestation surface.
Output: One modified file: `.github/workflows/docker.yml` (adds ~45 new lines: 3 new steps + comment headers).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-CONTEXT.md
@.planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-RESEARCH.md
@.planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-PATTERNS.md
@.planning/research/PITFALLS.md
@.github/workflows/docker.yml
@.planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-01-PLAN.md

<interfaces>
<!-- Inputs from Plan 23-01: -->
<!--   - id-token: write + attestations: write permissions on the docker job -->
<!--   - id: build on docker/build-push-action -->
<!--   - cosign 2.6.3 binary on $PATH (installed by sigstore/cosign-installer step) -->
<!--   - The `cosign sign` step produced the <image>:sha256-<HEX>.sig (ATTEST-01 deliverable) -->

<!-- This plan's outputs (registry-attached): -->
<!--   - sbom.spdx.json on the runner FS (transient — consumed only by attest-sbom step) -->
<!--   - SLSA in-toto SBOM attestation pushed to ghcr.io/.../blindjoin-<image>@<digest> referrer (ATTEST-03) -->
<!--   - SLSA v1.0 build provenance attestation pushed to ghcr.io/.../blindjoin-<image>@<digest> referrer (ATTEST-02) -->

<!-- Pin contract (D-04 — enforced by sigstore-pin-check job in Plan 23-03): -->
<!--   anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610 # v0.24.0 -->
<!--   actions/attest-sbom@bd218ad0dbcb3e146bd073d1d9c6d78e08aa8a0b # v2.4.0 -->
<!--   actions/attest-build-provenance@96278af6caaf10aea03fd8d33a09a777ca52d62f # v3.2.0 -->

<!-- Subject identification contract (all 3 steps): -->
<!--   subject-name: ghcr.io/${{ github.repository_owner }}/blindjoin-${{ matrix.image }} -->
<!--   subject-digest: ${{ steps.build.outputs.digest }}  <-- consumed from Plan 23-01 -->
<!--   push-to-registry: true (D-01 — registry-attached path; operator retrieval via gh attestation verify oci://...) -->

<!-- Verification path documented in Plan 23-04 (SECURITY.md): -->
<!--   ATTEST-02 + ATTEST-03 retrievable via `gh attestation verify oci://...` (primary) -->
<!--   - NOT via `cosign download attestation` (returns 404 against GitHub attestation artifacts — RESEARCH §2.4 + issue #162) -->

<!-- Step ordering (continuation of Plan 23-01's pattern; locked by D-01 + RESEARCH §3): -->
<!--   ... existing steps including Plan 23-01's cosign sign step ... -->
<!--   [+ NEW Plan 23-02 step 1] anchore/sbom-action — writes sbom.spdx.json -->
<!--   [+ NEW Plan 23-02 step 2] actions/attest-sbom — reads sbom.spdx.json, attests + pushes -->
<!--   [+ NEW Plan 23-02 step 3] actions/attest-build-provenance — emits SLSA + pushes -->
<!--   END OF DOCKER JOB -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `anchore/sbom-action` (Syft generator) + `actions/attest-sbom` (signer) for ATTEST-03</name>
  <files>.github/workflows/docker.yml</files>
  <read_first>
    - .github/workflows/docker.yml (verify Plan 23-01's `cosign sign` step is present; identify the line number where new steps will be appended)
    - .planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-RESEARCH.md §2.2 lines 129-153 (the load-bearing correction — `actions/attest-sbom` does NOT generate the SBOM; canonical anchore/sbom-action YAML shape with image: + format: + output-file: + upload-artifact: false)
    - .planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-RESEARCH.md §3.3 lines 268-314 (locked YAML for both steps: the anchore/sbom-action with upload-artifact: false / upload-release-assets: false rationale; the actions/attest-sbom with subject-name/subject-digest/sbom-path/push-to-registry: true)
    - .planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-RESEARCH.md §2.3 lines 158-181 (SHA pin resolution: anchore/sbom-action @ v0.24.0 = e22c3899...; actions/attest-sbom @ v2.4.0 = bd218ad0...)
    - .planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-RESEARCH.md §7.1 lines 652-658 (NEW Pitfall: attest-sbom is not a one-action solution; always pair with anchore/sbom-action)
    - .planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-RESEARCH.md §8 lines 718-722 (RESEARCH Open Question #4 — `upload-artifact: false` explicit confirmation: default is TRUE; explicit-false avoids "which SBOM is canonical?" question)
    - .planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-PATTERNS.md §4 lines 163-217 (analog: read-base-digests step shape — name + uses + with; multi-line comment block above)
    - .planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-PATTERNS.md §5 lines 221-251 (analog for attest-sbom; same shape; sbom-path consumes the previous step's output file)
    - .planning/research/PITFALLS.md (no new pitfall specifically about anchore/sbom-action, but Pitfall 4 SHA-pin discipline applies to all four new actions)
  </read_first>
  <action>
    Append TWO new steps to the `docker` job in `.github/workflows/docker.yml`, immediately AFTER Plan 23-01's `cosign sign` step. The two steps form the two halves of ATTEST-03 (generator + attest); they must be in this order because step 2's `sbom-path: 'sbom.spdx.json'` reads the file step 1 wrote with `output-file: sbom.spdx.json`.

    **Step 1 — Generate SPDX SBOM with Syft (ATTEST-03 generator half).** Insert a multi-line `#` comment header citing Phase 23 ATTEST-03 (generator), the load-bearing RESEARCH §2.2 correction (CONTEXT D-03 was incorrect that attest-sbom bundles Syft; the generator step is required), the digest-pinned image-scan rationale (avoid local-tag vs registry-pushed race), and the `upload-artifact: false` / `upload-release-assets: false` rationale (the operator-facing artifact is the ATTESTATION, not the SBOM file). The step uses:
      - `uses: anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610 # v0.24.0` (40-hex SHA + two spaces + `# v0.24.0` per PATTERNS §"Shared #2")
      - `with:` block with five inputs: `image:` set to `ghcr.io/${{ github.repository_owner }}/blindjoin-${{ matrix.image }}@${{ steps.build.outputs.digest }}` (digest-pinned, so we scan the EXACT bytes pushed); `format: spdx-json` (explicit even though it's the default — auditor clarity); `output-file: sbom.spdx.json` (consumed by step 2); `upload-artifact: false`; `upload-release-assets: false`.
      - Step name: `Generate SPDX SBOM with Syft (ATTEST-03 generator)`.

    **Step 2 — Attest SBOM (ATTEST-03 attest half).** Insert a multi-line `#` comment header citing Phase 23 ATTEST-03 (attest), the RESEARCH §2.2 correction (this step consumes the file, does not generate), the predicate-type auto-derivation from `sbom-path` file format (SPDX-JSON → `https://spdx.dev/Document`), and the v2.4.0 (NOT v3.X/v4.X wrapper) pin rationale per RESEARCH §2.3 + §7.4 (v3/v4 are wrappers on `actions/attest`; v2.4.0 is the last release with self-contained docs matching CONTEXT D-03's wording). The step uses:
      - `uses: actions/attest-sbom@bd218ad0dbcb3e146bd073d1d9c6d78e08aa8a0b # v2.4.0`
      - `with:` block with four inputs: `subject-name: ghcr.io/${{ github.repository_owner }}/blindjoin-${{ matrix.image }}`; `subject-digest: ${{ steps.build.outputs.digest }}` (consumed from Plan 23-01's `id: build`); `sbom-path: 'sbom.spdx.json'` (reads the file step 1 wrote); `push-to-registry: true` (D-01 inline registry-attached path; operator retrieval via `gh attestation verify oci://...` per Plan 23-04).
      - Step name: `Attest SBOM (ATTEST-03)`.

    Both steps land after Plan 23-01's `cosign sign` step. Per D-01, all matrix legs are independent; `fail-fast: false` (already set) means a per-leg failure does not block the others.

    Implementing ATTEST-03 via the RESEARCH §2.2-corrected TWO-step pattern (Syft → attest), D-03 (locked action choice), D-04 (SHA-pin discipline; the sigstore-pin-check job in Plan 23-03 enforces the 40-hex pins).

    DO NOT use `actions/attest-sbom@v3` or `@v4` — those are deprecation wrappers (RESEARCH §7.4). Pin v2.4.0 = `bd218ad0dbcb3e146bd073d1d9c6d78e08aa8a0b`.
    DO NOT omit the `anchore/sbom-action` step and try to satisfy ATTEST-03 with just `actions/attest-sbom` — the action does NOT generate SBOMs (RESEARCH §2.2 + §7.1; verified at the v2.4.0 README).
    DO NOT use `image: ghcr.io/.../<image>:<tag>` (tag form) on the anchore/sbom-action step — RESEARCH Assumption A2 + the locked YAML use the digest-pinned form `<image>@${{ steps.build.outputs.digest }}` to avoid scanning a tag that might point to a different digest than what was just pushed.
    DO NOT set `upload-artifact: true` (the default) — RESEARCH §8 Q4 + the locked YAML mandate `false`; otherwise the SBOM gets uploaded as a workflow artifact (90-day default retention) and creates the "which SBOM is canonical?" question the attestation is designed to answer.
    DO NOT use `format: spdx` (without `-json`) — REQUIREMENTS ATTEST-03 says "SPDX format, generated by Syft" and `actions/attest-sbom` accepts SPDX-JSON or CycloneDX-JSON; the locked format is `spdx-json` per RESEARCH §3.3.
    DO NOT reorder the two steps — attest-sbom's `sbom-path: 'sbom.spdx.json'` is a HARD dependency on the file generated by the anchore/sbom-action step.
  </action>
  <verify>
    <automated>python3 -c 'import yaml; yaml.safe_load(open(".github/workflows/docker.yml"))' &amp;&amp; grep -q 'anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610 # v0.24.0' .github/workflows/docker.yml &amp;&amp; grep -q 'actions/attest-sbom@bd218ad0dbcb3e146bd073d1d9c6d78e08aa8a0b # v2.4.0' .github/workflows/docker.yml &amp;&amp; grep -q 'format: spdx-json' .github/workflows/docker.yml &amp;&amp; grep -q 'output-file: sbom.spdx.json' .github/workflows/docker.yml &amp;&amp; grep -q "sbom-path: 'sbom.spdx.json'" .github/workflows/docker.yml &amp;&amp; grep -q 'upload-artifact: false' .github/workflows/docker.yml &amp;&amp; grep -q 'push-to-registry: true' .github/workflows/docker.yml &amp;&amp; grep -q 'ATTEST-03' .github/workflows/docker.yml &amp;&amp; grep -qE 'RESEARCH §?2\.2' .github/workflows/docker.yml &amp;&amp; ! grep -q 'actions/attest-sbom@v3\|actions/attest-sbom@v4\|anchore/sbom-action@v[0-9]\b' .github/workflows/docker.yml</automated>
  </verify>
  <acceptance_criteria>
    - File is valid YAML after edits: `python3 -c 'import yaml; yaml.safe_load(open(".github/workflows/docker.yml"))'` exit 0
    - anchore/sbom-action SHA pin (RESEARCH §2.3): `grep -q 'anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610 # v0.24.0' .github/workflows/docker.yml`
    - actions/attest-sbom SHA pin (RESEARCH §2.3 — v2.4.0, NOT v3/v4 wrappers): `grep -q 'actions/attest-sbom@bd218ad0dbcb3e146bd073d1d9c6d78e08aa8a0b # v2.4.0' .github/workflows/docker.yml`
    - SPDX-JSON format explicit (REQUIREMENTS ATTEST-03 + auditor clarity): `grep -q 'format: spdx-json' .github/workflows/docker.yml`
    - Generator writes the file step 2 consumes: `grep -q 'output-file: sbom.spdx.json' .github/workflows/docker.yml && grep -q "sbom-path: 'sbom.spdx.json'" .github/workflows/docker.yml`
    - Generator sets upload-artifact: false (RESEARCH §8 Q4): `grep -q 'upload-artifact: false' .github/workflows/docker.yml`
    - Generator sets upload-release-assets: false (RESEARCH §3.3): `grep -q 'upload-release-assets: false' .github/workflows/docker.yml`
    - attest-sbom pushes to registry (D-01 inline path): `grep -q 'push-to-registry: true' .github/workflows/docker.yml`
    - attest-sbom uses digest-pinned subject (consumed from Plan 23-01): `grep -q 'subject-digest: \${{ steps\.build\.outputs\.digest }}' .github/workflows/docker.yml`
    - SBOM scanner step uses digest-pinned image reference (RESEARCH Assumption A2): `grep -q 'image: ghcr\.io/\${{ github\.repository_owner }}/blindjoin-\${{ matrix\.image }}@\${{ steps\.build\.outputs\.digest }}' .github/workflows/docker.yml`
    - ATTEST-03 cited in comments (audit trail): `grep -q 'ATTEST-03' .github/workflows/docker.yml`
    - RESEARCH §2.2 correction cited (so the why-two-steps lineage is auditable): `grep -qE 'RESEARCH §?2\.2' .github/workflows/docker.yml`
    - No floating-tag versions of the two new actions: `! grep -qE 'actions/attest-sbom@v[0-9]|anchore/sbom-action@v[0-9]+\.[0-9]+(\.[0-9]+)?$' .github/workflows/docker.yml`
    - Step ordering: anchore/sbom-action line > cosign sign line; actions/attest-sbom line > anchore/sbom-action line:
      `awk '/cosign sign --yes/{a=NR} /anchore\/sbom-action@e22c3899/{b=NR} /actions\/attest-sbom@bd218ad0/{c=NR} END{exit !(a<b && b<c)}' .github/workflows/docker.yml`
    - Per-step comment headers present (PATTERNS §"Shared #3" comments-as-contract): each step has at least 4 comment lines above citing the REQ-ID + the why-this-step rationale.
    - End-to-end smoke (deferred to Plan 23-05 Stage 2): on a real rc.0 tag push, `cosign download attestation --predicate-type https://spdx.dev/Document` (legacy path may 404 — see RESEARCH §2.4) OR `gh attestation verify oci://ghcr.io/.../blindjoin-coordinator:1.6.0-rc.0 --repo <owner>/blindjoin --predicate-type https://spdx.dev/Document` (primary path, Plan 23-04 recipe) succeeds.
  </acceptance_criteria>
  <done>
    `.github/workflows/docker.yml` has two new steps appended to the `docker` job after Plan 23-01's cosign sign step. Step 1 is `anchore/sbom-action@e22c389... # v0.24.0` with the locked `with:` inputs (image: digest-pinned, format: spdx-json, output-file: sbom.spdx.json, upload-artifact: false, upload-release-assets: false). Step 2 is `actions/attest-sbom@bd218ad0... # v2.4.0` with the locked `with:` inputs (subject-name, subject-digest from steps.build.outputs.digest, sbom-path: 'sbom.spdx.json', push-to-registry: true). Both steps have multi-line comment headers citing ATTEST-03 + RESEARCH §2.2. Step ordering is generator → attest. File remains valid YAML.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add `actions/attest-build-provenance` step for ATTEST-02</name>
  <files>.github/workflows/docker.yml</files>
  <read_first>
    - .github/workflows/docker.yml (verify Task 1's attest-sbom step is present; identify the line where the new attest-build-provenance step will append)
    - .planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-RESEARCH.md §3.4 lines 316-342 (locked YAML for actions/attest-build-provenance: subject-name + subject-digest + push-to-registry: true; the v3.2.0 NOT v4.X pin rationale + v1.7 carry-forward note; the predicate-type auto-derivation)
    - .planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-RESEARCH.md §2.3 lines 164-166 (SHA pin: actions/attest-build-provenance @ v3.2.0 = 96278af6...)
    - .planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-RESEARCH.md §2.4 lines 184-213 (the architectural distinction: cosign sign produces <digest>.sig — ATTEST-01 needs this; attest-build-provenance produces sha256-<HEX> referrer manifest — ATTEST-02 needs this; both REQUIRED, NOT interchangeable; Architectural Responsibility Map at lines 204-213)
    - .planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-RESEARCH.md §7.3 lines 666-670 (NEW Pitfall: cosign sign and attest-build-provenance are NOT interchangeable; PLAN.md MUST include both with distinct REQ-IDs)
    - .planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-RESEARCH.md §7.4 lines 672-676 (NEW Pitfall: v4 wrapper; pin v3.2.0 / v2.4.0)
    - .planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-PATTERNS.md §6 lines 253-286 (locked destination shape for attest-build-provenance — exact subject-name / subject-digest / push-to-registry: true)
    - .planning/research/PITFALLS.md §Pitfall 5 (attest-build-provenance over slsa-github-generator — locked at roadmap level)
  </read_first>
  <action>
    Append ONE new step to the `docker` job in `.github/workflows/docker.yml`, immediately AFTER Task 1's `actions/attest-sbom` step. The step emits the SLSA v1.0 in-toto build provenance attestation that ATTEST-02 requires.

    Insert a multi-line `#` comment header citing Phase 23 ATTEST-02, the `https://slsa.dev/provenance/v1` predicate type emitted by the action (auto-derived from the workflow context), the registry-attached storage form (referrer of `ghcr.io/.../blindjoin-<image>@sha256:<HEX>`), the `gh attestation verify oci://...` primary retrieval path (Plan 23-04 SECURITY.md recipe — `cosign download attestation` returns 404 against GitHub attestation artifacts per RESEARCH §2.4 + issue #162), the v3.2.0 (NOT v4.X) pin rationale per RESEARCH §2.3 + §7.4 (v4 is a wrapper on actions/attest; v3.2.0 is the last release matching CONTEXT D-03 wording; v1.7 carry-forward note), and the explicit RESEARCH §2.4 + §7.3 reminder that `cosign sign` (Plan 23-01) and `attest-build-provenance` (this step) are NOT interchangeable — they produce different OCI artifacts under different tag conventions (`<digest>.sig` vs `sha256-<HEX>` referrer), so BOTH are required (ATTEST-01 needs the former, ATTEST-02 the latter).

    The step uses:
      - `uses: actions/attest-build-provenance@96278af6caaf10aea03fd8d33a09a777ca52d62f # v3.2.0` (40-hex SHA + two spaces + `# v3.2.0` per PATTERNS §"Shared #2")
      - `with:` block with three inputs: `subject-name: ghcr.io/${{ github.repository_owner }}/blindjoin-${{ matrix.image }}`; `subject-digest: ${{ steps.build.outputs.digest }}` (consumed from Plan 23-01's `id: build`); `push-to-registry: true` (D-01 inline registry-attached path)
      - Step name: `Attest build provenance (ATTEST-02)`

    The step lands AFTER Task 1's `actions/attest-sbom` step (alphabetical SBOM-then-provenance ordering is a reasonable convention per RESEARCH §3.4 — the order between attest-sbom and attest-build-provenance is not contentful, but consistent ordering aids audit reading). This is the LAST new step in the `docker` job for Phase 23; no further sign/attest steps are added.

    Implementing ATTEST-02 (SLSA v1.0 build-level-3 in-toto provenance via `actions/attest-build-provenance` per CONTEXT D-03 + Pitfall 5 locked path), the RESEARCH §7.3 invariant (both cosign sign AND attest-build-provenance required), and D-04 (SHA-pin enforced by Plan 23-03's sigstore-pin-check).

    DO NOT use `actions/attest-build-provenance@v4` — that is a deprecation wrapper around `actions/attest` (RESEARCH §7.4). Pin v3.2.0 = `96278af6caaf10aea03fd8d33a09a777ca52d62f`.
    DO NOT use `slsa-framework/slsa-github-generator` — Pitfall 5 locks the path at roadmap level + CONTEXT D-03 names actions/attest-build-provenance verbatim. Two paths would produce two competing attestations per image.
    DO NOT omit `push-to-registry: true` — without it the attestation is only persisted to GitHub's attestations API and not retrievable via `cosign verify-blob-attestation`/`gh attestation verify oci://...`. ATTEST-02 implies registry reachability.
    DO NOT add any other inputs to the `with:` block beyond the three locked ones — the action derives the predicate-type, workflow-file, tag-ref, and source-commit from the workflow context automatically. Extra inputs are unnecessary and add audit-surface.
    DO NOT skip this step thinking ATTEST-02 is satisfied by `cosign sign` from Plan 23-01 — RESEARCH §7.3 explicitly enumerates this failure mode. The two steps produce DIFFERENT OCI artifacts and ATTEST-02 specifically requires the SLSA in-toto bundle.
  </action>
  <verify>
    <automated>python3 -c 'import yaml; yaml.safe_load(open(".github/workflows/docker.yml"))' &amp;&amp; grep -q 'actions/attest-build-provenance@96278af6caaf10aea03fd8d33a09a777ca52d62f # v3.2.0' .github/workflows/docker.yml &amp;&amp; grep -q 'ATTEST-02' .github/workflows/docker.yml &amp;&amp; grep -q 'slsa.dev/provenance/v1\|SLSA v1\.0' .github/workflows/docker.yml &amp;&amp; grep -q 'gh attestation verify\|gh attestation verify oci' .github/workflows/docker.yml &amp;&amp; ! grep -q 'actions/attest-build-provenance@v4\|slsa-framework/slsa-github-generator' .github/workflows/docker.yml &amp;&amp; [ "$(grep -c 'push-to-registry: true' .github/workflows/docker.yml)" -ge 2 ]</automated>
  </verify>
  <acceptance_criteria>
    - File is valid YAML after edit: `python3 -c 'import yaml; yaml.safe_load(open(".github/workflows/docker.yml"))'` exit 0
    - actions/attest-build-provenance SHA pin (RESEARCH §2.3 — v3.2.0, NOT v4 wrapper): `grep -q 'actions/attest-build-provenance@96278af6caaf10aea03fd8d33a09a777ca52d62f # v3.2.0' .github/workflows/docker.yml`
    - subject-name uses repo owner + matrix image: `grep -q 'subject-name: ghcr\.io/\${{ github\.repository_owner }}/blindjoin-\${{ matrix\.image }}' .github/workflows/docker.yml`
    - subject-digest plumbed from Plan 23-01 id:build: `grep -q 'subject-digest: \${{ steps\.build\.outputs\.digest }}' .github/workflows/docker.yml`
    - push-to-registry: true on this step (D-01 inline path): file has at least 2 occurrences of `push-to-registry: true` (one for attest-sbom from Task 1, one for attest-build-provenance from this task): `[ "$(grep -c 'push-to-registry: true' .github/workflows/docker.yml)" -ge 2 ]`
    - ATTEST-02 cited in comments (audit trail): `grep -q 'ATTEST-02' .github/workflows/docker.yml`
    - SLSA v1.0 / predicate-type cited (so auditors see the predicate emitted): `grep -qE 'slsa\.dev/provenance/v1|SLSA v1\.0' .github/workflows/docker.yml`
    - gh attestation verify mentioned as the primary retrieval path (RESEARCH §2.4 — cosign download attestation returns 404 against these artifacts): `grep -qE 'gh attestation verify' .github/workflows/docker.yml`
    - Pitfall 5 cited (single-path choice — attest-build-provenance over slsa-github-generator): `grep -qE 'Pitfall 5|PITFALLS Pitfall 5' .github/workflows/docker.yml`
    - RESEARCH §7.3 / §2.4 NOT-interchangeable invariant cited (cosign sign AND attest-build-provenance both required): `grep -qE 'NOT interchangeable|not interchangeable|RESEARCH §?2\.4|RESEARCH §?7\.3' .github/workflows/docker.yml`
    - No deprecation-wrapper versions: `! grep -q 'actions/attest-build-provenance@v4' .github/workflows/docker.yml`
    - No alternative SLSA path: `! grep -q 'slsa-framework/slsa-github-generator' .github/workflows/docker.yml`
    - Step ordering: attest-build-provenance line > attest-sbom line:
      `awk '/actions\/attest-sbom@bd218ad0/{a=NR} /actions\/attest-build-provenance@96278af/{b=NR} END{exit !(a<b)}' .github/workflows/docker.yml`
    - This is the LAST new step in the docker job — no further sign/attest steps added in this plan: `awk '/actions\/attest-build-provenance@96278af/{found=1} found && /^[[:space:]]*-[[:space:]]+name:|^[[:space:]]*-[[:space:]]+uses:/{count++} END{exit !(count<=1)}' .github/workflows/docker.yml` (verifies no further `- name:` or `- uses:` step appears below the attest-build-provenance step within the docker job).
    - End-to-end smoke (deferred to Plan 23-05 Stage 2): on a real rc.0 tag push, `gh attestation verify oci://ghcr.io/.../blindjoin-coordinator:1.6.0-rc.0 --repo <owner>/blindjoin --predicate-type https://slsa.dev/provenance/v1` returns "Verified provenance attestation" (per Plan 23-04 recipe).
  </acceptance_criteria>
  <done>
    `.github/workflows/docker.yml` has one new step appended after Task 1's attest-sbom step: `actions/attest-build-provenance@96278af6... # v3.2.0` with the locked `with:` inputs (subject-name, subject-digest from steps.build.outputs.digest, push-to-registry: true). The step has a multi-line comment header citing ATTEST-02 + Pitfall 5 + the RESEARCH §2.4 + §7.3 not-interchangeable invariant + the v1.7 carry-forward note (migrate to actions/attest@v4 as a single step). File is valid YAML. Step ordering: cosign sign → anchore/sbom-action → actions/attest-sbom → actions/attest-build-provenance.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → GitHub attestations API | TLS-secured REST call persisting the attestation; bounded by `attestations: write` permission (granted by Plan 23-01). |
| GitHub Actions runner → ghcr.io (referrer push) | `push-to-registry: true` writes the attestation as an OCI referrer manifest of `<image>@<digest>`; bounded by `packages: write` (Plan 23-01). |
| anchore/sbom-action → Docker daemon | The scanner action uses the runner's Docker daemon to pull the digest-pinned image and run Syft against the filesystem. Bounded to the just-pushed image. |
| anchore/sbom-action → runner FS | Writes `sbom.spdx.json` to the runner working directory. Transient; consumed only by `actions/attest-sbom` then thrown away. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-08 | Tampering | `actions/attest-sbom` v4 wrapper used instead of v2.4.0 — inputs may have shifted (e.g., predicate-type now required for non-default attestations) | mitigate | Task 1 pins `@bd218ad0... # v2.4.0` explicitly; Plan 23-03's sigstore-pin-check enforces the SHA at file level. RESEARCH §7.4 documents the wrapper-deprecation pattern. |
| T-23-09 | Tampering | `anchore/sbom-action` omitted; planner naively follows CONTEXT D-03 which incorrectly claimed attest-sbom bundles Syft (RESEARCH §7.1) | mitigate | Task 1's `<read_first>` block surfaces the RESEARCH §2.2 correction explicitly; the acceptance criteria assert BOTH actions are present (`grep -q 'anchore/sbom-action@...'` AND `grep -q 'actions/attest-sbom@...'`). Plan 23-05 Stage 2 empirically confirms an SPDX attestation lands on the rc.0 image. |
| T-23-10 | Tampering | `cosign sign` (Plan 23-01) omitted thinking ATTEST-02's attest-build-provenance covers ATTEST-01 (RESEARCH §7.3) | mitigate | Plan 23-01 Task 2 delivers `cosign sign` independently. Plan 23-02's `<threat_model>` (this block) re-cites RESEARCH §7.3 as a load-bearing distinction. The two REQ-IDs (ATTEST-01 + ATTEST-02) are tracked separately in this plan's frontmatter. |
| T-23-11 | Tampering | `provenance: mode=max` / `sbom: true` added to docker/build-push-action `with:` block, producing competing BuildKit-native attestations (RESEARCH §7.7 + Pitfall 5) | mitigate | Plan 23-01 Task 1 explicitly forbids this; Plan 23-02 inherits the invariant. The Plan 23-01 acceptance criterion `! grep -qE '^[[:space:]]+provenance:[[:space:]]+(mode|true)'` continues to hold (this plan does not touch the build-push step). |
| T-23-12 | Information Disclosure | SBOM SPDX-JSON contains file paths, package versions, and image metadata | accept | Public information by design. SPDX format is open; CVE-grep is the operator workflow ATTEST-03 enables. No PII in container filesystems (Rust binaries + minimal Debian base). |
| T-23-13 | Denial of Service | `anchore/sbom-action` Syft scan times out on a large image | accept-with-action | blindjoin images are <100MB (distroless-derived); Syft scan completes in <30s typical. If timeout becomes an issue, the action accepts a `timeout:` input. Defer until observed. |
| T-23-14 | Information Disclosure | `sbom.spdx.json` left on the runner FS after the job completes | accept | Runner FS is destroyed after each job. `upload-artifact: false` ensures no 90-day workflow-artifact persistence. |
| T-23-15 | Repudiation | Attestations pushed to registry but not actually retrievable by operator | mitigate-with-UAT | Plan 23-05 Stage 2 runs `gh attestation verify oci://...` recipes against the rc.0 tag and confirms BOTH ATTEST-02 (SLSA) and ATTEST-03 (SPDX) retrieve cleanly. If a retrieval fails, the fix is a quick task before the production v1.6.0 tag. |
| T-23-SC | Tampering | npm/pip/cargo installs | n/a | This plan installs only GitHub Actions (`anchore/sbom-action`, `actions/attest-sbom`, `actions/attest-build-provenance`) all SHA-pinned. anchore/sbom-action bundles Syft as a binary — installed by the action, not via package manager. No npm/pip/cargo registries touched. |
</threat_model>

<verification>
After both tasks complete:

```bash
# 1. YAML validity.
python3 -c 'import yaml; yaml.safe_load(open(".github/workflows/docker.yml"))'

# 2. All three new actions present + SHA-pinned (RESEARCH §2.3).
grep -q 'anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610 # v0.24.0' .github/workflows/docker.yml
grep -q 'actions/attest-sbom@bd218ad0dbcb3e146bd073d1d9c6d78e08aa8a0b # v2.4.0' .github/workflows/docker.yml
grep -q 'actions/attest-build-provenance@96278af6caaf10aea03fd8d33a09a777ca52d62f # v3.2.0' .github/workflows/docker.yml

# 3. ATTEST-03 generator + attest pair (RESEARCH §2.2 corrected pattern).
grep -q 'format: spdx-json' .github/workflows/docker.yml
grep -q 'output-file: sbom.spdx.json' .github/workflows/docker.yml
grep -q "sbom-path: 'sbom.spdx.json'" .github/workflows/docker.yml

# 4. push-to-registry on BOTH attest-* steps (D-01 inline path).
test "$(grep -c 'push-to-registry: true' .github/workflows/docker.yml)" -ge 2

# 5. No deprecation-wrapper or alternative SLSA paths.
! grep -q 'actions/attest-build-provenance@v4' .github/workflows/docker.yml
! grep -q 'actions/attest-sbom@v3\|actions/attest-sbom@v4' .github/workflows/docker.yml
! grep -q 'slsa-framework/slsa-github-generator' .github/workflows/docker.yml

# 6. No competing BuildKit-native attestations (Pitfall 5 + RESEARCH §7.7).
! grep -qE '^[[:space:]]+provenance:[[:space:]]+(mode|true)' .github/workflows/docker.yml
! grep -qE '^[[:space:]]+sbom:[[:space:]]*true' .github/workflows/docker.yml

# 7. REQ-IDs cited in comments (audit trail).
grep -q 'ATTEST-02' .github/workflows/docker.yml
grep -q 'ATTEST-03' .github/workflows/docker.yml

# 8. Step ordering: cosign sign → sbom-action → attest-sbom → attest-build-provenance.
awk '/cosign sign --yes/{a=NR} /anchore\/sbom-action@e22c3899/{b=NR} /actions\/attest-sbom@bd218ad0/{c=NR} /actions\/attest-build-provenance@96278af/{d=NR} END{exit !(a<b && b<c && c<d)}' .github/workflows/docker.yml

# 9. End-to-end rehearsal — deferred to Plan 23-05 Stage 2 (post-rc.0 tag, fresh ubuntu:24.04).
```
</verification>

<success_criteria>
- `.github/workflows/docker.yml` has three new steps appended to the `docker` job, in order: `anchore/sbom-action` (Syft SPDX-JSON generator), `actions/attest-sbom` (signer for ATTEST-03), `actions/attest-build-provenance` (signer for ATTEST-02).
- All three actions are SHA-pinned to v3.X/v2.X (NOT v4.X deprecation wrappers) per RESEARCH §2.3.
- Both attest-* steps have `push-to-registry: true` (D-01 inline registry-attached path).
- The SBOM file `sbom.spdx.json` is generated by the Syft step and consumed by the attest-sbom step (file flow integrity).
- `upload-artifact: false` + `upload-release-assets: false` on the sbom-action step (RESEARCH §8 Q4 — attestation IS the operator-facing artifact).
- The build-push-action `with:` block stays free of `provenance:` / `sbom:` inputs (Pitfall 5 + RESEARCH §7.7).
- File is valid YAML.
- Maps to ROADMAP Phase 23 Success Criterion #2 (SLSA v1.0 provenance retrievable via `gh attestation verify oci://...`) and #3 (SPDX SBOM attestation retrievable for CVE grep).
</success_criteria>

<output>
Create `.planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-02-SUMMARY.md` when done. Include: (a) the literal new step blocks added (3 steps × comment-header + uses + with), (b) the SHA pins used (3 new actions; all 4 sigstore-ecosystem actions for the Plan 23-03 gate to verify), (c) the step ordering invariant verified (cosign sign → sbom-action → attest-sbom → attest-build-provenance), and (d) the cross-reference to Plan 23-04 (SECURITY.md) which documents the `gh attestation verify oci://...` operator-side retrieval recipe for ATTEST-02 + ATTEST-03.
</output>
</content>
</invoke>