---
phase: 24-release-tarball-signing-cosign-slsa-pgp
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/RELEASING.md
autonomous: true
requirements:
  - SIGN-03
requirements_addressed:
  - SIGN-03

must_haves:
  truths:
    - "`docs/RELEASING.md` is a new file at the repo's `docs/` directory (sibling to `docs/AUDIT-CHARTER.md`, `docs/PROTOCOL.md`, `docs/branch-protection.md`). Filename uses UPPERCASE convention matching `AUDIT-CHARTER.md` + `PROTOCOL.md` precedent."
    - "The file's audience disambiguation lede (opening paragraph) reads: 'Maintainer-side procedure for cutting a release. Contributors don't need this — see [CONTRIBUTING.md](../CONTRIBUTING.md) for the contributor manual.' (D-11 — separates maintainer ops from contributor onboarding)."
    - "An H2 `## Prerequisites` section lists 5 prerequisite items: YubiKey 5 (firmware ≥ 5.2.3 for ed25519 support), `gpg` 2.4+, `gh` 2.50+, `cosign` 2.6.3+ (for pre-flight verify per RESEARCH §5.5), and `<owner>.github.io` repo with WKD published (one-time setup — see Publishing the key to WKD section)."
    - "An H2 `## Per-release procedure (5 steps)` section enumerates the maintainer-side workflow as 5 numbered steps with literal `gpg`, `gh release download`, `gh release upload`, `gh release edit` commands (RESEARCH §5.5 verbatim skeleton). Step 1 is `git tag -s vX.Y.Z -m \"vX.Y.Z\"` + `git push --tags`; step 4 is `gpg --detach-sign --armor --local-user <FINGERPRINT-TBD> blindjoin-linux-amd64.tar.gz` (YubiKey prompts for touch); step 5 is `gh release upload vX.Y.Z blindjoin-linux-amd64.tar.gz.asc && gh release edit vX.Y.Z --draft=false`."
    - "An H2 `## Pre-flight check before flipping out of draft` section directs the maintainer to cosign-verify all 4 CI-produced assets BEFORE running step 5's `--draft=false`. If any cosign verify fails, DO NOT flip; delete the release with `gh release delete vX.Y.Z` and re-cut the tag after the fix (RESEARCH §5.5 pre-flight checklist)."
    - "An H2 `## PGP key generation (one-time, NOT a release-cut step)` section documents the 5-step YubiKey ceremony per RESEARCH §5.1: generate on YubiKey via `gpg --card-edit` → `admin` → `generate` with ed25519 + signing-only + 2-year expiry + UID `blindjoin maintainer <johnturner@gmail.com>` (D-08), generate revocation cert + store offline + shred from disk, export public key to `docs/pgp/<FINGERPRINT-TBD>.asc` (D-09), publish to keys.openpgp.org via `gpg --send-keys --keyserver hkps://keys.openpgp.org <FINGERPRINT-TBD>`, then publish to WKD (cross-reference to the WKD section)."
    - "An H2 `## PGP key rotation (every 2 years)` section documents the 6-step rotation procedure per RESEARCH §5.2: 6-months-before-expiry generation of new key, cross-signing with old key via `gpg --sign-key <new-FINGERPRINT-TBD>`, committing new public key alongside (NOT replacing) the old `.asc` file, updating SECURITY.md `<a id=\"pgp-current\"></a>` anchor to name the new fingerprint, publishing new key to keys.openpgp.org + WKD, cutting next release with new key + CHANGELOG entry naming the rotation event."
    - "An H2 `## PGP key revocation (emergency — YubiKey lost or compromised)` section documents publishing the offline-stored `revoke.asc` immediately via `gpg --import revoke.asc && gpg --send-keys --keyserver hkps://keys.openpgp.org <FINGERPRINT-TBD>`, then running the rotation procedure for the new key (RESEARCH §5.2 pitfall — revocation ≠ rotation)."
    - "An H2 `## Publishing the key to keys.openpgp.org` section documents `gpg --send-keys --keyserver hkps://keys.openpgp.org <FINGERPRINT-TBD>` + email confirmation flow (RESEARCH §5.1 step 4)."
    - "An H2 `## Publishing the key to WKD` section documents the 5-step WKD direct-method procedure per RESEARCH §5.3: verify `<owner>.github.io` repo exists (with `gh repo create <owner>.github.io --public` fallback for first-time setup), compute WKD hash via `gpg-wks-client --print-wkd-hash johnturner@gmail.com`, export binary keyring via `gpg --no-armor --export johnturner@gmail.com > <WKD_HASH>`, commit to `<owner>.github.io/.well-known/openpgpkey/hu/<WKD_HASH>`, test WKD resolution via `gpg --auto-key-locate wkd --locate-keys johnturner@gmail.com`. The procedure also notes that GitHub Pages does NOT support subdomain-method WKD on `*.github.io` (only direct method is viable per RESEARCH §5.3)."
    - "The file uses `<FINGERPRINT-TBD>` as the literal placeholder string everywhere the maintainer's actual fingerprint would appear (Plan 24-05 atomically replaces ALL `<FINGERPRINT-TBD>` occurrences when the maintainer generates the key per the §6 deliverable-ordering recommendation in RESEARCH)."
    - "The file does NOT execute any of these procedures (D-08 + D-17 + D-19 — documented, not executed in Phase 24; the maintainer's first execution is captured at the v1.6.0-rc.0 cut)."
  artifacts:
    - path: "docs/RELEASING.md"
      provides: "Maintainer-side release procedure (5-step per-release flow + audience-disambiguation lede)"
      contains: "Per-release procedure"
    - path: "docs/RELEASING.md"
      provides: "PGP key generation 5-step YubiKey ceremony (RESEARCH §5.1)"
      contains: "gpg --card-edit"
    - path: "docs/RELEASING.md"
      provides: "PGP key rotation 6-step procedure (RESEARCH §5.2)"
      contains: "PGP key rotation"
    - path: "docs/RELEASING.md"
      provides: "PGP key revocation emergency procedure (RESEARCH §5.2 pitfall)"
      contains: "PGP key revocation"
    - path: "docs/RELEASING.md"
      provides: "WKD direct-method publication procedure (RESEARCH §5.3)"
      contains: "gpg-wks-client --print-wkd-hash"
    - path: "docs/RELEASING.md"
      provides: "keys.openpgp.org publication procedure (RESEARCH §5.1 step 4)"
      contains: "hkps://keys.openpgp.org"
    - path: "docs/RELEASING.md"
      provides: "Prerequisite list (YubiKey 5 fw ≥ 5.2.3, gpg 2.4+, gh 2.50+, cosign 2.6.3+, <owner>.github.io)"
      contains: "YubiKey 5"
    - path: "docs/RELEASING.md"
      provides: "Pre-flight check (cosign verify all 4 CI-produced assets BEFORE --draft=false)"
      contains: "Pre-flight check"
    - path: "docs/RELEASING.md"
      provides: "Per-release step 4: detach-sign on YubiKey with <FINGERPRINT-TBD> placeholder"
      contains: "gpg --detach-sign --armor --local-user <FINGERPRINT-TBD>"
    - path: "docs/RELEASING.md"
      provides: "Per-release step 5: gh release upload .asc + gh release edit --draft=false (D-07 flip)"
      contains: "gh release edit"
    - path: "docs/RELEASING.md"
      provides: "Audience-disambiguation lede separating maintainer ops from contributor onboarding (D-11)"
      contains: "Contributors don't need this"
  key_links:
    - from: "docs/RELEASING.md per-release procedure"
      to: ".github/workflows/release.yml build job"
      via: "tag push triggers release.yml; CI produces 4-asset draft Release"
      pattern: "release\\.yml"
    - from: "docs/RELEASING.md PGP key generation section"
      to: "docs/pgp/<FINGERPRINT-TBD>.asc"
      via: "gpg --export --armor <fingerprint> > docs/pgp/<fingerprint>.asc"
      pattern: "docs/pgp/"
    - from: "docs/RELEASING.md WKD section"
      to: "<owner>.github.io/.well-known/openpgpkey/hu/<WKD_HASH>"
      via: "gpg-wks-client --print-wkd-hash + direct method file layout"
      pattern: "\\.well-known/openpgpkey/hu/"
    - from: "docs/RELEASING.md per-release step 5"
      to: "softprops/action-gh-release draft: true (Plan 24-01)"
      via: "gh release edit vX.Y.Z --draft=false"
      pattern: "--draft=false"
---

<objective>
Create the new file `docs/RELEASING.md` documenting the maintainer-side release procedure (D-11). The file covers: prerequisites (tools + YubiKey + `<owner>.github.io` repo), the 5-step per-release procedure (tag → wait for CI → download → PGP sign on YubiKey → upload .asc + flip draft), the pre-flight cosign-verify check before flipping draft, the one-time PGP key generation ceremony (RESEARCH §5.1), the 2-year key rotation procedure (RESEARCH §5.2), the emergency key revocation procedure, and the publication procedures for keys.openpgp.org + WKD direct-method on `<owner>.github.io`. The file uses `<FINGERPRINT-TBD>` as the placeholder string everywhere the maintainer's actual fingerprint will appear (Plan 24-05 atomically replaces them when the key is generated at v1.6.0-rc.0 cut per the §6 deliverable-ordering recommendation in RESEARCH).

Purpose: SIGN-03's procedural surface lives here. The plan-author cannot execute the YubiKey ceremony (D-08) but can document every step exactly enough that the maintainer can follow it on the day of v1.6.0-rc.0 cut. Without this file, the maintainer has no documented procedure for the post-CI portion of the release (the .asc upload + draft-flip + key rotation + revocation), and the SIGN-03 "non-OIDC alternative path" rationale collapses because no one knows how to operate it. The file's audience disambiguation lede also gates contributors from inadvertently reading maintenance-procedures they don't need (D-11 + D-20 — CONTRIBUTING.md gets a 1-line cross-ref to this file via Plan 24-04). Phase 24 documents these procedures; it does NOT execute them — the maintainer's first execution is the v1.6.0-rc.0 cut.

Output: One new file: `docs/RELEASING.md` (~200-300 lines fully populated per RESEARCH §5.5 estimate). The file uses Markdown with fenced ` ```bash ` blocks for every command snippet; H1 + 8 H2 sections; `<FINGERPRINT-TBD>` placeholder at every fingerprint reference.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-release-tarball-signing-cosign-slsa-pgp/24-CONTEXT.md
@.planning/phases/24-release-tarball-signing-cosign-slsa-pgp/24-RESEARCH.md
@.planning/phases/24-release-tarball-signing-cosign-slsa-pgp/24-PATTERNS.md
@docs/AUDIT-CHARTER.md
@CONTRIBUTING.md

<interfaces>
<!-- This plan creates one new file: docs/RELEASING.md -->
<!-- No code interfaces — pure documentation. -->

<!-- Cross-references this file makes (Plan-author writes; runtime correctness gated by Plan 24-05): -->
<!--   - ../CONTRIBUTING.md (relative link from docs/) — exists -->
<!--   - ../.github/workflows/release.yml — exists (Plan 24-01 modifies it) -->
<!--   - docs/pgp/<FINGERPRINT-TBD>.asc — Plan 24-05 creates this -->
<!--   - SECURITY.md#pgp-current — Plan 24-03 inserts this anchor -->

<!-- Cross-references TO this file (so this file's path matters): -->
<!--   - CONTRIBUTING.md (Plan 24-04 inserts a one-line cross-ref) -->
<!--   - SECURITY.md (Plan 24-03 may reference for the "see docs/RELEASING.md for maintainer ops" thread) -->

<!-- Placeholder convention (Plan 24-05 atomically replaces): -->
<!--   <FINGERPRINT-TBD>   — the full 40-char hex fingerprint, every occurrence -->
<!--   YYYY-MM-DD          — date placeholders kept as literal text (operators see generic dates -->
<!--                          like "generated YYYY-MM-DD, expires YYYY-MM-DD" so the field semantics -->
<!--                          are clear; Plan 24-05 replaces with actual dates from the maintainer's -->
<!--                          key generation event) -->

<!-- Source for procedural prose — copy structurally from RESEARCH §5 (procedural canonical): -->
<!--   §5.1 — ed25519-on-YubiKey ceremony (5 steps) → ## PGP key generation H2 -->
<!--   §5.2 — key rotation cadence + procedure (6 steps) → ## PGP key rotation H2 -->
<!--   §5.2 revocation pitfall → ## PGP key revocation H2 -->
<!--   §5.3 — WKD direct method (5 steps) → ## Publishing the key to WKD H2 -->
<!--   §5.5 — full file skeleton (top-of-file shape + H2 ordering) -->

<!-- Source for placeholder-replacement contract (Plan 24-05 reads this for its atomicity contract): -->
<!--   Every `<FINGERPRINT-TBD>` literal occurrence in docs/RELEASING.md + SECURITY.md -->
<!--   gets replaced with the maintainer's actual 40-char hex fingerprint in ONE atomic commit. -->
<!--   Plan 24-05 lists the exact occurrences. -->

<!-- Filename + path convention (PATTERNS §9): -->
<!--   docs/RELEASING.md (UPPERCASE matching AUDIT-CHARTER.md + PROTOCOL.md precedent) -->
<!--   H1: # Releasing blindjoin -->
<!--   H2 sections in fixed order per RESEARCH §5.5 skeleton -->

<!-- IMPORTANT — Phase 24 does NOT execute any of the documented procedures: -->
<!--   The YubiKey ceremony (D-08), the keys.openpgp.org upload, the WKD publication — -->
<!--   all are maintainer actions taken at v1.6.0-rc.0 cut. Phase 24's scope is documentation. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/RELEASING.md with full structural skeleton + audience-disambiguation lede + prerequisites + per-release procedure + pre-flight check</name>
  <files>docs/RELEASING.md</files>
  <read_first>
    - docs/AUDIT-CHARTER.md (full file — closest in-repo analog for long-form `docs/` policy file shape; focus on H1 + audience-disambiguation lede style + H2 ToC ordering at lines 23, 59, 219, 309, 412 etc.)
    - CONTRIBUTING.md (full file — focus on `## Tagging releases` section at lines 69-94 for the tag-bash example that the per-release procedure step 1 builds on; the relative link form `[CHANGELOG.md](CHANGELOG.md)` Phase 24 mirrors as `[CONTRIBUTING.md](../CONTRIBUTING.md)` from inside `docs/`)
    - .github/workflows/release.yml (verify the file exists pre-Plan-24-01; the per-release procedure step 2 says "Watch [`release.yml`](../.github/workflows/release.yml) in the Actions tab" — link target must resolve)
    - .planning/phases/24-release-tarball-signing-cosign-slsa-pgp/24-RESEARCH.md §5.5 lines 508-557 (the FULL file skeleton — H1, audience-disambiguation lede, 8 H2 sections in fixed order; copy this skeleton structure VERBATIM)
    - .planning/phases/24-release-tarball-signing-cosign-slsa-pgp/24-RESEARCH.md §5.5 lines 524-534 (the per-release 5-step procedure verbatim with the `git tag -s` + `gh release download` + `gpg --detach-sign --armor --local-user <FINGERPRINT-TBD>` + `gh release upload .asc` + `gh release edit --draft=false` literal commands)
    - .planning/phases/24-release-tarball-signing-cosign-slsa-pgp/24-CONTEXT.md §decisions D-11 lines 48 (new file owns maintainer-side procedure; audience disambiguation) + D-06 lines 34 (maintainer-local sign + upload; PGP out of release.yml entirely)
    - .planning/phases/24-release-tarball-signing-cosign-slsa-pgp/24-PATTERNS.md §9 lines 576-637 (the file structure pattern — H1 + 8 H2 ToC + fenced bash blocks + `<FINGERPRINT-TBD>` placeholder convention; cites AUDIT-CHARTER.md as the closest in-repo shape analog)
    - .planning/phases/24-release-tarball-signing-cosign-slsa-pgp/24-RESEARCH.md §"Open Questions" Q1 lines 919-923 (Plan 24-05 does NOT execute keys.openpgp.org or WKD publish — those are maintainer-side first actions at v1.6.0-rc.0; this file documents the procedures)
  </read_first>
  <action>
    Create the new file `docs/RELEASING.md` with the following structure (RESEARCH §5.5 + PATTERNS §9 verbatim skeleton). The file uses H1 + 8 H2 sections in fixed order. Inside each H2, use numbered procedures with fenced ` ```bash ` code blocks for every literal command. Use `<FINGERPRINT-TBD>` as the literal placeholder string everywhere the maintainer's actual fingerprint would appear (Plan 24-05 atomically replaces ALL `<FINGERPRINT-TBD>` occurrences).

    **H1:** `# Releasing blindjoin`

    **Audience-disambiguation lede** (one paragraph immediately after H1): `Maintainer-side procedure for cutting a release. Contributors don't need this — see [CONTRIBUTING.md](../CONTRIBUTING.md) for the contributor manual.` (D-11 lede; sets contributor expectations so they don't go down the maintenance-procedures rabbit hole).

    **H2 `## Prerequisites`** — 5-item bullet list of required tools + setup. Items:
    - `YubiKey 5 (firmware ≥ 5.2.3 for ed25519 support)` with the blindjoin maintainer PGP key (one-time generation: see PGP key generation section). Firmware requirement cited per RESEARCH §5.1 (ed25519 OpenPGP applet supported since YubiKey firmware 5.2.3).
    - `gpg 2.4+` on the maintainer's machine. Install: `brew install gnupg` (macOS) or `apt install gnupg` (Linux).
    - `gh 2.50+` on the maintainer's machine. Install via [cli.github.com](https://cli.github.com).
    - `cosign 2.6.3+` for pre-flight verify (RESEARCH §5.5 pre-flight section). Install: `brew install cosign` (macOS) or download from [sigstore/cosign releases](https://github.com/sigstore/cosign/releases).
    - `<owner>.github.io` repo exists with WKD published (one-time setup — see [Publishing the key to WKD](#publishing-the-key-to-wkd) section).

    **H2 `## Per-release procedure (5 steps)`** — numbered procedure copying RESEARCH §5.5 lines 524-534 verbatim. Each step is its own paragraph with a fenced ` ```bash ` block for the literal commands. Steps:

    1. **Cut and push the tag.** Use signed tags so the maintainer-side commit identity is bound to the release. Use 3-part semver per CONTRIBUTING.md `## Tagging releases`.
       ```bash
       git tag -s vX.Y.Z -m "vX.Y.Z"
       git push --tags
       ```

    2. **Watch `release.yml` until green.** [`release.yml`](../.github/workflows/release.yml) in the Actions tab. CI creates a DRAFT release with 4 assets (`blindjoin-linux-amd64.tar.gz`, `.sha256`, `.bundle`, `.sigstore`).

    3. **Download the CI-built tarball.**
       ```bash
       gh release download vX.Y.Z -p 'blindjoin-linux-amd64.tar.gz' --dir /tmp/blindjoin-release
       ```

    4. **Sign with PGP on YubiKey.** YubiKey will prompt for touch — physical confirmation required.
       ```bash
       cd /tmp/blindjoin-release
       gpg --detach-sign --armor --local-user <FINGERPRINT-TBD> blindjoin-linux-amd64.tar.gz
       ```
       Produces `blindjoin-linux-amd64.tar.gz.asc` in the same directory.

    5. **Upload .asc and flip the release out of draft.** AFTER running the [Pre-flight check](#pre-flight-check-before-flipping-out-of-draft) below.
       ```bash
       gh release upload vX.Y.Z blindjoin-linux-amd64.tar.gz.asc
       gh release edit vX.Y.Z --draft=false
       ```

    **H2 `## Pre-flight check before flipping out of draft`** — the safety gate per RESEARCH §5.5. Document the cosign verify recipe against the freshly-published draft Release artifacts BEFORE running step 5's `--draft=false`. The maintainer downloads the 4 CI-produced assets to a temp directory, runs `cosign verify-blob --bundle ... blindjoin-linux-amd64.tar.gz` against the documented identity-regexp from SECURITY.md, runs `cosign verify-attestation --bundle blindjoin-linux-amd64.tar.gz.sigstore --type slsaprovenance ...`, and ONLY proceeds to step 5 if both verify with exit 0. If any verify fails:
    ```bash
    gh release delete vX.Y.Z
    # Fix the issue (almost certainly a release.yml bug or a permissions misconfiguration);
    # commit + merge the fix; cut a new tag.
    ```

    DO NOT execute any of the procedures documented in this task (D-08 + D-11 — Phase 24 documents only; first execution is at v1.6.0-rc.0 cut).
    DO NOT use a real fingerprint anywhere — every fingerprint reference uses the literal `<FINGERPRINT-TBD>` string (Plan 24-05 replaces atomically).
    DO NOT add any subsection beyond what RESEARCH §5.5 specifies — H1 + audience lede + the H2 sections enumerated. Additional H3 nesting only inside the procedure sections if a long bash block benefits from a sub-bullet break.
    DO NOT use the filename `docs/Releasing.md` or `docs/releasing.md` — the convention is UPPERCASE matching `AUDIT-CHARTER.md` + `PROTOCOL.md` precedent.
    DO NOT cite docs/AUDIT-CHARTER.md beyond its shape inspiration — the content is GnuPG-canonical, not project-specific to blindjoin's threat model.
  </action>
  <verify>
    <automated>test -f docs/RELEASING.md &amp;&amp; grep -q '^# Releasing blindjoin$' docs/RELEASING.md &amp;&amp; grep -q "Contributors don't need this" docs/RELEASING.md &amp;&amp; grep -q '\[CONTRIBUTING.md\](\.\./CONTRIBUTING.md)' docs/RELEASING.md &amp;&amp; grep -q '^## Prerequisites$' docs/RELEASING.md &amp;&amp; grep -q '^## Per-release procedure' docs/RELEASING.md &amp;&amp; grep -q '^## Pre-flight check before flipping out of draft$' docs/RELEASING.md &amp;&amp; grep -q 'YubiKey 5' docs/RELEASING.md &amp;&amp; grep -q '5\.2\.3' docs/RELEASING.md &amp;&amp; grep -q 'gpg 2\.4' docs/RELEASING.md &amp;&amp; grep -q 'gh 2\.50' docs/RELEASING.md &amp;&amp; grep -q 'cosign 2\.6\.3' docs/RELEASING.md &amp;&amp; grep -q '<owner>\.github\.io' docs/RELEASING.md &amp;&amp; grep -q 'git tag -s vX\.Y\.Z' docs/RELEASING.md &amp;&amp; grep -q 'gh release download vX\.Y\.Z' docs/RELEASING.md &amp;&amp; grep -q 'gpg --detach-sign --armor --local-user <FINGERPRINT-TBD>' docs/RELEASING.md &amp;&amp; grep -q 'gh release upload vX\.Y\.Z blindjoin-linux-amd64\.tar\.gz\.asc' docs/RELEASING.md &amp;&amp; grep -q 'gh release edit vX\.Y\.Z --draft=false' docs/RELEASING.md &amp;&amp; grep -q 'cosign verify-blob' docs/RELEASING.md &amp;&amp; grep -q '\[`release.yml`\](\.\./\.github/workflows/release\.yml)' docs/RELEASING.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at the canonical path: `test -f docs/RELEASING.md`
    - H1 title exact match: `grep -q '^# Releasing blindjoin$' docs/RELEASING.md`
    - Audience-disambiguation lede present (D-11): `grep -q "Contributors don't need this" docs/RELEASING.md`
    - Relative link to CONTRIBUTING.md uses `../` from docs/ subdirectory: `grep -q '\[CONTRIBUTING.md\](\.\./CONTRIBUTING.md)' docs/RELEASING.md`
    - H2 `## Prerequisites` present: `grep -q '^## Prerequisites$' docs/RELEASING.md`
    - H2 `## Per-release procedure` (matches 5-step or generic form): `grep -q '^## Per-release procedure' docs/RELEASING.md`
    - H2 `## Pre-flight check before flipping out of draft` present: `grep -q '^## Pre-flight check before flipping out of draft$' docs/RELEASING.md`
    - Prerequisite items present: `grep -q 'YubiKey 5' docs/RELEASING.md && grep -q '5\.2\.3' docs/RELEASING.md && grep -q 'gpg 2\.4' docs/RELEASING.md && grep -q 'gh 2\.50' docs/RELEASING.md && grep -q 'cosign 2\.6\.3' docs/RELEASING.md && grep -q '<owner>\.github\.io' docs/RELEASING.md`
    - Per-release step 1 (tag): `grep -q 'git tag -s vX\.Y\.Z' docs/RELEASING.md`
    - Per-release step 3 (download): `grep -q 'gh release download vX\.Y\.Z' docs/RELEASING.md`
    - Per-release step 4 (detach-sign on YubiKey + placeholder): `grep -q 'gpg --detach-sign --armor --local-user <FINGERPRINT-TBD>' docs/RELEASING.md`
    - Per-release step 5 (upload .asc + draft flip): `grep -q 'gh release upload vX\.Y\.Z blindjoin-linux-amd64\.tar\.gz\.asc' docs/RELEASING.md && grep -q 'gh release edit vX\.Y\.Z --draft=false' docs/RELEASING.md`
    - Pre-flight check section uses cosign verify-blob: `grep -q 'cosign verify-blob' docs/RELEASING.md`
    - Link to release.yml in Actions tab works: `grep -q '\[`release.yml`\](\.\./\.github/workflows/release\.yml)' docs/RELEASING.md`
    - `<FINGERPRINT-TBD>` placeholder used (NOT a real fingerprint): `[ "$(grep -c '<FINGERPRINT-TBD>' docs/RELEASING.md)" -ge 1 ]`
    - No hardcoded fake fingerprint string (40-hex pattern): `! grep -qE '\b[A-F0-9]{40}\b' docs/RELEASING.md` — only `<FINGERPRINT-TBD>` placeholder anywhere; literal 40-hex strings forbidden.
    - File length grew from 0 to ≥40 lines for this task's portion (the full file grows in Task 2): `[ "$(wc -l < docs/RELEASING.md)" -ge 40 ]`
  </acceptance_criteria>
  <done>
    `docs/RELEASING.md` exists with H1 `# Releasing blindjoin`, an audience-disambiguation lede pointing to `[CONTRIBUTING.md](../CONTRIBUTING.md)`, three H2 sections (`## Prerequisites`, `## Per-release procedure (5 steps)`, `## Pre-flight check before flipping out of draft`). Prerequisites lists YubiKey 5 (≥ 5.2.3 fw), gpg 2.4+, gh 2.50+, cosign 2.6.3+, `<owner>.github.io` repo. Per-release procedure has 5 numbered steps with literal `git tag -s` / `gh release download` / `gpg --detach-sign --armor --local-user <FINGERPRINT-TBD>` / `gh release upload .asc` / `gh release edit --draft=false` commands in fenced bash blocks. Pre-flight check documents cosign verify-blob recipe against draft artifacts BEFORE the draft flip. `<FINGERPRINT-TBD>` placeholder used; no real fingerprints anywhere.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append PGP key generation + rotation + revocation + keys.openpgp.org + WKD sections to docs/RELEASING.md</name>
  <files>docs/RELEASING.md</files>
  <read_first>
    - docs/RELEASING.md (verify Task 1 sections are in place; Task 2 appends the 5 additional H2 sections after Task 1's three sections)
    - .planning/phases/24-release-tarball-signing-cosign-slsa-pgp/24-RESEARCH.md §5.1 lines 403-431 (the 5-step YubiKey ed25519 ceremony — generate via `gpg --card-edit` → `admin` → `generate`; generate revocation cert + `shred -u revoke.asc`; export to `docs/pgp/<FINGERPRINT-TBD>.asc`; publish to keys.openpgp.org; publish to WKD)
    - .planning/phases/24-release-tarball-signing-cosign-slsa-pgp/24-RESEARCH.md §5.2 lines 433-444 (the 6-step rotation procedure: 6-months-before generation; cross-sign with old key via `gpg --sign-key`; commit new .asc alongside old; update SECURITY.md `<a id="pgp-current"></a>` anchor; publish to keys.openpgp.org + WKD; first release with new key + CHANGELOG entry)
    - .planning/phases/24-release-tarball-signing-cosign-slsa-pgp/24-RESEARCH.md §5.2 lines 444 (the revocation pitfall — YubiKey loss/compromise = revocation, NOT rotation; publish offline-stored revoke.asc immediately, then rotation procedure)
    - .planning/phases/24-release-tarball-signing-cosign-slsa-pgp/24-RESEARCH.md §5.3 lines 447-491 (the WKD direct-method procedure — verify `<owner>.github.io` repo exists; compute WKD hash via `gpg-wks-client --print-wkd-hash johnturner@gmail.com`; export binary keyring via `gpg --no-armor --export`; commit to `<owner>.github.io/.well-known/openpgpkey/hu/<WKD_HASH>`; test WKD resolution; note subdomain method does NOT work on `*.github.io`)
    - .planning/phases/24-release-tarball-signing-cosign-slsa-pgp/24-CONTEXT.md §decisions D-08 (ed25519 + signing-only + 2-year + UID `blindjoin maintainer <johnturner@gmail.com>`) + D-09 (filename = full fingerprint) + D-10 (BOTH WKD + keys.openpgp.org) + D-17 (key-rotation cadence) + D-19 (WKD direct method)
    - .planning/phases/24-release-tarball-signing-cosign-slsa-pgp/24-RESEARCH.md §"Open Questions" Q1 lines 919-923 (Plan 24-05 does NOT execute keys.openpgp.org or WKD publish — maintainer-side first actions at v1.6.0-rc.0)
  </read_first>
  <action>
    Append 5 additional H2 sections to `docs/RELEASING.md` AFTER Task 1's Pre-flight check section. Each section documents one of the GnuPG procedures; do NOT execute them.

    **H2 `## PGP key generation (one-time, NOT a release-cut step)`** — 5-step procedure per RESEARCH §5.1. Each step is its own numbered paragraph with a fenced ` ```bash ` block.

    1. **Generate primary signing key directly on YubiKey.** Generation happens on-card; private key material never touches the host filesystem.
       ```bash
       gpg --card-edit
       # > admin
       # > generate
       # Select: (1) ed25519, signing-only, 2-year expiry
       # User-ID: blindjoin maintainer <johnturner@gmail.com>
       ```
       YubiKey ed25519 OpenPGP applet has been supported since YubiKey firmware 5.2.3 (Yubico docs).

    2. **Generate revocation certificate; store offline.** The revocation cert lets the maintainer invalidate the key if the YubiKey is lost/compromised. Keep on USB drive + paper backup; never on a host filesystem.
       ```bash
       gpg --output revoke.asc --gen-revoke <FINGERPRINT-TBD>
       # Move revoke.asc to USB + paper; remove from disk:
       shred -u revoke.asc 2>/dev/null || rm -P revoke.asc 2>/dev/null
       ```

    3. **Export public key to repo.**
       ```bash
       gpg --export --armor <FINGERPRINT-TBD> > docs/pgp/<FINGERPRINT-TBD>.asc
       ```
       Filename IS the full fingerprint (D-09 self-verifying convention).

    4. **Publish to keys.openpgp.org.** Cross-reference [Publishing the key to keys.openpgp.org](#publishing-the-key-to-keysopenpgporg) for the full procedure.

    5. **Publish to WKD.** Cross-reference [Publishing the key to WKD](#publishing-the-key-to-wkd) for the full procedure.

    **Operator-side verification of the public-key binding** (sub-paragraph at the end of the section): `gpg --with-colons --import-options show-only --import docs/pgp/<FINGERPRINT-TBD>.asc | head -2` returns a `fpr:` line whose 10th field (the fingerprint) equals the filename. Self-verifying; no SECURITY.md prose required to anchor the binding.

    **H2 `## PGP key rotation (every 2 years)`** — 6-step procedure per RESEARCH §5.2.

    1. **6 months before expiry**, generate a new ed25519 key on the same YubiKey (or a new YubiKey for stronger key isolation). Same `gpg --card-edit` ceremony as the initial generation.
    2. **Cross-sign the new key with the old key** while the old key is still valid. Provides a verifiable provenance chain for operators using WoT-aware paths (rare; modern keys.openpgp.org strips third-party signatures).
       ```bash
       gpg --sign-key <new-FINGERPRINT-TBD>
       ```
    3. **Commit the new public key** alongside the old one. The OLD key file STAYS in the repo as a historical record.
       ```bash
       gpg --export --armor <new-FINGERPRINT-TBD> > docs/pgp/<new-FINGERPRINT-TBD>.asc
       git add docs/pgp/<new-FINGERPRINT-TBD>.asc
       ```
    4. **Update SECURITY.md's `<a id="pgp-current"></a>` anchor** to name the new fingerprint. The old fingerprint stays in CHANGELOG.md (transparency).
    5. **Publish to keys.openpgp.org + WKD.** Wait 24h for propagation.
    6. **Cut the next release with the new key.** First release signed with the new key SHOULD be accompanied by a CHANGELOG entry naming the rotation event.

    **H2 `## PGP key revocation (emergency — YubiKey lost or compromised)`** — Per RESEARCH §5.2 pitfall: rotation ≠ revocation. If the YubiKey is lost/compromised, the maintainer publishes the offline-stored `revoke.asc` IMMEDIATELY, then runs the rotation procedure.

    1. **Recover the offline revocation cert** from USB + paper backup (generated at PGP key generation step 2).
    2. **Import the revocation cert** to invalidate the compromised key:
       ```bash
       gpg --import revoke.asc
       ```
    3. **Publish the revocation** to keys.openpgp.org + WKD so operators see the key is no longer valid:
       ```bash
       gpg --send-keys --keyserver hkps://keys.openpgp.org <FINGERPRINT-TBD>
       # Also re-run WKD publish for the now-revoked key — see Publishing the key to WKD.
       ```
    4. **Run the full PGP key generation procedure** for the new key. Skip the cross-sign step (the old key is revoked, so it cannot sign anything trustworthy).
    5. **Document the revocation event in CHANGELOG.md** with the date and a brief reason (compromise / loss / other).

    **H2 `## Publishing the key to keys.openpgp.org`** — Per RESEARCH §5.1 step 4.

    ```bash
    gpg --send-keys --keyserver hkps://keys.openpgp.org <FINGERPRINT-TBD>
    ```

    After sending, keys.openpgp.org sends an email confirmation link to the UID's mailbox (`johnturner@gmail.com`). Click the link to complete the publication. Without the email confirmation, keys.openpgp.org does NOT serve the key publicly.

    **H2 `## Publishing the key to WKD`** — 5-step direct-method procedure per RESEARCH §5.3.

    1. **Verify `<owner>.github.io` repo exists.** If not, one-time setup:
       ```bash
       gh repo create <owner>.github.io --public --description "GitHub Pages site for <owner> — hosts WKD .well-known/openpgpkey for blindjoin maintainer key"
       ```
       This is a one-time maintainer-side step at first key generation. The Phase 24 plan documents it; the actual repo creation is the maintainer's action.

    2. **Compute the WKD hash** for the UID mailbox:
       ```bash
       WKD_HASH=$(gpg-wks-client --print-wkd-hash johnturner@gmail.com | awk '{print $1}')
       ```
       The hash is `SHA-1(lowercase(local-part)) → z-base32`; using `gpg-wks-client` ensures the canonical computation.

    3. **Export the public key in WKD's binary keyring format** (NOT armored):
       ```bash
       gpg --no-armor --export johnturner@gmail.com > "${WKD_HASH}"
       ```

    4. **Commit to `<owner>.github.io`** at the direct-method path:
       ```bash
       cd path/to/<owner>.github.io
       mkdir -p .well-known/openpgpkey/hu
       mv /path/to/${WKD_HASH} .well-known/openpgpkey/hu/${WKD_HASH}
       git add .well-known/openpgpkey/hu/${WKD_HASH}
       git commit -m "wkd: publish blindjoin maintainer key for johnturner@gmail.com"
       git push
       ```

    5. **Test WKD resolution from a fresh machine:**
       ```bash
       gpg --auto-key-locate wkd --locate-keys johnturner@gmail.com
       # Should print the imported key and its fingerprint.
       ```

    **Sub-paragraph at end** documenting refresh cadence (rotation/revocation only — no daily refresh needed) and the subdomain-method incompatibility (`*.github.io` doesn't support subdomain wildcarding; direct method is the only viable path on GitHub Pages per RESEARCH §5.3).

    DO NOT execute any of the procedures documented here — Phase 24 documents only; maintainer's first execution is at v1.6.0-rc.0 cut.
    DO NOT hardcode a real fingerprint anywhere — use `<FINGERPRINT-TBD>` and `<new-FINGERPRINT-TBD>` placeholders throughout (Plan 24-05 atomic replacement).
    DO NOT add WoT-related signature procedures beyond the cross-sign step — modern keys.openpgp.org strips third-party sigs; full WoT is out of scope for this maintainer-procedure file (RESEARCH §"Deferred Ideas" Web-of-Trust line).
    DO NOT document signing-subkey-on-disk-managed-by-CI alternatives — D-06 + RESEARCH §"Deferred Ideas" reject this for the SIGN-03 rationale.
    DO NOT change the H2 section ordering — RESEARCH §5.5 locks it: Prerequisites → Per-release procedure → Pre-flight check → PGP key generation → PGP key rotation → PGP key revocation → Publishing to keys.openpgp.org → Publishing to WKD.
    DO NOT document subdomain-method WKD on `*.github.io` as viable — RESEARCH §5.3 confirms only direct method works on GitHub Pages.
  </action>
  <verify>
    <automated>test -f docs/RELEASING.md &amp;&amp; grep -q '^## PGP key generation' docs/RELEASING.md &amp;&amp; grep -q '^## PGP key rotation' docs/RELEASING.md &amp;&amp; grep -q '^## PGP key revocation' docs/RELEASING.md &amp;&amp; grep -q '^## Publishing the key to keys.openpgp.org$' docs/RELEASING.md &amp;&amp; grep -q '^## Publishing the key to WKD$' docs/RELEASING.md &amp;&amp; grep -q 'gpg --card-edit' docs/RELEASING.md &amp;&amp; grep -q 'gpg --output revoke.asc --gen-revoke' docs/RELEASING.md &amp;&amp; grep -q 'gpg --export --armor <FINGERPRINT-TBD> > docs/pgp/<FINGERPRINT-TBD>\.asc' docs/RELEASING.md &amp;&amp; grep -q 'gpg --sign-key <new-FINGERPRINT-TBD>' docs/RELEASING.md &amp;&amp; grep -q 'pgp-current' docs/RELEASING.md &amp;&amp; grep -q 'gpg --import revoke.asc' docs/RELEASING.md &amp;&amp; grep -q 'gpg --send-keys --keyserver hkps://keys.openpgp.org <FINGERPRINT-TBD>' docs/RELEASING.md &amp;&amp; grep -q 'gpg-wks-client --print-wkd-hash johnturner@gmail.com' docs/RELEASING.md &amp;&amp; grep -q '\.well-known/openpgpkey/hu/' docs/RELEASING.md &amp;&amp; grep -q 'gpg --auto-key-locate wkd --locate-keys johnturner@gmail.com' docs/RELEASING.md &amp;&amp; grep -q 'gh repo create <owner>\.github\.io' docs/RELEASING.md &amp;&amp; ! grep -qE '\b[A-F0-9]{40}\b' docs/RELEASING.md &amp;&amp; ! grep -q 'keyserver.ubuntu.com' docs/RELEASING.md &amp;&amp; ! grep -q 'subdomain method' docs/RELEASING.md || grep -q 'subdomain method.*not\|subdomain method.*does NOT' docs/RELEASING.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists and contains Task 1's three sections + Task 2's five new sections: `test -f docs/RELEASING.md && [ "$(grep -c '^## ' docs/RELEASING.md)" -ge 8 ]`
    - All 5 new H2 sections present (in addition to Task 1's three):
      - `grep -q '^## PGP key generation' docs/RELEASING.md`
      - `grep -q '^## PGP key rotation' docs/RELEASING.md`
      - `grep -q '^## PGP key revocation' docs/RELEASING.md`
      - `grep -q '^## Publishing the key to keys.openpgp.org$' docs/RELEASING.md`
      - `grep -q '^## Publishing the key to WKD$' docs/RELEASING.md`
    - PGP key generation: `gpg --card-edit` ceremony present: `grep -q 'gpg --card-edit' docs/RELEASING.md`
    - PGP key generation: revocation cert generation + shred: `grep -q 'gpg --output revoke.asc --gen-revoke' docs/RELEASING.md && grep -q 'shred -u revoke.asc' docs/RELEASING.md`
    - PGP key generation: public key export to docs/pgp/: `grep -q 'gpg --export --armor <FINGERPRINT-TBD> > docs/pgp/<FINGERPRINT-TBD>\.asc' docs/RELEASING.md`
    - PGP key rotation: cross-sign command with new fingerprint placeholder: `grep -q 'gpg --sign-key <new-FINGERPRINT-TBD>' docs/RELEASING.md`
    - PGP key rotation: SECURITY.md `<a id="pgp-current">` anchor reference (cross-reference to Plan 24-03): `grep -q 'pgp-current' docs/RELEASING.md`
    - PGP key revocation: `gpg --import revoke.asc` present: `grep -q 'gpg --import revoke.asc' docs/RELEASING.md`
    - keys.openpgp.org publish command present: `grep -q 'gpg --send-keys --keyserver hkps://keys.openpgp.org <FINGERPRINT-TBD>' docs/RELEASING.md`
    - WKD hash computation: `grep -q 'gpg-wks-client --print-wkd-hash johnturner@gmail.com' docs/RELEASING.md`
    - WKD direct-method path: `grep -q '\.well-known/openpgpkey/hu/' docs/RELEASING.md`
    - WKD resolution test command: `grep -q 'gpg --auto-key-locate wkd --locate-keys johnturner@gmail.com' docs/RELEASING.md`
    - WKD setup includes `<owner>.github.io` repo creation fallback: `grep -q 'gh repo create <owner>\.github\.io' docs/RELEASING.md`
    - No real fingerprint hex strings anywhere (only `<FINGERPRINT-TBD>` / `<new-FINGERPRINT-TBD>` placeholders): `! grep -qE '\b[A-F0-9]{40}\b' docs/RELEASING.md`
    - SKS-style keyservers NOT recommended (RESEARCH §"Deferred Ideas" — signature poisoning risk): `! grep -q 'keyserver.ubuntu.com' docs/RELEASING.md`
    - WKD subdomain-method either ABSENT or correctly noted as non-viable on `*.github.io`: `! grep -q 'subdomain method' docs/RELEASING.md || grep -q 'subdomain method.*not\|subdomain method.*does NOT' docs/RELEASING.md`
    - File length grew to ≥150 lines (RESEARCH §5.5 estimate is 200-300 lines fully populated; this task's portion + Task 1's portion ≥ 150 conservative floor): `[ "$(wc -l < docs/RELEASING.md)" -ge 150 ]`
    - H2 section ordering matches RESEARCH §5.5: `awk '/^## Prerequisites$/{a=NR} /^## Per-release procedure/{b=NR} /^## Pre-flight check/{c=NR} /^## PGP key generation/{d=NR} /^## PGP key rotation/{e=NR} /^## PGP key revocation/{f=NR} /^## Publishing the key to keys.openpgp.org$/{g=NR} /^## Publishing the key to WKD$/{h=NR} END{exit !(a<b && b<c && c<d && d<e && e<f && f<g && g<h)}' docs/RELEASING.md`
  </acceptance_criteria>
  <done>
    `docs/RELEASING.md` now has all 8 H2 sections in the locked order: Prerequisites, Per-release procedure (5 steps), Pre-flight check before flipping out of draft, PGP key generation (one-time, NOT a release-cut step), PGP key rotation (every 2 years), PGP key revocation (emergency — YubiKey lost or compromised), Publishing the key to keys.openpgp.org, Publishing the key to WKD. The PGP key generation section documents the 5-step YubiKey ed25519 ceremony with `gpg --card-edit` + revocation cert + `shred -u`. The rotation section has the 6-step procedure with `gpg --sign-key` cross-signing + SECURITY.md `<a id="pgp-current"></a>` anchor reference. The revocation section publishes the offline-stored revoke.asc + rotation. The keys.openpgp.org section is a 1-command publish + email confirmation note. The WKD section is the 5-step direct-method procedure with `gpg-wks-client --print-wkd-hash` + `<owner>.github.io/.well-known/openpgpkey/hu/<WKD_HASH>` + repo-creation fallback. No real fingerprint hex strings; only `<FINGERPRINT-TBD>` and `<new-FINGERPRINT-TBD>` placeholders (Plan 24-05 atomic replacement contract). No SKS-keyserver recommendations; no subdomain-method WKD claims for `*.github.io`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer's eyes → docs/RELEASING.md | The maintainer reads + executes the documented procedures. A wrong command (typo, nonexistent flag, drifted GnuPG behavior) propagates to every release. The procedures are GnuPG-canonical and verified against the standard manual; the operator-side WKD/keys.openpgp.org commands match the current `gpg 2.4+` shape. |
| `<FINGERPRINT-TBD>` placeholder → maintainer's actual fingerprint | Plan 24-05 atomically replaces every `<FINGERPRINT-TBD>` occurrence with the maintainer's actual 40-char hex fingerprint after key generation. A missed occurrence would leave a placeholder string in a published doc — visible failure, but bad UX. The atomicity contract is one commit containing both `.asc` file commit and ALL placeholder substitutions in docs/RELEASING.md + SECURITY.md. |
| Maintainer's host machine → YubiKey | The procedures repeatedly cross this boundary (gpg → smartcard daemon → YubiKey OpenPGP applet). Private key material never crosses back. The 5-step generation ceremony establishes the boundary; the per-release step 4 uses it; rotation + revocation procedures honor it. |
| Maintainer's host machine → keys.openpgp.org | `gpg --send-keys --keyserver hkps://keys.openpgp.org` crosses this boundary. The email confirmation gate at the keys.openpgp.org side is what prevents random key uploads from publishing publicly. |
| Maintainer's host machine → `<owner>.github.io` repo | The WKD direct-method procedure crosses this boundary (writing `.well-known/openpgpkey/hu/<WKD_HASH>`). Compromise of the `<owner>.github.io` repo allows attackers to substitute the published key. Mitigation: the public key in this blindjoin repo (`docs/pgp/<fp>.asc`) is the canonical trust anchor; WKD is operator UX, not the trust root. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-11 | Tampering | A typo in the documented per-release step 4 (`gpg --detach-sign --armor --local-user <FINGERPRINT-TBD>`) produces a signature for the wrong artifact | mitigate | The procedure runs in `/tmp/blindjoin-release` (a fresh, isolated directory) and signs exactly one file (`blindjoin-linux-amd64.tar.gz`). The pre-flight check section requires cosign verify before the draft flip — a missing or wrong `.asc` is caught at the operator's first verification attempt. |
| T-24-12 | Spoofing | Maintainer follows the documented WKD procedure but the WKD hash gets computed against a stale lowercase variant | accept | RESEARCH §5.3 specifies `gpg-wks-client --print-wkd-hash johnturner@gmail.com` as the canonical hash computation. GnuPG handles the lowercase + z-base32 conversion internally; manual computation is not documented. |
| T-24-13 | Spoofing | An attacker substitutes a different public-key file in `<owner>.github.io/.well-known/openpgpkey/hu/<WKD_HASH>` | mitigate | The canonical trust anchor for the maintainer's key is `docs/pgp/<FINGERPRINT-TBD>.asc` in THIS repo (committed via Plan 24-05). WKD is operator UX; if a WKD substitution attack happens, operators verifying against the in-repo file see the discrepancy. SECURITY.md (Plan 24-03) names the canonical fingerprint at the `<a id="pgp-current"></a>` anchor — independent of WKD. |
| T-24-14 | Repudiation | Maintainer loses access to the YubiKey + has no offline revocation cert | accept | The PGP key generation section (step 2) explicitly documents revocation cert generation + offline storage + `shred -u`. If the maintainer skips this step, they accept the residual risk of permanent loss of revocation capability. The documentation is the mitigation; execution is the maintainer's responsibility. |
| T-24-15 | Tampering | `<FINGERPRINT-TBD>` placeholder is left unreplaced in a published doc | mitigate | Plan 24-05's `checkpoint:human-verify` task lists every `<FINGERPRINT-TBD>` occurrence and replaces them atomically in one commit. Acceptance criteria for Plan 24-05 includes `! grep -q '<FINGERPRINT-TBD>' docs/RELEASING.md` post-replacement. |
| T-24-16 | Denial of Service | keys.openpgp.org email confirmation never received by `johnturner@gmail.com` | accept | keys.openpgp.org is a public good; DOS at the publish layer is unlikely. WKD on `<owner>.github.io` is the redundant publication path (D-10). If keys.openpgp.org is unreachable, WKD is sufficient. |
| T-24-17 | Information Disclosure | The maintainer's UID (`blindjoin maintainer <johnturner@gmail.com>`) is published with the key | accept | The UID is intentionally project-scoped (D-08 — distinct from any personal-key UID). The email mailbox is already in CONTRIBUTING.md / SECURITY.md cross-reference targets. No additional PII surface. |
| T-24-18 | Spoofing | An attacker reads docs/RELEASING.md, fakes a release, and posts it to a parallel website | mitigate | The release pipeline is gated by `release.yml`'s tag-gate AND OIDC subject claim. A faked release on a parallel website cannot have a valid cosign signature against the maintainer's identity-regexp (Plan 24-03 documents). The PGP path's trust anchor is the `<FINGERPRINT-TBD>` published canonically in this repo — an attacker without the maintainer's YubiKey cannot produce a `.asc` that verifies against that fingerprint. |
| T-24-SC | Tampering | npm/pip/cargo installs | n/a | This plan creates ONE markdown file. No code, no package installs, no CI workflow changes. The documented external dependencies (`gpg`, `gh`, `cosign`, `gpg-wks-client`) are operator-side installs the maintainer performs at v1.6.0-rc.0 cut — out of CI scope. |
</threat_model>

<verification>
After Tasks 1-2 complete:

```bash
# 1. File exists at canonical path.
test -f docs/RELEASING.md

# 2. H1 + audience lede.
grep -q '^# Releasing blindjoin$' docs/RELEASING.md
grep -q "Contributors don't need this" docs/RELEASING.md
grep -q '\[CONTRIBUTING.md\](\.\./CONTRIBUTING.md)' docs/RELEASING.md

# 3. All 8 H2 sections in fixed order.
awk '/^## Prerequisites$/{a=NR} /^## Per-release procedure/{b=NR} /^## Pre-flight check/{c=NR} /^## PGP key generation/{d=NR} /^## PGP key rotation/{e=NR} /^## PGP key revocation/{f=NR} /^## Publishing the key to keys.openpgp.org$/{g=NR} /^## Publishing the key to WKD$/{h=NR} END{exit !(a<b && b<c && c<d && d<e && e<f && f<g && g<h)}' docs/RELEASING.md

# 4. Per-release literal commands.
grep -q 'git tag -s vX\.Y\.Z' docs/RELEASING.md
grep -q 'gh release download vX\.Y\.Z' docs/RELEASING.md
grep -q 'gpg --detach-sign --armor --local-user <FINGERPRINT-TBD>' docs/RELEASING.md
grep -q 'gh release upload vX\.Y\.Z blindjoin-linux-amd64\.tar\.gz\.asc' docs/RELEASING.md
grep -q 'gh release edit vX\.Y\.Z --draft=false' docs/RELEASING.md

# 5. Pre-flight check uses cosign verify-blob.
grep -q 'cosign verify-blob' docs/RELEASING.md

# 6. PGP generation: card-edit + revocation cert + export.
grep -q 'gpg --card-edit' docs/RELEASING.md
grep -q 'gpg --output revoke.asc --gen-revoke' docs/RELEASING.md
grep -q 'shred -u revoke.asc' docs/RELEASING.md
grep -q 'gpg --export --armor <FINGERPRINT-TBD>' docs/RELEASING.md

# 7. Rotation: cross-sign + SECURITY.md anchor + new fingerprint placeholder.
grep -q 'gpg --sign-key <new-FINGERPRINT-TBD>' docs/RELEASING.md
grep -q 'pgp-current' docs/RELEASING.md

# 8. Revocation: gpg --import revoke.asc.
grep -q 'gpg --import revoke.asc' docs/RELEASING.md

# 9. keys.openpgp.org publish.
grep -q 'gpg --send-keys --keyserver hkps://keys.openpgp.org <FINGERPRINT-TBD>' docs/RELEASING.md

# 10. WKD direct method.
grep -q 'gpg-wks-client --print-wkd-hash johnturner@gmail.com' docs/RELEASING.md
grep -q '\.well-known/openpgpkey/hu/' docs/RELEASING.md
grep -q 'gpg --auto-key-locate wkd --locate-keys johnturner@gmail.com' docs/RELEASING.md
grep -q 'gh repo create <owner>\.github\.io' docs/RELEASING.md

# 11. No real fingerprint hex strings; only placeholders.
! grep -qE '\b[A-F0-9]{40}\b' docs/RELEASING.md

# 12. No SKS-style keyserver recommendations.
! grep -q 'keyserver.ubuntu.com' docs/RELEASING.md

# 13. Plan 24-05 atomic replacement contract: at least one <FINGERPRINT-TBD> exists.
[ "$(grep -c '<FINGERPRINT-TBD>' docs/RELEASING.md)" -ge 5 ]

# 14. End-to-end procedure rehearsal — deferred to first v1.6.0-rc.0 cut (maintainer's first execution).
```
</verification>

<success_criteria>
- `docs/RELEASING.md` exists at the canonical path with UPPERCASE filename matching `AUDIT-CHARTER.md` + `PROTOCOL.md` precedent.
- File has H1 `# Releasing blindjoin` + audience-disambiguation lede + 8 H2 sections in the locked RESEARCH §5.5 ordering: Prerequisites, Per-release procedure (5 steps), Pre-flight check before flipping out of draft, PGP key generation (one-time, NOT a release-cut step), PGP key rotation (every 2 years), PGP key revocation (emergency), Publishing the key to keys.openpgp.org, Publishing the key to WKD.
- Per-release procedure has 5 numbered steps with literal commands: `git tag -s vX.Y.Z` → wait for CI → `gh release download` → `gpg --detach-sign --armor --local-user <FINGERPRINT-TBD>` → `gh release upload .asc` + `gh release edit --draft=false`.
- Pre-flight check documents `cosign verify-blob` against the freshly-published draft Release BEFORE the draft flip; `gh release delete` recovery path documented.
- PGP key generation has 5 steps including `gpg --card-edit` YubiKey ceremony, revocation cert generation + offline storage + `shred -u`, public key export to `docs/pgp/<FINGERPRINT-TBD>.asc`, cross-references to keys.openpgp.org + WKD publication sections.
- PGP key rotation has 6 steps including cross-sign-with-old-key (`gpg --sign-key`), new public key commit alongside (not replacing) the old `.asc`, SECURITY.md `<a id="pgp-current"></a>` anchor update, CHANGELOG entry on rotation event.
- PGP key revocation documents the emergency procedure (publish offline `revoke.asc` first, then rotation).
- WKD section is the direct-method 5-step procedure (verify `<owner>.github.io` repo + `gpg-wks-client --print-wkd-hash` + binary keyring export + `.well-known/openpgpkey/hu/<WKD_HASH>` commit + WKD resolution test). Subdomain method explicitly noted as non-viable on `*.github.io`.
- All fingerprint occurrences use the literal `<FINGERPRINT-TBD>` placeholder string (Plan 24-05 atomic replacement contract); no real 40-char hex strings anywhere.
- No SKS-style keyserver recommendations (`keyserver.ubuntu.com` etc — RESEARCH §"Deferred Ideas" rejection).
- File length ≥ 150 lines (RESEARCH §5.5 estimate is 200-300 lines fully populated).
- Maps to ROADMAP Phase 24 Success Criterion #3 (`docs/pgp/<fingerprint>.asc` + keys.openpgp.org upload + signing key fingerprint documented; this file documents the PROCEDURES surrounding those artifacts, not the artifacts themselves — those live in Plan 24-05).
- End-to-end procedure rehearsal is the maintainer's first execution at v1.6.0-rc.0 cut (RESEARCH §"Open Questions" Q1).
</success_criteria>

<output>
Create `.planning/phases/24-release-tarball-signing-cosign-slsa-pgp/24-02-SUMMARY.md` when done. Include: (a) line-number ranges of each H2 section in the final docs/RELEASING.md, (b) total `<FINGERPRINT-TBD>` occurrence count (Plan 24-05 reads this number to verify atomic replacement covers all of them), (c) list of cross-references this file makes (`../CONTRIBUTING.md`, `../.github/workflows/release.yml`, `docs/pgp/<FINGERPRINT-TBD>.asc`, `SECURITY.md#pgp-current`) and confirmation each link target either exists in-tree or is created by Plans 24-01 / 24-03 / 24-05, (d) confirmation the file has no real 40-char hex fingerprint strings (only placeholders), (e) cross-reference to Plan 24-04 (CONTRIBUTING.md cross-ref insertion) and Plan 24-05 (atomic placeholder replacement at v1.6.0-rc.0 cut).
</output>
</content>
