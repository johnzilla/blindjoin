---
phase: 24-release-tarball-signing-cosign-slsa-pgp
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - SECURITY.md
autonomous: true
requirements:
  - SIGN-01
  - SIGN-02
  - SIGN-03
requirements_addressed:
  - SIGN-01
  - SIGN-02
  - SIGN-03

must_haves:
  truths:
    - "`SECURITY.md` § Supply-chain status grows a new `### Release tarball signatures + provenance (v1.6 onward)` H3 subsection appended BELOW the existing `### Image signatures + attestations (v1.6 onward)` subsection (which is the Phase 23 D-05 skeleton at lines 118-185) and ABOVE the existing `### Base-image digests (v1.6 onward)` Phase 22 subsection at line 187 (additive — Phase 23's image subsection stays byte-identical; Phase 22's base-image-digests subsection stays byte-identical)."
    - "The new subsection's opening prose names what's signed/attested as a 3-item numbered list with bold-lede claims (mirroring Phase 23's 3-claim list): (1) **Signed by cosign** via OIDC keyless flow producing `blindjoin-linux-amd64.tar.gz.bundle`; (2) **Attested with a SLSA v1.0 in-toto provenance bundle** (predicate type `https://slsa.dev/provenance/v1`), naming the workflow file (`release.yml`), tag ref, source commit, and runner image; pushed to GitHub Attestations API AND distributed as `blindjoin-linux-amd64.tar.gz.sigstore`; (3) **Detached PGP signature** (`blindjoin-linux-amd64.tar.gz.asc`) from the maintainer's YubiKey-held ed25519 key, uploaded post-CI as the SIGN-03 non-OIDC alternative trust path."
    - "An explicit standalone paragraph between the 3-claim list and the prerequisites paragraph reads: 'EITHER cosign OR PGP verification is sufficient — they are alternative paths, not both required.' (D-12 explicit lock — Phase 23 has no equivalent because there is no PGP alternative)."
    - "A prerequisites paragraph names `cosign 2.6.3 or compatible` (with 1-line cross-ref to the image subsection's version pin), `gh 2.50 or later` (D-16 — verified at gh 2.92.0), and `gpg 2.4 or later` (RESEARCH §4.3)."
    - "A fenced ` ```bash ` recipes block contains exactly FOUR numbered recipes in this order: (1) `cosign verify-blob --bundle blindjoin-linux-amd64.tar.gz.bundle --certificate-identity-regexp 'https://github.com/<owner>/blindjoin/\\.github/workflows/release\\.yml@refs/tags/v.*' --certificate-oidc-issuer 'https://token.actions.githubusercontent.com' blindjoin-linux-amd64.tar.gz` (SIGN-01); (2) `gh attestation verify blindjoin-linux-amd64.tar.gz --repo <owner>/blindjoin` (SLSA Path A — Plan-author confirms `<owner>` placeholder); (3) `cosign verify-attestation --bundle blindjoin-linux-amd64.tar.gz.sigstore --type slsaprovenance --certificate-identity-regexp '...' --certificate-oidc-issuer '...' blindjoin-linux-amd64.tar.gz` (SLSA Path B — offline after one-time TUF seeding); (4) `gpg --auto-key-locate wkd --locate-keys johnturner@gmail.com` then `gpg --verify blindjoin-linux-amd64.tar.gz.asc blindjoin-linux-amd64.tar.gz` (SIGN-03 — PGP path) WITH an inline `# IMPORTANT:` comment naming the operator's fingerprint-comparison gate per Pitfall 24-B."
    - "The cosign verify-blob + verify-attestation recipes use the workflow-file-anchored identity regex `release\\.yml` (NOT `docker\\.yml` like the image subsection) — verbatim Pitfall 1 narrow form scoped to Phase 24's workflow file."
    - "A single `> Note: cosign 3.0 CLI flag drift` callout below the recipes is a 1-LINE cross-ref to the image subsection's version-pin callout per D-12 (NOT a duplication — Phase 23's section is the canonical version-pin source)."
    - "An HTML anchor `<a id=\"pgp-current\"></a>` followed by a paragraph naming the current maintainer fingerprint, UID `blindjoin maintainer <johnturner@gmail.com>`, algorithm (ed25519), generation date, expiry date is appended at the END of the new subsection. The fingerprint placeholder is `<FINGERPRINT-TBD>` and the dates are `YYYY-MM-DD`/`YYYY-MM-DD`; Plan 24-05 atomically replaces all three placeholders when the maintainer generates the key (D-09 + D-12 + RESEARCH §4.6)."
    - "The committed public-key file reference inside the fingerprint anchor paragraph uses the relative link `[`docs/pgp/<FINGERPRINT-TBD>.asc`](docs/pgp/)` — folder-level link because the file does not exist until Plan 24-05 commits it."
    - "Pitfall 24-B (gpg --verify exits 0 without trust) is mitigated by the IMPORTANT comment inline in recipe 4 plus a `> Note:` callout below recipes stating that operators MUST compare the fingerprint printed by `gpg --verify` (on stderr) against the canonical fingerprint at the `#pgp-current` anchor; the scripted form `gpg --status-fd=1 --verify ... | grep VALIDSIG | grep <FINGERPRINT-TBD>` is suggested as an automation recipe."
    - "The v1.5 'GitHub Release archives ship a SHA-256 checksum but NO PGP / minisign signature' bullet at SECURITY.md:104-109 is strikethrough'd with cross-link to the new subsection — mirroring Phase 23's strikethrough closure of the Docker-unsigned bullet at line 110. Form: `- **~~GitHub Release archives ship a SHA-256 checksum but NO PGP / minisign signature.~~** **Closed in v1.6 Phase 24** — see [Release tarball signatures + provenance (v1.6 onward)](#release-tarball-signatures--provenance-v16-onward).` (single physical line — Phase 22 Plan 22-05 lesson)."
    - "All existing SECURITY.md sections, anchors, and structural elements stay untouched except for (a) the Release-archives bullet strikethrough and (b) the new subsection insertion. The Phase 23 `### Image signatures + attestations (v1.6 onward)` subsection at lines 118-185, the Phase 22 `### Base-image digests (v1.6 onward)` subsection at lines 187+, the `### v1.6 supply-chain plan` subsection, the `## Release versioning policy` H2, the `## Where to find more` H2 — all preserved byte-identically."
  artifacts:
    - path: "SECURITY.md"
      provides: "New ### Release tarball signatures + provenance (v1.6 onward) H3 subsection"
      contains: "Release tarball signatures + provenance (v1.6 onward)"
    - path: "SECURITY.md"
      provides: "EITHER-OR explicit prose (D-12 lock — SIGN-03 non-OIDC alternative path rationale)"
      contains: "EITHER cosign OR PGP verification is sufficient"
    - path: "SECURITY.md"
      provides: "Recipe 1 — cosign verify-blob with release.yml identity-regexp (SIGN-01)"
      contains: "cosign verify-blob --bundle blindjoin-linux-amd64.tar.gz.bundle"
    - path: "SECURITY.md"
      provides: "Recipe 1 identity-regexp targets release.yml (Pitfall 1 narrow form for Phase 24)"
      contains: "release\\.yml@refs/tags/v"
    - path: "SECURITY.md"
      provides: "Recipe 2 — gh attestation verify SLSA Path A (D-16)"
      contains: "gh attestation verify blindjoin-linux-amd64.tar.gz"
    - path: "SECURITY.md"
      provides: "Recipe 3 — cosign verify-attestation SLSA Path B (offline)"
      contains: "cosign verify-attestation"
    - path: "SECURITY.md"
      provides: "Recipe 3 uses --type slsaprovenance (cosign 2.6.3 form)"
      contains: "--type slsaprovenance"
    - path: "SECURITY.md"
      provides: "Recipe 4 — gpg --verify with WKD key fetch (SIGN-03)"
      contains: "gpg --verify blindjoin-linux-amd64.tar.gz.asc"
    - path: "SECURITY.md"
      provides: "Recipe 4 WKD key fetch via johnturner@gmail.com"
      contains: "gpg --auto-key-locate wkd --locate-keys johnturner@gmail.com"
    - path: "SECURITY.md"
      provides: "Pitfall 24-B fingerprint-comparison mitigation in recipe 4 inline comment"
      contains: "Primary key fingerprint"
    - path: "SECURITY.md"
      provides: "HTML anchor for current maintainer PGP fingerprint (D-09 + D-12 + RESEARCH §4.6)"
      contains: "<a id=\"pgp-current\"></a>"
    - path: "SECURITY.md"
      provides: "Current maintainer fingerprint paragraph with placeholder for Plan 24-05 replacement"
      contains: "<FINGERPRINT-TBD>"
    - path: "SECURITY.md"
      provides: "Reference to docs/pgp/ public-key file location"
      contains: "docs/pgp/"
    - path: "SECURITY.md"
      provides: "1-line cosign 3.0 cross-ref to image subsection (D-12 — no duplication)"
      contains: "cosign 3.0"
    - path: "SECURITY.md"
      provides: "v1.5 Release-archives-unsigned bullet strikethrough'd with cross-link"
      contains: "~~GitHub Release archives ship a SHA-256 checksum but NO PGP / minisign signature.~~"
  key_links:
    - from: "SECURITY.md ### Release tarball signatures + provenance (v1.6 onward)"
      to: ".github/workflows/release.yml"
      via: "markdown identity-regex naming the workflow file"
      pattern: "release\\.yml"
    - from: "SECURITY.md ### Known gaps at v1.5 (strikethrough Release-archives bullet)"
      to: "SECURITY.md #release-tarball-signatures--provenance-v16-onward"
      via: "markdown anchor cross-link"
      pattern: "#release-tarball-signatures--provenance-v16-onward"
    - from: "SECURITY.md <a id=\"pgp-current\"></a> anchor"
      to: "docs/pgp/<FINGERPRINT-TBD>.asc"
      via: "relative folder link"
      pattern: "docs/pgp/"
    - from: "SECURITY.md PGP recipe 4 fingerprint-comparison prose"
      to: "Plan 24-05 atomic fingerprint substitution"
      via: "<FINGERPRINT-TBD> placeholder string"
      pattern: "<FINGERPRINT-TBD>"
---

<objective>
Append a new `### Release tarball signatures + provenance (v1.6 onward)` H3 subsection to `SECURITY.md` § Supply-chain status, between the existing Phase 23 `### Image signatures + attestations (v1.6 onward)` subsection (lines 118-185) and the existing Phase 22 `### Base-image digests (v1.6 onward)` subsection (line 187+). The new subsection mirrors Phase 23's structural skeleton (D-12): opening prose + 3-item numbered claim list + EITHER-OR alternative-paths paragraph + prerequisites paragraph + fenced bash recipes block with 4 recipes + Pitfall 13 cross-ref callout + Pitfall 24-B fingerprint-trust callout + `<a id="pgp-current"></a>` anchor naming the current maintainer fingerprint as `<FINGERPRINT-TBD>` (Plan 24-05 atomic replacement). Additionally, strikethrough the v1.5 Release-archives-unsigned bullet at SECURITY.md:104-109 with a cross-link to the new subsection — mirroring Phase 23's closure of the Docker-unsigned bullet at line 110.

Purpose: SECURITY.md is the operator-facing trust boundary for Phase 24. Without this subsection, an operator pulling `blindjoin-linux-amd64.tar.gz` from a GitHub Release has no documented path to verify the cosign signature, the SLSA provenance attestation, or the PGP signature — even though Plan 24-01 produces the .bundle + .sigstore and Plan 24-05 produces the .asc. The recipes block is the LOAD-BEARING piece of ROADMAP Phase 24 SC#1 (cosign verify-blob with release.yml identity-regexp), SC#2 (cosign verify-attestation + gh attestation verify), SC#3 (gpg --verify with maintainer fingerprint documented in SECURITY.md), and SC#4 (BOTH verification recipes side by side with explicit EITHER-OR prose). The strikethrough closure of the v1.5 Release-archives-unsigned bullet is the symmetric counterpart to Phase 23's Docker-unsigned strikethrough — both v1.5 gaps closed in v1.6.

Output: One modified file: `SECURITY.md` (~80-100 new lines: subsection + 4-recipe fenced block + 2 callouts + fingerprint anchor; ~2 lines modified: the strikethrough on the v1.5 Release-archives bullet).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-release-tarball-signing-cosign-slsa-pgp/24-CONTEXT.md
@.planning/phases/24-release-tarball-signing-cosign-slsa-pgp/24-RESEARCH.md
@.planning/phases/24-release-tarball-signing-cosign-slsa-pgp/24-PATTERNS.md
@.planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-04-PLAN.md
@.planning/research/PITFALLS.md
@SECURITY.md

<interfaces>
<!-- This plan modifies operator-facing documentation only. -->
<!-- No functional dependency on Plans 24-01/02/04 — Wave 1 parallel-safe. -->

<!-- The recipes documented here will be empirically validated at the first v1.6.0-rc.0 tag push -->
<!-- (the Phase 23 closure pattern — no HUMAN-UAT scaffold plan in Phase 24 per CONTEXT.md domain). -->
<!-- If a recipe fails empirical validation, the fix is a quick task amending this SECURITY.md -->
<!-- subsection BEFORE the production v1.6.0 tag. -->

<!-- Locked identity regex (CONTEXT D-12 + RESEARCH §4.1 + Pitfall 1): -->
<!--   'https://github.com/<owner>/blindjoin/\.github/workflows/release\.yml@refs/tags/v.*' -->
<!--   - release.yml (Phase 24's workflow; NOT docker.yml — that is Phase 23's image subsection) -->
<!--   - refs/tags/v.* (spans rc.0, v1.6.0, v1.6.1, v1.7.0, ... by design) -->
<!--   - <owner> kept as a placeholder for operator-side substitution (matches Phase 23 image -->
<!--     subsection's <owner> placeholder convention) — NOTE: the project's canonical owner is -->
<!--     `johnzilla` per the Phase 23 SECURITY.md image subsection's hardcoded regex. -->

<!-- Locked recipes (RESEARCH §4.1, §4.2, §4.3 — VERIFIED against cosign 2.6.3 + gh 2.92.0): -->
<!--   1. cosign verify-blob --bundle ... (SIGN-01) -->
<!--   2. gh attestation verify <file> --repo <owner>/blindjoin (SLSA Path A — gh primary form per D-16) -->
<!--   3. cosign verify-attestation --bundle ...sigstore --type slsaprovenance (SLSA Path B — offline) -->
<!--   4. gpg --auto-key-locate wkd + gpg --verify ...asc ...tar.gz (SIGN-03) -->

<!-- Operator-side cosign version pin (D-12 — 1-line cross-ref, NOT duplication): -->
<!--   "See the image subsection above for the cosign version pin range; the same constraints -->
<!--    apply to tarball verification." -->
<!--   The image subsection at SECURITY.md:179-185 names `>= 2.6.3, < 3.0.0` per Phase 23 D-08 -->
<!--   plus the Pitfall 13 callout. Phase 24 does not duplicate. -->

<!-- Pitfall 24-B mitigation prose (RESEARCH §"Common Pitfalls" + RESEARCH §4.3): -->
<!--   gpg --verify exits 0 even without operator trust web. Operators MUST compare the -->
<!--   "Primary key fingerprint:" line printed by gpg --verify (on stderr) against the canonical -->
<!--   fingerprint at #pgp-current. The recipe 4 inline comment + the > Note: callout below -->
<!--   carry this message. -->

<!-- Fingerprint anchor (RESEARCH §4.6 + D-09 + D-12): -->
<!--   <a id="pgp-current"></a> -->
<!--   **Current maintainer PGP fingerprint:** `<FINGERPRINT-TBD>` -->
<!--     (UID `blindjoin maintainer <johnturner@gmail.com>`, ed25519, -->
<!--      generated YYYY-MM-DD, expires YYYY-MM-DD). -->
<!--   The committed public key lives at [`docs/pgp/<FINGERPRINT-TBD>.asc`](docs/pgp/) -->
<!--     and is published to keys.openpgp.org + WKD on `<owner>.github.io`. -->

<!-- Plan 24-05 atomic-replacement contract: -->
<!--   Every `<FINGERPRINT-TBD>` literal occurrence in this file + docs/RELEASING.md -->
<!--   gets replaced with the maintainer's actual 40-char hex fingerprint in ONE atomic commit -->
<!--   when the maintainer generates the key at v1.6.0-rc.0 cut. -->

<!-- Existing SECURITY.md structural anchors to preserve (PATTERNS §7 + SECURITY.md current state): -->
<!--   - ## Supply-chain status (the parent H2 — keep) -->
<!--   - ### Known gaps at v1.5 (modify: strikethrough the Release-archives bullet) -->
<!--   - ### Image signatures + attestations (v1.6 onward) (Phase 23 — DO NOT TOUCH) -->
<!--   - ### Base-image digests (v1.6 onward) (Phase 22 — DO NOT TOUCH) -->
<!--   - ### v1.6 supply-chain plan (existing — keep) -->
<!-- NEW subsection inserted BETWEEN ### Image signatures and ### Base-image digests: -->
<!--   - ### Release tarball signatures + provenance (v1.6 onward) -->

<!-- GitHub anchor auto-generation (for the strikethrough cross-link): -->
<!--   ### Release tarball signatures + provenance (v1.6 onward) -->
<!--     → #release-tarball-signatures--provenance-v16-onward -->
<!--   Rule: lowercase, spaces→hyphens, drop `+` resulting in two hyphens around it. -->
<!--   Verified pattern against Phase 23's `#image-signatures--attestations-v16-onward` anchor. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert `### Release tarball signatures + provenance (v1.6 onward)` subsection between Phase 23 + Phase 22 subsections; strikethrough v1.5 Release-archives bullet with cross-link</name>
  <files>SECURITY.md</files>
  <read_first>
    - SECURITY.md (full file — focus on `## Supply-chain status` parent H2 at line 95, `### Known gaps at v1.5` bullet list at lines 102-116 with the v1.5 Release-archives bullet at lines 104-109 needing strikethrough, the existing `### Image signatures + attestations (v1.6 onward)` Phase 23 subsection at lines 118-185 which STAYS untouched, the existing `### Base-image digests (v1.6 onward)` Phase 22 subsection at line 187+ which STAYS untouched. Note: Phase 23 already strikethrough'd the Docker-unsigned bullet at line 110 — Phase 24 mirrors that for the Release-archives bullet at lines 104-109.)
    - .planning/phases/24-release-tarball-signing-cosign-slsa-pgp/24-RESEARCH.md §4 lines 303-396 (the FULL recipes block + EITHER-OR prose + Pitfall callouts + fingerprint anchor — copy the markdown source VERBATIM where indicated)
    - .planning/phases/24-release-tarball-signing-cosign-slsa-pgp/24-RESEARCH.md §4.1 lines 305-323 (cosign verify-blob recipe shape — Pitfall 1 narrow regex form; release.yml NOT docker.yml)
    - .planning/phases/24-release-tarball-signing-cosign-slsa-pgp/24-RESEARCH.md §4.2 lines 325-354 (gh attestation verify --repo recommended form per D-16 + cosign verify-attestation --bundle --type slsaprovenance recipe shape; cosign 2.6.3 verified)
    - .planning/phases/24-release-tarball-signing-cosign-slsa-pgp/24-RESEARCH.md §4.3 lines 356-376 (gpg --auto-key-locate wkd + gpg --verify recipe; Pitfall 24-B fingerprint comparison prose; ci.yml:99-115 VALIDSIG-grep pattern cross-reference for scripted operators)
    - .planning/phases/24-release-tarball-signing-cosign-slsa-pgp/24-RESEARCH.md §4.4 lines 378-380 (EITHER-OR prose lock — D-12 explicit)
    - .planning/phases/24-release-tarball-signing-cosign-slsa-pgp/24-RESEARCH.md §4.5 lines 382-384 (Pitfall 13 cosign-3.0 1-line cross-ref to image subsection — NOT duplication)
    - .planning/phases/24-release-tarball-signing-cosign-slsa-pgp/24-RESEARCH.md §4.6 lines 386-395 (the `<a id="pgp-current"></a>` anchor + fingerprint paragraph with `<FINGERPRINT-TBD>` placeholder + dates placeholders)
    - .planning/phases/24-release-tarball-signing-cosign-slsa-pgp/24-PATTERNS.md §7 lines 406-528 (the structural mirror of Phase 23's subsection — H3 heading + opening prose + 3-claim list + prerequisites paragraph + fenced bash recipes + > Note: callouts; key differences from Phase 23 enumerated)
    - .planning/phases/24-release-tarball-signing-cosign-slsa-pgp/24-CONTEXT.md §decisions D-12 lines 50 (the full additive skeleton + EITHER-OR + fingerprint anchor cross-callout)
    - .planning/research/PITFALLS.md §Pitfall 1 (--certificate-identity-regexp vs --certificate-identity — narrow form mandatory)
    - .planning/research/PITFALLS.md §Pitfall 13 (cosign 3.0 CLI flag drift — cross-ref to image subsection)
    - .planning/phases/23-cosign-image-attestations-slsa-provenance-sbom/23-04-PLAN.md (Phase 23's SECURITY.md plan — Phase 24 mirrors its structure; the strikethrough form `~~Docker images on \`ghcr.io\` are unsigned.~~` + cross-link `[Image signatures + attestations (v1.6 onward)](#image-signatures--attestations-v16-onward)` is the analog for Phase 24's `~~GitHub Release archives ship a SHA-256 checksum but NO PGP / minisign signature.~~` + `[Release tarball signatures + provenance (v1.6 onward)](#release-tarball-signatures--provenance-v16-onward)`)
    - STATE.md §Recent Plan Decisions line 116 (Phase 22 Plan 22-05 lesson: single-line literal so file-level greps work — the strikethrough MUST be on one physical line)
  </read_first>
  <action>
    Make TWO coordinated edits to `SECURITY.md`.

    **Edit 1 — Insert the new `### Release tarball signatures + provenance (v1.6 onward)` subsection BELOW the existing `### Image signatures + attestations (v1.6 onward)` subsection (ends at line 185) and ABOVE the existing `### Base-image digests (v1.6 onward)` subsection (starts line 187).** Per D-12 + PATTERNS §7 + RESEARCH §4, the new subsection has this structure:

    - H3 heading: `### Release tarball signatures + provenance (v1.6 onward)` (single line; mirrors Phase 23's H3 anchor convention; auto-generated anchor is `#release-tarball-signatures--provenance-v16-onward`).
    - Opening paragraph: "Every `blindjoin-linux-amd64.tar.gz` Release archive published from a `vX.Y.Z` tag is:" followed by a numbered list of three items with bold-lede claims:
      1. **Signed by cosign** via OIDC keyless flow (no maintainer key custody). The signature is distributed as `blindjoin-linux-amd64.tar.gz.bundle` — a single JSON file containing the signature, Fulcio-issued cert, and Rekor transparency-log inclusion proof.
      2. **Attested with a SLSA v1.0 in-toto provenance bundle** (predicate type `https://slsa.dev/provenance/v1`), naming the workflow file (`release.yml`) + tag ref + source commit + runner image. The attestation is pushed to the GitHub Attestations API AND distributed as `blindjoin-linux-amd64.tar.gz.sigstore`.
      3. **Detached PGP signature** (`blindjoin-linux-amd64.tar.gz.asc`) from the maintainer's YubiKey-held ed25519 key, uploaded post-CI as an alternative trust path for operators who cannot reach Sigstore Fulcio/Rekor.

    - EITHER-OR prose paragraph (D-12 explicit lock): `EITHER cosign OR PGP verification is sufficient — they are alternative paths, not both required.`

    - Prerequisites paragraph: "Verification requires **cosign 2.6.3 or compatible** (see the image subsection above for the cosign version pin rationale), the **GitHub CLI (`gh`) 2.50 or later**, and **gpg 2.4 or later**. The verify recipes below have been tested on a clean `ubuntu:24.04` container."

    - Fenced ` ```bash ` block with FOUR numbered recipes (RESEARCH §4.1, §4.2, §4.3 verbatim):
      - Recipe 1 — SIGN-01 cosign blob signature verification:
        - Command: `cosign verify-blob --bundle blindjoin-linux-amd64.tar.gz.bundle --certificate-identity-regexp 'https://github.com/<owner>/blindjoin/\.github/workflows/release\.yml@refs/tags/v.*' --certificate-oidc-issuer 'https://token.actions.githubusercontent.com' blindjoin-linux-amd64.tar.gz`
        - Inline comment: `# Expected: "Verified OK" + JSON cert claims.`
      - Recipe 2 — SLSA Path A (GitHub Attestations API; requires github.com reachable, per D-16):
        - Command: `gh attestation verify blindjoin-linux-amd64.tar.gz --repo <owner>/blindjoin`
        - Inline comment: `# Expected: "✓ Verification succeeded! ..." + attestation summary.`
      - Recipe 3 — SLSA Path B (offline cosign-based verify; works without github.com reachable after one-time TUF cache seeding):
        - Command: `cosign verify-attestation --bundle blindjoin-linux-amd64.tar.gz.sigstore --type slsaprovenance --certificate-identity-regexp 'https://github.com/<owner>/blindjoin/\.github/workflows/release\.yml@refs/tags/v.*' --certificate-oidc-issuer 'https://token.actions.githubusercontent.com' blindjoin-linux-amd64.tar.gz`
        - Inline comment: `# Expected: "Verified OK" + the SLSA v1.0 in-toto predicate.`
        - Sub-comment: `# To inspect the SLSA predicate body itself, add --output-file slsa-predicate.json.`
      - Recipe 4 — SIGN-03 PGP detached signature (with Pitfall 24-B inline mitigation):
        - One-time key fetch: `gpg --auto-key-locate wkd --locate-keys johnturner@gmail.com  # one-time key fetch via WKD`
        - Fallback fetch: `gpg --keyserver hkps://keys.openpgp.org --recv-keys <FINGERPRINT-TBD>  # fallback if WKD .well-known is blocked`
        - Verify: `gpg --verify blindjoin-linux-amd64.tar.gz.asc blindjoin-linux-amd64.tar.gz`
        - Inline IMPORTANT comment naming the Pitfall 24-B fingerprint-comparison gate: `# IMPORTANT: gpg --verify returns exit 0 on cryptographic validity even when the` then `# operator's trust web has not certified the key. Compare the "Primary key` then `# fingerprint:" line printed to stderr against the canonical fingerprint below.`

    - Pitfall 13 cosign-3.0 callout (D-12 — 1-line cross-ref, NOT duplication):
      `> **Note: cosign 3.0 CLI flag drift** — see the image subsection above for the cosign version pin range; the same constraints apply to tarball verification.`

    - Pitfall 24-B fingerprint-trust callout (RESEARCH §"Common Pitfalls" + §4.3):
      `> **Note: gpg --verify trust gate.** \`gpg --verify\` exits 0 on cryptographic validity but does NOT certify operator trust. Operators MUST compare the `Primary key fingerprint:` line on stderr against the canonical fingerprint at [#pgp-current](#pgp-current). For scripted verification: \`gpg --status-fd=1 --verify blindjoin-linux-amd64.tar.gz.asc blindjoin-linux-amd64.tar.gz | grep VALIDSIG | grep <FINGERPRINT-TBD>\` — this is the same pattern blindjoin uses internally at [`.github/workflows/ci.yml`](.github/workflows/ci.yml) for bitcoind's PGP verification.`

    - HTML anchor + fingerprint paragraph at the END of the subsection (D-09 + D-12 + RESEARCH §4.6):
      ```
      <a id="pgp-current"></a>
      **Current maintainer PGP fingerprint:** `<FINGERPRINT-TBD>` (UID `blindjoin maintainer <johnturner@gmail.com>`, ed25519, generated YYYY-MM-DD, expires YYYY-MM-DD). The committed public key lives at [`docs/pgp/<FINGERPRINT-TBD>.asc`](docs/pgp/) and is published to keys.openpgp.org + WKD on `<owner>.github.io`.
      ```

    The recipes use the Phase 24 workflow-anchored identity regex `'https://github.com/<owner>/blindjoin/\.github/workflows/release\.yml@refs/tags/v.*'` (Pitfall 1 narrow form — `release\.yml` NOT `docker\.yml`). The fingerprint placeholder is the literal string `<FINGERPRINT-TBD>` — Plan 24-05 atomically replaces every occurrence (in this file + docs/RELEASING.md) with the maintainer's actual 40-char hex fingerprint.

    **Edit 2 — Strikethrough the v1.5 Release-archives-unsigned bullet at SECURITY.md:104-109.** The current bullet is multi-line:

        - **GitHub Release archives ship a SHA-256 checksum but NO PGP / minisign
          signature.** A user pulling
          `blindjoin-linux-amd64.tar.gz` from a GitHub Release verifies the
          archive is intact (the `.sha256` companion file), but cannot
          cryptographically attribute the archive to the maintainer. A compromised
          GitHub account could publish a replaced binary with a matching checksum.

    Replace it with the single-line strikethrough form (mirrors Phase 23's line 110 strikethrough of the Docker-unsigned bullet — Phase 24 PATTERNS reference):

        - **~~GitHub Release archives ship a SHA-256 checksum but NO PGP / minisign signature.~~** **Closed in v1.6 Phase 24** — see [Release tarball signatures + provenance (v1.6 onward)](#release-tarball-signatures--provenance-v16-onward).

    The anchor `#release-tarball-signatures--provenance-v16-onward` is the auto-generated GitHub anchor for the new H3 heading — verified by GitHub's anchor-generation algorithm (lowercase + space→hyphen + drop `+` resulting in two hyphens around the dropped character). Per the STATE.md §Recent Plan Decisions line 116 lesson (Phase 22 Plan 22-05), the strikethrough line MUST be a single physical line in source so any `grep -q '~~GitHub Release archives.*~~'` file-level audit gate matches across newlines.

    Implementing D-12 (SECURITY.md additive subsection with prose + recipes + Pitfall callouts + fingerprint anchor) + D-09 (fingerprint anchor) + ROADMAP Phase 24 SC#1-4.

    DO NOT touch the existing `### Image signatures + attestations (v1.6 onward)` Phase 23 subsection at lines 118-185 — Phase 23 is the source-of-truth for the image side; Phase 24 appends only.
    DO NOT touch the existing `### Base-image digests (v1.6 onward)` Phase 22 subsection at line 187+ — Phase 22 ships its own anchor; Phase 24 inserts ABOVE it.
    DO NOT touch the `### v1.6 supply-chain plan` subsection that follows.
    DO NOT use a hardcoded fingerprint string anywhere — every reference uses `<FINGERPRINT-TBD>` (Plan 24-05 atomic replacement).
    DO NOT add a forward-strikethrough on any other v1.5 bullet — Phase 24 owns ONLY the Release-archives-unsigned bullet at lines 104-109.
    DO NOT duplicate the Pitfall 13 cosign 3.0 callout from the image subsection — Phase 24 uses a 1-line cross-ref per D-12 explicit lock.
    DO NOT use `--certificate-identity` (without -regexp) — Pitfall 1 mandates regex form so the recipe survives across releases.
    DO NOT widen the regex beyond `'https://github.com/<owner>/blindjoin/\.github/workflows/release\.yml@refs/tags/v.*'` — wider forms are exploitable per Pitfall 1.
    DO NOT use `docker\.yml` in this subsection's identity-regexp — Phase 23's image subsection covers docker.yml; Phase 24's tarball subsection covers release.yml.
    DO NOT add `--insecure-ignore-tlog` to any documented recipe — Pitfall 3 mandates Rekor.
    DO NOT split the v1.5 Release-archives strikethrough across two lines (Phase 22 Plan 22-05 lesson — single-line literal so file-level greps work).
    DO NOT use cosign-blob `--insecure-skip-tlog-verify` — corresponds to the sign-side Pitfall 3 invariant.
  </action>
  <verify>
    <automated>test -f SECURITY.md &amp;&amp; grep -q '^### Release tarball signatures + provenance (v1.6 onward)$' SECURITY.md &amp;&amp; grep -q 'cosign verify-blob --bundle blindjoin-linux-amd64.tar.gz.bundle' SECURITY.md &amp;&amp; grep -q 'gh attestation verify blindjoin-linux-amd64.tar.gz --repo' SECURITY.md &amp;&amp; grep -q 'cosign verify-attestation' SECURITY.md &amp;&amp; grep -q -- '--type slsaprovenance' SECURITY.md &amp;&amp; grep -q 'gpg --verify blindjoin-linux-amd64.tar.gz.asc blindjoin-linux-amd64.tar.gz' SECURITY.md &amp;&amp; grep -q 'gpg --auto-key-locate wkd --locate-keys johnturner@gmail.com' SECURITY.md &amp;&amp; grep -q 'release\\\\.yml@refs/tags/v\.\*\|release\\.yml@refs/tags/v\.\*' SECURITY.md &amp;&amp; grep -q 'token.actions.githubusercontent.com' SECURITY.md &amp;&amp; grep -q 'EITHER cosign OR PGP verification is sufficient' SECURITY.md &amp;&amp; grep -q '<a id="pgp-current"></a>' SECURITY.md &amp;&amp; grep -q '<FINGERPRINT-TBD>' SECURITY.md &amp;&amp; grep -q 'cosign 3.0' SECURITY.md &amp;&amp; grep -q 'Primary key fingerprint' SECURITY.md &amp;&amp; grep -q '~~GitHub Release archives ship a SHA-256 checksum but NO PGP / minisign signature\.~~' SECURITY.md &amp;&amp; grep -q '#release-tarball-signatures--provenance-v16-onward' SECURITY.md &amp;&amp; grep -q '^### Image signatures + attestations (v1.6 onward)$' SECURITY.md &amp;&amp; grep -q '^### Base-image digests (v1.6 onward)$' SECURITY.md &amp;&amp; awk '/### Image signatures \+ attestations/{a=NR} /### Release tarball signatures \+ provenance/{b=NR} /### Base-image digests/{c=NR} END{exit !(a<b &amp;&amp; b<c)}' SECURITY.md &amp;&amp; ! grep -q -- '--insecure-ignore-tlog' SECURITY.md &amp;&amp; ! grep -q -- '--insecure-skip-tlog-verify' SECURITY.md &amp;&amp; ! grep -qE 'certificate-identity[^-]' SECURITY.md &amp;&amp; ! grep -qE '\b[A-F0-9]{40}\b' SECURITY.md</automated>
  </verify>
  <acceptance_criteria>
    - File still exists and is well-formed markdown: `test -f SECURITY.md`
    - New H3 subsection heading present (auto-anchor `#release-tarball-signatures--provenance-v16-onward`): `grep -q '^### Release tarball signatures + provenance (v1.6 onward)$' SECURITY.md`
    - Recipe 1 (SIGN-01 cosign verify-blob): `grep -q 'cosign verify-blob --bundle blindjoin-linux-amd64.tar.gz.bundle' SECURITY.md`
    - Recipe 2 (SLSA Path A — gh attestation verify --repo per D-16): `grep -q 'gh attestation verify blindjoin-linux-amd64.tar.gz --repo' SECURITY.md`
    - Recipe 3 (SLSA Path B — cosign verify-attestation): `grep -q 'cosign verify-attestation' SECURITY.md && grep -q -- '--type slsaprovenance' SECURITY.md`
    - Recipe 4 (SIGN-03 PGP verify): `grep -q 'gpg --verify blindjoin-linux-amd64.tar.gz.asc blindjoin-linux-amd64.tar.gz' SECURITY.md && grep -q 'gpg --auto-key-locate wkd --locate-keys johnturner@gmail.com' SECURITY.md`
    - Identity regex targets Phase 24 workflow file `release.yml` (Pitfall 1 narrow form): `grep -q 'release\\\\.yml@refs/tags/v\.\*\|release\\.yml@refs/tags/v\.\*' SECURITY.md`
    - OIDC issuer URL present: `grep -q 'token.actions.githubusercontent.com' SECURITY.md`
    - EITHER-OR explicit prose (D-12): `grep -q 'EITHER cosign OR PGP verification is sufficient' SECURITY.md`
    - HTML anchor for current fingerprint (D-09 + D-12 + RESEARCH §4.6): `grep -q '<a id="pgp-current"></a>' SECURITY.md`
    - Fingerprint placeholder for Plan 24-05 atomic replacement: `[ "$(grep -c '<FINGERPRINT-TBD>' SECURITY.md)" -ge 1 ]`
    - Pitfall 13 cross-reference present (1-line cross-ref per D-12): `grep -q 'cosign 3.0' SECURITY.md`
    - Pitfall 24-B fingerprint-comparison mitigation present (RESEARCH §"Common Pitfalls"): `grep -q 'Primary key fingerprint' SECURITY.md`
    - v1.5 Release-archives bullet strikethrough'd with cross-link to new subsection (single line per Phase 22 Plan 22-05 lesson): `grep -q '~~GitHub Release archives ship a SHA-256 checksum but NO PGP / minisign signature\.~~' SECURITY.md && grep -q '#release-tarball-signatures--provenance-v16-onward' SECURITY.md`
    - Phase 23 image subsection preserved untouched: `grep -q '^### Image signatures + attestations (v1.6 onward)$' SECURITY.md && grep -A 25 '^### Image signatures + attestations (v1.6 onward)$' SECURITY.md | grep -q 'docker\\.yml'`
    - Phase 22 Base-image-digests subsection preserved untouched: `grep -q '^### Base-image digests (v1.6 onward)$' SECURITY.md && grep -A 10 '^### Base-image digests (v1.6 onward)$' SECURITY.md | grep -q 'docker/digests.txt'`
    - Subsection ordering: Phase 23 image subsection → Phase 24 tarball subsection → Phase 22 base-image subsection: `awk '/### Image signatures \+ attestations/{a=NR} /### Release tarball signatures \+ provenance/{b=NR} /### Base-image digests/{c=NR} END{exit !(a<b && b<c)}' SECURITY.md`
    - --insecure-ignore-tlog ABSENT (Pitfall 3 invariant): `! grep -q -- '--insecure-ignore-tlog' SECURITY.md`
    - --insecure-skip-tlog-verify ABSENT (sign-side Pitfall 3 sibling invariant): `! grep -q -- '--insecure-skip-tlog-verify' SECURITY.md`
    - --certificate-identity (without -regexp) ABSENT (Pitfall 1 invariant): `! grep -qE 'certificate-identity[^-]' SECURITY.md`
    - No hardcoded fingerprint hex strings (40-hex pattern) anywhere — only `<FINGERPRINT-TBD>` placeholder: `! grep -qE '\b[A-F0-9]{40}\b' SECURITY.md`
    - File length grew by ≥60 lines: `[ "$(wc -l < SECURITY.md)" -ge 350 ]` (current 290 + ≥60 new ≥ 350)
    - All existing sections still present (no accidental deletions): `grep -q '^## Reporting a vulnerability$' SECURITY.md && grep -q '^## Supply-chain status$' SECURITY.md && grep -q '^### Known gaps at v1.5$' SECURITY.md && grep -q '^### Image signatures + attestations (v1.6 onward)$' SECURITY.md && grep -q '^### Base-image digests (v1.6 onward)$' SECURITY.md && grep -q '^### v1.6 supply-chain plan$' SECURITY.md && grep -q '^## Release versioning policy$' SECURITY.md && grep -q '^## Where to find more$' SECURITY.md`
    - End-to-end recipe rehearsal — DEFERRED to first v1.6.0-rc.0 cut per Phase 23 closure pattern (CONTEXT §domain — no HUMAN-UAT scaffold plan); if recipes fail empirical validation a quick task amends this section BEFORE the production v1.6.0 tag.
  </acceptance_criteria>
  <done>
    `SECURITY.md` has a new `### Release tarball signatures + provenance (v1.6 onward)` subsection inserted between the existing Phase 23 `### Image signatures + attestations (v1.6 onward)` subsection and the existing Phase 22 `### Base-image digests (v1.6 onward)` subsection. The new subsection contains a 3-item numbered claim list (cosign + SLSA + PGP), an EITHER-OR alternative-paths paragraph, a prerequisites paragraph naming cosign 2.6.3+ / gh 2.50+ / gpg 2.4+, a fenced bash block with 4 recipes (cosign verify-blob + gh attestation verify + cosign verify-attestation + gpg --verify with WKD key fetch), a 1-line Pitfall 13 cross-reference callout, a Pitfall 24-B fingerprint-trust callout, and a `<a id="pgp-current"></a>` anchor + fingerprint paragraph with `<FINGERPRINT-TBD>` placeholder (Plan 24-05 atomic replacement). The v1.5 Release-archives-unsigned bullet at SECURITY.md:104-109 is strikethrough'd on a single physical line with cross-link to the new subsection. Phase 23's image subsection and Phase 22's base-image subsection are untouched. The locked identity-regexp targets `release\.yml` (Pitfall 1 narrow form for Phase 24). No hardcoded 40-char hex fingerprint strings.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator workstation → SECURITY.md recipes | The recipes ARE the operator-facing trust boundary. A wrong recipe (typo, nonexistent flag, drifted regex) propagates to every operator. Empirical validation at first v1.6.0-rc.0 cut catches this. |
| SECURITY.md identity regex → OIDC subject claims | The locked regex `'release\.yml@refs/tags/v.*'` is narrow on workflow file + tag namespace per Pitfall 1. Drift between this regex and Plan 24-01's actual workflow filename = silent verify failure. |
| Operator → Sigstore Fulcio / Rekor (online verify) | Recipes 1-3 require network to Fulcio + Rekor + GH Attestations API. Documented assumption; Pitfall 3 invariant forbids `--insecure-ignore-tlog`. |
| Operator → WKD `.well-known` directory | Recipe 4's one-time `gpg --auto-key-locate wkd` requires reachability to `<owner>.github.io/.well-known/openpgpkey/hu/<WKD_HASH>`. Fallback to keys.openpgp.org is documented inline. |
| `<FINGERPRINT-TBD>` placeholder → maintainer's actual fingerprint | Plan 24-05 atomically replaces every `<FINGERPRINT-TBD>` occurrence with the maintainer's actual 40-char hex fingerprint after key generation. A missed occurrence leaves a placeholder in a published doc — visible failure, bad UX. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-21 | Spoofing | OIDC subject regex too wide — `'https://github.com/<owner>/.*'` would let any of `<owner>`'s repos produce a "valid" signature for blindjoin (Pitfall 1) | mitigate | Locked regex binds to BOTH workflow file (`release\.yml`) AND tag namespace (`refs/tags/v.*`). The acceptance criterion grepping the exact substring `release\.yml@refs/tags/v.*` enforces this at file level. |
| T-24-22 | Tampering | Recipe contains a nonexistent CLI flag (e.g., `--output-name` from the now-corrected CONTEXT D-14) — operator copy-pastes, gets "Error: unknown flag" | mitigate | Recipes 1-4 are verified at RESEARCH §4 against cosign 2.6.3 / gh 2.92.0 / gpg 2.4+. Empirical validation at first v1.6.0-rc.0 cut catches drift; quick task amends. |
| T-24-23 | Repudiation | Operator runs `gpg --verify`, sees "Good signature", trusts a malicious key (Pitfall 24-B) | mitigate | Recipe 4 has IMPORTANT inline comment naming the fingerprint-comparison gate. The `> Note: gpg --verify trust gate.` callout below recipes provides scripted-verification pattern `gpg --status-fd=1 ... \| grep VALIDSIG \| grep <FINGERPRINT-TBD>` (matching ci.yml:99-115 internal pattern). The `<a id="pgp-current"></a>` anchor gives operators a canonical fingerprint to compare. |
| T-24-24 | Tampering | cosign 3.0 ships with renamed flags; documented recipes break for operators on 3.0 (Pitfall 13) | mitigate | 1-line cross-ref to image subsection's cosign version pin range `>= 2.6.3, < 3.0.0` per D-12. When cosign 3.0 lands, a quick task updates BOTH image and tarball subsections together. |
| T-24-25 | Spoofing | Operator following the SECURITY.md recipe verifies a signature from a different repo whose maintainer used a similar `release.yml` workflow path | mitigate | The locked regex includes `<owner>/blindjoin` as the owner+repo prefix. A signature from `someother/blindjoin/.github/workflows/release.yml` would NOT match (Pitfall 1 narrow-form). Forking the project produces a different OIDC subject. |
| T-24-26 | Tampering | The strikethrough cross-link `#release-tarball-signatures--provenance-v16-onward` breaks after a future SECURITY.md restructure | accept-with-action | The cross-link is internal. A future restructure must preserve the anchor OR update both ends. Documented as the cross-phase invariant of the strikethrough closures (mirrors Phase 23 T-23-28). |
| T-24-27 | Information Disclosure | Documented recipes name `<owner>` as a placeholder; operator might paste in private repo info | accept | The recipes use placeholder syntax; operators substitute their own values. No PII in SECURITY.md prose itself. |
| T-24-28 | Tampering | The `<FINGERPRINT-TBD>` placeholder is left unreplaced when Plan 24-05 runs | mitigate | Plan 24-05's `checkpoint:human-verify` task explicitly lists every `<FINGERPRINT-TBD>` occurrence (in SECURITY.md + docs/RELEASING.md) and replaces them atomically in one commit. Acceptance criteria include `! grep -q '<FINGERPRINT-TBD>' SECURITY.md` post-replacement. |
| T-24-29 | Spoofing | Operator fetches the key via WKD and trusts whatever is returned without comparing fingerprint | mitigate | Recipe 4 inline IMPORTANT comment + the `> Note: gpg --verify trust gate.` callout direct operators to compare the printed fingerprint against `#pgp-current` BEFORE trusting any signature. Pitfall 24-B documented inline. |
| T-24-SC | Tampering | npm/pip/cargo installs | n/a | This plan modifies one documentation file. No code, no package installs, no CI workflow changes. The documented external dependencies (cosign, gh, gpg) are operator-side installs out of CI scope; empirical validation at v1.6.0-rc.0 cut covers their install paths. |
</threat_model>

<verification>
After Task 1 completes:

```bash
# 1. New subsection exists.
grep -q '^### Release tarball signatures + provenance (v1.6 onward)$' SECURITY.md

# 2. All 4 recipes present.
grep -q 'cosign verify-blob --bundle blindjoin-linux-amd64.tar.gz.bundle' SECURITY.md   # Recipe 1 — SIGN-01
grep -q 'gh attestation verify blindjoin-linux-amd64.tar.gz --repo' SECURITY.md         # Recipe 2 — SLSA Path A
grep -q 'cosign verify-attestation' SECURITY.md                                          # Recipe 3 — SLSA Path B
grep -q -- '--type slsaprovenance' SECURITY.md
grep -q 'gpg --verify blindjoin-linux-amd64.tar.gz.asc blindjoin-linux-amd64.tar.gz' SECURITY.md  # Recipe 4 — SIGN-03

# 3. Locked identity regex targets release.yml (Pitfall 1).
grep -q 'release\\.yml@refs/tags/v\.\*\|release\.yml@refs/tags/v\.\*' SECURITY.md

# 4. OIDC issuer URL.
grep -q 'token.actions.githubusercontent.com' SECURITY.md

# 5. EITHER-OR prose (D-12).
grep -q 'EITHER cosign OR PGP verification is sufficient' SECURITY.md

# 6. Fingerprint anchor + placeholder.
grep -q '<a id="pgp-current"></a>' SECURITY.md
grep -q '<FINGERPRINT-TBD>' SECURITY.md

# 7. Callouts present.
grep -q 'cosign 3.0' SECURITY.md            # Pitfall 13 cross-ref
grep -q 'Primary key fingerprint' SECURITY.md   # Pitfall 24-B mitigation

# 8. v1.5 Release-archives bullet strikethrough'd with cross-link.
grep -q '~~GitHub Release archives ship a SHA-256 checksum but NO PGP / minisign signature\.~~' SECURITY.md
grep -q '#release-tarball-signatures--provenance-v16-onward' SECURITY.md

# 9. Phase 23 + Phase 22 subsections preserved untouched.
grep -q '^### Image signatures + attestations (v1.6 onward)$' SECURITY.md
grep -A 25 '^### Image signatures + attestations (v1.6 onward)$' SECURITY.md | grep -q 'docker\.yml'
grep -q '^### Base-image digests (v1.6 onward)$' SECURITY.md

# 10. Subsection ordering: image → tarball → base-image.
awk '/### Image signatures \+ attestations/{a=NR} /### Release tarball signatures \+ provenance/{b=NR} /### Base-image digests/{c=NR} END{exit !(a<b && b<c)}' SECURITY.md

# 11. Forbidden tokens absent.
! grep -q -- '--insecure-ignore-tlog' SECURITY.md
! grep -q -- '--insecure-skip-tlog-verify' SECURITY.md
! grep -qE 'certificate-identity[^-]' SECURITY.md

# 12. No real fingerprint hex strings (only placeholders).
! grep -qE '\b[A-F0-9]{40}\b' SECURITY.md

# 13. Anchor sanity check: GitHub auto-anchor for new H3 = #release-tarball-signatures--provenance-v16-onward.
# The strikethrough's cross-link MUST match this anchor.
# Empirically validated at first v1.6.0-rc.0 cut by clicking the cross-link on github.com.

# 14. End-to-end recipe rehearsal — DEFERRED to first v1.6.0-rc.0 cut per Phase 23 closure pattern.
```
</verification>

<success_criteria>
- `SECURITY.md` has a new `### Release tarball signatures + provenance (v1.6 onward)` H3 subsection inserted between the existing Phase 23 image subsection and the existing Phase 22 base-image-digests subsection.
- Subsection contains a 3-item numbered claim list (cosign + SLSA + PGP), EITHER-OR prose paragraph (D-12), prerequisites paragraph (cosign 2.6.3+ / gh 2.50+ / gpg 2.4+), a fenced bash block with 4 verify recipes (cosign verify-blob + gh attestation verify --repo + cosign verify-attestation + gpg --verify with WKD key fetch), a 1-line Pitfall 13 cross-reference callout, a Pitfall 24-B fingerprint-trust callout, and a `<a id="pgp-current"></a>` anchor + fingerprint paragraph using `<FINGERPRINT-TBD>` placeholder.
- The cosign verify-blob + verify-attestation recipes use the workflow-anchored identity-regexp `'https://github.com/<owner>/blindjoin/\.github/workflows/release\.yml@refs/tags/v.*'` (Pitfall 1 narrow form scoped to Phase 24's workflow file).
- The gh attestation verify recipe uses `--repo <owner>/blindjoin` per D-16 (tighter identity binding than `--owner`).
- The cosign verify-attestation recipe uses `--type slsaprovenance` per RESEARCH §4.2.
- The PGP recipe 4 has inline IMPORTANT comment naming the Pitfall 24-B fingerprint-comparison gate.
- The v1.5 Release-archives-unsigned bullet at `### Known gaps at v1.5` is strikethrough'd on a single physical line with cross-link `#release-tarball-signatures--provenance-v16-onward`.
- Phase 23 `### Image signatures + attestations (v1.6 onward)` subsection and Phase 22 `### Base-image digests (v1.6 onward)` subsection are untouched.
- All occurrences of the maintainer's fingerprint use the literal `<FINGERPRINT-TBD>` placeholder (Plan 24-05 atomic replacement contract).
- File length grew by ≥60 lines; no existing sections deleted.
- Maps to ROADMAP Phase 24 Success Criteria #1 (cosign verify-blob recipe locked to identity-regexp) + #2 (SLSA verify recipe consistent with image subsection — same actions/attest-build-provenance machinery, BUT NOTE: cosign verify-attestation `.sigstore` bundle path is Phase 24-specific) + #3 (gpg --verify recipe with maintainer fingerprint documented) + #4 (BOTH cosign + PGP recipes side by side with explicit EITHER-OR prose).
- End-to-end recipe rehearsal is DEFERRED to the first `v1.6.0-rc.0` tag push per Phase 23 closure pattern (CONTEXT §domain — no HUMAN-UAT scaffold plan in Phase 24).
</success_criteria>

<output>
Create `.planning/phases/24-release-tarball-signing-cosign-slsa-pgp/24-03-SUMMARY.md` when done. Include: (a) the exact line numbers of the new subsection (before and after edit), (b) the literal regex string used in the cosign verify-blob + cosign verify-attestation recipes (for cross-reference with Plan 24-01's workflow file identity claim — same `release.yml@refs/tags/v.*` pattern), (c) the verified anchor `#release-tarball-signatures--provenance-v16-onward` that the strikethrough cross-links to, (d) a note confirming the Phase 23 `### Image signatures + attestations (v1.6 onward)` subsection (lines 118-185) and the Phase 22 `### Base-image digests (v1.6 onward)` subsection (line 187+) are byte-identical before and after, (e) total `<FINGERPRINT-TBD>` occurrence count in this file (Plan 24-05 reads this number to verify atomic replacement covers all of them across both this file and docs/RELEASING.md), (f) cross-reference to Plan 24-05 (checkpoint:human-verify — atomic fingerprint substitution at v1.6.0-rc.0 cut).
</output>
</content>
