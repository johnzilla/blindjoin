---
phase: 14-sprint-0-spikes-discuss-phase-decisions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/sprint-0-B.md
  - client/examples/spike-p2tr.rs
autonomous: true
requirements: []
tags:
  - bdk-wallet
  - p2tr
  - bip322
  - spike

must_haves:
  truths:
    - "sprint-0-B.md exists at .planning/research/sprint-0-B.md and embeds the FULL PoC source code from client/examples/spike-p2tr.rs verbatim (per CD-1)"
    - "sprint-0-B.md embeds the captured 64-byte Schnorr witness as a hex string (128 hex chars on PASS) AND the exact `secp256k1::verify_schnorr` return (`Ok(())` on PASS, error variant on FAIL)"
    - "sprint-0-B.md records an explicit PASS / FAIL / INCONCLUSIVE verdict and a one-line recommendation matching the verdict: PASS → 'bdk path'; FAIL → 'manual fallback per D-15'; INCONCLUSIVE → 'manual fallback per D-15 (timebox D-18)'"
    - "The PoC source MUST construct PSBTs with `SignOptions { trust_witness_utxo: true, ..Default::default() }` AND populate real on-chain `witness_utxo` values (load-bearing per v1.3 Phase 12 lesson — Phase 12 CONTEXT.md is read-first); a PoC that omits either tests a strawman and the verdict is invalid"
    - "Spike branch `spike/14-B-bdk-p2tr-poc` exists locally AND has been pushed to `origin` for reproducibility (D-19)"
    - "Spike branch is NOT merged into `main`; no commits touching `coordinator/`, `client/`, `shared/`, or `liquidity-bot/` source land in `main` from this plan (D-21)"
    - "The `client/examples/spike-p2tr.rs` binary lives ONLY on the spike branch (per CD-4); main's `client/` member crate has no `examples/spike-p2tr.rs` file after this plan completes"
    - "Sprint cap of 2 calendar days (D-18) respected; on cap-with-no-conclusion, verdict line reads `INCONCLUSIVE:` and downstream defaults to manual fallback (D-15)"
  artifacts:
    - path: ".planning/research/sprint-0-B.md"
      provides: "Canonical Sprint-0-B record (PoC source excerpt, witness hex, verify_schnorr verdict, one-line recommendation)"
      contains: "trust_witness_utxo: true"
  key_links:
    - from: ".planning/research/sprint-0-B.md"
      to: ".planning/decisions/v1.4-adr.md"
      via: "verdict + recommendation consumed by Plan 14-03 to set ADR Decision #4 STATUS"
      pattern: "(PASS|FAIL|INCONCLUSIVE):"
    - from: "spike branch `spike/14-B-bdk-p2tr-poc`"
      to: "origin remote"
      via: "git push origin spike/14-B-bdk-p2tr-poc"
      pattern: "spike/14-B-bdk-p2tr-poc"
---

<objective>
Run Sprint-0-B: a timeboxed proof-of-concept that asks `bdk_wallet 2.3` to sign a BIP-322 message for a P2TR (taproot keypath) descriptor, captures the produced 64-byte Schnorr witness, and verifies it against the expected sighash via `secp256k1::verify_schnorr`. The verdict resolves Open Decision #4 (bdk handles the multi-descriptor sign path OR manual `secp256k1::Secp256k1::sign_schnorr` fallback pre-spec'd in D-15 fires).

Purpose: Phase 17 (WALLET-02, BIP-322 signing for all 3 script types) needs unambiguous direction on whether to lean on bdk_wallet's PSBT signer or to ship a hand-rolled `shared/src/bip322/p2tr.rs::sign_p2tr_keypath` under D-15's 80-LOC budget. Sprint-0-B's verdict is the input contract for Plan 14-03's ADR Decision #4 STATUS line.

Output: `.planning/research/sprint-0-B.md` checked into `main` with the PoC source code excerpt, witness hex, and verdict. Spike branch `spike/14-B-bdk-p2tr-poc` (containing `client/examples/spike-p2tr.rs`) pushed to `origin` for reproducibility but NEVER merged into `main` (D-19, CD-4).

PoC location rationale: CD-4 says "either works" between `client/src/bin/spike-p2tr.rs` and `examples/spike-p2tr.rs`. We lock the location to `client/examples/spike-p2tr.rs` (i.e., inside the `client` member crate's `examples/` directory). Reason: the workspace `Cargo.toml` has no root package (only `members = ["coordinator", "client", "shared", "liquidity-bot"]`), so `cargo run --example` from the repo root needs a `-p <crate>` selector to disambiguate which member crate hosts the example. Placing it under `client/examples/` (a) is auto-discovered by Cargo with zero `Cargo.toml` surgery (member crates' `examples/` dirs are convention-discovered), (b) avoids the "register `examples/` at the workspace root" workaround, (c) lets us invoke unambiguously as `cargo run -p client --example spike-p2tr`. The `client` crate is the natural host because bdk_wallet is the client's signer (the PoC's whole subject is the client signing path).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-sprint-0-spikes-discuss-phase-decisions/14-CONTEXT.md
@.planning/research/SUMMARY.md
@.planning/milestones/v1.3-phases/12-repair-client-src-wallet-rs-260-bdk-wallet-2-3-signoptions-r/12-CONTEXT.md
@shared/src/bip322.rs

<interfaces>
<!-- Primitives the PoC must call. Source: shared/src/bip322.rs (existing, 133 LOC, script-type-NEUTRAL per V1.4-MOD-07). -->
<!-- These primitives ARE single source of truth — the PoC reuses them, does NOT reimplement to_spend / to_sign / message_hash. -->

From shared/src/bip322.rs (existing public functions — exact signatures verified by `grep -n 'pub fn' shared/src/bip322.rs`):
- `bip322_message_hash(message: &[u8]) -> sha256::Hash`
- `build_bip322_to_spend(script_pubkey: &Script, message_hash: sha256::Hash) -> Transaction`
- `build_bip322_to_sign(to_spend: &Transaction) -> Transaction`

From bdk_wallet 2.3 (already in Cargo.toml; v1.3 Phase 12 lessons apply):
- `bdk_wallet::Wallet::create(descriptor, change_descriptor)` — descriptor-based wallet constructor
- `bdk_wallet::SignOptions { trust_witness_utxo: bool, .. }` — MUST be `true` for segwit signing per v1.3 Phase 12 (load-bearing)
- `wallet.sign(&mut psbt, sign_options)` — populates input signatures
- Real on-chain `witness_utxo` values required on PSBT inputs (NOT zero placeholders; v1.3 Phase 12 retry-zero-placeholders ban)

From bitcoin 0.32.x (transitive via bdk_wallet):
- `bitcoin::psbt::Psbt` with `inputs[0].tap_key_sig: Option<bitcoin::taproot::Signature>` — extraction site for the produced Schnorr sig
- `bitcoin::secp256k1::{Secp256k1, Verification}` + `secp256k1::schnorr::Signature` + `XOnlyPublicKey::from_slice`
- `bitcoin::sighash::SighashCache::taproot_key_spend_signature_hash` — expected sighash for verification step 8

Target descriptor (BIP-86, single-key Taproot for v1.4 P2TR WALLET-01):
- `tr(.../86'/1'/0'/0/0)` (testnet/signet derivation path; matches Phase 17 WALLET-01 spec)
- Generate from a deterministic seed (e.g., `[0u8; 32]` — repeatable across machines; this is throwaway PoC, not production key material)

PoC host crate: `client` (workspace member). Cargo auto-discovers `client/examples/*.rs` files without any `Cargo.toml` registration. Invocation pattern: `cargo run -p client --example spike-p2tr` (the `-p client` selector is REQUIRED because the workspace root has no package).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create spike branch with client/examples/spike-p2tr.rs PoC implementing the 8-step sequence</name>
  <files>
    - client/examples/spike-p2tr.rs (NEW, on `spike/14-B-bdk-p2tr-poc` branch ONLY — NEVER committed to main per D-19/CD-4)
  </files>
  <read_first>
    - .planning/phases/14-sprint-0-spikes-discuss-phase-decisions/14-CONTEXT.md (Specifics #2 — the 8-step sequence; D-14 PoC verdict shape; D-15 fallback location; D-19 branch hygiene; CD-4 location convention — locked here to `client/examples/`)
    - .planning/milestones/v1.3-phases/12-repair-client-src-wallet-rs-260-bdk-wallet-2-3-signoptions-r/12-CONTEXT.md (full file — `trust_witness_utxo: true` + real on-chain `witness_utxo` requirement is load-bearing)
    - shared/src/bip322.rs (confirm exact signatures of `bip322_message_hash`, `build_bip322_to_spend`, `build_bip322_to_sign` — the PoC reuses them via `shared::bip322::...`)
    - .planning/research/SUMMARY.md §"Open Decision #4"
    - client/Cargo.toml (confirm `client` member crate already depends on bdk_wallet, bitcoin, secp256k1, shared; PoC under `client/examples/` inherits these dev-deps automatically — no `[dev-dependencies]` edits required if the client crate's runtime deps already cover the PoC's needs)
  </read_first>
  <action>
    From `main` at HEAD, create and check out a new branch:
    `git checkout -b spike/14-B-bdk-p2tr-poc`

    Create `client/examples/spike-p2tr.rs` on the spike branch implementing the EXACT 8-step sequence from CONTEXT.md Specifics #2. Cargo auto-discovers files under `client/examples/` — no `Cargo.toml` edits required. If the `client` crate's runtime dependencies do not already cover bdk_wallet + bitcoin + secp256k1 + shared, add the missing crates to `client/Cargo.toml` `[dev-dependencies]` (NOT `[dependencies]`, to keep them out of `cargo build --release`). The PoC location decision (CD-4 → `client/examples/`) is locked in this plan's objective.

    The 8-step sequence (verbatim from CONTEXT.md Specifics #2):

    1. Generate a deterministic seed (use `[0u8; 32]` or `[0x01; 32]` — repeatable; documented in source as throwaway PoC seed; absolutely NOT production key material).
    2. Derive a `tr(.../86'/1'/0'/0/*)` BIP-86 descriptor via `bdk_wallet 2.3`. Use the testnet/signet derivation path (BIP-86 coin type `1'`). Construct descriptor string explicitly; if `bdk_wallet` exposes a descriptor template helper for BIP-86, use it.
    3. Construct the BIP-322 `to_spend` virtual transaction via `shared::bip322::build_bip322_to_spend(&script_pubkey, shared::bip322::bip322_message_hash(message))`. The `script_pubkey` is the P2TR scriptPubKey derived in step 2. The `message` is a fixed test string (e.g., `b"blindjoin spike-0-B taproot test message"`).
    4. Construct the `to_sign` virtual transaction via `shared::bip322::build_bip322_to_sign(&to_spend)`.
    5. Wrap `to_sign` in a `bitcoin::psbt::Psbt`. Populate `psbt.inputs[0].witness_utxo` with the REAL on-chain `witness_utxo` — specifically the `TxOut { value: 0, script_pubkey: <P2TR SPK from step 2> }` per BIP-322's `to_spend` shape (value = 0 is correct PER BIP-322 spec — that's the `to_spend` output, not a zero placeholder). DO NOT leave `witness_utxo` as `None` — that's the v1.3 Phase 12 trap (CONTEXT.md Specifics #2 load-bearing).
    6. Call `wallet.sign(&mut psbt, SignOptions { trust_witness_utxo: true, ..Default::default() })`. `trust_witness_utxo: true` is LOAD-BEARING per v1.3 Phase 12 — DO NOT omit; DO NOT use `Default::default()` alone (which sets it to `false` for some bdk versions and silently produces no signature).
    7. Extract `psbt.inputs[0].tap_key_sig`. On bdk-success this is `Some(bitcoin::taproot::Signature)` containing a 64-byte Schnorr sig + sighash type. On bdk-failure this is `None` and step 7 FAILs.
    8. Verify the Schnorr signature against the expected sighash:
       - Compute `expected_sighash = bitcoin::sighash::SighashCache::new(&to_sign).taproot_key_spend_signature_hash(0, &Prevouts::All(&[txout_from_step_5]), TapSighashType::Default)?`.
       - Extract `xonly_pubkey` from the P2TR scriptPubKey in step 2 (`bitcoin::XOnlyPublicKey::from_slice(&spk.as_bytes()[2..34])?`).
       - Call `secp256k1::Secp256k1::new().verify_schnorr(&tap_key_sig.sig, &expected_sighash.as_byte_array(), &xonly_pubkey)`. PASS = `Ok(())`. FAIL = `Err(_)`.

    Print to stdout in this exact line format (parser-friendly for Task 2 to grep into `sprint-0-B.md`):
    ```
    STEP_6_BDK_SIGN: <Ok|Err: ...>
    STEP_7_EXTRACT_TAP_KEY_SIG: <Some(<hex>) | None>
    STEP_8_VERIFY_SCHNORR: <Ok | Err: ...>
    WITNESS_HEX: <hex string, 128 chars on PASS, empty on FAIL>
    VERDICT: <PASS | FAIL>
    ```

    Compile and run on the spike branch:
    `cargo run -p client --example spike-p2tr`

    (The `-p client` selector is REQUIRED because the workspace root has no package. Without it, Cargo errors with "could not find package: please specify which one with -p" since the workspace has multiple member crates and the example name is not unique without disambiguation.)

    Capture stdout to a file (e.g., `/tmp/spike-0-B-output.txt`) for embedding in sprint-0-B.md (Task 2).

    Commit the PoC on the spike branch ONLY:
    `git add client/examples/spike-p2tr.rs && git commit -m "spike(14-B): bdk_wallet 2.3 P2TR BIP-322 sign PoC (8-step sequence)"`
    (If `client/Cargo.toml` was edited to add dev-dependencies, include it in the same commit. The commit MUST NOT include `coordinator/`, `shared/`, or `liquidity-bot/` changes.)

    DO NOT merge or cherry-pick this commit onto main. CD-4 puts the PoC in `client/examples/` (Cargo convention for throwaway binaries excluded from `cargo build --release`) and D-19 keeps the branch out of main.
  </action>
  <verify>
    <automated>git rev-parse --abbrev-ref HEAD | grep -qx "spike/14-B-bdk-p2tr-poc" && test -f client/examples/spike-p2tr.rs && grep -q 'trust_witness_utxo: true' client/examples/spike-p2tr.rs && grep -q 'build_bip322_to_spend' client/examples/spike-p2tr.rs && grep -q 'build_bip322_to_sign' client/examples/spike-p2tr.rs && grep -q 'verify_schnorr' client/examples/spike-p2tr.rs && cargo run -p client --example spike-p2tr 2>&1 | grep -E '^VERDICT: (PASS|FAIL)$'</automated>
  </verify>
  <acceptance_criteria>
    - Current branch is exactly `spike/14-B-bdk-p2tr-poc`.
    - File `client/examples/spike-p2tr.rs` exists on the spike branch.
    - File contains the literal `trust_witness_utxo: true` (load-bearing v1.3 Phase 12 invariant).
    - File contains `build_bip322_to_spend` and `build_bip322_to_sign` invocations (reuses shared primitives — V1.4-MOD-07 single source of truth).
    - File contains `verify_schnorr` invocation (step 8).
    - File contains `witness_utxo` population with non-`None` value (no v1.3 Phase 12 strawman).
    - `cargo run -p client --example spike-p2tr` exits with status 0 OR with a non-zero status whose stdout still contains `VERDICT: FAIL` (a FAIL verdict is a valid PoC outcome; a panic/compile-error is not).
    - stdout contains exactly one line matching `^VERDICT: (PASS|FAIL)$`.
    - On PASS: stdout contains a line `^WITNESS_HEX: [0-9a-f]{128}$` (64-byte Schnorr sig = 128 hex chars).
    - The spike's PoC file is NOT in main's history — verified with empty-input-guarded check: `LATEST=$(git log main --oneline -- client/examples/spike-p2tr.rs | grep -v "^$" | head -1); [ -z "$LATEST" ] || ! echo "$LATEST" | grep -q "spike(14-B)"`. The check passes when there are zero commits touching the file (the desired state for the doc-only land on main) AND fails if the spike commit was accidentally cherry-picked over with the doc.
  </acceptance_criteria>
  <done>PoC source `client/examples/spike-p2tr.rs` compiles and runs on the spike branch end-to-end via `cargo run -p client --example spike-p2tr`, exercising the 8-step sequence with `trust_witness_utxo: true` + real `witness_utxo`. Verdict line emitted to stdout. main's `client/examples/spike-p2tr.rs` does not exist.</done>
</task>

<task type="auto">
  <name>Task 2: Write sprint-0-B.md with PoC excerpt, witness hex, verdict, recommendation</name>
  <files>.planning/research/sprint-0-B.md (NEW, COMMITTED TO MAIN — the canonical record per D-19/CD-1)</files>
  <read_first>
    - .planning/phases/14-sprint-0-spikes-discuss-phase-decisions/14-CONTEXT.md (D-14 verdict binary by design; D-15 manual fallback pre-spec — `shared/src/bip322/p2tr.rs::sign_p2tr_keypath` 80 LOC budget; CD-1 embed full PoC code)
    - client/examples/spike-p2tr.rs on the spike branch (read the file Task 1 just committed; this becomes the embedded source excerpt)
    - the captured stdout from `cargo run -p client --example spike-p2tr` (Task 1 captured to `/tmp/spike-0-B-output.txt`)
  </read_first>
  <action>
    On the spike branch, write `.planning/research/sprint-0-B.md` with this exact structure (embed FULL PoC source per CD-1 — do not abbreviate the 8-step body; embed FULL stdout per CD-1):

    ```
    # Sprint-0-B: bdk_wallet 2.3 P2TR BIP-322 sign PoC

    **Branch:** spike/14-B-bdk-p2tr-poc (pushed to origin; NOT merged to main per D-19)
    **PoC location:** client/examples/spike-p2tr.rs (spike branch only; per CD-4)
    **Invocation:** `cargo run -p client --example spike-p2tr` (the `-p client` selector is required — workspace root has no package; only member crates host examples)
    **Date:** <YYYY-MM-DD>
    **Sprint cap:** 2 days (D-18)

    ## 8-Step Sequence (per CONTEXT.md Specifics #2)

    1. Deterministic seed: `[0u8; 32]` (throwaway PoC, NOT production key material)
    2. BIP-86 descriptor: `tr(.../86'/1'/0'/0/*)` (testnet/signet coin type 1')
    3. `to_spend` via `shared::bip322::build_bip322_to_spend`
    4. `to_sign` via `shared::bip322::build_bip322_to_sign`
    5. PSBT input populated with REAL on-chain `witness_utxo` (load-bearing per v1.3 Phase 12)
    6. `wallet.sign(&mut psbt, SignOptions { trust_witness_utxo: true, ..Default::default() })`
    7. Extract `psbt.inputs[0].tap_key_sig`
    8. Verify the 64-byte Schnorr sig via `secp256k1::verify_schnorr(&sig, &sighash, &xonly_pubkey)`

    ## PoC Source (verbatim from client/examples/spike-p2tr.rs)

    ```rust
    <full contents of client/examples/spike-p2tr.rs — every line, including imports and main function>
    ```

    ## Captured Output (verbatim from `cargo run -p client --example spike-p2tr`)

    ```
    <full stdout from the run, including the STEP_6 / STEP_7 / STEP_8 / WITNESS_HEX / VERDICT lines>
    ```

    ## Witness Hex (extracted)

    `<128-char hex string on PASS, or "(none — bdk did not produce a tap_key_sig)" on FAIL>`

    ## verify_schnorr Result

    `<Ok(()) on PASS, or the exact secp256k1 error variant on FAIL>`

    ## Overall verdict

    <PASS | FAIL | INCONCLUSIVE>: <one-line explanation>

    ## Recommendation

    <"bdk path" on PASS | "manual fallback per D-15" on FAIL | "manual fallback per D-15 (timebox D-18)" on INCONCLUSIVE>

    Rationale:
    - PASS → Phase 17 WALLET-02 uses bdk_wallet's PSBT-sign for the P2TR descriptor; no new `shared/src/bip322/p2tr.rs::sign_p2tr_keypath` needed. D-05 asymmetry still applies (bdk has no BIP-322 sign API per se — we're using its PSBT signer for a BIP-322-shaped PSBT; verify path remains ours regardless).
    - FAIL → Phase 17 WALLET-02 implements `shared/src/bip322/p2tr.rs::sign_p2tr_keypath` per D-15 (80 LOC budget, reuses `shared::bip322::bip322_message_hash`, symmetric with D-04's module split).
    - INCONCLUSIVE → same as FAIL per D-18 timebox + D-15 default.

    ## Reproducibility
    - Spike branch HEAD SHA: <git rev-parse HEAD>
    - PoC seed: `[0u8; 32]` (deterministic; results reproducible)
    - bdk_wallet version pinned at: <from Cargo.toml, e.g., `=2.3.x`>
    - bitcoin version pinned at: <from Cargo.toml>
    - To reproduce locally: `git fetch origin spike/14-B-bdk-p2tr-poc && git checkout spike/14-B-bdk-p2tr-poc && cargo run -p client --example spike-p2tr`
    ```

    The verdict line MUST start at column 0 with one of the exact strings `PASS:`, `FAIL:`, or `INCONCLUSIVE:` so Plan 14-03's grep can detect it deterministically.

    The recommendation line MUST contain one of `bdk path`, `manual fallback per D-15`, or `manual fallback per D-15 (timebox D-18)` exactly.

    Commit `sprint-0-B.md` on the spike branch (`spike(14-B): capture PoC source, witness hex, verdict <X>`), then cherry-pick that single doc-file commit onto `main` (or recreate it on main; the file is the canonical record per D-19 and must land in main's history independent of the spike branch). Verify the spike branch's `client/examples/spike-p2tr.rs` does NOT come along with the cherry-pick — only `.planning/research/sprint-0-B.md`.
  </action>
  <verify>
    <automated>test -f .planning/research/sprint-0-B.md && grep -E '^(PASS|FAIL|INCONCLUSIVE):' .planning/research/sprint-0-B.md && grep -q 'trust_witness_utxo: true' .planning/research/sprint-0-B.md && grep -E '(bdk path|manual fallback per D-15)' .planning/research/sprint-0-B.md && grep -q 'build_bip322_to_spend' .planning/research/sprint-0-B.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/research/sprint-0-B.md` exists in main.
    - File contains a verdict line matching `^(PASS|FAIL|INCONCLUSIVE):` at column 0.
    - File contains the literal string `trust_witness_utxo: true` (confirms embedded PoC honors the v1.3 Phase 12 invariant — this is the structural check that the PoC tested the real thing, not a strawman).
    - File contains the literal string `build_bip322_to_spend` (confirms PoC reused shared primitives — V1.4-MOD-07).
    - File contains a recommendation line containing `bdk path` OR `manual fallback per D-15` (Plan 14-03's grep target).
    - File embeds the FULL PoC source (length check: typically > 30 lines for a Rust binary with imports, main, and 8 steps; reject if embedded source block is < 15 lines).
    - On PASS: file contains a 128-char hex string somewhere in the witness-hex section (regex `[0-9a-f]{128}`).
    - On FAIL or INCONCLUSIVE: file contains an explicit `(none — bdk did not produce a tap_key_sig)` or equivalent disclaimer.
    - `git log main --oneline -- .planning/research/sprint-0-B.md` shows the file landed in main.
    - The spike PoC file is NOT in main's history — empty-input-guarded check: `LATEST=$(git log main --oneline -- client/examples/spike-p2tr.rs | grep -v "^$" | head -1); [ -z "$LATEST" ] || ! echo "$LATEST" | grep -q "spike(14-B)"`. Passes via short-circuit when there are zero commits touching the file (the desired state — D-21 invariant).
  </acceptance_criteria>
  <done>sprint-0-B.md exists in main with verbatim PoC source, captured stdout, witness hex (or explicit absence), verdict line at column 0, and a recommendation matching one of three exact strings. main's `client/examples/spike-p2tr.rs` does not exist (CD-4 + D-21). ADR Decision #4 has its input contract.</done>
</task>

<task type="auto">
  <name>Task 3: Push spike branch to origin and verify structural D-21 invariant</name>
  <files>(no file modifications — git remote push + structural diff verification only)</files>
  <read_first>
    - .planning/phases/14-sprint-0-spikes-discuss-phase-decisions/14-CONTEXT.md (D-19 branch hygiene, D-21 structural invariant)
  </read_first>
  <action>
    Push the spike branch to `origin` for reproducibility (D-19):
    `git push -u origin spike/14-B-bdk-p2tr-poc`

    Switch back to main:
    `git checkout main`

    Verify the D-21 structural invariant for THIS plan (narrow check — only this plan's contributions to main):
    1. `git log main --oneline -- coordinator/ client/ shared/ liquidity-bot/` since plan start MUST be empty (no production-code commits on main from this plan).
    2. `git log main -- .planning/research/sprint-0-B.md | head -1` MUST show the sprint-0-B.md commit landed in main.
    3. Confirm `client/examples/spike-p2tr.rs` does not exist on `main`: `test ! -f client/examples/spike-p2tr.rs && echo OK` (the PoC file lives only on the spike branch per CD-4).
    4. Empty-input-guarded structural check for the spike(14-B) contamination test on the PoC path (handles the "no commits = desired state" case where `head -1` yields empty input):
       ```
       LATEST=$(git log main --oneline -- client/examples/spike-p2tr.rs | grep -v "^$" | head -1)
       [ -z "$LATEST" ] || ! echo "$LATEST" | grep -q "spike(14-B)"
       ```
       A truly-empty `git log` (no commits ever touched `client/examples/spike-p2tr.rs` on main — the desired state) passes via the `[ -z "$LATEST" ]` short-circuit; a populated log fails iff the most-recent commit was a `spike(14-B)` commit.
  </action>
  <verify>
    <automated>git ls-remote --heads origin spike/14-B-bdk-p2tr-poc | grep -q "refs/heads/spike/14-B-bdk-p2tr-poc" && test ! -f client/examples/spike-p2tr.rs && git log main -- .planning/research/sprint-0-B.md | head -1 | grep -q commit && LATEST=$(git log main --oneline -- client/examples/spike-p2tr.rs | grep -v "^$" | head -1) && { [ -z "$LATEST" ] || ! echo "$LATEST" | grep -q "spike(14-B)"; }</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-remote --heads origin spike/14-B-bdk-p2tr-poc` returns a ref (branch pushed successfully).
    - main's `client/examples/spike-p2tr.rs` does not exist (CD-4 + D-21 — PoC lives only on spike branch).
    - main contains `.planning/research/sprint-0-B.md` as the canonical record.
    - `git log main --oneline -- coordinator/ client/ shared/ liquidity-bot/` since plan start is empty (D-21 structural invariant).
    - Empty-input-guarded spike-contamination check passes: zero commits touching `client/examples/spike-p2tr.rs` on main is the desired state.
  </acceptance_criteria>
  <done>Spike branch reproducibly fetchable from origin; main's production-code paths AND `client/examples/spike-p2tr.rs` are untouched by this plan; canonical record sprint-0-B.md is in main with the input contract for Plan 14-03's ADR Decision #4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike branch `client/examples/spike-p2tr.rs` → main | Throwaway PoC binary must not contaminate main's release artifact (D-19, CD-4 — `examples/` is Cargo's convention for throwaway code excluded from `cargo build --release`). PoC location locked under `client/` member crate so Cargo's example auto-discovery picks it up without workspace-root surgery. |
| PoC PSBT invariants → recorded verdict integrity | If the PoC omits `trust_witness_utxo: true` or uses zero `witness_utxo` placeholders, the verdict tests a strawman and Phase 17's planning input is poisoned (v1.3 Phase 12 lesson — load-bearing) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | Tampering | spike branch `client/examples/spike-p2tr.rs` accidentally cherry-picked into main | mitigate | Task 2 commits sprint-0-B.md doc-file alone (not client/examples/); Task 3 verifies `test ! -f client/examples/spike-p2tr.rs` on main + empty-input-guarded check that no `spike(14-B)` commit touches the file on main; structural D-21 gate in Plan 14-03 catches any drift at phase close |
| T-14-06 | Tampering | PoC silently tests a strawman (omits `trust_witness_utxo: true` or uses zero placeholders for `witness_utxo`), producing a false PASS or false FAIL verdict | mitigate | Task 1 acceptance criterion requires `grep -q 'trust_witness_utxo: true' client/examples/spike-p2tr.rs`; Task 2 acceptance criterion requires the embedded source in sprint-0-B.md ALSO contain `trust_witness_utxo: true`; v1.3 Phase 12 CONTEXT.md is mandatory read-first for both tasks |
| T-14-07 | Repudiation | sprint-0-B.md records a verdict but the PoC source / stdout is summarized away, leaving no way to audit the decision later | mitigate | CD-1 + Task 2 acceptance criterion forces VERBATIM full embedded PoC source (> 15 lines) and full stdout; reviewer can re-derive verdict from the embedded source and run it locally per the reproducibility section |
| T-14-08 | Information Disclosure | PoC seed is hardcoded `[0u8; 32]` and pushed to origin | accept | Throwaway PoC, deterministic by design; never used for production key material; documented inline as throwaway |

Severity: LOW across the board. PoC runs in an isolated Rust binary on a throwaway branch; main's attack surface is unchanged.
</threat_model>

<verification>
- `test -f .planning/research/sprint-0-B.md`
- `grep -E '^(PASS|FAIL|INCONCLUSIVE):' .planning/research/sprint-0-B.md` succeeds
- `grep -q 'trust_witness_utxo: true' .planning/research/sprint-0-B.md` succeeds (embedded PoC honors v1.3 Phase 12 invariant)
- `grep -q 'build_bip322_to_spend' .planning/research/sprint-0-B.md` succeeds (embedded PoC reuses shared primitives — V1.4-MOD-07)
- `grep -E '(bdk path|manual fallback per D-15)' .planning/research/sprint-0-B.md` succeeds (recommendation line present)
- `git ls-remote --heads origin spike/14-B-bdk-p2tr-poc | grep -q "refs/heads/spike/14-B-bdk-p2tr-poc"` succeeds
- `test ! -f client/examples/spike-p2tr.rs` succeeds on main (CD-4 + D-21 — PoC lives only on spike branch)
- Empty-input-guarded structural check succeeds: `LATEST=$(git log main --oneline -- client/examples/spike-p2tr.rs | grep -v "^$" | head -1); [ -z "$LATEST" ] || ! echo "$LATEST" | grep -q "spike(14-B)"` (zero commits touching the file is the desired state; non-empty log with a non-`spike(14-B)` head commit also passes)
- `git log main --oneline -- coordinator/ client/ shared/ liquidity-bot/` since plan start is empty (D-21)
</verification>

<success_criteria>
- sprint-0-B.md exists in main with verbatim PoC source, captured stdout, witness hex (128 chars on PASS), and column-0 verdict
- Verdict line is exactly one of `PASS:`, `FAIL:`, or `INCONCLUSIVE:`
- Recommendation line contains `bdk path` (PASS) or `manual fallback per D-15` (FAIL/INCONCLUSIVE)
- Embedded PoC source proves `trust_witness_utxo: true` + real `witness_utxo` (no v1.3 Phase 12 strawman)
- Spike branch pushed to origin, never merged
- main's `coordinator/`, `client/`, `shared/`, `liquidity-bot/` paths are unmodified by this plan AND `client/examples/spike-p2tr.rs` does not exist on main
- Plan 14-03 can grep sprint-0-B.md's verdict + recommendation lines and set ADR Decision #4 STATUS deterministically
- Sprint completed within 2-day cap OR explicitly marked INCONCLUSIVE on cap-reached (D-18); inconclusive defaults downstream to manual fallback per D-15
</success_criteria>

<output>
Create `.planning/phases/14-sprint-0-spikes-discuss-phase-decisions/14-02-SUMMARY.md` when done. SUMMARY records: branch SHA pushed, verdict ({PASS|FAIL|INCONCLUSIVE}), witness hex (or absence), recommendation (`bdk path` or `manual fallback per D-15`), downstream input contract for Plan 14-03 (which STATUS line Decision #4 will get).
</output>
