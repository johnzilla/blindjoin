---
phase: 14-sprint-0-spikes-discuss-phase-decisions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/sprint-0-A.md
autonomous: true
requirements: []
tags:
  - bip322
  - cargo-tree
  - cargo-audit
  - spike

must_haves:
  truths:
    - "sprint-0-A.md exists at .planning/research/sprint-0-A.md and embeds the FULL `cargo tree -p bip322 -e normal --format \"{p} {f}\"` output verbatim (per CD-1)"
    - "sprint-0-A.md embeds the FULL `cargo audit` output verbatim (per CD-1)"
    - "sprint-0-A.md records an explicit GO / NO-GO / INCONCLUSIVE verdict against each of the 3 D-02 gates: (1) bitcoin = 0.32.x in the transitive tree, (2) cargo audit clean on new edges, (3) adapter < 50 LOC with zero lossy conversions"
    - "sprint-0-A.md records the adapter LOC estimate (a concrete integer) AND the zero-lossy-conversion check (presence/absence of `unwrap_or` / field-shape squashing in the sketched adapter)"
    - "Spike branch `spike/14-A-bip322-cargo-tree` exists locally AND has been pushed to `origin` for reproducibility (D-19)"
    - "Spike branch is NOT merged into `main`; no commits touching `coordinator/`, `client/`, `shared/`, or `liquidity-bot/` source land in `main` from this plan (D-21)"
    - "Sprint cap of 2 calendar days (D-18) respected; on cap-with-no-conclusion, verdict line reads `INCONCLUSIVE:` and downstream defaults to EXTEND (D-03)"
  artifacts:
    - path: ".planning/research/sprint-0-A.md"
      provides: "Canonical Sprint-0-A record (cargo tree, cargo audit, 3-gate verdict, adapter LOC sketch)"
      contains: "cargo tree -p bip322"
  key_links:
    - from: ".planning/research/sprint-0-A.md"
      to: ".planning/decisions/v1.4-adr.md"
      via: "verdict consumed by Plan 14-03 to set ADR Decision #1 STATUS"
      pattern: "(GO|NO-GO|INCONCLUSIVE):"
    - from: "spike branch `spike/14-A-bip322-cargo-tree`"
      to: "origin remote"
      via: "git push origin spike/14-A-bip322-cargo-tree"
      pattern: "spike/14-A-bip322-cargo-tree"
---

<objective>
Run Sprint-0-A: a timeboxed `cargo tree` + `cargo audit` probe against `bip322 = "=0.0.10"` on a throwaway branch, and write the canonical record at `.planning/research/sprint-0-A.md` containing the full command output and a GO/NO-GO verdict against the three D-02 gates. Resolves Open Decision #1 (adopt the `bip322` crate vs extend our custom `shared/src/bip322.rs`).

Purpose: Determine whether the `bip322` crate's transitive dependency graph is compatible with our pinned `bitcoin = 0.32.x` stack, free of cargo-audit advisories on the new edges, and whether a wire-shape adapter over its `verify_simple(&Address, message, Witness)` API can land in under 50 LOC with zero lossy conversions (per D-02). Sprint-0-A's verdict is the input contract for Plan 14-03's ADR Decision #1 STATUS line.

Output: `.planning/research/sprint-0-A.md` checked into `main`. Spike branch `spike/14-A-bip322-cargo-tree` pushed to `origin` for reproducibility but NEVER merged into `main` (D-19).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-sprint-0-spikes-discuss-phase-decisions/14-CONTEXT.md
@.planning/research/SUMMARY.md
@shared/src/bip322.rs
@shared/Cargo.toml

<interfaces>
<!-- Existing primitives in shared/src/bip322.rs (133 LOC, script-type-NEUTRAL per CONTEXT.md). -->
<!-- The adapter sketched in Task 2 must wrap the bip322 crate's verify_simple API to a wire shape compatible with these primitives. -->
<!-- The adapter does NOT need to be runnable code in this plan — only counted for LOC and audited for lossy conversions. -->

From shared/src/bip322.rs (existing, 133 LOC):
- `bip322_message_hash(message: &[u8]) -> sha256::Hash` — script-type-neutral, single source of truth (V1.4-MOD-07)
- `build_bip322_to_spend(script_pubkey: &Script, message_hash: sha256::Hash) -> Transaction`
- `build_bip322_to_sign(to_spend: &Transaction) -> Transaction`
- Plus existing P2WPKH verify/sign helpers (carried forward into `shared/src/bip322/p2wpkh.rs` in Phase 15 per D-04)

Target wire shape (from shared/src/protocol.rs:13-28 — read-only reference for this plan):
- `OwnershipProof { script_pubkey: ScriptBuf, witness: Witness, message: Vec<u8> }` (v1.3 shape; v1.4 extends per D-11/D-12 in Phase 15)

Hypothesized adapter shape over bip322 crate's API (sketch only; not committed to main):
- `fn verify_from_wire(spk: &Script, witness: &Witness, message: &[u8]) -> Result<(), Bip322Error>`
- Body wraps `bip322::verify_simple(&Address::from_script(spk, network)?, message, witness.clone())`
- LOC budget per D-02 gate 3: < 50 LOC including imports + error mapping
- Lossy-conversion check: forbids `unwrap_or`, `unwrap_or_default`, field-shape squashing (e.g., dropping witness items, collapsing Address variants)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create throwaway spike branch and add bip322 = "=0.0.10" to shared/Cargo.toml</name>
  <files>shared/Cargo.toml (modified on `spike/14-A-bip322-cargo-tree` branch ONLY — NEVER committed to main per D-19)</files>
  <read_first>
    - .planning/phases/14-sprint-0-spikes-discuss-phase-decisions/14-CONTEXT.md (D-01, D-02, D-17, D-18, D-19, D-21)
    - .planning/research/SUMMARY.md §"Open Decision #1"
    - shared/Cargo.toml (current dependencies — confirm bip322 is NOT already present)
  </read_first>
  <action>
    From `main` at HEAD, create and check out a new branch:
    `git checkout -b spike/14-A-bip322-cargo-tree`

    Edit `shared/Cargo.toml` and add a single line under `[dependencies]`:
    `bip322 = "=0.0.10"`

    Exact-pin per v1.3 REPAIR-02 (carry-forward constraint #3 in STATE.md). Do not use `^0.0.10`, `~0.0.10`, or `0.0.10` — must be `"=0.0.10"`.

    Stage and commit ON THE SPIKE BRANCH ONLY:
    `git add shared/Cargo.toml && git commit -m "spike(14-A): add bip322 = \"=0.0.10\" dep for cargo tree probe"`

    DO NOT merge, rebase, or cherry-pick this commit onto `main`. The branch is throwaway per D-19.
  </action>
  <verify>
    <automated>git rev-parse --abbrev-ref HEAD | grep -qx "spike/14-A-bip322-cargo-tree" && grep -E '^bip322 = "=0\.0\.10"$' shared/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - Current branch is exactly `spike/14-A-bip322-cargo-tree` (no other branch name accepted).
    - `shared/Cargo.toml` contains the line `bip322 = "=0.0.10"` exactly (exact-pin format per REPAIR-02).
    - `git log main..HEAD --oneline` shows exactly one commit on the spike branch touching `shared/Cargo.toml` (no other files).
    - The spike commit is NOT in main's history — verified with empty-input-guarded check: `LATEST=$(git log main --oneline -- shared/Cargo.toml | grep -v "^$" | head -1); [ -z "$LATEST" ] || ! echo "$LATEST" | grep -q "spike(14-A)"`.
  </acceptance_criteria>
  <done>Spike branch created from main, `bip322 = "=0.0.10"` added to `shared/Cargo.toml` on the branch and committed, main's `shared/Cargo.toml` is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Capture cargo tree + cargo audit + adapter LOC sketch and write sprint-0-A.md verdict</name>
  <files>.planning/research/sprint-0-A.md (NEW, COMMITTED TO MAIN — the canonical record per D-19/CD-1)</files>
  <read_first>
    - .planning/phases/14-sprint-0-spikes-discuss-phase-decisions/14-CONTEXT.md (D-02 gates 1/2/3, D-03 ambiguous policy, CD-1 full-output bias, D-18 2-day cap)
    - .planning/research/SUMMARY.md §"Open Decision #1" and §"Pitfalls Watchlist"
    - shared/src/bip322.rs (count existing custom impl LOC for adapter comparison — currently 133 LOC)
    - shared/src/protocol.rs lines 13-28 (current `OwnershipProof` wire shape)
  </read_first>
  <action>
    Still on `spike/14-A-bip322-cargo-tree`. Run the three D-02 gate probes and capture verbatim output:

    1. **Gate 1 (bitcoin pin):** Run `cargo tree -p bip322 -e normal --format "{p} {f}"` from the repo root. Capture the entire stdout verbatim. Grep the output for `bitcoin v0.32` — presence = gate 1 PASS, absence (or `bitcoin v0.31` or earlier) = gate 1 FAIL.

    2. **Gate 2 (cargo audit):** Run `cargo audit` from the repo root (operates on `Cargo.lock` produced by the spike-branch build). Capture entire stdout + stderr verbatim. Exit code 0 with no advisories on transitive bip322 edges = gate 2 PASS. Any advisory whose dependency path includes `bip322` or one of its NEW transitive crates (crates not already in main's `Cargo.lock`) = gate 2 FAIL. Pre-existing advisories on edges that were already in main's lockfile are NOT this spike's concern.

    3. **Gate 3 (adapter < 50 LOC, zero lossy):** Sketch the adapter in a scratch comment block (do NOT commit the sketch as Rust code — this is paper analysis). Concrete shape: a single function `verify_from_wire(spk: &Script, witness: &Witness, message: &[u8]) -> Result<(), Bip322Error>` that wraps `bip322::verify_simple(&Address::from_script(spk, Network::Signet)?, message, witness.clone())`, plus minimal error mapping. Count: import lines + function signature + body lines (excluding blank lines and `//` comments). Audit: scan the sketched body for `unwrap_or`, `unwrap_or_default`, `unwrap_or_else`, and any place where a `Witness` field, `Address` variant, or message slice is dropped/collapsed. PASS = LOC < 50 AND zero of the banned patterns appear. FAIL = either condition violated.

    Write `.planning/research/sprint-0-A.md` on the spike branch with this exact structure (embed FULL captured output per CD-1 — do not abbreviate):

    ```
    # Sprint-0-A: bip322 0.0.10 cargo tree probe

    **Branch:** spike/14-A-bip322-cargo-tree (pushed to origin; NOT merged to main per D-19)
    **Pinned dep added:** bip322 = "=0.0.10" in shared/Cargo.toml (spike branch only)
    **Date:** <YYYY-MM-DD>
    **Sprint cap:** 2 days (D-18)

    ## Gate 1 — bitcoin = 0.32.x in transitive tree (D-02 #1)

    ### Command
    `cargo tree -p bip322 -e normal --format "{p} {f}"`

    ### Output (verbatim)
    ```
    <full cargo tree output, every line>
    ```

    ### Result
    <PASS | FAIL> — <one-line explanation: e.g., "bitcoin v0.32.7 present at depth 1" OR "bitcoin v0.31.x in tree at depth N — fails 0.32.x pin">

    ## Gate 2 — cargo audit clean on new edges (D-02 #2)

    ### Command
    `cargo audit`

    ### Output (verbatim)
    ```
    <full cargo audit output, every line, both stdout and stderr>
    ```

    ### Result
    <PASS | FAIL> — <one-line explanation: e.g., "no advisories on bip322 transitive edges" OR "RUSTSEC-YYYY-NNNN on <crate> via bip322 → ...">

    ## Gate 3 — adapter < 50 LOC, zero lossy conversions (D-02 #3)

    ### Sketched adapter
    ```rust
    <the function sketch — NOT committed as a .rs file; lives only in this markdown>
    ```

    ### LOC count
    <integer count of non-blank, non-comment lines including imports + signature + body>

    ### Lossy-conversion audit
    - `unwrap_or` / `unwrap_or_default` / `unwrap_or_else` occurrences: <integer>
    - Field-shape squashing (witness items dropped, Address variant collapsed, message slice copied lossily): <yes / no with one-line justification>

    ### Result
    <PASS | FAIL> — <one-line explanation>

    ## Overall verdict

    <GO | NO-GO | INCONCLUSIVE>: <one-line summary>

    - GO = all three gates PASS → ADR Decision #1 flips from default EXTEND to ACCEPTED-ADOPT
    - NO-GO = any gate FAILs → ADR Decision #1 stays at default ACCEPTED-EXTEND per D-03
    - INCONCLUSIVE = 2-day cap reached without a determinative result on one or more gates → ADR Decision #1 defaults to ACCEPTED-EXTEND per D-03 (D-18 timebox)

    ## Reproducibility
    - Spike branch HEAD SHA: <git rev-parse HEAD>
    - bip322 = "=0.0.10" added to shared/Cargo.toml on the spike branch (one-line edit; see commit `spike(14-A): add bip322 = "=0.0.10" dep for cargo tree probe`)
    - To reproduce locally: `git fetch origin spike/14-A-bip322-cargo-tree && git checkout spike/14-A-bip322-cargo-tree && cargo tree -p bip322 -e normal --format "{p} {f}"`
    ```

    The verdict line MUST start at column 0 with one of the exact strings `GO:`, `NO-GO:`, or `INCONCLUSIVE:` so Plan 14-03's grep can detect it deterministically.

    Commit `sprint-0-A.md` on the spike branch first (`spike(14-A): capture cargo tree, audit, adapter sketch — verdict <X>`), then cherry-pick that single doc-file commit onto `main` (or recreate it on main; the file is the canonical record per D-19 and must land in main's history independent of the spike branch). Verify the spike branch's `shared/Cargo.toml` edit does NOT come along with the cherry-pick — only `.planning/research/sprint-0-A.md`.
  </action>
  <verify>
    <automated>test -f .planning/research/sprint-0-A.md && grep -E '^(GO|NO-GO|INCONCLUSIVE):' .planning/research/sprint-0-A.md && grep -v '^#' .planning/research/sprint-0-A.md | grep -q 'cargo tree -p bip322' && grep -v '^#' .planning/research/sprint-0-A.md | grep -q 'cargo audit'</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/research/sprint-0-A.md` exists.
    - File contains a verdict line matching `^(GO|NO-GO|INCONCLUSIVE):` at column 0 (Plan 14-03 greps this).
    - File contains the literal string `cargo tree -p bip322` at least once outside of `#`-prefixed lines (full command captured per CD-1).
    - File contains the literal string `cargo audit` at least once outside of `#`-prefixed lines (full command captured per CD-1).
    - File embeds the FULL cargo tree stdout (not a summary; not "see branch") — measurable by length: cargo tree output for a crate with non-trivial deps is typically > 20 lines; reject if the captured block is < 5 lines.
    - File embeds the FULL cargo audit output (similarly: > 5 lines or an explicit "0 advisories found" terminal line).
    - File records the adapter LOC count as a concrete integer in the `### LOC count` block (regex `^[0-9]+$` on its own line, or inline numeric).
    - File records the lossy-conversion audit with explicit `unwrap_or` / field-shape findings.
    - `git log main --oneline -- .planning/research/sprint-0-A.md` shows the file landed in main.
    - The spike's Cargo.toml edit did NOT come over with the cherry-pick — verified with empty-input-guarded check: `LATEST=$(git log main --oneline -- shared/Cargo.toml | grep -v "^$" | head -1); [ -z "$LATEST" ] || ! echo "$LATEST" | grep -q "spike(14-A)"` (only the sprint-0-A.md file landed in main per D-21).
  </acceptance_criteria>
  <done>sprint-0-A.md exists in main with full cargo tree output, full cargo audit output, adapter LOC sketch + integer count, zero-lossy-conversion audit, and a column-0 verdict line. main's `shared/Cargo.toml` is unchanged. ADR Decision #1 has its input contract.</done>
</task>

<task type="auto">
  <name>Task 3: Push spike branch to origin and verify structural D-21 invariant</name>
  <files>(no file modifications — git remote push + structural diff verification only)</files>
  <read_first>
    - .planning/phases/14-sprint-0-spikes-discuss-phase-decisions/14-CONTEXT.md (D-19 branch hygiene, D-21 structural invariant)
  </read_first>
  <action>
    Push the spike branch to `origin` for reproducibility (D-19):
    `git push -u origin spike/14-A-bip322-cargo-tree`

    Then verify the D-21 structural invariant from main:
    `git checkout main`
    `git diff main..spike/14-A-bip322-cargo-tree -- coordinator/ client/ shared/ liquidity-bot/` — this WILL show the `shared/Cargo.toml` line addition on the spike side, which is fine (the spike branch is allowed to differ from main on `shared/`); this command is just diagnostic, NOT a gate.

    The D-21 gate this plan is responsible for is narrower: confirm that the commits this plan lands ON MAIN do not touch `coordinator/`, `client/`, `shared/`, or `liquidity-bot/`. Verify:
    `git log main --oneline --since="<plan start date>" -- coordinator/ client/ shared/ liquidity-bot/`

    Output MUST be empty for the time window covering this plan's execution. If non-empty, an unintended commit slipped through and must be investigated before declaring the plan done.

    Empty-input-guarded structural check for the spike(14-A) contamination test (handles the "no commits = desired state" case where `head -1` yields empty input):
    ```
    LATEST=$(git log main --oneline -- shared/Cargo.toml | grep -v "^$" | head -1)
    [ -z "$LATEST" ] || ! echo "$LATEST" | grep -q "spike(14-A)"
    ```
    A truly-empty `git log` (no commits touched `shared/Cargo.toml` since repo init) passes via the `[ -z "$LATEST" ]` short-circuit; a populated log fails iff the most-recent commit was a `spike(14-A)` commit.
  </action>
  <verify>
    <automated>git ls-remote --heads origin spike/14-A-bip322-cargo-tree | grep -q "refs/heads/spike/14-A-bip322-cargo-tree" && LATEST=$(git log main --oneline -- shared/Cargo.toml | grep -v "^$" | head -1) && { [ -z "$LATEST" ] || ! echo "$LATEST" | grep -q "spike(14-A)"; }</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-remote --heads origin spike/14-A-bip322-cargo-tree` returns a ref (branch pushed successfully).
    - The most recent commit on main's `shared/Cargo.toml` is NOT a `spike(14-A)` commit (the spike's Cargo.toml edit did not contaminate main). Empty-input-guarded — passes when there are zero commits touching `shared/Cargo.toml` at all (the desired state for a doc-only milestone bump).
    - `git log main -- .planning/research/sprint-0-A.md | head -1` shows the sprint-0-A.md commit is in main (so the canonical record landed where it belongs).
  </acceptance_criteria>
  <done>Spike branch reproducibly fetchable from origin; main's production-code paths (`coordinator/`, `client/`, `shared/`, `liquidity-bot/`) are untouched by this plan; canonical record sprint-0-A.md is in main.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike branch → main | Throwaway Cargo.toml edit must not contaminate main's production dependency graph (D-19, D-21) |
| transitive deps of bip322 0.0.10 → our supply chain | New crates introduced by adopting bip322 could carry CVE advisories that downstream Phase 15 inherits (D-02 gate #2) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | spike branch `shared/Cargo.toml` edit accidentally cherry-picked into main | mitigate | Task 2 commits sprint-0-A.md doc-file alone (not Cargo.toml) when landing on main; Task 3 verifies (empty-input-guarded) that the most-recent `git log main -- shared/Cargo.toml` commit is NOT a `spike(14-A)` commit; structural D-21 gate in Plan 14-03 catches any drift at phase close |
| T-14-02 | Tampering | bip322 0.0.10 transitive deps introduce supply-chain CVE | mitigate | D-02 gate #2 (cargo audit) must PASS on the spike branch's lockfile; any advisory on a NEW transitive edge fails the gate and forces EXTEND per D-03 (no relitigation) |
| T-14-03 | Repudiation | sprint-0-A.md records a verdict but the cargo tree / audit output is summarized away, leaving no way to audit the decision later | mitigate | CD-1 + Task 2 acceptance criterion forces VERBATIM full output (cargo tree captured >5 lines; cargo audit captured >5 lines or explicit "0 advisories" line); reviewer can re-derive verdict from the embedded output |
| T-14-04 | Information Disclosure | spike branch HEAD SHA published in sprint-0-A.md leaks no secrets | accept | No private keys, no PII; SHA is intentional reproducibility metadata per D-19 |

Severity: LOW across the board. This is a paper-analysis plan with zero attack surface in production code.
</threat_model>

<verification>
- `test -f .planning/research/sprint-0-A.md`
- `grep -E '^(GO|NO-GO|INCONCLUSIVE):' .planning/research/sprint-0-A.md` succeeds (verdict line present)
- `grep -v '^#' .planning/research/sprint-0-A.md | grep -q 'cargo tree -p bip322'` succeeds (filter out comment-prefix lines to avoid self-invalidation from this plan's own prose in any sibling docs)
- `grep -v '^#' .planning/research/sprint-0-A.md | grep -q 'cargo audit'` succeeds
- `git ls-remote --heads origin spike/14-A-bip322-cargo-tree | grep -q "refs/heads/spike/14-A-bip322-cargo-tree"` succeeds
- Empty-input-guarded structural check succeeds: `LATEST=$(git log main --oneline -- shared/Cargo.toml | grep -v "^$" | head -1); [ -z "$LATEST" ] || ! echo "$LATEST" | grep -q "spike(14-A)"` (spike's Cargo.toml edit did NOT contaminate main; D-21 invariant holds; handles the "zero commits" base case correctly)
- `git log main --oneline -- coordinator/ client/ shared/ liquidity-bot/` since plan start shows zero new commits (D-21 structural invariant)
</verification>

<success_criteria>
- sprint-0-A.md exists in main with verbatim cargo tree + cargo audit output and a GO/NO-GO/INCONCLUSIVE verdict against all 3 D-02 gates
- Adapter LOC count is a concrete integer; lossy-conversion audit is explicit (not "n/a")
- Spike branch pushed to origin, never merged
- main's `coordinator/`, `client/`, `shared/`, `liquidity-bot/` paths are unmodified by this plan
- Plan 14-03 can grep sprint-0-A.md's verdict line and set ADR Decision #1 STATUS deterministically
- Sprint completed within 2-day cap OR explicitly marked INCONCLUSIVE on cap-reached (D-18); inconclusive defaults downstream to EXTEND per D-03
</success_criteria>

<output>
Create `.planning/phases/14-sprint-0-spikes-discuss-phase-decisions/14-01-SUMMARY.md` when done. SUMMARY records: branch SHA pushed, verdict ({GO|NO-GO|INCONCLUSIVE}), per-gate pass/fail, adapter LOC integer, downstream input contract for Plan 14-03 (which STATUS line Decision #1 will get).
</output>
