---
phase: 25-reproducible-build-recipe-scheduled-verifier-registry
plan: 05
type: execute
wave: 3
depends_on:
  - 25-04
files_modified:
  - docs/RELEASING.md
  - SECURITY.md
autonomous: true
requirements:
  - REPRO-04
tags: [docs, releasing, security-md, registry-submission, rehearsal-procedure, draft-cleanup]

must_haves:
  truths:
    - "`docs/RELEASING.md` has been restructured for the D-13 SIGN-03 deferral cleanup: the lede at L5 no longer references draft-flipping; the step-3 `Pre-flight check` link no longer says \"before flipping out of draft\"; old step 4 `Flip the release out of draft` is replaced with `Verify the published release` per RESEARCH Example 7; the section header `Pre-flight check before flipping out of draft` is renamed to `Pre-flight check after CI completes`; all 4 `--draft=false` literal references are removed (L34 was the canonical occurrence)."
    - "`docs/RELEASING.md` has a new H2 section `## Reproducibility verification rehearsal (v1.6.0-rc.0 procedure)` per D-10: appends after the existing Pre-flight check section; documents the 5-step rehearsal that (1) triggers `.github/workflows/reproducible-verify.yml` via workflow_dispatch, (2) captures the ImageVersion + sha256 from the workflow run, (3) atomically replaces `<TBD-v1.6.0-cut>` in BOTH `docs/REPRODUCIBLE-BUILD.md` Toolchain pins ImageVersion AND Expected sha256sum tables AND in `docs/REPRODUCIBLE-BUILD.expected-sha256.txt`, (4) commits with citation to the rc.0 cut, (5) tags v1.6.0 and pushes."
    - "`docs/RELEASING.md` has a new H2 section `## Reproducible-builds.org registry submission` per D-14: appends after the rehearsal section; documents the 4-step manual maintainer procedure (verify ≥1 green monthly cycle → fork reproducible-builds.org repo + add YAML entry → open PR + respond to review → after merge link the registry entry from docs/REPRODUCIBLE-BUILD.md §Continuous verification AND SECURITY.md §Supply-chain status §Reproducibility)."
    - "`SECURITY.md` `## Supply-chain status` has a new H3 subsection `### Reproducibility (v1.6 onward)` per D-19: inserted between L231 (end of `### Release tarball signatures + provenance (v1.6 onward)`) and L232 (start of `### Base-image digests (v1.6 onward)`); contains 1 paragraph + 1 fenced \"how to verify yourself\" bash block; cross-links to `docs/REPRODUCIBLE-BUILD.md` + `.github/workflows/reproducible-verify.yml` + placeholder for reproducible-builds.org registry entry URL (filled in after D-14 lands)."
    - "`SECURITY.md` `### Known gaps at v1.5` bullet at L106-110 is updated with strikethrough + closed-status pointer per RESEARCH (PATTERNS.md L472): `- **~~No reproducible-build pipeline.~~** **Closed in v1.6 Phase 25** — see [Reproducibility (v1.6 onward)](#reproducibility-v16-onward).`"
    - "`SECURITY.md` `### v1.6 supply-chain plan` third bullet at L281-283 (Reproducible-build instructions) is updated with strikethrough + closed-status pointer mirroring the Phase 22/23 pattern at L284: `- **~~Reproducible-build instructions~~** ✓ Shipped in Phase 25 — see [Reproducibility (v1.6 onward)](#reproducibility-v16-onward).`"
    - "Forbidden-token absence audits: `! grep -- '--draft=false' docs/RELEASING.md` returns zero matches; `! grep -i 'flip.*out of draft' docs/RELEASING.md` returns zero matches; the prose `flipping the GitHub Release out of draft` is gone from L5."
  artifacts:
    - path: "docs/RELEASING.md"
      provides: "D-13 cleanup (no --draft=false, no flip-out-of-draft prose)"
      contains: "Verify the published release"
    - path: "docs/RELEASING.md"
      provides: "D-10 rehearsal section (v1.6.0-rc.0 procedure)"
      contains: "## Reproducibility verification rehearsal"
    - path: "docs/RELEASING.md"
      provides: "D-14 registry submission section"
      contains: "## Reproducible-builds.org registry submission"
    - path: "SECURITY.md"
      provides: "D-19 ### Reproducibility (v1.6 onward) subsection"
      contains: "### Reproducibility (v1.6 onward)"
    - path: "SECURITY.md"
      provides: "Known-gaps strikethrough for reproducible-build pipeline"
      contains: "~~No reproducible-build pipeline.~~"
    - path: "SECURITY.md"
      provides: "v1.6 supply-chain plan strikethrough for shipped Reproducible-build instructions"
      contains: "Shipped in Phase 25"
  key_links:
    - from: "SECURITY.md ### Reproducibility (v1.6 onward)"
      to: "docs/REPRODUCIBLE-BUILD.md"
      via: "markdown cross-link"
      pattern: "REPRODUCIBLE-BUILD\\.md"
    - from: "SECURITY.md ### Reproducibility (v1.6 onward)"
      to: ".github/workflows/reproducible-verify.yml"
      via: "markdown cross-link"
      pattern: "reproducible-verify\\.yml"
    - from: "docs/RELEASING.md ## Reproducibility verification rehearsal"
      to: ".github/workflows/reproducible-verify.yml workflow_dispatch entry"
      via: "named workflow_dispatch trigger procedure"
      pattern: "workflow_dispatch"
    - from: "docs/RELEASING.md ## Reproducible-builds.org registry submission"
      to: "reproducible-builds.org/projects"
      via: "external HTTPS link in submission step 2"
      pattern: "reproducible-builds\\.org"
---

<objective>
Close the v1.6 milestone with the docs-and-procedure surface that REPRO-04 names: (a) the D-13 SIGN-03-deferral cleanup of `docs/RELEASING.md` (remove all `--draft=false` references + flip-out-of-draft prose; Plan 25-02 removed the YAML side; this plan removes the docs side); (b) the D-10 v1.6.0-rc.0 rehearsal procedure that captures the expected sha256 and ImageVersion via `workflow_dispatch` of `reproducible-verify.yml` and atomically replaces the `<TBD-v1.6.0-cut>` placeholders across both docs; (c) the D-14 reproducible-builds.org registry submission procedure (≥1 green monthly cycle prerequisite + manual fork-and-PR steps); (d) the D-19 SECURITY.md `### Reproducibility (v1.6 onward)` subsection cross-linking the Phase 25 surface from the operator-facing supply-chain status doc; (e) SECURITY.md strikethrough updates for the now-closed `No reproducible-build pipeline` gap and the now-shipped `Reproducible-build instructions` v1.6 plan item.

Purpose: REPRO-04 requires a documented maintainer procedure for the registry submission (the actual submission is the maintainer's action after observing ≥1 green monthly cycle; Phase 25 ships the procedure, not the executed submission — mirrors Phase 24's PGP-key-generation pattern). D-13 cleanup of `docs/RELEASING.md` is folded in because the YAML side (Plan 25-02) and the docs side must land in the same milestone — leaving a stale `--draft=false` reference in docs after Plan 25-02 ships the published-not-draft release would mislead the maintainer.

Output: 2 modified files: `docs/RELEASING.md` (currently 66 lines; post-mod ~120-150 lines with the two new sections + cleanup) and `SECURITY.md` (currently 290 lines; post-mod ~310-325 lines with the new subsection + strikethrough updates).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-reproducible-build-recipe-scheduled-verifier-registry/25-CONTEXT.md
@.planning/phases/25-reproducible-build-recipe-scheduled-verifier-registry/25-RESEARCH.md
@.planning/phases/25-reproducible-build-recipe-scheduled-verifier-registry/25-PATTERNS.md
@docs/RELEASING.md
@SECURITY.md
@docs/REPRODUCIBLE-BUILD.md
@docs/REPRODUCIBLE-BUILD.expected-sha256.txt
@.github/workflows/reproducible-verify.yml

<interfaces>
<!-- Inputs from Plan 25-04: -->
<!--   - .github/workflows/reproducible-verify.yml exists with workflow_dispatch trigger — the rehearsal procedure invokes this manually -->

<!-- Inputs from Plan 25-03: -->
<!--   - docs/REPRODUCIBLE-BUILD.md has `<TBD-v1.6.0-cut>` placeholder in both Toolchain pins (ImageVersion row) and Expected sha256sum tables -->
<!--   - docs/REPRODUCIBLE-BUILD.expected-sha256.txt has `v1.6.0:<TBD-v1.6.0-cut>` line -->
<!--   - The rehearsal procedure documents the atomic substitution across both files -->

<!-- Inputs from Plan 25-02: -->
<!--   - .github/workflows/release.yml no longer has draft: true — every tag push publishes immediately -->
<!--   - This is the precondition for removing the --draft=false references in docs/RELEASING.md -->

<!-- Existing patterns to mirror (verified at planning time 2026-06-02): -->
<!--   - SECURITY.md lines 113-180 (Image signatures + attestations subsection) — sibling H3 shape -->
<!--   - SECURITY.md lines 182-230 (Release tarball signatures + provenance subsection) — closest sibling for layout -->
<!--   - SECURITY.md line 104 (existing strikethrough pattern for SIGN-01 closure: `- **~~text~~** **Closed in v1.6 Phase 24** — see [link].`) — exact format to mirror for the No reproducible-build pipeline gap closure -->
<!--   - SECURITY.md line 284 (existing strikethrough pattern for DRIFT shipped: `- **~~Automated base-image digest drift check~~** ✓ Shipped in Phase 22 — see [link].`) — exact format to mirror for the Reproducible-build instructions v1.6 plan item -->
<!--   - docs/RELEASING.md current 66-line structure — Plan 25-05 restructures lines 5, 10, 27-34, 37-65 -->

<!-- Forbidden-token absence audits (per Plan 22-04 paraphrasing discipline + D-13 contract): -->
<!--   - `--draft=false` MUST NOT appear in docs/RELEASING.md post-mod -->
<!--   - `flipping the GitHub Release out of draft` MUST NOT appear in docs/RELEASING.md post-mod -->
<!--   - `flip the release out of draft` MUST NOT appear in docs/RELEASING.md post-mod -->
<!--   - The SECURITY.md insertion MUST NOT contain `ubuntu-latest` literal (paraphrasing if explaining the ubuntu-24.04 pin) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Restructure docs/RELEASING.md for D-13 cleanup + append D-10 rehearsal section + D-14 registry submission section</name>
  <files>docs/RELEASING.md</files>
  <read_first>
    - docs/RELEASING.md full file (66 lines — verify all `--draft=false` and `flip` occurrences at L5, 10, 18, 29, 31, 34, 37, 65)
    - .planning/phases/25-reproducible-build-recipe-scheduled-verifier-registry/25-CONTEXT.md §Decisions D-10 + D-13 + D-14 + §Specific Ideas (D-14 4-step procedure recommended shape)
    - .planning/phases/25-reproducible-build-recipe-scheduled-verifier-registry/25-RESEARCH.md §Code Examples Example 7 (D-13 docs-side cleanup verbatim replacement prose for step 4)
    - .planning/phases/25-reproducible-build-recipe-scheduled-verifier-registry/25-PATTERNS.md L376-437 (docs/RELEASING.md MOD pattern: D-13 cleanup line-by-line + D-10 rehearsal append + D-14 registry append)
    - docs/REPRODUCIBLE-BUILD.expected-sha256.txt (verify the line format `v1.6.0:<TBD-v1.6.0-cut>` so the rehearsal procedure cites the correct file)
  </read_first>
  <action>
    Apply three categories of modifications to `docs/RELEASING.md`:

    **(a) D-13 cleanup of existing 66-line content** (line numbers from current state — re-verify at execution time):
    - **L5 lede**: replace `This file documents the post-CI portion of the release cycle: pre-flight verifying the CI-built artifacts, then flipping the GitHub Release out of draft.` with `This file documents the post-CI portion of the release cycle: pre-flight verifying the CI-built artifacts before tagging, then rehearsing reproducibility at the v1.6.0-rc.0 cut to capture the expected sha256 hash.`
    - **L10 prerequisite**: replace `(see [Pre-flight check](#pre-flight-check-before-flipping-out-of-draft))` with `(see [Pre-flight check](#pre-flight-check-after-ci-completes))`. The cosign 2.6.3+ prerequisite line itself stays.
    - **L18 per-release-procedure lede**: replace `CI handles the signing + attestation; the maintainer drives the tag cut, the pre-flight verify, and the draft flip.` with `CI handles the signing + attestation; the maintainer drives the tag cut and the pre-flight verify. After Phase 25 (v1.6.0+), every tag push produces a non-draft GitHub Release immediately — no manual draft-flip step.`
    - **L27 step 2 prose**: change `CI creates a DRAFT GitHub Release with 4 assets` to `CI creates a published GitHub Release with 4 assets` (mirror — single word change; rest of the sentence stays).
    - **L29-35 (step 3 + step 4)**: replace per RESEARCH Example 7. Step 3 becomes: `3. **Run the [Pre-flight check](#pre-flight-check-after-ci-completes) below.** If any verify fails, recover via `gh release delete vX.Y.Z` and re-cut the tag after the fix.` Step 4 becomes (single short step): `4. **Verify the published release.** Once CI is green and pre-flight passes, the release is already published — operators can `gh release download vX.Y.Z` immediately.` The fenced bash block at L33-35 (`gh release edit vX.Y.Z --draft=false`) is DELETED entirely (the step is no longer about flipping).
    - **L37 section header**: rename `## Pre-flight check before flipping out of draft` to `## Pre-flight check after CI completes`.
    - **L39 section intro**: replace `Before running step 4's `--draft=false`, cosign-verify the CI-produced assets against the documented identity-regexp.` with `After CI completes, cosign-verify the CI-produced assets against the documented identity-regexp.` (drop the `--draft=false` reference).
    - **L65 closing sentence**: replace `**DO NOT** flip the release out of draft. Recovery: `gh release delete vX.Y.Z`, fix the underlying issue, re-push the tag.` with `recover via `gh release delete vX.Y.Z`, fix the underlying issue, and re-push the tag.` (Drop the flip-out-of-draft prose; the bash command itself stays.)

    **(b) APPEND a new H2 section `## Reproducibility verification rehearsal (v1.6.0-rc.0 procedure)` per D-10** after the Pre-flight check section (insert at the very end of the file, after L65). Use PATTERNS.md L408-421 template + RESEARCH §Open Question #2 procedure:

    Multi-line content covering:
    - 2-line lede: "Before the v1.6.0 cut, capture the expected sha256sum and runner ImageVersion that go into [docs/REPRODUCIBLE-BUILD.md](REPRODUCIBLE-BUILD.md) §Expected sha256sum and §Toolchain pins. The rehearsal runs the same verifier that monthly verification will use — this is the one-shot pre-tag dry run."
    - 5-step numbered procedure:
      1. Trigger `.github/workflows/reproducible-verify.yml` via `workflow_dispatch` from the Actions tab on the branch you intend to tag from. The workflow has no inputs.
      2. Wait for the run to complete. From the "Capture runner ImageVersion" step's logs, copy the `Verifier image: <VALUE>` line; from the "Compare sha256 + classify result" step, copy the `Actual (rebuilt locally): <SHA>` line. (Note: the first rehearsal run will go RED because the verifier compares against `<TBD-v1.6.0-cut>` placeholder, NOT a real sha256 — this is expected; capture the values, do not interpret the failure as a regression.)
      3. **Atomically substitute** the captured values across THREE locations:
         - `docs/REPRODUCIBLE-BUILD.md` §Expected sha256sum table: replace `<TBD-v1.6.0-cut>` with the captured sha256
         - `docs/REPRODUCIBLE-BUILD.md` §Toolchain pins table (ubuntu-24.04 row): replace `<TBD-v1.6.0-cut>` with the captured ImageVersion
         - `docs/REPRODUCIBLE-BUILD.expected-sha256.txt`: replace `v1.6.0:<TBD-v1.6.0-cut>` with `v1.6.0:<captured-sha256>`
         The placeholders share the exact literal string so a single `sed -i 's/<TBD-v1.6.0-cut>/<captured-value>/g'` across both files works — BUT note the markdown doc has TWO occurrences with DIFFERENT replacement values (ImageVersion vs sha256), so substitute each occurrence by file + section explicitly rather than via a global sed.
      4. Commit the changes with message `docs(25): capture v1.6.0-rc.0 reproducibility baseline (ImageVersion=<X>, sha256=<Y>)` — single commit, both files together.
      5. Tag v1.6.0 and push (`git tag -s v1.6.0 -m "v1.6.0"` + `git push --tags`). The first scheduled `reproducible-verify.yml` run on the 1st of the next month will now compare against the real value and go GREEN if reproducibility holds.

    Close with a 1-line note: "After the first green scheduled run, proceed to [Reproducible-builds.org registry submission](#reproducible-buildsorg-registry-submission) below."

    **(c) APPEND a second new H2 section `## Reproducible-builds.org registry submission` per D-14** after the rehearsal section. Use CONTEXT §Specific Ideas D-14 4-step skeleton verbatim:

    Multi-line content covering:
    - 2-line lede: "After `.github/workflows/reproducible-verify.yml` has run green for ≥1 monthly cycle post-v1.6.0, blindjoin is eligible for [reproducible-builds.org](https://reproducible-builds.org) project registry submission. This is a maintainer-driven manual procedure — the registry accepts only human-reviewed PRs."
    - 4-step numbered procedure:
      1. **Verify the green-monthly-cycle prerequisite.** Open the [reproducible-verify workflow Actions tab](../../../actions/workflows/reproducible-verify.yml) and confirm at least one scheduled (NOT workflow_dispatch) run after the v1.6.0 tag exited 0.
      2. **Fork and add the entry.** Fork [github.com/reproducible-builds/reproducible-builds.org](https://github.com/reproducible-builds/reproducible-builds.org). Add an entry under `_data/projects/` per the project's existing YAML schema. Required fields: name (blindjoin), language (Rust), reproducibility-doc URL (link to `docs/REPRODUCIBLE-BUILD.md`), verify-command (copy the Recipe section from `docs/REPRODUCIBLE-BUILD.md`).
      3. **Open the PR + respond to reviewer feedback.** The reproducible-builds.org maintainers will review and may request schema adjustments. Expect ~1-2 round trips before merge.
      4. **After merge: link back from blindjoin.** Edit `docs/REPRODUCIBLE-BUILD.md` §Continuous verification: replace `Registry entry: <added after blindjoin's submission lands...>` with the actual registry URL. Edit `SECURITY.md` §Supply-chain status §Reproducibility: replace the same placeholder with the actual URL. Commit both edits together.

    Close with a 1-line note: "REPRO-04 is closed once the registry PR is merged AND both blindjoin-side cross-links are updated."
  </action>
  <verify>
    <automated>
      # D-13 cleanup: no --draft=false; no flip-out-of-draft prose
      ! grep -q -- '--draft=false' docs/RELEASING.md &&
      ! grep -qi 'flipping the github release out of draft' docs/RELEASING.md &&
      ! grep -qi 'flip the release out of draft' docs/RELEASING.md &&
      ! grep -qi 'flips out of draft' docs/RELEASING.md &&
      # Section header renamed
      grep -q '^## Pre-flight check after CI completes$' docs/RELEASING.md &&
      ! grep -q '^## Pre-flight check before flipping out of draft$' docs/RELEASING.md &&
      # New rehearsal section appended
      grep -q '^## Reproducibility verification rehearsal' docs/RELEASING.md &&
      grep -q 'workflow_dispatch' docs/RELEASING.md &&
      grep -q '<TBD-v1\.6\.0-cut>' docs/RELEASING.md &&
      grep -q 'REPRODUCIBLE-BUILD\.expected-sha256\.txt' docs/RELEASING.md &&
      # New registry submission section appended
      grep -q '^## Reproducible-builds.org registry submission$' docs/RELEASING.md &&
      grep -q 'reproducible-builds\.org' docs/RELEASING.md &&
      grep -q '_data/projects/' docs/RELEASING.md &&
      # Verify cosign verify-blob/verify-attestation lines preserved
      grep -q 'cosign verify-blob' docs/RELEASING.md &&
      grep -q 'cosign verify-attestation' docs/RELEASING.md &&
      # Verify new step 4 prose lands
      grep -q 'Verify the published release' docs/RELEASING.md &&
      # Min line count increase
      [ "$(wc -l < docs/RELEASING.md)" -ge 100 ] &&
      echo "All Task 1 assertions passed"
    </automated>
  </verify>
  <done>
    `docs/RELEASING.md` has zero `--draft=false` literals and zero flip-out-of-draft prose. The Pre-flight section header is renamed. Two new H2 sections (Reproducibility verification rehearsal + Reproducible-builds.org registry submission) are appended at end of file with verbatim D-10 5-step + D-14 4-step procedures. Cosign verify-blob/verify-attestation commands in Pre-flight are preserved.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Insert SECURITY.md ### Reproducibility (v1.6 onward) subsection + strikethrough updates for closed gaps</name>
  <files>SECURITY.md</files>
  <read_first>
    - SECURITY.md lines 95-290 (entire Supply-chain status section — verify all current subsection boundaries and strikethrough patterns)
    - SECURITY.md lines 104-105 (existing strikethrough pattern for Phase 23 + Phase 24 closures — exact format to mirror for the No reproducible-build pipeline gap)
    - SECURITY.md lines 106-110 (the gap bullet to update with strikethrough)
    - SECURITY.md lines 231-232 (insertion boundary for the new subsection — between Release tarball signatures and Base-image digests)
    - SECURITY.md lines 281-283 (v1.6 supply-chain plan third bullet — Reproducible-build instructions; update with strikethrough mirroring L284's Phase 22 shipped pattern)
    - .planning/phases/25-reproducible-build-recipe-scheduled-verifier-registry/25-CONTEXT.md §Specific Ideas (D-19 RECOMMENDED 1-paragraph + 1-recipe subsection content)
    - .planning/phases/25-reproducible-build-recipe-scheduled-verifier-registry/25-RESEARCH.md §Code Examples (D-19 verbatim text in CONTEXT around lines 297-310 — preferred over re-paraphrasing)
    - .planning/phases/25-reproducible-build-recipe-scheduled-verifier-registry/25-PATTERNS.md L441-474 (SECURITY.md MOD pattern + sibling-subsection template + strikethrough rules)
  </read_first>
  <action>
    Apply three modifications to `SECURITY.md`:

    **(a) Update the existing gap bullet at L106-110** (the `- **No reproducible-build pipeline.** ...` bullet) per PATTERNS.md L472 + the sibling pattern at L104-105:

    Replace the entire 5-line bullet (lines 106-110 of current SECURITY.md) with a single-line strikethrough closure pointer:
    `- **~~No reproducible-build pipeline.~~** **Closed in v1.6 Phase 25** — see [Reproducibility (v1.6 onward)](#reproducibility-v16-onward).`

    The replacement collapses the multi-line current bullet (which described the gap) to a single line because the v1.5-era prose is no longer the truth — the gap is closed; the cross-link is the operator's path.

    **(b) INSERT a new H3 subsection `### Reproducibility (v1.6 onward)`** between the end of `### Release tarball signatures + provenance (v1.6 onward)` (currently ending at L230 with the `> **Note: no detached PGP signature path...`) and the start of `### Base-image digests (v1.6 onward)` (currently L232).

    Use CONTEXT §Specific Ideas D-19 RECOMMENDED content + PATTERNS.md L468-471 sibling pattern verbatim. The subsection content:

    ```markdown
    ### Reproducibility (v1.6 onward)

    Every tagged release tarball is reproducible byte-for-byte from source on the
    pinned `ubuntu-24.04` runner image. The full recipe, expected `sha256sum` per
    release, and toolchain pins live in [docs/REPRODUCIBLE-BUILD.md](docs/REPRODUCIBLE-BUILD.md).
    Continuous verification runs monthly via
    [.github/workflows/reproducible-verify.yml](.github/workflows/reproducible-verify.yml);
    a failure opens a `[reproducibility-regression]` issue. blindjoin is registered
    with the reproducible-builds.org project registry: <added after blindjoin's submission lands; see [docs/RELEASING.md](docs/RELEASING.md) §Reproducible-builds.org registry submission>.

    To verify a release yourself, follow the Recipe section of REPRODUCIBLE-BUILD.md.
    Quick reference (single command from a clean checkout of the tag):

    ` ` `bash
    export SOURCE_DATE_EPOCH=$(git log -1 --format=%ct HEAD)
    export RUSTFLAGS="--remap-path-prefix=$(pwd)=/build --remap-path-prefix=$HOME/.cargo=/cargo"
    export CARGO_INCREMENTAL=0
    cargo build --release --locked --bin coordinator --bin client --bin liquidity-bot
    mkdir -p dist
    cp target/release/coordinator target/release/client target/release/liquidity-bot dist/
    tar --sort=name --owner=0 --group=0 --numeric-owner \
        --mtime="@${SOURCE_DATE_EPOCH}" \
        -cf - -C dist . \
      | gzip -n > blindjoin-linux-amd64.tar.gz
    sha256sum blindjoin-linux-amd64.tar.gz
    # Compare against the expected hash in docs/REPRODUCIBLE-BUILD.md §Expected sha256sum.
    ` ` `

    > **Note: Rust reproducibility long tail** — bit-for-bit equality is verified on
    > `ubuntu-24.04` only. Rebuilding on a different distribution or runner image
    > may produce divergent bytes due to system-tool differences. The monthly
    > verifier (linked above) catches `ubuntu-24.04` ImageVersion drift distinctly
    > from real divergence per Phase 25 D-12.
    ```

    The placeholder `<added after blindjoin's submission lands...>` is the registry-URL slot that Plan 25-05's D-14 procedure step 4 substitutes after the registry PR merges.

    CRITICAL forbidden-token discipline: the subsection prose says `ubuntu-24.04` repeatedly; this is the PINNED value (allowed). Do NOT add any `ubuntu-latest` literal in any prose, even in negation ("we pin ubuntu-24.04, NOT ubuntu-latest"). Use "rolling-release alias" or "unpinned runner image" if comparison is needed.

    **(c) Update the v1.6 supply-chain plan bullet at L281-283** (the third bullet — `Reproducible-build instructions for the release archive...`) per PATTERNS.md L473 + mirror of L284's Phase 22 shipped pattern:

    Replace the 3-line bullet (currently spans L281-283 in the original — re-verify lines at execution time) with a single-line shipped-status pointer:
    `- **~~Reproducible-build instructions~~** ✓ Shipped in Phase 25 — see [Reproducibility (v1.6 onward)](#reproducibility-v16-onward).`

    After this change, all four v1.6 supply-chain plan items at L275-284 are either shipped (3/4 with strikethrough) or in progress (1/4: cosign image attestations — but verify status; if Phase 23 closed it, that bullet may also need a strikethrough — re-read the current state and update consistently to avoid inconsistency).
  </action>
  <verify>
    <automated>
      # New subsection exists at the right structural location
      grep -q '^### Reproducibility (v1.6 onward)$' SECURITY.md &&
      grep -q '\[docs/REPRODUCIBLE-BUILD\.md\](docs/REPRODUCIBLE-BUILD\.md)' SECURITY.md &&
      grep -q '\[\.github/workflows/reproducible-verify\.yml\]' SECURITY.md &&
      # Quick-reference bash block contains the literal-recipe contract
      grep -q -- '--sort=name --owner=0 --group=0 --numeric-owner' SECURITY.md &&
      grep -q 'cargo build --release --locked --bin coordinator --bin client --bin liquidity-bot' SECURITY.md &&
      grep -q 'gzip -n > blindjoin-linux-amd64\.tar\.gz' SECURITY.md &&
      # Known-gaps strikethrough for No reproducible-build pipeline
      grep -q '~~No reproducible-build pipeline\.~~' SECURITY.md &&
      grep -q 'Closed in v1.6 Phase 25' SECURITY.md &&
      # v1.6 supply-chain plan strikethrough for Reproducible-build instructions
      grep -q '~~Reproducible-build instructions~~' SECURITY.md &&
      grep -q 'Shipped in Phase 25' SECURITY.md &&
      # Forbidden tokens absence
      ! grep -qE 'ubuntu-latest' SECURITY.md &&
      # The Note blockquote about long tail
      grep -q '> \*\*Note: Rust reproducibility long tail\*\*' SECURITY.md &&
      # Original ### Release tarball signatures + provenance subsection preserved
      grep -q '^### Release tarball signatures + provenance (v1.6 onward)$' SECURITY.md &&
      # Original ### Base-image digests subsection preserved
      grep -q '^### Base-image digests (v1.6 onward)$' SECURITY.md &&
      # Anchor lowercase match (#reproducibility-v16-onward) — markdown standard
      grep -q '#reproducibility-v16-onward' SECURITY.md &&
      echo "All Task 2 assertions passed"
    </automated>
  </verify>
  <done>
    `SECURITY.md` has a new `### Reproducibility (v1.6 onward)` subsection between the Release tarball signatures and Base-image digests subsections. Content includes the 1-paragraph intro + cross-links + quick-reference bash block + Note blockquote. Known-gaps bullet for `No reproducible-build pipeline` is replaced with a strikethrough + closed-status pointer. v1.6 supply-chain plan bullet for `Reproducible-build instructions` is replaced with a strikethrough + shipped-status pointer. No `ubuntu-latest` literal anywhere in SECURITY.md. Cross-links from both inline references and the bullet pointers all resolve to the new subsection anchor.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → SECURITY.md cross-link | The new subsection cross-links to docs/REPRODUCIBLE-BUILD.md (canonical recipe) and reproducible-verify.yml (continuous verification) — drift between SECURITY.md's quick-reference bash block and the canonical Recipe in REPRODUCIBLE-BUILD.md is the threat |
| maintainer → rehearsal procedure | Plan 25-05 documents the v1.6.0-rc.0 rehearsal; if the maintainer skips the rehearsal and tags v1.6.0 with `<TBD-v1.6.0-cut>` placeholder still in source, the first scheduled verifier run goes red |
| docs/RELEASING.md → external reproducible-builds.org registry | The registry submission procedure points to an external service; if reproducible-builds.org changes its schema, the doc goes stale |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-05-01 | Information Disclosure | SECURITY.md quick-reference bash block diverges from the canonical Recipe in docs/REPRODUCIBLE-BUILD.md as the canonical Recipe evolves | mitigate | SECURITY.md cross-links to REPRODUCIBLE-BUILD.md as the canonical source; the bash block is a quick-reference convenience — operators following the SECURITY.md path also see the cross-link. Future-plan-author note: any change to the deterministic env vars or tar/gzip pipeline MUST update both files atomically. |
| T-25-05-02 | Tampering | Maintainer skips the v1.6.0-rc.0 rehearsal and tags v1.6.0 with `<TBD-v1.6.0-cut>` placeholder remaining → first scheduled verifier run goes red on placeholder-vs-real comparison | mitigate | docs/RELEASING.md rehearsal section is the documented procedure; planner-discretion CI gate `! grep -q '<TBD-' docs/REPRODUCIBLE-BUILD.expected-sha256.txt` could be added as a release-gate but is deferred per D-10 (placeholder approach is simpler; the docs procedure is the primary mitigation). |
| T-25-05-03 | Repudiation | Registry submission procedure documents a manual external action — no automated proof it happened | accept | REPRO-04 is conditional on the submission landing; the closure proof is the registry URL substituted into docs/REPRODUCIBLE-BUILD.md §Continuous verification + SECURITY.md §Reproducibility (per D-14 step 4). Future audit can grep for the placeholder being replaced. |
| T-25-05-04 | Information Disclosure | docs/RELEASING.md leaks --draft=false reference after Plan 25-02's YAML cleanup | mitigate | This plan's Task 1 deletes all 4 `--draft=false` occurrences + the flip-out-of-draft prose. Acceptance criteria asserts `! grep -- '--draft=false' docs/RELEASING.md` returns zero matches. |
| T-25-05-05 | Tampering | The atomic substitution in the rehearsal procedure is non-atomic in practice (maintainer updates one file but not the other) → markdown table vs .expected-sha256.txt disagree | mitigate | Both placeholders use identical literal `<TBD-v1.6.0-cut>` string; the rehearsal procedure step 3 enumerates all 3 substitution sites (sha256 occurrence in markdown table + ImageVersion occurrence in markdown table + line in .expected-sha256.txt) explicitly. Plan 25-03 also notes this in its SUMMARY for the maintainer's reference. |
| T-25-05-06 | Spoofing | reproducible-builds.org registry entry references a forked or tampered URL for the recipe | accept | Registry submission is a human-reviewed PR to an external project; their maintainers vet entries. The doc procedure cites the canonical URL pattern (raw github.com path). |
</threat_model>

<verification>
After both tasks complete:

1. `docs/RELEASING.md` has zero `--draft=false` literal references AND zero flip-out-of-draft prose.
2. `docs/RELEASING.md` Pre-flight section header is renamed to `Pre-flight check after CI completes`.
3. `docs/RELEASING.md` has a new H2 section `## Reproducibility verification rehearsal (v1.6.0-rc.0 procedure)` with the 5-step procedure.
4. `docs/RELEASING.md` has a new H2 section `## Reproducible-builds.org registry submission` with the 4-step procedure.
5. `docs/RELEASING.md` is ≥100 lines (post-mod expectation; pre-mod was 66).
6. `SECURITY.md` has a new `### Reproducibility (v1.6 onward)` subsection between the Release tarball signatures and Base-image digests subsections.
7. `SECURITY.md` §Reproducibility contains the 1-paragraph intro + cross-links + quick-reference bash block + Note blockquote per D-19.
8. `SECURITY.md` Known-gaps bullet for `No reproducible-build pipeline` is replaced with a strikethrough + closed-status pointer.
9. `SECURITY.md` v1.6-supply-chain-plan bullet for `Reproducible-build instructions` is replaced with a strikethrough + shipped-status pointer.
10. No `ubuntu-latest` literal anywhere in `SECURITY.md` (forbidden-token absence audit per Plan 22-04).
11. Plan 25-02 + Plan 25-05 together fully close the D-13 cleanup (YAML side in 25-02; docs side here).
</verification>

<success_criteria>
- REPRO-04 requirement procedurally addressed: documented maintainer procedure for reproducible-builds.org submission exists in docs/RELEASING.md (the actual submission is conditional on ≥1 green monthly cycle — Phase 25 ships the procedure, not the executed submission, mirroring Phase 24's PGP pattern)
- D-10 (v1.6.0-rc.0 rehearsal procedure), D-13 (docs-side --draft=false cleanup), D-14 (registry submission procedure), D-19 (SECURITY.md ### Reproducibility subsection) all implemented
- Forbidden-token absence audits hold: no `--draft=false`, no `flip*out of draft` prose in docs/RELEASING.md; no `ubuntu-latest` in SECURITY.md
- SECURITY.md §Supply-chain status now reflects v1.6 closure: 3/4 known-gaps strikethrough + closed-status pointers; 4/4 v1.6 supply-chain plan items strikethrough + shipped-status pointers (assuming Phase 23 cosign image attestations are also marked shipped if not already — re-verify state)
- Plan 25-05 completes the Phase 25 surface; all 4 REPRO requirements have plan coverage; the next milestone-level action is the maintainer-driven v1.6.0-rc.0 rehearsal + tag cut (outside Phase 25's plan scope per D-10/D-14 procedural-only contract)
</success_criteria>

<output>
Create `.planning/phases/25-reproducible-build-recipe-scheduled-verifier-registry/25-05-SUMMARY.md` when done. Include:
- Final line counts of both files (docs/RELEASING.md pre/post; SECURITY.md pre/post)
- Confirmation of D-13 docs-side cleanup completeness (zero --draft=false; zero flip-out-of-draft prose)
- The post-modification line ranges of the two new docs/RELEASING.md sections
- The post-modification line range of the new SECURITY.md ### Reproducibility (v1.6 onward) subsection
- The exact strikethrough lines replacing the No-reproducible-build-pipeline gap bullet AND the v1.6 supply-chain plan bullet
- Note for milestone close: REPRO-04 marked procedurally complete; actual registry submission awaits ≥1 green monthly cycle post-v1.6.0 (outside Phase 25 scope)
- Note for v1.6.0-rc.0 cut: maintainer runs the rehearsal procedure in docs/RELEASING.md, captures ImageVersion + sha256, substitutes the 3 `<TBD-v1.6.0-cut>` placeholders atomically, commits, then tags
</output>
