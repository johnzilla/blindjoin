---
phase: 10-full-round-rs-decision-execution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/mod.rs
  - tests/integration/full_round.rs
autonomous: true
requirements:
  - REPAIR-01

must_haves:
  truths:
    - "tests/integration/mod.rs exports pub async fn fund_regtest and pub struct FundedSetup"
    - "fund_regtest uses get_raw_transaction_verbose (NOT list_unspent) to locate funded outputs — wallet-agnostic vout discovery"
    - "tests/integration/full_round.rs has no file-private fund_regtest or file-private FundedSetup; both consumed via crate::"
    - "6 consumer sites total in full_round.rs now resolve to crate::fund_regtest: 4 explicit `fund_regtest(exe).await` callsites (lines 966, 1090, 1147, 1472) + 2 inline-body collapses (line 193 in full_round_three_clients AND line 577 in blame_non_signer_timeout — both inline bodies that called list_unspent are replaced)"
    - "tests/integration/full_round.rs contains zero `list_unspent` references after this plan (file-level invariant — the descriptor-wallet root cause is gone from this file)"
    - "cargo check --tests compiles cleanly"
    - "All 6 #[ignore] markers in full_round.rs are UNTOUCHED by this plan (count remains 6)"
  artifacts:
    - path: "tests/integration/mod.rs"
      provides: "shared fund_regtest async helper + FundedSetup struct"
      contains: "pub async fn fund_regtest"
    - path: "tests/integration/mod.rs"
      provides: "FundedSetup struct (promoted)"
      contains: "pub struct FundedSetup"
    - path: "tests/integration/full_round.rs"
      provides: "imports + callsites updated; file-private definitions removed; both inline bodies collapsed"
      contains: "use crate::{"
  key_links:
    - from: "tests/integration/full_round.rs"
      to: "tests/integration/mod.rs::fund_regtest"
      via: "crate::fund_regtest"
      pattern: "crate::fund_regtest"
    - from: "tests/integration/mod.rs::fund_regtest"
      to: "corepc_node::Client::get_raw_transaction_verbose"
      via: "spawn_blocking RPC call"
      pattern: "get_raw_transaction_verbose"
---

<objective>
Promote `fund_regtest` and `FundedSetup` from `tests/integration/full_round.rs` (file-private) to `tests/integration/mod.rs` (pub), repairing the underlying root cause: the helper currently uses `list_unspent` to locate UTXOs paid to external addresses, which silently returns nothing on Bitcoin Core v30+ descriptor wallets (mandatory since v30.0). Replace with wallet-agnostic vout discovery via `get_raw_transaction_verbose` (10-RESEARCH.md Pattern 1, Example 4). Update the 6 consumer sites inside `full_round.rs` (4 explicit callsites + 2 inline-body collapses) to `crate::fund_regtest(exe).await`.

This plan does NOT touch any `#[ignore]` marker, does NOT replace any bare sleeps, and does NOT modify CI / ROADMAP / REQUIREMENTS. All that work is owned by Plan 10-02 (which depends on this one).

Purpose: Implements D-04 Plan 10-01 (schema port) and D-05 (typed v30 Client choice) and D-06 (promote helper to mod.rs). This is the load-bearing repair that turns the 6 carve-out tests from "panic in fund_regtest" to "runnable", unblocking Plan 10-02's per-test unmute cycle.

Output:
- `tests/integration/mod.rs` gains a `pub async fn fund_regtest(exe: String) -> (BitcoindGuard, FundedSetup)` and a `pub struct FundedSetup { pub rpc_url: String, pub rpc_user: String, pub rpc_pass: String, pub utxos: [(String, u64); 3] }`
- `tests/integration/full_round.rs` loses its file-private definitions AND both inline bodies (line 193 in `full_round_three_clients` and line 577 in `blame_non_signer_timeout`); all 6 consumer sites resolve to the shared helper
- `cargo check --tests` exits 0
- `grep -c 'list_unspent' tests/integration/full_round.rs` returns 0 (the broken descriptor-wallet pattern is gone file-wide)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-full-round-rs-decision-execution/10-CONTEXT.md
@.planning/phases/10-full-round-rs-decision-execution/10-RESEARCH.md
@.planning/phases/10-full-round-rs-decision-execution/10-PATTERNS.md

# Source files this plan modifies — read in full before editing
@tests/integration/mod.rs
@tests/integration/full_round.rs

# Reference: the Cargo.toml line that justifies the corepc-node typed Client choice
@coordinator/Cargo.toml

<interfaces>
<!-- Key contracts the executor needs. Pulled from PATTERNS.md / RESEARCH.md. -->
<!-- Use these directly — no codebase exploration needed for the helper contract. -->

Existing Phase 9 fixtures already in tests/integration/mod.rs (UNTOUCHED by this plan):

```rust
// tests/integration/mod.rs (Phase 9 — preserve verbatim)
pub struct BitcoindGuard { /* RAII bitcoind ownership */ }
pub struct RpcCreds { pub url: String, pub user: String, pub pass: String }
#[macro_export] macro_rules! require_bitcoind { /* skip-or-panic */ }
pub async fn bootstrap_regtest_bitcoind(exe: String) -> (BitcoindGuard, RpcCreds);
```

New contracts this plan ADDS to tests/integration/mod.rs:

```rust
/// (Phase 10 — D-06 promotion) UTXO funding result for an integration test.
/// Three P2WPKH UTXOs derived from hardcoded test WIFs (NOT wallet-owned), each
/// funded with (denomination + 50_000 sats fee margin) via sendtoaddress + a
/// 1-confirmation block.
#[derive(Clone, Debug)]
pub struct FundedSetup {
    pub rpc_url: String,
    pub rpc_user: String,
    pub rpc_pass: String,
    /// (outpoint "txid:vout", value_sats) for each participant
    pub utxos: [(String, u64); 3],
}

/// (Phase 10 — D-06 promotion) Spin up regtest bitcoind, mine 101 blocks, derive
/// 3 P2WPKH addresses from hardcoded test WIFs, send (denomination + 50_000) sats
/// to each, mine 1 confirmation block, and locate each output's vout via
/// `get_raw_transaction_verbose` (wallet-agnostic — works against v30+ descriptor
/// wallets where listunspent does NOT return UTXOs paid to addresses the wallet
/// does not own; see 10-RESEARCH.md Pitfall 1).
pub async fn fund_regtest(exe: String) -> (BitcoindGuard, FundedSetup);
```

Key corepc-node 0.12 (features = ["30_2"]) RPC types this plan calls:

```rust
// From corepc-node 0.12.0 at feature 30_2 (Example 2 + 4 in 10-RESEARCH.md):
impl corepc_node::Client {
    pub fn send_to_address(&self, address: &Address<NetworkChecked>, amount: Amount) -> Result<SendToAddress>;
    pub fn generate_to_address(&self, nblocks: usize, address: &bitcoin::Address) -> Result<GenerateToAddress>;
    pub fn get_raw_transaction_verbose(&self, txid: Txid) -> Result<GetRawTransactionVerbose>;
}

pub struct GetRawTransactionVerbose {
    #[serde(rename = "vout")] pub outputs: Vec<RawTransactionOutput>,
    /* ...other fields ignored by this plan... */
}

pub struct RawTransactionOutput {
    pub value: f64,                       // BTC (not sats)
    #[serde(rename = "n")] pub index: u64,  // the vout number
    #[serde(rename = "scriptPubKey")] pub script_pubkey: ScriptPubKey,
}
// ScriptPubKey.address: Option<String> on v23+ (re-exported at 30_2);
// if absent, fall back to Address::from_script(&ScriptBuf::from_hex(&hex)?, Network::Regtest).
```

Auth + Client construction inside spawn_blocking (existing pattern from full_round.rs:384-395 — preserve):

```rust
use corepc_node::client::client_sync::Auth;
let auth = Auth::UserPass(rpc_user, rpc_pass); // both Strings, moved in
let client = corepc_node::Client::new_with_auth(&rpc_url, auth).expect("create rpc client");
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Promote FundedSetup + fund_regtest to tests/integration/mod.rs with wallet-agnostic vout discovery</name>
  <files>tests/integration/mod.rs</files>
  <read_first>
    - `tests/integration/mod.rs` (read the FULL file — the sibling helper `bootstrap_regtest_bitcoind` at lines 271-322 is the canonical style template; `RpcCreds` at lines 108-120 is the struct-promotion template; `BitcoindGuard` is the RAII type the new helper returns)
    - `tests/integration/full_round.rs` (read lines 140-146 for current file-private `struct FundedSetup`, and lines 749-849 for current file-private `async fn fund_regtest` — the executor copies the funding logic verbatim except for the listunspent→get_raw_transaction_verbose swap)
    - `.planning/phases/10-full-round-rs-decision-execution/10-RESEARCH.md` §"Pattern 1: Wallet-Agnostic Vout Discovery" and §"Example 4: GetRawTransactionVerbose and RawTransactionOutput field shapes" (the exact code shape to apply at the listunspent site)
    - `.planning/phases/10-full-round-rs-decision-execution/10-RESEARCH.md` §"Pitfall 1: Bitcoin Core v30 descriptor wallets break the listunspent external-address pattern" (the *why* — to include in the doc block)
    - `.planning/phases/10-full-round-rs-decision-execution/10-PATTERNS.md` §"tests/integration/mod.rs — ADD fund_regtest + FundedSetup" (full pattern assignment: doc-block template, async-blocking-bridge shape, struct-promotion conventions, `#[derive(Clone, Debug)]`, function-local `use` declarations, `.expect` not `?` error style)
    - `coordinator/Cargo.toml` lines 61-69 (verify the existing pin `corepc-node = { version = "0.12", features = ["30_2"] }` — no change here, but confirms the RPC types are available)
  </read_first>
  <action>
Add the following two items to `tests/integration/mod.rs`, after the existing `bootstrap_regtest_bitcoind` (lines 271-322 today):

(a) `pub struct FundedSetup` — promote from `tests/integration/full_round.rs:140-146` with all four fields made `pub`:
- `pub rpc_url: String`
- `pub rpc_user: String`
- `pub rpc_pass: String`
- `pub utxos: [(String, u64); 3]` (the existing array shape; do NOT change to Vec)
- Apply `#[derive(Clone, Debug)]` (matches sibling `RpcCreds` at mod.rs:108).
- Doc block: 1-paragraph contract statement matching `RpcCreds`'s doc style — "Canonical handoff struct between `fund_regtest` and consuming tests. Three P2WPKH UTXOs derived from hardcoded test WIFs (not wallet-owned), each funded with (denomination + 50_000 sats fee margin)."

(b) `pub async fn fund_regtest(exe: String) -> (BitcoindGuard, FundedSetup)` — promote from `tests/integration/full_round.rs:749-849` with:
- Visibility `pub` (mod-level entry point for the integration test crate).
- Same outer signature as the file-private version (per D-06 contract and Assumption A8 in 10-RESEARCH.md): single `exe: String` arg, returns `(BitcoindGuard, FundedSetup)`.
- Body composes `bootstrap_regtest_bitcoind(exe).await` first (NOT a second self-spawning bitcoind), then runs the funding work inside ONE `tokio::task::spawn_blocking(move || { ... }).await.expect(...)` block — matches sibling pattern.
- **Cleanup opportunity (10-RESEARCH.md Anti-Pattern + 10-PATTERNS.md "BitcoindGuard cleanup"):** Drop the `Arc<BitcoindGuard>` + `Arc::try_unwrap` plumbing from the file-private version. Move the bare `BitcoindGuard` into the spawn_blocking closure, return it from the closure with the `FundedSetup`. This eliminates ~6 lines of plumbing.
- **The repair (10-RESEARCH.md Pattern 1 + Example 4):** at the spot the old code calls `node.client.list_unspent()` and matches by `entry.address == recipient_addr_str` (around `full_round.rs:813-825`), REPLACE with: for each of the 3 funding txids, call `node.client.get_raw_transaction_verbose(Txid::from_str(&funding_txid_str)?)`, iterate `tx.outputs`, find the `RawTransactionOutput` whose `script_pubkey.address.as_deref() == Some(&recipient_addr.to_string())`, and capture `(format!("{}:{}", funding_txid_str, out.index), (out.value * 100_000_000.0).round() as u64)`. **Fallback per Assumption A2 in 10-RESEARCH.md:** if `script_pubkey.address` is absent at the v17→v30 re-export, use `bitcoin::Address::from_script(&ScriptBuf::from_hex(&out.script_pubkey.hex).expect("hex"), Network::Regtest).expect("addr from script")` to compare. The executor picks whichever compiles first; both are wallet-agnostic and correct.
- **Imports:** function-local `use` declarations inside the spawn_blocking closure (mirror sibling style at mod.rs:272-279). Required: `bitcoin::{Address, Amount, Network, PrivateKey, Txid, secp256k1::Secp256k1}`, `corepc_node::Conf` if needed indirectly, `std::str::FromStr`. Existing `use` lines at the top of mod.rs are not touched.
- **Doc block:** include (1) the "single locus" justification (D-06 promotion mirrors the sibling helper's pattern), (2) the caller contract ("Hold the returned BitcoindGuard for the test's full duration"), (3) a **schema note** quoting the descriptor-wallet gotcha — 1-2 sentences from 10-RESEARCH.md Pitfall 1 explaining *why* `get_raw_transaction_verbose` instead of `list_unspent`, and (4) a canonical caller skeleton in a ```ignore code block. Reference `RawTransactionOutput` / `GetRawTransactionVerbose` by name so future maintainers can grep.
- **Error handling:** `.expect("message")` on every RPC call, matching the sibling helper. NOT `?` (tests want loud panics; loud is the contract). Each `.expect` message names the specific failure (e.g., `"send_to_address failed"`, `"get_raw_transaction_verbose"`, `"funding tx <id> has no output to <addr>"`).
- DO NOT modify `bootstrap_regtest_bitcoind`, `BitcoindGuard`, `RpcCreds`, or `require_bitcoind!()` — they remain Phase 9 verbatim (10-RESEARCH.md Pitfall 3 protects against silently re-introducing the pipe-buffering hang).

After this change, `cargo check --tests` must compile. Plan 10-02 will consume these new exports.
  </action>
  <verify>
    <automated>cd /Users/john/Desktop/vault/projects/github.com/blindjoin && set -o pipefail; cargo check --tests 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/mod.rs` contains the literal string `pub async fn fund_regtest(` (verify: `grep -c 'pub async fn fund_regtest(' tests/integration/mod.rs` returns `1`)
    - `tests/integration/mod.rs` contains the literal string `pub struct FundedSetup` (verify: `grep -c 'pub struct FundedSetup' tests/integration/mod.rs` returns `1`)
    - `tests/integration/mod.rs` contains the literal string `get_raw_transaction_verbose` (verify: `grep -c 'get_raw_transaction_verbose' tests/integration/mod.rs` returns at least `1`)
    - `tests/integration/mod.rs` contains the literal string `#[derive(Clone, Debug)]` directly above `pub struct FundedSetup` (verify by Read + visual confirmation OR `grep -B1 'pub struct FundedSetup' tests/integration/mod.rs | grep -c 'derive(Clone, Debug)'` returns `1`)
    - `tests/integration/mod.rs::bootstrap_regtest_bitcoind` is UNCHANGED (verify: the function body, signature, and `view_stdout = false` line are byte-identical to pre-plan; the executor must NOT touch these per 10-RESEARCH.md Pitfall 3)
    - `cargo check --tests` exits 0
    - The new `fund_regtest` body does NOT call `list_unspent` (verify: `grep -c 'list_unspent' tests/integration/mod.rs` returns `0` for the new helper region; existence elsewhere in tree is fine but the new helper body must not use it)
  </acceptance_criteria>
  <done>
    `tests/integration/mod.rs` exports `pub struct FundedSetup` and `pub async fn fund_regtest`. The helper uses `get_raw_transaction_verbose` for vout discovery (NOT `list_unspent`). `cargo check --tests` compiles cleanly. `bootstrap_regtest_bitcoind` / `BitcoindGuard` / `RpcCreds` / `require_bitcoind!()` are byte-identical to their pre-plan state.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire full_round.rs to crate::fund_regtest and crate::FundedSetup; delete file-private definitions AND collapse BOTH inline bodies (lines 193 and 577)</name>
  <files>tests/integration/full_round.rs</files>
  <read_first>
    - `tests/integration/full_round.rs` (read FULL file — required to verify the 4 explicit `fund_regtest(exe).await` callsites at lines 966, 1090, 1147, 1472, the FIRST inline body at 193-285 in `full_round_three_clients`, the SECOND inline body at 577-635 in `blame_non_signer_timeout` (this is the one that calls `node.client.list_unspent()` at line 613 and the `.find(|u| u.txid == *txid_str && u.address == *addr_str)` pattern at line 620 — the exact broken descriptor-wallet pattern from RESEARCH.md Pitfall 1), plus the file-private definitions at 140-146 and 749-849 the executor must delete)
    - `tests/integration/mod.rs` (the version produced by Task 1 — needed to import `fund_regtest` and `FundedSetup`)
    - `.planning/phases/10-full-round-rs-decision-execution/10-PATTERNS.md` §"tests/integration/full_round.rs — repair callsites + unmute + sleep→poll" → "Import-update pattern" + "Local fund_regtest deletion" subsections (the exact import line change and the 4 explicit callsite locations + 2 inline-body collapse locations)
    - `.planning/phases/10-full-round-rs-decision-execution/10-RESEARCH.md` Assumption A8 (the promoted signature preserves all consumer invocations), Assumption A6 (`cargo check --tests` already verified passes), and Pitfall 1 (the `list_unspent` line at full_round.rs:613 inside `blame_non_signer_timeout` is the SAME failure mode — `blame_non_signer_timeout` is one of the 6 ignored tests REPAIR-01 must repair, and leaving its inline body intact means it would STILL panic with "Could not find UTXO" after Plan 10-01 lands)
  </read_first>
  <action>
Three edits to `tests/integration/full_round.rs`:

(a) **Import line update** (current line 24):
- Before: `use crate::{bootstrap_regtest_bitcoind, require_bitcoind, BitcoindGuard, RpcCreds};`
- After: `use crate::{bootstrap_regtest_bitcoind, fund_regtest, require_bitcoind, BitcoindGuard, FundedSetup, RpcCreds};` (alphabetical-within-group; the existing line orders by category — keep that ordering)

(b) **Delete file-private definitions:**
- Delete the `struct FundedSetup { ... }` declaration at current lines 140-146 (replaced by `crate::FundedSetup`).
- Delete the `async fn fund_regtest(exe: String) -> (BitcoindGuard, FundedSetup) { ... }` definition at current lines 749-849 (replaced by `crate::fund_regtest`).
- Both deletions are pure removal — no replacement code at those line ranges.

(c) **Update 6 consumer sites total to use the shared helper** — 4 explicit callsites + 2 inline-body collapses:

EXPLICIT CALLSITES (4 sites — lines 966, 1090, 1147, 1472):
- Each currently calls `fund_regtest(exe).await`. Since the import now brings `fund_regtest` into scope from `crate`, the bare call `fund_regtest(exe).await` resolves identically to `crate::fund_regtest(exe).await` after the import update. Per 10-PATTERNS.md "Local fund_regtest deletion" the explicit `crate::` form is preferred for grep-ability — apply that at all 4 sites.

INLINE-BODY COLLAPSES (2 sites — lines 193 AND 577):
- **Inline body 1 (line 193, inside `full_round_three_clients`):** the in-test funding setup that the file-private `fund_regtest` was originally extracted to replace. Per 10-PATTERNS.md "Local fund_regtest deletion" and 10-RESEARCH.md Assumption A8, collapse to `let (bitcoind_guard, setup) = crate::fund_regtest(exe).await;`. Verify the inline body's outputs (rpc_url/user/pass + utxos) match the FundedSetup contract; if they match, replace the block. If a divergence exists (e.g., a different mining count or different WIF set), surface as a checkpoint — DO NOT silently change test semantics.
- **Inline body 2 (line 577, inside `blame_non_signer_timeout`):** A SECOND inline body that performs the same fund-3-UTXOs flow as inline body 1, INCLUDING the broken `node.client.list_unspent()` call at line 613 and the `.find(|u| u.txid == *txid_str && u.address == *addr_str)` pattern at line 620. This is the exact root-cause failure mode RESEARCH.md Pitfall 1 documents — `list_unspent` on a v30 descriptor wallet does NOT return UTXOs paid to externally-derived addresses, so this test panics with `"Could not find UTXO txid=... addr=..."` at line 621. `blame_non_signer_timeout` is ONE OF THE 6 IGNORED TESTS REPAIR-01 MUST REPAIR; leaving this inline body intact would mean Plan 10-02 Task 3's per-test unmute cycle still fails on this test even after Plan 10-01 lands. Collapse this entire block (lines 577-635, the `tokio::task::spawn_blocking(move || { ... }).await.expect(...)` plus the `let setup: FundedSetup =` binding and the preceding `let guard_for_setup = Arc::clone(&bitcoind_guard);` + `let RpcCreds { ... } = creds;` plumbing) to: `let (bitcoind_guard, setup) = crate::fund_regtest(exe).await;`. Verify the same contract-match check before replacement (rpc_url/user/pass + utxos shape). Note this also removes the `Arc<BitcoindGuard>` plumbing at that callsite — the new helper returns a bare `BitcoindGuard` per Task 1's cleanup.

DO NOT touch any `#[ignore = "TODO(Phase-10)..."]` marker. DO NOT touch any `tokio::time::sleep(...)` line. Both are owned by Plan 10-02.

Compile verification: after this task `cargo check --tests` must compile, AND the file MUST contain zero `list_unspent` references (the file-level invariant confirming the root-cause fix landed everywhere).
  </action>
  <verify>
    <automated>cd /Users/john/Desktop/vault/projects/github.com/blindjoin && set -o pipefail; cargo check --tests 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^struct FundedSetup' tests/integration/full_round.rs` returns `0` (file-private struct deleted)
    - `grep -c '^async fn fund_regtest' tests/integration/full_round.rs` returns `0` (file-private function deleted)
    - `grep -c '^use crate::' tests/integration/full_round.rs` returns `1` and that line contains both `fund_regtest` and `FundedSetup` (verify: `grep '^use crate::' tests/integration/full_round.rs | grep -c 'fund_regtest'` returns `1` AND `grep '^use crate::' tests/integration/full_round.rs | grep -c 'FundedSetup'` returns `1`)
    - `grep -c 'crate::fund_regtest' tests/integration/full_round.rs` returns at least `6` (4 explicit callsites at lines 966/1090/1147/1472 + 2 inline-body collapses at lines 193 and 577)
    - **`grep -c 'list_unspent' tests/integration/full_round.rs` returns `0`** (file-level invariant — confirms the root-cause fix landed throughout this file, including the inline body inside `blame_non_signer_timeout` at line 577; this is the load-bearing acceptance for REPAIR-01 being unblockable by Plan 10-02)
    - `grep -c '^#\[ignore' tests/integration/full_round.rs` returns `6` UNCHANGED (this plan does NOT remove ignore markers; that is Plan 10-02's job)
    - `grep -cE 'tokio::time::sleep\(Duration::from_secs\(' tests/integration/full_round.rs` returns `4` UNCHANGED (this plan does NOT replace bare sleeps; that is Plan 10-02's job)
    - `cargo check --tests` exits 0
  </acceptance_criteria>
  <done>
    `tests/integration/full_round.rs` has no file-private `fund_regtest` or `FundedSetup`. The single top-level `use crate::{...}` line imports both. All 6 consumer sites (4 explicit callsites + 2 inline-body collapses at lines 193 and 577) resolve to the shared helper. **Zero `list_unspent` references remain in the file** — the descriptor-wallet root cause is gone from `full_round.rs` everywhere, including the previously-overlooked inline body inside `blame_non_signer_timeout`. The 6 `#[ignore]` markers and 4 bare-sleep sites are byte-identical to their pre-plan state. `cargo check --tests` compiles.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture → bitcoind RPC (regtest) | Test code sends regtest RPC; no untrusted input crosses; clearnet local only |
| Promoted shared helper → consuming test modules | Internal Rust module boundary; `pub` items cross from `mod.rs` to `full_round.rs` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Information disclosure | `fund_regtest` RPC creds in panic messages | accept | Regtest cookie creds are ephemeral per-`TempDir`; no real keys; printing in panic diagnostics is acceptable per test-fixture norms (Phase 9 precedent) |
| T-10-01-02 | Tampering | Promoted helper introducing test cross-talk | mitigate | `BitcoindGuard` RAII pattern (Phase 9) guarantees per-test daemon teardown; helper composes existing guard rather than introducing shared state |
| T-10-01-03 | Denial of service | New helper panics blocking test runner | accept | `.expect` panics are bounded by `tokio-rt-worker` thread; cargo test harness catches per-test; Phase 9 fixed pipe-buffer leak so this no longer cascades |
| T-10-01-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new package installs in this plan — `coordinator/Cargo.toml:65` `corepc-node = { version = "0.12", features = ["30_2"] }` already in tree; no slopcheck trigger |

Expected verdict: PASS with no high-severity threats. Plan 10-01 modifies test infrastructure only; production attack surface unchanged.
</threat_model>

<verification>
- `cargo check --tests` exits 0
- `grep -c 'pub async fn fund_regtest(' tests/integration/mod.rs` returns `1`
- `grep -c 'pub struct FundedSetup' tests/integration/mod.rs` returns `1`
- `grep -c '^struct FundedSetup' tests/integration/full_round.rs` returns `0`
- `grep -c '^async fn fund_regtest' tests/integration/full_round.rs` returns `0`
- `grep -c 'crate::fund_regtest' tests/integration/full_round.rs` returns at least `6` (4 explicit + 2 inline-body collapses)
- `grep -c 'list_unspent' tests/integration/full_round.rs` returns `0` (file-level invariant)
- `grep -c '^#\[ignore' tests/integration/full_round.rs` returns `6` (UNCHANGED — this plan does not touch markers)
- `grep -cE 'tokio::time::sleep\(Duration::from_secs\(' tests/integration/full_round.rs` returns `4` (UNCHANGED — this plan does not touch sleeps)
</verification>

<success_criteria>
- Shared `fund_regtest` and `FundedSetup` live in `tests/integration/mod.rs` with `pub` visibility
- The helper uses wallet-agnostic vout discovery via `get_raw_transaction_verbose` (NOT `list_unspent`)
- `tests/integration/full_round.rs` consumes the shared helper at all 6 consumer sites (4 explicit callsites + 2 inline-body collapses at lines 193 and 577); file-private definitions removed
- Zero `list_unspent` references remain in `tests/integration/full_round.rs` (file-level invariant)
- Compilation passes (`cargo check --tests` exits 0)
- 6 `#[ignore]` markers and 4 bare-sleep sites in `full_round.rs` are untouched (Plan 10-02 owns them)
- `bootstrap_regtest_bitcoind` / `BitcoindGuard` / `RpcCreds` / `require_bitcoind!()` are byte-identical to pre-plan state (Phase 9 invariants preserved)
</success_criteria>

<output>
Create `.planning/phases/10-full-round-rs-decision-execution/10-01-SUMMARY.md` when done. Record:
- Final signature chosen for `fund_regtest` (exact line)
- Which fallback was used for vout-discovery's address compare (the `.address` field path vs the `Address::from_script` hex path)
- Whether the inline body at full_round.rs:193-285 (in `full_round_three_clients`) was collapsed (expected: yes)
- Whether the inline body at full_round.rs:577-635 (in `blame_non_signer_timeout`) was collapsed (expected: yes — this is the previously-overlooked second body that called `list_unspent` at line 613)
- Whether the `Arc<BitcoindGuard>` cleanup was applied at BOTH callsites
- Final value of `grep -c 'list_unspent' tests/integration/full_round.rs` (expected: 0)
- Final value of `grep -c 'crate::fund_regtest' tests/integration/full_round.rs` (expected: at least 6)
- Output of the final `cargo check --tests` run
</output>
