/target/

# Local environment overrides — contains bot signing keys (BOT_WIF); never commit
.env

# Temporary sqlite3 symlink dir for builds on systems without sqlite-devel
.sqlite-lib/

# BdkClientWallet::generate writes mnemonic + descriptors here (mode 0600);
# emitted into cwd whenever generate() runs in tests / smoke runs. Master key
# material — NEVER commit. Phase 17 17-02 added test paths that exercise this.
descriptors.txt
**/descriptors.txt

# Session progress scratchpad (see checkpoint skill) — local-only, not shared repo state.
CHECKPOINT.md
